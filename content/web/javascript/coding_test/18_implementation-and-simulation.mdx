---
title: '18. 구현과 시뮬레이션'
description: '상태·규칙·처리 순서를 코드로 정확하게 옮기고 경계 조건을 관리합니다.'
---

<Callout type="info">
  **학습 목표:** 복잡한 규칙을 작은 상태 전이로 나누고 예시를 단계별로 추적합니다. 모든 예시는 주어진 매개변수를 처리해 정답을 반환하는 `solution` 함수로 완성합니다.
</Callout>

## 문제를 푸는 기준

상태 목록과 명령 하나의 처리 순서를 먼저 적습니다. 후보 상태를 만든 뒤 유효할 때만 현재 상태로 확정합니다.

## 핵심 개념

| 개념 | 코딩테스트에서의 역할 |
|---|---|
| 상태 | 위치, 방향, 점수처럼 다음 단계에 필요한 값입니다. |
| 전이 | 명령 하나가 상태를 어떻게 바꾸는지 정의합니다. |
| 경계 검사 | 범위와 금지 조건을 상태 변경 전에 확인합니다. |

## 대표 문제

### 장애물 격자 로봇

문자 격자 board, 시작 좌표 start, UDLR 명령 commands가 주어집니다. 범위 밖이나 # 칸 이동을 무시한 최종 좌표를 반환하세요.

**함수 시그니처:** `solution(board, start, commands)`

### 제한사항

- 행과 열은 각각 1 이상 500 이하입니다.
- start는 이동 가능한 칸입니다.

### 입출력 예

| 매개변수 | 반환값 |
|---|---|
| `[...., .##., ....], [0,0], RRDD` | `[0,1]` |

### 풀이 설계

1. 명령별 행·열 변화량을 찾습니다.
2. 후보 좌표를 계산합니다.
3. 범위 안이고 장애물이 아닐 때만 좌표를 갱신합니다.

### JavaScript 풀이

```js
function solution(board, start, commands) {
  const move = { U: [-1, 0], D: [1, 0], L: [0, -1], R: [0, 1] };
  let [row, col] = start;
  for (const command of commands) {
    const [dr, dc] = move[command];
    const nr = row + dr;
    const nc = col + dc;
    const inRange = nr >= 0 && nr < board.length && nc >= 0 && nc < board[0].length;
    if (!inRange || board[nr][nc] === '#') continue;
    row = nr;
    col = nc;
  }
  return [row, col];
}
```

### 복잡도

- 시간복잡도: `O(m)`
- 공간복잡도: `O(1)`
- 핵심 패턴: 후보 상태를 만든 뒤 유효할 때만 확정
- 경계 사례: 무효 이동은 좌표를 바꾸지 않고 다음 명령 처리

## 연습 문제

### 1. 행렬 회전

정사각 행렬을 시계 방향으로 회전해 반환하세요.

<details>
<summary>힌트</summary>

원래 행·열의 새 위치

</details>

### 2. 달력 계산

특정 날짜의 요일을 반환하세요.

<details>
<summary>힌트</summary>

이전 달 일수 누적

</details>

### 3. 블록 제거

열에서 블록을 꺼내 같은 값이 만나면 제거하세요.

<details>
<summary>힌트</summary>

열 상태와 스택

</details>

## 자주 하는 실수

- 상태를 먼저 바꾸고 검증하는 실수
- 행과 열 변화량을 뒤집는 실수
- 규칙 우선순위를 바꾸는 실수
- 직사각형을 정사각형으로 가정하는 실수

## 핵심 정리

- 함수 시그니처와 반환 자료형을 먼저 확정합니다.
- 제한사항으로 가능한 시간복잡도를 판단합니다.
- 예시와 경계 사례를 손으로 추적한 뒤 구현합니다.
- 완성한 `solution` 함수가 입력을 불필요하게 바꾸지 않는지도 확인합니다.

## 확인 문제

<Quiz
  questionNumber={1}
  question="경계 밖 이동을 처리하는 안전한 순서는?"
  options={['현재 좌표부터 변경', '후보 좌표를 만든 뒤 유효하면 변경', '명령을 정렬', '마지막 명령만 처리']}
  correctAnswer={2}
  explanation="후보 상태를 검증한 뒤 확정하면 무효 명령에서 원래 상태를 보존합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={2}
  question="대표 문제의 시간복잡도로 알맞은 것은?"
  options={['O(1)', 'O(m)', 'O(n⁴)', 'O(2ⁿ)']}
  correctAnswer={2}
  explanation="대표 풀이는 O(m) 시간에 동작합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={3}
  question="장애물 격자 로봇에서 사용한 핵심 패턴은?"
  options={['후보 상태를 만든 뒤 유효할 때만 확정', '모든 순열을 항상 생성', '첫 원소를 바로 반환', '문자열로만 변환']}
  correctAnswer={1}
  explanation="후보 상태를 만든 뒤 유효할 때만 확정이 문제 조건을 직접 해결합니다."
  optionExplanations={['정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={4}
  question="대표 문제에서 반드시 확인할 경계 사례는?"
  options={['배경색', '변수 이름 길이', '주석 개수', '무효 이동은 좌표를 바꾸지 않고 다음 명령 처리']}
  correctAnswer={4}
  explanation="무효 이동은 좌표를 바꾸지 않고 다음 명령 처리 조건을 별도로 점검해야 합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.']}
/>

<Quiz
  questionNumber={5}
  question="이 과정의 정답 코드가 지켜야 할 계약은?"
  options={['직접 데이터를 읽는다', 'solution 함수에서 요구된 값을 return한다', '결과를 화면에만 출력한다', '전역 변수에 정답을 저장한다']}
  correctAnswer={2}
  explanation="채점기는 solution 함수의 반환값을 검사하므로 요구된 자료형을 return해야 합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

## 참고 자료

- [프로그래머스 코딩테스트 고득점 Kit](https://school.programmers.co.kr/learn/challenges?tab=algorithm_practice_kit)
- [MDN Array](https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Global_Objects/Array)
- [MIT 6.006 알고리즘 설명·정당성·복잡도 지침](https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/pages/syllabus/)
