---
title: '20. 중급 모의고사'
description: '투 포인터, 이분 탐색, BFS, 그리디를 조합한 네 문제를 풉니다.'
---

<Callout type="warning">
  이 문서의 네 문제는 학습용으로 새로 구성한 문제입니다. 먼저 `solution` 함수만 직접 완성한 뒤 풀이를 확인하세요.
</Callout>

## 응시 방법

- 각 문제의 매개변수와 반환값부터 확인합니다.
- 제한사항을 근거로 목표 복잡도를 정합니다.
- 예시와 직접 만든 경계 사례를 손으로 검산합니다.
- 코드는 정답 값을 `return`하는 `solution` 함수로 작성합니다.

## 문제 1. 2인용 셔틀

승객 몸무게 배열 weights와 셔틀 한 대의 제한 무게 limit가 주어집니다. 셔틀에는 최대 두 명이 탈 수 있을 때 필요한 최소 셔틀 수를 반환하세요.

**함수 시그니처:** `solution(weights, limit)`

### 제한사항

- weights의 길이는 1 이상 200,000 이하입니다.
- 각 몸무게는 limit 이하입니다.

### 입출력 예

| 매개변수 | 반환값 |
|---|---|
| `[70, 50, 80, 50], 100` | `3` |

<details>
<summary>풀이와 코드 보기</summary>

**핵심 패턴:** 가장 무거운 사람을 기준으로 가장 가벼운 사람과 짝짓기

```js
function solution(weights, limit) {
  const sorted = [...weights].sort((a, b) => a - b);
  let left = 0;
  let right = sorted.length - 1;
  let count = 0;
  while (left <= right) {
    if (left < right && sorted[left] + sorted[right] <= limit) left += 1;
    right -= 1;
    count += 1;
  }
  return count;
}
```

- 시간복잡도: `O(n log n)`
- 공간복잡도: `O(n)`
- 경계 사례: 한 명만 남아도 셔틀 한 대 필요

</details>

## 문제 2. 최소 처리 속도

작업량 배열 jobs와 제한 시간 h가 주어집니다. 한 시간에 speed만큼 한 작업을 처리하고 다음 작업으로 넘어갈 때, h시간 안에 끝내는 최소 speed를 반환하세요.

**함수 시그니처:** `solution(jobs, h)`

### 제한사항

- jobs의 길이는 1 이상 100,000 이하입니다.
- h는 jobs의 길이 이상입니다.

### 입출력 예

| 매개변수 | 반환값 |
|---|---|
| `[3, 6, 7, 11], 8` | `4` |

<details>
<summary>풀이와 코드 보기</summary>

**핵심 패턴:** 속도가 충분한지 판정하며 최솟값 이분 탐색

```js
function solution(jobs, h) {
  let left = 1;
  let right = jobs.reduce((max, job) => Math.max(max, job), 0);
  while (left < right) {
    const speed = Math.floor((left + right) / 2);
    const hours = jobs.reduce((sum, job) => sum + Math.ceil(job / speed), 0);
    if (hours <= h) right = speed;
    else left = speed + 1;
  }
  return left;
}
```

- 시간복잡도: `O(n log M)`
- 공간복잡도: `O(1)`
- 경계 사례: 속도 1로 가능하면 1 반환

</details>

## 문제 3. 격자 최단 이동

0은 이동 가능, 1은 벽인 직사각 격자 grid가 주어집니다. 왼쪽 위에서 오른쪽 아래까지 상하좌우 최단 이동 횟수를 반환하고 도달할 수 없으면 -1을 반환하세요.

**함수 시그니처:** `solution(grid)`

### 제한사항

- 행과 열은 각각 1 이상 500 이하입니다.
- 시작 칸과 도착 칸도 벽일 수 있습니다.

### 입출력 예

| 매개변수 | 반환값 |
|---|---|
| `[[0,0,1],[1,0,0],[1,1,0]]` | `4` |

<details>
<summary>풀이와 코드 보기</summary>

**핵심 패턴:** 간선 비용이 같은 격자의 BFS

```js
function solution(grid) {
  const rows = grid.length;
  const cols = grid[0].length;
  if (grid[0][0] === 1 || grid[rows - 1][cols - 1] === 1) return -1;
  const queue = [[0, 0, 0]];
  const visited = Array.from({ length: rows }, () => Array(cols).fill(false));
  visited[0][0] = true;
  const directions = [[1, 0], [-1, 0], [0, 1], [0, -1]];
  let head = 0;
  while (head < queue.length) {
    const [row, col, distance] = queue[head++];
    if (row === rows - 1 && col === cols - 1) return distance;
    for (const [dr, dc] of directions) {
      const nr = row + dr;
      const nc = col + dc;
      if (nr < 0 || nr >= rows || nc < 0 || nc >= cols) continue;
      if (grid[nr][nc] === 1 || visited[nr][nc]) continue;
      visited[nr][nc] = true;
      queue.push([nr, nc, distance + 1]);
    }
  }
  return -1;
}
```

- 시간복잡도: `O(RC)`
- 공간복잡도: `O(RC)`
- 경계 사례: 시작 또는 도착이 벽이면 즉시 -1

</details>

## 문제 4. 최대 상담 수

상담 시간 구간 배열 intervals가 주어집니다. 한 상담이 끝난 시각에 다음 상담을 시작할 수 있을 때 겹치지 않게 선택할 수 있는 최대 상담 수를 반환하세요.

**함수 시그니처:** `solution(intervals)`

### 제한사항

- intervals의 길이는 1 이상 200,000 이하입니다.
- 각 구간은 [시작, 종료]이며 시작은 종료보다 작습니다.

### 입출력 예

| 매개변수 | 반환값 |
|---|---|
| `[[1,4],[2,3],[3,5],[5,7]]` | `3` |

<details>
<summary>풀이와 코드 보기</summary>

**핵심 패턴:** 종료 시각이 빠른 구간부터 선택

```js
function solution(intervals) {
  const sorted = [...intervals].sort((a, b) => a[1] - b[1] || a[0] - b[0]);
  let end = -Infinity;
  let count = 0;
  for (const [start, finish] of sorted) {
    if (start < end) continue;
    end = finish;
    count += 1;
  }
  return count;
}
```

- 시간복잡도: `O(n log n)`
- 공간복잡도: `O(n)`
- 경계 사례: 종료와 시작이 같은 두 상담은 함께 선택 가능

</details>

## 중급 모의고사 점검표

- 문제를 읽자마자 자료구조를 정하지 않고 제한사항부터 확인했는가
- 풀이의 정당성을 한두 문장으로 설명할 수 있는가
- 빈 결과, 한 원소, 중복, 도달 불가 같은 경계를 확인했는가
- 시간복잡도와 공간복잡도를 직접 계산했는가

## 확인 문제

<Quiz
  questionNumber={1}
  question="2인용 셔틀의 핵심 풀이 패턴은?"
  options={['가장 무거운 사람을 기준으로 가장 가벼운 사람과 짝짓기', '무조건 모든 순열 생성', '첫 값만 반환', '문자열 정렬만 수행']}
  correctAnswer={1}
  explanation="가장 무거운 사람을 기준으로 가장 가벼운 사람과 짝짓기이 제한사항을 만족하는 핵심입니다."
  optionExplanations={['정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={2}
  question="2인용 셔틀 풀이의 시간복잡도는?"
  options={['O(1)', 'O(n log n)', 'O(n⁴)', '항상 O(2ⁿ)']}
  correctAnswer={2}
  explanation="제시한 풀이는 O(n log n) 시간에 동작합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={3}
  question="2인용 셔틀에서 확인할 경계 사례는?"
  options={['함수 이름의 길이', '주석 위치', '들여쓰기 폭', '한 명만 남아도 셔틀 한 대 필요']}
  correctAnswer={4}
  explanation="한 명만 남아도 셔틀 한 대 필요 상황을 별도로 확인해야 합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.']}
/>

<Quiz
  questionNumber={4}
  question="최소 처리 속도의 핵심 풀이 패턴은?"
  options={['속도가 충분한지 판정하며 최솟값 이분 탐색', '무조건 모든 순열 생성', '첫 값만 반환', '문자열 정렬만 수행']}
  correctAnswer={1}
  explanation="속도가 충분한지 판정하며 최솟값 이분 탐색이 제한사항을 만족하는 핵심입니다."
  optionExplanations={['정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={5}
  question="최소 처리 속도 풀이의 시간복잡도는?"
  options={['O(1)', 'O(n log M)', 'O(n⁴)', '항상 O(2ⁿ)']}
  correctAnswer={2}
  explanation="제시한 풀이는 O(n log M) 시간에 동작합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={6}
  question="최소 처리 속도에서 확인할 경계 사례는?"
  options={['함수 이름의 길이', '주석 위치', '들여쓰기 폭', '속도 1로 가능하면 1 반환']}
  correctAnswer={4}
  explanation="속도 1로 가능하면 1 반환 상황을 별도로 확인해야 합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.']}
/>

<Quiz
  questionNumber={7}
  question="격자 최단 이동의 핵심 풀이 패턴은?"
  options={['간선 비용이 같은 격자의 BFS', '무조건 모든 순열 생성', '첫 값만 반환', '문자열 정렬만 수행']}
  correctAnswer={1}
  explanation="간선 비용이 같은 격자의 BFS이 제한사항을 만족하는 핵심입니다."
  optionExplanations={['정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={8}
  question="격자 최단 이동 풀이의 시간복잡도는?"
  options={['O(1)', 'O(RC)', 'O(n⁴)', '항상 O(2ⁿ)']}
  correctAnswer={2}
  explanation="제시한 풀이는 O(RC) 시간에 동작합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={9}
  question="격자 최단 이동에서 확인할 경계 사례는?"
  options={['함수 이름의 길이', '주석 위치', '들여쓰기 폭', '시작 또는 도착이 벽이면 즉시 -1']}
  correctAnswer={4}
  explanation="시작 또는 도착이 벽이면 즉시 -1 상황을 별도로 확인해야 합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.']}
/>

<Quiz
  questionNumber={10}
  question="최대 상담 수의 핵심 풀이 패턴은?"
  options={['종료 시각이 빠른 구간부터 선택', '무조건 모든 순열 생성', '첫 값만 반환', '문자열 정렬만 수행']}
  correctAnswer={1}
  explanation="종료 시각이 빠른 구간부터 선택이 제한사항을 만족하는 핵심입니다."
  optionExplanations={['정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={11}
  question="최대 상담 수 풀이의 시간복잡도는?"
  options={['O(1)', 'O(n log n)', 'O(n⁴)', '항상 O(2ⁿ)']}
  correctAnswer={2}
  explanation="제시한 풀이는 O(n log n) 시간에 동작합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={12}
  question="최대 상담 수에서 확인할 경계 사례는?"
  options={['함수 이름의 길이', '주석 위치', '들여쓰기 폭', '종료와 시작이 같은 두 상담은 함께 선택 가능']}
  correctAnswer={4}
  explanation="종료와 시작이 같은 두 상담은 함께 선택 가능 상황을 별도로 확인해야 합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.']}
/>

<Quiz
  questionNumber={13}
  question="제한사항을 먼저 읽는 가장 중요한 이유는?"
  options={['허용 가능한 알고리즘 복잡도를 정하기 위해', '제목을 줄이기 위해', '변수 이름을 고르기 위해', '주석 수를 정하기 위해']}
  correctAnswer={1}
  explanation="입력 크기는 완전탐색, 정렬, 선형 풀이 중 가능한 범위를 판단하게 합니다."
  optionExplanations={['정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={14}
  question="정렬이 필요하지만 원본 배열도 이후 사용한다면?"
  options={['원본을 바로 정렬한다', '복사본을 만들어 정렬한다', '정렬을 포기한다', '문자열로 바꾼다']}
  correctAnswer={2}
  explanation="전개 구문 등으로 복사한 배열을 정렬하면 원본 변경을 피할 수 있습니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

<Quiz
  questionNumber={15}
  question="채점기가 확인하는 최종 결과는?"
  options={['console 출력 횟수', 'solution 함수의 반환값', '파일 이름', '주석 내용']}
  correctAnswer={2}
  explanation="문제에서 요구한 자료형을 solution 함수가 반환해야 합니다."
  optionExplanations={['핵심 조건이나 복잡도 기준과 맞지 않습니다.', '정답입니다. 핵심 조건에 맞는 선택입니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.', '핵심 조건이나 복잡도 기준과 맞지 않습니다.']}
/>

## 참고 자료

- [프로그래머스 코딩테스트 고득점 Kit](https://school.programmers.co.kr/learn/challenges?tab=algorithm_practice_kit)
- [Princeton Algorithms 4e - Sorting](https://algs4.cs.princeton.edu/20sorting/)
- [Princeton Algorithms 4e - Graphs](https://algs4.cs.princeton.edu/40graphs/)
- [MIT OpenCourseWare 6.006 Introduction to Algorithms](https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/)
