---
title: '14. 서버 상태·폼·테스트 종합 연습 문제'
description: '캐시, 무효화, 낙관적 업데이트, 폼 오류 흐름과 테스트 경계를 실무 시나리오 18문항으로 점검한다'
---

<Callout type="info">
  **이번 문서의 목표:** 기능별 API를 외우는 수준을 넘어, 서버 상태·폼·테스트가 만나는 지점에서 데이터 출처와 실패 복구 전략을 근거 있게 선택할 수 있다.
</Callout>

<Callout type="warning">
  아래 문제는 특정 시험이나 인터뷰 문제를 복제하지 않은 창작 연습입니다. 먼저 답과 근거를 적고, 해설에서 성공 경로뿐 아니라 실패·재시도·캐시 일관성까지 확인하세요.
</Callout>

## 종합 연습

<Quiz questionNumber={1} question="상품 목록 API 결과를 useState와 Query 캐시에 동시에 저장했다. 가장 큰 위험은?" options={['배열을 두 번 저장하면 문법 오류가 난다', '두 복사본 중 어느 것이 최신인지 동기화해야 한다', 'Query 캐시는 객체를 저장하지 못한다', 'useState는 서버 응답을 받을 수 없다']} correctAnswer={2} explanation="서버 상태의 클라이언트 복사본을 두 곳에 두면 무효화·재요청 뒤 서로 어긋날 수 있다. 서버 상태 도구를 단일 캐시 출처로 두고 UI state만 지역적으로 관리한다." optionExplanations={['문법상 가능하지만 설계가 문제다', '정답 — 중복 출처가 일관성 비용을 만든다', '객체와 배열을 저장한다', '저장할 수 있지만 중복이 문제다']} />

<Quiz questionNumber={2} question="queryKey가 ['issue', id]인 상세 쿼리에서 id가 바뀐다. 기대되는 동작은?" options={['같은 캐시 항목을 덮어쓴다', '각 id가 독립된 캐시 주소를 가지며 해당 키의 데이터를 조회한다', '쿼리가 영원히 비활성화된다', '모든 issue 캐시가 삭제된다']} correctAnswer={2} explanation="queryKey는 캐시 주소이자 요청 의존성이다. id를 포함하면 각 상세 데이터가 분리되고 id 변화에 맞춰 올바른 쿼리가 실행된다." optionExplanations={['id를 키에서 빼면 그런 오류가 생긴다', '정답 — 입력을 키에 포함한다', 'enabled 설정과 별개다', '자동 전체 삭제는 일어나지 않는다']} />

<Quiz questionNumber={3} question="게시글 생성 성공 뒤 목록을 최신화하는 가장 단순하고 안전한 첫 전략은?" options={['페이지를 강제 새로고침한다', '관련 목록 query를 invalidate해 재검증한다', 'DOM에 li를 직접 추가한다', 'staleTime을 무한대로 바꾼다']} correctAnswer={2} explanation="생성 결과가 목록 정렬·권한·서버 계산에 영향을 준다면 관련 query 무효화가 서버를 다시 진실의 출처로 삼는 안전한 기본 전략이다." optionExplanations={['앱 상태와 UX를 잃는다', '정답 — 관련 캐시를 stale로 만들고 재요청한다', 'React와 캐시가 변경을 모른다', '오래된 데이터를 고정한다']} />

<Quiz questionNumber={4} question="낙관적 좋아요 업데이트 전에 해야 할 핵심 작업은?" options={['관련 진행 쿼리를 취소하고 이전 캐시 스냅샷을 보관한다', '모든 캐시를 localStorage로 옮긴다', '오류 처리를 삭제한다', '버튼을 DOM에서 제거한다']} correctAnswer={1} explanation="진행 중 refetch가 낙관적 값을 덮지 않도록 취소하고, 실패 시 되돌릴 이전 값을 저장한다. onError에서 rollback하고 종료 시 재검증한다." optionExplanations={['정답 — 경쟁 상태와 롤백을 준비한다', '필수 조건이 아니다', '실패 시 거짓 UI가 남는다', '상호작용을 숨길 뿐 일관성을 해결하지 않는다']} />

<Quiz questionNumber={5} question="금액 이체처럼 실패 비용이 큰 mutation에 낙관적 업데이트를 무조건 적용하면 안 되는 이유는?" options={['mutation은 숫자를 지원하지 않아서', '성공을 먼저 보여준 뒤 실패하면 사용자가 잘못된 완료 상태를 믿을 수 있어서', '캐시는 텍스트만 저장해서', '폼과 함께 쓸 수 없어서']} correctAnswer={2} explanation="낙관적 UI는 성공 가능성이 높고 쉽게 되돌릴 수 있는 작업에 적합하다. 금전·재고처럼 서버 확인이 중요한 작업은 제출 중 상태와 확정 결과를 명확히 보여준다." optionExplanations={['숫자와 무관하다', '정답 — 도메인 위험을 고려한다', '다양한 데이터를 저장한다', '폼 mutation도 가능하다']} />

<Quiz questionNumber={6} question="staleTime과 gcTime의 차이로 옳은 것은?" options={['둘 다 요청 timeout이다', 'staleTime은 신선도, gcTime은 사용되지 않는 캐시 보관 시간을 다룬다', 'gcTime은 서버 응답 헤더다', 'staleTime은 mutation 재시도 횟수다']} correctAnswer={2} explanation="staleTime 동안 데이터는 fresh로 취급되고, gcTime은 observer가 없는 inactive 캐시를 메모리에 얼마나 보관할지 정한다." optionExplanations={['네트워크 timeout과 다르다', '정답 — 신선도와 수명을 구분한다', '클라이언트 캐시 설정이다', 'mutation retry와 무관하다']} />

<Quiz questionNumber={7} question="클라이언트 Zod 검증을 통과했지만 서버가 email 충돌을 반환했다. 처리로 적절한 것은?" options={['성공으로 이동한다', 'setError로 email 필드 오류에 매핑한다', '모든 Query 캐시를 삭제한다', '스키마를 제거한다']} correctAnswer={2} explanation="서버만 알 수 있는 유일성 오류는 해당 필드의 서버 오류다. RHF setError로 연결하고 사용자가 수정할 위치에 포커스를 제공한다." optionExplanations={['저장되지 않았다', '정답 — 수정 가능한 위치와 오류를 연결한다', '관련 없는 캐시까지 잃는다', '클라이언트 형식 검증은 여전히 가치가 있다']} />

<Quiz questionNumber={8} question="폼 제출 중 버튼 disabled만으로 주문 중복을 완전히 막을 수 있는가?" options={['가능하다', '불가능하다. 서버 멱등성도 필요하다', '가능하지만 Context가 필요하다', '불가능하므로 버튼을 제거해야 한다']} correctAnswer={2} explanation="disabled는 한 화면의 중복 클릭을 줄인다. 재전송·여러 탭·클라이언트 우회를 고려한 최종 중복 방지는 서버의 멱등성 키와 트랜잭션 책임이다." optionExplanations={['서버 수준 중복을 막지 못한다', '정답 — UX와 무결성 경계를 구분한다', 'Context가 해결하지 않는다', '제출 UI는 필요하다']} />

<Quiz questionNumber={9} question="RHF Controller를 우선 사용해야 하는 경우는?" options={['모든 기본 input', 'value와 onChange API를 가진 외부 DatePicker', '정적 제목', 'QueryClientProvider']} correctAnswer={2} explanation="Controller는 외부 제어 컴포넌트와 RHF field 계약을 연결한다. 기본 HTML 입력은 register가 더 단순하다." optionExplanations={['register가 기본 선택이다', '정답 — 제어 컴포넌트 어댑터다', '폼 필드가 아니다', '서버 상태 Provider다']} />

<Quiz questionNumber={10} question="Testing Library에서 로딩 뒤 나타날 heading을 찾는 쿼리는?" options={['queryByRole을 즉시 호출', 'findByRole을 await', 'document.querySelector만 반복', 'getByTestId로 sleep']} correctAnswer={2} explanation="findBy 계열은 비동기적으로 나타나는 요소를 기다린다. 역할과 접근 가능한 이름을 사용하면 사용자 관점의 계약도 검증한다." optionExplanations={['없으면 null로 끝나 기다리지 않는다', '정답 — 비동기 등장에 맞다', '수동 폴링이 불필요하다', '고정 시간 대기는 불안정하다']} />

<Quiz questionNumber={11} question="mutation 오류 테스트에서 fetch를 구현 세부까지 직접 mock하기보다 MSW가 주는 이점은?" options={['실제 결제를 수행한다', '네트워크 경계에서 요청을 가로채 앱 코드를 운영과 같은 방식으로 실행한다', 'React 렌더를 생략한다', '모든 단언을 자동 생성한다']} correctAnswer={2} explanation="MSW는 fetch 구현을 교체하지 않고 요청·응답 계약을 모킹한다. 개발과 테스트에서 같은 handler를 재사용하고 UI 전체 흐름을 검증하기 쉽다." optionExplanations={['외부 운영 호출을 막는다', '정답 — 네트워크 경계 모킹이다', 'UI는 실제로 렌더한다', '단언은 직접 작성한다']} />

<Quiz questionNumber={12} question="테스트마다 같은 QueryClient를 공유했더니 순서에 따라 성공 데이터가 남는다. 해결은?" options={['테스트를 항상 같은 순서로만 실행', '각 테스트에 새 QueryClient를 만들고 기본 retry를 제어', '모든 단언을 삭제', '캐시 시간을 무한대로 설정']} correctAnswer={2} explanation="테스트는 서로 독립이어야 한다. 새 QueryClient로 캐시를 격리하고 retry를 끄면 오류 시나리오 시간도 예측 가능해진다." optionExplanations={['순서 의존성을 숨길 뿐이다', '정답 — 격리된 테스트 환경이다', '회귀를 잡지 못한다', '오염을 더 오래 유지한다']} />

<Quiz questionNumber={13} question="쿼리 오류 화면의 좋은 사용자 계약은?" options={['스택 추적 전체 표시', '무슨 일이 실패했는지와 다시 시도 행동 제공', '빈 화면', '자동 무한 재요청']} correctAnswer={2} explanation="오류 UI는 원인 범주와 회복 행동을 제공해야 한다. 개발 세부는 관찰 로그로 보내고 사용자에게 민감한 정보를 노출하지 않는다." optionExplanations={['민감 정보와 혼란을 준다', '정답 — 실패에서 회복할 수 있다', '상태를 알 수 없다', '서버 부하와 반복 실패를 만든다']} />

<Quiz questionNumber={14} question="서버 응답에서 계산 가능한 filteredIssues를 별도 useState로 복제할 때 위험은?" options={['filter를 사용할 수 없다', '원본 캐시와 파생 state가 어긋날 수 있다', 'state는 배열을 금지한다', 'Query는 렌더를 하지 않는다']} correctAnswer={2} explanation="필터 결과는 query data와 filter 조건에서 렌더 중 계산한다. 복제하면 refetch 뒤 별도 동기화가 필요해 오래된 화면이 생길 수 있다." optionExplanations={['배열 filter는 사용할 수 있다', '정답 — 파생 상태 중복 문제다', '배열 저장은 가능하다', 'Query 결과 변화는 소비자를 갱신한다']} />

<Quiz questionNumber={15} question="서버 상태 통합 테스트의 가장 가치 있는 단언은?" options={['useQuery가 정확히 한 번 호출됐다', '사용자 행동 뒤 로딩·성공 또는 오류 UI가 요구사항대로 바뀐다', '내부 queryKey 변수가 특정 줄에 있다', '컴포넌트 함수 이름이 유지된다']} correctAnswer={2} explanation="사용자가 관찰하는 결과와 요청 계약을 검증하면 내부 훅 구성 리팩터링에도 견디면서 실제 회귀를 잡는다." optionExplanations={['구현 세부에 결합된다', '정답 — 외부 행동 계약이다', '파일 구조 단언은 가치가 낮다', '이름 변경에 불필요하게 깨진다']} />

<Quiz questionNumber={16} question="SPA route를 lazy load한 뒤 배포에서 직접 주소 접근만 404가 난다. 필요한 설정은?" options={['모든 route를 제거', '정적 호스트의 history fallback을 앱 엔트리로 설정', 'staleTime 증가', '폼 resolver 제거']} correctAnswer={2} explanation="클라이언트 route 주소를 서버가 파일 경로로 찾다가 404를 낸다. 존재하지 않는 경로를 index.html로 돌리는 플랫폼별 rewrite가 필요하다." optionExplanations={['라우팅 기능을 잃는다', '정답 — SPA 호스팅 계약이다', '캐시 신선도와 무관하다', '폼 검증과 무관하다']} />

<Quiz questionNumber={17} question="환경 변수에 운영 API 비밀 키를 VITE_SECRET으로 넣으면 안전하다는 설명은?" options={['맞다. VITE 접두사가 암호화한다', '틀리다. 클라이언트 번들에 포함되는 값은 사용자에게 노출될 수 있다', '맞다. minify가 비밀을 숨긴다', '틀리다. 대신 localStorage에 넣는다']} correctAnswer={2} explanation="브라우저로 전달되는 번들·환경 값은 비밀이 아니다. 비밀 자격 증명은 서버에 두고 클라이언트에는 공개 가능한 설정만 제공한다." optionExplanations={['접두사는 노출 허용 변수를 선택할 뿐이다', '정답 — 클라이언트는 신뢰 경계가 아니다', '난독화는 보안이 아니다', 'localStorage도 사용자에게 보인다']} />

<Quiz questionNumber={18} question="전체 설계가 좋은지 판단하는 최종 기준은?" options={['라이브러리 API를 가장 많이 썼다', '데이터 출처·오류 소유자·사용자 회복 행동·테스트 경계가 명확하다', '모든 요청을 한 컴포넌트에 넣었다', '오류가 보이지 않게 숨겼다']} correctAnswer={2} explanation="도구 수가 아니라 책임 경계가 핵심이다. 서버 상태 캐시, 폼 필드, API 오류, 사용자 피드백, 테스트가 한 방향의 계약을 공유해야 한다." optionExplanations={['복잡성 지표일 뿐이다', '정답 — 실패까지 설명 가능한 구조다', '거대한 결합을 만든다', '실패를 해결하지 못한다']} />

## 참고 자료

- [TanStack Query 공식 문서 - Query Invalidation](https://tanstack.com/query/latest/docs/framework/react/guides/query-invalidation)
- [TanStack Query 공식 문서 - Optimistic Updates](https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates)
- [React Hook Form 공식 문서](https://react-hook-form.com/)
- [Testing Library 공식 문서 - About Queries](https://testing-library.com/docs/queries/about)
- [MSW 공식 문서](https://mswjs.io/docs/)
- [Vite 공식 문서 - Static Deploy](https://vite.dev/guide/static-deploy.html)
