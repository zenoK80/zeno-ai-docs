---
title: '20. 카이제곱 검정: 적합도·독립성 검정'
description: '카이제곱 통계량으로 이론분포 적합성과 두 범주형 변수의 독립성을 검정하며, 기대도수 계산부터 자유도까지 손으로 검산한다.'
---

<Callout type="info">
  **이번 문서의 목표:** 이 문서를 다 읽으면 범주형 자료가 주어졌을 때 적합도 검정과 독립성 검정을 구분하고, 각 칸의 기대도수를 정확히 계산해 카이제곱 통계량과 자유도를 구한 뒤 결론까지 내릴 수 있다.
</Callout>

## 이 편에서 하는 일

지금까지 다룬 검정(18편·19편)은 모두 평균·비율·분산처럼 **숫자로 된 값**을 비교했다. 이번 편에서 다루는 카이제곱 검정(chi-square test, 그리스 문자 $\chi$(카이)를 제곱한 값을 쓰는 검정)은 "관찰된 도수(count, 개수)가 기대한 도수와 얼마나 다른가"를 다룬다. 즉 대상이 범주형 자료(categorical data, 성별·혈액형·선호 브랜드처럼 숫자가 아니라 범주로 나뉘는 자료)라는 점이 이전 두 편과 다르다. 13편에서 배운 카이제곱분포가 여기서 검정통계량의 분포로 실제 쓰인다.

> **쉽게 말하면:** 실제로 세어 보니 나온 숫자(관찰도수)와, 원래 그래야 한다고 기대했던 숫자(기대도수)의 차이를 하나의 점수로 합쳐 "이 정도 차이는 우연이라 보기엔 너무 크다"를 판정한다.

## 카이제곱 통계량: 하나의 공식, 두 가지 쓰임

### 공식과 뜻

적합도 검정이든 독립성 검정이든 카이제곱 통계량의 계산 방식은 완전히 같다.

$$
\chi^2 = \sum \frac{(O_i - E_i)^2}{E_i}
$$

- $\chi^2$ (카이제곱): 관찰도수와 기대도수의 차이를 합산한 검정통계량
- $O_i$ (observed, 관찰도수): 실제로 자료에서 센 $i$번째 칸의 개수
- $E_i$ (expected, 기대도수): 귀무가설이 맞다고 가정했을 때 이론적으로 기대되는 $i$번째 칸의 개수
- $\sum$ (시그마, 모두 더하라는 기호): 모든 칸에 대해 $(O_i-E_i)^2/E_i$ 를 구해 다 더한다

각 칸에서 관찰도수와 기대도수의 차이를 제곱해서 기대도수로 나누는 이유는, 차이의 부호(더 많음/더 적음)를 없애면서도(제곱), 원래 기대되는 규모가 큰 칸에서 생긴 차이보다 작은 칸에서 생긴 같은 크기의 차이를 상대적으로 더 크게 반영하기 위해서다(기대도수로 나눔). 예를 들어 기대도수 100인 칸에서 차이 10과, 기대도수 5인 칸에서 차이 10은 후자가 훨씬 더 심각한 어긋남이다.

### 카이제곱분포와 자유도

이렇게 계산한 $\chi^2$ 값은 자유도(degree of freedom)가 정해진 카이제곱분포를 따른다. 카이제곱분포는 **항상 0 이상의 값만 가지며 오른쪽으로 긴 꼬리**를 가진 분포이므로, 카이제곱 검정의 기각역은 **오른쪽 꼬리 한쪽에만** 있다. "차이가 크다"는 것은 항상 $\chi^2$ 값이 크다는 뜻이지, 방향(더 많다/적다)의 개념이 없기 때문이다.

> **자주 틀리는 점:** 다른 검정에서 배운 "양측이냐 단측이냐"의 구분을 카이제곱 검정에 그대로 적용하려는 실수가 많다. 카이제곱 검정은 $O$와 $E$의 차이를 제곱해 부호를 없애 버렸으므로 애초에 방향이라는 개념이 없다. 기각역은 예외 없이 오른쪽 꼬리 하나뿐이다.

## 적합도 검정: 자료가 특정 분포를 따르는가

### 언제 쓰는가

적합도 검정(goodness-of-fit test)은 "이 자료가 특정한 이론적 비율(또는 분포)을 따른다고 볼 수 있는가"를 검정한다. 예를 들어 주사위가 공정한지(6개 눈이 각각 1/6씩 나오는지), 어느 매장의 요일별 방문객 비율이 예상한 비율과 같은지를 확인할 때 쓴다.

### 자유도

적합도 검정의 자유도는 범주의 개수 $k$ 에서 1을 뺀 값이다.

$$
df = k - 1
$$

- $k$: 범주(칸)의 개수
- $-1$: 전체 도수의 합이 이미 정해져 있으므로($O_i$ 들의 합이 표본크기로 고정), 마지막 칸의 기대도수는 나머지 칸이 정해지면 자동으로 결정되어 자유롭게 정할 수 없기 때문이다

### 예제로 다섯 단계 밟기

한 주사위를 120번 굴려 나온 눈의 도수가 아래와 같았다. 유의수준 $\alpha = 0.05$ 에서 이 주사위가 공정한지(각 눈이 같은 확률로 나오는지) 검정한다.

| 눈 | 1 | 2 | 3 | 4 | 5 | 6 | 합계 |
|----|---|---|---|---|---|---|------|
| 관찰도수 $O_i$ | 25 | 17 | 15 | 23 | 24 | 16 | 120 |

공정한 주사위라면 각 눈이 나올 확률은 $1/6$ 이므로, 기대도수는 모든 칸에서 $120 \times 1/6 = 20$ 으로 동일하다.

| 눈 | 1 | 2 | 3 | 4 | 5 | 6 | 합계 |
|----|---|---|---|---|---|---|------|
| 기대도수 $E_i$ | 20 | 20 | 20 | 20 | 20 | 20 | 120 |

기대도수의 합(120)이 관찰도수의 합(120)과 정확히 같은지 먼저 확인한다. 같으므로 다음 단계로 넘어간다.

<Steps>
1. **가설 설정**
   $$
   H_0: \text{각 눈이 나올 확률은 모두 } 1/6 \text{이다 (공정하다)}
   $$
   $$
   H_1: \text{적어도 한 눈의 확률이 } 1/6 \text{과 다르다 (공정하지 않다)}
   $$
2. **유의수준**: $\alpha = 0.05$, 자유도 $df = k-1 = 6-1 = 5$
3. **검정통계량**: 칸마다 $(O_i-E_i)^2/E_i$ 를 구해 더한다.
   $$
   \frac{(25-20)^2}{20} = \frac{25}{20} = 1.25
   $$
   $$
   \frac{(17-20)^2}{20} = \frac{9}{20} = 0.45
   $$
   $$
   \frac{(15-20)^2}{20} = \frac{25}{20} = 1.25
   $$
   $$
   \frac{(23-20)^2}{20} = \frac{9}{20} = 0.45
   $$
   $$
   \frac{(24-20)^2}{20} = \frac{16}{20} = 0.80
   $$
   $$
   \frac{(16-20)^2}{20} = \frac{16}{20} = 0.80
   $$
   이 여섯 값을 모두 더하면
   $$
   \chi^2 = 1.25+0.45+1.25+0.45+0.80+0.80 = 5.00
   $$
4. **기각역/p값 판정**: 자유도 5인 카이제곱분포에서 $\alpha=0.05$ 의 임계값은 $\chi^2_{0.05}(5) = 11.07$ 이다. 계산된 $\chi^2 = 5.00$ 은 $11.07$ 보다 작으므로 기각역 밖에 있다. p값으로 확인해도 자유도 5, $\chi^2=5.00$ 에서 $p \approx 0.416$ 으로 $\alpha=0.05$ 보다 훨씬 크다. 두 판정이 일치한다.
5. **결론**: 귀무가설을 기각하지 않는다. 관찰된 눈의 분포는 공정한 주사위에서 우연히 나올 수 있는 변동의 범위 안에 있다고 볼 수 있다.
</Steps>

## 독립성 검정: 두 범주형 변수가 서로 관련이 있는가

### 언제 쓰는가

독립성 검정(test of independence)은 교차표(contingency table, 두 범주형 변수를 행과 열로 교차시켜 만든 표)에서 "두 변수가 서로 독립인가, 아니면 관련이 있는가"를 검정한다. 예를 들어 "성별과 특정 상품 선호도 사이에 관련이 있는가"가 전형적인 질문이다.

### 기대도수 계산: 셀마다 직접

교차표에서 칸 $(i,j)$(행 $i$, 열 $j$)의 기대도수는 다음 공식으로 구한다. 이 공식은 "두 변수가 독립이라면, 어떤 칸의 비율은 그 행의 합과 열의 합에서 결정되는 확률의 곱과 같아야 한다"는 독립성의 정의를 그대로 숫자로 옮긴 것이다.

$$
E_{ij} = \frac{(\text{행 } i \text{의 합}) \times (\text{열 } j \text{의 합})}{\text{전체 합}}
$$

- $E_{ij}$: 행 $i$, 열 $j$ 칸의 기대도수
- 행의 합, 열의 합: 해당 행 또는 열에 속한 모든 관찰도수의 합
- 전체 합: 교차표 전체 관찰도수의 총합

### 자유도

독립성 검정의 자유도는 행의 개수 $r$ 과 열의 개수 $c$ 로 정해진다.

$$
df = (r-1)(c-1)
$$

- $r$: 교차표의 행 개수
- $c$: 교차표의 열 개수
- 이 값이 이런 형태인 이유는, 각 행의 합과 각 열의 합이 이미 고정되어 있는 상태에서 자유롭게 값을 정할 수 있는 칸은 전체 $r \times c$ 칸 중 가로·세로 마지막 줄을 뺀 $(r-1) \times (c-1)$ 칸뿐이기 때문이다. 나머지 칸은 행 합·열 합을 맞추기 위해 자동으로 결정된다

### 기대도수 조건

카이제곱 근사가 정확하려면 **모든 칸의 기대도수가 5 이상**이어야 한다는 것이 일반적인 기준이다. 기대도수가 5 미만인 칸이 있으면 근사가 부정확해지므로, 인접한 범주를 합치거나 다른 검정(예: 피셔의 정확검정)을 고려해야 한다. 독학사 시험 수준에서는 이 조건을 "왜 확인해야 하는지" 이해하고 있으면 충분하다.

### 예제로 다섯 단계 밟기: 교차표 전체를 채운다

성별과 특정 온라인 강의 선호 여부를 조사해 아래 교차표(관찰도수)를 얻었다. 유의수준 $\alpha=0.05$ 에서 "성별과 강의 선호 여부는 독립인지" 검정한다.

| 구분 | 선호함 | 선호하지 않음 | 행 합계 |
|------|--------|---------------|---------|
| 남성 | 30 | 20 | 50 |
| 여성 | 45 | 25 | 70 |
| 열 합계 | 75 | 45 | 120 |

먼저 칸마다 기대도수를 전부 계산한다.

$$
E_{\text{남,선호}} = \frac{50 \times 75}{120} = \frac{3750}{120} = 31.25
$$

$$
E_{\text{남,비선호}} = \frac{50 \times 45}{120} = \frac{2250}{120} = 18.75
$$

$$
E_{\text{여,선호}} = \frac{70 \times 75}{120} = \frac{5250}{120} = 43.75
$$

$$
E_{\text{여,비선호}} = \frac{70 \times 45}{120} = \frac{3150}{120} = 26.25
$$

기대도수 표로 정리하면 다음과 같다.

| 구분 | 선호함 | 선호하지 않음 | 행 합계 |
|------|--------|---------------|---------|
| 남성 | 31.25 | 18.75 | 50 |
| 여성 | 43.75 | 26.25 | 70 |
| 열 합계 | 75.00 | 45.00 | 120 |

기대도수의 행 합계·열 합계가 관찰도수의 행 합계·열 합계와 정확히 같은지 확인한다: $31.25+18.75=50$, $43.75+26.25=70$, $31.25+43.75=75$, $18.75+26.25=45$, 전체 합 $31.25+18.75+43.75+26.25=120$. 관찰도수 전체 합 120과 일치하므로 검산 통과다. 네 칸 모두 기대도수가 5 이상이므로 근사 조건도 충족한다.

<Steps>
1. **가설 설정**
   $$
   H_0: \text{성별과 강의 선호 여부는 독립이다 (관련 없다)}
   $$
   $$
   H_1: \text{성별과 강의 선호 여부는 독립이 아니다 (관련 있다)}
   $$
2. **유의수준**: $\alpha=0.05$, 자유도 $df=(r-1)(c-1)=(2-1)(2-1)=1$
3. **검정통계량**: 네 칸의 $(O-E)^2/E$ 를 각각 구한다.
   $$
   \frac{(30-31.25)^2}{31.25} = \frac{1.5625}{31.25} = 0.05
   $$
   $$
   \frac{(20-18.75)^2}{18.75} = \frac{1.5625}{18.75} \approx 0.0833
   $$
   $$
   \frac{(45-43.75)^2}{43.75} = \frac{1.5625}{43.75} \approx 0.0357
   $$
   $$
   \frac{(25-26.25)^2}{26.25} = \frac{1.5625}{26.25} \approx 0.0595
   $$
   네 값을 더하면
   $$
   \chi^2 = 0.05+0.0833+0.0357+0.0595 \approx 0.229
   $$
4. **기각역/p값 판정**: 자유도 1인 카이제곱분포에서 $\alpha=0.05$ 의 임계값은 $\chi^2_{0.05}(1)=3.841$ 이다. 계산된 $\chi^2 \approx 0.229$ 는 $3.841$ 보다 훨씬 작으므로 기각역 밖에 있다. p값으로 확인해도 자유도 1, $\chi^2 \approx 0.229$ 에서 $p \approx 0.632$ 로 $\alpha=0.05$ 보다 크다. 두 판정이 일치한다.
5. **결론**: 귀무가설을 기각하지 않는다. 표본에서 관찰된 남녀 간 선호 비율의 차이는, 성별과 강의 선호 여부가 서로 관련이 있다고 볼 만큼 크지 않다. 즉 이 조사 결과만으로는 두 변수가 독립이 아니라고 말할 통계적 근거가 부족하다.
</Steps>

## 적합도 검정과 독립성 검정 비교

| 구분 | 적합도 검정 | 독립성 검정 |
|------|-------------|-------------|
| 무엇을 묻는가 | 자료가 특정 이론적 비율(분포)을 따르는가 | 두 범주형 변수가 서로 관련이 있는가 |
| 자료 형태 | 범주 하나에 대한 도수표 | 두 변수를 교차시킨 교차표 |
| 기대도수 계산 | 전체 표본크기 × 이론적 비율 | (행 합계 × 열 합계) ÷ 전체 합 |
| 자유도 | $k-1$ (범주 개수 − 1) | $(r-1)(c-1)$ (행–1, 열–1의 곱) |
| 공통점 | 검정통계량 공식, 기각역이 오른쪽 꼬리 하나뿐이라는 점은 동일 | 좌동 |

## 자주 틀리는 점 모음

- **기대도수의 합이 관찰도수의 합과 다르게 계산된다.** 계산 실수가 있으면 두 합이 어긋난다. 항상 계산 직후 전체 합을 비교해 검산한다.
- **독립성 검정의 자유도를 $r \times c$ 로 착각한다.** 올바른 자유도는 $(r-1)(c-1)$ 이다. 위 예제처럼 $2\times2$ 표는 자유도가 $1$ 이지 $4$ 가 아니다.
- **적합도 검정의 자유도에서 $-1$ 을 빠뜨린다.** 범주가 6개면 자유도는 5이지 6이 아니다.
- **기대도수가 5 미만인 칸을 확인하지 않고 그대로 검정한다.** 근사 조건을 벗어나면 카이제곱분포로 근사한 p값 자체가 부정확해진다.
- **카이제곱 검정에 단측·양측 개념을 적용하려 한다.** 기각역은 항상 오른쪽 꼬리 하나뿐이며, 방향의 개념 자체가 없다.

## 핵심 정리

- 카이제곱 통계량은 $\chi^2 = \sum (O_i-E_i)^2/E_i$ 로 계산하며, 적합도·독립성 검정 모두 같은 공식을 쓴다.
- 기각역은 항상 오른쪽 꼬리 하나이며, 계산 직후 기대도수 합이 관찰도수 합과 같은지 반드시 검산한다.
- 적합도 검정의 자유도는 $k-1$, 독립성 검정의 자유도는 $(r-1)(c-1)$ 이다.
- 독립성 검정의 기대도수는 칸마다 (행 합계 × 열 합계) ÷ 전체 합으로 따로 계산해야 한다.
- 모든 칸의 기대도수가 5 이상이어야 카이제곱 근사가 정확하다는 조건을 확인한다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="카이제곱 검정의 기각역에 대한 설명으로 옳은 것은?"
  options={['양측검정일 때는 양쪽 꼬리에, 단측검정일 때는 한쪽 꼬리에 있다', '항상 오른쪽 꼬리 한쪽에만 있다', '항상 왼쪽 꼬리 한쪽에만 있다', '유의수준에 따라 왼쪽 또는 오른쪽으로 달라진다']}
  correctAnswer={2}
  explanation="카이제곱 통계량은 관찰도수와 기대도수의 차이를 제곱해 부호를 없앤 값이므로 방향이라는 개념이 없다. 차이가 클수록 값이 커지므로 기각역은 항상 오른쪽 꼬리 하나뿐이다."
  optionExplanations={['카이제곱 검정에는 단측·양측 구분 자체가 없다', '정답이다. 제곱해 부호를 없앴으므로 큰 값 쪽인 오른쪽 꼬리만 기각역이 된다', '왼쪽 꼬리는 차이가 거의 없다는 뜻이므로 기각역이 될 수 없다', '기각역의 위치는 유의수준과 무관하게 항상 오른쪽으로 고정된다']}
/>

<Quiz
  questionNumber={2}
  question="4개 범주로 이루어진 자료로 적합도 검정을 할 때 자유도로 옳은 것은?"
  options={['4', '3', '5', '2']}
  correctAnswer={2}
  explanation="적합도 검정의 자유도는 범주 개수 k에서 1을 뺀 k-1이다. 전체 도수의 합이 고정되어 있어 마지막 범주의 기대도수는 자동으로 결정되기 때문이다."
  optionExplanations={['범주 개수를 그대로 쓴 값으로 -1을 빼지 않았다', '정답이다. k-1=4-1=3이다', '범주 개수보다 오히려 큰 값으로 틀렸다', '자유도를 절반으로 잘못 계산한 값이다']}
/>

<Quiz
  questionNumber={3}
  question="3행 4열로 이루어진 교차표로 독립성 검정을 할 때 자유도로 옳은 것은?"
  options={['12', '7', '6', '3']}
  correctAnswer={3}
  explanation="독립성 검정의 자유도는 (r-1)(c-1)이다. r=3, c=4이므로 (3-1)(4-1)=2*3=6이다."
  optionExplanations={['행과 열의 개수를 그대로 곱한 값으로 -1을 적용하지 않았다', '행과 열에서 각각 1씩만 뺀 뒤 더한 값으로 곱해야 하는데 더했다', '정답이다. (3-1)(4-1)=2*3=6이다', '열의 개수만 반영하고 행은 반영하지 않은 값이다']}
/>

<Quiz
  questionNumber={4}
  question="교차표에서 어느 행의 합계가 60, 어느 열의 합계가 90, 전체 합계가 200일 때 그 칸의 기대도수는? (반올림 없음)"
  options={['27', '150', '5400', '24.3']}
  correctAnswer={1}
  explanation="기대도수는 (행 합계 x 열 합계) / 전체 합이다. (60 x 90)/200 = 5400/200 = 27이다."
  optionExplanations={['정답이다. 60x90을 200으로 나누면 27이다', '행 합계와 열 합계를 더한 값으로 공식과 다르다', '60x90의 곱셈 결과일 뿐 전체 합으로 나누지 않은 값이다', '200 대신 다른 수로 나눠 생긴 오답이다']}
/>

<Quiz
  questionNumber={5}
  question="어느 칸의 관찰도수가 O=40, 기대도수가 E=25일 때 이 칸이 카이제곱 통계량에 기여하는 값 (O-E)^2/E는? (반올림 없음)"
  options={['9', '6', '15', '225']}
  correctAnswer={1}
  explanation="(40-25)^2/25 = 225/25 = 9이다. 차이 15를 제곱한 225를 기대도수 25로 나누면 9가 된다."
  optionExplanations={['정답이다. 15의 제곱 225를 25로 나누면 9이다', '차이 15를 기대도수로 나누지 않고 대략 계산한 값으로 공식과 다르다', '차이 O-E=15 자체이며 제곱하지 않은 값이다', '제곱까지만 계산하고 기대도수로 나누지 않은 값이다']}
/>

<Quiz
  questionNumber={6}
  question="적합도 검정에서 기대도수를 모두 더한 값과 반드시 같아야 하는 것은?"
  options={['범주의 개수', '유의수준을 100으로 나눈 값', '관찰도수를 모두 더한 값(표본크기)', '자유도']}
  correctAnswer={3}
  explanation="기대도수는 귀무가설의 비율을 표본크기에 곱해 만들므로, 모든 칸의 기대도수를 더하면 반드시 전체 표본크기(관찰도수의 합)와 같아진다. 이 검산으로 계산 실수를 잡아낼 수 있다."
  optionExplanations={['범주 개수는 자유도 계산에 쓰일 뿐 도수의 합과는 무관하다', '유의수준은 판정 기준일 뿐 도수의 합과 관계가 없다', '정답이다. 기대도수의 합은 항상 관찰도수의 합, 즉 표본크기와 같아야 한다', '자유도는 범주 개수에서 파생되는 값으로 도수의 합과는 다른 개념이다']}
/>

<Quiz
  questionNumber={7}
  question="2행 2열 교차표에서 모든 칸의 기대도수가 3, 2, 4, 3으로 계산되었다. 이 결과에 대한 올바른 판단은?"
  options={['기대도수가 모두 정수이므로 계산이 맞고 그대로 검정을 진행하면 된다', '기대도수 중 5 미만인 칸이 있으므로 카이제곱 근사 조건을 벗어난다', '기대도수의 합이 관찰도수의 합과 다르므로 계산이 틀렸다', '2행 2열 표에서는 기대도수 조건을 확인할 필요가 없다']}
  correctAnswer={2}
  explanation="카이제곱 근사가 정확하려면 모든 칸의 기대도수가 5 이상이어야 하는데, 이 표는 네 칸 모두 5 미만이다. 이런 경우 근사가 부정확해질 수 있어 범주를 합치거나 다른 검정 방법을 고려해야 한다."
  optionExplanations={['정수 여부는 근사 조건과 무관하다. 기대도수는 소수여도 정상이다', '정답이다. 5 미만인 칸이 있으면 근사 조건을 벗어난 것이다', '기대도수의 합이 얼마인지는 이 문제에서 주어지지 않았고, 정수라는 사실만으로 틀렸다고 볼 수 없다', '표의 크기와 무관하게 모든 카이제곱 검정에서 기대도수 조건을 확인해야 한다']}
/>

## 참고 자료

- [Statistics for Applications / Introduction to Statistical Inference 슬라이드 — 카이제곱 검정 개관](https://www.itl.nist.gov/div898/handbook/)
- [NIST/SEMATECH e-Handbook of Statistical Methods](https://www.itl.nist.gov/div898/handbook/)
- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
