---
title: '24. 독학사 2단계 논리회로 예상·기출 문제 모음'
description: '진법·코드, 불 대수·카르노맵, 조합회로, 플립플롭·순차회로, 카운터·레지스터·메모리 전 범위를 국가평생교육진흥원 출제기준에 맞춰 재구성한 종합 문제'
---

<Callout type="warning">
  이 문서의 문제는 실제 독학사 기출문제를 그대로 옮긴 것이 아니라, 국가평생교육진흥원이 공개한 2단계 논리회로 **출제기준(평가영역)을 바탕으로 새로 재구성**한 예상 문제다. 회차별 실제 문항 수·난이도·출제 경향은 매년 공고와 시행 결과에 따라 달라질 수 있으므로, 반드시 최신 시행공고를 함께 확인해야 한다.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="십진수 233을 16진수로 옳게 변환한 것은?"
  options={['D9', 'E9', 'F9', 'C9']}
  correctAnswer={2}
  explanation="233을 16으로 나누면 몫 14, 나머지 9다. 몫 14는 16진수로 E이므로 233 = E9(16)이다."
  optionExplanations={['233을 16으로 나눈 몫 14(E)를 13(D)으로 착각했을 때 나오는 값이다', '정답이다. 233 = 14×16+9이며 14는 16진수로 E이므로 E9이다', '몫 14(E)를 15(F)로 착각했을 때 나오는 값이다', '몫 14(E)를 12(C)로 착각했을 때 나오는 값이다']}
/>

<Quiz
  questionNumber={2}
  question="BCD 코드로 십진수 58을 옳게 표현한 것은?"
  options={['0101 1000', '0011 1010', '1000 0101', '0101 0111']}
  correctAnswer={1}
  explanation="BCD는 십진 자릿수마다 4비트씩 따로 변환한다. 5는 0101, 8은 1000이므로 58의 BCD 표현은 0101 1000이다."
  optionExplanations={['정답이다. 5→0101, 8→1000을 그대로 이어 붙인 값이다', '이는 58을 그대로 8비트 이진수로 변환한 값(00111010)이며 BCD 방식이 아니다', '두 BCD 니블(5와 8)의 자리 순서를 서로 바꾼 값이다', '8의 BCD 표현을 1000이 아니라 0111(7)로 잘못 계산한 값이다']}
/>

<Quiz
  questionNumber={3}
  question="2진수 1011을 그레이 코드로 옳게 변환한 것은?"
  options={['1110', '1101', '1001', '0110']}
  correctAnswer={1}
  explanation="그레이 코드는 최상위 비트를 그대로 두고, 그 아래 비트는 바로 왼쪽(상위) 이진수 비트와 XOR한다. G3=B3=1, G2=B3 XOR B2=1 XOR 0=1, G1=B2 XOR B1=0 XOR 1=1, G0=B1 XOR B0=1 XOR 1=0이므로 1110이다."
  optionExplanations={['정답이다. 자리마다 인접한 두 이진수 비트를 XOR한 결과다', '최하위 비트 계산에서 B1과 B0을 XOR하지 않고 B0을 그대로 사용했을 때 나오는 값에 가깝다', '상위 비트를 계산하는 순서를 반대로 적용했을 때 나오는 값이다', '최상위 비트를 그대로 두지 않고 반전시켰을 때 나오는 값이다']}
/>

<Quiz
  questionNumber={4}
  question="8비트 체계에서 십진수 -37을 2의 보수로 옳게 표현한 것은?"
  options={['11011010', '11011011', '11011001', '00100101']}
  correctAnswer={2}
  explanation="37=00100101이며, 1의 보수는 11011010이고, 여기에 1을 더하면 2의 보수 11011011이 된다."
  optionExplanations={['이는 37의 1의 보수이며, 1을 더하는 마지막 단계가 빠졌다', '정답이다. 1의 보수 11011010에 1을 더한 11011011이 2의 보수다', '1을 더하는 과정에서 자리올림 처리를 잘못했을 때 나오는 값이다', '이는 +37의 표현이며 부호를 바꾸지 않은 값이다']}
/>

<Quiz
  questionNumber={5}
  question="8비트 2의 보수 체계에서 -70과 -80을 더하면 발생하는 현상은?"
  options={['오버플로 발생, 두 음수를 더했는데 결과가 양수로 나타난다', '오버플로 없음, 정상적으로 -150이 표현된다', '오버플로 발생하지만 결과는 여전히 정확한 음수다', '이 조합은 계산 자체가 불가능하다']}
  correctAnswer={1}
  explanation="실제 합 -150은 8비트 2의 보수 표현 범위(-128~127)를 벗어난다. 두 피연산자가 모두 음수인데 결과 비트열의 부호 비트가 0(양수)으로 나오는 것이 오버플로의 전형적인 증상이다."
  optionExplanations={['정답이다. 두 음수를 더했는데 결과가 양수처럼 보이는 것이 오버플로의 신호다', '8비트로는 -150을 표현할 수 없으므로 이 진술은 틀렸다', '오버플로가 발생하면 결과 부호 자체가 뒤집혀 더 이상 정확한 값이 아니다', '2의 보수 덧셈은 항상 수행되며, 다만 표현 범위를 벗어나면 오버플로가 발생할 뿐이다']}
/>

<Quiz
  questionNumber={6}
  question="데이터 10110에 짝수 패리티(even parity)를 적용할 때 추가해야 할 패리티 비트는?"
  options={['0', '1', '2', '추가할 필요가 없다']}
  correctAnswer={2}
  explanation="10110에서 1의 개수는 3개(홀수)다. 짝수 패리티는 전체 1의 개수를 짝수로 맞추는 것이 목표이므로, 1비트를 추가해 1의 개수를 4개(짝수)로 만들어야 한다."
  optionExplanations={['이미 홀수 개(3개)인 1의 개수를 그대로 두면 여전히 홀수가 되어 짝수 패리티 조건을 만족하지 못한다', '정답이다. 1을 추가하면 1의 개수가 4개(짝수)가 된다', '패리티 비트는 1비트만 추가하며 2는 유효한 값이 아니다', '현재 1의 개수가 홀수이므로 짝수 패리티를 위해 반드시 비트를 추가해야 한다']}
/>

<Quiz
  questionNumber={7}
  question="논리식 A + (NOT A 그리고 B)를 불 대수 법칙으로 간소화한 결과로 옳은 것은?"
  options={['A', 'B', 'A+B', 'AB']}
  correctAnswer={3}
  explanation="흡수 법칙에 따라 A + (A의 여)B = A + B로 간소화된다. 분배법칙으로 풀면 (A + A의 여)(A + B) = 1 × (A + B) = A + B와 동일한 결과가 나온다."
  optionExplanations={['B 항이 특정 조건에서 결과에 영향을 줄 수 있어 A만으로는 원래 식과 같지 않다', 'A가 1일 때는 B값과 무관하게 전체 식이 1이 되므로 B만으로는 원래 식과 같지 않다', '정답이다. 흡수 법칙에 의해 A + A의 여 B = A + B이다', 'AB는 두 변수가 모두 1일 때만 참이 되어 원래 식보다 훨씬 좁은 범위를 나타낸다']}
/>

<Quiz
  questionNumber={8}
  question="드모르간 법칙에 따라 A OR B 전체의 부정을 옳게 나타낸 것은?"
  options={['A′ 그리고 B′', 'A′ 또는 B′', 'A 그리고 B', 'A 또는 B']}
  correctAnswer={1}
  explanation="드모르간 법칙에 따르면 OR 전체의 부정은 각 항을 부정한 뒤 AND로 묶은 것과 같다. 즉 NOT(A OR B) = (NOT A) AND (NOT B)다."
  optionExplanations={['정답이다. 합의 부정은 각 항의 부정을 곱(AND)한 것과 같다', '이는 NOT(A AND B), 즉 곱의 부정에 대한 드모르간 결과다', '이는 원래 식에 부정을 적용하지 않은 형태다', '이는 원래 식 A 또는 B 그 자체이며 부정이 반영되지 않았다']}
/>

<Quiz
  questionNumber={9}
  question="3변수 함수 F(A,B,C)에서 F=1인 행이 A=1,B=0,C=1과 A=0,B=1,C=1뿐일 때, 최소항의 합(SOP)으로 옳은 것은?"
  options={['AB′C 또는 A′BC', 'ABC', 'A′B′C′', 'AB′C′ 또는 A′BC′']}
  correctAnswer={1}
  explanation="A=1,B=0,C=1은 AB′C, A=0,B=1,C=1은 A′BC에 대응한다. 이 두 최소항을 OR로 묶은 것이 SOP 표현이다."
  optionExplanations={['정답이다. 주어진 두 행을 각각 최소항으로 옮긴 뒤 OR로 결합한 형태다', '두 최소항 모두 A와 B가 동시에 1인 경우는 없으므로 ABC는 조건에 해당하지 않는다', 'C=0인 경우에 해당하는 항이며 주어진 조건(C=1)과 다르다', 'C=0으로 잘못 설정된 항들이며 주어진 조건(C=1)과 맞지 않는다']}
/>

<Quiz
  questionNumber={10}
  question="카르노맵에서 인접한 두 칸(2칸)을 하나로 묶으면 몇 개의 변수가 소거되는가?"
  options={['0개', '1개', '2개', '변수 개수에 따라 다르다']}
  correctAnswer={2}
  explanation="카르노맵에서 2^k개의 칸을 묶으면 k개의 변수가 소거된다. 2칸(2^1) 묶음은 정확히 1개의 변수를 소거한다."
  optionExplanations={['묶음을 만들면 항상 최소 하나의 변수가 소거된다', '정답이다. 2칸 묶음(2의 1승)은 정확히 1개의 변수를 소거한다', '2개 변수 소거는 4칸(2의 2승) 묶음에서 일어난다', '묶음 크기(2, 4, 8, ...)와 소거되는 변수 개수의 관계는 2의 거듭제곱으로 정확히 정해져 있다']}
/>

<Quiz
  questionNumber={11}
  question="퀴인-맥클러스키(Quine-McCluskey) 방법에서 두 항을 결합할 수 있는 조건은?"
  options={['두 항의 모든 비트가 같을 때', '두 항이 정확히 한 비트만 다르고 나머지는 모두 같을 때', '두 항의 1의 개수가 다를 때', '두 항이 서로 다른 변수 개수를 가질 때']}
  correctAnswer={2}
  explanation="퀴인-맥클러스키 방법은 1의 개수가 1개 차이 나는 그룹끼리 비교해, 정확히 한 비트만 다르고 나머지가 모두 같은 두 항을 하나로 결합해 그 비트를 무관 항으로 처리한다."
  optionExplanations={['모든 비트가 같으면 애초에 동일한 항이므로 결합할 대상이 아니다', '정답이다. 정확히 한 비트만 다른 항끼리 결합해 프라임 임플리컨트를 만든다', '1의 개수가 다르면 두 항은 인접하지 않아 결합할 수 없다', '같은 함수를 이루는 항들은 항상 동일한 변수 개수(자릿수)를 갖는다']}
/>

<Quiz
  questionNumber={12}
  question="조합논리회로 설계 절차의 올바른 순서는?"
  options={['회로도 작성 → 진리표 작성 → 간소화 → 사양 정의', '사양(입출력) 정의 → 진리표 작성 → 불 함수 간소화 → 회로도 작성', '간소화 → 사양 정의 → 진리표 작성 → 회로도 작성', '진리표 작성 → 회로도 작성 → 사양 정의 → 간소화']}
  correctAnswer={2}
  explanation="조합논리회로 설계는 먼저 입출력 사양을 정의하고, 그 관계를 진리표로 정리한 뒤, 불 함수를 간소화하고, 마지막으로 그 결과를 게이트로 구현한 회로도를 그리는 순서로 진행한다."
  optionExplanations={['순서가 완전히 거꾸로 배치되어 있다', '정답이다. 사양 정의 → 진리표 → 간소화 → 회로도 순서가 표준 설계 절차다', '사양 정의가 가장 먼저 이루어져야 이후 진리표를 작성할 수 있다', '간소화는 진리표 작성 이후, 회로도 작성 이전에 이루어져야 한다']}
/>

<Quiz
  questionNumber={13}
  question="전가산기(Full Adder)의 합(S) 출력에 대한 논리식으로 옳은 것은?"
  options={['A AND B AND Cin', 'A XOR B XOR Cin', 'A OR B OR Cin', '(A AND B) OR Cin']}
  correctAnswer={2}
  explanation="전가산기의 합(S)은 세 입력(A, B, 이전 자리올림 Cin) 중 1의 개수가 홀수일 때 1이 되므로, 세 입력을 모두 XOR로 묶은 식과 같다."
  optionExplanations={['AND 연산으로는 세 입력 중 하나만 1이어도 합이 1이 되어야 하는 상황을 표현하지 못한다', '정답이다. XOR 연산이 홀수 개의 1을 판별하는 합(S)의 동작과 정확히 일치한다', 'OR 연산으로는 두 개 이상의 입력이 1일 때도 합이 1로 나와 실제 가산 결과와 맞지 않는다', '이는 캐리 출력 계산식의 일부이며 합(S) 계산식이 아니다']}
/>

<Quiz
  questionNumber={14}
  question="전가산기(Full Adder)의 캐리 출력(Cout)에 대한 논리식으로 옳은 것은?"
  options={['AB 또는 BCin 또는 ACin', 'A XOR B XOR Cin', 'AB 그리고 BCin 그리고 ACin', 'A OR B OR Cin']}
  correctAnswer={1}
  explanation="세 입력 A, B, Cin 중 두 개 이상이 1이면 캐리가 발생한다. 이를 논리식으로 쓰면 AB, BCin, ACin 중 하나라도 1이면 1이 되는 OR 결합, 즉 AB+BCin+ACin이다."
  optionExplanations={['정답이다. 두 쌍 이상이 동시에 1이면 캐리가 발생하는 조건을 정확히 표현한다', '이는 합(S)을 구하는 식이며 캐리 출력식이 아니다', 'AND로 모두 연결하면 세 조건이 동시에 참이어야만 캐리가 발생하게 되어 실제 동작과 다르다', '단순 OR로는 입력 하나만 1이어도 캐리가 발생한 것처럼 나와 실제 캐리 발생 조건과 맞지 않는다']}
/>

<Quiz
  questionNumber={15}
  question="8비트 2의 보수 체계에서 25-40을 감산기(2의 보수를 이용한 뺄셈)로 계산한 결과는?"
  options={['11110001(-15)', '00001111(15)', '11101111(-17)', '00010101(21)']}
  correctAnswer={1}
  explanation="25=00011001이고 40의 2의 보수는 11011000이다. 두 값을 더하면 00011001+11011000=11110001이며, 이를 다시 2의 보수로 해석하면 -15가 되어 25-40=-15와 일치한다."
  optionExplanations={['정답이다. 25에 -40(2의 보수)을 더한 결과가 -15와 일치한다', '부호 비트를 반영하지 않고 절댓값 차이만 취했을 때 나오는 값이다', '40의 2의 보수를 구하는 과정에서 자리올림을 잘못 처리했을 때 나오는 값에 가깝다', '뺄셈이 아니라 두 수를 그대로 더했을 때 나오는 값과 유사하다']}
/>

<Quiz
  questionNumber={16}
  question="4비트 크기 비교기에 A=1010, B=1001이 입력되었을 때 옳은 출력은?"
  options={['A>B', 'A<B', 'A=B', '판단할 수 없다']}
  correctAnswer={1}
  explanation="A=1010은 십진수 10, B=1001은 십진수 9이므로 A가 B보다 크다."
  optionExplanations={['정답이다. 10>9이므로 A가 B보다 크다', 'A가 B보다 작다는 결론은 실제 크기 비교 결과와 반대된다', '두 값의 비트 패턴이 다르므로 두 값이 같다는 결론은 틀렸다', '4비트 크기 비교기는 두 입력의 대소를 항상 명확히 판단할 수 있다']}
/>

<Quiz
  questionNumber={17}
  question="3-to-8 디코더에 101이 입력되었을 때 활성화되는 출력선은?"
  options={['Y3', 'Y5', 'Y6', 'Y7']}
  correctAnswer={2}
  explanation="입력 101을 십진수로 바꾸면 5이므로, 그 값에 대응하는 출력선 Y5가 활성화된다."
  optionExplanations={['101은 십진수로 5이며 3이 아니다', '정답이다. 101=5이므로 Y5가 활성화된다', '110(십진수 6)이 입력되었을 때 활성화되는 출력선이다', '111(십진수 7)이 입력되었을 때 활성화되는 출력선이다']}
/>

<Quiz
  questionNumber={18}
  question="4-to-1 멀티플렉서에서 선택선 S1S0=10일 때 출력되는 입력은?"
  options={['I0', 'I1', 'I2', 'I3']}
  correctAnswer={3}
  explanation="선택선 S1S0=10을 십진수로 바꾸면 2이므로, 그 값에 대응하는 입력 I2가 출력으로 선택된다."
  optionExplanations={['S1S0=00일 때 선택되는 입력이다', 'S1S0=01일 때 선택되는 입력이다', '정답이다. S1S0=10은 십진수 2이므로 I2가 선택된다', 'S1S0=11일 때 선택되는 입력이다']}
/>

<Quiz
  questionNumber={19}
  question="주소선이 10개, 데이터선이 4개인 ROM의 용량을 옳게 나타낸 것은?"
  options={['1K x 4', '4K x 1', '1K x 8', '512 x 4']}
  correctAnswer={1}
  explanation="주소선 10개이므로 워드 개수는 2^10=1024=1K개이고, 데이터선 4개이므로 워드 길이는 4비트다. 따라서 용량은 1K x 4다."
  optionExplanations={['정답이다. 2^10=1K 워드, 워드 길이 4비트에 정확히 대응한다', '워드 개수와 워드 길이가 서로 뒤바뀐 값이다', '데이터선 개수를 8개로 착각했을 때 나오는 값이다', '주소선 개수를 9개로 착각했을 때 나오는 워드 개수(512)다']}
/>

<Quiz
  questionNumber={20}
  question="SR 래치에서 S=0, R=0일 때의 출력 상태는?"
  options={['0으로 리셋', '1로 세트', '이전 상태를 그대로 유지', '부정(정의되지 않음)']}
  correctAnswer={3}
  explanation="S=0, R=0은 SR 래치에서 아무 동작도 요청하지 않는 조합이므로, 이전에 저장되어 있던 상태를 그대로 유지한다."
  optionExplanations={['R=1일 때의 동작이며 R=0인 이 경우와 다르다', 'S=1일 때의 동작이며 S=0인 이 경우와 다르다', '정답이다. S=R=0은 상태 유지 조건이다', 'S=1, R=1일 때의 금지 조건에 대한 설명이며 S=R=0과는 다르다']}
/>

<Quiz
  questionNumber={21}
  question="JK 플립플롭에서 J=1, K=1일 때 다음 상태 Q(t+1)로 옳은 것은?"
  options={['0', '1', '이전 상태 Q(t) 그대로 유지', '이전 상태 Q(t)의 반전(토글)']}
  correctAnswer={4}
  explanation="JK 플립플롭은 J=1, K=1일 때 토글(현재 상태를 반전)하도록 정의되어 있다."
  optionExplanations={['J=0, K=1일 때의 결과다', 'J=1, K=0일 때의 결과다', 'J=0, K=0일 때의 결과다', '정답이다. J=K=1은 토글 동작에 해당한다']}
/>

<Quiz
  questionNumber={22}
  question="순차회로의 Mealy 모델과 Moore 모델의 차이에 대한 설명으로 옳은 것은?"
  options={['Moore는 출력이 현재 상태에만 의존하고, Mealy는 현재 상태와 입력 모두에 의존한다', 'Moore는 입력에만 의존하고 Mealy는 상태에만 의존한다', 'Moore와 Mealy는 출력을 결정하는 방식이 완전히 동일하다', 'Moore는 클록이 없고 Mealy는 클록이 있다']}
  correctAnswer={1}
  explanation="Moore 머신은 출력이 오직 현재 상태에 의해서만 결정되고, Mealy 머신은 현재 상태뿐 아니라 그 순간의 입력값에도 함께 의존해 출력이 결정된다."
  optionExplanations={['정답이다. 출력이 상태에만 의존하는지, 상태와 입력 모두에 의존하는지가 두 모델의 핵심 차이다', '두 모델의 의존 관계 설명이 서로 뒤바뀌었다', '두 모델은 출력 결정 방식이 근본적으로 다르므로 동일하다는 설명은 틀렸다', '두 모델 모두 순차회로로서 클록 신호를 사용해 상태를 갱신한다']}
/>

<Quiz
  questionNumber={23}
  question="0부터 5까지 6개의 상태를 순환하는 mod-6 카운터를 만들 때 필요한 최소 플립플롭 개수는?"
  options={['2개', '3개', '4개', '6개']}
  correctAnswer={2}
  explanation="6개의 상태를 표현하려면 2^n &gt;= 6을 만족하는 최소 n이 필요하다. 2비트로는 2^2=4개 상태밖에 만들 수 없어 부족하고, 3비트면 2^3=8개 상태로 6개를 모두 표현할 수 있다."
  optionExplanations={['2비트로는 최대 4개 상태만 표현 가능해 6개 상태를 모두 담을 수 없다', '정답이다. 3비트면 8개 상태 중 6개를 정상 경로로, 2개를 비정상 상태로 쓸 수 있다', '3비트로 이미 충분하므로 4비트는 필요 이상이다', '6은 필요한 상태의 개수이지 필요한 비트(플립플롭) 수가 아니다']}
/>

<Quiz
  questionNumber={24}
  question="주소선이 11개, 데이터선이 8개인 기억장치의 용량을 옳게 나타낸 것은?"
  options={['2K x 8', '1K x 8', '2K x 4', '4K x 8']}
  correctAnswer={1}
  explanation="주소선 11개이므로 워드 개수는 2^11=2048=2K개이고, 데이터선 8개이므로 워드 길이는 8비트다. 따라서 용량은 2K x 8이다."
  optionExplanations={['정답이다. 2^11=2K 워드, 워드 길이 8비트에 정확히 대응한다', '주소선을 10개로 착각했을 때 나오는 워드 개수(1K)다', '데이터선 개수를 4개로 착각했을 때 나오는 값이다', '주소선을 12개로 착각했을 때 나오는 워드 개수(4K)다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 - 전공분야/과목 시험안내](https://bdes.nile.or.kr/nile/exam/nExam2_1.do)
- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
- [국가평생교육진흥원 독학학위제 - 출제방향/수준](https://bdes.nile.or.kr/nile/study/nStudy1_1.do)
