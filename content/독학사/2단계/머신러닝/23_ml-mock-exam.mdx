---
title: '23. 독학사 2단계 머신러닝 예상기출모의고사'
description: '독학사 2단계 머신러닝 전 범위(01~20편)를 균형 있게 담은 24문항 모의고사, 개념·계산·비교 유형 종합'
---

<Callout type="warning">
  **이 모의고사는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학사 2단계 머신러닝 출제기준(평가영역)을 바탕으로 새로 구성한 연습 문제다. 실제 회차의 문항 수·배점·시험 시간은 매 시행 공고마다 다를 수 있으므로 공식 공고를 반드시 확인해야 한다. **권장 풀이 시간은 약 35분**이다(선택형 문항당 약 1.2분 기준으로 24문항을 계산하고, 계산형 문항의 여유 시간과 마킹·재검토 시간을 더했다). 실전처럼 시간을 재고 먼저 풀어본 뒤 해설과 대조하는 것을 권장한다.
</Callout>

이 편은 01편부터 20편까지 다룬 전 범위를 개념 확인형·계산형·비교형 문제로 고르게 섞어 구성한 종합 모의고사다. 21~23편에서 유형별로 집중 훈련했다면, 이 편에서는 실제 시험처럼 유형이 뒤섞인 상태로 풀어 보는 것이 핵심이다.

## 문항 배분

| 영역 | 다루는 편 | 문항 수 | 문항 번호 |
|------|-----------|---------|-----------|
| 기초 개념·용어 | 01–03 | 3 | 1–3 |
| 전처리·학습 패러다임 | 06–07 | 2 | 4–5 |
| 회귀·정규화 계산 | 08–09 | 3 | 6–8 |
| 로지스틱 회귀 | 10 | 2 | 9–10 |
| 트리·나이브베이즈·거리 기반 분류 | 11–12 | 4 | 11–14 |
| 앙상블 | 13 | 1 | 15 |
| 비지도학습·연관 규칙 | 14–15 | 2 | 16–17 |
| 차원 축소 | 16–17 | 2 | 18–19 |
| 평가 지표 | 18 | 2 | 20–21 |
| 모델 선택·신경망 | 19–20 | 3 | 22–24 |
| 합계 | - | 24 | 1–24 |

## 선행 개념 점검 (1–3번)

<Quiz
  questionNumber={1}
  question="어떤 회사가 과거 3년간의 '집 면적, 방 개수, 준공연도, 실제 판매가격' 데이터를 이용해 새 매물의 판매가격을 예측하는 모델을 만들려고 한다. 이 문제는 어떤 학습 패러다임에 해당하는가?"
  options={['비지도학습(unsupervised learning)', '지도학습(supervised learning) 중 회귀', '지도학습(supervised learning) 중 분류', '강화학습(reinforcement learning)']}
  correctAnswer={2}
  explanation="이 문제는 '집 면적, 방 개수, 준공연도'라는 입력(특징)과 '실제 판매가격'이라는 정답(레이블)이 쌍으로 주어져 있고, 예측 대상인 판매가격이 연속적인 수치이므로 지도학습 중에서도 회귀(regression) 문제에 해당한다."
  optionExplanations={['정답 레이블(판매가격)이 주어져 있으므로 레이블 없이 구조를 찾는 비지도학습이 아니다.', '정답이다. 레이블(가격)이 있고 예측 대상이 연속적인 수치이므로 지도학습의 회귀 문제다.', '예측 대상이 몇 개의 범주(클래스)가 아니라 연속적인 수치(가격)이므로 분류가 아니라 회귀다.', '보상을 통한 행동 학습 구조가 없으므로 강화학습이 아니다.']}
/>

<Quiz
  questionNumber={2}
  question="머신러닝 데이터셋을 구성하는 '특징 행렬(feature matrix)'과 '타깃 벡터(target vector)'에 대한 설명으로 옳은 것은?"
  options={['특징 행렬은 예측하려는 정답 값들을 모아 놓은 것이고, 타깃 벡터는 입력 변수들을 모아 놓은 것이다', '특징 행렬은 각 행이 하나의 샘플, 각 열이 하나의 입력 변수(특징)로 이루어진 표이고, 타깃 벡터는 각 샘플에 대응하는 정답(레이블) 값들의 모음이다', '특징 행렬과 타깃 벡터는 항상 같은 크기의 정사각 행렬이어야 한다', '비지도학습에서는 특징 행렬 없이 타깃 벡터만 사용한다']}
  correctAnswer={2}
  explanation="특징 행렬(feature matrix, 흔히 X로 표기)은 행이 샘플(데이터 하나하나), 열이 특징(입력 변수)으로 구성된 2차원 표이며, 타깃 벡터(target vector, 흔히 y로 표기)는 각 샘플에 대응하는 정답 값들을 순서대로 모아 놓은 1차원 배열이다. 지도학습은 이 X와 y의 쌍으로 학습한다."
  optionExplanations={['특징 행렬과 타깃 벡터의 역할이 서로 뒤바뀐 설명이다.', '정답이다. 특징 행렬은 입력 변수들의 표, 타깃 벡터는 정답값들의 모음이다.', '특징 행렬은 샘플 수 × 특징 수 형태의 직사각 행렬인 경우가 대부분이며, 정사각 행렬일 필요는 없다.', '비지도학습은 정답(타깃)이 없는 특징 행렬만 사용해 구조를 찾는 학습 방식이다.']}
/>

<Quiz
  questionNumber={3}
  question="두 특징 벡터 사이의 코사인 유사도(cosine similarity)가 1에 가깝다는 것은 무엇을 의미하는가?"
  options={['두 벡터의 크기(길이)가 거의 같다는 뜻이다', '두 벡터가 가리키는 방향이 거의 같다는 뜻이다', '두 벡터가 서로 정확히 반대 방향을 가리킨다는 뜻이다', '두 벡터의 모든 성분 값이 0이라는 뜻이다']}
  correctAnswer={2}
  explanation="코사인 유사도는 두 벡터 사이의 각도에 기반한 지표로, 벡터의 길이(크기)와는 무관하게 방향이 얼마나 비슷한지를 나타낸다. 값이 1에 가까우면 두 벡터의 방향이 거의 일치한다는 뜻이고, 0이면 직교(수직), -1에 가까우면 방향이 거의 반대라는 뜻이다."
  optionExplanations={['코사인 유사도는 벡터의 크기(노름)를 나눠서 정규화한 값이므로 크기가 아니라 방향의 유사성을 나타낸다.', '정답이다. 코사인 유사도가 1에 가까우면 두 벡터의 방향이 거의 같다는 뜻이다.', '방향이 정확히 반대이면 코사인 유사도는 1이 아니라 -1에 가깝다.', '모든 성분이 0인 벡터는 크기가 0이 되어 코사인 유사도 자체를 정의할 수 없는 경우다.']}
/>

## 전처리·학습 패러다임 (4–5번)

<Quiz
  questionNumber={4}
  question="온라인 쇼핑몰이 회원 탈퇴 여부를 예측하는 모델을 만들 때, 전체 회원의 2퍼센트만 탈퇴자이고 98퍼센트는 유지 회원이었다. 이런 데이터 불균형 상황에서 취할 수 있는 대응으로 가장 적절한 것은?"
  options={['탈퇴자 데이터를 오버샘플링하거나 유지 회원 데이터를 언더샘플링해 클래스 비율을 조정하고, 정확도 대신 정밀도·재현율·F1 등의 지표로 평가한다', '정확도만 확인하면 충분하므로 별도의 조정 없이 그대로 학습한다', '탈퇴자 데이터를 전부 삭제하고 유지 회원 데이터만으로 학습한다', '입력 변수를 하나로 줄여서 불균형 문제를 해결한다']}
  correctAnswer={1}
  explanation="탈퇴자가 2퍼센트뿐인 심한 불균형 상황에서는 오버샘플링(소수 클래스 복제·합성)이나 언더샘플링(다수 클래스 축소)으로 클래스 비율을 조정하는 것이 일반적인 대응이며, 평가할 때도 모든 회원을 유지로만 예측해도 98퍼센트가 나오는 정확도 대신 정밀도·재현율·F1처럼 소수 클래스 성능을 드러내는 지표를 함께 봐야 한다."
  optionExplanations={['정답이다. 샘플링으로 비율을 조정하고 정확도 이외의 지표로 평가하는 것이 표준적인 불균형 대응이다.', '불균형 상황에서 정확도만 보면 소수 클래스(탈퇴자) 예측 성능을 전혀 확인할 수 없어 부적절하다.', '예측하려는 대상인 탈퇴자 데이터를 삭제하면 애초에 탈퇴 여부를 학습할 수 없게 된다.', '입력 변수 수를 줄이는 것은 차원 축소와 관련된 별개의 문제이며, 클래스 불균형을 해결하지 못한다.']}
/>

<Quiz
  questionNumber={5}
  question="레이블이 없는 고객 구매 데이터를 이용해, 비슷한 구매 패턴을 보이는 고객들끼리 몇 개의 그룹으로 자동으로 묶으려고 한다. 이는 어떤 학습 패러다임에 해당하는가?"
  options={['지도학습 중 회귀', '지도학습 중 분류', '비지도학습 중 군집화(clustering)', '강화학습']}
  correctAnswer={3}
  explanation="이 문제는 정답 레이블(예: '이 고객은 A그룹') 없이, 데이터 자체의 유사성만으로 비슷한 것끼리 묶는 작업이므로 비지도학습에 속하며, 그중에서도 데이터를 몇 개의 군집(cluster)으로 나누는 군집화(clustering) 문제에 해당한다."
  optionExplanations={['회귀는 레이블(정답 수치)이 있는 지도학습 문제이므로 레이블 없는 이 상황과 맞지 않는다.', '분류도 레이블(정답 범주)이 있는 지도학습 문제이므로 맞지 않는다.', '정답이다. 레이블 없이 유사한 데이터끼리 그룹화하는 것은 비지도학습의 군집화 문제다.', '보상을 통한 행동 학습 구조가 없으므로 강화학습이 아니다.']}
/>

## 회귀·정규화 계산 (6–8번)

<Quiz
  questionNumber={6}
  question="데이터 x=(0,1,2,3), y=(1,4,7,10)에 단순 선형회귀 직선 y=ax+b를 맞춰 기울기 a=3, 절편 b=1을 얻었다. x=5일 때의 예측값은?"
  options={['15', '16', '17', '18']}
  correctAnswer={2}
  explanation="회귀식 y=ax+b에 a=3, b=1, x=5를 대입한다. y=3×5+1=15+1=16이다. 원본 데이터에서도 x가 1씩 늘 때 y가 3씩 늘어나는 패턴(y=3x+1)이 그대로 유지되므로, x=3일 때 y=10인 흐름을 이어가면 x=4일 때 13, x=5일 때 16이 되어 계산이 일치한다."
  optionExplanations={['이것은 절편 1을 더하지 않고 3×5-1을 계산하는 등 절편 부호를 잘못 처리한 값이다.', '정답이다. 3×5+1=16이다.', '이것은 x=5가 아니라 x=5.33 근처를 대입한 값에 가깝다.', '이것은 16에 절편 1을 한 번 더 더한 계산 실수다.']}
/>

<Quiz
  questionNumber={7}
  question="릿지 회귀에서 정규화 이전 잔차제곱합(RSS)이 8, 회귀계수가 β=(1, 2, -2), 정규화 강도 λ=1일 때, 릿지의 전체 목적함수 값(RSS + L2 페널티)은?"
  options={['13', '14', '16', '17']}
  correctAnswer={4}
  explanation="릿지 회귀의 목적함수는 RSS + λ×Σβ²이다. 먼저 계수 제곱의 합을 구하면 1²+2²+(-2)²=1+4+4=9이다. λ=1이므로 페널티는 1×9=9이다. RSS 8과 더하면 전체 목적함수 값은 8+9=17이다."
  optionExplanations={['이것은 계수 제곱의 합을 9가 아니라 5로 잘못 계산한 경우다(계수 하나를 절댓값으로 처리하는 등의 실수).', '이것은 페널티를 9가 아니라 6으로 잘못 계산한 값이다.', '이것은 λ를 곱하기 전 계수 제곱의 합(9)만 RSS 8과 더하지 않고 그대로 둔 뒤 다른 값을 더한 계산 실수다.', '정답이다. RSS 8에 페널티 1×9=9를 더하면 17이다.']}
/>

<Quiz
  questionNumber={8}
  question="같은 회귀계수 β=(1, 2, -2), 정규화 강도 λ=1을 라쏘 회귀(L1 정규화)에 적용할 때, L1 페널티 값(λ×Σ|β|)은?"
  options={['3', '4', '5', '9']}
  correctAnswer={3}
  explanation="라쏘 회귀의 L1 페널티는 λ×Σ|β|이다. 계수 절댓값의 합은 |1|+|2|+|-2|=1+2+2=5이다. λ=1이므로 L1 페널티는 1×5=5이다."
  optionExplanations={['이것은 계수 하나(-2)를 빠뜨리고 |1|+|2|=3만 계산한 값이다.', '이것은 |1|+|-2|=3에 다른 값을 더 뺀 계산 실수에 가깝다.', '정답이다. 1×(1+2+2)=5이다.', '이것은 릿지의 L2 페널티 값(제곱합 9에 λ=1을 곱한 값)과 혼동한 값이다.']}
/>

## 로지스틱 회귀 (9–10번)

<Quiz
  questionNumber={9}
  question="로지스틱 회귀에서 선형결합 값 z=-1일 때, 시그모이드 함수 σ(z)=1/(1+e^(-z))의 값은 약 얼마인가? (e≈2.718)"
  options={['약 0.12', '약 0.27', '약 0.50', '약 0.73']}
  correctAnswer={2}
  explanation="σ(z)=1/(1+e^(-z))에 z=-1을 대입하면 σ(-1)=1/(1+e^(1))이다. e^1≈2.718이므로 분모는 1+2.718=3.718이고, σ(-1)=1/3.718≈0.269이다. 반올림하면 약 0.27이다."
  optionExplanations={['이것은 e^(-1)≈0.368을 그대로 답으로 낸 계산 중단이다.', '정답이다. 1/(1+2.718)≈0.269로 약 0.27이다.', '이것은 z=0일 때의 값(0.5)으로 z=-1을 대입하지 않은 경우다.', '이것은 z=1일 때의 값(약 0.731)으로 부호를 반대로 대입한 경우다.']}
/>

<Quiz
  questionNumber={10}
  question="로지스틱 회귀에서 예측 확률 σ(z)에 임계값 0.5를 적용해 클래스를 나눌 때, σ(z)=0.5가 되는 지점(z=0)이 갖는 의미로 가장 적절한 것은?"
  options={['모델이 두 클래스 중 어느 쪽인지 전혀 예측하지 못하는 오류 상태를 의미한다', '결정 경계(decision boundary), 즉 이 지점을 기준으로 클래스 예측이 갈리는 경계를 의미한다', '해당 데이터가 반드시 이상치(outlier)라는 의미다', '이 지점에서는 로지스틱 회귀가 회귀 문제로 바뀐다는 의미다']}
  correctAnswer={2}
  explanation="z=0일 때 시그모이드 값은 정확히 0.5가 되며, 이는 두 클래스 중 어느 쪽으로 분류할지가 갈리는 경계 지점이다. 이를 결정 경계(decision boundary)라고 하며, z가 0보다 크면 확률이 0.5를 넘어 한쪽 클래스로, z가 0보다 작으면 반대쪽 클래스로 분류된다."
  optionExplanations={['확률이 정확히 0.5라는 것은 오류가 아니라 두 클래스 확률이 동일하게 나뉘는 경계 상태를 뜻한다.', '정답이다. σ(z)=0.5(z=0)는 클래스 예측이 갈리는 결정 경계를 의미한다.', '결정 경계에 위치한 데이터가 반드시 이상치라는 근거는 없다.', '로지스틱 회귀는 z 값과 관계없이 항상 분류를 위한 확률을 출력하는 모델이며, 특정 지점에서 회귀 문제로 바뀌지 않는다.']}
/>

## 트리·나이브베이즈·거리 기반 분류 (11–14번)

<Quiz
  questionNumber={11}
  question="어떤 노드에 양성 12개, 음성 4개(총 16개)가 섞여 있을 때, 이 노드의 지니 지수(Gini index)는?"
  options={['0.25', '0.375', '0.5', '0.75']}
  correctAnswer={2}
  explanation="지니 지수 공식은 Gini=1-Σp²이다. 양성 비율 p₊=12/16=0.75, 음성 비율 p₋=4/16=0.25를 대입하면 p₊²=0.5625, p₋²=0.0625이고, 둘을 더하면 0.625이다. 1에서 빼면 Gini=1-0.625=0.375이다."
  optionExplanations={['이것은 음성 비율(0.25) 자체를 지니 지수로 착각한 값이다.', '정답이다. 1-(0.75²+0.25²)=1-0.625=0.375이다.', '이것은 1에서 빼기 전 Σp² 값(0.625)의 절반 정도로 어림한 값이다.', '이것은 양성 비율(0.75) 자체를 지니 지수로 착각한 값이다.']}
/>

<Quiz
  questionNumber={12}
  question="스팸 메일 분류에서 나이브 베이즈(Naive Bayes)가 특히 널리 쓰이는 이유로 가장 적절한 것은?"
  options={['단어들 사이의 문법적 상호작용을 가장 정교하게 반영하는 알고리즘이기 때문이다', '조건부 독립 가정 덕분에 계산이 단순하고 빠르며, 단어 등장 여부 같은 특징이 매우 많은 텍스트 데이터에서도 비교적 잘 동작하기 때문이다', '레이블이 전혀 없는 데이터에서도 스팸 여부를 구분할 수 있기 때문이다', '항상 결정트리나 SVM보다 정확도가 높기 때문이다']}
  correctAnswer={2}
  explanation="나이브 베이즈는 단어(특징)들이 클래스가 주어졌을 때 서로 조건부 독립이라는 단순화된 가정을 사용하기 때문에, 특징이 수천 개(단어 종류)에 달하는 텍스트 데이터에서도 계산이 빠르고 적은 데이터로도 비교적 안정적인 성능을 낸다. 이 실용적 장점이 스팸 분류 등 텍스트 분류에서 널리 쓰이는 이유다."
  optionExplanations={['조건부 독립을 가정하기 때문에 오히려 단어 간 상호작용은 정교하게 반영하지 못한다는 것이 나이브 베이즈의 한계다.', '정답이다. 조건부 독립 가정에 따른 계산 단순성과 텍스트처럼 특징이 많은 데이터에서의 실용성이 핵심 이유다.', '나이브 베이즈는 스팸/정상이라는 레이블이 있는 지도학습 알고리즘으로, 레이블 없이 학습할 수 없다.', '알고리즘 간 정확도 우열은 데이터 특성에 따라 달라지므로 항상 더 높다고 단정할 수 없다.']}
/>

<Quiz
  questionNumber={13}
  question="2차원 점 A(0, 0)와 B(6, 8) 사이의 유클리드 거리는?"
  options={['8', '10', '14', '48']}
  correctAnswer={2}
  explanation="유클리드 거리 공식은 √((x₂-x₁)²+(y₂-y₁)²)이다. x좌표 차이는 6-0=6, y좌표 차이는 8-0=8이다. 제곱해 더하면 6²+8²=36+64=100이고, 제곱근을 씌우면 √100=10이다."
  optionExplanations={['이것은 y좌표 차이(8)만 답으로 낸 계산 누락이다.', '정답이다. √(6²+8²)=√100=10이다.', '이것은 x좌표 차이와 y좌표 차이를 단순히 더한 맨해튼 거리(6+8=14)와 혼동한 값이다.', '이것은 제곱근을 씌우기 전의 제곱합(36+64=100)에 가까운 값을 잘못 어림한 것이다.']}
/>

<Quiz
  questionNumber={14}
  question="SVM에서 선형으로 분리되지 않는 데이터를 다루기 위해 커널(kernel) 함수를 사용하는 목적으로 가장 적절한 것은?"
  options={['데이터의 결측치를 자동으로 채우기 위해서다', '데이터를 더 높은 차원의 공간으로 매핑해, 원래 공간에서는 직선으로 나눌 수 없던 데이터를 새로운 공간에서 선형으로 분리할 수 있게 하기 위해서다', '데이터의 클래스 수를 줄이기 위해서다', '훈련 데이터의 개수를 늘리기 위해서다']}
  correctAnswer={2}
  explanation="커널 함수는 원래 공간에서 직선(또는 초평면)으로 나눌 수 없는 데이터를, 계산상으로 더 높은 차원의 공간에 매핑한 것과 같은 효과를 내어 그 공간에서는 선형으로 분리할 수 있게 만들어 주는 기법이다. 대표적으로 RBF(가우시안) 커널, 다항 커널 등이 있다."
  optionExplanations={['커널 함수는 결측치 처리와 관련이 없는, 데이터 공간을 변환하는 기법이다.', '정답이다. 더 높은 차원으로 매핑해 선형 분리가 가능하게 만드는 것이 커널의 목적이다.', '커널 함수는 클래스 수를 줄이는 기법이 아니라 데이터의 표현 공간을 바꾸는 기법이다.', '커널 함수는 기존 데이터의 표현 방식을 바꾸는 것이지, 데이터 개수 자체를 늘리는 기법이 아니다.']}
/>

## 앙상블 (15번)

<Quiz
  questionNumber={15}
  question="부스팅(boosting)에 대한 설명으로 옳은 것은?"
  options={['모든 개별 모델을 동시에 병렬로 학습시킨 뒤 평균을 낸다', '이전 모델이 잘못 예측한 데이터에 더 큰 가중치를 부여하며 다음 모델을 순차적으로 추가해 나간다', '결정트리를 전혀 사용할 수 없는 앙상블 기법이다', '항상 배깅보다 계산 속도가 빠르다']}
  correctAnswer={2}
  explanation="부스팅은 여러 개의 약한 학습기(weak learner)를 순차적으로 학습시키되, 이전 모델이 틀리게 예측한 데이터에 더 큰 가중치를 부여해 다음 모델이 그 부분을 더 잘 맞히도록 유도하는 방식이다. AdaBoost, Gradient Boosting 등이 대표적인 예다."
  optionExplanations={['모델을 병렬로 학습시켜 평균을 내는 것은 부스팅이 아니라 배깅에 대한 설명이다.', '정답이다. 오분류된 데이터에 가중치를 주며 순차적으로 모델을 추가하는 것이 부스팅의 핵심이다.', '부스팅은 결정트리를 개별 약한 학습기로 자주 사용하는 대표적인 앙상블 기법이다.', '부스팅은 이전 모델의 결과를 기다려야 다음 모델을 학습할 수 있어 순차적이므로, 병렬화가 쉬운 배깅보다 일반적으로 더 오래 걸리는 경우가 많다.']}
/>

## 비지도학습·연관 규칙 (16–17번)

<Quiz
  questionNumber={16}
  question="DBSCAN에서 하이퍼파라미터 eps(반경)와 min_samples(최소 이웃 수)가 하는 역할로 가장 적절한 것은?"
  options={['eps와 min_samples는 군집의 개수를 직접 지정하는 값이다', 'eps는 이웃으로 인정할 거리 범위를, min_samples는 하나의 밀집 영역(군집)으로 인정하기 위해 그 반경 안에 있어야 할 최소 데이터 개수를 정의한다', 'eps와 min_samples는 결정트리의 분할 기준을 정하는 값이다', 'eps는 학습률을, min_samples는 배치 크기를 의미한다']}
  correctAnswer={2}
  explanation="DBSCAN은 밀도 기반 군집화 알고리즘으로, eps(epsilon)는 한 점을 기준으로 이웃이라고 볼 반경(거리)을 정의하고, min_samples는 그 반경 안에 적어도 몇 개의 점이 있어야 그 지역을 밀집된 군집의 일부로 볼지를 정의한다. 이 두 값의 조합에 따라 군집 수와 이상치 판정 결과가 달라진다."
  optionExplanations={['DBSCAN은 군집 개수를 사람이 직접 지정하지 않고, eps와 min_samples라는 밀도 기준으로 결과적으로 몇 개의 군집이 나올지 결정된다.', '정답이다. eps는 이웃 반경, min_samples는 군집으로 인정할 최소 이웃 수를 의미한다.', '결정트리의 분할 기준은 지니 지수·엔트로피이며 DBSCAN의 하이퍼파라미터와는 관련이 없다.', '학습률과 배치 크기는 경사하강법 기반 학습(신경망 등)에서 쓰이는 개념으로 DBSCAN과는 관련이 없다.']}
/>

<Quiz
  questionNumber={17}
  question="전체 거래 100건 중 항목 A와 B가 함께 나타난 거래가 20건이고, 항목 A가 포함된 전체 거래가 40건일 때, 연관 규칙 'A이면 B이다'의 신뢰도(confidence)는?"
  options={['0.2', '0.4', '0.5', '0.8']}
  correctAnswer={3}
  explanation="신뢰도(confidence)는 'A를 포함한 거래 중 B도 함께 포함된 거래의 비율'로, 공식은 (A와 B가 함께 나타난 거래 수)/(A가 포함된 거래 수)이다. 20/40=0.5이므로 신뢰도는 0.5, 즉 50퍼센트다."
  optionExplanations={['이것은 지지도(support) 계산 공식인 20/100=0.2를 신뢰도로 착각한 값이다.', '이것은 40을 100으로 나눈 값(0.4, A의 지지도)과 혼동한 값이다.', '정답이다. 20/40=0.5이다.', '이것은 20/25처럼 분모를 잘못 잡은 계산 실수다.']}
/>

## 차원 축소 (18–19번)

<Quiz
  questionNumber={18}
  question="고차원 데이터에서 나타나는 '차원의 저주(curse of dimensionality)'에 대한 설명으로 옳은 것은?"
  options={['차원이 늘어날수록 데이터 포인트들 사이의 거리가 상대적으로 비슷해지고 데이터가 희소(sparse)해져, 거리 기반 알고리즘의 성능이 떨어지기 쉽다', '차원이 늘어날수록 모델의 학습 속도가 항상 빨라진다', '차원의 저주는 오직 이미지 데이터에서만 발생하는 현상이다', '차원이 늘어나면 데이터가 항상 더 촘촘하게 채워진다']}
  correctAnswer={1}
  explanation="차원(특징의 개수)이 늘어날수록, 같은 양의 데이터라도 전체 공간에서 점들이 차지하는 비중이 작아져 점들 사이가 상대적으로 멀어지고 데이터가 희소해진다. 이로 인해 거리(유클리드 거리 등)에 기반해 유사도를 판단하는 K-NN 같은 알고리즘의 성능이 떨어지기 쉬운 현상을 차원의 저주라고 부른다."
  optionExplanations={['정답이다. 차원이 늘수록 데이터가 희소해지고 거리 기반 알고리즘의 성능이 떨어지기 쉬운 것이 차원의 저주다.', '차원이 늘어나면 계산해야 할 특징이 많아져 오히려 학습 속도가 느려지는 경우가 많다.', '차원의 저주는 이미지뿐 아니라 특징이 많은 모든 유형의 데이터(텍스트, 유전체 데이터 등)에서 나타날 수 있는 일반적인 현상이다.', '차원이 늘어나면 같은 데이터 양으로는 오히려 공간이 더 희소해지는 것이지, 촘촘해지는 것이 아니다.']}
/>

<Quiz
  questionNumber={19}
  question="PCA에서 각 주성분의 고유값이 6, 3, 1일 때, 첫 번째 주성분(PC1)이 설명하는 분산의 비율은?"
  options={['10%', '30%', '60%', '90%']}
  correctAnswer={3}
  explanation="한 주성분이 설명하는 분산 비율은 그 고유값을 전체 고유값의 합으로 나눈 값이다. 전체 합은 6+3+1=10이다. PC1의 고유값은 6이므로 비율은 6/10=0.6, 즉 60퍼센트다."
  optionExplanations={['이것은 세 번째 주성분(고유값 1)의 분산 비율(1/10=10%)이다.', '이것은 두 번째 주성분(고유값 3)의 분산 비율(3/10=30%)이다.', '정답이다. 6/10=0.6으로 60퍼센트다.', '이것은 PC1과 PC2의 고유값을 합친 값(6+3=9)의 비율(9/10=90%)로, 질문과 다른 것을 계산한 값이다.']}
/>

## 평가 지표 (20–21번)

<Quiz
  questionNumber={20}
  question="혼동행렬에서 참양성(TP)=50, 거짓양성(FP)=5, 거짓음성(FN)=15, 참음성(TN)=30일 때, 정밀도(precision)는? (소수 셋째 자리에서 반올림)"
  options={['0.769', '0.8', '0.909', '0.95']}
  correctAnswer={3}
  explanation="정밀도 공식은 TP/(TP+FP)이다. TP=50, FP=5를 대입하면 50/(50+5)=50/55≈0.909이다."
  optionExplanations={['이것은 재현율(TP/(TP+FN)=50/65≈0.769)과 혼동한 값이다.', '이것은 50/55를 0.8로 어림해 계산을 정확히 마치지 않은 값이다.', '정답이다. 50/55≈0.909이다.', '이것은 50/55가 아니라 다른 분모(약 52.6)를 사용한 것과 같은 계산 실수다.']}
/>

<Quiz
  questionNumber={21}
  question="문제 20과 같은 혼동행렬(TP=50, FP=5, FN=15, TN=30, 전체=100)에서 정확도(accuracy)는?"
  options={['0.65', '0.75', '0.8', '0.85']}
  correctAnswer={3}
  explanation="정확도 공식은 (TP+TN)/전체이다. TP=50, TN=30을 더하면 80이고, 전체 데이터 수는 50+5+15+30=100이다. 따라서 정확도는 80/100=0.8이다."
  optionExplanations={['이것은 TP만 전체의 절반 정도로 어림한 값과 혼동한 계산이다.', '이것은 TP와 TN을 더하지 않고 TN과 FN만 더한 값(30+15=45, 45/60에 가까운) 계산 실수와 유사하다.', '정답이다. (50+30)/100=80/100=0.8이다.', '이것은 정밀도(0.909)에 가깝게 반올림 자리를 착각한 값이다.']}
/>

## 모델 선택·신경망 (22–24번)

<Quiz
  questionNumber={22}
  question="k-폴드 교차검증을 사용하는 목적으로 가장 적절한 것은?"
  options={['훈련 데이터 없이 모델을 학습시키기 위해서다', '데이터를 한 번만 분할했을 때 생길 수 있는 우연성의 영향을 줄이고, 모델의 일반화 성능을 더 안정적으로 추정하기 위해서다', '모델의 하이퍼파라미터 개수를 자동으로 늘리기 위해서다', '결측치를 자동으로 채우기 위해서다']}
  correctAnswer={2}
  explanation="k-폴드 교차검증은 데이터를 k개의 폴드로 나누어 매번 다른 폴드를 검증용으로 사용하는 과정을 반복함으로써, 특정 한 번의 분할에 따라 성능 추정치가 우연히 좋거나 나쁘게 나오는 것을 방지하고 더 신뢰할 수 있는 성능 추정을 제공하는 것이 목적이다."
  optionExplanations={['교차검증도 각 반복에서 훈련 데이터를 사용해 모델을 학습시킨다.', '정답이다. 분할에 따른 우연성을 줄이고 안정적인 성능 추정치를 얻는 것이 목적이다.', '교차검증은 평가 방법이지 하이퍼파라미터 개수를 자동으로 늘리는 기법이 아니다.', '결측치 처리는 전처리 단계의 작업으로 교차검증의 목적과는 관련이 없다.']}
/>

<Quiz
  questionNumber={23}
  question="단일 퍼셉트론이 XOR 문제를 풀 수 없는 근본적인 이유로 가장 적절한 것은?"
  options={['XOR 문제는 데이터 개수가 너무 적어서 학습이 불가능하기 때문이다', 'XOR의 두 클래스는 하나의 직선(선형 결정 경계)으로 완전히 분리할 수 없는 배치를 이루기 때문이다', '단일 퍼셉트론은 활성화 함수를 전혀 사용하지 않기 때문이다', 'XOR 문제는 회귀 문제이지 분류 문제가 아니기 때문이다']}
  correctAnswer={2}
  explanation="XOR 문제는 입력 두 개가 서로 다를 때만 1, 같을 때는 0을 출력하는 논리 연산으로, 이 두 클래스를 좌표평면에 그려 보면 하나의 직선으로는 완전히 분리할 수 없는 배치(대각선 방향으로 클래스가 교차하는 형태)를 이룬다. 단일 퍼셉트론은 하나의 선형 결정 경계만 만들 수 있으므로 이런 비선형 분리 문제를 근본적으로 풀 수 없다."
  optionExplanations={['XOR 문제는 데이터 개수가 아니라 클래스 배치 자체가 선형으로 분리되지 않는다는 구조적인 한계 때문에 풀리지 않는다.', '정답이다. XOR의 클래스 배치는 하나의 직선으로 분리할 수 없는 비선형 구조이기 때문이다.', '단일 퍼셉트론도 계단 함수 등의 활성화 함수를 사용하며, 문제의 핵심은 활성화 함수의 유무가 아니라 표현할 수 있는 결정 경계가 선형뿐이라는 점이다.', 'XOR은 출력이 0 또는 1인 대표적인 이진 분류 문제이지 회귀 문제가 아니다.']}
/>

<Quiz
  questionNumber={24}
  question="퍼셉트론의 가중치 w=(0.2, 0.3), 입력 x=(1, -1), 목표값(target)=0, 현재 출력(output)=1, 학습률 η=0.2일 때, 가중치 갱신 공식 w_new=w+η×(target-output)×x를 적용한 새로운 가중치는?"
  options={['(0.0, 0.5)', '(0.4, 0.1)', '(0.2, 0.3)', '(-0.2, 0.2)']}
  correctAnswer={1}
  explanation="먼저 target-output=0-1=-1을 구한다. 갱신량은 η×(target-output)×x=0.2×(-1)×(1,-1)=(-0.2)×(1,-1)=(-0.2, 0.2)이다. 이를 기존 가중치 (0.2, 0.3)에 더하면 새로운 가중치는 (0.2-0.2, 0.3+0.2)=(0.0, 0.5)이다."
  optionExplanations={['정답이다. (0.2-0.2, 0.3+0.2)=(0.0, 0.5)이다.', '이것은 갱신량의 부호를 반대로 적용해 더한 값이다.', '이것은 갱신량을 전혀 반영하지 않은 원래 가중치 값이다.', '이것은 갱신량 자체((-0.2, 0.2))를 새로운 가중치로 착각한 값으로, 기존 가중치에 더하지 않은 계산 누락이다.']}
/>

## 채점 후 복습 동선

| 틀린 영역 | 돌아갈 편 | 확인할 핵심 |
|-----------|-----------|-------------|
| 기초 개념·용어 | 01–03편 | AI·ML·DL 관계, 학습 패러다임 구분, 특징 행렬·타깃 벡터, 벡터 연산 |
| 전처리·패러다임 | 06–07편 | 결측치·스케일링·인코딩·불균형 대응, 지도·비지도·강화 구분 |
| 회귀·정규화 | 08–09편 | 회귀식 예측, 릿지·라쏘의 페널티 계산과 차이 |
| 로지스틱 회귀 | 10편 | 시그모이드 계산, 결정 경계의 의미 |
| 트리·거리 기반 분류 | 11–12편 | 지니 지수 계산, 나이브 베이즈의 조건부 독립, 거리 계산, SVM 커널 |
| 앙상블 | 13편 | 배깅·부스팅의 학습 방식 차이 |
| 비지도·연관 규칙 | 14–15편 | DBSCAN의 eps·min_samples, 지지도·신뢰도·향상도 계산 |
| 차원 축소 | 16–17편 | 차원의 저주, PCA 분산 설명 비율 계산 |
| 평가 지표 | 18편 | 정밀도·재현율·정확도 계산과 차이 |
| 모델 선택·신경망 | 19–20편 | 교차검증의 목적, 퍼셉트론의 한계와 가중치 갱신 계산 |

전 영역에서 고르게 틀렸다면 05편(시험 구조·출제 포인트)으로 돌아가 시간 배분과 문항 유형별 접근 전략부터 다시 점검하는 것을 권장한다.

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [scikit-learn User Guide — Supervised learning, Clustering, Dimensionality reduction](https://scikit-learn.org/stable/user_guide.html)
- [scikit-learn Model Evaluation — Metrics and scoring](https://scikit-learn.org/stable/modules/model_evaluation.html)
