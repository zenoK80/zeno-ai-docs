---
title: '23. 알고리즘 비교·장단점·적용 상황 문제'
description: '독학사 2단계 머신러닝 출제기준을 기반으로 재구성한 알고리즘 비교 문제 21선, 지도·비지도·회귀·분류·앙상블의 가정과 적용 상황 대비'
---

<Callout type="warning">
  **이 문제는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학사 2단계 머신러닝 출제기준을 바탕으로 새로 구성한 비교형 연습 문제입니다. 실제 회차의 문항 수·난이도는 다를 수 있으므로 공식 시행 공고를 함께 확인해야 한다. 이 편은 10~13편(회귀·분류·앙상블 알고리즘)과 18~19편(평가 지표·모델 선택)에서 다룬 "A와 B의 차이", "어느 상황에 어떤 방법을 쓸 것인가"를 21문항으로 재구성했다.
</Callout>

독학사 머신러닝 과목은 알고리즘 이름과 정의를 아는 것만으로는 부족하다. "이 상황에는 어떤 알고리즘이 더 적절한가", "A 방법과 B 방법의 차이는 정확히 무엇인가"를 묻는 비교형 문항이 특히 많다. "옳지 않은 것은?" 유형이 섞여 있으니 문제 지시문을 끝까지 읽고 접근한다.

## 학습 패러다임·회귀 비교 (1–4번)

<Quiz
  questionNumber={1}
  question="지도학습과 비지도학습의 차이에 대한 설명으로 옳지 않은 것은?"
  options={['지도학습은 정답 레이블이 있는 데이터로 학습하고, 비지도학습은 레이블 없이 데이터의 구조를 찾는다', '회귀와 분류는 지도학습의 대표적인 문제 유형이다', '군집화(clustering)와 차원 축소(dimensionality reduction)는 비지도학습에 속한다', '지도학습은 항상 비지도학습보다 필요한 데이터의 양이 적다']}
  correctAnswer={4}
  explanation="지도학습에 필요한 데이터 양이 비지도학습보다 항상 적다고 일반화할 근거는 없다. 오히려 지도학습은 레이블이 달린 데이터를 확보해야 하므로 레이블링 비용과 별개로, 문제의 복잡도·모델 종류에 따라 필요한 데이터 양이 비지도학습보다 많을 수도 적을 수도 있다. '항상'이라는 단정적 표현이 들어간 비교는 예외 상황을 못 찾아도 의심해 봐야 한다."
  optionExplanations={['레이블 유무에 따른 지도·비지도 구분은 정확한 정의이므로 옳은 설명이다.', '회귀·분류가 지도학습의 대표 유형이라는 것은 정확한 분류이므로 옳은 설명이다.', '군집화와 차원 축소는 레이블을 사용하지 않는 비지도학습의 대표 기법이므로 옳은 설명이다.', '정답이다. 필요한 데이터 양은 문제와 모델에 따라 달라지며 항상 적다고 단정할 수 없다.']}
/>

<Quiz
  questionNumber={2}
  question="선형회귀와 로지스틱 회귀를 비교한 설명으로 옳은 것은?"
  options={['선형회귀는 연속적인 수치를 예측하고, 로지스틱 회귀는 클래스에 속할 확률을 예측해 분류에 사용한다', '두 모델 모두 출력값이 항상 0과 1 사이로 제한된다', '로지스틱 회귀는 손실함수로 평균제곱오차(MSE)를 사용하고, 선형회귀는 사용하지 않는다', '선형회귀는 분류 문제에, 로지스틱 회귀는 회귀 문제에 주로 사용된다']}
  correctAnswer={1}
  explanation="선형회귀는 입력의 가중합을 그대로 출력해 연속적인 수치(예: 집값, 온도)를 예측하는 반면, 로지스틱 회귀는 가중합을 시그모이드 함수에 통과시켜 0과 1 사이의 확률로 변환한 뒤 이를 기준으로 클래스를 분류한다. 이것이 두 알고리즘의 핵심적인 용도 차이다."
  optionExplanations={['정답이다. 선형회귀는 연속값을, 로지스틱 회귀는 확률(분류)을 예측한다.', '출력이 0과 1 사이로 제한되는 것은 로지스틱 회귀만의 특징이며, 선형회귀의 출력은 실수 전체 범위를 가진다.', '평균제곱오차(MSE)는 선형회귀의 대표적인 손실함수이며, 로지스틱 회귀는 주로 교차 엔트로피 손실을 사용한다는 점에서 설명이 뒤바뀌었다.', '선형회귀는 회귀 문제에, 로지스틱 회귀는 분류 문제에 주로 쓰인다는 점에서 용도가 뒤바뀐 설명이다.']}
/>

<Quiz
  questionNumber={3}
  question="릿지 회귀와 라쏘 회귀의 비교로 옳지 않은 것은?"
  options={['정규화 강도 λ(람다)를 0에 가깝게 줄이면 두 방법 모두 일반 선형회귀와 비슷해진다', '변수가 매우 많고 그중 일부만 실제로 중요할 것으로 예상될 때는 변수를 자동으로 제거해 주는 라쏘가 더 유리할 수 있다', '릿지는 다중공선성이 있는 상황에서 계수를 완만하게 축소해 안정시키는 데 도움이 된다', 'λ 값은 관계없이 릿지와 라쏘의 예측 성능은 항상 완전히 동일하다']}
  correctAnswer={4}
  explanation="릿지(L2)와 라쏘(L1)는 서로 다른 페널티 항을 사용하므로, 데이터의 특성(변수 간 상관관계, 실제로 중요한 변수의 개수 등)에 따라 두 모델의 예측 성능이 달라질 수 있다. 'λ 값과 관계없이 항상 완전히 동일하다'는 서술은 두 정규화 방식의 차이 자체를 부정하는 잘못된 일반화다."
  optionExplanations={['λ를 0에 가깝게 줄이면 정규화 페널티의 영향이 사라져 일반 선형회귀와 비슷해진다는 것은 옳은 설명이다.', '중요 변수만 남기고 나머지 계수를 0으로 만드는 변수 선택 효과는 라쏘의 장점이므로 옳은 설명이다.', '릿지는 계수를 완만히 줄여 다중공선성으로 인한 불안정한 추정을 완화하는 데 도움이 되므로 옳은 설명이다.', '정답이다. 두 모델의 정규화 방식이 다르므로 데이터에 따라 성능이 달라질 수 있어, 항상 동일하다는 서술은 틀렸다.']}
/>

<Quiz
  questionNumber={4}
  question="다항 회귀(polynomial regression)와 단순 선형회귀를 비교한 설명으로 옳은 것은?"
  options={['다항 회귀는 입력 변수의 거듭제곱 항을 추가해 곡선 형태의 관계까지 표현할 수 있지만, 차수가 너무 높으면 과적합 위험이 커진다', '다항 회귀는 회귀계수를 전혀 사용하지 않는다', '단순 선형회귀는 곡선 관계도 추가 변형 없이 그대로 표현할 수 있다', '다항 회귀의 차수를 높일수록 항상 검증 데이터에 대한 성능이 좋아진다']}
  correctAnswer={1}
  explanation="다항 회귀는 원래 입력 x에 x², x³ 같은 거듭제곱 항을 새로운 변수로 추가해, 직선이 아니라 곡선 형태의 관계까지 표현할 수 있게 확장한 선형회귀의 한 형태다. 다만 차수를 지나치게 높이면 훈련 데이터의 잡음까지 따라가는 과적합이 발생해 새로운 데이터에 대한 성능(일반화 성능)이 오히려 나빠질 수 있다."
  optionExplanations={['정답이다. 거듭제곱 항으로 곡선 관계를 표현하되, 차수가 높아지면 과적합 위험이 커진다.', '다항 회귀도 선형회귀와 마찬가지로 각 항(거듭제곱 변수 포함)에 대한 회귀계수를 추정하는 모델이다.', '단순 선형회귀는 직선 관계만 표현할 수 있으며, 곡선 관계를 표현하려면 다항 항을 추가해야 한다.', '차수를 계속 높이면 훈련 데이터에는 더 잘 맞지만, 일정 수준을 넘으면 과적합으로 검증 데이터 성능은 오히려 나빠진다.']}
/>

## 분류 알고리즘 비교 (5–10번)

<Quiz
  questionNumber={5}
  question="결정트리와 나이브 베이즈를 비교한 설명으로 옳은 것은?"
  options={['결정트리는 변수 간 상호작용을 자연스럽게 반영할 수 있지만, 나이브 베이즈는 특징 간 조건부 독립을 가정해 상호작용을 직접 반영하지 못한다', '나이브 베이즈는 트리 구조로 시각화되고, 결정트리는 확률 기반으로 분류한다', '결정트리는 항상 나이브 베이즈보다 적은 데이터로 학습할 수 있다', '두 알고리즘 모두 조건부 독립을 가정한다']}
  correctAnswer={1}
  explanation="결정트리는 한 변수로 먼저 분기한 뒤 그 하위에서 다른 변수로 다시 분기하는 구조이므로, 변수들이 함께 작용하는 상호작용을 자연스럽게 반영할 수 있다. 반면 나이브 베이즈는 모든 특징이 클래스가 주어졌을 때 서로 조건부 독립이라고 가정하므로, 특징 간의 실제 상호작용을 직접적으로 반영하지 못하는 한계가 있다."
  optionExplanations={['정답이다. 결정트리는 상호작용을 반영할 수 있고, 나이브 베이즈는 조건부 독립 가정 때문에 이를 반영하지 못한다.', '트리 구조로 시각화되는 쪽은 결정트리이고, 확률 기반으로 분류하는 쪽은 나이브 베이즈이므로 설명이 뒤바뀌었다.', '나이브 베이즈는 오히려 적은 데이터로도 비교적 안정적으로 동작하는 것으로 알려져 있어, 이 비교는 근거가 부족한 일반화다.', '조건부 독립을 가정하는 것은 나이브 베이즈만의 특징이며, 결정트리는 이런 가정을 하지 않는다.']}
/>

<Quiz
  questionNumber={6}
  question="K-NN과 SVM을 비교한 설명으로 옳지 않은 것은?"
  options={['K-NN은 훈련 시 별도의 모델을 만들지 않고 데이터를 저장해 두었다가 예측 시점에 거리를 계산한다', 'SVM은 마진을 최대화하는 결정 경계를 학습 단계에서 미리 구해 둔다', 'K-NN과 SVM 모두 커널(kernel) 함수를 반드시 사용해야만 동작하는 알고리즘이다', '데이터셋이 매우 크면 K-NN은 예측마다 전체 데이터와 거리를 계산해야 해 속도가 느려질 수 있다']}
  correctAnswer={3}
  explanation="커널(kernel) 함수는 SVM에서 데이터를 더 높은 차원으로 매핑해 선형으로 분리하기 어려운 데이터를 다루기 위해 선택적으로 사용하는 기법이다. 반면 K-NN은 커널 함수 없이 단순히 거리(유클리드 거리 등)만으로 동작하는 알고리즘이므로, '두 알고리즘 모두 커널 함수를 반드시 사용해야 한다'는 서술은 K-NN의 동작 방식과 맞지 않는다."
  optionExplanations={['K-NN이 게으른 학습(lazy learning) 방식으로 동작한다는 설명은 옳다.', 'SVM은 훈련 단계에서 마진을 최대화하는 서포트 벡터와 결정 경계를 구해 둔다는 설명은 옳다.', '정답이다. 커널은 SVM에서 선택적으로 쓰이는 기법이며, K-NN은 커널 없이 거리 계산만으로 동작한다.', 'K-NN은 예측 시점마다 전체 훈련 데이터와 거리를 계산해야 하므로 데이터가 크면 느려진다는 설명은 옳다.']}
/>

<Quiz
  questionNumber={7}
  question="랜덤포레스트(random forest)와 단일 결정트리를 비교한 설명으로 옳은 것은?"
  options={['랜덤포레스트는 여러 결정트리를 배깅 방식으로 결합해, 단일 결정트리보다 일반적으로 분산(variance)이 줄어들어 과적합에 덜 취약하다', '랜덤포레스트는 결정트리를 단 하나만 사용하는 알고리즘이다', '단일 결정트리는 랜덤포레스트보다 항상 예측 성능이 뛰어나다', '랜덤포레스트는 결정트리와 전혀 관련이 없는 별개의 알고리즘이다']}
  correctAnswer={1}
  explanation="랜덤포레스트는 배깅(bagging)의 아이디어에 특징(feature)까지 무작위로 선택하는 방식을 더해 여러 개의 결정트리를 만들고, 그 예측을 평균(회귀) 또는 다수결(분류)로 종합한다. 여러 트리의 예측을 종합하는 과정에서 개별 트리 하나가 가진 높은 분산(잡음에 민감해 과적합되기 쉬운 성질)이 평균화되어 줄어드는 효과가 있다."
  optionExplanations={['정답이다. 여러 결정트리를 배깅으로 결합해 분산을 줄이는 것이 랜덤포레스트의 핵심 효과다.', '랜덤포레스트는 이름 그대로 여러 개(흔히 수십~수백 개)의 결정트리로 이루어진 앙상블 모델이다.', '일반적으로 앙상블인 랜덤포레스트가 단일 결정트리보다 안정적인 성능을 보이는 경우가 많아, 단일 트리가 항상 뛰어나다는 서술은 근거가 부족하다.', '랜덤포레스트는 결정트리를 기본 구성 요소로 사용하는 앙상블 기법이므로 결정트리와 밀접한 관련이 있다.']}
/>

<Quiz
  questionNumber={8}
  question="배깅 기반 앙상블(예: 랜덤포레스트)과 부스팅 기반 앙상블(예: AdaBoost)을 비교한 설명으로 옳지 않은 것은?"
  options={['배깅은 여러 모델을 병렬로 독립 학습시키므로 상대적으로 학습을 병렬화하기 쉽다', '부스팅은 이전 모델의 오차에 집중하며 순차적으로 모델을 추가하므로 배깅보다 순서에 의존적이다', '부스팅은 약한 학습기(weak learner)를 여러 개 결합해 강한 학습기를 만드는 아이디어에 기반한다', '배깅과 부스팅 모두 이전 모델이 틀린 데이터에 가중치를 부여하는 방식으로 동작한다는 점에서 동일하다']}
  correctAnswer={4}
  explanation="이전 모델이 틀린 데이터에 가중치를 부여해 다음 모델이 이를 더 신경 쓰도록 하는 것은 부스팅의 핵심 메커니즘이다. 배깅은 원본 데이터에서 복원추출로 만든 부트스트랩 표본을 사용할 뿐, 오분류된 데이터에 가중치를 주는 절차가 없으므로 두 방법이 동일하다는 서술은 틀렸다."
  optionExplanations={['배깅은 각 모델이 서로 다른 표본을 독립적으로 학습하므로 병렬 처리가 가능하다는 설명은 옳다.', '부스팅은 이전 모델의 결과에 따라 다음 모델이 결정되므로 순차적이고 순서에 의존적이라는 설명은 옳다.', '개별 성능이 약한 모델(약한 학습기)을 여러 개 결합해 강한 모델을 만드는 것이 부스팅의 기본 아이디어이므로 옳은 설명이다.', '정답이다. 오분류 데이터에 가중치를 주는 것은 부스팅만의 특징이며, 배깅에는 이 절차가 없다.']}
/>

<Quiz
  questionNumber={9}
  question="스팸 메일 필터처럼 정상 메일을 스팸으로 잘못 분류하는 것(오탐)에 특히 민감한 상황에서 상대적으로 더 중요하게 고려해야 할 지표는?"
  options={['재현율(recall)', '정밀도(precision)', '군집 내 분산', '지지도(support)']}
  correctAnswer={2}
  explanation="정밀도(precision)는 모델이 양성(스팸)이라고 예측한 것 중 실제로 양성인 비율이다. 정상 메일을 스팸으로 잘못 분류하는 오류(거짓양성, FP)를 줄이는 것이 중요한 상황에서는, 예측이 얼마나 믿을 만한지를 나타내는 정밀도를 특히 중요하게 살펴야 한다. 반대로 암 진단처럼 실제 양성을 놓치면 안 되는 상황에서는 재현율이 더 중요하다."
  optionExplanations={['재현율은 실제 양성을 놓치지 않는 것이 중요한 상황(예: 암 진단)에서 더 중요하게 고려되는 지표다.', '정답이다. 거짓양성(정상을 스팸으로 오분류)을 줄이는 것이 중요하므로 정밀도가 핵심이다.', '군집 내 분산은 군집화(clustering) 품질을 보는 개념으로 스팸 필터의 분류 성능 지표가 아니다.', '지지도는 연관 규칙 분석의 지표로 분류 문제의 오탐·미탐과는 관련이 없다.']}
/>

<Quiz
  questionNumber={10}
  question="데이터가 극단적으로 불균형(양성 1퍼센트, 음성 99퍼센트)한 상황에서 모델 성능을 평가할 때, 정확도(accuracy)만 사용하는 것이 부적절한 이유로 가장 적절한 것은?"
  options={['정확도는 계산이 너무 복잡해서 실무에서 쓸 수 없기 때문이다', '모든 데이터를 다수 클래스(음성)로만 예측해도 정확도가 99퍼센트에 가깝게 나올 수 있어, 소수 클래스 탐지 성능을 제대로 반영하지 못하기 때문이다', '정확도는 회귀 문제에서만 정의되는 지표이기 때문이다', '정확도는 항상 정밀도·재현율보다 낮게 계산되기 때문이다']}
  correctAnswer={2}
  explanation="양성이 1퍼센트뿐인 극단적 불균형 데이터에서는, 모델이 아무 학습도 하지 않고 모든 샘플을 음성(다수 클래스)으로만 예측해도 정확도가 약 99퍼센트에 이른다. 이 경우 정확도는 높지만 실제로 필요한 소수 클래스(양성) 탐지는 전혀 하지 못하는 것이므로, 정밀도·재현율·F1·ROC-AUC 등 클래스별 성능을 함께 봐야 한다."
  optionExplanations={['정확도는 (TP+TN)/전체라는 단순한 공식으로 계산이 복잡하지 않다. 문제는 계산 난이도가 아니라 불균형 상황에서의 해석 문제다.', '정답이다. 다수 클래스만 예측해도 정확도가 높게 나와 소수 클래스 성능을 가리는 것이 핵심 문제다.', '정확도는 회귀가 아니라 분류 문제에서 정의되는 지표다.', '정확도가 정밀도·재현율보다 항상 낮다는 근거는 없으며, 상황에 따라 어느 지표가 더 높거나 낮을 수 있다.']}
/>

## 비지도학습·차원 축소 비교 (11–15번)

<Quiz
  questionNumber={11}
  question="K-means와 계층적 군집(hierarchical clustering)을 비교한 설명으로 옳은 것은?"
  options={['K-means는 군집 수 K를 미리 지정해야 하지만, 계층적 군집은 덴드로그램(dendrogram)을 자른 위치에 따라 사후에 군집 수를 정할 수 있다', 'K-means는 지도학습이고 계층적 군집은 비지도학습이다', '계층적 군집은 항상 K-means보다 대용량 데이터에서 더 빠르게 동작한다', '두 방법 모두 군집 수를 미리 지정할 수 없다']}
  correctAnswer={1}
  explanation="K-means는 하이퍼파라미터로 군집 수 K를 미리 정해야 알고리즘을 시작할 수 있다. 반면 계층적 군집은 데이터를 순차적으로 병합(또는 분할)해 나무 구조인 덴드로그램을 만들고, 이 덴드로그램을 원하는 높이에서 잘라 사후에 군집 수를 유연하게 정할 수 있다는 차이가 있다."
  optionExplanations={['정답이다. K-means는 K를 사전에 지정해야 하고, 계층적 군집은 덴드로그램을 잘라 사후에 군집 수를 정할 수 있다.', 'K-means와 계층적 군집 모두 레이블 없이 데이터의 구조를 찾는 비지도학습 기법이다.', '계층적 군집은 일반적으로 계산 복잡도가 높아 대용량 데이터에서는 K-means보다 느린 경우가 많다.', 'K-means는 K를 미리 지정해야 하는 대표적인 알고리즘이므로 이 서술은 틀렸다.']}
/>

<Quiz
  questionNumber={12}
  question="K-means와 DBSCAN을 비교한 설명으로 옳지 않은 것은?"
  options={['DBSCAN은 밀도가 낮은 지역의 점을 이상치(noise)로 분류할 수 있다', 'K-means는 군집의 모양이 원형(구형)에 가깝다고 가정하는 경향이 있어, 복잡한 모양의 군집에는 잘 맞지 않을 수 있다', 'DBSCAN은 eps(반경)와 min_samples(최소 이웃 수) 같은 밀도 관련 하이퍼파라미터를 가진다', 'K-means는 군집의 형태와 밀도에 관계없이 항상 DBSCAN보다 정확한 군집을 찾는다']}
  correctAnswer={4}
  explanation="K-means는 각 점을 가장 가까운 중심에 할당하는 방식이라 군집이 원형에 가깝고 크기가 비슷할 때 잘 동작하지만, 초승달 모양처럼 복잡한 형태의 군집이나 밀도가 크게 다른 군집에는 잘 맞지 않는다. 이런 상황에서는 오히려 밀도 기반의 DBSCAN이 더 적합할 수 있으므로, 'K-means가 항상 더 정확하다'는 서술은 데이터 형태를 고려하지 않은 과도한 일반화다."
  optionExplanations={['밀도가 낮은 지역의 점을 이상치로 분류하는 것은 DBSCAN의 대표적인 장점이므로 옳은 설명이다.', 'K-means가 원형에 가까운 군집을 가정하는 경향이 있다는 것은 잘 알려진 한계이므로 옳은 설명이다.', 'eps와 min_samples는 DBSCAN의 대표적인 하이퍼파라미터이므로 옳은 설명이다.', '정답이다. 군집의 형태·밀도에 따라 DBSCAN이 K-means보다 더 적합한 경우도 많아, 항상 더 정확하다는 단정은 틀렸다.']}
/>

<Quiz
  questionNumber={13}
  question="PCA(주성분 분석)와 LDA(선형판별분석)를 비교한 설명으로 옳은 것은?"
  options={['PCA는 클래스 레이블을 사용해 클래스 간 분리를 최대화하고, LDA는 레이블 없이 분산을 최대화한다', 'PCA는 레이블 없이 데이터의 분산을 최대한 보존하는 축을 찾고, LDA는 레이블을 사용해 클래스 간 분리를 최대화하는 축을 찾는다', 'PCA와 LDA는 완전히 동일한 계산을 수행하는 같은 알고리즘이다', 'PCA와 LDA 모두 군집의 개수를 자동으로 결정해 준다']}
  correctAnswer={2}
  explanation="PCA는 비지도 기법으로 클래스 레이블을 전혀 사용하지 않고, 데이터 전체의 분산을 가장 많이 보존하는 방향으로 축을 새로 만든다. 반면 LDA(Linear Discriminant Analysis)는 클래스 레이블 정보를 사용해, 클래스 내부의 분산은 작게 하면서 클래스 간의 분산(분리)은 최대화하는 축을 찾는 지도 기반의 차원 축소·분류 보조 기법이다."
  optionExplanations={['PCA와 LDA의 레이블 사용 여부가 뒤바뀐 설명이다.', '정답이다. PCA는 레이블 없이 분산 보존, LDA는 레이블을 이용해 클래스 간 분리를 최대화한다.', 'PCA와 LDA는 목적함수와 사용하는 정보(레이블 유무)가 다른 별개의 기법이다.', '군집 개수 결정은 PCA·LDA의 목적이 아니라 K-means 같은 군집화 알고리즘에서 별도로 다루는 문제다.']}
/>

<Quiz
  questionNumber={14}
  question="차원 축소를 적용했을 때 나타나는 트레이드오프에 대한 설명으로 옳지 않은 것은?"
  options={['차원을 줄이면 계산 비용과 저장 공간이 줄어드는 경우가 많다', '차원을 지나치게 많이 줄이면 원본 데이터가 가진 정보의 일부를 잃어 예측 성능이 떨어질 수 있다', '차원 축소는 데이터의 정보 손실 없이 항상 원본과 동일한 성능을 보장한다', '차원 축소로 시각화(2차원·3차원)가 가능해져 데이터의 구조를 직관적으로 이해하는 데 도움이 될 수 있다']}
  correctAnswer={3}
  explanation="차원 축소는 원래 여러 차원에 퍼져 있던 정보를 더 적은 축으로 압축하는 과정이므로, 축소 과정에서 일정 부분의 정보 손실이 불가피한 경우가 많다. 따라서 '정보 손실 없이 항상 원본과 동일한 성능을 보장한다'는 서술은 차원 축소의 본질적인 트레이드오프를 무시한 틀린 설명이다."
  optionExplanations={['차원이 줄면 다뤄야 할 특징 수가 줄어 계산·저장 비용이 감소하는 경우가 많다는 것은 옳은 설명이다.', '과도한 차원 축소는 유용한 정보까지 잃게 만들어 성능 저하로 이어질 수 있다는 것은 옳은 설명이다.', '정답이다. 차원 축소는 일반적으로 어느 정도의 정보 손실을 동반하는 트레이드오프를 가진다.', '2~3차원으로 축소하면 산점도 등으로 데이터를 시각화하기 쉬워진다는 것은 차원 축소의 실질적인 장점 중 하나이므로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={15}
  question="연관 규칙 분석에서 지지도(support), 신뢰도(confidence), 향상도(lift)를 비교한 설명으로 옳은 것은?"
  options={['지지도는 조건부 확률을, 신뢰도는 전체 거래 중 등장 비율을 나타낸다', '향상도가 1보다 크면 두 항목이 우연히 함께 나타나는 것보다 더 강한 양의 연관성이 있다고 해석할 수 있다', '신뢰도는 항상 지지도보다 작은 값을 가진다', '향상도는 군집 간 거리 지표이며 연관 규칙과는 관련이 없다']}
  correctAnswer={2}
  explanation="향상도(lift)는 두 항목이 실제로 함께 나타나는 정도를, 두 항목이 서로 독립이라고 가정했을 때(우연히 함께 나타날 확률) 기대되는 정도와 비교한 값이다. 향상도가 1이면 두 항목이 우연히 함께 나타나는 정도와 같다는 뜻이고, 1보다 크면 우연 이상의 양의 연관성이, 1보다 작으면 오히려 음의 연관성(함께 잘 나타나지 않음)이 있다고 해석한다."
  optionExplanations={['지지도는 전체 거래 중 등장 비율, 신뢰도는 조건부 확률을 나타내므로 두 정의가 서로 뒤바뀌었다.', '정답이다. 향상도가 1보다 크면 우연보다 강한 양의 연관성을 의미한다.', '신뢰도와 지지도는 계산 기준이 달라 상황에 따라 어느 쪽이 크거나 작을 수 있으며, 항상 신뢰도가 작다고 단정할 수 없다.', '향상도는 연관 규칙 분석에서 두 항목 간의 연관 강도를 나타내는 지표이며, 군집 간 거리와는 관련이 없다.']}
/>

## 신경망·모델 선택 비교 (16–21번)

<Quiz
  questionNumber={16}
  question="단일 퍼셉트론과 다층 퍼셉트론(MLP)을 비교한 설명으로 옳은 것은?"
  options={['단일 퍼셉트론은 은닉층이 없어 선형적으로 분리되지 않는 문제(예: XOR)를 풀 수 없지만, MLP는 은닉층과 비선형 활성화 함수를 통해 이런 문제를 풀 수 있다', '단일 퍼셉트론은 은닉층을 여러 개 가지고 있어 MLP보다 더 복잡한 문제를 풀 수 있다', 'MLP는 역전파를 사용할 수 없고 단일 퍼셉트론만 역전파를 사용한다', '두 모델 모두 항상 동일한 성능을 보인다']}
  correctAnswer={1}
  explanation="단일 퍼셉트론은 입력을 하나의 직선(초평면)으로만 나눌 수 있어 XOR처럼 선형적으로 분리되지 않는 문제를 표현할 수 없다. MLP는 입력층과 출력층 사이에 은닉층과 비선형 활성화 함수(예: 시그모이드, ReLU)를 추가해 복잡한 비선형 결정 경계를 표현할 수 있고, 여러 층에 걸친 가중치는 역전파(backpropagation) 알고리즘으로 학습한다."
  optionExplanations={['정답이다. 단일 퍼셉트론은 XOR을 풀 수 없고, MLP는 은닉층·비선형 활성화로 이를 해결한다.', '은닉층을 여러 개 가지는 쪽은 단일 퍼셉트론이 아니라 MLP다.', '역전파는 오히려 은닉층을 가진 MLP 같은 다층 신경망을 학습시키기 위해 고안된 알고리즘이다.', '두 모델은 표현할 수 있는 문제의 복잡도가 다르므로 성능이 항상 같다고 볼 수 없다.']}
/>

<Quiz
  questionNumber={17}
  question="모델의 복잡도가 지나치게 높아 훈련 데이터에는 잘 맞지만 새로운 데이터에서 성능이 떨어지는 상황(과적합)에 대한 대응으로 옳지 않은 것은?"
  options={['정규화(L1·L2)를 적용해 계수 크기에 페널티를 준다', '훈련 데이터의 양을 늘리거나 데이터 증강을 고려한다', '검증 데이터 성능은 전혀 확인하지 않고 훈련 데이터 성능만 계속 높인다', '모델의 복잡도(트리 깊이, 신경망 은닉층 수 등)를 줄이거나 조기 종료(early stopping)를 적용한다']}
  correctAnswer={3}
  explanation="과적합 여부를 판단하려면 훈련에 사용하지 않은 검증 데이터(validation data)에서의 성능을 반드시 함께 확인해야 한다. 훈련 데이터 성능만 계속 높이는 것은 오히려 과적합을 심화시키는 방향이므로, 이는 과적합에 대한 대응이 아니라 과적합을 방치하는 잘못된 접근이다."
  optionExplanations={['정규화는 계수 크기를 억제해 모델이 훈련 데이터의 잡음까지 따라가는 것을 막는 대표적인 과적합 대응이므로 옳은 설명이다.', '데이터가 많아지면 모델이 잡음보다 일반적인 패턴을 학습하기 쉬워져 과적합 완화에 도움이 되므로 옳은 설명이다.', '정답이다. 검증 데이터 성능을 확인하지 않고 훈련 성능만 높이는 것은 과적합을 방치하는 접근이다.', '모델 복잡도를 낮추거나 검증 성능이 나빠지기 전에 학습을 멈추는 조기 종료는 과적합 대응의 대표적인 방법이므로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={18}
  question="k-폴드 교차검증(k-fold cross-validation)과 단순 훈련/검증 데이터 1회 분할(hold-out)을 비교한 설명으로 옳은 것은?"
  options={['k-폴드 교차검증은 데이터를 k개로 나누어 각 폴드를 번갈아 검증에 사용함으로써, 1회 분할보다 더 안정적인 성능 추정치를 얻을 수 있다', '1회 분할은 항상 k-폴드 교차검증보다 계산 비용이 크다', 'k-폴드 교차검증은 훈련 데이터를 전혀 사용하지 않는 평가 방법이다', '두 방법 모두 동일한 하나의 성능 값만을 출력하며 차이가 없다']}
  correctAnswer={1}
  explanation="k-폴드 교차검증은 데이터를 k개의 폴드(fold, 조각)로 나눈 뒤, 매번 다른 폴드를 검증용으로 남기고 나머지로 학습하는 과정을 k번 반복해 k개의 성능 값을 얻는다. 이렇게 얻은 성능 값들의 평균과 분산을 보면, 데이터를 한 번만 나눈 hold-out 방식보다 특정 분할에 따른 우연성의 영향을 줄여 더 안정적인 성능 추정을 할 수 있다."
  optionExplanations={['정답이다. 여러 번 나누어 평가함으로써 1회 분할보다 안정적인 추정치를 얻을 수 있다.', 'k-폴드 교차검증은 학습을 k번 반복해야 하므로 일반적으로 1회 분할보다 계산 비용이 더 크다는 점에서 이 서술은 반대로 되어 있다.', 'k-폴드 교차검증도 각 반복에서 훈련 데이터를 사용해 모델을 학습시킨다.', 'k-폴드 교차검증은 k개의 성능 값을 얻어 평균·분산을 볼 수 있다는 점에서 1회 값만 얻는 hold-out과 차이가 있다.']}
/>

<Quiz
  questionNumber={19}
  question="편향(bias)이 높은 모델과 분산(variance)이 높은 모델을 비교한 설명으로 옳은 것은?"
  options={['편향이 높은 모델은 훈련 데이터에도 잘 맞지 않는 경향이 있어 과소적합과 연결되고, 분산이 높은 모델은 훈련 데이터에는 잘 맞지만 새로운 데이터에서는 성능이 크게 흔들려 과적합과 연결된다', '편향이 높은 모델은 항상 분산이 높은 모델보다 복잡하다', '분산이 높은 모델은 항상 편향이 높은 모델보다 훈련 데이터 성능이 낮다', '편향과 분산은 항상 함께 커지거나 함께 작아진다']}
  correctAnswer={1}
  explanation="편향(bias)은 모델이 지나치게 단순해 데이터의 실제 패턴을 충분히 반영하지 못하는 정도를 말하며, 편향이 높으면 훈련 데이터에서도 성능이 낮은 과소적합으로 이어진다. 분산(variance)은 훈련 데이터가 조금만 바뀌어도 모델의 예측이 크게 달라지는 민감도를 말하며, 분산이 높으면 훈련 데이터에는 잘 맞지만 새로운 데이터에서 성능이 크게 흔들리는 과적합으로 이어진다. 이 둘은 대체로 한쪽을 줄이면 다른 쪽이 커지는 트레이드오프 관계에 있다."
  optionExplanations={['정답이다. 편향이 높으면 과소적합, 분산이 높으면 과적합과 연결된다.', '일반적으로는 반대로, 모델이 단순할수록(복잡도가 낮을수록) 편향이 높아지는 경향이 있다.', '분산이 높은 모델은 오히려 훈련 데이터에는 매우 잘 맞아 훈련 성능이 높게 나오는 경우가 많다.', '편향과 분산은 일반적으로 한쪽이 줄면 다른 쪽이 느는 트레이드오프 관계이지, 항상 같은 방향으로 움직이지 않는다.']}
/>

<Quiz
  questionNumber={20}
  question="해석 가능성(interpretability)이 매우 중요한 상황(예: 대출 승인 여부를 고객에게 설명해야 하는 경우)에서 상대적으로 더 적합한 모델에 대한 설명으로 가장 적절한 것은?"
  options={['블랙박스에 가까운 복잡한 신경망이 항상 유일한 선택지다', '결정트리나 로지스틱 회귀처럼 분할 기준이나 계수의 의미를 사람이 직접 해석할 수 있는 모델이 상대적으로 설명에 유리하다', '해석 가능성은 머신러닝 모델 선택과 아무 관련이 없는 개념이다', 'K-means 군집화가 대출 승인 여부를 예측하는 지도학습 모델로 가장 적합하다']}
  correctAnswer={2}
  explanation="결정트리는 '어떤 조건으로 분기했는지'를 트리 구조로 그대로 보여줄 수 있고, 로지스틱 회귀는 각 변수의 계수 부호와 크기로 '이 변수가 커지면 승인 확률이 어떻게 변하는지'를 설명할 수 있어, 복잡한 신경망이나 대규모 앙상블보다 결과를 사람에게 설명하기에 유리한 경우가 많다. 실무에서 규제나 설명 책임이 중요한 도메인(금융 심사 등)에서는 이런 해석 가능성이 성능만큼 중요한 선택 기준이 된다."
  optionExplanations={['성능이 뛰어나더라도 신경망은 내부 동작을 사람이 직관적으로 설명하기 어려운 블랙박스에 가깝다는 한계가 있어, 해석이 중요한 상황의 유일한 선택지로 보기 어렵다.', '정답이다. 결정트리·로지스틱 회귀처럼 분할 기준이나 계수를 직접 해석할 수 있는 모델이 설명에 유리하다.', '해석 가능성은 규제 산업이나 사용자 설명 책임이 있는 상황에서 실제로 모델 선택에 중요한 기준이 된다.', 'K-means는 레이블 없이 군집을 찾는 비지도학습 기법으로, 대출 승인처럼 레이블(승인/거절)이 있는 지도학습 문제에는 적합하지 않다.']}
/>

<Quiz
  questionNumber={21}
  question="ROC 곡선과 AUC(Area Under the Curve)에 대한 설명으로 옳지 않은 것은?"
  options={['ROC 곡선은 분류 임계값을 바꿔가며 참양성비율(TPR)과 거짓양성비율(FPR)의 변화를 나타낸 곡선이다', 'AUC 값이 1에 가까울수록 모델이 양성과 음성을 잘 구분한다고 해석할 수 있다', 'AUC가 0.5에 가깝다는 것은 모델이 무작위 추측과 비슷한 수준으로 분류한다는 의미다', 'AUC는 특정 임계값 하나에서만 계산할 수 있는 지표로, 임계값을 바꾸면 다른 지표로 대체해야 한다']}
  correctAnswer={4}
  explanation="AUC(Area Under the Curve)는 ROC 곡선 아래의 면적으로, 여러 임계값에 걸쳐 모델이 양성과 음성을 얼마나 잘 구분하는지를 종합적으로 나타내는 지표다. 즉 AUC는 하나의 특정 임계값에서만 계산되는 것이 아니라, 임계값을 바꿔가며 그린 ROC 곡선 전체를 요약한 값이라는 점이 핵심이므로, '특정 임계값 하나에서만 계산할 수 있다'는 서술은 AUC의 정의와 반대다."
  optionExplanations={['ROC 곡선이 임계값 변화에 따른 TPR·FPR의 궤적을 나타낸다는 것은 정확한 정의이므로 옳은 설명이다.', 'AUC가 1에 가까울수록 구분 성능이 좋다는 것은 정확한 해석이므로 옳은 설명이다.', 'AUC 0.5는 무작위 추측 수준의 구분 성능을 의미한다는 것은 정확한 해석이므로 옳은 설명이다.', '정답이다. AUC는 여러 임계값에 걸친 ROC 곡선 전체를 요약한 값이지, 임계값 하나에서만 계산되는 지표가 아니다.']}
/>

## 참고 자료

- [비교 논문: In-depth Comparison of Supervised Classification Models](https://thesai.org/Downloads/Volume16No8/Paper_62-In_Depth_Comparison_of_Supervised_Classification_Models.pdf)
- [scikit-learn Clustering 모듈 문서](https://scikit-learn.org/stable/modules/clustering.html)
- [scikit-learn Model Evaluation — Metrics and scoring](https://scikit-learn.org/stable/modules/model_evaluation.html)
