---
title: '18. 독학사 2단계 웹 프로그래밍 예상기출문제 4'
description: '인터넷·HTML·CSS·JavaScript·DOM·XML·Ajax·웹 표준까지 전 범위를 섞은 실전형 종합 모의고사'
---

<Callout type="info">
  **이번 문서의 목표:** 지금까지 배운 인터넷·HTML·CSS·JavaScript·XML·Ajax·웹 표준 전 범위를 하나의 모의고사로 다시 풀어 보며, 실제 시험처럼 시간 안에 정답을 골라내는 감각을 점검한다.
</Callout>

<Callout type="warning">
  이 문제 세트는 실제 독학사 기출문제를 그대로 옮긴 것이 아니다. 국가평생교육진흥원이 공개한 독학사 2단계 웹 프로그래밍 출제기준(평가영역)을 근거로 새로 구성한 예상 문제이며, 실제 시험의 문항 수·배점·난이도·출제 범위는 응시 회차 공고에 따라 달라질 수 있으므로 반드시 최신 시험 공고를 함께 확인해야 한다.
</Callout>

## 전 범위 종합 모의고사

아래 22문항은 인터넷·웹 기초, HTML 구조·시맨틱·폼·표, CSS 선택자·박스 모델·레이아웃, JavaScript 문법·자료형·함수·배열·객체·DOM·이벤트, XML·Ajax, 웹 표준·접근성 순서로 섞여 있다. 실제 시험처럼 앞에서부터 순서대로 풀고, 채점 후에는 문항별로 정답 위치와 근거 문장을 다시 대조한다.

<Quiz
  questionNumber={1}
  question="인터넷과 월드와이드웹(WWW)의 관계에 대한 설명으로 가장 옳은 것은?"
  options={['인터넷은 웹의 하위 서비스 중 하나에 불과하다', '웹은 인터넷이라는 통신망 위에서 동작하는 하이퍼텍스트 기반 서비스다', '인터넷과 웹은 완전히 같은 개념이라 서로 바꿔 써도 무방하다', '웹은 이메일과 마찬가지로 인터넷보다 먼저 등장한 기술이다']}
  correctAnswer={2}
  explanation="인터넷은 전 세계 컴퓨터를 연결하는 통신망 인프라이고, 웹은 그 인터넷 위에서 하이퍼텍스트(HTML) 문서를 주고받는 여러 서비스 중 하나다. 이메일, 파일 전송 등도 인터넷 위의 다른 서비스이므로 인터넷과 웹은 같은 범위의 개념이 아니다."
  optionExplanations={['반대로 설명한 진술이다. 인터넷이 더 큰 통신 인프라이고 웹은 그 위에 얹힌 서비스다', '정답이다. 인터넷이라는 하부 네트워크 위에 웹이라는 하이퍼텍스트 서비스가 동작한다', '인터넷은 통신망이고 웹은 그 위의 서비스로 범위가 다르므로 동일 개념이 아니다', '인터넷과 그 전신 통신망이 웹보다 먼저 만들어졌고 웹은 1990년대에 그 위에 등장했다']}
/>

<Quiz
  questionNumber={2}
  question="주소 https://www.example.com:443/docs/page.html?id=10 에서 443이 나타내는 부분은?"
  options={['포트 번호', '경로(디렉터리) 이름', '요청하는 파일 이름', '쿼리 스트링 파라미터']}
  correctAnswer={1}
  explanation="URL은 스킴 뒤에 호스트와 콜론으로 구분한 포트 번호를 붙일 수 있다. 443은 HTTPS의 기본 포트로 흔히 생략되지만, 명시하면 콜론 뒤 숫자로 나타난다."
  optionExplanations={['정답이다. 콜론 뒤에 오는 443은 서버가 요청을 받는 통신 포트 번호다', 'docs는 경로(path)에 해당하는 디렉터리 이름이다', 'page.html은 요청 대상 파일 이름, 즉 리소스 이름이다', 'id=10은 물음표 뒤에 오는 쿼리 스트링 파라미터다']}
/>

<Quiz
  questionNumber={3}
  question="HTTP 요청 메서드 중 GET 방식의 특징으로 옳은 것은?"
  options={['서버의 리소스를 변경하지 않고 조회하며, 요청 정보가 URL에 그대로 노출될 수 있다', 'GET은 항상 요청 본문에만 데이터를 담아 전송하고 URL에는 아무것도 남지 않는다', 'GET은 서버의 데이터를 새로 생성하거나 삭제하는 용도로 설계되었다', 'GET 요청은 브라우저 주소창에 절대로 표시되지 않는다']}
  correctAnswer={1}
  explanation="GET은 서버 상태를 바꾸지 않는 조회용 요청으로 설계되었고, 전달할 데이터를 URL의 쿼리 스트링에 담기 때문에 주소창이나 서버 로그에 노출될 수 있다. 그래서 비밀번호 같은 민감한 값은 GET으로 보내지 않는다."
  optionExplanations={['정답이다. GET은 조회 전용이며 데이터가 URL에 그대로 담긴다', 'GET은 URL(쿼리 스트링)에 데이터를 담아 보내는 방식이며 이 설명은 반대로 서술되어 있다', '리소스 생성·삭제처럼 서버 상태를 바꾸는 작업은 보통 POST·PUT·DELETE 같은 메서드가 맡는다', 'GET 요청은 쿼리 스트링 형태로 주소창에 그대로 나타나는 경우가 많다']}
/>

<Quiz
  questionNumber={4}
  question="클라이언트-서버 모델에 대한 설명으로 가장 옳은 것은?"
  options={['클라이언트는 서비스를 제공하고 서버는 요청만 보내는 주체다', '서버는 요청을 받아 처리한 뒤 응답을 돌려주고, 클라이언트는 요청을 보내는 주체다', '클라이언트와 서버는 반드시 같은 컴퓨터 한 대에서만 실행될 수 있다', '웹 브라우저는 서버 역할을 하고 웹 서버 프로그램은 클라이언트 역할을 한다']}
  correctAnswer={2}
  explanation="클라이언트-서버 모델에서 클라이언트(웹 브라우저 등)는 필요한 자원을 서버에 요청하는 주체이고, 서버는 그 요청을 받아 처리한 뒤 결과를 응답으로 돌려주는 주체다. 두 역할은 서로 다른 컴퓨터에서 실행되는 것이 일반적이다."
  optionExplanations={['클라이언트와 서버의 역할이 반대로 서술되어 있다', '정답이다. 서버는 요청 처리와 응답, 클라이언트는 요청 발신을 담당한다', '클라이언트와 서버는 물리적으로 다른 컴퓨터(각자 다른 네트워크 위치)에서 실행되는 것이 일반적이다', '웹 브라우저가 클라이언트, 웹 서버 프로그램이 서버 역할을 하므로 이 설명은 반대로 서술되어 있다']}
/>

<Quiz
  questionNumber={5}
  question="HTML 문서의 문서 형식 선언, head 영역, body 영역에 대한 설명으로 옳지 않은 것은?"
  options={['문서 형식 선언은 브라우저에게 표준 모드로 문서를 해석하라고 알려주는 역할을 한다', 'head 영역에는 문서 제목, 메타데이터, 외부 스타일시트 연결 정보가 들어간다', 'body 영역에는 화면에 실제로 표시되는 콘텐츠가 들어간다', 'head 영역의 내용은 title을 포함해 모두 브라우저 화면에 그대로 렌더링되어 사용자에게 보인다']}
  correctAnswer={4}
  explanation="head 영역에 담긴 메타데이터, 스타일시트 연결, 스크립트 등은 일반적으로 화면에 직접 렌더링되지 않는다. title 요소의 내용도 문서 본문이 아니라 브라우저 탭이나 창 제목표시줄에 표시될 뿐이다. 이 문항은 옳지 않은 진술을 고르는 문제다."
  optionExplanations={['옳은 설명이다. 문서 형식 선언은 브라우저가 표준을 준수하는 모드로 문서를 해석하도록 지시한다', '옳은 설명이다. head 영역은 제목, 메타데이터, 외부 자원 연결 정보를 담는다', '옳은 설명이다. body 영역이 실제 화면에 그려지는 콘텐츠를 담는다', '정답(옳지 않은 설명)이다. head 영역 내용은 화면 본문에 직접 렌더링되지 않으며 title도 탭 제목으로만 쓰인다']}
/>

<Quiz
  questionNumber={6}
  question="문서에서 독립적으로 배포·재사용될 수 있는 콘텐츠 한 덩어리(예: 블로그 게시글 한 편)를 감싸는 데 가장 적합한 시맨틱 요소는?"
  options={['div', 'section', 'article', 'span']}
  correctAnswer={3}
  explanation="article 요소는 그 자체만으로도 독립적으로 배포·재사용될 수 있는 콘텐츠 단위(게시글, 뉴스 기사, 댓글 한 개 등)를 나타내는 시맨틱 요소다. section은 주제별로 묶인 문서의 한 구획을 나타내고, div와 span은 의미 없는 범용 컨테이너다."
  optionExplanations={['div는 의미를 담지 않는 범용 블록 컨테이너라 시맨틱 태그로 적합하지 않다', 'section은 문서 안의 주제별 구획을 나타내지만 독립 배포 단위라는 의미는 없다', '정답이다. article은 독립적으로 재사용 가능한 콘텐츠 단위를 나타낸다', 'span은 의미 없는 인라인 컨테이너로 문서의 한 덩어리를 감싸는 시맨틱 요소가 아니다']}
/>

<Quiz
  questionNumber={7}
  question="사용자가 입력한 값이 반드시 채워져야만 폼이 제출되도록 강제하는 입력 속성은?"
  options={['placeholder', 'required', 'readonly', 'disabled']}
  correctAnswer={2}
  explanation="required는 필수 입력을 강제하는 속성으로, 값이 비어 있으면 브라우저가 제출을 막고 경고 메시지를 보여준다. placeholder는 입력칸이 비었을 때 보이는 안내 문구일 뿐 값이 아니고, readonly는 값 수정은 막지만 값 자체는 전송되며, disabled는 입력도 전송도 되지 않는다."
  optionExplanations={['placeholder는 값을 채우라고 강제하지 않으며 입력 전 안내 문구만 보여준다', '정답이다. required는 값이 비어 있으면 제출을 막는다', 'readonly는 수정만 막을 뿐 값 입력 여부를 강제하지 않으며 초기값이 없으면 빈 값으로도 제출된다', 'disabled는 입력 자체를 막고 값이 서버로 전송되지도 않는다']}
/>

<Quiz
  questionNumber={8}
  question="표에서 하나의 셀이 여러 열에 걸쳐 확장되도록 지정할 때 사용하는 속성은?"
  options={['rowspan', 'colspan', 'span', 'merge']}
  correctAnswer={2}
  explanation="colspan은 셀이 가로 방향(여러 열)으로 확장되도록 지정하는 속성이고, rowspan은 세로 방향(여러 행)으로 확장되도록 지정하는 속성이다. span과 merge는 표 확장을 위한 HTML 속성이 아니다."
  optionExplanations={['rowspan은 열이 아니라 행(세로) 방향 확장을 지정하는 속성이다', '정답이다. colspan은 열(가로) 방향 확장을 지정한다', 'span은 표 셀 확장 속성이 아니라 인라인 컨테이너 요소의 이름이다', 'merge라는 이름의 표 확장 속성은 HTML에 존재하지 않는다']}
/>

<Quiz
  questionNumber={9}
  question="같은 요소에 아이디 선택자로 파란색, 클래스 선택자로 빨간색, 요소 선택자로 초록색을 각각 지정했을 때 실제로 화면에 적용되는 색상은?"
  options={['파란색', '빨간색', '초록색', '세 선언 모두 적용되지 않는다']}
  correctAnswer={1}
  explanation="CSS 명시도(specificity, 여러 선택자가 충돌할 때 어느 것을 우선 적용할지 정하는 점수 체계)는 아이디 선택자, 클래스 선택자, 요소 선택자 순으로 높다. 세 선언이 충돌하면 명시도가 가장 높은 아이디 선택자로 지정한 파란색이 최종 적용된다."
  optionExplanations={['정답이다. 아이디 선택자의 명시도가 가장 높아 최종 적용된다', '클래스 선택자는 아이디 선택자보다 명시도가 낮아 항상 이기지는 않는다', '요소 선택자는 명시도가 가장 낮은 축에 속해 다른 두 선택자에 밀린다', '세 선언 모두 문법상 유효하므로 무시되지 않고, 명시도 규칙에 따라 하나가 최종 적용된다']}
/>

<Quiz
  questionNumber={10}
  question="콘텐츠 너비가 200픽셀, 좌우 패딩이 각각 20픽셀, 좌우 테두리가 각각 5픽셀이고 박스 사이징이 기본값(콘텐츠 기준으로 너비를 계산하는 방식)일 때, 이 요소가 화면에서 실제로 차지하는 전체 너비는?"
  options={['200픽셀', '220픽셀', '240픽셀', '250픽셀']}
  correctAnswer={4}
  explanation="기본 박스 모델에서 전체 너비는 콘텐츠 너비에 좌우 패딩과 좌우 테두리를 모두 더한 값이다. 200(콘텐츠)에 좌우 패딩 20씩 더해 240, 여기에 좌우 테두리 5씩을 더하면 250픽셀이 된다."
  optionExplanations={['패딩과 테두리를 전혀 더하지 않은 콘텐츠 너비만 계산한 값이다', '패딩만 더하고 테두리를 빠뜨린 값(200+40=240이 아니라 220으로 잘못 계산)이다', '테두리를 빠뜨리고 패딩만 더한 값(200+40=240)으로, 좌우 테두리 10픽셀을 더 더해야 한다', '정답이다. 200+20×2+5×2=250픽셀이 전체 너비다']}
/>

<Quiz
  questionNumber={11}
  question="화면에서 요소를 완전히 제거해 공간조차 차지하지 않게 하는 방식과, 화면에는 보이지 않지만 자리(공간)는 그대로 차지하게 하는 방식을 올바르게 짝지은 것은?"
  options={['앞은 자리를 차지하고 뒤는 차지하지 않는다', '앞은 자리를 차지하지 않고 뒤는 차지한다', '둘 다 자리를 차지하지 않는다', '둘 다 자리를 그대로 차지한다']}
  correctAnswer={2}
  explanation="화면에서 완전히 제거해 레이아웃 공간까지 없애는 표시 방식(디스플레이 값을 없음으로 지정)은 요소가 차지하던 공간까지 사라지게 만든다. 반면 보이지만 않게 숨기는 방식(가시성 속성을 숨김으로 지정)은 화면에 그려지지 않을 뿐 레이아웃 상의 공간은 그대로 남긴다."
  optionExplanations={['설명이 반대로 짝지어져 있다', '정답이다. 완전 제거 방식은 공간을 없애고, 숨김 방식은 공간을 남긴다', '숨김 방식은 공간을 그대로 차지하므로 둘 다 차지하지 않는다는 설명은 틀렸다', '완전 제거 방식은 공간까지 없애므로 둘 다 차지한다는 설명은 틀렸다']}
/>

<Quiz
  questionNumber={12}
  question="부모 요소를 기준으로 하지 않고 브라우저 뷰포트(화면에 보이는 영역)를 기준으로 위치가 고정되어, 스크롤을 해도 화면에서 움직이지 않는 위치 지정 값은?"
  options={['static', 'relative', 'absolute', 'fixed']}
  correctAnswer={4}
  explanation="fixed는 가장 가까운 조상 요소가 아니라 브라우저 뷰포트를 기준으로 위치가 고정되므로, 페이지를 스크롤해도 화면상의 같은 자리에 계속 머무른다. static은 기본 배치, relative는 자기 자신의 원래 위치를 기준으로 이동, absolute는 위치가 지정된 가장 가까운 조상을 기준으로 배치된다."
  optionExplanations={['static은 특별한 위치 지정 없이 문서 흐름을 그대로 따르는 기본값이다', 'relative는 자기 자신의 원래 위치를 기준으로 상대 이동할 뿐 뷰포트에 고정되지 않는다', 'absolute는 위치가 지정된 가장 가까운 조상 요소를 기준으로 배치되며 스크롤에 영향을 받을 수 있다', '정답이다. fixed는 뷰포트를 기준으로 고정되어 스크롤해도 움직이지 않는다']}
/>

<Quiz
  questionNumber={13}
  question="반복문 안에서 선언한 변수가 반복문 블록 바깥에서는 접근되지 않기를 원할 때 사용하기에 가장 적절한 선언 방식은?"
  options={['var', 'let', '전역 변수로 선언하고 값만 초기화', '선언 없이 값만 대입']}
  correctAnswer={2}
  explanation="let(그리고 const)은 블록 스코프(중괄호로 둘러싸인 코드 블록 단위로 유효 범위가 정해지는 것)를 가지므로 선언된 블록 안에서만 접근할 수 있다. var는 함수 스코프라서 반복문 블록을 벗어나도 값이 남아 있어 의도치 않게 접근될 수 있다."
  optionExplanations={['var는 함수 스코프를 가져 블록을 벗어나도 값이 그대로 접근되므로 목적에 맞지 않는다', '정답이다. let은 블록 스코프라서 선언된 블록을 벗어나면 접근할 수 없다', '전역 변수로 선언하면 오히려 어디서든 접근 가능해져 반대 효과가 난다', '선언 없이 값을 대입하면 의도치 않게 전역 변수가 만들어져 블록을 제한하는 효과가 없다']}
/>

<Quiz
  questionNumber={14}
  question="자바스크립트에서 문자열 5와 숫자 3을 더하기 연산자로 계산했을 때(문자열 5 더하기 숫자 3) 결과와 그 이유로 옳은 것은?"
  options={['8이 되며 문자열이 자동으로 숫자로 변환된다', '53이 되며 숫자가 문자열로 변환되어 이어붙기가 일어난다', '오류가 발생하며 계산이 중단된다', '숫자도 문자열도 아닌 undefined가 나온다']}
  correctAnswer={2}
  explanation="더하기 연산자는 피연산자 중 하나라도 문자열이면 다른 피연산자를 문자열로 바꾼 뒤 이어붙이기(연결)를 수행한다. 문자열 5와 숫자 3을 더하면 숫자 3이 문자열 3으로 바뀌어 53이라는 문자열이 만들어진다."
  optionExplanations={['숫자끼리의 덧셈 결과이며, 문자열이 포함된 덧셈에서는 이어붙기가 일어나므로 틀렸다', '정답이다. 문자열이 섞인 덧셈은 이어붙기가 되어 53이라는 문자열이 나온다', '자바스크립트의 더하기 연산자는 문자열과 숫자를 함께 써도 오류 없이 이어붙기를 수행한다', '더하기 연산 결과는 항상 값이 만들어지며 undefined가 나오지 않는다']}
/>

<Quiz
  questionNumber={15}
  question="함수 안에서 return문을 만나면 벌어지는 일로 옳은 것은?"
  options={['해당 함수의 실행을 즉시 종료하고 지정한 값을 호출한 곳으로 돌려준다', '함수 실행을 잠시 멈췄다가 다음 줄부터 이어서 실행한다', '함수를 처음부터 다시 반복 실행한다', '아무 효과가 없으며 그대로 무시된다']}
  correctAnswer={1}
  explanation="return문은 함수의 실행 흐름을 그 자리에서 즉시 끝내고, 지정한 값을 함수를 호출한 코드 쪽으로 돌려준다. return 뒤에 남은 코드가 있어도 실행되지 않는다."
  optionExplanations={['정답이다. return은 즉시 함수를 끝내고 값을 반환한다', 'return은 실행을 재개시키는 것이 아니라 완전히 종료시킨다', '함수를 반복 실행하는 것은 반복문의 역할이며 return과는 관계가 없다', 'return은 반환값을 호출한 곳으로 전달하는 실질적인 효과를 가진다']}
/>

<Quiz
  questionNumber={16}
  question="배열의 각 요소를 순회하면서 원하는 조건에 맞는 요소만 걸러 새로운 배열로 만들 때 사용하는 배열 메서드는?"
  options={['map', 'filter', 'reduce', 'forEach']}
  correctAnswer={2}
  explanation="filter는 콜백 함수가 참으로 판정하는 요소만 모아 새로운 배열을 만든다. map은 모든 요소를 변형한 새 배열을 만들고, reduce는 배열 전체를 누적값 하나로 축약하며, forEach는 반환값 없이 각 요소를 순회만 한다."
  optionExplanations={['map은 조건으로 거르지 않고 모든 요소를 변형한 새 배열을 만든다', '정답이다. filter는 조건에 맞는 요소만 걸러 새 배열을 만든다', 'reduce는 배열을 하나의 누적값으로 축약할 뿐 거르는 동작이 아니다', 'forEach는 값을 걸러 새 배열을 만들지 않고 순회하며 부수 효과만 실행한다']}
/>

<Quiz
  questionNumber={17}
  question="객체의 프로퍼티 이름이 변수에 담겨 있어서 실행 시점에야 결정될 때, 그 변수를 이용해 프로퍼티 값을 읽으려면 어떤 표기법을 써야 하는가?"
  options={['마침표 표기법(예: obj.name)', '대괄호 표기법(예: obj[변수])', '느낌표를 앞에 붙이는 표기법', '중괄호로 감싸는 표기법']}
  correctAnswer={2}
  explanation="대괄호 표기법은 대괄호 안에 문자열이나 변수를 넣어 프로퍼티 이름을 동적으로 지정할 수 있다. 마침표 표기법은 프로퍼티 이름을 코드 작성 시점에 고정된 식별자로 직접 적어야 하므로 변수에 담긴 이름으로는 접근할 수 없다."
  optionExplanations={['마침표 표기법은 프로퍼티 이름을 코드에 고정해서 적어야 하며 변수 값을 그대로 이름처럼 쓸 수 없다', '정답이다. 대괄호 표기법은 변수에 담긴 값을 프로퍼티 이름으로 사용할 수 있다', '느낌표를 앞에 붙이는 것은 논리 부정 연산이며 프로퍼티 접근 문법이 아니다', '중괄호로 감싸는 것은 객체 리터럴이나 코드 블록을 나타낼 뿐 프로퍼티 접근 문법이 아니다']}
/>

<Quiz
  questionNumber={18}
  question="HTML 문서 안에서 아이디 값이 특정 문자열인 요소 하나를 정확히 선택할 때 사용하는 DOM 메서드는?"
  options={['querySelectorAll', 'getElementsByClassName', 'getElementById', 'getElementsByTagName']}
  correctAnswer={3}
  explanation="getElementById는 문서 전체에서 지정한 아이디 값을 가진 요소 하나만 정확히 찾아 반환한다. 아이디는 문서 안에서 유일해야 하므로 이 메서드는 항상 요소 하나 또는 없음을 돌려준다."
  optionExplanations={['querySelectorAll은 조건에 맞는 모든 요소를 담은 목록을 반환하는 범용 선택 메서드다', 'getElementsByClassName은 아이디가 아니라 클래스 이름으로 여러 요소를 찾는 메서드다', '정답이다. getElementById는 아이디로 요소 하나를 정확히 찾는다', 'getElementsByTagName은 태그 이름으로 여러 요소를 찾는 메서드로 아이디 검색과 무관하다']}
/>

<Quiz
  questionNumber={19}
  question="자식 요소에서 발생한 이벤트가 부모 요소로 전파되어, 부모에 등록된 같은 종류의 이벤트 처리기도 함께 실행되는 현상을 가리키는 용어는?"
  options={['이벤트 캡처링', '이벤트 버블링', '이벤트 위임', '이벤트 취소']}
  correctAnswer={2}
  explanation="이벤트 버블링(bubbling, 거품이 올라오듯 퍼지는 것)은 이벤트가 발생한 요소에서 시작해 상위 조상 요소 방향으로 차례로 전파되는 흐름을 말한다. 캡처링은 반대로 최상위 조상에서 이벤트가 발생한 요소 방향으로 먼저 내려가는 흐름이고, 이벤트 위임은 이 버블링 성질을 이용해 부모 하나에만 처리기를 두는 응용 기법이다."
  optionExplanations={['캡처링은 상위에서 하위로 먼저 내려가는 반대 방향의 전파 단계다', '정답이다. 버블링은 하위에서 상위로 이벤트가 전파되는 현상 자체를 가리킨다', '이벤트 위임은 버블링을 활용하는 프로그래밍 기법의 이름이지, 전파 현상 자체의 이름은 아니다', '이벤트 취소는 기본 동작이나 추가 전파를 막는 조작으로 전파 현상 자체를 가리키는 말이 아니다']}
/>

<Quiz
  questionNumber={20}
  question="XML 문서와 HTML 문서를 비교한 설명으로 가장 옳은 것은?"
  options={['XML은 태그의 의미와 표시 방법이 미리 정해져 있고, HTML은 태그 이름을 사용자가 자유롭게 정의한다', 'HTML은 태그의 의미와 표시 방법이 미리 정해져 있고, XML은 태그 이름을 사용자가 목적에 맞게 직접 정의할 수 있다', 'XML과 HTML은 태그를 정의하는 방식이 완전히 동일하다', 'XML은 데이터를 표현할 수 없고 오직 화면 표시에만 쓰인다']}
  correctAnswer={2}
  explanation="HTML은 태그 집합과 각 태그의 의미·기본 표시 방식이 표준으로 고정된 마크업 언어다. 반면 확장 가능한 마크업 언어(XML)는 특정 표시 방식을 정하지 않고, 사용자가 데이터 구조에 맞게 태그 이름을 직접 정의해 쓰는 범용 데이터 표현 언어다."
  optionExplanations={['HTML과 XML의 특징이 반대로 서술되어 있다', '정답이다. HTML은 태그가 고정되어 있고 XML은 태그를 사용자가 정의한다', '두 언어는 태그 정의 방식이 서로 달라 동일하다고 볼 수 없다', 'XML은 화면 표시가 아니라 구조화된 데이터를 표현하고 교환하는 데 주로 쓰인다']}
/>

<Quiz
  questionNumber={21}
  question="Ajax(비동기 자바스크립트와 XML) 방식의 핵심 특징으로 가장 적절한 것은?"
  options={['페이지 전체를 다시 불러오지 않고도 서버와 데이터를 주고받아 화면 일부만 갱신할 수 있다', '반드시 XML 형식으로만 데이터를 주고받을 수 있다', '서버 요청이 끝날 때까지 사용자의 다른 모든 조작을 차단한다', '자바스크립트 없이 순수 HTML만으로 구현하는 기법이다']}
  correctAnswer={1}
  explanation="Ajax는 브라우저가 백그라운드에서 서버와 데이터를 주고받게 해, 페이지 전체를 새로 불러오지 않고도 필요한 부분만 갱신할 수 있게 하는 기법이다. 이름에 XML이 들어가지만 실제로는 JSON 등 다른 형식도 널리 쓰인다."
  optionExplanations={['정답이다. 화면 전체 새로고침 없이 필요한 부분만 비동기로 갱신하는 것이 핵심이다', 'Ajax라는 이름과 달리 실제로는 JSON 등 XML이 아닌 형식도 널리 사용된다', '비동기(asynchronous, 동시에 진행되지 않아도 되는 방식) 통신이 핵심이므로 다른 조작을 차단하지 않는 것이 장점이다', 'Ajax는 자바스크립트를 이용해 서버와 통신하는 기법이므로 자바스크립트가 반드시 필요하다']}
/>

<Quiz
  questionNumber={22}
  question="시각장애인이 스크린 리더(화면 내용을 음성으로 읽어 주는 보조 기술)로 이미지를 인식할 수 있도록 대체 텍스트를 제공하는 데 사용하는 HTML 속성은?"
  options={['title', 'alt', 'placeholder', 'label']}
  correctAnswer={2}
  explanation="alt 속성은 이미지가 표시되지 못하거나 스크린 리더로 읽힐 때를 대비해 이미지의 내용을 대신 전달하는 대체 텍스트를 담는다. 웹 접근성 관점에서 의미 있는 이미지에는 alt 속성이 반드시 있어야 한다."
  optionExplanations={['title은 마우스를 올렸을 때 나타나는 보조 설명용 속성으로 대체 텍스트의 역할을 대신하지 않는다', '정답이다. alt는 이미지의 대체 텍스트를 제공하는 접근성 핵심 속성이다', 'placeholder는 입력칸에 쓰는 안내 문구 속성으로 이미지와는 관계가 없다', 'label은 폼 요소의 이름표를 만드는 별도 요소로 이미지의 대체 텍스트 속성이 아니다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 시험안내](https://bdes.nile.or.kr/nile/exam/nExam2_1.do)
- [국가평생교육진흥원 독학학위제 공지사항](https://bdes.nile.or.kr/nile/newAdv/nNewAdv1_1.do)
