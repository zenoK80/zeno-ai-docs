---
title: '24. 독학사 2단계 이산수학 예상 모의고사 ①'
description: '독학사 2단계 이산수학 출제기준 전 범위(논리·증명, 집합·관계·함수, 정수론, 세기·조합, 점화식, 그래프·트리, 부울대수)를 종합한 예상 모의고사 24문항과 전 과정 해설'
---

<Callout type="warning">
  **이 문제는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학사 2단계 이산수학 출제기준을 바탕으로 새로 구성한 종합 모의고사입니다. 실제 회차의 문항 수·배점·시험 시간·합격 기준은 공고마다 다를 수 있으므로 반드시 최신 시행 공고를 함께 확인해야 한다. 이 편은 03편부터 19편까지 다룬 이산수학 전 범위(명제논리, 술어논리, 증명 기법, 집합, 관계, 함수, 정수론, 세기·조합, 점화식, 그래프·트리, 부울대수·논리회로)를 실제 시험과 비슷한 비중으로 섞어 재구성했다.
</Callout>

이 모의고사는 24문항으로 전 범위를 고르게 다룬다. 실전처럼 시간을 정해두고(문항당 2~3분 기준) 먼저 전부 풀어본 뒤, 채점 후 틀린 문항의 해설을 정독하는 순서를 권장한다. 계산형 문항은 답만 확인하지 말고 해설에 적힌 대입 과정을 직접 손으로 다시 써 보며 검산하고, 증명 기법을 묻는 문항은 어떤 증명법이 왜 그 상황에 적합한지 근거까지 설명할 수 있어야 한다.

## 명제논리와 술어논리 (1–3번)

<Quiz
  questionNumber={1}
  question="명제 p가 거짓이고 q가 참일 때, 조건문 p→q(만약 p이면 q이다)의 진리값은?"
  options={['참', '거짓', 'p, q 값만으로는 알 수 없다', '참이면서 거짓이다']}
  correctAnswer={1}
  explanation="조건문 p→q는 오직 p가 참이면서 q가 거짓인 경우에만 거짓이 되고, 나머지 세 조합(p거짓·q참, p거짓·q거짓, p참·q참)에서는 모두 참이다. 이 문제는 p=거짓, q=참인 경우이므로 조건문은 참이다."
  optionExplanations={['정답이다. p가 거짓이면 전건이 거짓이므로 조건문은 무조건 참이 된다.', 'p=참, q=거짓인 유일한 거짓 경우와 혼동한 답이다.', '조건문은 p, q 값이 정해지면 진리값도 하나로 정해지는 명제이므로 이 설명은 틀렸다.', '한 명제가 동시에 참이면서 거짓일 수는 없으므로 성립하지 않는다.']}
/>

<Quiz
  questionNumber={2}
  question="명제 (p∧q)∨(p∧¬q)와 논리적으로 동치인 식은?"
  options={['q', 'p', '¬p', 'p∨q']}
  correctAnswer={2}
  explanation="분배법칙을 적용하면 (p∧q)∨(p∧¬q) = p∧(q∨¬q)이다. q∨¬q는 배중률에 의해 항상 참(true)이므로 p∧참 = p가 된다. 진리표로 확인해도 p가 참이면 q값과 무관하게 전체 식이 참이고, p가 거짓이면 두 항 모두 거짓이 되어 전체가 거짓이므로 원래 식은 p와 정확히 같은 진리값을 갖는다."
  optionExplanations={['이것은 분배법칙을 적용하지 않고 q만 남긴 오답으로, 실제로는 p값에 따라 전체 식의 진리값이 달라진다.', '정답이다. p∧(q∨¬q)에서 q∨¬q는 항상 참이므로 식 전체는 p와 같아진다.', '이것은 원래 식을 부정한 것과 혼동한 답으로, ¬p는 원래 식과 반대의 진리값을 갖는다.', '이것은 p나 q 중 하나만 참이어도 참이 되는 식으로, 원래 식이 p=거짓일 때 항상 거짓이 되는 것과 다르다(p=거짓, q=참이면 p∨q는 참이지만 원래 식은 거짓이다).']}
/>

<Quiz
  questionNumber={3}
  question="전칭명제 ∀x(P(x)→Q(x))(모든 x에 대해 P(x)이면 Q(x)이다)의 부정으로 옳은 것은?"
  options={['∀x(¬P(x)∨Q(x))', '∃x(¬P(x)∨Q(x))', '∃x(P(x)∧¬Q(x))', '∀x(P(x)∧¬Q(x))']}
  correctAnswer={3}
  explanation="전칭 정량자의 부정은 존재 정량자로 바뀌므로 먼저 ¬∀x(...) = ∃x ¬(...)가 된다. 그다음 안쪽의 조건문 P(x)→Q(x)의 부정은 논리적 동치 관계(p→q ≡ ¬p∨q)를 이용해 ¬(P(x)→Q(x)) = ¬(¬P(x)∨Q(x)) = P(x)∧¬Q(x)로 정리된다. 두 단계를 합치면 ∃x(P(x)∧¬Q(x))가 된다."
  optionExplanations={['정량자를 전칭 그대로 두어 부정 법칙(전칭↔존재 교환)을 적용하지 않은 오류다.', '정량자는 존재로 올바르게 바꿨지만 안쪽 조건문의 부정을 잘못 처리해 P(x)를 부정하지 않은 오류다.', '정답이다. 정량자는 존재로 바뀌고, 조건문의 부정 법칙에 따라 P(x)는 그대로, Q(x)는 부정되어 AND로 묶인다.', '정량자를 전칭 그대로 둔 채 조건문만 부정한 오류로, 정량자 교환을 빠뜨렸다.']}
/>

## 증명 기법 (4–5번)

<Quiz
  questionNumber={4}
  question="명제 '어떤 정수 n에 대해 n²이 짝수이면 n도 짝수이다'를 증명할 때 가장 적절한 방법과 그 개요로 옳은 것은?"
  options={['직접증명법 - n²이 짝수이므로 n=2k로 놓고 전개한다', '수학적 귀납법 - n=1부터 시작해 다음 항으로 넘어간다', '귀류법 - n이 짝수라고 가정한 뒤 모순을 이끌어낸다', '대우법 - n이 홀수이면 n²도 홀수임을 보인다']}
  correctAnswer={4}
  explanation="원래 명제 'n²이 짝수이면 n도 짝수이다'의 대우는 'n이 홀수이면 n²도 홀수이다'이며, 두 명제는 논리적으로 동치이다. 대우 쪽은 n=2k+1(k는 정수)로 놓고 바로 전개하면 n²=(2k+1)²=4k²+4k+1=2(2k²+2k)+1로, 2배수에 1을 더한 형태이므로 홀수임이 곧바로 드러난다. 원래 명제를 직접 증명하려 하면 'n²이 짝수'라는 조건에서 n의 꼴을 바로 잡기 어려운 반면, 대우는 출발점(n이 홀수)이 명확해 계산이 간단하다."
  optionExplanations={['n²이 짝수라는 사실만으로는 n을 곧바로 2k꼴로 놓을 근거가 없어 이 경로로는 증명이 매끄럽게 진행되지 않는다.', '이 명제는 자연수 n에 대해 n=k에서 n=k+1로 이어지는 귀납적 구조를 갖지 않으므로 수학적 귀납법의 대상이 아니다.', '귀류법도 원리적으로는 가능하지만 결론(n이 짝수)의 부정, 즉 n이 홀수라는 가정을 세우고 전개하는 과정이 사실상 대우법과 같은 계산을 거치므로 표준적인 선택은 아니다.', '정답이다. 대우 명제(n이 홀수이면 n²도 홀수이다)는 n=2k+1로 두고 바로 전개되어 원래 증명보다 훨씬 간단하다.']}
/>

<Quiz
  questionNumber={5}
  question="수학적 귀납법으로 1+2+3+...+n = n(n+1)/2 를 증명할 때, 귀납 단계에서 n=k일 때 성립한다고 가정한 후 n=k+1에서 보여야 할 식은?"
  options={['1+2+...+k+(k+1) = (k+1)(k+2)/2', '1+2+...+k = k(k+1)/2', '(k+1)(k+2)/2 = k(k+1)/2', '1+2+...+(k+1) = k(k+1)/2']}
  correctAnswer={1}
  explanation="수학적 귀납법의 귀납 단계는 n=k일 때 등식이 성립한다는 귀납 가정을 이용해, n=k+1일 때도 등식이 성립함을 보이는 것이다. 원래 등식의 n 자리에 k+1을 대입하면 좌변은 1+2+...+k+(k+1), 우변은 (k+1)((k+1)+1)/2 = (k+1)(k+2)/2가 되므로, 이 등식을 보이는 것이 목표다. 실제 증명은 귀납 가정(1+2+...+k=k(k+1)/2)의 양변에 (k+1)을 더해 우변이 k(k+1)/2+(k+1) = (k+1)(k+2)/2로 정리됨을 보인다."
  optionExplanations={['정답이다. n 자리에 k+1을 대입한 좌변과 우변이 바로 귀납 단계에서 보여야 할 식이다.', '이것은 귀납 가정 그 자체로, n=k일 때 이미 참이라고 전제한 식이지 n=k+1에서 새로 보여야 할 식이 아니다.', '좌변에 (k+1)번째 항을 더하지 않아 원래 등식의 좌변 형태를 갖추지 못했다.', '우변에 n=k+1을 대입하지 않고 n=k일 때의 우변을 그대로 남겨둔 오류다.']}
/>

## 집합 (6–7번)

<Quiz
  questionNumber={6}
  question="전체집합 U의 원소 수가 20, 집합 A의 원소 수가 12일 때, A의 여집합(complement) 원소 수는?"
  options={['12', '8', '32', '20']}
  correctAnswer={2}
  explanation="여집합 Aᶜ는 전체집합 U에 속하지만 A에는 속하지 않는 원소들의 모임이므로, |Aᶜ| = |U| - |A|로 계산한다. 값을 대입하면 |Aᶜ| = 20 - 12 = 8이다."
  optionExplanations={['이것은 원래 A의 원소 수를 그대로 답으로 낸 것으로, 여집합의 정의를 적용하지 않았다.', '정답이다. 20-12=8이다.', '이것은 20과 12를 빼는 대신 더한 계산 실수다.', '이것은 여집합을 구하지 않고 전체집합의 원소 수를 그대로 낸 것이다.']}
/>

<Quiz
  questionNumber={7}
  question="원소 수가 5인 집합에서, 원소 수가 정확히 3인 부분집합의 개수는?"
  options={['15', '20', '10', '5']}
  correctAnswer={3}
  explanation="크기가 n인 집합에서 크기가 r인 부분집합의 개수는 조합 공식 C(n, r) = n!/(r!(n-r)!)로 구한다. n=5, r=3을 대입하면 C(5,3) = 5!/(3!2!) = (5×4×3×2×1)/((3×2×1)×(2×1)) = 120/12 = 10이다."
  optionExplanations={['이것은 C(5,3)이 아니라 다른 조합값(예: 5×3)과 혼동한 계산 실수다.', '이것은 4!(=24)에 가까운 값으로, 조합 공식을 잘못 적용한 계산 실수다.', '정답이다. C(5,3)=10이다.', '이것은 원래 집합의 원소 수 5를 그대로 답으로 낸 것으로, 부분집합 개수를 구하지 않았다.']}
/>

## 관계 (8–9번)

<Quiz
  questionNumber={8}
  question="집합 A={1,2,3} 위의 관계 R = {(1,1),(2,2),(3,3),(1,3)}에 대한 설명으로 옳은 것은?"
  options={['대칭적이고 추이적이지만 반사적이지는 않다', '반사적이지도 추이적이지도 않다', '대칭적이지도 반사적이지도 않다', '반사적이고 추이적이지만 대칭적이지는 않다']}
  correctAnswer={4}
  explanation="반사성은 (1,1), (2,2), (3,3)이 모두 R에 있으므로 성립한다. 대칭성은 (1,3)이 R에 있지만 (3,1)은 R에 없으므로 성립하지 않는다. 추이성은 R에 있는 모든 순서쌍의 조합을 확인해야 하는데, (1,3)에서 이어지는 쌍이 (3,3) 하나뿐이고 (1,3)&(3,3)이면 (1,3)이 있어야 하는데 실제로 있으므로 문제가 되는 조합이 없다. 따라서 R은 반사적이고 추이적이지만 대칭적이지 않다."
  optionExplanations={['(1,1), (2,2), (3,3)이 모두 존재하므로 반사적이라는 점을 놓친 설명이고, (1,3)만 있고 (3,1)이 없으므로 대칭적이라는 설명도 틀렸다.', '(1,1), (2,2), (3,3)이 모두 존재하므로 반사적이며, 위에서 확인했듯 추이성 조건을 위반하는 조합도 없으므로 이 설명은 틀렸다.', '반사성은 성립하므로 이 설명은 틀렸다(대칭적이지 않다는 부분만 옳다).', '정답이다. (1,1),(2,2),(3,3)이 있어 반사적이고, 추이성을 위반하는 조합이 없어 추이적이지만, (1,3)만 있고 (3,1)이 없어 대칭적이지 않다.']}
/>

<Quiz
  questionNumber={9}
  question="정수 집합에서 a와 b가 mod 3에 대해 합동(a≡b, 3으로 나눈 나머지가 같다)이라는 관계로 만들어지는 동치류(equivalence class)의 개수는?"
  options={['3', '2', '6', '1']}
  correctAnswer={1}
  explanation="mod 3 합동 관계는 정수를 3으로 나눈 나머지에 따라 분류한다. 나머지가 될 수 있는 값은 0, 1, 2 세 가지뿐이므로, 정수 전체는 나머지가 0인 정수들의 모임, 나머지가 1인 정수들의 모임, 나머지가 2인 정수들의 모임이라는 서로 겹치지 않는 3개의 동치류로 나누어진다."
  optionExplanations={['정답이다. 3으로 나눈 나머지는 0, 1, 2 세 가지뿐이므로 동치류도 3개다.', '이것은 나머지의 가짓수를 잘못 센 값이다.', '이것은 mod 6처럼 다른 법(modulus)과 혼동한 값이다.', '동치류가 1개라면 모든 정수가 서로 합동이어야 하는데, 예를 들어 1과 2는 3으로 나눈 나머지가 달라 합동이 아니므로 이 설명은 틀렸다.']}
/>

## 함수 (10–11번)

<Quiz
  questionNumber={10}
  question="정수 전체 집합에서 정수 전체 집합으로 정의된 함수 f(x)=2x의 성질로 옳은 것은?"
  options={['전사이지만 단사는 아니다', '단사이지만 전사는 아니다', '단사이면서 전사이다(전단사이다)', '단사도 전사도 아니다']}
  correctAnswer={2}
  explanation="단사는 서로 다른 입력이 항상 서로 다른 출력을 내는 성질이다. x1≠x2이면 2x1≠2x2이므로 f는 단사다. 하지만 공역(치역이 아니라 정의된 도착 집합)이 정수 전체인데, f(x)=2x의 결과는 항상 짝수 정수만 나오므로 3, 5 같은 홀수 정수는 어떤 입력의 출력도 될 수 없다. 따라서 공역을 전부 채우지 못해 전사가 아니다."
  optionExplanations={['f는 서로 다른 입력이 항상 서로 다른 출력을 내므로 단사이며, 이 설명은 단사 여부를 반대로 판단했다.', '정답이다. f는 단사이지만, 홀수 정수는 치역에 포함되지 않아 전사는 아니다.', '전사가 아니므로 전단사(단사이면서 전사)일 수 없다.', 'f는 서로 다른 입력에 대해 항상 서로 다른 출력을 내므로 단사는 성립한다.']}
/>

<Quiz
  questionNumber={11}
  question="f(x)=3x-1, g(x)=x+2일 때, 합성함수 (g∘f)(2)의 값은?"
  options={['5', '9', '7', '8']}
  correctAnswer={3}
  explanation="합성함수 (g∘f)(x)는 f를 먼저 적용한 뒤 그 결과에 g를 적용하는 것으로, (g∘f)(x)=g(f(x))로 계산한다. 먼저 f(2)=3×2-1=5를 구하고, 그 결과를 g에 대입하면 g(5)=5+2=7이다."
  optionExplanations={['이것은 f(2)=5까지만 계산하고 g를 적용하지 않은 중간 결과다.', '이것은 g(2)=4를 먼저 구한 뒤 f를 반대 순서로 적용하는 등 합성 순서를 뒤바꾼 계산 실수다(3×4-1=11도 아니어서 단순 오기일 수 있다).', '정답이다. f(2)=5이고 g(5)=7이다.', '이것은 3×2+2처럼 함수식을 정확히 대입하지 않은 계산 실수다.']}
/>

## 정수론 (12–13번)

<Quiz
  questionNumber={12}
  question="유클리드 알고리즘으로 gcd(48,18)을 구하는 과정과 답으로 옳은 것은?"
  options={['48=2×18+12, 18=2×12+... 이므로 gcd=12', '48과 18을 소인수분해해서 공통 인수를 곱하면 gcd=18이다', '48÷18=2이므로 gcd(48,18)=2이다', '48=2×18+12, 18=1×12+6, 12=2×6+0 이므로 gcd(48,18)=6']}
  correctAnswer={4}
  explanation="유클리드 알고리즘은 나눗셈을 반복해 나머지가 0이 되는 순간의 나누는 수가 최대공약수임을 이용한다. 48=2×18+12(나머지 12), 18=1×12+6(나머지 6), 12=2×6+0(나머지 0)으로 나머지가 0이 되었으므로, 그 직전 나누는 수인 6이 gcd(48,18)이다."
  optionExplanations={['18=2×12+...로 계산하면 18-24가 음수가 되어 나눗셈 몫이 틀렸다(18=1×12+6이 올바른 몫과 나머지다).', '소인수분해로 구할 수는 있지만(48=2⁴×3, 18=2×3², 공통 인수는 2×3=6) 이 선택지의 결과값 18은 계산이 틀렸다.', '이것은 나눗셈의 몫만 구한 것으로 최대공약수와는 무관하다.', '정답이다. 나머지가 0이 될 때까지 나눗셈을 반복하면 마지막 나누는 수 6이 최대공약수다.']}
/>

<Quiz
  questionNumber={13}
  question="17을 5로 나눈 나머지와, 23이 17과 mod 5에서 합동인지 여부로 옳은 것은?"
  options={['17 mod 5 = 2이고, 23은 17과 mod 5에서 합동이 아니다', '17 mod 5 = 3이고, 23은 17과 합동이다', '17 mod 5 = 2이고, 23은 17과 합동이다', '17 mod 5 = 0이고, 23은 17과 합동이 아니다']}
  correctAnswer={1}
  explanation="17을 5로 나누면 17=3×5+2이므로 나머지는 2이다. 23을 5로 나누면 23=4×5+3이므로 나머지는 3이다. 두 수를 5로 나눈 나머지가 각각 2와 3으로 서로 다르므로, 23은 17과 mod 5에서 합동이 아니다."
  optionExplanations={['정답이다. 17 mod 5=2이고, 23 mod 5=3으로 나머지가 달라 합동이 아니다.', '17을 5로 나눈 나머지는 2이지 3이 아니며, 나머지가 다르므로 합동이라는 결론도 틀렸다.', '17 mod 5는 2가 맞지만, 23 mod 5=3으로 나머지가 달라 합동이라는 결론은 틀렸다.', '17을 5로 나눈 나머지는 0이 아니라 2이다.']}
/>

## 세기·조합·포함배제·비둘기집 (14–16번)

<Quiz
  questionNumber={14}
  question="서로 다른 8명 중 3명을 뽑아 순서를 따지지 않고 위원회를 구성하는 경우의 수는?"
  options={['336', '56', '24', '168']}
  correctAnswer={2}
  explanation="순서를 따지지 않고 뽑는 것이므로 조합 공식 C(n,r)=n!/(r!(n-r)!)을 쓴다. n=8, r=3을 대입하면 C(8,3)=8!/(3!5!)=(8×7×6)/(3×2×1)=336/6=56이다."
  optionExplanations={['이것은 순서를 고려한 순열 P(8,3)=8×7×6=336을 그대로 답으로 낸 것으로, 조합이 아니라 순열을 계산한 오류다.', '정답이다. C(8,3)=56이다.', '이것은 계산 도중 나눗셈을 잘못 적용한 값이다.', '이것은 P(8,3)=336을 2로 나누는 등 잘못된 보정을 한 값이다.']}
/>

<Quiz
  questionNumber={15}
  question="1부터 100까지 자연수 중 3의 배수 또는 5의 배수인 수의 개수는?"
  options={['53', '40', '47', '60']}
  correctAnswer={3}
  explanation="포함배제원리에 따라 |A∪B|=|A|+|B|-|A∩B|를 쓴다. 3의 배수는 100/3을 내림하여 33개, 5의 배수는 100/5=20개, 3과 5의 공배수(즉 15의 배수)는 100/15를 내림하여 6개다. 3의 배수와 5의 배수를 단순히 더하면 15의 배수(둘 다에 속하는 수)가 두 번 세어지므로 한 번 빼서 보정한다. 33+20-6=47이다."
  optionExplanations={['이것은 15의 배수 개수를 빼지 않고 그대로 더한 값(33+20=53)으로, 중복 계수 오류다.', '이것은 15의 배수 개수를 잘못 계산해서 두 번 뺀 값(33+20-6-6=41과도 다르지만 유사한 유형의 계산 실수)이다.', '정답이다. 33+20-6=47이다.', '이것은 3의 배수와 5의 배수 개수 자체를 잘못 계산한 값이다.']}
/>

<Quiz
  questionNumber={16}
  question="빨강, 파랑, 검정 세 가지 색 양말이 서랍 안에 색깔을 볼 수 없게 섞여 있을 때, 같은 색 양말 한 쌍을 반드시 확보하려면 최소 몇 짝을 꺼내야 하는가?"
  options={['3', '6', '2', '4']}
  correctAnswer={4}
  explanation="비둘기집 원리에서 색깔 세 가지를 3개의 집(구멍)으로, 꺼내는 양말을 비둘기로 본다. 집이 3개일 때 비둘기가 3개까지는 각 집에 정확히 하나씩 들어가 짝이 안 생길 수 있지만(예: 빨강 1, 파랑 1, 검정 1), 4번째 양말을 꺼내는 순간 어떤 색이든 이미 그 색이 한 번 나온 상태이므로 반드시 같은 색 쌍이 생긴다. 일반식으로는 (색깔 수)+1=3+1=4짝이 최소 보장 개수다."
  optionExplanations={['3짝까지는 세 색이 각각 한 짝씩 나와 같은 색 쌍이 전혀 없는 경우(최악의 경우)가 가능하므로 3짝으로는 보장되지 않는다.', '6짝은 지나치게 많은 값으로, 최소로 보장되는 개수가 아니다.', '2짝만으로는 서로 다른 두 색이 나올 수 있어 같은 색 쌍이 보장되지 않는다.', '정답이다. 색깔 수(3)에 1을 더한 4짝을 꺼내야 비둘기집 원리에 의해 같은 색 쌍이 반드시 생긴다.']}
/>

## 점화식 (17–18번)

<Quiz
  questionNumber={17}
  question="점화식 a_n = 5a_(n-1) - 6a_(n-2)의 특성방정식과 그 근으로 옳은 것은?"
  options={['x²-5x+6=0, 근은 x=2와 x=3이다', 'x²+5x+6=0, 근은 x=-2와 x=-3이다', 'x²-5x-6=0, 근은 x=6과 x=-1이다', 'x²-6x+5=0, 근은 x=1과 x=5이다']}
  correctAnswer={1}
  explanation="상수계수 선형 점화식 a_n=5a_(n-1)-6a_(n-2)에서 a_n을 xⁿ으로 치환하면 xⁿ=5x^(n-1)-6x^(n-2)이 되고, 양변을 x^(n-2)로 나누면 특성방정식 x²=5x-6, 즉 x²-5x+6=0을 얻는다. 이 이차방정식을 인수분해하면 (x-2)(x-3)=0이므로 근은 x=2와 x=3이다."
  optionExplanations={['정답이다. x²-5x+6=0을 인수분해하면 (x-2)(x-3)=0이므로 x=2, 3이다.', '이것은 부호를 반대로 처리해 계수의 부호를 잘못 옮긴 특성방정식이다.', '이것은 가운데 항의 부호를 잘못 옮긴 특성방정식으로, 원래 점화식의 계수와 맞지 않는다.', '이것은 계수 5와 6의 위치를 바꿔 잘못 세운 특성방정식이다.']}
/>

<Quiz
  questionNumber={18}
  question="점화식 a_n=5a_(n-1)-6a_(n-2)에서 a_0=1, a_1=5일 때, a_2의 값은?"
  options={['25', '19', '13', '31']}
  correctAnswer={2}
  explanation="점화식에 n=2를 대입하면 a_2=5a_1-6a_0이다. 값을 대입하면 a_2=5×5-6×1=25-6=19이다. 특성방정식의 근(2, 3)을 이용한 일반해 a_n=A·2ⁿ+B·3ⁿ로 계산해도 a_0=A+B=1, a_1=2A+3B=5에서 A=-2, B=3을 얻고, a_2=-2×4+3×9=-8+27=19로 같은 값이 나와 검산된다."
  optionExplanations={['이것은 6×1을 빼지 않고 그대로 더한 계산 실수(5×5+6×1×...)와 유사한 오류다.', '정답이다. 5×5-6×1=19이다.', '이것은 6×1이 아니라 다른 값을 뺀 계산 실수다.', '이것은 5와 6의 계산 순서를 바꾼 계산 실수다.']}
/>

## 그래프·트리 (19–21번)

<Quiz
  questionNumber={19}
  question="정점이 6개이고 각 정점의 차수가 모두 3인 정규 그래프(3-regular graph)의 간선 수는?"
  options={['18', '9', '6', '12']}
  correctAnswer={2}
  explanation="핸드셰이킹 정리(handshaking theorem)에 따르면 그래프의 모든 정점의 차수를 합한 값은 간선 수의 2배와 같다(각 간선이 양 끝 정점에서 차수 1씩을 두 번 기여하기 때문이다). 정점 6개가 모두 차수 3이므로 차수의 합은 6×3=18이고, 이는 2×(간선 수)와 같으므로 간선 수=18/2=9이다."
  optionExplanations={['이것은 차수의 합(18)을 2로 나누지 않고 그대로 답으로 낸 오류다.', '정답이다. 차수의 합 18을 2로 나누면 간선 수는 9다.', '이것은 정점 수를 그대로 답으로 낸 것으로 핸드셰이킹 정리를 적용하지 않았다.', '이것은 6×3을 잘못 나눈(3으로 나눈) 계산 실수다.']}
/>

<Quiz
  questionNumber={20}
  question="어떤 연결 그래프의 모든 정점의 차수가 짝수일 때 이 그래프에 대한 설명으로 옳은 것은?"
  options={['해밀턴 회로가 반드시 존재한다', '트리이다', '평면 그래프가 아니다', '오일러 회로(Euler circuit)가 존재한다']}
  correctAnswer={4}
  explanation="오일러 정리에 따르면, 연결 그래프에서 모든 정점의 차수가 짝수일 필요충분조건으로 오일러 회로(모든 간선을 정확히 한 번씩 지나 출발점으로 돌아오는 경로)가 존재한다. 이는 각 정점을 지나갈 때마다 들어오는 간선과 나가는 간선이 짝을 이뤄야 하므로, 차수가 짝수여야 회로가 끊기지 않고 이어질 수 있기 때문이다."
  optionExplanations={['해밀턴 회로는 오일러 회로와 별개의 조건(모든 정점을 정확히 한 번씩 방문)으로, 차수가 모두 짝수라는 조건만으로는 해밀턴 회로의 존재가 보장되지 않는다.', '트리는 사이클이 전혀 없는 그래프인데, 모든 정점의 차수가 짝수이면서 간선이 있는 그래프는 대부분 사이클을 포함하므로 트리와는 무관한 조건이다.', '차수가 모두 짝수라는 조건은 평면성(그래프를 평면에 선이 겹치지 않게 그릴 수 있는지)과는 직접적인 관련이 없다.', '정답이다. 모든 정점의 차수가 짝수인 연결 그래프는 오일러 회로를 갖는다는 것이 오일러 정리의 핵심 내용이다.']}
/>

<Quiz
  questionNumber={21}
  question="정점이 10개인 트리(tree)의 간선 수는?"
  options={['10', '8', '9', '45']}
  correctAnswer={3}
  explanation="트리는 정의상 사이클이 없는 연결 그래프이며, 정점이 n개인 트리는 항상 정확히 n-1개의 간선을 갖는다. 이는 트리를 정점 1개짜리 그래프에서 시작해 새 정점을 추가할 때마다 기존 트리와 연결하는 간선을 정확히 1개씩만 추가해야 사이클이 생기지 않기 때문이다. 정점이 10개이므로 간선 수는 10-1=9이다."
  optionExplanations={['이것은 정점 수를 그대로 간선 수로 낸 오류로, n-1 공식을 적용하지 않았다.', '이것은 10-1이 아니라 다른 값을 뺀 계산 실수다.', '정답이다. 트리는 정점 수보다 정확히 1개 적은 간선을 가지므로 10-1=9이다.', '이것은 완전그래프 K10의 간선 수 C(10,2)=45와 혼동한 값으로, 트리와는 무관한 값이다.']}
/>

## 부울대수·논리회로 (22–24번)

<Quiz
  questionNumber={22}
  question="부울식 A + ¬A·B(A 또는 (A의 부정 AND B))를 간소화하면?"
  options={['A', 'B', 'AB', 'A+B']}
  correctAnswer={4}
  explanation="분배법칙을 적용하면 A+¬A·B = (A+¬A)(A+B)이다. 여기서 A+¬A는 보원법칙(complement law)에 의해 항상 1(참)이므로 식은 1×(A+B)=A+B로 정리된다. 진리표로도 확인할 수 있다: A=1이면 좌변은 1+(0×B)=1이고 우변 A+B도 1+B=1로 같다. A=0이면 좌변은 0+(1×B)=B이고 우변도 0+B=B로 같다."
  optionExplanations={['A=0, B=1인 경우 원래 식은 0+(1×1)=1이지만 이 선택지의 값(A=0)은 0이 되어 일치하지 않는다.', 'A=1, B=0인 경우 원래 식은 1+(0×0)=1이지만 이 선택지의 값(B=0)은 0이 되어 일치하지 않는다.', 'A=1, B=0인 경우 원래 식은 1이지만 AB=1×0=0이 되어 일치하지 않는다.', '정답이다. 분배법칙과 보원법칙을 차례로 적용하면 A+¬A·B=A+B가 된다.']}
/>

<Quiz
  questionNumber={23}
  question="AND, OR, NOT 게이트로 구성한 부울식 A·¬B + ¬A·B가 나타내는 값에서, A=1, B=1일 때 출력값은?"
  options={['1', 'A와 B에 따라 다르다', '0', '항상 1이다']}
  correctAnswer={3}
  explanation="부울식 A·¬B + ¬A·B는 A와 B가 서로 다를 때만 1이 되는 배타적 논리합(XOR) 연산을 나타낸다. A=1, B=1을 대입하면 A·¬B = 1×0 = 0이고 ¬A·B = 0×1 = 0이므로, 전체 식은 0+0=0이다. 이는 XOR의 정의(두 입력이 같으면 0, 다르면 1)와도 일치한다."
  optionExplanations={['A=1, ¬B=0이므로 A·¬B=0이고, ¬A=0, B=1이므로 ¬A·B=0이 되어 합은 1이 아니라 0이다.', '이 값은 A, B가 주어지면 정해진 부울식을 계산한 것이므로 항상 하나의 값으로 정해지며, 이 문제(A=1,B=1)에서는 0으로 고정된다.', '정답이다. A와 B가 같은 값(둘 다 1)일 때 이 식(XOR)은 0이 된다.', 'A=1, B=1일 때는 0이 나오므로 항상 1이라는 설명은 틀렸다(A와 B가 서로 다를 때만 1이 나온다).']}
/>

<Quiz
  questionNumber={24}
  question="완전그래프(complete graph) K5(정점 5개, 서로 다른 모든 정점 쌍이 간선으로 연결된 그래프)의 간선 수는?"
  options={['20', '5', '25', '10']}
  correctAnswer={4}
  explanation="완전그래프 Kn의 간선 수는 서로 다른 정점 두 개를 순서 없이 고르는 경우의 수와 같으므로 조합 공식 C(n,2)로 구한다. n=5이므로 C(5,2)=5!/(2!3!)=(5×4)/(2×1)=20/2=10이다."
  optionExplanations={['이것은 C(5,2)의 분자값 5×4=20을 2로 나누지 않고 그대로 답으로 낸 계산 실수다.', '이것은 정점 수를 그대로 간선 수로 낸 것으로 완전그래프의 정의를 적용하지 않았다.', '이것은 5×5처럼 조합이 아니라 곱을 그대로 계산한 오류다.', '정답이다. C(5,2)=10이다.']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [Wolfram MathWorld — Discrete Mathematics](https://mathworld.wolfram.com/topics/DiscreteMathematics.html)
