---
title: '25. 독학사 C프로그래밍 기출·유형 ③ (구조체·파일·동적 메모리)'
description: '구조체·공용체·열거형, 파일 입출력, 동적 메모리 할당 영역을 국가평생교육진흥원 출제기준(평가영역)에 근거해 재구성한 예상 문항 22개로 점검한다'
---

<Callout type="warning">
  **이 편은 실제 기출 문제가 아닙니다.** 독학사 2단계 C프로그래밍 관련 `reference/` 기출 원문은 확보되어 있지 않습니다. 아래 22개 문항은 국가평생교육진흥원 독학학위제 출제기준(평가영역) 중 "구조체·공용체·열거형", "파일 입출력", "동적 메모리 관리" 영역의 세부 항목을 근거로 **새로 구성한 예상 문항**입니다. 실제 시험의 문장·코드·보기 배열과 다를 수 있으므로, 코드를 암기하기보다 한 줄씩 실행을 추적하는 방법 자체를 익히는 데 써야 합니다.

  회차별 실제 출제 문항 수·배점·시간은 매 공고마다 다를 수 있으므로, 응시 전 국가평생교육진흥원 공고 원문을 반드시 확인하십시오.
</Callout>

## 이 편이 다루는 범위와 접근법

이 편은 14편(구조체·공용체·열거형), 16편(파일 입출력), 17편(동적 메모리 할당), 18편(구조체·포인터·파일 통합)에서 다룬 내용을 하나로 묶어 문제 형태로 점검합니다. 이 세 영역은 독학사 C프로그래밍에서 **가장 배점이 크고 실수가 잦은 구간**이며, 크게 세 가지 문제 유형으로 나뉩니다.

1. **출력 예측형** — 구조체 멤버 접근, 공용체의 메모리 공유, 파일에 쓰고 다시 읽는 코드, malloc으로 할당한 배열을 다루는 코드의 실행 결과를 정확히 추적하는 유형입니다. 반드시 **변수 값이 한 줄씩 어떻게 바뀌는지** 표를 그려 가며 확인해야 합니다.
2. **오류 찾기형** — free 이후 포인터 사용(댕글링 포인터), free를 두 번 호출(이중 해제), malloc 이후 반환값을 확인하지 않는 코드, realloc 실패를 처리하지 않는 코드처럼 **컴파일은 되지만 실행 시점에 위험한 코드**를 찾는 유형입니다.
3. **개념 비교형** — 구조체와 공용체의 차이, malloc과 calloc의 차이, 텍스트 모드와 이진 모드의 차이처럼 "겉보기에 비슷하지만 목적이 다른 개념 쌍"을 정확히 구분하는 유형입니다.

풀 때는 항상 다음 순서를 지키십시오. 먼저 코드에 등장하는 변수와 포인터를 나열하고, 한 줄씩 실행하면서 값이 바뀌는 시점을 적습니다. 구조체·공용체는 멤버가 메모리의 어느 위치에 자리 잡는지 머릿속으로(또는 표로) 그려 보고, 포인터가 나오면 "이 포인터가 지금 가리키는 대상이 유효한가"를 항상 확인하는 습관을 들이십시오. 해설은 정답 근거뿐 아니라 각 오답이 어떤 개념과 착각되어 나온 오답인지까지 적어 두었으니, 틀린 문항은 해당 개념이 다뤄진 14·16·17·18편으로 돌아가 다시 확인하십시오.

## 마무리 복습

### 유형 1 — 구조체 정의·멤버 접근·메모리 배치

**이 유형은 이렇게 접근한다:** 구조체(struct)는 서로 다른 자료형을 하나로 묶어 새 자료형을 만들지만, 멤버들이 메모리에 차례로 놓일 때 자료형별 정렬(alignment) 규칙 때문에 **패딩(padding)** 이라는 빈 바이트가 끼어들 수 있습니다. 그래서 구조체의 크기는 "멤버 크기의 단순 합"이 아니라 "정렬 규칙까지 반영한 합"입니다.

<Quiz
  questionNumber={1}
  question="구조체(struct)에 대한 설명으로 옳지 않은 것은?"
  options={['① 구조체는 서로 다른 자료형의 변수들을 하나로 묶어 새로운 자료형을 만든다', '② 구조체 변수를 선언하면 각 멤버가 저장될 만큼의 메모리가 한꺼번에 확보된다', '③ 구조체의 전체 크기는 항상 각 멤버 크기를 단순히 더한 값과 정확히 일치한다', '④ 구조체 태그 이름과 변수 이름은 별개의 개념이며, typedef로 구조체 자료형에 별칭을 붙일 수 있다']}
  correctAnswer={3}
  explanation="구조체는 서로 다른 자료형을 하나로 묶은 새 자료형이고, 변수를 선언하는 순간 멤버들이 저장될 메모리가 한꺼번에 확보된다는 것은 옳다. 태그 이름과 변수 이름이 별개이며 typedef로 별칭을 붙일 수 있다는 것도 옳다. 문제는 크기 계산이다. 대부분의 컴파일러는 각 멤버가 자신의 자료형 크기에 맞는 경계(주소)에서 시작하도록 멤버 사이에 패딩(padding, 사용하지 않는 빈 바이트)을 끼워 넣는다. 그 결과 구조체의 전체 크기는 멤버 크기의 단순 합보다 커질 수 있으므로, 항상 정확히 일치한다는 서술은 옳지 않다."
  optionExplanations={['구조체의 기본 정의를 정확히 설명한 옳은 문장이다.', '구조체 변수 선언 시 메모리가 한꺼번에 확보된다는 것을 정확히 설명한 옳은 문장이다.', '정렬을 위한 패딩 때문에 구조체 크기가 멤버 크기 합보다 커질 수 있다. 항상 정확히 일치한다는 서술이 옳지 않다.', '태그 이름과 변수 이름의 구분, typedef 사용법을 정확히 설명한 옳은 문장이다.']}
/>

다음 코드로 구조체 포인터의 두 접근 방식을 확인해 보겠습니다.

```c
#include <stdio.h>

struct Point {
    int x;
    int y;
};

int main(void) {
    struct Point p1 = {3, 4};
    struct Point *pp = &p1;

    pp->x = pp->x + 10;
    (*pp).y = (*pp).y * 2;

    printf("%d %d\n", p1.x, p1.y);
    return 0;
}
```

```text
13 8
```

<Quiz
  questionNumber={2}
  question="위 프로그램의 실행 결과로 옳은 것은?"
  options={['① 13 8', '② 3 4', '③ 13 4', '④ 3 8']}
  correctAnswer={1}
  explanation="pp는 p1의 주소를 가리키는 구조체 포인터다. pp-&gt;x는 (*pp).x와 완전히 같은 의미로, 화살표 연산자(-&gt;)는 포인터를 역참조한 뒤 멤버에 접근하는 것을 한 번에 표현하는 문법 설탕(syntactic sugar)이다. pp-&gt;x = pp-&gt;x + 10;에서 pp-&gt;x는 p1.x와 같은 메모리를 가리키므로 3 + 10 = 13이 p1.x에 저장된다. 이어서 (*pp).y = (*pp).y * 2;도 마찬가지로 p1.y와 같은 메모리를 다루므로 4 * 2 = 8이 p1.y에 저장된다. 따라서 p1.x=13, p1.y=8이 출력된다."
  optionExplanations={['pp->x와 (*pp).y는 각각 p1.x, p1.y와 같은 메모리를 가리키므로 13과 8이 정확한 계산 결과다.', 'p1의 초기값을 그대로 출력한 값으로, pp를 통한 수정이 반영되지 않았다고 착각한 오답이다.', 'x만 수정되고 y는 초기값 그대로라고 착각한 오답이다. (*pp).y도 p1.y와 같은 메모리이므로 함께 바뀐다.', 'x는 초기값 그대로이고 y만 수정되었다고 착각한 오답으로, 반대로 x도 pp->x 대입문에서 이미 바뀌었다.']}
/>

```c
#include <stdio.h>

struct Sample {
    char c;
    int i;
    char c2;
};

int main(void) {
    printf("%d\n", (int)sizeof(struct Sample));
    return 0;
}
```

<Quiz
  questionNumber={3}
  question="int가 4바이트이고 구조체 멤버가 자신의 크기에 맞는 배수 위치에서 시작하도록 정렬되는 일반적인 환경을 기준으로, 위 프로그램의 실행 결과로 가장 적절한 것은?"
  options={['① 12', '② 6', '③ 9', '④ 16']}
  correctAnswer={1}
  explanation="char c는 1바이트를 쓰지만, 바로 뒤의 int i가 4바이트 경계에서 시작해야 하므로 c 뒤에 3바이트의 패딩이 끼어든다. 그다음 int i가 4바이트를 차지하고, 마지막 char c2가 1바이트를 쓴다. 여기까지 합은 1+3(패딩)+4+1=9바이트지만, 구조체 전체의 크기도 가장 엄격한 정렬 요구사항(여기서는 int의 4바이트)의 배수가 되도록 끝에 다시 패딩이 붙는다. 9바이트는 4의 배수가 아니므로 3바이트를 더 채워 12바이트가 된다. 실제 값은 컴파일러와 플랫폼의 정렬 규칙에 따라 달라질 수 있지만, 4바이트 정렬을 쓰는 일반적인 환경에서는 12가 된다."
  optionExplanations={['char(1)+패딩(3)+int(4)+char(1)+끝 패딩(3)=12바이트로, 멤버 사이·구조체 끝의 정렬 패딩을 모두 반영한 정확한 계산이다.', '패딩을 전혀 고려하지 않고 1+4+1을 단순히 더한 값이다. 실제로는 정렬 규칙 때문에 패딩이 끼어든다.', '멤버 사이의 패딩만 반영하고 구조체 끝의 패딩을 빠뜨린 값이다. 구조체 전체 크기도 정렬 배수를 맞춰야 한다.', '패딩을 과도하게 계산한 값으로, 실제 정렬 규칙에 따른 계산과 맞지 않는다.']}
/>

### 유형 2 — 공용체·열거형

**이 유형은 이렇게 접근한다:** 공용체(union)는 모든 멤버가 **같은 메모리 주소를 공유**합니다. 그래서 한 멤버에 값을 쓰면 다른 멤버로 읽을 때 그 비트 패턴을 다른 자료형으로 재해석한 값이 나옵니다. 열거형(enum)은 이름 붙인 정수 상수의 모음이며, 값을 명시하지 않은 멤버는 바로 앞 멤버보다 1 큰 값을 자동으로 받습니다.

```c
#include <stdio.h>

union Data {
    int i;
    char c[4];
};

int main(void) {
    union Data d;
    d.i = 65;
    printf("%d %c\n", d.i, d.c[0]);
    return 0;
}
```

<Quiz
  questionNumber={4}
  question="리틀 엔디안(little-endian, 정수의 최하위 바이트가 가장 낮은 주소에 저장되는 방식) 시스템을 기준으로, 위 프로그램의 실행 결과로 가장 적절한 것은?"
  options={['① 65 A', '② 65 0', '③ 4 A', '④ 0 A']}
  correctAnswer={1}
  explanation="공용체는 i와 c[4]가 같은 4바이트 메모리를 공유한다. d.i = 65를 대입하면 이 메모리에는 정수 65의 비트 패턴이 저장된다. 리틀 엔디안 시스템에서는 정수의 최하위 바이트가 메모리의 가장 낮은 주소에 저장되므로, c[0]에는 65의 최하위 바이트인 65 자체가 그대로 들어간다. 아스키 코드 65는 문자 A에 대응하므로 %c로 출력하면 A가 나온다. 따라서 d.i는 65, d.c[0]은 A로 출력된다."
  optionExplanations={['공용체는 같은 메모리를 공유하므로 d.i=65, 리틀 엔디안에서 c[0]은 그 최하위 바이트인 65(문자 A)로 정확히 일치한다.', 'c[0]이 0이 된다고 착각한 오답이다. 최하위 바이트는 65 그대로 저장되어 있다.', 'd.i를 배열 c의 원소 개수(4)로 착각한 오답이다. d.i는 여전히 정수 65다.', 'd.i를 0으로 착각한 오답으로, d.i = 65 대입문의 효과를 반영하지 않았다.']}
/>

```c
#include <stdio.h>

enum Weekday { MON, TUE, WED, THU, FRI, SAT = 10, SUN };

int main(void) {
    printf("%d %d %d\n", WED, SAT, SUN);
    return 0;
}
```

<Quiz
  questionNumber={5}
  question="위 프로그램의 실행 결과로 옳은 것은?"
  options={['① 2 10 11', '② 2 5 6', '③ 3 10 11', '④ 2 10 10']}
  correctAnswer={1}
  explanation="열거형은 값을 명시하지 않으면 0부터 시작해 하나씩 증가한다. MON=0, TUE=1, WED=2, THU=3, FRI=4까지는 자동 부여된다. SAT는 명시적으로 10을 지정했으므로 SAT=10이고, 그다음 SUN은 값을 지정하지 않았으므로 바로 앞 멤버인 SAT보다 1 큰 11이 된다. 따라서 WED=2, SAT=10, SUN=11이 출력된다."
  optionExplanations={['WED는 0부터 순서대로 센 2, SAT는 명시된 10, SUN은 SAT+1인 11로 모두 정확하다.', 'SAT 이후에도 앞의 순서(FRI=4)를 그대로 이어 5, 6으로 계산한 오답이다. SAT=10으로 명시되면 그 이후 값은 10을 기준으로 이어진다.', 'WED를 TUE 다음이 아니라 THU 자리로 착각해 3으로 계산한 오답이다.', 'SUN이 SAT와 같은 값을 갖는다고 착각한 오답이다. 값을 지정하지 않은 멤버는 반드시 앞 멤버보다 1 커야 한다.']}
/>

<Quiz
  questionNumber={6}
  question="구조체(struct)와 공용체(union)의 차이에 대한 설명으로 옳지 않은 것은?"
  options={['① 구조체는 멤버마다 독립된 메모리 공간을 가지지만, 공용체는 모든 멤버가 같은 메모리 공간을 공유한다', '② 구조체의 크기는 대체로 멤버 크기의 합에 정렬 패딩을 더한 값이지만, 공용체의 크기는 가장 큰 멤버의 크기(및 그 정렬 요구)를 기준으로 정해진다', '③ 공용체는 한 시점에 여러 멤버가 서로 다른 값을 동시에 유지할 수 있어 구조체보다 메모리를 절약한다', '④ 공용체는 같은 메모리 영역을 여러 자료형으로 다르게 해석하고 싶을 때 사용한다']}
  correctAnswer={3}
  explanation="구조체는 멤버마다 독립된 공간을 갖고, 공용체는 모든 멤버가 같은 공간을 공유한다는 것이 두 자료형의 핵심 차이이며 옳다. 구조체 크기가 정렬 패딩을 포함한 멤버 크기 합인 반면 공용체 크기는 가장 큰 멤버 기준이라는 것도 옳다. 공용체가 메모리를 절약하는 것도 맞지만, 그 이유가 '여러 멤버를 동시에 서로 다른 값으로 유지할 수 있어서'라는 3번의 설명은 틀렸다. 공용체는 오히려 모든 멤버가 같은 메모리를 공유하기 때문에, 한 멤버에 값을 쓰는 순간 다른 멤버로 저장했던 값은 그 비트 패턴이 덮어써져 더 이상 의미 있게 읽을 수 없다. 즉 공용체는 한 시점에 오직 하나의 멤버만 유효한 값으로 유지할 수 있고, 이 점 덕분에 메모리를 절약하는 것이다."
  optionExplanations={['구조체와 공용체의 메모리 공유 여부 차이를 정확히 설명한 옳은 문장이다.', '구조체·공용체의 크기 결정 방식 차이를 정확히 설명한 옳은 문장이다.', '공용체는 모든 멤버가 메모리를 공유하므로 한 시점에는 하나의 멤버만 유효하다. 여러 멤버를 동시에 다른 값으로 유지한다는 설명이 옳지 않다.', '공용체를 같은 메모리의 다른 해석에 쓴다는 용도 설명은 옳다.']}
/>

### 유형 3 — 구조체 배열·포인터·함수 인자

**이 유형은 이렇게 접근한다:** 구조체를 함수 인자로 넘길 때 아무 표시 없이 그냥 넘기면 **구조체 전체가 복사**되어 함수 안에서 값을 바꿔도 원본은 그대로입니다. 원본을 바꾸려면 반드시 구조체의 주소(포인터)를 넘겨야 합니다.

```c
#include <stdio.h>

struct Student {
    char name[10];
    int score;
};

int main(void) {
    struct Student list[3] = {
        {"Kim", 80},
        {"Lee", 90},
        {"Park", 70}
    };
    int sum = 0;
    for (int i = 0; i < 3; i++) {
        sum += list[i].score;
    }
    printf("%d\n", sum);
    return 0;
}
```

<Quiz
  questionNumber={7}
  question="위 프로그램의 실행 결과로 옳은 것은?"
  options={['① 240', '② 230', '③ 250', '④ 80']}
  correctAnswer={1}
  explanation="for문은 i=0, 1, 2를 차례로 순회하며 list[i].score를 sum에 더한다. list[0].score=80, list[1].score=90, list[2].score=70이므로 sum = 80 + 90 + 70 = 240이다."
  optionExplanations={['80+90+70을 정확히 더한 값이다.', '80+90+70 대신 80+90+60처럼 한 항목을 잘못 더한 값이다.', '80+90+70보다 10 많은 값으로 덧셈 과정에서 자릿수를 잘못 더한 오답이다.', 'list[0].score 하나만 출력한 값으로, 반복문 전체 합을 구하지 않은 오답이다.']}
/>

```c
#include <stdio.h>

struct Point { int x, y; };

int main(void) {
    struct Point arr[3] = {{1, 1}, {2, 2}, {3, 3}};
    struct Point *p = arr;

    p++;
    p->x += 10;

    printf("%d %d %d\n", arr[0].x, arr[1].x, arr[2].x);
    return 0;
}
```

<Quiz
  questionNumber={8}
  question="위 프로그램의 실행 결과로 옳은 것은?"
  options={['① 1 12 3', '② 11 2 3', '③ 1 2 13', '④ 1 2 3']}
  correctAnswer={1}
  explanation="p는 처음에 arr을 가리켜 arr[0]의 주소를 담는다. p++;는 구조체 포인터를 구조체 하나 크기만큼 증가시키므로, p는 이제 arr[1]을 가리킨다. p-&gt;x += 10;은 arr[1].x에 10을 더하는 것과 같으므로 arr[1].x가 2에서 12가 된다. arr[0].x와 arr[2].x는 손대지 않았으므로 각각 1, 3 그대로다. 따라서 1 12 3이 출력된다."
  optionExplanations={['p++로 arr[1]을 가리키게 된 뒤 p->x += 10으로 arr[1].x만 12로 바뀌고 나머지는 그대로다.', 'p가 arr[0]을 그대로 가리킨다고 착각해 arr[0].x가 바뀐다고 본 오답이다. p++는 다음 원소로 이동시킨다.', 'p++가 arr[2]까지 두 칸 이동한다고 착각한 오답이다. p++는 한 칸만 이동시킨다.', 'p++와 p->x += 10의 효과가 전혀 반영되지 않은, 초기값을 그대로 출력한 오답이다.']}
/>

```c
#include <stdio.h>

struct Point { int x, y; };

void moveByValue(struct Point p) {
    p.x += 100;
}

int main(void) {
    struct Point a = {1, 2};
    moveByValue(a);
    printf("%d\n", a.x);
    return 0;
}
```

<Quiz
  questionNumber={9}
  question="위 프로그램을 실행하면 101이 아니라 1이 출력된다. 그 이유로 가장 적절한 것은?"
  options={['① moveByValue 함수가 a의 주소를 넘겨받아 처리했기 때문이다', '② 구조체를 함수 인자로 전달하면 구조체 전체가 복사되어, 함수 안의 매개변수 p는 main의 a와 별개의 메모리이기 때문이다', '③ x 멤버가 상수로 선언되어 값을 바꿀 수 없기 때문이다', '④ printf 함수의 서식 문자가 잘못되어 이전 값을 그대로 출력했기 때문이다']}
  correctAnswer={2}
  explanation="moveByValue(struct Point p)처럼 매개변수를 구조체 값으로 선언하면, 함수를 호출할 때 인자로 넘긴 구조체 전체가 그대로 복사되어 매개변수 p에 담긴다. 이 p는 main의 a와 이름만 같을 뿐 서로 다른 메모리에 있는 별개의 구조체이므로, 함수 안에서 p.x를 바꿔도 그 변경은 복사본에만 반영되고 원본 a에는 전혀 영향을 주지 않는다. 그래서 함수를 호출한 뒤에도 a.x는 초기값 1 그대로 출력된다. 원본을 바꾸려면 struct Point *p처럼 포인터를 매개변수로 받아 p-&gt;x += 100;으로 접근해야 한다."
  optionExplanations={['이 함수는 struct Point p로 값 전달을 받았을 뿐 주소(포인터)를 받지 않았다. 주소를 받았다면 원본이 바뀌었을 것이다.', '구조체 값 전달은 전체 복사를 일으켜 함수 안의 수정이 원본에 반영되지 않는다는 것을 정확히 설명한 정답이다.', 'x 멤버는 const로 선언되지 않았으며, 값을 바꿀 수 없다는 근거는 코드 어디에도 없다.', 'printf("%d\\n", a.x);는 a.x 값을 정확히 출력하는 정상적인 서식이며, 문제는 서식이 아니라 값 전달 방식에 있다.']}
/>

### 유형 4 — 파일 입출력 기초

**이 유형은 이렇게 접근한다:** fopen은 모드 문자열에 따라 동작이 크게 달라집니다. "r"은 파일이 반드시 있어야 하고, "w"는 있어도 내용을 지우며, "a"는 없으면 만들고 있으면 끝에 이어 씁니다. 파일을 다루는 모든 코드는 **fopen이 NULL을 반환할 가능성**을 먼저 의심해야 합니다.

<Quiz
  questionNumber={10}
  question="fopen 함수의 파일 열기 모드에 대한 설명으로 옳지 않은 것은?"
  options={['① 모드 r로 열 때 해당 파일이 존재하지 않으면 fopen은 NULL을 반환한다', '② 모드 w로 열 때 파일이 이미 존재하면 기존 내용을 모두 지우고 처음부터 새로 쓴다', '③ 모드 a로 열 때 파일이 없으면 새로 만들고, 있으면 기존 내용 끝에 이어서 쓴다', '④ 모드 r+로 열 때 파일이 존재하지 않아도 fopen이 자동으로 새 파일을 만들어 읽기·쓰기를 모두 수행한다']}
  correctAnswer={4}
  explanation="r 모드는 파일이 반드시 존재해야 하고 없으면 NULL을 반환하는 것이 옳다. w 모드는 기존 내용을 지우고 새로 쓰기 시작하는 것이 옳고, a 모드는 없으면 생성하고 있으면 끝에 이어 쓰는 것도 옳다. 문제는 r+ 모드다. r+는 읽기·쓰기를 모두 지원하지만 w와 달리 **파일이 반드시 이미 존재해야 하며**, 존재하지 않으면 r 모드와 마찬가지로 fopen이 NULL을 반환한다. 파일이 없어도 새로 만들어 읽기·쓰기를 시작하고 싶다면 w+ 모드를 써야 한다."
  optionExplanations={['r 모드는 파일이 없으면 실패(NULL)하는 것이 옳은 동작이다.', 'w 모드가 기존 내용을 지우고 새로 쓴다는 것은 정확한 설명이다.', 'a 모드의 생성·이어쓰기 동작을 정확히 설명한 옳은 문장이다.', 'r+ 모드는 파일이 존재해야만 정상적으로 열리며, 존재하지 않으면 NULL을 반환한다. 자동으로 새 파일을 만든다는 설명은 옳지 않다.']}
/>

```c
#include <stdio.h>

int main(void) {
    FILE *fp = fopen("data.txt", "w");
    fprintf(fp, "%d %d\n", 3, 5);
    fclose(fp);

    int a, b;
    fp = fopen("data.txt", "r");
    fscanf(fp, "%d %d", &a, &b);
    fclose(fp);

    printf("%d\n", a + b);
    return 0;
}
```

<Quiz
  questionNumber={11}
  question="위 프로그램(파일 생성이 정상적으로 이루어졌다고 가정)의 실행 결과로 옳은 것은?"
  options={['① 8', '② 35', '③ 3', '④ 0']}
  correctAnswer={1}
  explanation="fprintf(fp, %d %d, 3, 5)는 data.txt 파일에 3과 5를 공백으로 구분해 문자열 형태로 저장한다. 파일을 다시 읽기 모드로 열어 fscanf(fp, %d %d, &a, &b)를 실행하면, 공백으로 구분된 두 정수를 각각 a와 b에 읽어 들인다. 따라서 a=3, b=5가 되고, a+b=8이 출력된다."
  optionExplanations={['fprintf로 저장한 3과 5를 fscanf로 각각 a, b에 정확히 읽어 들여 더한 값이다.', '3과 5를 더하지 않고 옆으로 이어 붙인 것처럼 착각한 오답이다. %d %d는 정수 두 개를 각각 읽는 서식이지 문자열 연결이 아니다.', 'b 값을 읽지 못해 a만 출력됐다고 착각한 오답이다. fscanf는 두 값을 모두 정상적으로 읽는다.', 'fscanf가 값을 전혀 읽지 못했다고 착각한 오답이다. 파일이 정상적으로 쓰이고 읽혔다는 전제이므로 값은 정확히 채워진다.']}
/>

```c
#include <stdio.h>

int main(void) {
    FILE *fp = fopen("not_exist.txt", "r");
    char buf[100];
    fscanf(fp, "%s", buf);
    fclose(fp);
    return 0;
}
```

<Quiz
  questionNumber={12}
  question="not_exist.txt 파일이 실제로 존재하지 않는다고 할 때, 이 프로그램이 실행 중 비정상 종료될 가능성이 높은 이유로 가장 적절한 것은?"
  options={['① 파일이 존재하지 않으면 fopen이 NULL을 반환하는데, 이를 확인하지 않고 fscanf에 그대로 전달했기 때문이다', '② fscanf의 서식 문자 %s는 애초에 문자열을 읽을 수 없기 때문이다', '③ buf 배열의 크기가 100바이트를 초과했기 때문이다', '④ fclose를 두 번 호출했기 때문이다']}
  correctAnswer={1}
  explanation="fopen은 파일을 열지 못하면 유효한 FILE 포인터 대신 NULL을 반환한다. 이 코드는 fp가 NULL인지 확인하지 않고 곧바로 fscanf(fp, %s, buf);를 호출하는데, 내부적으로 NULL 포인터가 가리키는 존재하지 않는 파일 구조체에 접근하려 시도하게 되어 정의되지 않은 동작(undefined behavior)을 일으키고, 대부분의 환경에서 프로그램이 비정상 종료된다. 이런 문제를 막으려면 fopen 직후 반드시 if (fp == NULL) 검사를 하고, NULL이면 오류 메시지를 출력한 뒤 함수를 종료해야 한다."
  optionExplanations={['fopen 실패로 NULL이 된 fp를 검사 없이 fscanf에 넘긴 것이 실제 원인이며, 정확한 설명이다.', '%s는 문자열을 읽는 정상적인 서식 문자이며 코드 자체의 문법 문제는 없다.', 'buf 크기는 100바이트로 이 예제에서 그 한계를 넘는 입력이 주어졌다는 근거가 없다. 문제의 핵심은 NULL 검사 누락이다.', 'fclose는 이 코드에서 한 번만 호출되었으며, 이중 호출은 일어나지 않는다.']}
/>

### 유형 5 — 파일 입출력 심화(이진 파일)

**이 유형은 이렇게 접근한다:** 구조체나 실수처럼 이진 데이터를 있는 그대로 저장·복원하려면 텍스트 모드가 아니라 **이진 모드("b" 포함)** 로 파일을 열고, fscanf·fprintf 대신 **fread·fwrite** 를 써야 합니다.

```c
#include <stdio.h>

struct Item { int id; double price; };

int main(void) {
    struct Item a = {1, 1000.0};
    FILE *fp = fopen("item.bin", "wb");
    fwrite(&a, sizeof(struct Item), 1, fp);
    fclose(fp);

    struct Item b;
    fp = fopen("item.bin", "rb");
    fread(&b, sizeof(struct Item), 1, fp);
    fclose(fp);

    printf("%d %.1f\n", b.id, b.price);
    return 0;
}
```

<Quiz
  questionNumber={13}
  question="파일 생성이 정상적으로 이루어졌다고 가정할 때, 위 프로그램의 실행 결과로 옳은 것은?"
  options={['① 1 1000.0', '② 0 0.0', '③ 1 0.0', '④ 0 1000.0']}
  correctAnswer={1}
  explanation="fwrite(&a, sizeof(struct Item), 1, fp)는 구조체 a의 메모리 내용을 있는 그대로 1개(item 1개 분량) item.bin에 이진 형태로 저장한다. fread(&b, sizeof(struct Item), 1, fp)는 그 파일에서 같은 크기만큼 읽어 구조체 b의 메모리에 그대로 복원한다. 이진 모드로 저장·복원했으므로 정수 id와 실수 price 모두 손실 없이 그대로 전달되어 b.id=1, b.price=1000.0이 출력된다."
  optionExplanations={['fwrite로 저장한 구조체를 fread로 그대로 복원했으므로 id, price 모두 원본과 같다.', '이진 모드로 저장·복원했다는 전제를 무시하고 값이 아예 전달되지 않았다고 본 오답이다.', 'id만 복원되고 price는 복원되지 않았다고 본 오답이다. fwrite·fread는 구조체 전체를 한 번에 다룬다.', 'price만 복원되고 id는 복원되지 않았다고 본 오답으로, 역시 구조체 전체 단위 복사라는 점을 놓쳤다.']}
/>

<Quiz
  questionNumber={14}
  question="텍스트 모드와 이진 모드로 파일을 다루는 것에 대한 설명으로 옳지 않은 것은?"
  options={['① 텍스트 모드는 운영체제에 따라 개행 문자를 변환할 수 있지만, 이진 모드는 데이터를 있는 그대로 저장·복원한다', '② 구조체나 실수 같은 이진 데이터를 손실 없이 저장하려면 이진 모드로 파일을 열어야 한다', '③ 이진 모드로 저장한 파일은 메모장 같은 일반 텍스트 편집기로 열어도 항상 원래 숫자 그대로 읽을 수 있다', '④ fopen의 모드 문자열에 b를 추가하면(예: rb, wb) 이진 모드로 파일을 연다']}
  correctAnswer={3}
  explanation="텍스트 모드가 개행 문자를 변환할 수 있고 이진 모드는 데이터를 그대로 다룬다는 것, 이진 데이터는 이진 모드로 저장해야 손실이 없다는 것, b를 붙여 이진 모드를 지정한다는 것은 모두 옳다. 문제는 3번이다. 이진 모드로 저장한 구조체나 정수는 사람이 읽는 문자가 아니라 **비트 패턴 그대로** 저장되므로, 메모장 같은 텍스트 편집기로 열면 숫자가 아니라 알아볼 수 없는 문자들이 뒤섞여 보인다. 그 내용을 다시 숫자로 확인하려면 반드시 fread로 같은 구조체 형태로 읽어야 한다."
  optionExplanations={['텍스트·이진 모드의 개행 처리 차이를 정확히 설명한 옳은 문장이다.', '이진 데이터를 손실 없이 저장하려면 이진 모드가 필요하다는 것을 정확히 설명한 옳은 문장이다.', '이진 모드로 저장된 데이터는 비트 패턴 그대로라 텍스트 편집기로 열면 사람이 읽을 수 있는 숫자로 보이지 않는다. 항상 원래 숫자 그대로 읽을 수 있다는 서술이 옳지 않다.', 'b 옵션으로 이진 모드를 지정한다는 것을 정확히 설명한 옳은 문장이다.']}
/>

<Quiz
  questionNumber={15}
  question="파일 위치 지시자(file position indicator)를 다루는 함수에 대한 설명으로 옳지 않은 것은?"
  options={['① ftell 함수는 현재 파일 위치 지시자의 값을 파일 시작 지점으로부터의 바이트 수로 반환한다', '② fseek(fp, 0, SEEK_END)를 호출하면 파일 위치 지시자를 파일의 끝으로 이동시킨다', '③ rewind 함수는 파일 위치 지시자를 파일의 끝으로 이동시켜 이어쓰기를 준비한다', '④ fseek의 두 번째 인자는 이동할 오프셋이고, 세 번째 인자는 SEEK_SET·SEEK_CUR·SEEK_END 같은 기준 위치다']}
  correctAnswer={3}
  explanation="ftell이 현재 위치를 바이트 수로 반환한다는 것, fseek(fp, 0, SEEK_END)가 파일 끝으로 이동시킨다는 것, fseek의 두 번째·세 번째 인자가 각각 오프셋과 기준 위치라는 것은 모두 옳다. 문제는 rewind다. rewind(fp)는 이름 그대로 파일 위치 지시자를 **파일의 맨 처음**으로 되돌리는 함수이며, fseek(fp, 0, SEEK_SET)와 같은 효과를 낸다. 파일의 끝으로 이동시켜 이어쓰기를 준비한다는 설명은 rewind가 아니라 fseek(fp, 0, SEEK_END)의 동작이므로 옳지 않다."
  optionExplanations={['ftell의 정의를 정확히 설명한 옳은 문장이다.', 'fseek로 파일 끝으로 이동하는 동작을 정확히 설명한 옳은 문장이다.', 'rewind는 파일의 처음으로 되돌리는 함수다. 파일 끝으로 이동시킨다는 설명은 rewind와 fseek(fp, 0, SEEK_END)의 동작을 뒤바꾼 것이다.', 'fseek의 두 인자 의미를 정확히 설명한 옳은 문장이다.']}
/>

### 유형 6 — 동적 메모리 할당: malloc·calloc·realloc

**이 유형은 이렇게 접근한다:** malloc은 요청한 크기만큼 힙(heap) 메모리를 할당하지만 내용을 초기화하지 않고, calloc은 할당과 동시에 모든 바이트를 0으로 채웁니다. realloc은 기존 데이터를 보존한 채 크기만 조정합니다.

<Quiz
  questionNumber={16}
  question="malloc과 calloc의 차이에 대한 설명으로 옳지 않은 것은?"
  options={['① malloc은 할당한 메모리를 초기화하지 않아 임의의 값(쓰레기 값)이 남아 있을 수 있다', '② calloc은 할당한 메모리를 모두 0으로 초기화한다', '③ malloc(n)은 n바이트를 할당하지만, calloc(n, size)은 n개의 원소를 각각 size바이트씩 할당한다', '④ malloc과 calloc 모두 메모리 할당에 실패하면 반드시 프로그램을 강제 종료시키므로 반환값을 확인할 필요가 없다']}
  correctAnswer={4}
  explanation="malloc이 초기화하지 않는다는 것, calloc이 0으로 초기화한다는 것, 두 함수의 인자 구성 차이는 모두 옳다. 문제는 4번이다. malloc과 calloc은 메모리 할당에 실패하면 프로그램을 강제 종료시키지 않고 **NULL을 반환**한다. 이후 코드가 이 반환값을 확인하지 않고 그대로 포인터를 역참조하면 프로그램이 비정상 종료될 수 있으므로, 실무에서는 항상 반환값이 NULL인지 확인하는 절차가 필요하다."
  optionExplanations={['malloc이 초기화하지 않는다는 것을 정확히 설명한 옳은 문장이다.', 'calloc의 0 초기화 동작을 정확히 설명한 옳은 문장이다.', 'malloc과 calloc의 인자 의미 차이를 정확히 설명한 옳은 문장이다.', 'malloc과 calloc은 실패 시 NULL을 반환할 뿐 프로그램을 직접 종료시키지 않으므로, 호출한 쪽에서 반드시 반환값을 확인해야 한다.']}
/>

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int n = 5;
    int *arr = (int *)malloc(n * sizeof(int));

    for (int i = 0; i < n; i++) {
        arr[i] = i * i;
    }

    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += arr[i];
    }

    printf("%d\n", sum);
    free(arr);
    return 0;
}
```

<Quiz
  questionNumber={17}
  question="위 프로그램의 실행 결과로 옳은 것은?"
  options={['① 30', '② 25', '③ 20', '④ 16']}
  correctAnswer={1}
  explanation="malloc으로 int형 5개를 담을 수 있는 동적 배열을 할당한 뒤, 첫 번째 반복문에서 arr[i] = i * i를 대입한다. i가 0,1,2,3,4로 늘어나면서 arr에는 0, 1, 4, 9, 16이 차례로 저장된다. 두 번째 반복문은 이 값을 모두 더하므로 sum = 0+1+4+9+16 = 30이 된다."
  optionExplanations={['0+1+4+9+16을 정확히 더한 값이다.', 'i*i 대신 i만 더한 것처럼(0+1+2+3+4=10에 15를 더한 값 등) 계산을 착각한 오답이다.', '마지막 원소 16을 빠뜨리고 0+1+4+9=14에 가까운 값을 어림한 오답이다.', '마지막 원소 arr[4]=16만 출력한 값으로, 반복 합산을 하지 않은 오답이다.']}
/>

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *arr = (int *)malloc(3 * sizeof(int));
    arr[0] = 1; arr[1] = 2; arr[2] = 3;

    arr = (int *)realloc(arr, 5 * sizeof(int));
    arr[3] = 4;
    arr[4] = 5;

    int sum = 0;
    for (int i = 0; i < 5; i++) sum += arr[i];

    printf("%d\n", sum);
    free(arr);
    return 0;
}
```

<Quiz
  questionNumber={18}
  question="위 프로그램의 실행 결과로 옳은 것은?"
  options={['① 15', '② 10', '③ 6', '④ 20']}
  correctAnswer={1}
  explanation="realloc(arr, 5 * sizeof(int))은 arr이 가리키던 기존 3개 원소의 값(1, 2, 3)을 보존한 채로 5개를 담을 수 있는 크기로 메모리를 조정한다. 그 뒤 arr[3]=4, arr[4]=5를 새로 채우면 배열에는 1, 2, 3, 4, 5가 담기고, 이를 모두 더하면 1+2+3+4+5=15가 된다."
  optionExplanations={['1부터 5까지를 정확히 더한 값이다.', '기존 값 1, 2, 3이 realloc 이후 사라진다고 착각해 새로 채운 4, 5와 일부만 더한 오답이다.', '기존 값(1+2+3=6)만 더하고 새로 채운 4, 5를 반영하지 않은 오답이다.', '1부터 5까지의 합에 5를 더 더한 값으로, 계산 과정에서 항을 중복 계산한 오답이다.']}
/>

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int *p = (int *)malloc(sizeof(int));
    *p = 10;
    free(p);
    printf("%d\n", *p);
    return 0;
}
```

<Quiz
  questionNumber={19}
  question="위 코드의 문제점으로 가장 적절한 것은?"
  options={['① free(p) 이후 p가 가리키던 메모리는 이미 반납되었으므로, 그 뒤 *p로 접근하는 것은 정의되지 않은 동작(댕글링 포인터 사용)이다', '② free 함수는 정수형 포인터에는 사용할 수 없기 때문에 컴파일 오류가 발생한다', '③ malloc으로 할당한 메모리는 free 없이도 자동으로 사라지므로 free(p) 호출 자체가 불필요한 오류다', '④ *p = 10;에서 대입 연산자를 잘못 사용해 오류가 발생한다']}
  correctAnswer={1}
  explanation="free(p)는 p가 가리키던 힙 메모리를 운영체제(또는 메모리 관리자)에 반납하는 함수다. 이 시점 이후 p는 더 이상 유효하지 않은 메모리를 가리키는 댕글링 포인터(dangling pointer)가 되며, 그 값을 *p로 역참조하는 것은 C 표준에서 정의되지 않은 동작이다. 운 좋게 이전 값이 그대로 남아 10이 출력될 수도 있지만, 다른 코드가 그 메모리를 재사용했다면 전혀 다른 값이 나오거나 프로그램이 비정상 종료될 수 있다. malloc으로 할당한 메모리는 프로그램이 명시적으로 free하지 않으면 자동으로 해제되지 않으므로 free(p) 자체는 필요한 호출이다."
  optionExplanations={['free 이후의 포인터 역참조가 정의되지 않은 동작이라는 점을 정확히 설명한 정답이다.', 'free는 malloc·calloc·realloc으로 할당한 모든 자료형의 포인터에 사용할 수 있으며, 정수형 포인터라고 예외가 아니다.', 'malloc으로 할당한 힙 메모리는 free를 호출하지 않으면 프로그램이 끝날 때까지(혹은 그 이상) 해제되지 않는다. free(p)는 꼭 필요한 정상적인 호출이다.', '*p = 10;은 문법적으로 올바른 대입문이며, 이 코드의 문제는 그 뒤 free 이후의 역참조에 있다.']}
/>

### 유형 7 — 동적 메모리 오류: 누수·이중 해제·댕글링 포인터

**이 유형은 이렇게 접근한다:** 컴파일은 되지만 실행 시점에 위험한 코드를 찾는 문제입니다. "할당한 메모리를 되찾을 방법이 사라졌는가(누수)", "이미 해제한 메모리를 또 해제하는가(이중 해제)", "이미 사라진 지역 변수의 주소를 계속 쓰는가(댕글링)"를 순서대로 점검하십시오.

```c
#include <stdio.h>
#include <stdlib.h>

void makeArray(int n) {
    int *arr = (int *)malloc(n * sizeof(int));
    for (int i = 0; i < n; i++) {
        arr[i] = i;
    }
    printf("%d\n", arr[0]);
}

int main(void) {
    makeArray(5);
    return 0;
}
```

<Quiz
  questionNumber={20}
  question="이 프로그램의 문제점으로 가장 적절한 것은?"
  options={['① makeArray 함수 안에서 malloc으로 할당한 메모리를 free하지 않고 함수가 끝나, arr에 대한 접근 수단을 잃어버리는 메모리 누수가 발생한다', '② n의 값이 5로 고정되어 있어 배열 크기를 바꿀 수 없다는 것이 문제다', '③ arr[0]에 저장된 값이 잘못 계산되어 0이 아닌 값이 출력된다', '④ printf 함수 호출 위치가 잘못되어 컴파일 오류가 발생한다']}
  correctAnswer={1}
  explanation="makeArray 함수 안의 지역 변수 arr은 malloc으로 할당받은 힙 메모리의 주소를 담고 있다. 함수가 끝나면 지역 변수 arr 자체는 스택에서 사라지지만, 그 arr이 가리키던 힙 메모리는 free를 호출하지 않았으므로 그대로 남아 있다. 문제는 그 메모리의 주소를 담고 있던 유일한 변수인 arr이 사라져 버려, 프로그램의 어떤 코드도 더 이상 그 메모리에 접근하거나 해제할 방법이 없어진다는 점이다. 이렇게 할당은 되었지만 되찾을 수 없게 된 메모리를 메모리 누수(memory leak)라고 부르며, 이 함수가 여러 번 호출될수록 낭비되는 메모리가 계속 쌓인다."
  optionExplanations={['malloc으로 할당한 메모리를 free하지 않고 그 주소를 담은 지역 변수가 사라지는 전형적인 메모리 누수 패턴을 정확히 설명한 정답이다.', 'n이 고정값 5로 호출된 것은 이 예제의 단순화일 뿐, 함수 자체는 n을 매개변수로 받아 크기를 바꿀 수 있게 설계되어 있다.', 'arr[i] = i;에 따라 arr[0]은 정확히 0이 되며, 계산 자체에는 오류가 없다.', '이 코드는 문법적으로 올바르게 컴파일되며, 문제는 컴파일 오류가 아니라 실행 후 남는 메모리 누수에 있다.']}
/>

```c
#include <stdio.h>

int *makePointer(void) {
    int local = 42;
    return &local;
}

int main(void) {
    int *p = makePointer();
    printf("%d\n", *p);
    return 0;
}
```

<Quiz
  questionNumber={21}
  question="이 프로그램에 내재된 문제점으로 가장 적절한 것은?"
  options={['① local이 makePointer 함수의 지역 변수이므로 함수가 끝나면 그 저장 공간(스택 프레임)이 회수되어, main에서 반환받은 주소로 접근하는 것은 이미 유효하지 않은 메모리에 접근하는 댕글링 포인터 문제다', '② local 변수의 자료형이 int라서 그 주소를 반환할 수 없다', '③ main 함수에서 포인터 변수를 int *p로 선언한 것이 문법 오류다', '④ makePointer 함수가 값을 반환하지 않아 컴파일 오류가 발생한다']}
  correctAnswer={1}
  explanation="local은 makePointer 함수 안에서 선언된 지역 변수로, 함수 호출 시 스택에 만들어지는 스택 프레임 안에 저장된다. 함수가 return &local;로 그 주소를 반환하더라도, makePointer 함수 실행이 끝나는 순간 그 스택 프레임은 회수되어 local이 있던 공간은 다른 함수 호출에 재사용될 수 있는 상태가 된다. main의 p는 이렇게 더 이상 유효하지 않은 주소를 담은 댕글링 포인터가 되며, *p로 그 값을 읽는 것은 정의되지 않은 동작이다. 우연히 42가 그대로 남아 출력될 수도 있지만, 다른 함수 호출이 그 스택 공간을 덮어썼다면 전혀 다른 값이 나올 수 있다."
  optionExplanations={['지역 변수의 주소를 함수 밖으로 반환하면 스택 프레임 회수 이후 그 주소가 무효해진다는 댕글링 포인터 문제를 정확히 설명한 정답이다.', 'C에서는 int를 비롯한 어떤 자료형의 변수든 & 연산자로 주소를 얻어 반환할 수 있다. 자료형 자체가 문제가 아니다.', 'int *p;는 정수형 변수의 주소를 담는 포인터 변수를 선언하는 올바른 문법이다.', 'makePointer 함수는 return &local;로 int * 자료형의 값을 정상적으로 반환하고 있어 컴파일 오류가 아니다.']}
/>

```c
#include <stdlib.h>

int main(void) {
    int *p = (int *)malloc(sizeof(int));
    *p = 1;
    free(p);
    free(p);
    return 0;
}
```

<Quiz
  questionNumber={22}
  question="이 프로그램의 문제점으로 가장 적절한 것은?"
  options={['① free(p)를 두 번 호출하는 이중 해제(double free)로, 이미 반납된 메모리 블록을 다시 해제하려 시도해 프로그램이 비정상 종료되거나 메모리 관리 구조가 손상될 수 있다', '② p가 가리키는 값이 1이라서 free를 두 번 호출해도 항상 안전하다', '③ malloc으로 할당한 메모리는 애초에 free로 해제할 수 없다', '④ free 함수는 원래 두 번 호출하도록 설계되어 있어 문제가 없다']}
  correctAnswer={1}
  explanation="첫 번째 free(p) 호출로 p가 가리키던 힙 메모리는 이미 메모리 관리자(할당자)에게 반납되었다. 이 시점 이후 p는 댕글링 포인터가 되었는데도 두 번째 free(p)가 다시 호출되면, 메모리 관리자는 이미 반납된 블록을 또 반납받으려 시도하게 된다. 이는 이중 해제(double free)라 불리는 대표적인 동적 메모리 오류로, 내부 자료구조(가용 메모리 목록 등)가 손상되어 프로그램이 예측할 수 없게 동작하거나 그 자리에서 비정상 종료될 수 있다. 이를 막으려면 free(p) 직후 p = NULL;로 만들어, 실수로 다시 free를 호출하더라도 free(NULL)이 아무 일도 하지 않도록(표준에서 안전하게 정의됨) 하는 습관이 필요하다."
  optionExplanations={['이중 해제가 메모리 관리자의 내부 구조를 손상시킬 수 있는 심각한 오류라는 점을 정확히 설명한 정답이다.', 'free는 포인터가 가리키는 값이 아니라 그 포인터가 가리키는 메모리 블록 자체의 해제 상태를 다루므로, 값이 무엇이든 이중 해제는 위험하다.', 'malloc으로 할당한 메모리는 정확히 free로 해제하도록 설계되어 있으며, 첫 번째 free(p) 호출 자체는 정상이다.', 'free는 한 번의 할당에 대해 정확히 한 번만 호출하도록 설계되어 있으며, 두 번 호출을 허용하거나 의도한 것이 아니다.']}
/>

## 참고 자료

- 국가평생교육진흥원 독학학위제: [https://bdes.nile.or.kr](https://bdes.nile.or.kr)
- cppreference — C 표준 라이브러리·문법 레퍼런스: [https://en.cppreference.com/w/c](https://en.cppreference.com/w/c)
