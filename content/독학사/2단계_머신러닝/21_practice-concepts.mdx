---
title: '21. 개념 확인·용어 정의 연습문제'
description: '독학사 2단계 머신러닝 출제기준을 기반으로 재구성한 핵심 개념·용어 정의 문제 22선, 자주 혼동되는 개념의 경계 확인'
---

<Callout type="warning">
  **이 문제는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학사 2단계 머신러닝 출제기준(평가영역)을 바탕으로 새로 구성한 연습 문제입니다. 실제 회차의 문항 수·배점·난이도는 매 시행 공고를 확인해야 합니다. 여기서는 01편부터 20편까지 다룬 전 범위에서 "정의를 정확히 알고 있는가", "비슷해 보이는 두 개념의 경계를 구분할 수 있는가"에 초점을 맞춘 22문항을 담았다.
</Callout>

이 편은 계산이 아니라 **개념의 정확성**을 확인하는 데 목적이 있다. 독학사는 "그럴듯하지만 미묘하게 틀린" 선택지를 넣는 방식으로 이해도를 검증하므로, 정답만 확인하고 넘어가지 말고 오답이 왜 틀렸는지까지 읽어야 한다. 특히 "옳지 않은 것은?" 유형은 나머지 세 보기가 왜 옳은 진술인지도 함께 확인하는 것이 좋다.

## 인공지능·머신러닝 기초 용어 (1–4번)

<Quiz
  questionNumber={1}
  question="인공지능(AI), 머신러닝(ML), 딥러닝(DL)의 관계에 대한 설명으로 옳은 것은?"
  options={['AI, ML, DL은 서로 겹치지 않는 별개의 독립된 분야다', 'DL은 ML의 하위 분야이고, ML은 AI의 하위 분야다', 'ML은 DL의 하위 분야이고, DL은 AI의 하위 분야다', 'AI는 ML의 하위 분야이고, ML은 DL의 하위 분야다']}
  correctAnswer={2}
  explanation="인공지능(AI, Artificial Intelligence)은 사람처럼 지각·추론·학습하는 시스템을 만드는 가장 넓은 개념이다. 머신러닝(ML, Machine Learning)은 규칙을 사람이 직접 프로그래밍하지 않고 데이터로부터 규칙을 학습시키는 AI의 하위 분야이며, 딥러닝(DL, Deep Learning)은 다층 인공신경망을 사용하는 ML의 하위 분야다. 따라서 포함 관계는 AI가 가장 크고, 그 안에 ML이, 그 안에 DL이 있는 구조다."
  optionExplanations={['세 분야는 독립된 것이 아니라 AI를 가장 큰 원으로 하는 포함 관계다.', '정답이다. DL은 ML에 포함되고 ML은 AI에 포함되는 3중 포함 관계다.', '포함 관계의 방향이 반대다. DL이 ML보다 좁은 개념이지 그 반대가 아니다.', 'AI가 가장 넓은 상위 개념이므로 AI를 ML의 하위로 두는 것은 관계가 뒤집힌 서술이다.']}
/>

<Quiz
  questionNumber={2}
  question="머신러닝 모델에서 '파라미터(parameter)'와 '하이퍼파라미터(hyperparameter)'를 구분하는 기준으로 가장 적절한 것은?"
  options={['파라미터는 사람이 학습 전에 정하고, 하이퍼파라미터는 학습 과정에서 데이터로부터 자동으로 정해진다', '파라미터는 학습 과정에서 데이터로부터 값이 정해지고, 하이퍼파라미터는 학습 전에 사람이 설정한다', '두 용어는 완전히 같은 의미로, 문헌에 따라 다르게 부를 뿐이다', '파라미터는 분류 모델에서만 쓰이고, 하이퍼파라미터는 회귀 모델에서만 쓰인다']}
  correctAnswer={2}
  explanation="파라미터(parameter, 모수)는 선형회귀의 회귀계수나 신경망의 가중치처럼 학습 알고리즘이 훈련 데이터를 보고 값을 결정하는 대상이다. 반면 하이퍼파라미터(hyperparameter)는 학습률, K-NN의 K값, 결정트리의 최대 깊이처럼 학습을 시작하기 전에 사람(또는 튜닝 절차)이 미리 정해 주는 값이다. 이 둘의 결정 시점과 결정 주체가 핵심 구분 기준이다."
  optionExplanations={['결정 주체와 시점이 뒤바뀐 설명이다. 사람이 미리 정하는 쪽은 파라미터가 아니라 하이퍼파라미터다.', '정답이다. 데이터로부터 자동 결정되면 파라미터, 학습 전 사람이 설정하면 하이퍼파라미터다.', '두 용어는 결정 시점과 주체가 명확히 다른 별개의 개념이므로 같은 의미가 아니다.', '두 용어의 구분은 분류·회귀 여부가 아니라 값이 결정되는 시점과 방식에 있다.']}
/>

<Quiz
  questionNumber={3}
  question="다음 중 지도학습(supervised learning)에 대한 설명으로 옳은 것은?"
  options={['정답 레이블(label) 없이 데이터의 숨은 구조나 군집을 찾는 학습 방식이다', '에이전트가 환경과 상호작용하며 보상을 최대화하는 행동을 학습하는 방식이다', '입력과 정답 레이블의 쌍으로 이루어진 데이터로 입력에서 출력을 예측하는 함수를 학습하는 방식이다', '레이블이 전혀 없는 데이터에서 이상치만 걸러내는 학습 방식이다']}
  correctAnswer={3}
  explanation="지도학습(supervised learning)은 입력 특징(feature)과 그에 대응하는 정답 레이블(label)이 쌍으로 주어진 훈련 데이터를 사용해, 새로운 입력이 들어왔을 때 출력을 예측하는 함수를 학습하는 방식이다. 회귀와 분류가 대표적인 지도학습 문제 유형이다."
  optionExplanations={['레이블 없이 구조를 찾는 것은 비지도학습(unsupervised learning)에 대한 설명이다.', '보상을 최대화하는 행동을 학습하는 것은 강화학습(reinforcement learning)에 대한 설명이다.', '정답이다. 입력-레이블 쌍으로 예측 함수를 학습하는 것이 지도학습의 정의다.', '이상치 탐지만을 지도학습의 정의로 한정하는 것은 범위를 지나치게 좁힌 서술이다.']}
/>

<Quiz
  questionNumber={4}
  question="강화학습(reinforcement learning)의 핵심 요소로 옳지 않은 것은?"
  options={['에이전트(agent)가 환경(environment)과 상호작용한다', '행동에 대한 보상(reward)을 받아 정책(policy)을 개선한다', '학습 데이터 전체에 미리 정답 레이블이 부여되어 있어야 한다', '누적 보상을 최대화하는 방향으로 의사결정 전략을 학습한다']}
  correctAnswer={3}
  explanation="강화학습은 에이전트가 환경과 상호작용하며 행동에 대한 보상(reward) 신호만을 받고, 그 보상을 누적으로 최대화하는 정책(policy)을 스스로 탐색해 나가는 학습 방식이다. 지도학습처럼 모든 상황에 대한 정답 레이블이 미리 주어지지 않는다는 점이 지도학습과의 결정적 차이이므로, '학습 데이터 전체에 미리 정답 레이블이 부여되어 있어야 한다'는 강화학습의 특징이 아니라 지도학습의 특징이다."
  optionExplanations={['에이전트와 환경의 상호작용은 강화학습의 기본 구조이므로 옳은 설명이다.', '보상을 통해 정책을 개선하는 것은 강화학습의 핵심 메커니즘이므로 옳은 설명이다.', '정답이다. 이는 지도학습의 특징이며, 강화학습은 정답 레이블이 아니라 보상 신호로 학습한다.', '누적 보상 최대화는 강화학습의 목표 정의 자체이므로 옳은 설명이다.']}
/>

## 데이터 전처리 (5–8번)

<Quiz
  questionNumber={5}
  question="결측치(missing value) 처리 방법에 대한 설명으로 옳지 않은 것은?"
  options={['결측치가 있는 행을 전부 제거하는 방법은 데이터가 충분히 많고 결측 비율이 낮을 때 고려할 수 있다', '평균이나 중앙값으로 대체하는 방법은 항상 원본 데이터의 분산을 그대로 보존한다', '결측 자체가 의미 있는 정보일 때는 결측 여부를 나타내는 새 변수를 만들 수 있다', '수치형 변수는 평균이나 중앙값으로, 범주형 변수는 최빈값으로 대체하는 것이 일반적이다']}
  correctAnswer={2}
  explanation="평균이나 중앙값으로 결측치를 대체하면 해당 값 주변으로 데이터가 몰리게 되어, 대체 전보다 전체 데이터의 분산(variance, 값이 평균으로부터 얼마나 퍼져 있는지를 나타내는 지표)이 오히려 줄어드는 것이 일반적이다. 즉 분산을 '항상 그대로 보존한다'는 서술은 사실과 반대다."
  optionExplanations={['결측 비율이 낮고 표본이 충분하면 결측 행 제거(listwise deletion)가 실용적인 선택이 될 수 있으므로 옳은 설명이다.', '정답이다. 평균·중앙값 대체는 분산을 보존하지 않고 오히려 줄이는 경향이 있다.', '결측 여부 자체를 이진 변수로 만들어 정보를 활용하는 것은 실무에서 쓰이는 옳은 접근이다.', '수치형은 평균·중앙값, 범주형은 최빈값 대체가 가장 널리 쓰이는 기본 전략이므로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={6}
  question="표준화(standardization)와 정규화(normalization, min-max scaling)의 차이에 대한 설명으로 옳은 것은?"
  options={['표준화는 값을 0과 1 사이로 압축하고, 정규화는 평균 0·표준편차 1로 변환한다', '표준화는 평균 0·표준편차 1이 되도록 변환하고, 정규화는 값을 정해진 범위(흔히 0과 1 사이)로 압축한다', '두 방법 모두 이상치(outlier)의 영향을 완전히 제거한다', '표준화와 정규화는 범주형 변수에만 적용하는 기법이다']}
  correctAnswer={2}
  explanation="표준화(standardization)는 각 값에서 평균을 빼고 표준편차로 나누어 평균 0, 표준편차 1인 분포로 변환하는 방법이다. 정규화(normalization, min-max scaling)는 최솟값과 최댓값을 이용해 데이터를 흔히 0과 1 사이의 정해진 범위로 압축하는 방법이다. 두 방법 모두 서로 다른 단위·범위를 가진 변수들을 비교 가능하게 맞추는 스케일링 기법이지만 변환의 기준이 다르다."
  optionExplanations={['표준화와 정규화의 정의가 서로 뒤바뀐 설명이다.', '정답이다. 표준화는 평균 0·표준편차 1, 정규화는 정해진 범위로의 압축이다.', '정규화(min-max)는 오히려 이상치에 민감하다. 최솟값·최댓값 자체가 이상치의 영향을 크게 받기 때문이다.', '두 기법 모두 수치형 변수의 스케일을 맞추는 데 쓰이며 범주형 변수에는 적용하지 않는다.']}
/>

<Quiz
  questionNumber={7}
  question="범주형 변수를 원-핫 인코딩(one-hot encoding)으로 변환할 때의 특징으로 옳은 것은?"
  options={['범주 사이에 크기·순서 관계가 있다고 모델에 잘못 알려줄 위험이 있다', '범주의 개수만큼 새로운 이진 변수가 생기며, 범주 간 크기·순서 관계를 가정하지 않는다', '결측치를 자동으로 채워 주는 기법이다', '수치형 변수의 스케일을 0과 1 사이로 맞추는 기법이다']}
  correctAnswer={2}
  explanation="원-핫 인코딩(one-hot encoding)은 범주형 변수의 각 범주마다 하나의 이진(0 또는 1) 변수를 만들어, 해당 범주에 속하면 1, 아니면 0으로 표시하는 방식이다. 이렇게 하면 범주에 임의의 숫자(1, 2, 3 ...)를 매기는 레이블 인코딩과 달리, 범주 사이에 크기나 순서 관계가 있다고 모델이 잘못 학습할 위험을 피할 수 있다."
  optionExplanations={['크기·순서 관계를 잘못 학습시킬 위험이 있는 쪽은 레이블 인코딩(label encoding)이며, 원-핫 인코딩은 그 위험을 피하기 위한 방법이다.', '정답이다. 원-핫 인코딩은 범주 개수만큼 이진 변수를 만들고 순서를 가정하지 않는다.', '원-핫 인코딩은 결측치 처리 기법이 아니라 범주형 변수를 수치형으로 표현하는 기법이다.', '값의 범위를 0과 1 사이로 맞추는 것은 정규화(min-max scaling)이며, 원-핫 인코딩과는 목적이 다르다.']}
/>

<Quiz
  questionNumber={8}
  question="데이터 불균형(class imbalance) 문제를 다루는 방법으로 옳지 않은 것은?"
  options={['소수 클래스 데이터를 복제하거나 합성해 늘리는 오버샘플링(oversampling)을 적용한다', '다수 클래스 데이터를 일부만 남기고 줄이는 언더샘플링(undersampling)을 적용한다', '불균형 데이터에서는 정확도(accuracy)만으로 모델 성능을 판단해도 항상 충분하다', '오분류에 대한 비용(cost)을 클래스마다 다르게 부여하는 비용 민감 학습을 적용한다']}
  correctAnswer={3}
  explanation="데이터 불균형 상황에서는 다수 클래스를 전부 맞히기만 해도 정확도(accuracy)가 매우 높게 나올 수 있어, 정확도만으로는 소수 클래스에 대한 실제 성능(놓친 것이 많은지, 오탐이 많은지)을 알 수 없다. 이런 경우 정밀도(precision)·재현율(recall)·F1 점수·ROC-AUC 같은 지표를 함께 봐야 한다는 것이 18–19편에서 다룬 핵심 내용이다."
  optionExplanations={['오버샘플링은 소수 클래스를 늘려 균형을 맞추는 대표적인 불균형 대응 기법이므로 옳은 설명이다.', '언더샘플링은 다수 클래스를 줄여 균형을 맞추는 대표적인 불균형 대응 기법이므로 옳은 설명이다.', '정답이다. 불균형 데이터에서 정확도만 보면 소수 클래스 성능을 놓칠 수 있어, 정밀도·재현율·F1 등을 함께 확인해야 한다.', '클래스별로 오분류 비용을 다르게 주는 비용 민감 학습(cost-sensitive learning)은 실제로 쓰이는 불균형 대응 방법이므로 옳은 설명이다.']}
/>

## 지도학습 알고리즘 개념 (9–15번)

<Quiz
  questionNumber={9}
  question="선형회귀(linear regression)의 기본 가정으로 옳지 않은 것은?"
  options={['독립변수와 종속변수 사이에 선형(linear) 관계가 있다고 가정한다', '오차항의 분산이 독립변수 값과 무관하게 일정하다고 가정한다(등분산성)', '독립변수들 사이에 강한 상관관계(다중공선성)가 있을수록 계수 추정이 더 안정적이 된다고 가정한다', '오차항들이 서로 독립이라고 가정한다']}
  correctAnswer={3}
  explanation="선형회귀는 독립변수 사이의 강한 상관관계, 즉 다중공선성(multicollinearity)이 커질수록 회귀계수 추정치의 분산이 커져 추정이 불안정해진다고 본다. 다중공선성은 선형회귀가 '있어도 된다고 가정하는' 조건이 아니라 오히려 '피해야 할' 문제 상황이므로, 다중공선성이 커질수록 안정적이 된다는 서술은 사실과 반대다."
  optionExplanations={['선형성(linearity) 가정은 선형회귀의 이름 자체가 나타내는 가장 기본적인 가정이므로 옳은 설명이다.', '등분산성(homoscedasticity)은 선형회귀의 표준 가정 중 하나이므로 옳은 설명이다.', '정답이다. 다중공선성은 계수 추정을 불안정하게 만드는 문제이지, 안정적으로 만드는 조건이 아니다.', '오차항의 독립성 가정도 선형회귀의 표준 가정 중 하나이므로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={10}
  question="릿지 회귀(Ridge regression)와 라쏘 회귀(Lasso regression)의 차이로 옳은 것은?"
  options={['릿지는 L1 정규화, 라쏘는 L2 정규화를 사용한다', '릿지는 회귀계수를 정확히 0으로 만들어 변수 선택 효과를 내지만, 라쏘는 계수를 0에 가깝게만 줄인다', '라쏘는 회귀계수를 정확히 0으로 만들 수 있어 변수 선택 효과를 내지만, 릿지는 계수를 0에 가깝게 줄일 뿐 정확히 0으로 만들지는 않는다', '두 방법 모두 정규화를 전혀 사용하지 않는 일반 선형회귀와 동일하다']}
  correctAnswer={3}
  explanation="릿지 회귀는 손실함수에 계수 제곱의 합(L2 정규화, 페널티가 완만한 곡선 형태)을 더하고, 라쏘 회귀는 계수 절댓값의 합(L1 정규화, 페널티가 뾰족한 형태)을 더한다. L1 페널티의 뾰족한 형태 때문에 라쏘는 일부 계수를 정확히 0으로 만들어 자동으로 변수를 제거하는 효과(변수 선택)를 내지만, 릿지는 계수를 0에 가깝게 줄일 뿐 정확히 0으로 만들지는 못한다."
  optionExplanations={['릿지와 라쏘가 사용하는 정규화 방식이 서로 뒤바뀐 설명이다.', '변수 선택 효과를 내는 쪽과 계수를 완만히 줄이기만 하는 쪽이 릿지·라쏘와 뒤바뀐 설명이다.', '정답이다. 라쏘의 L1 페널티는 계수를 정확히 0으로 만들 수 있어 변수 선택 효과가 있고, 릿지의 L2 페널티는 그렇지 않다.', '릿지·라쏘는 각각 L2·L1 페널티를 손실함수에 추가한 정규화 회귀이므로 일반 선형회귀와 동일하지 않다.']}
/>

<Quiz
  questionNumber={11}
  question="로지스틱 회귀(logistic regression)에 대한 설명으로 옳은 것은?"
  options={['출력값이 실수 전체 범위를 가지는 회귀 문제에 사용한다', '시그모이드(sigmoid) 함수를 이용해 출력을 0과 1 사이의 확률로 변환하여 분류에 사용한다', '이름에 회귀가 들어가므로 항상 연속적인 수치를 예측하는 데만 쓰인다', '결정트리처럼 데이터를 재귀적으로 분할해 예측하는 알고리즘이다']}
  correctAnswer={2}
  explanation="로지스틱 회귀는 선형회귀처럼 입력의 가중합을 구한 뒤, 그 값을 시그모이드 함수(sigmoid function, S자 모양으로 값을 0과 1 사이로 눌러 주는 함수)에 통과시켜 특정 클래스에 속할 확률을 출력한다. 이 확률에 임계값(흔히 0.5)을 적용해 클래스를 분류하므로, 이름에 '회귀'가 들어가지만 실제로는 분류(classification) 문제에 사용하는 대표적인 알고리즘이다."
  optionExplanations={['출력을 실수 전체 범위로 예측하는 것은 선형회귀에 대한 설명이며, 로지스틱 회귀는 확률(0~1)을 출력한다.', '정답이다. 시그모이드 함수로 확률을 만들어 분류에 사용하는 것이 로지스틱 회귀의 핵심이다.', '이름과 달리 로지스틱 회귀는 분류 문제에 사용되는 알고리즘이다.', '재귀적으로 데이터를 분할하는 것은 결정트리의 방식이며, 로지스틱 회귀는 가중합과 시그모이드 함수를 사용하는 별개의 방식이다.']}
/>

<Quiz
  questionNumber={12}
  question="결정트리(decision tree)의 분할 기준으로 지니 지수(Gini index)나 엔트로피(entropy)를 사용하는 목적으로 가장 적절한 것은?"
  options={['트리의 깊이를 항상 최소로 만들기 위해서다', '분할 후 자식 노드가 한 클래스로 최대한 순수(pure)해지는 분할 기준을 찾기 위해서다', '결측치를 자동으로 대체하기 위해서다', '변수 간 상관관계를 제거하기 위해서다']}
  correctAnswer={2}
  explanation="지니 지수와 엔트로피는 모두 노드에 섞여 있는 클래스가 얼마나 불순(impure)한지, 즉 여러 클래스가 뒤섞여 있는 정도를 수치화한 지표다. 결정트리는 각 분할 후보에 대해 이 불순도가 가장 크게 줄어드는(자식 노드가 최대한 한 클래스로 순수해지는) 분할을 선택해 트리를 키워 나간다."
  optionExplanations={['깊이를 최소화하는 것은 지니 지수·엔트로피의 직접적인 목적이 아니며, 트리 깊이는 별도의 하이퍼파라미터(최대 깊이 등)로 제어한다.', '정답이다. 불순도를 최대한 줄이는 분할 기준을 찾는 것이 지니 지수·엔트로피 사용의 목적이다.', '지니 지수와 엔트로피는 결측치 처리와 관련이 없는 분할 품질 지표다.', '변수 간 상관관계 제거는 결정트리의 분할 기준과 관련이 없다.']}
/>

<Quiz
  questionNumber={13}
  question="나이브 베이즈(Naive Bayes) 분류기의 '나이브(naive)'라는 이름이 붙은 이유로 가장 적절한 것은?"
  options={['계산이 매우 복잡하고 정교하기 때문이다', '모든 특징(feature)이 클래스가 주어졌을 때 서로 조건부 독립(conditional independence)이라고 가정하기 때문이다', '학습 데이터가 전혀 없어도 예측이 가능하기 때문이다', '항상 결정트리보다 성능이 낮기 때문이다']}
  correctAnswer={2}
  explanation="나이브 베이즈는 베이즈 정리(Bayes' theorem)를 이용해 클래스 확률을 계산할 때, 모든 특징이 클래스가 주어진 조건에서 서로 조건부 독립(conditional independence, 한 특징의 값이 다른 특징의 값에 영향을 주지 않는다는 가정)이라고 단순화한다. 실제 데이터에서는 특징들이 서로 연관되어 있는 경우가 많아 이 가정이 지나치게 순진(naive)하다는 점에서 이름이 붙었지만, 이 단순화 덕분에 계산이 빠르고 적은 데이터로도 잘 작동하는 장점이 있다."
  optionExplanations={['오히려 계산을 단순화하는 가정 때문에 붙은 이름이지, 계산이 복잡해서가 아니다.', '정답이다. 특징들의 조건부 독립을 가정하는 단순화(naive)가 이름의 유래다.', '나이브 베이즈도 학습 데이터로부터 확률(사전확률·우도)을 추정해야 하므로 데이터가 필요 없는 것은 아니다.', '나이브 베이즈의 성능은 데이터 특성에 따라 달라지며, 항상 결정트리보다 낮다고 단정할 수 없다.']}
/>

<Quiz
  questionNumber={14}
  question="K-최근접 이웃(K-NN, K-Nearest Neighbors) 알고리즘의 특징으로 옳지 않은 것은?"
  options={['훈련 단계에서 별도의 모델 파라미터를 학습하지 않고 훈련 데이터를 그대로 저장해 두는 게으른 학습(lazy learning) 방식이다', 'K값이 너무 작으면 이상치나 잡음의 영향을 크게 받아 과적합되기 쉽다', '예측할 때마다 새로운 데이터와 모든 훈련 데이터 사이의 거리를 계산해야 하므로 예측 시점의 계산량이 많다', 'K값이 커질수록 결정 경계가 항상 더 복잡하고 세밀해진다']}
  correctAnswer={4}
  explanation="K-NN에서 K값이 커질수록 더 많은 이웃의 다수결로 예측하게 되어 결정 경계가 오히려 더 완만하고 단순해지는 경향이 있다(과소적합 위험 증가). 반대로 K값이 작을수록 소수의 가까운 이웃에만 의존해 결정 경계가 복잡하고 세밀해지며 잡음에 민감해진다(과적합 위험 증가). 따라서 'K값이 커질수록 결정 경계가 항상 더 복잡해진다'는 서술은 방향이 반대다."
  optionExplanations={['K-NN은 훈련 시점에 파라미터를 학습하지 않고 데이터를 저장만 해 두므로 게으른 학습이라는 설명은 옳다.', 'K값이 작으면 가까운 소수 이웃에만 의존해 잡음에 민감해지고 과적합되기 쉽다는 설명은 옳다.', 'K-NN은 예측 시점마다 전체 훈련 데이터와의 거리를 계산해야 해 예측 비용이 크다는 설명은 옳다.', '정답이다. K값이 커질수록 결정 경계는 더 복잡해지는 것이 아니라 더 완만하고 단순해지는 경향이 있다.']}
/>

<Quiz
  questionNumber={15}
  question="서포트 벡터 머신(SVM, Support Vector Machine)의 핵심 아이디어로 가장 적절한 것은?"
  options={['모든 훈련 데이터와의 거리를 계산해 다수결로 클래스를 정한다', '두 클래스를 나누는 경계 중, 각 클래스에서 경계에 가장 가까운 데이터(서포트 벡터)까지의 여백(마진)을 최대화하는 경계를 찾는다', '데이터를 여러 개의 부트스트랩 표본으로 나누어 각각 모델을 학습한 뒤 평균을 낸다', '조건부 독립 가정을 이용해 클래스별 확률을 계산한다']}
  correctAnswer={2}
  explanation="SVM은 두 클래스를 구분하는 여러 경계(초평면, hyperplane) 후보 중, 경계에 가장 가깝게 위치한 데이터 포인트인 서포트 벡터(support vector)까지의 거리, 즉 마진(margin)을 최대로 넓히는 경계를 최적의 결정 경계로 선택한다. 선형으로 나눌 수 없는 데이터는 커널(kernel) 함수를 이용해 더 높은 차원으로 매핑한 뒤 분리하기도 한다."
  optionExplanations={['다수결로 클래스를 정하는 것은 K-NN에 대한 설명이다.', '정답이다. 서포트 벡터까지의 마진을 최대화하는 경계를 찾는 것이 SVM의 핵심 아이디어다.', '부트스트랩 표본으로 여러 모델을 학습해 평균 내는 것은 배깅(bagging)에 대한 설명이다.', '조건부 독립 가정으로 확률을 계산하는 것은 나이브 베이즈에 대한 설명이다.']}
/>

## 앙상블·비지도학습·차원 축소 (16–19번)

<Quiz
  questionNumber={16}
  question="배깅(bagging)과 부스팅(boosting)의 차이에 대한 설명으로 옳은 것은?"
  options={['배깅은 여러 모델을 순차적으로 학습시켜 이전 모델의 오차를 다음 모델이 보완하고, 부스팅은 여러 모델을 병렬로 독립 학습시켜 평균·다수결을 낸다', '배깅은 부트스트랩(bootstrap, 복원추출)으로 만든 여러 표본에서 병렬로 모델을 학습해 평균·다수결을 내고, 부스팅은 이전 모델이 틀린 데이터에 가중치를 주며 순차적으로 모델을 추가한다', '배깅과 부스팅은 완전히 동일한 알고리즘이며 이름만 다르다', '배깅과 부스팅 모두 단일 모델만 사용하고 여러 모델을 결합하지 않는다']}
  correctAnswer={2}
  explanation="배깅(bagging, Bootstrap Aggregating)은 원본 데이터에서 복원추출로 여러 개의 부트스트랩 표본을 만들고, 각 표본으로 독립적인 모델을 병렬로 학습시킨 뒤 평균(회귀)이나 다수결(분류)로 최종 예측을 낸다(랜덤포레스트가 대표적 예). 부스팅(boosting)은 이전 모델이 잘못 예측한 데이터에 더 큰 가중치를 주면서 다음 모델을 순차적으로 추가해 나가는 방식이다."
  optionExplanations={['순차 학습과 병렬 학습의 설명이 배깅·부스팅과 서로 뒤바뀌었다.', '정답이다. 배깅은 병렬 학습 후 평균·다수결, 부스팅은 오차에 가중치를 주며 순차적으로 모델을 추가한다.', '배깅과 부스팅은 모델을 결합하는 방식이 근본적으로 다른 별개의 앙상블 기법이다.', '배깅과 부스팅은 모두 여러 개의 모델(주로 결정트리)을 결합하는 앙상블 기법이므로 단일 모델만 쓴다는 설명은 틀렸다.']}
/>

<Quiz
  questionNumber={17}
  question="K-means 군집화(clustering)와 DBSCAN 군집화의 차이로 옳은 것은?"
  options={['K-means는 군집 수(K)를 미리 지정해야 하지만, DBSCAN은 밀도 기반으로 군집 수를 자동으로 찾고 이상치를 별도로 구분할 수 있다', 'K-means는 밀도 기반, DBSCAN은 군집 수를 미리 지정하는 방식이다', '두 방법 모두 군집 수를 반드시 미리 지정해야 한다', 'DBSCAN은 지도학습이고 K-means는 비지도학습이다']}
  correctAnswer={1}
  explanation="K-means는 하이퍼파라미터로 군집의 개수 K를 미리 정해야 하고, 각 데이터를 가장 가까운 중심(centroid)에 할당하는 방식으로 동작한다. DBSCAN(Density-Based Spatial Clustering of Applications with Noise)은 군집 수를 미리 정하지 않고, 반경(eps)과 최소 이웃 수(min_samples) 같은 밀도 기준으로 촘촘한 영역을 군집으로 묶으며, 어느 군집에도 속하지 않는 점을 이상치(noise)로 분리할 수 있다."
  optionExplanations={['정답이다. K-means는 K를 미리 지정하고, DBSCAN은 밀도 기준으로 군집 수를 자동으로 정하며 이상치를 구분한다.', '두 알고리즘의 특징이 서로 뒤바뀐 설명이다.', 'DBSCAN은 군집 수를 미리 지정하지 않는 것이 핵심 특징이므로 이 서술은 틀렸다.', 'K-means와 DBSCAN은 둘 다 레이블 없이 학습하는 비지도학습 알고리즘이다.']}
/>

<Quiz
  questionNumber={18}
  question="주성분 분석(PCA, Principal Component Analysis)의 목적으로 가장 적절한 것은?"
  options={['클래스 레이블을 이용해 클래스 간 분리를 최대화하는 축을 찾는다', '데이터의 분산(variance)을 최대한 보존하면서 서로 직교(orthogonal)하는 새로운 축(주성분)으로 데이터를 변환해 차원을 줄인다', '결측치를 자동으로 채워 넣는다', '군집의 개수를 자동으로 결정한다']}
  correctAnswer={2}
  explanation="PCA는 레이블 정보를 사용하지 않는 비지도 기법으로, 원본 데이터의 분산을 가장 많이 설명하는 방향을 첫 번째 주성분으로 잡고, 그와 직교(90도를 이루는, 서로 독립적인 방향)하면서 그다음으로 분산을 많이 설명하는 방향을 두 번째 주성분으로 잡는 방식으로 축을 새로 구성한다. 분산이 큰 축 몇 개만 남기면 정보 손실을 최소화하면서 차원을 줄일 수 있다."
  optionExplanations={['클래스 레이블을 이용해 클래스 간 분리를 최대화하는 것은 PCA가 아니라 LDA(선형판별분석)의 목적이다.', '정답이다. 분산을 최대한 보존하는 직교 축을 찾아 차원을 줄이는 것이 PCA의 목적이다.', 'PCA는 결측치 처리 기법이 아니라 차원 축소 기법이다.', '군집 개수 결정은 PCA의 목적이 아니라 K-means 등 군집화에서 별도로 다루는 문제다.']}
/>

<Quiz
  questionNumber={19}
  question="연관 규칙(association rule) 분석에서 '지지도(support)'가 의미하는 것으로 옳은 것은?"
  options={['어떤 항목집합이 전체 거래 중 몇 퍼센트에서 함께 나타나는지를 나타내는 비율이다', '조건 항목을 산 사람이 결과 항목도 살 조건부 확률이다', '두 항목이 우연히 함께 나타날 확률 대비 실제로 함께 나타난 정도의 비율이다', '군집 간의 거리를 나타내는 지표다']}
  correctAnswer={1}
  explanation="지지도(support)는 특정 항목집합(예: 빵과 우유)이 전체 거래 중 몇 퍼센트에 함께 등장하는지를 나타내는 비율이다. 이는 그 조합이 얼마나 흔하게 나타나는 패턴인지를 알려 주는 기초 지표이며, 조건부 확률을 다루는 신뢰도(confidence), 우연 대비 연관성을 다루는 향상도(lift)와는 정의가 다르다."
  optionExplanations={['정답이다. 지지도는 전체 거래 중 해당 항목집합이 함께 나타나는 비율이다.', '조건 항목을 산 사람이 결과 항목도 살 조건부 확률은 신뢰도(confidence)에 대한 설명이다.', '우연히 함께 나타날 확률 대비 실제 비율은 향상도(lift)에 대한 설명이다.', '지지도는 연관 규칙 분석의 지표로, 군집 간 거리와는 관련이 없다.']}
/>

## 평가·모델선택·신경망 (20–22번)

<Quiz
  questionNumber={20}
  question="분류 모델 평가에서 정밀도(precision)와 재현율(recall)의 차이로 옳은 것은?"
  options={['정밀도는 실제 양성 중 모델이 양성으로 맞힌 비율이고, 재현율은 모델이 양성이라고 예측한 것 중 실제 양성 비율이다', '정밀도는 모델이 양성이라고 예측한 것 중 실제로 양성인 비율이고, 재현율은 실제 양성 중 모델이 양성으로 맞힌 비율이다', '정밀도와 재현율은 항상 같은 값을 갖는다', '정밀도와 재현율은 회귀 문제에서만 사용하는 지표다']}
  correctAnswer={2}
  explanation="정밀도(precision)는 모델이 양성(positive)이라고 예측한 것들 중 실제로 양성인 것의 비율(TP/(TP+FP))이고, 재현율(recall)은 실제 양성인 것들 중 모델이 양성으로 올바르게 찾아낸 것의 비율(TP/(TP+FN))이다. 정밀도는 '예측이 얼마나 정확한가', 재현율은 '실제 양성을 얼마나 놓치지 않았는가'를 각각 나타내며, 이 둘은 서로 트레이드오프 관계에 있는 경우가 많다."
  optionExplanations={['정밀도와 재현율의 정의가 서로 뒤바뀐 설명이다.', '정답이다. 정밀도는 예측 양성 중 실제 양성 비율, 재현율은 실제 양성 중 맞힌 비율이다.', '두 지표는 계산 기준이 달라 일반적으로 값이 다르며, 둘 사이에는 트레이드오프가 존재한다.', '정밀도·재현율은 분류(classification) 문제에서 사용하는 대표적인 평가 지표다.']}
/>

<Quiz
  questionNumber={21}
  question="과적합(overfitting)과 과소적합(underfitting)에 대한 설명으로 옳지 않은 것은?"
  options={['과적합은 모델이 훈련 데이터의 잡음까지 지나치게 학습해 새로운 데이터에 대한 일반화 성능이 떨어지는 현상이다', '과소적합은 모델이 너무 단순해 훈련 데이터의 패턴조차 제대로 학습하지 못하는 현상이다', '정규화(regularization)를 강하게 걸수록 과적합 위험은 줄어들지만, 지나치면 과소적합이 될 수 있다', '교차검증(cross-validation)을 사용하면 과적합 여부와 관계없이 모델의 실제 성능이 항상 100퍼센트 향상된다']}
  correctAnswer={4}
  explanation="교차검증(cross-validation)은 데이터를 여러 겹(fold)으로 나누어 학습·검증을 반복함으로써 모델의 일반화 성능을 더 신뢰성 있게 추정하는 평가 절차일 뿐, 그 자체가 모델의 실제 예측 성능을 향상시키는 학습 기법은 아니다. '모델의 실제 성능이 항상 100퍼센트 향상된다'는 서술은 교차검증의 역할을 과장한 잘못된 설명이다."
  optionExplanations={['훈련 데이터의 잡음까지 학습해 일반화 성능이 떨어지는 것은 과적합의 정확한 정의이므로 옳은 설명이다.', '모델이 너무 단순해 패턴을 제대로 못 배우는 것은 과소적합의 정확한 정의이므로 옳은 설명이다.', '정규화를 강하게 걸수록 과적합은 줄지만 지나치면 과소적합으로 이어진다는 것은 옳은 트레이드오프 설명이다.', '정답이다. 교차검증은 성능을 더 정확히 추정하는 평가 절차이지, 모델 성능을 향상시키는 기법이 아니다.']}
/>

<Quiz
  questionNumber={22}
  question="퍼셉트론(perceptron), 다층 퍼셉트론(MLP, Multi-Layer Perceptron), 역전파(backpropagation)의 관계에 대한 설명으로 옳은 것은?"
  options={['퍼셉트론은 은닉층(hidden layer)을 여러 개 가진 구조로, 선형·비선형 문제를 모두 풀 수 있다', 'MLP는 입력층과 출력층 사이에 하나 이상의 은닉층을 두어 단일 퍼셉트론이 풀지 못하는 비선형 문제를 다룰 수 있고, 역전파는 오차를 출력층에서 입력층 방향으로 전파해 각 층의 가중치를 갱신하는 학습 알고리즘이다', '역전파는 데이터를 전처리하는 기법이다', '단일 퍼셉트론은 XOR과 같은 선형적으로 분리되지 않는 문제도 학습 없이 바로 풀 수 있다']}
  correctAnswer={2}
  explanation="단일 퍼셉트론은 입력을 하나의 직선(또는 초평면)으로만 분리할 수 있어 XOR처럼 선형적으로 분리되지 않는 문제를 풀지 못한다. MLP는 입력층과 출력층 사이에 하나 이상의 은닉층과 비선형 활성화 함수를 추가해 이런 비선형 문제까지 표현할 수 있게 확장한 구조다. 역전파(backpropagation)는 예측값과 실제값의 오차를 출력층에서부터 입력층 방향으로 거꾸로 전파하면서, 각 층의 가중치가 오차에 얼마나 기여했는지(기울기)를 계산해 가중치를 갱신하는 학습 알고리즘이다."
  optionExplanations={['은닉층을 여러 개 갖는 구조는 퍼셉트론이 아니라 MLP에 대한 설명이다. 단일 퍼셉트론은 은닉층이 없다.', '정답이다. MLP는 은닉층으로 비선형 문제를 다루고, 역전파는 오차를 역방향으로 전파해 가중치를 갱신한다.', '역전파는 전처리 기법이 아니라 신경망의 가중치를 학습(갱신)시키는 알고리즘이다.', '단일 퍼셉트론은 XOR과 같은 비선형 문제를 학습으로도 풀 수 없다는 것이 역사적으로도 유명한 한계다.']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [scikit-learn User Guide](https://scikit-learn.org/stable/user_guide.html)
