---
title: '22. 계산·지표·수식 문제 집중 연습'
description: '독학사 2단계 머신러닝 출제기준을 기반으로 재구성한 계산형 문제 22선, 회귀·확률·불순도·거리·경사하강·혼동행렬 계산을 대입부터 답까지'
---

<Callout type="warning">
  **이 문제는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학사 2단계 머신러닝 출제기준을 바탕으로 새로 구성한 계산 연습 문제입니다. 실제 회차의 숫자·문항 수는 다를 수 있으므로 공식 시행 공고를 함께 확인해야 한다. 이 편은 08~10편(회귀·정규화·로지스틱 회귀), 11~12편(결정트리·거리 기반 분류), 17~19편(차원 축소·평가 지표·모델 선택), 20편(신경망 기초)에서 다룬 계산을 실제 숫자로 다시 훈련하는 데 목적이 있다.
</Callout>

계산형 문제는 답만 외워서는 다른 숫자로 바뀌면 바로 틀린다. 아래 해설은 공식을 적고, 그 공식에 실제 값을 대입하고, 정리해서 답을 내는 순서를 전부 보여 준다. 스스로 풀어 본 뒤 해설과 한 줄씩 대조하는 것을 권장한다.

## 회귀·정규화 계산 (1–4번)

<Quiz
  questionNumber={1}
  question="데이터 x=(1,2,3,4), y=(3,5,7,9)에 단순 선형회귀 직선 y=ax+b를 맞춰 기울기 a=2, 절편 b=1을 얻었다고 할 때, x=6에서의 예측값 y는?"
  options={['11', '12', '13', '14']}
  correctAnswer={3}
  explanation="단순 선형회귀식 y=ax+b에서 a=2(기울기), b=1(절편), x=6을 대입한다. y=2×6+1=12+1=13이다. 검산으로 원본 데이터를 보면 x가 1씩 늘 때 y가 2씩 느는 완전한 직선 관계(y=2x+1)이므로, x=4일 때 y=9인 패턴을 그대로 이어가면 x=5일 때 11, x=6일 때 13이 되어 계산이 일치한다."
  optionExplanations={['이것은 x=5를 대입한 값(2×5+1=11)으로 x를 잘못 대입한 경우다.', '이것은 절편 1을 더하지 않고 2×6만 계산한 값이다.', '정답이다. 2×6+1=13이다.', '이것은 13에 절편 1을 한 번 더 더한 계산 실수다.']}
/>

<Quiz
  questionNumber={2}
  question="실제값이 (3, 5, 2, 7)이고 모델의 예측값이 (4, 5, 3, 6)일 때, 평균제곱오차(MSE, Mean Squared Error)는?"
  options={['0.5', '0.75', '1', '3']}
  correctAnswer={2}
  explanation="MSE는 각 오차(실제값-예측값)를 제곱한 뒤 평균을 낸 값이다. 오차는 순서대로 3-4=-1, 5-5=0, 2-3=-1, 7-6=1이다. 각각을 제곱하면 1, 0, 1, 1이고, 이를 모두 더하면 3이다. 데이터 개수 4로 나누면 MSE=3/4=0.75이다."
  optionExplanations={['이것은 제곱합 3을 데이터 개수 6으로 나누는 등 분모를 잘못 잡은 값이다.', '정답이다. 제곱오차의 합 3을 데이터 개수 4로 나누면 0.75이다.', '이것은 제곱합 3을 개수로 나누지 않고 반올림한 값에 가깝다.', '이것은 나누기 전 제곱오차의 합(3) 자체를 답으로 낸 계산 누락이다.']}
/>

<Quiz
  questionNumber={3}
  question="릿지 회귀에서 정규화 이전 잔차제곱합(RSS)이 10, 회귀계수가 β=(2, -3), 정규화 강도 λ(람다)=0.5일 때, 릿지의 전체 목적함수 값(RSS + L2 페널티)은?"
  options={['15', '16.5', '10.5', '13']}
  correctAnswer={2}
  explanation="릿지 회귀의 목적함수는 RSS + λ×Σβ²(계수 제곱의 합)이다. 먼저 계수 제곱의 합을 구하면 2²+(-3)²=4+9=13이다. 여기에 λ=0.5를 곱하면 L2 페널티는 0.5×13=6.5이다. 마지막으로 RSS 10과 더하면 전체 목적함수 값은 10+6.5=16.5이다."
  optionExplanations={['이것은 λ를 곱하지 않고 계수 제곱의 합 13에서 절반 정도만 더한 값에 해당하는 계산 실수다.', '정답이다. RSS 10에 페널티 0.5×13=6.5를 더하면 16.5이다.', '이것은 계수 제곱의 합 13 대신 계수의 합 자체를 잘못 사용해 λ를 곱한 값에 가깝다.', '이것은 λ를 곱하기 전의 계수 제곱합 13의 절반(6.5) 대신 3만 더한 계산 실수다.']}
/>

<Quiz
  questionNumber={4}
  question="같은 회귀계수 β=(2, -3), 정규화 강도 λ=0.5를 라쏘 회귀(L1 정규화)에 적용할 때, L1 페널티 값(λ×Σ|β|)은?"
  options={['1.5', '2.5', '3.5', '6.5']}
  correctAnswer={2}
  explanation="라쏘 회귀의 L1 페널티는 λ×Σ|β|(계수 절댓값의 합)이다. 계수 절댓값의 합은 |2|+|-3|=2+3=5이다. 여기에 λ=0.5를 곱하면 L1 페널티는 0.5×5=2.5이다. 이는 릿지의 L2 페널티(제곱을 사용, 결과가 6.5)와 계산 방식이 다르다는 점에 유의한다."
  optionExplanations={['이것은 절댓값의 합 5를 잘못 나누거나 계수 하나만 반영한 계산 실수다.', '정답이다. λ×(2+3)=0.5×5=2.5이다.', '이것은 절댓값의 합 5에 λ를 곱하지 않고 다른 값을 더한 계산 실수다.', '이것은 릿지의 L2 페널티 값(제곱합 13에 λ를 곱한 6.5)과 혼동한 값이다.']}
/>

## 로지스틱 회귀·확률 계산 (5–7번)

<Quiz
  questionNumber={5}
  question="로지스틱 회귀에서 선형결합 값 z=2일 때, 시그모이드 함수 σ(z)=1/(1+e^(-z))의 값은 약 얼마인가? (e≈2.718)"
  options={['약 0.12', '약 0.50', '약 0.73', '약 0.88']}
  correctAnswer={4}
  explanation="시그모이드 함수는 σ(z)=1/(1+e^(-z))이다. z=2를 대입하면 σ(2)=1/(1+e^(-2))이다. e^(-2)는 1/e²로 약 1/7.389≈0.135이다. 이를 대입하면 σ(2)=1/(1+0.135)=1/1.135≈0.881이다. 따라서 약 0.88이다."
  optionExplanations={['이것은 e^(-2)의 값 자체(약 0.135)에 가까운 값으로, 분모 계산을 끝내지 않고 멈춘 값이다.', '이것은 z=0일 때의 시그모이드 값(1/2=0.5)으로, z=2를 대입하지 않은 경우다.', '이것은 z=1일 때의 시그모이드 값(약 0.731)으로 z를 잘못 대입한 경우다.', '정답이다. 1/(1+0.135)≈0.881로 약 0.88이다.']}
/>

<Quiz
  questionNumber={6}
  question="어떤 사건이 일어날 확률이 p=0.8일 때, 오즈(odds)와 로그 오즈(log-odds)의 값으로 옳은 것은? (ln4≈1.386)"
  options={['오즈=4, 로그 오즈≈1.386', '오즈=0.8, 로그 오즈≈0.8', '오즈=0.25, 로그 오즈≈-1.386', '오즈=1.25, 로그 오즈≈0.223']}
  correctAnswer={1}
  explanation="오즈는 p/(1-p)로 계산한다. p=0.8이므로 1-p=0.2이고, 오즈=0.8/0.2=4이다. 로그 오즈(log-odds)는 오즈에 자연로그를 취한 값이므로 ln(4)≈1.386이다."
  optionExplanations={['정답이다. 오즈=0.8/0.2=4이고, 로그 오즈=ln4≈1.386이다.', '이것은 확률 p 자체를 오즈와 로그 오즈로 착각한 값이다.', '이것은 오즈를 1/4로 뒤집어 계산한(실패 확률/성공 확률로 거꾸로 계산한) 값이다.', '이것은 p=0.8이 아니라 p=0.556 근처의 값에서나 나올 법한 오즈로, 계산 대입이 틀렸다.']}
/>

<Quiz
  questionNumber={7}
  question="로지스틱 회귀 모델의 가중치 w=(0.5, -1), 절편 b=0.5, 입력 x=(2, 1)일 때, 선형결합 z=w·x+b의 값은?"
  options={['0', '0.5', '1', '1.5']}
  correctAnswer={2}
  explanation="선형결합 z는 각 가중치와 입력을 곱해 더하고 절편을 더한 값이다. w·x는 0.5×2+(-1)×1=1-1=0이다. 여기에 절편 b=0.5를 더하면 z=0+0.5=0.5이다."
  optionExplanations={['이것은 절편 b=0.5를 더하지 않고 w·x=0만 남긴 계산 누락이다.', '정답이다. w·x=1-1=0에 절편 0.5를 더하면 0.5이다.', '이것은 0.5×2=1만 계산하고 -1×1=-1을 반영하지 않은 값이다.', '이것은 0.5×2+1×1+0.5처럼 두 번째 가중치의 부호를 잘못 처리한 값이다.']}
/>

## 결정트리 불순도 계산 (8–10번)

<Quiz
  questionNumber={8}
  question="어떤 노드에 양성 10개, 음성 6개(총 16개)가 섞여 있을 때, 이 노드의 엔트로피(entropy, 밑이 2인 로그 기준)는 약 얼마인가? (log₂0.625≈-0.678, log₂0.375≈-1.415)"
  options={['약 0.47', '약 0.63', '약 0.95', '약 1.00']}
  correctAnswer={3}
  explanation="엔트로피 공식은 H=-Σp·log₂(p)이다. 양성 비율 p₊=10/16=0.625, 음성 비율 p₋=6/16=0.375이다. 이를 대입하면 H=-(0.625×log₂0.625+0.375×log₂0.375)=-(0.625×(-0.678)+0.375×(-1.415))=-(-0.424-0.531)=0.955이다. 따라서 약 0.95이다."
  optionExplanations={['이것은 이 노드의 엔트로피가 아니라 지니 지수 계산 결과(약 0.469)에 해당하는 값이다.', '이것은 로그 계산 중 한쪽 항만 반영하고 나머지를 절반만 더한 계산 실수다.', '정답이다. 0.625×0.678+0.375×1.415≈0.955이다.', '이것은 양성·음성 비율이 정확히 반반(0.5, 0.5)일 때의 최대 엔트로피 값과 혼동한 값이다.']}
/>

<Quiz
  questionNumber={9}
  question="문제 8과 같은 노드(양성 10개, 음성 6개, 총 16개)의 지니 지수(Gini index)는?"
  options={['0.469', '0.531', '0.625', '0.955']}
  correctAnswer={1}
  explanation="지니 지수 공식은 Gini=1-Σp²이다. 양성 비율 p₊=0.625, 음성 비율 p₋=0.375를 대입하면 p₊²=0.390625, p₋²=0.140625이고, 둘을 더하면 0.53125이다. 1에서 이 값을 빼면 Gini=1-0.53125=0.46875, 반올림하면 0.469이다."
  optionExplanations={['정답이다. 1-(0.625²+0.375²)=1-0.53125=0.469이다.', '이것은 1에서 빼기 전의 Σp² 값(0.531)을 그대로 답으로 낸 계산 누락이다.', '이것은 양성 비율 p₊ 자체를 지니 지수로 착각한 값이다.', '이것은 지니 지수가 아니라 같은 노드의 엔트로피 값(약 0.955)이다.']}
/>

<Quiz
  questionNumber={10}
  question="부모 노드의 엔트로피가 1.0(양성 8개, 음성 8개, 총 16개)인 상태에서, 이를 두 자식 노드(각각 8개씩, 자식마다 엔트로피 약 0.811)로 균등하게 나누는 분할을 했을 때 정보 이득(information gain)은 약 얼마인가?"
  options={['0.095', '0.189', '0.811', '1.000']}
  correctAnswer={2}
  explanation="정보 이득은 '부모의 엔트로피 - 자식들의 가중평균 엔트로피'로 계산한다. 두 자식이 각각 8개씩 동일한 크기이고 엔트로피가 모두 0.811이므로, 가중평균은 (8/16)×0.811+(8/16)×0.811=0.811이다. 정보 이득은 1.0-0.811=0.189이다."
  optionExplanations={['이것은 정보 이득 0.189의 절반 정도에 해당하는 값으로, 가중치를 다시 절반으로 나누는 계산 실수다.', '정답이다. 부모 엔트로피 1.0에서 자식 가중평균 엔트로피 0.811을 빼면 0.189이다.', '이것은 자식 노드의 엔트로피 값 자체를 정보 이득으로 착각한 값이다.', '이것은 부모 노드의 엔트로피 값 자체를 정보 이득으로 착각한 값이다.']}
/>

## 거리·유사도 계산 (11–13번)

<Quiz
  questionNumber={11}
  question="2차원 점 A(1, 2)와 B(4, 6) 사이의 유클리드 거리(Euclidean distance)는?"
  options={['3', '4', '5', '7']}
  correctAnswer={3}
  explanation="유클리드 거리 공식은 두 점의 각 좌표 차이를 제곱해 더한 뒤 제곱근을 씌운 것이다. x좌표 차이는 4-1=3, y좌표 차이는 6-2=4이다. 이를 제곱해 더하면 3²+4²=9+16=25이고, 제곱근을 씌우면 √25=5이다."
  optionExplanations={['이것은 x좌표 차이 3만 답으로 낸 계산 누락이다.', '이것은 y좌표 차이 4만 답으로 낸 계산 누락이다.', '정답이다. √(3²+4²)=√25=5이다.', '이것은 맨해튼 거리(|3|+|4|=7)와 혼동한 값이다.']}
/>

<Quiz
  questionNumber={12}
  question="문제 11과 같은 두 점 A(1, 2), B(4, 6) 사이의 맨해튼 거리(Manhattan distance)는?"
  options={['5', '6', '7', '25']}
  correctAnswer={3}
  explanation="맨해튼 거리는 각 좌표 차이의 절댓값을 그대로 더한 값이다(제곱근을 씌우지 않는다). x좌표 차이의 절댓값은 |4-1|=3, y좌표 차이의 절댓값은 |6-2|=4이다. 이를 더하면 3+4=7이다."
  optionExplanations={['이것은 유클리드 거리(√25=5)와 혼동한 값이다.', '이것은 3+4를 잘못 더해 6으로 계산한 산술 실수다.', '정답이다. |4-1|+|6-2|=3+4=7이다.', '이것은 제곱근을 씌우기 전 제곱합(9+16=25)을 그대로 답으로 낸 계산 누락이다.']}
/>

<Quiz
  questionNumber={13}
  question="벡터 a=(1, 0)과 b=(1, 1)의 코사인 유사도(cosine similarity)는? (√2≈1.414)"
  options={['0', '0.5', '0.707', '1']}
  correctAnswer={3}
  explanation="코사인 유사도는 두 벡터의 내적을 각 벡터 크기(노름, norm)의 곱으로 나눈 값이다. 내적은 a·b=1×1+0×1=1이다. a의 크기는 √(1²+0²)=1이고, b의 크기는 √(1²+1²)=√2≈1.414이다. 따라서 코사인 유사도는 1/(1×1.414)≈0.707이다."
  optionExplanations={['이것은 두 벡터가 완전히 직교(90도)할 때의 코사인 유사도 값이며, 이 문제의 벡터는 직교하지 않는다.', '이것은 내적 1을 벡터 크기의 곱(1.414)이 아니라 2로 나눈 계산 실수다.', '정답이다. 내적 1을 크기의 곱 1×1.414로 나누면 약 0.707이다.', '이것은 두 벡터의 크기를 곱하지 않고 내적 값 1을 그대로 답으로 낸 계산 누락이다.']}
/>

## 경사하강·퍼셉트론·신경망 계산 (14–17번)

<Quiz
  questionNumber={14}
  question="파라미터 θ(세타)=3, 손실함수의 θ에 대한 기울기(gradient) ∇=4, 학습률(learning rate) η(에타)=0.1일 때, 경사하강법 1스텝을 거친 새로운 θ 값은?"
  options={['2.6', '2.9', '3.4', '7']}
  correctAnswer={1}
  explanation="경사하강법의 갱신 공식은 θ_new=θ_old-η×∇이다. θ_old=3, η=0.1, ∇=4를 대입하면 θ_new=3-0.1×4=3-0.4=2.6이다. 기울기가 양수이므로 θ 값이 줄어드는 방향으로 갱신되었다."
  optionExplanations={['정답이다. 3-0.1×4=3-0.4=2.6이다.', '이것은 0.1×4=0.4가 아니라 0.1만 뺀 계산 실수다.', '이것은 부호를 반대로 처리해 3+0.4를 계산한 값이다.', '이것은 η와 ∇를 곱하지 않고 그대로 더한(3+4) 계산 실수다.']}
/>

<Quiz
  questionNumber={15}
  question="퍼셉트론의 가중치 w=(0.5, -0.2), 입력 x=(1, 2), 목표값(target)=1, 현재 출력(output)=0, 학습률 η=0.1일 때, 가중치 갱신 공식 w_new=w+η×(target-output)×x를 적용한 새로운 가중치는?"
  options={['(0.5, -0.2)', '(0.6, 0.0)', '(0.4, -0.4)', '(0.7, 0.2)']}
  correctAnswer={2}
  explanation="갱신량은 η×(target-output)×x이다. target-output=1-0=1이므로 갱신량은 0.1×1×(1,2)=(0.1, 0.2)이다. 이를 기존 가중치 (0.5, -0.2)에 더하면 새로운 가중치는 (0.5+0.1, -0.2+0.2)=(0.6, 0.0)이다."
  optionExplanations={['이것은 갱신량을 아예 더하지 않은, 갱신 전의 원래 가중치 값이다.', '정답이다. (0.5+0.1, -0.2+0.2)=(0.6, 0.0)이다.', '이것은 갱신량의 부호를 반대로 적용해 뺀 값이다.', '이것은 학습률 0.1을 곱하지 않고 (target-output)×x 값을 그대로 더한 계산 실수다.']}
/>

<Quiz
  questionNumber={16}
  question="신경망의 출력층 값(로짓, logit)이 z=(2, 1, 0)일 때, 소프트맥스(softmax) 함수를 적용하면 첫 번째 클래스의 확률은 약 얼마인가? (e≈2.718, e²≈7.389)"
  options={['약 0.09', '약 0.24', '약 0.33', '약 0.67']}
  correctAnswer={4}
  explanation="소프트맥스는 각 값을 지수함수에 통과시킨 뒤 전체 합으로 나눠 확률로 만든다. e^2≈7.389, e^1≈2.718, e^0=1이다. 전체 합은 7.389+2.718+1=11.107이다. 첫 번째 클래스의 확률은 7.389/11.107≈0.665로, 약 0.67이다."
  optionExplanations={['이것은 세 번째 클래스(z=0)의 확률(1/11.107≈0.09)에 해당하는 값이다.', '이것은 두 번째 클래스(z=1)의 확률(2.718/11.107≈0.245)에 해당하는 값이다.', '이것은 세 클래스가 균등하다고 가정했을 때(1/3)의 값으로, 실제 지수 계산을 반영하지 않은 값이다.', '정답이다. 7.389/11.107≈0.665로 약 0.67이다.']}
/>

<Quiz
  questionNumber={17}
  question="PCA에서 각 주성분의 고유값(eigenvalue)이 5, 3, 2일 때, 첫 번째 주성분(PC1)이 설명하는 분산의 비율은?"
  options={['20%', '30%', '50%', '80%']}
  correctAnswer={3}
  explanation="한 주성분이 설명하는 분산 비율은 그 주성분의 고유값을 전체 고유값의 합으로 나눈 값이다. 전체 고유값의 합은 5+3+2=10이다. PC1의 고유값은 5이므로 분산 설명 비율은 5/10=0.5, 즉 50퍼센트다."
  optionExplanations={['이것은 세 번째 주성분(고유값 2)의 분산 비율(2/10=20%)이다.', '이것은 두 번째 주성분(고유값 3)의 분산 비율(3/10=30%)이다.', '정답이다. 5/10=0.5로 50퍼센트다.', '이것은 PC1과 PC2의 고유값을 합쳐(5+3=8) 8/10으로 잘못 계산한 값이다.']}
/>

## 혼동행렬·평가지표 계산 (18–22번)

<Quiz
  questionNumber={18}
  question="혼동행렬에서 참양성(TP)=40, 거짓양성(FP)=10, 거짓음성(FN)=20, 참음성(TN)=30일 때, 정밀도(precision)는?"
  options={['0.5', '0.6', '0.7', '0.8']}
  correctAnswer={4}
  explanation="정밀도 공식은 TP/(TP+FP)이다. TP=40, FP=10을 대입하면 40/(40+10)=40/50=0.8이다."
  optionExplanations={['이것은 TP를 전체 데이터 수(TP+FP+FN+TN=100)로 나눈 값과 혼동한 계산이다.', '이것은 분모에 FN을 잘못 포함해 40/(40+10+10)처럼 계산한 값에 가깝다.', '이것은 재현율(TP/(TP+FN)=40/60≈0.667)에 더 가깝게 어림한 값이다.', '정답이다. 40/(40+10)=0.8이다.']}
/>

<Quiz
  questionNumber={19}
  question="문제 18과 같은 혼동행렬(TP=40, FP=10, FN=20, TN=30)에서 재현율(recall)은? (분수로 계산 후 소수 셋째 자리에서 반올림)"
  options={['0.5', '0.6', '0.667', '0.8']}
  correctAnswer={3}
  explanation="재현율 공식은 TP/(TP+FN)이다. TP=40, FN=20을 대입하면 40/(40+20)=40/60=0.6666...이며, 소수 셋째 자리에서 반올림하면 0.667이다."
  optionExplanations={['이것은 재현율 공식의 분모를 다르게 잡은 계산 실수다.', '이것은 40/60을 0.6으로 어림해 반올림 자리를 틀린 값이다.', '정답이다. 40/60=0.6667…이므로 반올림하면 0.667이다.', '이것은 정밀도(TP/(TP+FP)=40/50=0.8)와 혼동한 값이다.']}
/>

<Quiz
  questionNumber={20}
  question="문제 18의 정밀도(0.8)와 문제 19의 재현율(0.667)을 이용해 F1 점수(F1=2×정밀도×재현율/(정밀도+재현율))를 계산하면 약 얼마인가?"
  options={['0.6', '0.7', '0.727', '0.8']}
  correctAnswer={3}
  explanation="F1 점수는 정밀도와 재현율의 조화평균으로, 공식은 2×정밀도×재현율/(정밀도+재현율)이다. 정밀도=0.8, 재현율=0.667을 대입하면 분자는 2×0.8×0.667=1.067이고 분모는 0.8+0.667=1.467이다. 이를 나누면 1.067/1.467≈0.727이다."
  optionExplanations={['이것은 정밀도와 재현율의 단순 산술평균(0.8+0.6)/2=0.7보다도 낮게 어림한 값이다.', '이것은 정밀도와 재현율의 단순 산술평균(0.8+0.667)/2≈0.733에 가깝게 반올림을 잘못한 값이다.', '정답이다. 2×0.8×0.667/(0.8+0.667)≈0.727이다.', '이것은 정밀도 값 0.8을 그대로 F1으로 착각한 값이다.']}
/>

<Quiz
  questionNumber={21}
  question="문제 18과 같은 혼동행렬(TP=40, FP=10, FN=20, TN=30, 전체=100)에서 정확도(accuracy)는?"
  options={['0.6', '0.7', '0.75', '0.8']}
  correctAnswer={2}
  explanation="정확도 공식은 (TP+TN)/전체 데이터 수이다. TP=40, TN=30을 더하면 70이고, 전체 데이터 수는 40+10+20+30=100이다. 따라서 정확도는 70/100=0.7이다."
  optionExplanations={['이것은 TN(30)만 전체로 나눈(30/50) 값과 혼동한 계산이다.', '정답이다. (40+30)/100=70/100=0.7이다.', '이것은 TP만 전체(40+30+... )가 아니라 다른 기준으로 나눈 값에 가깝다.', '이것은 정밀도(0.8)와 혼동한 값이다.']}
/>

<Quiz
  questionNumber={22}
  question="다른 혼동행렬에서 TP=40, FN=10, FP=15, TN=35일 때, 거짓양성비율(FPR, False Positive Rate = FP/(FP+TN))은?"
  options={['0.15', '0.3', '0.35', '0.8']}
  correctAnswer={2}
  explanation="거짓양성비율(FPR)은 실제 음성 중 모델이 잘못 양성으로 예측한 비율로, 공식은 FP/(FP+TN)이다. FP=15, TN=35를 대입하면 15/(15+35)=15/50=0.3이다."
  optionExplanations={['이것은 FP 값(15) 자체를 100으로 나눈 값(0.15)으로, 분모를 잘못 잡은 계산이다.', '정답이다. 15/(15+35)=15/50=0.3이다.', '이것은 TN 비율과 혼동해 35/100=0.35로 계산한 값이다.', '이것은 재현율(TPR=TP/(TP+FN)=40/50=0.8)과 혼동한 값이다.']}
/>

## 참고 자료

- [scikit-learn Model Evaluation — Metrics and scoring](https://scikit-learn.org/stable/modules/model_evaluation.html)
- [scikit-learn User Guide](https://scikit-learn.org/stable/user_guide.html)
- [Dimensionality Reduction 강의 노트 (PCA, LDA)](https://web.pdx.edu/~arhodes/ml6.pdf)
