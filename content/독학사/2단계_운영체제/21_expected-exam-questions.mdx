---
title: '21. 독학사 2단계 운영체제 예상기출문제'
description: '국가평생교육진흥원 출제기준에 근거해 재구성한 전 범위 혼합 모의고사 25문항 — 스케줄링·페이지 교체·은행원 알고리즘·디스크 이동 거리 계산 포함'
---

<Callout type="warning">
  **이 모의고사는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학학위제 2단계 운영체제 평가영역(출제기준)을 근거로, 다루는 개념·계산 유형·오답 구성 방식을 참고해 숫자와 지문을 모두 새로 구성한 예상문제입니다. 실제 회차의 문항 수와 시험 시간은 매 공고마다 달라질 수 있으므로 **반드시 응시 회차의 공식 공고를 확인**하시기 바랍니다. 이 편은 01~20편에서 다룬 전 범위를 골고루 섞은 혼합 문제 25개로 구성했습니다.
</Callout>

CPU 스케줄링과 페이지 교체, 은행원 알고리즘의 안전 순서, 디스크 헤드 이동 거리처럼 계산 과정이 있는 문항은 해설에 계산의 각 단계를 전부 풀어 두었습니다. 답만 확인하지 말고, 해설의 계산 과정을 손으로 따라 그려 보면서 검산하는 방식으로 복습하는 것을 권장합니다.

## 문항 배분

| 영역 | 문항 수 | 문항 번호 |
|------|---------|-----------|
| 프로세스 관리와 CPU 스케줄링 | 8 | 1–8 |
| 동기화와 교착상태 | 6 | 9–14 |
| 메모리 관리와 페이지 교체 | 6 | 15–20 |
| 파일 시스템·입출력·보호와 보안 | 5 | 21–25 |
| 합계 | 25 | 1–25 |

## 프로세스 관리와 CPU 스케줄링 (1–8번)

<Quiz
  questionNumber={1}
  question="마이크로커널(microkernel) 구조에 대한 설명으로 옳지 않은 것은?"
  options={['최소한의 핵심 기능만 커널에 남기고 나머지 서비스는 사용자 영역의 서버 프로세스로 옮긴다.', '모놀리식 커널에 비해 서비스 간 메시지 전달에 따른 오버헤드가 일반적으로 더 크다.', '파일 시스템 같은 서비스도 반드시 커널 내부에 포함되어야 동작할 수 있다.', '특정 서비스 하나에 결함이 생겨도 전체 시스템이 다운될 위험이 상대적으로 낮다.']}
  correctAnswer={3}
  explanation="마이크로커널의 핵심은 파일 시스템, 장치 드라이버 같은 서비스를 커널 밖의 사용자 영역 서버로 분리하는 것이다. 따라서 파일 시스템이 반드시 커널 내부에 있어야 한다는 설명은 마이크로커널의 정의에 정면으로 어긋난다."
  optionExplanations={['옳은 설명이다. 최소 기능만 남기는 것이 마이크로커널의 핵심 설계 원칙이다.', '옳은 설명이다. 메시지 전달 방식이라 모놀리식보다 오버헤드가 크다는 것이 일반적인 트레이드오프다.', '정답. 마이크로커널은 오히려 파일 시스템 같은 서비스를 커널 밖으로 빼내는 구조다.', '옳은 설명이다. 서비스가 분리되어 있어 결함 격리에 유리하다.']}
/>

<Quiz
  questionNumber={2}
  question="다음 중 프로세스 제어 블록(PCB)에 일반적으로 포함되지 않는 정보는?"
  options={['프로그램 카운터 값', '프로세스 상태(준비, 실행, 대기 등)', '파일에 대한 접근 권한 정보', 'CPU 레지스터의 저장된 값']}
  correctAnswer={3}
  explanation="파일에 대한 접근 권한은 파일 제어 블록(FCB)에 담기는 정보이며, 프로세스의 실행 문맥을 담는 PCB의 일반적인 구성 요소가 아니다."
  optionExplanations={['프로그램 카운터는 PCB의 핵심 구성 요소다.', '프로세스 상태 역시 PCB에 반드시 담기는 정보다.', '정답. 파일 접근 권한은 FCB의 정보이지 PCB의 정보가 아니다.', 'CPU 레지스터 값은 문맥 교환을 위해 PCB에 저장되는 정보다.']}
/>

<Quiz
  questionNumber={3}
  question="실행(running) 상태의 프로세스가 디스크 입출력을 요청했을 때 전이하는 상태는?"
  options={['준비(ready) 상태', '대기(waiting/blocked) 상태', '종료(terminated) 상태', '새로운(new) 상태']}
  correctAnswer={2}
  explanation="실행 중인 프로세스가 입출력처럼 즉시 끝나지 않는 이벤트를 요청하면, CPU를 내려놓고 그 이벤트가 끝날 때까지 대기 상태로 전이한다."
  optionExplanations={['준비 상태는 CPU 배정을 기다리는 상태로, 시간 할당량 만료 등으로 전이한다.', '정답. 입출력 요청처럼 결과를 기다려야 하는 이벤트가 발생하면 대기 상태로 전이한다.', '종료 상태는 프로세스 실행이 끝났을 때의 상태다.', '새로운 상태는 프로세스가 막 생성되어 아직 준비 큐에 들어가기 전의 상태다.']}
/>

<Quiz
  questionNumber={4}
  question="스레드와 프로세스에 대한 설명으로 옳지 않은 것은?"
  options={['같은 프로세스에 속한 스레드들은 코드 영역과 데이터 영역을 공유한다.', '각 스레드는 자신만의 독립적인 스택과 레지스터 값을 가진다.', '스레드 간 문맥 교환은 일반적으로 프로세스 간 문맥 교환보다 비용이 더 크다.', '커널 수준 스레드 모델에서는 한 스레드가 시스템 호출로 블록되어도 같은 프로세스의 다른 스레드는 계속 실행될 수 있다.']}
  correctAnswer={3}
  explanation="스레드는 같은 프로세스 내에서 코드·데이터·힙 등 많은 자원을 이미 공유하고 있어 전환 시 바꿔야 할 정보가 적으므로, 스레드 간 문맥 교환이 프로세스 간 문맥 교환보다 일반적으로 비용이 더 적다. 더 크다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 코드·데이터 영역 공유는 스레드의 정의적 특징이다.', '옳은 설명이다. 스택과 레지스터는 스레드마다 독립적으로 유지된다.', '정답. 스레드 전환이 프로세스 전환보다 비용이 더 적은 것이 일반적이므로 옳지 않다.', '옳은 설명이다. 커널 수준 스레드는 커널이 각 스레드를 독립적으로 스케줄링할 수 있어 한 스레드의 블록이 다른 스레드에 영향을 주지 않는다.']}
/>

<Quiz
  questionNumber={5}
  question="도착 시간이 모두 0인 프로세스 P1(버스트 5), P2(버스트 3), P3(버스트 8), P4(버스트 6)를 SJF(비선점) 알고리즘으로 스케줄링할 때 평균 대기 시간은?"
  options={['5.25', '6.25', '7.25', '8.25']}
  correctAnswer={2}
  explanation="SJF는 버스트 시간이 짧은 순서 P2(3), P1(5), P4(6), P3(8)로 실행한다. P2는 대기 없이 0에서 시작해 대기시간 0, P1은 P2가 끝난 3부터 시작해 대기시간 3, P4는 P1이 끝난 8부터 시작해 대기시간 8, P3는 P4가 끝난 14부터 시작해 대기시간 14다. 평균 대기시간은 (0+3+8+14)를 4로 나눈 25/4=6.25다."
  optionExplanations={['버스트 순서를 잘못 배열하면 나오는 값으로, 실제 SJF 순서와 다르다.', '정답. (0+3+8+14)/4=25/4=6.25다.', '이는 같은 프로세스들을 FCFS(도착 순서 P1,P2,P3,P4)로 스케줄링했을 때의 평균 대기시간이다.', '대기시간 합을 잘못 더했을 때 나오는 값이다.']}
/>

<Quiz
  questionNumber={6}
  question="프로세스 P1(버스트 24), P2(버스트 3), P3(버스트 3)가 모두 시각 0에 도착했다. 시간 할당량(quantum)이 4인 라운드 로빈으로 스케줄링할 때 평균 대기 시간은? (소수 둘째 자리에서 반올림)"
  options={['4.67', '5.67', '6.67', '7.67']}
  correctAnswer={2}
  explanation="간트 차트는 P1(0–4), P2(4–7, 남은 3 모두 소진), P3(7–10, 남은 3 모두 소진), P1(10–14), P1(14–18), P1(18–22), P1(22–26)의 순서로 진행되어 P1은 26에 종료된다. 완료 시각에서 도착 시각과 버스트 시간을 빼면 대기시간은 P1=26-0-24=2가 아니라, 첫 실행 뒤 대기한 시간을 모두 더하는 방식으로 계산하면 P1은 4~10 대기(6)와 10 이후 연속 실행이므로 총 대기시간 6, P2는 0~4 대기(4), P3는 0~7 대기(7)다. 평균은 (6+4+7)/3=17/3=5.67이다."
  optionExplanations={['P3의 대기시간을 실제보다 1턴 적게 계산했을 때 나오는 값이다.', '정답. (6+4+7)/3=17/3=5.666...으로 반올림하면 5.67이다.', 'P1의 대기시간을 과대 계산했을 때 나오는 값이다.', '세 프로세스의 대기시간을 모두 1씩 크게 계산했을 때 나오는 값이다.']}
/>

<Quiz
  questionNumber={7}
  question="CPU 스케줄링 알고리즘에 대한 설명으로 옳지 않은 것은?"
  options={['HRRN은 대기시간과 서비스시간을 함께 고려해 우선순위를 계산하는 비선점 방식이다.', 'SRTF는 SJF의 선점형 버전으로, 더 짧은 잔여 실행시간을 가진 프로세스가 도착하면 실행 중인 프로세스를 중단시킨다.', '라운드 로빈은 시간 할당량이 매우 크게 설정되면 사실상 FCFS와 비슷하게 동작한다.', '우선순위 스케줄링은 어떤 구현에서도 항상 기아 상태를 방지할 수 있는 스케줄링 기법이다.']}
  correctAnswer={4}
  explanation="우선순위 스케줄링은 낮은 우선순위 프로세스가 계속 뒤로 밀리는 기아 상태가 발생할 수 있는 대표적인 알고리즘이며, 이를 막으려면 대기 시간에 따라 우선순위를 점점 높여 주는 에이징(aging) 같은 별도의 보완책이 필요하다. 항상 기아 상태를 방지한다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. HRRN 우선순위 계산식(대기시간+서비스시간)/서비스시간에 대한 정확한 설명이다.', '옳은 설명이다. SRTF는 SJF의 선점형 버전이 맞다.', '옳은 설명이다. 할당량이 프로세스 버스트보다 훨씬 크면 라운드 로빈은 FCFS와 유사해진다.', '정답. 우선순위 스케줄링은 에이징 같은 보완책이 없으면 기아 상태가 발생할 수 있다.']}
/>

<Quiz
  questionNumber={8}
  question="스케줄러가 선택한 프로세스에게 실제로 CPU 제어권을 넘겨주는 역할을 담당하는 운영체제 구성 요소는?"
  options={['준비 큐(ready queue)', '디스패처(dispatcher)', '인터럽트 벡터(interrupt vector)', '세마포어(semaphore)']}
  correctAnswer={2}
  explanation="디스패처는 단기 스케줄러가 다음에 실행할 프로세스를 결정하면, 문맥 교환을 수행하고 사용자 모드로 전환해 실제로 CPU 제어권을 그 프로세스에게 넘기는 역할을 담당한다."
  optionExplanations={['준비 큐는 CPU 배정을 기다리는 프로세스들이 대기하는 자료구조일 뿐 제어권을 넘기지는 않는다.', '정답. 디스패처가 실제 문맥 교환과 제어권 이양을 담당한다.', '인터럽트 벡터는 인터럽트 번호와 처리 루틴 주소를 매핑하는 표다.', '세마포어는 프로세스 동기화를 위한 도구로 스케줄링 제어권 이양과는 관련이 없다.']}
/>

## 동기화와 교착상태 (9–14번)

<Quiz
  questionNumber={9}
  question="세마포어(semaphore)의 P 연산과 V 연산에 대한 설명으로 옳은 것은?"
  options={['P 연산은 세마포어 값을 증가시키고, V 연산은 감소시킨다.', 'P 연산은 세마포어 값을 감소시키며 값이 음수가 되면 그 프로세스를 대기시키고, V 연산은 값을 증가시키며 대기 중인 프로세스를 깨운다.', 'P 연산과 V 연산은 상호배제 없이 여러 프로세스가 동시에 실행해도 안전하다.', '세마포어는 항상 0과 1 두 값만 가질 수 있는 자료형이다.']}
  correctAnswer={2}
  explanation="P 연산(wait)은 세마포어 값을 감소시키고 음수가 되면 해당 프로세스를 대기 상태로 만들며, V 연산(signal)은 값을 증가시키고 대기 중인 프로세스가 있으면 하나를 깨운다."
  optionExplanations={['P와 V의 증가·감소 방향이 반대로 서술되었다.', '정답. P는 감소·대기, V는 증가·깨움이라는 정확한 동작이다.', 'P·V 연산 자체는 반드시 원자적(상호배제가 보장된 상태)으로 실행되어야 한다.', '0과 1만 갖는 것은 이진 세마포어(binary semaphore)이며, 계수 세마포어(counting semaphore)는 더 넓은 범위의 정수 값을 가질 수 있다.']}
/>

<Quiz
  questionNumber={10}
  question="임계구역 문제의 해결책이 만족해야 할 조건에 대한 설명으로 옳지 않은 것은?"
  options={['상호배제: 한 프로세스가 임계구역을 실행 중이면 다른 프로세스는 그 임계구역에 들어갈 수 없다.', '진행: 임계구역이 비어 있을 때 들어가려는 프로세스가 있다면, 그중 하나는 무한정 배제되어서는 안 된다.', '유한 대기: 한 프로세스가 임계구역 진입을 요청한 뒤 다른 프로세스들이 먼저 임계구역에 들어가는 횟수에는 상한이 없어야 한다.', '이 조건들은 프로세스의 실행 속도나 CPU 개수에 대한 특정 가정 없이 성립해야 한다.']}
  correctAnswer={3}
  explanation="유한 대기(bounded waiting) 조건은 한 프로세스가 진입을 요청한 뒤 다른 프로세스가 먼저 들어갈 수 있는 횟수에 상한(한계)이 있어야 한다는 조건이다. 상한이 없어야 한다는 설명은 정반대로 서술된 것이므로 옳지 않다."
  optionExplanations={['옳은 설명이다. 상호배제의 정확한 정의다.', '옳은 설명이다. 진행 조건의 정확한 정의다.', '정답. 유한 대기는 오히려 상한이 있어야 한다는 조건이므로 이 설명은 옳지 않다.', '옳은 설명이다. 세 조건 모두 특정 실행 속도나 하드웨어 가정 없이 성립해야 한다는 일반적 요건이다.']}
/>

<Quiz
  questionNumber={11}
  question="모니터(monitor)에 대한 설명으로 가장 적절한 것은?"
  options={['모니터는 조건변수 없이도 임계구역 밖에서 대기 상태를 관리할 수 있는 하드웨어 장치다.', '모니터는 공유 자원과 이를 다루는 프로시저를 하나의 모듈로 묶고, 내부적으로 상호배제를 자동으로 보장하는 고수준 동기화 도구다.', '모니터는 세마포어보다 더 하위 수준에서 동작하는 동기화 도구다.', '모니터 내부의 프로시저는 여러 프로세스가 동시에 실행할 수 있도록 설계되어 있다.']}
  correctAnswer={2}
  explanation="모니터는 공유 데이터와 그 데이터를 다루는 프로시저(연산)를 하나의 캡슐로 묶어, 한 번에 한 프로세스만 모니터 내부 프로시저를 실행하도록 컴파일러나 언어 차원에서 상호배제를 자동으로 보장하는 고수준 동기화 도구다."
  optionExplanations={['모니터는 소프트웨어(언어 차원)의 동기화 도구이며, 조건변수는 대기·신호를 위해 함께 쓰인다.', '정답. 모니터의 핵심 정의를 정확히 서술했다.', '모니터는 세마포어보다 사용하기 쉬운 고수준 도구로 취급된다.', '모니터의 핵심은 내부 프로시저를 한 번에 하나의 프로세스만 실행하도록 강제하는 것이다.']}
/>

<Quiz
  questionNumber={12}
  question="교착상태(deadlock) 발생의 4가지 필요조건에 해당하지 않는 것은?"
  options={['상호배제', '점유와 대기', '선점', '순환 대기']}
  correctAnswer={3}
  explanation="교착상태의 4가지 필요조건은 상호배제, 점유와 대기, 비선점(자원을 강제로 뺏을 수 없음), 순환 대기다. '선점'이 아니라 그 반대인 '비선점'이 필요조건이므로, 선점은 4조건에 해당하지 않는다."
  optionExplanations={['상호배제는 4조건 중 하나다.', '점유와 대기 역시 4조건 중 하나다.', '정답. 4조건에 해당하는 것은 선점이 아니라 비선점이다.', '순환 대기도 4조건 중 하나다.']}
/>

<Quiz
  questionNumber={13}
  question="자원 A, B, C에 대해 5개 프로세스 P0~P4의 현재 할당(Allocation)과 최대 요구량(Max)이 다음과 같고, 시스템의 가용 자원(Available)이 (3, 3, 2)일 때, 이 상태를 안전 상태로 만드는 안전 순서로 옳은 것은? (Need = Max − Allocation이며, P0 Allocation(0,1,0)/Max(7,5,3), P1 Allocation(2,0,0)/Max(3,2,2), P2 Allocation(3,0,2)/Max(9,0,2), P3 Allocation(2,1,1)/Max(2,2,2), P4 Allocation(0,0,2)/Max(4,3,3))"
  options={['P0 → P1 → P2 → P3 → P4', 'P1 → P3 → P4 → P0 → P2', 'P2 → P1 → P0 → P3 → P4', 'P4 → P2 → P1 → P3 → P0']}
  correctAnswer={2}
  explanation="각 프로세스의 Need는 P0(7,4,3), P1(1,2,2), P2(6,0,0), P3(0,1,1), P4(4,3,1)이다. Available(3,3,2)에서 Need가 가장 먼저 만족되는 P1을 실행해 자원을 반납하면 Available은 (5,3,2)가 되고, 그다음 P3의 Need(0,1,1)가 만족되어 반납 후 (7,4,3)이 되며, 이어서 P4의 Need(4,3,1)가 만족되어 반납 후 (7,4,5), P0의 Need(7,4,3)가 만족되어 반납 후 (7,5,5), 마지막으로 P2의 Need(6,0,0)가 만족되어 모든 프로세스가 완료된다. 따라서 P1→P3→P4→P0→P2 순서가 안전 순서다."
  optionExplanations={['P0의 Need(7,4,3)는 초기 Available(3,3,2)보다 크므로 가장 먼저 실행할 수 없다.', '정답. Need를 만족하는 순서대로 자원을 반납해 나가면 이 순서가 성립한다.', 'P2의 Need(6,0,0) 중 첫 번째 값 6이 초기 Available의 3보다 크므로 가장 먼저 실행할 수 없다.', 'P4의 Need(4,3,1) 중 첫 번째 값 4가 초기 Available의 3보다 커서 가장 먼저 실행할 수 없다.']}
/>

<Quiz
  questionNumber={14}
  question="교착상태 복구(recovery)에 대한 설명으로 옳지 않은 것은?"
  options={['교착상태에 연루된 프로세스를 모두 강제 종료하는 방법이 있다.', '교착상태에 연루된 프로세스를 하나씩 종료하며 교착상태가 풀렸는지 확인하는 방법이 있다.', '한 프로세스가 점유한 자원을 선점해 다른 프로세스에게 임시로 할당한 뒤 나중에 되돌리는 방법이 있다.', '교착상태 복구는 자원을 요청하기 이전 시점에 미리 개입해 문제를 막는 예방(prevention) 기법과 같은 시점에 이루어진다.']}
  correctAnswer={4}
  explanation="복구는 교착상태가 이미 발생했다는 것이 탐지(detection)된 이후에 이루어지는 사후 조치이며, 자원 요청 이전에 개입하는 예방과는 적용 시점 자체가 다르다."
  optionExplanations={['옳은 설명이다. 프로세스 종료는 대표적인 복구 방법이다.', '옳은 설명이다. 하나씩 종료하며 확인하는 점진적 방법도 실제로 쓰인다.', '옳은 설명이다. 자원 선점 후 되돌리는 방법도 복구 기법의 하나다.', '정답. 복구는 교착상태 발생 이후의 사후 조치로, 사전 조치인 예방과 시점이 다르다.']}
/>

## 메모리 관리와 페이지 교체 (15–20번)

<Quiz
  questionNumber={15}
  question="논리 주소와 물리 주소에 대한 설명으로 옳은 것은?"
  options={['논리 주소는 CPU가 생성하는 주소로, MMU(메모리 관리 장치)를 거쳐 물리 주소로 변환된다.', '물리 주소는 프로그램 내부에서 사용하는 상대적인 주소를 가리킨다.', '논리 주소와 물리 주소는 항상 같은 값을 가진다.', 'MMU는 물리 주소를 논리 주소로 변환하는 장치다.']}
  correctAnswer={1}
  explanation="CPU가 실행 중 생성하는 주소는 논리 주소(가상 주소)이며, 이는 MMU를 거쳐 실제 메모리 상의 위치인 물리 주소로 변환된다."
  optionExplanations={['정답. 논리 주소 생성과 MMU에 의한 변환 과정을 정확히 서술했다.', '프로그램 내부의 상대적 주소는 논리 주소에 대한 설명이며, 물리 주소가 아니다.', '두 주소는 재배치(relocation) 과정을 거치므로 값이 다를 수 있다.', 'MMU는 반대로 논리 주소를 물리 주소로 변환하는 장치다.']}
/>

<Quiz
  questionNumber={16}
  question="페이징과 세그먼테이션에 대한 설명으로 옳지 않은 것은?"
  options={['페이징은 고정 크기 블록으로 메모리를 나누어 내부 단편화가 발생할 수 있다.', '세그먼테이션은 논리적 의미 단위로 메모리를 나누어 외부 단편화가 발생할 수 있다.', '페이지 테이블은 논리 주소의 페이지 번호를 프레임 번호로 매핑하는 자료구조다.', '세그먼테이션은 내부 단편화만 발생시킬 뿐 외부 단편화는 전혀 발생시키지 않는다.']}
  correctAnswer={4}
  explanation="세그먼테이션은 가변 크기의 논리적 단위로 메모리를 나누기 때문에 세그먼트들 사이에 사용하지 못하는 자잘한 빈 공간, 즉 외부 단편화가 발생할 수 있다. 내부 단편화만 발생시킨다는 설명은 사실과 반대다."
  optionExplanations={['옳은 설명이다. 고정 크기 페이지의 마지막 부분이 다 채워지지 않으면 내부 단편화가 생긴다.', '옳은 설명이다. 세그먼테이션의 대표적인 단점이다.', '옳은 설명이다. 페이지 테이블의 정확한 정의다.', '정답. 세그먼테이션은 외부 단편화가 발생할 수 있는 방식이므로 이 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={17}
  question="TLB(Translation Lookaside Buffer)에 대한 설명으로 가장 적절한 것은?"
  options={['TLB는 디스크에 저장된 페이지 테이블 전체의 복사본이다.', 'TLB는 최근 사용된 페이지 번호와 프레임 번호의 매핑을 캐시해 주소 변환 속도를 높인다.', 'TLB는 페이지 부재가 발생했을 때만 참조되는 저장 장치다.', 'TLB는 세그먼트 테이블을 완전히 대체하는 저장 장치다.']}
  correctAnswer={2}
  explanation="TLB는 CPU 근처에 있는 작고 빠른 캐시로, 최근 사용된 페이지 번호와 프레임 번호의 매핑 일부를 저장해 두어, 매번 메인 메모리의 페이지 테이블까지 접근하지 않고도 주소 변환을 빠르게 수행할 수 있게 한다."
  optionExplanations={['TLB는 디스크가 아니라 CPU에 가까운 고속 캐시 메모리에 위치한다.', '정답. TLB의 핵심 역할은 주소 변환을 빠르게 하는 캐싱이다.', 'TLB는 페이지 부재와 무관하게 모든 주소 변환 시도에서 먼저 참조된다.', '세그먼트 테이블은 세그먼테이션 기법의 자료구조로 TLB와는 별개의 개념이다.']}
/>

<Quiz
  questionNumber={18}
  question="페이지 참조열 7, 0, 1, 2, 0, 3, 0, 4, 2, 3, 0, 3, 2를 프레임 3개, FIFO 교체 알고리즘으로 처리할 때 총 페이지 부재 횟수는? (초기 프레임은 모두 비어 있다)"
  options={['9회', '10회', '11회', '12회']}
  correctAnswer={2}
  explanation="순서대로 처리하면 7, 0, 1이 차례로 적재되어 부재 3회, 2가 들어오며 가장 먼저 들어온 7을 교체해 부재 4회(프레임 0,1,2), 이어지는 0은 이미 있어 적중, 3이 들어오며 0을 교체해 부재 5회(프레임 1,2,3), 다시 0이 들어오며 1을 교체해 부재 6회(프레임 2,3,0), 4가 들어오며 2를 교체해 부재 7회(프레임 3,0,4), 2가 들어오며 3을 교체해 부재 8회(프레임 0,4,2), 3이 들어오며 0을 교체해 부재 9회(프레임 4,2,3), 0이 들어오며 4를 교체해 부재 10회(프레임 2,3,0), 마지막 3과 2는 이미 프레임에 있어 적중이다. 따라서 총 부재 횟수는 10회다."
  optionExplanations={['적중으로 처리해야 할 참조를 부재로 잘못 세면 나오는 값이다.', '정답. 단계별로 세면 부재가 정확히 10회 발생한다.', '이미 프레임에 있는 참조 중 하나를 부재로 잘못 계산했을 때 나오는 값이다.', '초기 3회 적재를 이중으로 계산하는 등의 실수를 하면 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={19}
  question="같은 참조열(7, 0, 1, 2, 0, 3, 0, 4, 2, 3, 0, 3, 2)을 프레임 3개, LRU 교체 알고리즘으로 처리할 때 총 페이지 부재 횟수는?"
  options={['8회', '9회', '10회', '11회']}
  correctAnswer={2}
  explanation="7, 0, 1이 적재되어 부재 3회, 2가 들어오며 가장 오래전에 쓰인 7을 교체해 부재 4회(프레임 0,1,2), 0은 적중, 3이 들어오며 가장 오래전에 쓰인 1을 교체해 부재 5회(프레임 0,2,3), 0은 다시 적중, 4가 들어오며 가장 오래전에 쓰인 2를 교체해 부재 6회(프레임 0,3,4), 2가 들어오며 가장 오래전에 쓰인 3을 교체해 부재 7회(프레임 0,4,2), 3이 들어오며 가장 오래전에 쓰인 0을 교체해 부재 8회(프레임 4,2,3), 0이 들어오며 가장 오래전에 쓰인 4를 교체해 부재 9회(프레임 2,3,0), 마지막 3과 2는 적중이다. 따라서 총 부재 횟수는 9회다."
  optionExplanations={['적중을 부재로 잘못 세면 나오는 값이다.', '정답. 단계별로 세면 LRU의 부재 횟수는 9회다.', '이는 같은 참조열을 FIFO로 처리했을 때의 부재 횟수다.', '적중 참조 두 개를 부재로 잘못 계산했을 때 나오는 값이다.']}
/>

<Quiz
  questionNumber={20}
  question="문항 18, 19의 참조열과 동일한 조건에서 OPT(최적 교체) 알고리즘을 적용하면 부재 횟수는 7회로 계산된다. FIFO(10회), LRU(9회), OPT(7회) 세 결과의 관계에 대한 설명으로 옳은 것은?"
  options={['OPT는 미래의 참조를 미리 알아야 적용할 수 있어 실제 구현은 불가능하지만, 다른 알고리즘의 성능을 비교하는 이론적 하한선으로 쓰인다.', '이 예시에서 FIFO가 세 알고리즘 중 부재 횟수가 가장 적어 가장 우수하다.', 'OPT가 세 알고리즘 중 부재 횟수가 가장 많아 가장 비효율적이다.', 'LRU는 어떤 참조열에서도 항상 FIFO보다 부재 횟수가 많다.']}
  correctAnswer={1}
  explanation="OPT는 앞으로 가장 오랫동안 사용되지 않을 페이지를 교체해야 하므로, 이를 위해서는 미래의 참조 순서를 미리 알아야 한다. 현실에서는 미래를 알 수 없어 그대로 구현할 수 없지만, 이론상 가능한 최소 부재 횟수를 보여 주므로 다른 알고리즘의 성능을 평가하는 기준선으로 쓰인다."
  optionExplanations={['정답. OPT의 비현실성과 이론적 의의를 정확히 설명했다.', '이 예시에서는 FIFO(10회)가 세 알고리즘 중 부재 횟수가 가장 많다.', 'OPT(7회)는 세 알고리즘 중 부재 횟수가 가장 적다.', '이 예시에서는 LRU(9회)가 FIFO(10회)보다 부재 횟수가 적으므로 항상 많다는 설명은 옳지 않다.']}
/>

## 파일 시스템·입출력·보호와 보안 (21–25번)

<Quiz
  questionNumber={21}
  question="은행원 알고리즘(Banker's algorithm)에 대한 설명으로 옳지 않은 것은?"
  options={['자원 요청이 들어왔을 때 시스템이 안전 상태를 유지할 수 있는지 미리 검사하는 교착상태 회피 기법이다.', '이 알고리즘을 적용하려면 각 프로세스의 최대 요구량(Max)을 미리 알고 있어야 한다.', '안전 순서가 하나라도 존재하면 안전 상태이며, 안전 순서가 없다고 해서 곧바로 교착상태가 발생했다는 뜻은 아니다.', '은행원 알고리즘은 교착상태가 이미 발생한 뒤 이를 찾아내는 탐지 기법이다.']}
  correctAnswer={4}
  explanation="은행원 알고리즘은 자원을 실제로 배분하기 전에 안전 상태 유지 여부를 검사해 교착상태 자체가 생기지 않도록 막는 회피(avoidance) 기법이다. 교착상태가 이미 발생한 후 찾아내는 탐지(detection) 기법이 아니다."
  optionExplanations={['옳은 설명이다. 회피 기법의 정확한 정의다.', '옳은 설명이다. Max를 미리 알아야 한다는 것이 은행원 알고리즘의 전제 조건이다.', '옳은 설명이다. 안전 순서 부재와 교착상태 발생은 다른 개념이다.', '정답. 은행원 알고리즘은 사전 회피 기법이지 사후 탐지 기법이 아니다.']}
/>

<Quiz
  questionNumber={22}
  question="파일 시스템에 대한 설명으로 옳지 않은 것은?"
  options={['연속 할당은 디스크에 사용하지 못하는 자잘한 빈 공간이 남는 외부 단편화가 발생할 수 있다.', '색인 할당은 색인 블록을 먼저 읽어 위치를 확인한 뒤 목표 블록에 접근한다.', 'FCB(파일 제어 블록)는 파일의 접근 권한과 저장 위치 같은 정보를 담는다.', '디렉터리 구조가 트리 형태이면 상대 경로만으로 현재 작업 디렉터리에 대한 정보 없이도 모든 파일에 접근할 수 있다.']}
  correctAnswer={4}
  explanation="상대 경로는 현재 작업 디렉터리를 기준으로 해석되므로, 현재 작업 디렉터리가 무엇인지에 대한 정보 없이는 상대 경로만으로 파일의 실제 위치를 특정할 수 없다."
  optionExplanations={['옳은 설명이다. 연속 할당의 대표적인 단점이다.', '옳은 설명이다. 색인 할당의 2단계 접근 방식이다.', '옳은 설명이다. FCB에 담기는 대표적인 정보다.', '정답. 상대 경로는 현재 작업 디렉터리를 전제로 해석되므로 이 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={23}
  question="디스크 헤드가 50번 트랙에 있고, 요청 큐가 60, 10, 90, 30 순서로 도착했다. FCFS 방식으로 처리할 때 헤드의 총 이동 거리는?"
  options={['150', '180', '200', '220']}
  correctAnswer={3}
  explanation="FCFS는 도착 순서 그대로 처리하므로 50에서 60까지 10, 60에서 10까지 50, 10에서 90까지 80, 90에서 30까지 60을 이동한다. 이를 모두 더하면 10+50+80+60=200이다."
  optionExplanations={['중간 구간 하나를 빠뜨리고 더했을 때 나오는 값이다.', '한 구간의 거리를 실제보다 작게 계산했을 때 나오는 값이다.', '정답. 10+50+80+60=200이다.', '한 구간의 거리를 실제보다 크게 계산했을 때 나오는 값이다.']}
/>

<Quiz
  questionNumber={24}
  question="입출력 기법에 대한 설명으로 옳은 것은?"
  options={['프로그램된 I/O는 CPU가 장치의 상태를 반복적으로 확인하며 완료를 기다리는 방식이다.', 'DMA는 CPU 레지스터를 거쳐 데이터를 한 바이트씩 전송하는 방식이다.', '스풀링은 입출력 속도 차이를 완충하기 위해 버퍼 두 개를 번갈아 쓰는 기법이다.', '인터럽트 기반 I/O는 CPU가 장치의 상태를 계속 폴링하는 방식이다.']}
  correctAnswer={1}
  explanation="프로그램된 I/O(폴링)는 CPU가 장치의 상태 레지스터를 반복적으로 확인하며 완료될 때까지 바쁜 대기 상태로 기다리는 방식이다."
  optionExplanations={['정답. 프로그램된 I/O의 정확한 정의다.', 'DMA는 CPU를 거치지 않고 DMA 컨트롤러가 메모리와 장치 사이의 데이터를 직접 전송하는 방식이다.', '두 버퍼를 번갈아 쓰는 것은 이중 버퍼링에 대한 설명이며, 스풀링은 비공유 장치의 출력 요청을 큐로 관리하는 기법이다.', '계속 폴링하는 방식은 프로그램된 I/O이며, 인터럽트 기반 I/O는 장치가 완료를 알릴 때만 CPU가 개입한다.']}
/>

<Quiz
  questionNumber={25}
  question="보호와 보안에 대한 설명으로 옳지 않은 것은?"
  options={['접근 제어 목록(ACL)은 객체를 기준으로 그 객체에 접근 가능한 주체와 권한을 정리한 방식이다.', '인증은 신원을 확인하는 절차이고, 권한 부여는 확인된 신원에 대해 실제 접근 가능 범위를 결정하는 절차다.', '웜(worm)은 다른 정상 프로그램에 기생해야만 실행되고 확산될 수 있는 악성 프로그램이다.', '유닉스 계열의 파일 권한 표기는 소유자, 그룹, 기타 순서로 각각 읽기·쓰기·실행 권한을 나타낸다.']}
  correctAnswer={3}
  explanation="다른 정상 프로그램에 기생해야만 실행·확산되는 것은 바이러스(virus)의 특징이다. 웜(worm)은 숙주 프로그램 없이 독립적으로 실행되며 스스로 네트워크를 통해 확산된다."
  optionExplanations={['옳은 설명이다. ACL은 객체 기준으로 정리된 접근 제어 방식이다.', '옳은 설명이다. 인증과 권한 부여를 정확히 구분했다.', '정답. 숙주 기생은 바이러스의 특징이며, 웜은 독립적으로 실행·확산된다.', '옳은 설명이다. 유닉스 권한 비트의 순서와 구성에 대한 정확한 설명이다.']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
- [국가평생교육진흥원 독학학위제 시험안내](https://bdes.nile.or.kr/nile/exam/nExam2_1.do)
