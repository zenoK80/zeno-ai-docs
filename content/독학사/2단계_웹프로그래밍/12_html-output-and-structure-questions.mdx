---
title: '12. HTML 개념 확인 문제'
description: '문서 구조 판별, 태그 용도 구분, 표·폼 기본 개념, 시맨틱 태그 비교를 코드와 함께 재구성한 유형 문제 20문항'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 문제집은 국가평생교육진흥원 독학학위제 2단계 웹 프로그래밍 출제기준(HTML 문서 구조·주요 태그·표/폼·시맨틱 영역)의 평가 범위와 문항 유형을 분석해 새로 구성한 **재구성 문항**입니다. 실제 기출 문항을 그대로 옮긴 것이 아닙니다. 회차별 정확한 문항 수·시험 시간은 시행처 공고를 확인해야 하며, 여기서는 20문항을 표준 분량으로 구성했습니다.
</Callout>

## 출제 범위와 문항 배분

| 영역 | 문항 수 | 문항 번호 | 관련 편 |
| --- | --- | --- | --- |
| 문서 구조 판별 | 5 | 1–5 | 03 |
| 태그 용도 구분 | 5 | 6–10 | 03, 04 |
| 표·폼 기본 개념 | 6 | 11–16 | 05 |
| 시맨틱 태그 비교 | 4 | 17–20 | 04, 11 |

코드가 있는 문항은 먼저 눈으로 태그를 하나씩 짚어가며 "이 문서가 브라우저에 어떻게 그려질까"를 그려본 뒤 정답을 확인하세요. 독학사 시험은 태그를 실제로 그려보라는 실기 문제가 아니라, 태그의 **의미·계층·용도**를 정확히 짝지었는지를 묻는 문항이 대부분입니다.

## 문서 구조 판별 (1–5)

### 문제 1

```html
<!DOCTYPE html>
<html>
  <head>
    <title>연습 문서</title>
  </head>
  <body>
    <p>안녕하세요</p>
  </body>
</html>
```

<Quiz
  questionNumber={1}
  question="위 문서의 첫 줄에 있는 선언의 역할로 가장 적절한 것은?"
  options={['브라우저에게 이 문서를 최신 HTML 표준 모드로 해석하라고 알려준다.', '문서의 저작권 정보를 브라우저에 전달한다.', '문서에 사용된 프로그래밍 언어의 버전을 서버에 알려준다.', '문서를 압축된 형태로 전송하라고 서버에 요청한다.']}
  correctAnswer={1}
  explanation="문서 맨 앞의 선언은 문서 형식 선언(document type declaration)으로, 브라우저가 이 문서를 표준 준수 모드로 해석하도록 지시하는 역할을 한다. 이 선언이 없거나 잘못되면 브라우저가 과거 방식의 호환 모드로 전환해 레이아웃이 예상과 달라질 수 있다."
  optionExplanations={['표준 모드 렌더링을 지시하는 선언이라는 설명이 정확하므로 정답이다.', '저작권 정보는 이 선언과 무관하며 별도의 메타데이터나 문서 밖 정보로 다룬다.', '이 선언은 서버로 전송되는 정보가 아니라 브라우저의 렌더링 방식을 결정하는 지시문이다.', '압축 전송 여부는 서버와 브라우저 간 별도의 헤더로 협상되며 이 선언과 무관하다.']}
/>

### 문제 2

<Quiz
  questionNumber={2}
  question="HTML 문서에서 head 요소 안에 넣는 내용으로 가장 적절한 것은?"
  options={['문서 제목, 문자 인코딩, 외부 스타일시트 연결처럼 화면에 직접 보이지 않는 메타 정보', '사용자에게 보여줄 문단과 이미지', '사용자가 클릭하는 버튼과 링크 목록', '문서의 각주와 참고 문헌 목록']}
  correctAnswer={1}
  explanation="head 요소는 문서 자체에 대한 정보(메타데이터)를 담는 자리다. 제목, 문자 인코딩 선언, 외부 스타일시트·스크립트 연결 정보 등이 여기에 들어가며, 이 내용은 화면에 직접 렌더링되지 않는다. 실제로 화면에 보이는 콘텐츠는 body 요소 안에 작성한다."
  optionExplanations={['메타 정보를 담는다는 설명이 head 요소의 정의와 정확히 일치하므로 정답이다.', '화면에 보이는 문단·이미지는 body 안에 작성하는 내용이다.', '버튼·링크 같은 상호작용 요소도 body 안에 들어가는 내용이다.', '각주·참고 문헌도 문서의 실제 콘텐츠이므로 body 안에 작성한다.']}
/>

### 문제 3

```html
<!DOCTYPE html>
<html>
  <body>
    <head>
      <title>순서 실험</title>
    </head>
    <p>본문</p>
  </body>
</html>
```

<Quiz
  questionNumber={3}
  question="위 문서처럼 head를 body 안쪽에 넣으면 어떤 일이 벌어지는가?"
  options={['브라우저는 대부분 이를 보정해서 렌더링을 시도하지만, 표준 문서 계층을 어긴 잘못된 구조다.', '문서가 두 배로 빠르게 로딩된다.', 'title에 지정한 문자열이 두 번 화면에 출력된다.', '어떤 브라우저에서도 완전히 빈 화면만 나온다.']}
  correctAnswer={1}
  explanation="html 요소 아래에는 head와 body가 형제 관계로, head가 먼저 오는 것이 표준 구조다. 브라우저는 오류 복구력이 강해 이런 문서도 최대한 보정해서 그려주지만, 이는 표준을 어긴 잘못된 마크업이며 시험에서는 옳은 구조로 인정되지 않는다."
  optionExplanations={['브라우저가 보정을 시도하되 구조 자체는 잘못되었다는 설명이 가장 정확하므로 정답이다.', '태그의 배치 순서는 로딩 속도와 직접적인 인과관계가 없다.', 'title은 문서 제목 표시줄에 한 번 반영되는 정보이지 본문에 반복 출력되는 요소가 아니다.', '브라우저의 오류 보정 덕분에 완전히 빈 화면이 되는 경우는 드물다.']}
/>

### 문제 4

<Quiz
  questionNumber={4}
  question="HTML에서 주석을 작성하는 방법으로 옳은 것은?"
  options={['느낌표와 대시 두 개로 시작해서 대시 두 개로 닫는 형식을 사용한다.', '슬래시 두 개로 한 줄 전체를 주석 처리한다.', '샵 기호로 시작하는 줄은 모두 주석으로 처리된다.', '별표 두 개로 감싼 문자열은 주석으로 처리된다.']}
  correctAnswer={1}
  explanation="HTML 주석은 여는 기호가 부등호·느낌표·대시·대시(모양은 코드 스팬 참고: 다음 편에서 다룬다) 형태이고, 닫는 기호는 대시·대시·부등호 형태다. 이 사이에 있는 내용은 브라우저에 렌더링되지 않으며 문서를 읽는 사람을 위한 설명으로만 남는다."
  optionExplanations={['느낌표와 대시로 시작해 대시 두 개로 닫는 형식이 HTML 주석의 정확한 문법이므로 정답이다.', '슬래시 두 개로 시작하는 방식은 JavaScript·CSS 계열의 한 줄 주석 방식이지 HTML 주석이 아니다.', '샵 기호로 시작하는 줄 주석은 셸 스크립트나 파이썬 계열의 규칙이며 HTML과 무관하다.', '별표 두 개로 감싸는 방식은 마크다운의 굵게 표시 문법이지 HTML 주석 문법이 아니다.']}
/>

### 문제 5

```html
<p>정상가는 100&amp;40원입니다.</p>
```

<Quiz
  questionNumber={5}
  question="위 코드에서 앰퍼샌드(&)를 그대로 쓰지 않고 특별한 표기로 대체해서 쓴 이유로 가장 적절한 것은?"
  options={['앰퍼샌드는 HTML 문법에서 특수한 의미로 예약된 기호라서 그대로 쓰면 다른 문자 참조의 시작으로 오인될 수 있기 때문이다.', '앰퍼샌드는 한글 인코딩과 충돌해서 문서가 깨지기 때문이다.', '앰퍼샌드를 그대로 쓰면 브라우저가 그 줄 전체를 주석으로 처리하기 때문이다.', '앰퍼샌드는 파일 이름에 쓸 수 없는 문자라서 문서 안에서도 금지되기 때문이다.']}
  correctAnswer={1}
  explanation="HTML에서 앰퍼샌드는 문자 참조(엔티티)를 시작하는 예약 기호다. 화면에 앰퍼샌드 자체를 보여주고 싶다면 그 문자를 가리키는 엔티티 표기로 바꿔 써야 파서가 혼동하지 않는다. 부등호 기호 역시 태그의 시작·끝으로 해석되므로 같은 이유로 엔티티로 바꿔 쓴다."
  optionExplanations={['앰퍼샌드가 문자 참조를 시작하는 예약 기호라는 설명이 정확한 이유이므로 정답이다.', '엔티티 표기를 쓰는 이유는 인코딩 충돌이 아니라 HTML 문법상의 예약 기호 문제다.', 'HTML은 줄 단위 주석 문법이 없으며 앰퍼샌드가 주석을 유발하지도 않는다.', '파일 이름 규칙과 문서 본문 안의 문자 표기 규칙은 서로 다른 별개의 규칙이다.']}
/>

## 태그 용도 구분 (6–10)

### 문제 6

```html
<h1>제목</h1>
<h3>소제목</h3>
```

<Quiz
  questionNumber={6}
  question="위 코드에서 제목 태그 번호를 1에서 3으로 건너뛴 것에 대한 설명으로 가장 적절한 것은?"
  options={['화면에는 문제없이 보일 수 있지만 문서의 논리적 제목 계층 구조를 건너뛰는 것이므로 구조적으로는 바람직하지 않다.', '브라우저가 이를 문법 오류로 판단해 문서 전체를 렌더링하지 않는다.', '건너뛴 숫자만큼 자동으로 빈 제목이 삽입된다.', '제목 태그의 번호는 화면에 보이는 글자 크기만 결정할 뿐 아무 의미가 없다.']}
  correctAnswer={1}
  explanation="제목 태그의 숫자는 문서의 논리적 계층(개요 구조)을 나타낸다. 브라우저는 문법 오류로 처리하지 않고 화면에도 표시하지만, 스크린 리더 등 보조 기술이나 문서 구조를 분석하는 도구 입장에서는 계층을 건너뛴 문서로 인식되어 접근성과 구조적 일관성 면에서 바람직하지 않다."
  optionExplanations={['화면 렌더링은 되지만 논리적 계층을 건너뛴 구조라는 설명이 정확하므로 정답이다.', '제목 번호를 건너뛰는 것은 문법 오류가 아니므로 렌더링이 막히지 않는다.', '건너뛴 번호만큼 빈 제목이 자동으로 채워지는 동작은 없다.', '제목 태그 번호는 기본적으로 글자 크기에도 영향을 주지만, 그보다 먼저 문서의 논리적 계층을 나타내는 의미를 갖는다.']}
/>

### 문제 7

```html
<ul>
  <li>첫 항목</li>
  <li>둘째 항목</li>
</ul>
<ol>
  <li>첫 단계</li>
  <li>둘째 단계</li>
</ol>
```

<Quiz
  questionNumber={7}
  question="위 두 목록의 차이로 가장 적절한 것은?"
  options={['위쪽은 순서가 중요하지 않은 목록, 아래쪽은 순서가 있는 목록으로 브라우저가 기본적으로 번호를 매겨 표시한다.', '두 목록은 화면에 동일하게 표시되며 이름만 다를 뿐 기능 차이는 없다.', '위쪽 목록은 최대 두 개 항목만 담을 수 있고 아래쪽은 제한이 없다.', '아래쪽 목록은 항목을 클릭해야만 내용이 보이는 접이식 목록이다.']}
  correctAnswer={1}
  explanation="순서 없는 목록은 각 항목 앞에 기본적으로 불릿 기호가 붙고, 순서 있는 목록은 항목 앞에 브라우저가 자동으로 1, 2, 3처럼 번호를 매긴다. 두 태그 모두 안에는 목록 항목 태그로 각 항목을 감싼다는 점은 같지만, 항목의 순서 정보를 표현하느냐가 다르다."
  optionExplanations={['순서 없는 목록과 순서 있는 목록의 렌더링 차이를 정확히 설명하므로 정답이다.', '두 태그는 기본 스타일(불릿 대 번호)이 다르게 렌더링되므로 동일하지 않다.', '두 목록 모두 담을 수 있는 항목 수에 태그 자체의 제한은 없다.', '접이식 동작은 별도의 상호작용 요소나 스크립트로 구현하는 기능이며 순서 있는 목록 태그 자체의 기본 동작이 아니다.']}
/>

### 문제 8

```html
<a href="https://example.com">예시 사이트</a>
```

<Quiz
  questionNumber={8}
  question="위 코드에서 속성에 지정한 값의 역할로 옳은 것은?"
  options={['링크를 클릭했을 때 이동할 대상 주소를 지정한다.', '링크 글자의 색상을 지정한다.', '링크가 새 탭에서 열릴지 여부를 지정한다.', '링크에 마우스를 올렸을 때 나타날 설명 문구를 지정한다.']}
  correctAnswer={1}
  explanation="해당 속성은 링크가 가리키는 목적지 주소(hyperlink reference)를 지정하는 필수 속성이다. 이 값이 없으면 링크 태그는 클릭해도 아무 동작을 하지 않는 텍스트로만 남는다. 새 탭 여부는 다른 속성이, 색상은 CSS가, 툴팁 문구는 또 다른 속성이 담당한다."
  optionExplanations={['이동할 목적지 주소를 지정하는 속성이라는 설명이 정확하므로 정답이다.', '글자 색상은 CSS로 지정하는 것이 표준 방식이며 이 속성의 역할이 아니다.', '새 탭에서 열지 여부는 target이라는 별도의 속성이 담당한다.', '마우스를 올렸을 때 나타나는 설명 문구는 title이라는 별도의 속성이 담당한다.']}
/>

### 문제 9

```html
<p>이 문장은 <strong>중요</strong>하고, 이 문장은 <em>강조</em>합니다.</p>
```

<Quiz
  questionNumber={9}
  question="위 코드에서 두 태그의 의미 차이로 가장 적절한 것은?"
  options={['앞의 태그는 내용의 중요성을 나타내고, 뒤의 태그는 어조상의 강조를 나타내며 둘 다 의미론적 태그다.', '두 태그는 색상만 다를 뿐 완전히 같은 의미를 가진다.', '앞의 태그는 글자를 기울이고, 뒤의 태그는 글자를 굵게 만든다.', '두 태그는 시각 장애인을 위한 보조 기술에서는 아무 차이 없이 동일하게 무시된다.']}
  correctAnswer={1}
  explanation="두 태그 모두 단순히 모양만 바꾸는 것이 아니라 의미를 전달하는 시맨틱 태그다. 앞의 태그는 내용이 중요하다는 것을 나타내고(브라우저는 보통 굵게 렌더링), 뒤의 태그는 문장 안에서 어조상 강조되는 부분을 나타낸다(브라우저는 보통 기울임으로 렌더링). 두 태그 모두 스크린 리더가 억양이나 강세로 구분해 읽어줄 수 있다."
  optionExplanations={['중요성과 어조 강조라는 의미 차이를 정확히 설명하므로 정답이다.', '두 태그는 기본 렌더링 스타일도 다르고 전달하는 의미도 다르다.', '기본 렌더링 스타일이 앞뒤가 바뀌어 서술되어 있어 옳지 않다.', '의미론적 태그이므로 스크린 리더가 이를 활용해 읽는 방식을 달리할 수 있다.']}
/>

### 문제 10

<Quiz
  questionNumber={10}
  question="이미지 삽입 태그에서 대체 텍스트를 지정하는 속성의 역할로 가장 적절한 것은?"
  options={['이미지를 불러오지 못했을 때 대신 표시할 설명이자 스크린 리더가 읽어줄 텍스트를 제공한다.', '이미지의 실제 파일 용량을 지정한다.', '이미지를 클릭했을 때 이동할 주소를 지정한다.', '이미지의 가로세로 비율을 강제로 고정한다.']}
  correctAnswer={1}
  explanation="이미지 태그의 대체 텍스트 속성은 이미지가 로드되지 않는 상황(네트워크 문제, 경로 오류 등)에서 사용자에게 어떤 내용이 있었는지 알려주고, 시각 장애인이 사용하는 스크린 리더가 이미지 대신 읽어주는 텍스트로도 쓰인다. 이는 접근성 측면에서 매우 중요한 속성이다."
  optionExplanations={['이미지 로드 실패 시 대체 표시와 스크린 리더용 설명이라는 두 가지 역할을 정확히 설명하므로 정답이다.', '파일 용량은 이 속성이 아니라 실제 이미지 파일 자체의 속성이다.', '클릭 시 이동 주소는 이미지 태그를 링크 태그로 감쌌을 때 링크 태그의 속성이 담당한다.', '가로세로 비율 고정은 별도의 크기 속성이나 CSS가 담당한다.']}
/>

## 표·폼 기본 개념 (11–16)

### 문제 11

```html
<table>
  <tr>
    <th>이름</th>
    <th>점수</th>
  </tr>
  <tr>
    <td>영희</td>
    <td>90</td>
  </tr>
</table>
```

<Quiz
  questionNumber={11}
  question="위 표에서 첫 번째 행에 쓰인 셀 태그와 두 번째 행에 쓰인 셀 태그의 차이로 옳은 것은?"
  options={['첫 번째 행의 태그는 제목 셀을 나타내 보통 굵고 가운데 정렬로 렌더링되고, 두 번째 행의 태그는 일반 데이터 셀을 나타낸다.', '두 태그는 완전히 동일하며 이름만 다를 뿐이다.', '첫 번째 행의 태그는 표의 테두리 색상을 결정한다.', '두 번째 행의 태그는 표 전체의 열 개수를 결정한다.']}
  correctAnswer={1}
  explanation="제목 셀 태그는 해당 셀이 표의 머리글임을 의미하며 브라우저는 기본적으로 굵은 글씨와 가운데 정렬로 보여준다. 일반 데이터 셀 태그는 실제 값이 담기는 자리다. 두 태그 모두 같은 행 태그 안에서 함께 쓸 수 있으며, 의미와 기본 스타일이 다르다."
  optionExplanations={['제목 셀과 데이터 셀의 의미·기본 스타일 차이를 정확히 설명하므로 정답이다.', '기본 렌더링 스타일이 다르므로 이름만 다른 동일한 태그가 아니다.', '테두리 색상은 CSS로 지정하는 것이며 셀 태그의 종류와는 무관하다.', '열 개수는 각 행에 실제로 몇 개의 셀 태그를 썼는지로 결정되며 특정 셀 태그 하나가 결정하지 않는다.']}
/>

### 문제 12

<Quiz
  questionNumber={12}
  question="폼 안에서 사용자가 입력한 값을 다른 서버로 어떤 방식으로 보낼지 지정하는 속성 두 가지로 가장 적절한 것은?"
  options={['action(보낼 주소)과 method(전송 방식)', 'name(요소 이름)과 id(요소 식별자)', 'placeholder(안내 문구)와 required(필수 여부)', 'width(너비)와 height(높이)']}
  correctAnswer={1}
  explanation="폼 태그는 action 속성으로 데이터를 보낼 목적지 주소를 지정하고, method 속성으로 GET 또는 POST 같은 전송 방식을 지정한다. 이 두 속성이 폼 제출의 핵심 동작을 결정한다."
  optionExplanations={['action과 method가 폼 제출의 목적지와 방식을 결정하는 핵심 속성이므로 정답이다.', 'name과 id는 요소를 식별하는 용도이지 전송 목적지나 방식을 지정하지 않는다.', 'placeholder와 required는 개별 입력 요소의 안내와 검증에 관련된 속성이다.', 'width와 height는 크기를 지정하는 속성으로 폼 전송 방식과 무관하다.']}
/>

### 문제 13

```html
<label for="email">이메일</label>
<input type="email" id="email" name="email" />
```

<Quiz
  questionNumber={13}
  question="위 코드에서 label 태그의 for 속성 값과 input 태그의 id 속성 값을 일치시킨 이유로 가장 적절한 것은?"
  options={['둘을 연결해 라벨 텍스트를 클릭해도 해당 입력란에 초점이 가도록 하고, 스크린 리더가 입력란의 이름표를 정확히 읽게 하기 위해서다.', '두 값이 같아야만 브라우저가 이메일 형식 검증을 수행하기 때문이다.', '두 값을 일치시키지 않으면 폼 전체가 전송되지 않기 때문이다.', '두 값이 같으면 입력란의 배경색이 자동으로 바뀌기 때문이다.']}
  correctAnswer={1}
  explanation="label의 for 속성과 input의 id 속성을 같은 값으로 맞추면 브라우저가 이 둘을 하나의 짝으로 인식한다. 그 결과 라벨 텍스트를 클릭해도 입력란에 초점이 이동하고, 스크린 리더는 입력란에 초점이 갔을 때 연결된 라벨의 텍스트를 함께 읽어줘 어떤 입력란인지 알려준다."
  optionExplanations={['클릭 시 초점 이동과 스크린 리더 연결이라는 두 효과를 정확히 설명하므로 정답이다.', '이메일 형식 검증은 type 속성 값이 email이기 때문에 이루어지는 것이지 for·id 연결과는 무관하다.', 'for와 id가 다르더라도 값 자체는 정상적으로 전송된다.', '입력란 배경색은 CSS로 지정하는 것이며 for·id 일치 여부와는 무관하다.']}
/>

### 문제 14

<Quiz
  questionNumber={14}
  question="입력 요소에 required 속성만 추가했을 때 나타나는 기본 동작으로 옳은 것은?"
  options={['해당 입력란을 비운 채로 폼을 제출하려 하면 브라우저가 기본 제공하는 검증 메시지를 보여주며 제출을 막는다.', '입력란에 아무 값이나 입력해도 항상 오류로 표시된다.', '입력란이 자동으로 숨겨진다.', '입력란에 입력할 수 있는 글자 수가 5자로 제한된다.']}
  correctAnswer={1}
  explanation="required 속성은 브라우저 기본 폼 검증 기능 중 하나로, 해당 입력란이 비어 있는 채로 폼을 제출하려 하면 브라우저가 자체 메시지를 띄우고 제출을 진행하지 않는다. 이는 서버 쪽 검증을 대신하지는 못하지만 기본적인 클라이언트 측 검증 수단이다."
  optionExplanations={['필수 입력 검증과 기본 제출 차단 동작을 정확히 설명하므로 정답이다.', '값이 채워지면 required 조건을 만족하므로 오류로 표시되지 않는다.', 'required는 입력란을 숨기는 속성이 아니다.', '글자 수 제한은 maxlength 같은 별도의 속성이 담당한다.']}
/>

### 문제 15

```html
<video src="movie.mp4" controls></video>
```

<Quiz
  questionNumber={15}
  question="위 코드에서 controls 속성을 뺐을 때 달라지는 점으로 가장 적절한 것은?"
  options={['영상은 그대로 삽입되지만 재생·정지·음량 같은 기본 재생 도구 모음이 화면에 나타나지 않는다.', '영상 파일 자체가 브라우저에서 아예 로드되지 않는다.', '영상이 소리 없이 무한 반복 재생된다.', '영상 태그가 문법 오류로 인식되어 페이지 전체가 표시되지 않는다.']}
  correctAnswer={1}
  explanation="controls 속성은 브라우저가 기본 제공하는 재생·일시정지·음량 조절 같은 사용자 인터페이스를 표시할지 여부를 결정한다. 이 속성이 없어도 영상 요소 자체는 문서에 존재하며 자바스크립트로 직접 제어할 수도 있지만, 사용자가 직접 조작할 기본 도구 모음만 보이지 않게 된다."
  optionExplanations={['재생 도구 모음이 표시되지 않는다는 설명이 controls 속성의 정확한 역할이므로 정답이다.', 'controls 유무와 영상 파일 로드 여부는 별개의 동작이다.', '무한 반복 재생은 loop라는 별도의 속성이 담당하는 동작이다.', 'controls를 생략해도 문법 오류가 아니므로 페이지 표시에는 영향이 없다.']}
/>

### 문제 16

<Quiz
  questionNumber={16}
  question="input 요소의 type 속성 값을 checkbox로 설정했을 때의 기본 동작으로 옳은 것은?"
  options={['사용자가 여러 항목 중 하나 이상을 선택하거나 선택하지 않을 수 있는 사각형 선택 상자가 표시된다.', '여러 옵션 중 반드시 하나만 선택할 수 있는 원형 선택 버튼이 표시된다.', '사용자가 자유롭게 여러 줄의 글을 입력할 수 있는 넓은 입력란이 표시된다.', '파일을 첨부할 수 있는 파일 선택 창이 자동으로 열린다.']}
  correctAnswer={1}
  explanation="type이 checkbox인 입력 요소는 켜기·끄기 두 상태를 가지는 사각형 선택 상자를 표시하며, 같은 이름을 가진 여러 체크박스 중 몇 개든 자유롭게 선택할 수 있다. 이는 하나만 선택 가능한 라디오 버튼(type이 radio)과 대비된다."
  optionExplanations={['자유롭게 선택 가능한 사각형 상자라는 설명이 checkbox의 기본 동작과 일치하므로 정답이다.', '하나만 선택하는 원형 버튼은 type이 radio일 때의 동작이다.', '여러 줄 입력란은 textarea 요소의 역할이지 checkbox의 역할이 아니다.', '파일 선택 창은 type이 file일 때의 동작이다.']}
/>

## 시맨틱 태그 비교 (17–20)

### 문제 17

<Quiz
  questionNumber={17}
  question="시맨틱 태그가 등장하기 전, 문서의 모든 구획을 범용 컨테이너 태그 하나로만 나누어 쓰던 방식의 한계로 가장 적절한 것은?"
  options={['태그 이름만으로는 그 구획이 머리말인지 목차인지 본문인지 구분할 수 없어 검색 엔진이나 스크린 리더가 문서 구조를 이해하기 어렵다.', '범용 컨테이너 태그는 한 문서에 한 번만 사용할 수 있어서 구획을 여러 개 만들 수 없다.', '범용 컨테이너 태그 안에는 다른 태그를 전혀 넣을 수 없다.', '범용 컨테이너 태그를 쓰면 문서가 항상 두 배로 느리게 로딩된다.']}
  correctAnswer={1}
  explanation="범용 컨테이너 태그는 이름 자체에 의미가 없어서, 그 안에 무엇이 들어있는지는 클래스 이름이나 주석을 봐야 짐작할 수 있었다. 시맨틱 태그는 header·nav·main·section·article·aside·footer처럼 이름 자체가 역할을 드러내, 검색 엔진과 스크린 리더가 문서의 뼈대를 기계적으로 이해할 수 있게 한다."
  optionExplanations={['이름만으로 구조를 구분할 수 없다는 한계를 정확히 짚었으므로 정답이다.', '범용 컨테이너 태그는 한 문서에서 몇 번이든 반복해서 쓸 수 있다.', '범용 컨테이너 태그 안에는 다른 어떤 태그도 자유롭게 넣을 수 있다.', '태그 종류 자체가 로딩 속도를 두 배로 만드는 인과관계는 없다.']}
/>

### 문제 18

```html
<article>
  <header>
    <h2>오늘의 소식</h2>
  </header>
  <p>본문 내용</p>
</article>
```

<Quiz
  questionNumber={18}
  question="위 코드에서 article 태그를 사용하기에 적절한 상황으로 가장 적절한 것은?"
  options={['블로그 게시글처럼 그 부분만 떼어내 다른 곳에 옮겨도 독립적으로 의미가 통하는 콘텐츠일 때', '페이지 전체의 내비게이션 메뉴를 감쌀 때', '페이지 맨 아래의 저작권 표시를 감쌀 때', '문서 전체에서 딱 한 번만 등장하는 핵심 본문 영역을 감쌀 때']}
  correctAnswer={1}
  explanation="article 태그는 독립적으로 배포되거나 재사용해도 의미가 통하는 콘텐츠 단위(게시글, 뉴스 기사, 댓글 하나 등)를 감쌀 때 쓴다. 내비게이션은 nav, 하단 정보는 footer, 문서의 주된 단일 콘텐츠 영역은 main이 각각 더 적절한 태그다."
  optionExplanations={['독립적으로도 의미가 통하는 콘텐츠 단위라는 설명이 article의 정의와 정확히 맞으므로 정답이다.', '내비게이션 메뉴는 nav 태그를 쓰는 것이 의미상 더 적절하다.', '저작권 같은 하단 정보는 footer 태그를 쓰는 것이 의미상 더 적절하다.', '문서 전체에서 한 번만 나오는 주 콘텐츠 영역은 main 태그를 쓰는 것이 의미상 더 적절하다.']}
/>

### 문제 19

<Quiz
  questionNumber={19}
  question="nav 태그와 aside 태그의 용도 차이로 가장 적절한 것은?"
  options={['nav는 주요 이동 링크 모음을 감싸고, aside는 본문과 간접적으로만 관련된 보충 정보를 감싼다.', 'nav는 표를 감싸고, aside는 폼을 감싼다.', 'nav와 aside는 완전히 같은 의미이며 서로 바꿔 써도 무방하다.', 'nav는 이미지 갤러리 전용이고, aside는 동영상 전용이다.']}
  correctAnswer={1}
  explanation="nav 태그는 사이트 내 주요 내비게이션(메뉴, 목차 등) 링크 묶음을 나타낸다. aside 태그는 본문의 핵심 흐름과는 직접 관련이 없지만 곁들이면 도움이 되는 보충 정보(관련 글 목록, 광고, 용어 설명 등)를 나타낸다. 두 태그는 담는 내용의 성격이 뚜렷이 다르다."
  optionExplanations={['주요 이동 링크와 보충 정보라는 역할 차이를 정확히 설명하므로 정답이다.', '표나 폼을 감싸는 것은 이 두 태그의 정의와 관련이 없다.', '두 태그는 의미가 다르므로 서로 바꿔 쓰면 문서의 의미가 왜곡된다.', '이미지나 동영상 전용 태그라는 제한은 두 태그 어느 쪽에도 없다.']}
/>

### 문제 20

<Quiz
  questionNumber={20}
  question="한 문서 안에서 header 태그와 footer 태그를 여러 번 사용할 수 있는 경우로 가장 적절한 것은?"
  options={['article이나 section처럼 각각 독립적인 구획 안에서 그 구획만의 머리말·꼬리말로 반복해서 쓸 수 있다.', '문서 전체에서 header와 footer는 각각 정확히 한 번씩만 쓸 수 있다.', 'header와 footer는 body 태그 바로 아래에서만 쓸 수 있고 다른 태그 안에는 넣을 수 없다.', 'header와 footer는 표 안에서만 반복해서 쓸 수 있다.']}
  correctAnswer={1}
  explanation="header와 footer는 문서 전체의 머리말·꼬리말뿐 아니라, article이나 section 같은 개별 구획 안에서도 그 구획만의 머리말·꼬리말로 사용할 수 있다. 따라서 게시글이 여러 개 나열된 페이지라면 게시글마다 header가 반복해서 등장할 수 있다."
  optionExplanations={['구획별로 반복 사용할 수 있다는 설명이 정확하므로 정답이다.', '문서 전체 기준으로는 보통 한 번씩 쓰지만, 구획 안에서는 여러 번 쓸 수 있으므로 이 제한은 옳지 않다.', 'header와 footer는 article·section 같은 구획 태그 안에도 자유롭게 넣을 수 있다.', '표 안에서만 반복 가능하다는 제한은 없다.']}
/>

## 참고 자료

- [WHATWG HTML Standard](https://html.spec.whatwg.org/)
- [MDN Web Docs - HTML](https://developer.mozilla.org/en-US/docs/Web/HTML)
- [국가평생교육진흥원 독학학위제 시험안내](https://bdes.nile.or.kr/nile/exam/nExam2_1.do)
