---
title: '20. 독학사 2단계 컴퓨터구조 예상기출문제'
description: '진법·보수, 부동소수점, 주소지정, 캐시/가상메모리, 인터럽트/DMA, 파이프라인 전 범위를 국가평생교육진흥원 출제기준에 맞춰 재구성한 종합 문제'
---

<Callout type="warning">
  이 문서의 문제는 실제 독학사 기출문제를 그대로 옮긴 것이 아니라, 국가평생교육진흥원이 공개한 2단계 컴퓨터구조 **출제기준(평가영역)을 바탕으로 새로 재구성**한 예상 문제다. 회차별 실제 문항 수·난이도·출제 경향은 매년 공고와 시행 결과에 따라 달라질 수 있으므로, 반드시 최신 시행공고를 함께 확인해야 한다.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="십진수 219를 8비트 2진수로 옳게 표현한 것은?"
  options={['10101101', '11011101', '11011011', '01011011']}
  correctAnswer={3}
  explanation="219 = 128+64+16+8+2+1이므로 8비트 자리값에 맞춰 채우면 11011011이다."
  optionExplanations={['이는 173을 2진수로 표현한 값이며 219가 아니다', '자리값 조합이 219와 일치하지 않는 값이다', '정답이다. 128+64+16+8+2+1=219이며 비트 배열은 11011011이다', '최상위 비트가 0이면 128 자리값이 빠져 최대 127까지만 표현되므로 219를 나타낼 수 없다']}
/>

<Quiz
  questionNumber={2}
  question="8비트 체계에서 십진수 -45를 2의 보수로 표현한 것은?"
  options={['11010010', '11010011', '10101101', '00101101']}
  correctAnswer={2}
  explanation="45=00101101이며, 1의 보수는 11010010, 여기에 1을 더하면 2의 보수 11010011이 된다."
  optionExplanations={['이는 45의 1의 보수이며 2의 보수가 아니다(1을 더하는 단계 누락)', '정답이다. 1의 보수 11010010에 1을 더한 11010011이 2의 보수다', '이는 45의 부호화 크기 표현과 혼동한 값이다', '이는 +45의 표현이며 -45가 아니다']}
/>

<Quiz
  questionNumber={3}
  question="8비트 2의 보수 체계에서 -70과 -90을 더하면 오버플로가 발생하는가?"
  options={['발생한다. 두 음수의 합이 표현 범위를 벗어나 결과가 양수 비트열로 나온다', '발생하지 않는다. 두 수의 부호가 다르기 때문이다', '발생한다. 두 수가 8비트로 정확히 표현되지 않기 때문이다', '발생하지 않는다. 결과가 정확히 -160으로 표현된다']}
  correctAnswer={1}
  explanation="-70과 -90을 8비트 2의 보수로 더하면 실제 합 -160이 8비트 표현 범위(-128~127)를 벗어나 부호 비트가 0인 양수 비트열(96)로 나타난다. 두 피연산자의 부호가 같은데 결과 부호가 다르므로 오버플로다."
  optionExplanations={['정답이다. 두 음수를 더했는데 결과가 양수처럼 보이는 것이 오버플로의 전형적인 증상이다', '두 수는 모두 음수로 부호가 같으며, 부호가 다르다는 진술 자체가 틀렸다', '70과 90은 각각 8비트로 정확히 표현 가능한 값이며, 문제는 표현이 아니라 합의 범위 초과다', '8비트로는 -160을 표현할 수 없으므로 이 진술은 틀렸다']}
/>

<Quiz
  questionNumber={4}
  question="IEEE 754 단정도(single precision, 32비트) 부동소수점 표현의 비트 구성으로 옳은 것은?"
  options={['부호 1비트, 지수 8비트, 가수 23비트', '부호 1비트, 지수 11비트, 가수 52비트', '부호 2비트, 지수 8비트, 가수 22비트', '부호 1비트, 지수 23비트, 가수 8비트']}
  correctAnswer={1}
  explanation="IEEE 754 단정도(32비트)는 부호 1비트, 지수(exponent) 8비트, 가수(mantissa) 23비트로 구성된다."
  optionExplanations={['정답이다. 1+8+23=32비트로 단정도 형식과 일치한다', '이는 배정도(double precision, 64비트) 형식의 비트 구성이다', '부호 비트는 1비트만 사용하며 2비트를 쓰지 않는다', '지수와 가수 비트 수가 서로 뒤바뀐 값이다']}
/>

<Quiz
  questionNumber={5}
  question="IEEE 754 정규화(normalized) 부동소수점 표현에서 가수(mantissa) 부분에 대한 설명으로 옳은 것은?"
  options={['0.으로 시작하는 형태로 저장된다', '1.로 시작하며 최상위 비트 1은 저장 시 생략(암묵)된다', '가수는 항상 0으로만 채워진다', '가수는 지수와 완전히 동일한 값을 가진다']}
  correctAnswer={2}
  explanation="정규화된 부동소수점은 1.xxxx × 2^e 형태로 표현되며, 항상 1로 시작하기 때문에 그 1은 저장 공간을 아끼기 위해 생략(암묵의 1, implicit 1)하고 소수점 아래 자리만 저장한다."
  optionExplanations={['정규화 표현은 0.이 아니라 1.으로 시작하는 형태를 기준으로 한다', '정답이다. 암묵의 1(implicit 1) 규칙이 IEEE 754 정규화 표현의 핵심이다', '가수는 실제 값에 따라 다양한 비트 패턴을 가지며 항상 0인 것은 아니다', '가수와 지수는 서로 다른 필드에 저장되는 별개의 값이다']}
/>

<Quiz
  questionNumber={6}
  question="데이터 1011001에 짝수 패리티(even parity)를 적용할 때 추가해야 할 패리티 비트는?"
  options={['0', '1', '2', '1011001']}
  correctAnswer={1}
  explanation="1011001에서 1의 개수는 4개(짝수)다. 짝수 패리티는 전체 1의 개수를 짝수로 맞추는 것이 목표이므로, 이미 짝수이면 패리티 비트로 0을 추가한다."
  optionExplanations={['정답이다. 이미 1의 개수가 짝수(4개)이므로 패리티 비트는 0이다', '1을 추가하면 1의 개수가 홀수(5개)가 되어 짝수 패리티 조건을 위반한다', '패리티 비트는 0 또는 1 중 하나만 가능하며 2는 유효한 값이 아니다', '패리티 비트는 원본 데이터를 반복하는 것이 아니라 1비트의 검사 비트를 추가하는 것이다']}
/>

<Quiz
  questionNumber={7}
  question="명령어 수 5,000,000개, CPI(평균 명령어당 사이클 수) 2.5, 클록 속도 500MHz인 프로그램의 실행 시간은?"
  options={['0.025초(25ms)', '0.25초(250ms)', '2.5초', '25초']}
  correctAnswer={1}
  explanation="실행 시간 = (명령어 수 × CPI) / 클록 속도 = (5,000,000 × 2.5) / 500,000,000 = 12,500,000 / 500,000,000 = 0.025초다."
  optionExplanations={['정답이다. 총 사이클 수 12,500,000을 클록 속도 500,000,000Hz로 나눈 값이다', '클록 속도를 50MHz로 잘못 대입했을 때 나오는 값에 가깝다', '단위를 초 단위가 아니라 밀리초 단위로 착각한 값에 가깝다', '클록 속도를 500KHz로 잘못 대입했을 때 나오는 값에 가깝다']}
/>

<Quiz
  questionNumber={8}
  question="명령 사이클의 기본 단계로 옳은 순서는?"
  options={['인출 → 실행 → 해독', '해독 → 인출 → 실행', '인출 → 해독 → 실행', '실행 → 인출 → 해독']}
  correctAnswer={3}
  explanation="명령 사이클의 기본 단계는 명령어를 가져오는 인출(Fetch), 그 명령어의 의미를 파악하는 해독(Decode), 실제로 수행하는 실행(Execute) 순서로 진행된다."
  optionExplanations={['해독과 실행의 순서가 뒤바뀌었다', '인출이 가장 먼저 이루어져야 하는데 순서가 맞지 않는다', '정답이다. 인출 → 해독 → 실행이 명령 사이클의 기본 순서다', '실행이 가장 먼저 올 수 없다. 무엇을 실행할지 먼저 인출·해독해야 한다']}
/>

<Quiz
  questionNumber={9}
  question="즉시 주소지정 방식(immediate addressing)에 대한 설명으로 옳은 것은?"
  options={['오퍼랜드 필드에 실제 데이터 값이 직접 들어 있다', '오퍼랜드 필드에 메모리 주소가 들어 있고 그 주소의 값을 사용한다', '오퍼랜드 필드의 값이 다시 주소로 쓰여 그 주소가 가리키는 곳의 값을 참조한다', '오퍼랜드 필드에 레지스터 번호가 들어 있다']}
  correctAnswer={1}
  explanation="즉시 주소지정 방식은 오퍼랜드 필드 자체에 사용할 데이터 값이 직접 들어 있어, 메모리를 한 번도 더 참조하지 않고 바로 값을 얻을 수 있다."
  optionExplanations={['정답이다. 즉시 주소지정의 핵심은 오퍼랜드가 곧 데이터라는 점이다', '이는 직접 주소지정(direct addressing) 방식에 대한 설명이다', '이는 간접 주소지정(indirect addressing) 방식에 대한 설명이다', '이는 레지스터 주소지정(register addressing) 방식에 대한 설명이다']}
/>

<Quiz
  questionNumber={10}
  question="간접 주소지정 방식에서 명령어의 주소 필드가 300이고, 메모리 300번지에 저장된 값이 500일 때 유효주소(effective address)는?"
  options={['300', '500', '800', '300과 500을 더한 값']}
  correctAnswer={2}
  explanation="간접 주소지정은 명령어의 주소 필드가 가리키는 위치(300번지)에 저장된 값(500)을 다시 유효주소로 사용한다. 따라서 유효주소는 500이다."
  optionExplanations={['300은 명령어에 적힌 주소 필드 값일 뿐, 간접 주소지정에서는 그 자체가 유효주소가 아니다', '정답이다. 300번지에 저장된 값 500이 실제로 참조할 유효주소가 된다', '간접 주소지정은 두 값을 더하는 방식이 아니라 한 번 더 참조하는 방식이다', '간접 주소지정은 덧셈이 아니라 메모리 참조를 한 번 더 거치는 절차다']}
/>

<Quiz
  questionNumber={11}
  question="CISC와 RISC의 차이에 대한 설명으로 옳은 것은?"
  options={['RISC는 명령어 종류가 많고 실행 사이클이 명령어마다 다양하다', 'CISC는 명령어 길이와 형식이 고정되어 있다', 'RISC는 명령어 형식이 단순하고 고정 길이인 경향이 있어 파이프라이닝에 유리하다', 'CISC는 레지스터 수가 RISC보다 항상 많다']}
  correctAnswer={3}
  explanation="RISC(Reduced Instruction Set Computer)는 명령어 종류를 줄이고 형식을 단순화·고정 길이화해 파이프라인 설계를 쉽게 만든 것이 특징이다."
  optionExplanations={['명령어 종류가 많고 실행 사이클이 다양한 쪽은 CISC의 특징이다', '명령어 길이·형식이 고정된 쪽은 RISC의 특징이며 CISC는 가변 길이·형식이 많다', '정답이다. 단순하고 고정된 명령어 형식은 RISC가 파이프라이닝에 유리한 핵심 이유다', '레지스터 수는 설계에 따라 다르며, RISC가 오히려 레지스터를 더 많이 두는 경향이 있다']}
/>

<Quiz
  questionNumber={12}
  question="주기억장치 2MB, 캐시 8KB, 블록 크기 32바이트인 시스템에서 직접사상 방식의 태그 비트 수는?"
  options={['5비트', '8비트', '13비트', '21비트']}
  correctAnswer={2}
  explanation="전체 주소 비트는 log2(2MB)=21비트, 오프셋은 log2(32)=5비트, 캐시 라인 수는 8192/32=256개이므로 인덱스는 8비트다. 태그 = 21-8-5 = 8비트다."
  optionExplanations={['이는 오프셋 비트 수(블록 크기 32바이트에 해당)다', '정답이다. 21-8-5=8비트가 태그로 쓰인다', '이는 인덱스와 오프셋을 합친 비트 수(8+5)다', '이는 전체 주소 비트 수 자체이며 태그만의 비트 수가 아니다']}
/>

<Quiz
  questionNumber={13}
  question="캐시 접근시간 8ns, 실패 손실 120ns, 적중률 90%(0.9)일 때 평균 기억장치 접근시간(AMAT)은?"
  options={['8ns', '12ns', '20ns', '120ns']}
  correctAnswer={3}
  explanation="AMAT = 8 + (1-0.9)*120 = 8 + 12 = 20ns다."
  optionExplanations={['캐시 접근시간만 반영하고 실패 손실을 더하지 않은 값이다', '실패 손실 항(12ns)만 계산하고 캐시 접근시간을 더하지 않은 값이다', '정답이다. 실패율 0.1에 실패 손실 120ns를 곱한 12ns를 캐시 접근시간 8ns에 더한 값이다', '실패 손실 전체를 실패율 없이 그대로 반영한 값이다']}
/>

<Quiz
  questionNumber={14}
  question="논리주소 공간이 32비트이고 페이지 크기가 8KB(2의 13승 바이트)일 때, 논리주소의 페이지 번호 비트 수는?"
  options={['13비트', '16비트', '19비트', '32비트']}
  correctAnswer={3}
  explanation="전체 논리주소 비트(32) - 오프셋 비트(13, 페이지 크기 8KB에 해당) = 19비트가 페이지 번호에 쓰인다."
  optionExplanations={['이는 오프셋 비트 수 그 자체이며 페이지 번호 비트 수가 아니다', '32와 13의 차이를 잘못 계산한 값이다', '정답이다. 32-13=19비트다', '이는 논리주소 전체 비트 수이며 페이지 번호만의 비트 수가 아니다']}
/>

<Quiz
  questionNumber={15}
  question="페이지 부재(page fault)에 대한 설명으로 옳은 것은?"
  options={['페이지 부재는 페이지 테이블의 유효 비트가 1일 때 발생한다', '페이지 부재 처리가 끝나면 중단됐던 명령어를 처음부터 다시 실행한다', '페이지 부재는 캐시 미스(cache miss)와 완전히 동일한 현상이다', '페이지 부재가 발생하면 해당 프로세스는 강제로 종료된다']}
  correctAnswer={2}
  explanation="페이지 부재가 처리되어 필요한 페이지가 물리 메모리에 적재되고 페이지 테이블이 갱신되면, 운영체제는 페이지 부재가 발생했던 명령어를 처음부터 다시 실행한다."
  optionExplanations={['페이지 부재는 유효 비트가 0일 때(그 페이지가 물리 메모리에 없을 때) 발생한다', '정답이다. 페이지 적재와 페이지 테이블 갱신 후 명령어를 재실행하는 것이 페이지 부재 처리의 마지막 단계다', '페이지 부재는 가상메모리 계층의 현상이고 캐시 미스는 캐시-주기억장치 계층의 현상으로 서로 다른 계층에서 발생한다', '페이지 부재는 정상적인 예외 처리 과정이며 프로세스를 강제 종료하지 않는다']}
/>

<Quiz
  questionNumber={16}
  question="TLB 탐색 4ns, 메모리 접근 100ns, TLB 적중률 95%일 때 유효 접근시간(EAT)은? (TLB 실패 시 TLB 탐색+페이지 테이블 조회+데이터 접근이 모두 필요하다고 가정)"
  options={['104ns', '109ns', '204ns', '100ns']}
  correctAnswer={2}
  explanation="TLB 적중 시 4+100=104ns, 실패 시 4+100+100=204ns이며, EAT = 0.95*104 + 0.05*204 = 98.8+10.2 = 109ns다."
  optionExplanations={['이는 TLB 적중 시에만 걸리는 시간이며 적중률·실패율 가중을 반영하지 않았다', '정답이다. 적중률과 실패율을 각각의 소요 시간에 곱해 더한 가중평균이다', '이는 TLB 실패 시에만 걸리는 시간이며 적중 확률을 반영하지 않았다', '이는 메모리 접근시간 자체이며 TLB 탐색 시간이 전혀 반영되지 않았다']}
/>

<Quiz
  questionNumber={17}
  question="인터럽트 기반 I/O와 DMA의 차이에 대한 설명으로 옳은 것은?"
  options={['인터럽트 기반 I/O는 데이터 전송 자체를 CPU 개입 없이 수행한다', 'DMA는 전송이 끝난 뒤 한 번의 인터럽트로 CPU에 완료를 알린다', 'DMA는 폴링(polling) 방식으로 동작한다', '인터럽트 기반 I/O가 대량 데이터 전송에서 DMA보다 효율적이다']}
  correctAnswer={2}
  explanation="DMA는 전송 도중에는 CPU가 개입하지 않고, 전송이 모두 끝난 뒤에 단 한 번의 인터럽트로 CPU에 완료 사실을 알린다."
  optionExplanations={['인터럽트 기반 I/O는 여전히 데이터 단어(word) 단위마다 CPU가 직접 개입해 옮긴다', '정답이다. DMA는 전송 전체가 끝난 뒤 1회의 인터럽트로 완료를 알리는 것이 특징이다', '폴링은 프로그램 제어 I/O의 방식이며 DMA는 폴링을 사용하지 않는다', '대량 데이터 전송에서는 DMA가 인터럽트 기반 I/O보다 효율적이다']}
/>

<Quiz
  questionNumber={18}
  question="메모리 사이클 시간이 100ns이고, 입출력 장치가 2000ns마다 워드 하나씩 전송을 요청하며 요청마다 DMA가 메모리 사이클 1회를 훔쳐 쓴다면, 사이클 스틸로 인한 CPU 성능 저하율은?"
  options={['1%', '2%', '5%', '10%']}
  correctAnswer={3}
  explanation="2000ns 동안 CPU가 쓸 수 있는 메모리 사이클은 2000/100=20사이클이며, 이 중 1사이클을 DMA가 가져가므로 저하율은 1/20=5%다."
  optionExplanations={['20사이클 중 1사이클을 잘못된 분모로 나눈 값에 가깝다', '정확한 계산 결과와 다른 값이다', '정답이다. 1/20=0.05, 즉 5%의 성능 저하가 발생한다', '요청 주기가 1000ns로 절반이 됐을 때 나오는 저하율과 혼동한 값이다']}
/>

<Quiz
  questionNumber={19}
  question="버스(bus)의 구성 요소와 역할에 대한 설명으로 옳은 것은?"
  options={['주소 버스는 데이터를 실어 나른다', '데이터 버스는 접근할 위치 정보를 실어 나른다', '제어 버스는 읽기·쓰기 신호와 버스 요청·승인 신호를 실어 나른다', '버스는 항상 CPU만 사용할 수 있고 다른 장치는 사용할 수 없다']}
  correctAnswer={3}
  explanation="제어 버스는 읽기/쓰기 신호, 인터럽트 요청, 버스 요청·승인 같은 제어 정보를 실어 나른다."
  optionExplanations={['주소 버스는 접근할 위치 정보를 실어 나르며 데이터 자체를 싣지 않는다', '데이터 버스는 실제 데이터를 실어 나르며 위치 정보를 싣지 않는다', '정답이다. 제어 버스의 정의와 정확히 일치한다', 'DMA 컨트롤러 같은 장치도 버스 중재를 거쳐 버스를 사용할 수 있다']}
/>

<Quiz
  questionNumber={20}
  question="5단계 파이프라인으로 명령어 8개를 처리할 때 필요한 총 사이클 수는?"
  options={['8', '12', '13', '40']}
  correctAnswer={2}
  explanation="총 사이클 수 = k+(n-1) = 5+(8-1) = 5+7 = 12사이클이다."
  optionExplanations={['이는 명령어 개수 n 그 자체이며 파이프라인 단계 수를 반영하지 않았다', '정답이다. 5+7=12사이클이다', 'k+n을 그대로 더한 값으로, n-1이 아니라 n을 더한 계산 실수다', '이는 파이프라인을 쓰지 않고 순차 처리했을 때(n*k)의 사이클 수다']}
/>

<Quiz
  questionNumber={21}
  question="분기(branch) 명령어의 실행 여부가 늦게 확정되어 뒤따르는 명령어들을 잘못 인출하게 되는 해저드는?"
  options={['구조적 해저드', '데이터 해저드', '제어 해저드', '오버플로 해저드']}
  correctAnswer={3}
  explanation="분기 명령어의 결과가 늦게 확정되어 그사이 잘못된 명령어를 인출하는 문제는 제어 해저드다."
  optionExplanations={['구조적 해저드는 하드웨어 자원 충돌 문제이며 분기와는 무관하다', '데이터 해저드는 값의 의존 관계 문제이며 분기와는 무관하다', '정답이다. 분기의 불확실성으로 인한 문제는 제어 해저드의 정의와 정확히 일치한다', '오버플로 해저드라는 용어는 파이프라인 해저드 분류에 존재하지 않는다']}
/>

<Quiz
  questionNumber={22}
  question="n비트 2의 보수 체계에서 표현 가능한 정수의 최솟값은?"
  options={['-2^(n-1)', '-(2^(n-1)-1)', '-2^n', '0']}
  correctAnswer={1}
  explanation="n비트 2의 보수의 표현 범위는 -2^(n-1) ~ 2^(n-1)-1이므로 최솟값은 -2^(n-1)이다."
  optionExplanations={['정답이다. 2의 보수 표현 범위의 하한이다', '이는 1의 보수나 부호화 크기 표현의 최솟값이다', '이는 n비트로 표현 가능한 값의 개수(2^n)이며 최솟값이 아니다', '0은 표현 범위의 중간 부근에 있는 값이며 최솟값이 아니다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 - 전공분야/과목 시험안내](https://bdes.nile.or.kr/nile/exam/nExam2_1.do)
- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
- [국가평생교육진흥원 독학학위제 - 출제방향/수준](https://bdes.nile.or.kr/nile/study/nStudy1_1.do)
