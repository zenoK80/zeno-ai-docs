---
title: '20. 핵심 개념 유형 문제 100제 (MLP·역전파·정규화)'
description: '퍼셉트론·MLP 구조, 활성화·손실 함수 조합, 순전파·역전파 계산, 과적합·정규화를 묻는 22문항 실전 유형 문제 모음'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 문제들은 국가평생교육진흥원 독학학위제 3단계 딥러닝 출제기준(퍼셉트론·MLP, 활성화·손실 함수, 순전파·역전파, 과적합·정규화 영역)을 근거로 새로 구성한 **재구성 문항**입니다. 실제 기출 문항을 그대로 옮긴 것이 아니며, "100제"라는 제목은 05·06·07·09편에서 다룬 이 영역 전체의 문제 은행 규모를 뜻합니다. 이 문서에는 그중 **출제 빈도가 높은 핵심 유형 22문항**을 골라 전 과정 해설과 함께 싣습니다. 나머지 유형은 05·06·07·09편 본문의 "자주 틀리는 점" 박스를 반복 학습하는 것으로 보충하세요.
</Callout>

## 문항 구성

| 영역 | 문항 번호 | 문항 수 | 관련 편 |
| --- | --- | --- | --- |
| 퍼셉트론·MLP 구조 | 1–5 | 5 | 05편 |
| 활성화·손실 함수 조합 | 6–10 | 5 | 06편 |
| 순전파·역전파 계산 | 11–16 | 6 | 07편 |
| 과적합·정규화 | 17–22 | 6 | 09편 |

## 영역 1: 퍼셉트론·MLP 구조 (1–5)

### 문제 1

가중치 $w_1 = 0.5$, $w_2 = -0.3$, 편향(bias) $b = 0.2$를 가진 단층 퍼셉트론(perceptron)에 입력 $x_1 = 1$, $x_2 = 1$을 넣었다.

<Quiz
  questionNumber={1}
  question="이 퍼셉트론의 가중합(net input) 값은 얼마인가?"
  options={['0.4', '0.2', '1.0', '0.6']}
  correctAnswer={1}
  explanation="가중합은 net = w1*x1 + w2*x2 + b 공식으로 구합니다. 대입하면 net = 0.5*1 + (-0.3)*1 + 0.2 = 0.5 - 0.3 + 0.2 = 0.4 입니다."
  optionExplanations={['공식대로 정확히 계산한 값이므로 정답입니다.', '편향 b=0.2를 빠뜨리고 0.5-0.3만 계산하면 0.2가 나와 틀렸습니다.', 'w2의 부호를 반대로(+0.3) 계산하면 1.0이 나와 틀렸습니다.', '편향을 두 번 더하는 실수를 하면 0.6이 나와 틀렸습니다.']}
/>

### 문제 2

문제 1의 퍼셉트론이 계단 함수(step function)를 쓰되, 임계값이 0.5여서 net이 0.5 이상이면 출력 1, 미만이면 출력 0을 낸다고 하자.

<Quiz
  questionNumber={2}
  question="문제 1에서 구한 net=0.4를 임계값 0.5인 계단 함수에 통과시키면 최종 출력은?"
  options={['0', '1', '0.4', '0.5']}
  correctAnswer={1}
  explanation="net=0.4는 임계값 0.5보다 작습니다. 계단 함수는 net이 임계값 미만이면 0을 출력하도록 정의되었으므로, 최종 출력은 0입니다."
  optionExplanations={['0.4 < 0.5이므로 0을 출력하는 것이 맞아 정답입니다.', 'net이 임계값보다 작은데 1을 출력한다고 하면 계단 함수의 정의를 반대로 적용한 것이라 틀렸습니다.', 'net 값 자체(0.4)를 그대로 답으로 쓰면 계단 함수를 거치지 않은 것이라 틀렸습니다.', '임계값(0.5)을 출력값으로 착각한 것이라 틀렸습니다.']}
/>

### 문제 3

입력층 노드 4개, 은닉층 노드 5개, 출력층 노드 3개로 구성된 완전연결(fully connected) MLP가 있다. 입력층-은닉층, 은닉층-출력층 각 연결마다 편향이 하나씩 있다.

<Quiz
  questionNumber={3}
  question="이 MLP의 전체 학습 파라미터(가중치+편향) 개수는?"
  options={['43', '35', '40', '51']}
  correctAnswer={1}
  explanation="입력-은닉 구간의 가중치는 4*5=20개, 은닉층 편향은 5개이므로 25개입니다. 은닉-출력 구간의 가중치는 5*3=15개, 출력층 편향은 3개이므로 18개입니다. 두 구간을 더하면 25+18=43개입니다."
  optionExplanations={['가중치와 편향을 모두 반영한 정확한 합계이므로 정답입니다.', '편향을 아예 빼고 20+15만 더하면 35가 나와 틀렸습니다.', '출력층 편향 3개를 빠뜨리면 25+15=40이 나와 틀렸습니다.', '편향을 층마다 두 번씩 중복해서 더하면 51이 나와 틀렸습니다.']}
/>

### 문제 4

단층 퍼셉트론과 다층 퍼셉트론(MLP)의 표현 능력에 대한 설명 중 옳지 않은 것을 고르시오.

<Quiz
  questionNumber={4}
  question="단층 퍼셉트론과 MLP의 표현 능력에 대한 설명으로 옳지 않은 것은?"
  options={['단층 퍼셉트론은 선형 분리가 가능한 문제만 풀 수 있다.', 'XOR 문제는 선형 분리가 불가능하므로 단층 퍼셉트론으로는 해결할 수 없다.', '은닉층을 하나 이상 추가하고 비선형 활성화 함수를 쓰면 MLP는 XOR 문제를 해결할 수 있다.', '단층 퍼셉트론도 활성화 함수를 계단 함수에서 시그모이드로 바꾸기만 하면 XOR 문제를 풀 수 있다.']}
  correctAnswer={4}
  explanation="단층 퍼셉트론의 출력은 net = w1*x1 + w2*x2 + b라는 하나의 선형식의 부호(또는 그 값)로 결정됩니다. 활성화 함수를 계단 함수에서 시그모이드로 바꿔도 net=0을 경계로 하는 결정 경계는 여전히 하나의 직선(초평면)이므로, 층을 늘리지 않는 한 XOR처럼 선형 분리가 불가능한 문제는 풀 수 없습니다. 따라서 이 설명이 옳지 않습니다."
  optionExplanations={['단층 퍼셉트론의 결정 경계가 직선(초평면) 하나뿐이라는 옳은 설명입니다.', 'XOR은 하나의 직선으로 두 클래스를 나눌 수 없다는 옳은 설명입니다.', '은닉층과 비선형 활성화 함수의 조합이 XOR을 풀 수 있게 한다는 옳은 설명입니다.', '활성화 함수만 바꾸는 것으로는 결정 경계가 여전히 하나의 직선이라 XOR을 풀 수 없으므로, 이 설명이 틀린 지점이며 정답입니다.']}
/>

### 문제 5

다음 설명에 해당하는 이론의 이름을 고르시오. "은닉층이 하나 이상이고 은닉 노드 수가 충분하다면, 비선형 활성화 함수를 가진 MLP는 임의의 연속 함수를 원하는 정확도로 근사할 수 있다."

<Quiz
  questionNumber={5}
  question="위 설명에 해당하는 이론(정리)의 이름은?"
  options={['보편근사정리(Universal Approximation Theorem)', '경사소실정리', '과적합정리', '선형분리정리']}
  correctAnswer={1}
  explanation="이 설명은 은닉층 하나짜리 MLP도 충분한 노드 수와 비선형 활성화 함수만 있으면 임의의 연속 함수를 근사할 수 있다는 보편근사정리의 정의 그대로입니다. 나머지 세 용어는 실제로 통용되는 정리 이름이 아니라 이 문제를 위해 만든 오답 보기입니다."
  optionExplanations={['설명과 정확히 일치하는 정리 이름이므로 정답입니다.', '그라디언트 소실은 07편에서 다루는 현상 이름이지 이 설명에 대응하는 정리가 아니라 틀렸습니다.', '과적합은 09편에서 다루는 현상이지 이 설명과 무관해 틀렸습니다.', '선형분리 가능성은 단층 퍼셉트론의 한계를 설명하는 개념이지 이 설명과 무관해 틀렸습니다.']}
/>

## 영역 2: 활성화·손실 함수 조합 (6–10)

### 문제 6

시그모이드 함수 $\sigma(x) = \frac{1}{1+e^{-x}}$의 미분은 $\sigma'(x) = \sigma(x)(1-\sigma(x))$이다. $\sigma(0) = 0.5$임을 이용한다.

<Quiz
  questionNumber={6}
  question="x=0에서 시그모이드 함수의 미분값 σ'(0)은?"
  options={['0.25', '0.5', '1', '0']}
  correctAnswer={1}
  explanation="σ'(0) = σ(0)*(1-σ(0)) = 0.5*(1-0.5) = 0.5*0.5 = 0.25 입니다. 시그모이드 미분값은 σ=0.5일 때 최댓값 0.25를 가지며, 0이나 1에 가까워질수록 0에 수렴합니다."
  optionExplanations={['공식에 σ(0)=0.5를 대입해 정확히 계산한 값이므로 정답입니다.', 'σ(0) 값 자체를 미분값으로 착각하면 0.5가 나와 틀렸습니다.', '미분 공식을 적용하지 않고 최댓값을 1로 착각하면 틀립니다.', '시그모이드가 0에서 미분 불가능하다고 착각하면 나올 수 있는 오답입니다.']}
/>

### 문제 7

활성화 함수에 대한 설명 중 옳지 않은 것을 고르시오.

<Quiz
  questionNumber={7}
  question="활성화 함수에 대한 설명으로 옳지 않은 것은?"
  options={['시그모이드의 출력 범위는 0과 1 사이이다.', 'tanh의 출력 범위는 -1과 1 사이이며 0을 중심으로 대칭이라 시그모이드보다 학습이 잘 되는 경우가 많다.', 'ReLU는 입력이 0보다 크면 기울기가 항상 1이고 0보다 작으면 기울기가 0이라, 그라디언트 소실 문제를 완전히 없앤다.', 'Leaky ReLU는 음수 입력에도 작은 기울기를 주어 죽은 ReLU 문제를 완화한다.']}
  correctAnswer={3}
  explanation="ReLU는 시그모이드나 tanh보다 그라디언트 소실을 크게 줄여주지만, 입력이 계속 음수로만 들어오는 뉴런은 기울기가 0으로 고정되어 아예 학습되지 않는 죽은 ReLU(dead ReLU) 문제가 생깁니다. 즉 그라디언트 소실을 완전히 없애는 것이 아니라 완화하는 수준이므로, 이 설명이 옳지 않습니다."
  optionExplanations={['시그모이드의 정의 그대로인 옳은 설명입니다.', 'tanh가 0 중심 대칭이라 시그모이드보다 수렴이 빠른 경우가 많다는 옳은 설명입니다.', '완전히 없앤다는 표현이 과장이며, 죽은 ReLU 문제가 남아 있으므로 이 설명이 틀린 지점이라 정답입니다.', 'Leaky ReLU가 죽은 ReLU 문제를 완화한다는 옳은 설명입니다.']}
/>

### 문제 8

<Quiz
  questionNumber={8}
  question="이진 분류 문제에서 가장 널리 쓰이는 출력층 활성화 함수와 손실 함수의 조합은?"
  options={['시그모이드 + 이진 크로스엔트로피', '소프트맥스 + 평균제곱오차(MSE)', 'ReLU + 평균절대오차(MAE)', 'tanh + 힌지 손실']}
  correctAnswer={1}
  explanation="이진 분류에서는 출력을 0과 1 사이의 확률로 해석해야 하므로 시그모이드를 출력층 활성화 함수로 쓰고, 그 확률과 실제 정답(0 또는 1) 사이의 차이를 재는 이진 크로스엔트로피를 손실 함수로 사용하는 조합이 표준입니다."
  optionExplanations={['확률 출력(시그모이드)과 확률 오차를 재는 손실(이진 크로스엔트로피)의 표준 조합이므로 정답입니다.', '소프트맥스+MSE는 다중 분류에서도 잘 쓰이지 않는 비표준 조합이라 틀렸습니다.', 'ReLU는 출력을 0과 1 사이로 제한하지 않아 확률 해석이 안 되므로 이진 분류 출력층에 부적합해 틀렸습니다.', 'tanh+힌지 손실은 SVM 계열에서 쓰는 조합이지 신경망 이진 분류의 표준 조합이 아니라 틀렸습니다.']}
/>

### 문제 9

<Quiz
  questionNumber={9}
  question="출력층에 시그모이드를 쓰면서 손실 함수로 MSE를 쓰면 학습이 느려질 수 있는 이유로 가장 적절한 것은?"
  options={['출력이 0이나 1 근처로 포화되면 시그모이드 미분값이 0에 가까워지고, 이 값이 그라디언트 계산에 곱해져 그라디언트가 매우 작아지기 때문이다.', 'MSE는 회귀 문제에서만 정의되는 손실이라 분류 문제에서는 아예 계산이 안 되기 때문이다.', '시그모이드는 입력이 커질수록 계산 비용이 기하급수적으로 증가하기 때문이다.', 'MSE를 쓰면 가중치가 매 스텝 무작위로 초기화되기 때문이다.']}
  correctAnswer={1}
  explanation="MSE의 그라디언트에는 출력층 활성화 함수의 미분(σ(1-σ))이 곱해집니다. 시그모이드 출력이 0이나 1 근처로 포화되면 이 미분값이 0에 가까워지므로, 예측이 크게 틀렸을 때조차 그라디언트가 작아져 가중치 갱신이 느려집니다. 크로스엔트로피는 이 포화 항이 상쇄되도록 설계되어 이 문제를 완화합니다."
  optionExplanations={['시그모이드 포화와 미분값 감소를 정확히 짚은 설명이므로 정답입니다.', 'MSE는 분류 문제에도 수식적으로 계산은 되므로(다만 비효율적일 뿐) 틀린 설명입니다.', '시그모이드의 계산 비용은 입력 크기와 관계없이 일정하므로 틀린 설명입니다.', 'MSE 사용 여부와 가중치 초기화 방식은 무관하므로 틀린 설명입니다.']}
/>

### 문제 10

3개 클래스 분류 문제에서 소프트맥스 출력이 [0.7, 0.2, 0.1]이고 정답 클래스는 첫 번째(원-핫 [1, 0, 0])이다. $\ln 0.7 \approx -0.357$을 이용한다.

<Quiz
  questionNumber={10}
  question="이 예측에 대한 크로스엔트로피 손실 값은 약 얼마인가?"
  options={['0.357', '0.7', '1.0', '0.643']}
  correctAnswer={1}
  explanation="다중 클래스 크로스엔트로피는 L = -sum(y_i * ln(p_i))로 계산합니다. 정답 원-핫 벡터가 [1,0,0]이므로 정답 클래스에 해당하는 항만 남아 L = -1*ln(0.7) - 0 - 0 = -ln(0.7) ≈ -(-0.357) = 0.357 입니다."
  optionExplanations={['정답 클래스의 예측 확률에만 -ln을 취한 정확한 계산이므로 정답입니다.', '예측 확률 0.7 자체를 손실로 착각하면 나오는 오답입니다.', '손실을 임의로 1로 놓은 오답입니다.', '1에서 예측 확률을 빼는 정확도 계산법을 손실에 잘못 적용한 오답(1-0.7=0.3이 아니라 0.643으로 잘못 계산한 경우 포함)입니다.']}
/>

## 영역 3: 순전파·역전파 계산 (11–16)

다음 세 문제(11–13)는 하나의 신경망을 공유한다. 입력 $x=1$이 가중치 $w_1=0.3$을 거쳐 은닉 뉴런에 들어가고, 은닉 뉴런의 시그모이드 출력은 $h=0.6$이다. $h$는 가중치 $w_2=0.5$를 거쳐 출력 뉴런에 들어가고, 출력 뉴런의 시그모이드 출력은 $y=0.7$이다. 정답(target)은 $t=1$이고, 손실 함수는 $L = \frac{1}{2}(t-y)^2$이다.

### 문제 11

<Quiz
  questionNumber={11}
  question="출력층 가중치 w2에 대한 손실의 그라디언트 dL/dw2는 약 얼마인가?"
  options={['-0.0378', '-0.063', '0.0378', '-0.21']}
  correctAnswer={1}
  explanation="체인 룰로 세 단계를 곱합니다. 1) dL/dy = y-t = 0.7-1 = -0.3. 2) dy/d(net) = y(1-y) = 0.7*0.3 = 0.21이므로 출력층 델타 δ_o = dL/dy * dy/d(net) = -0.3*0.21 = -0.063. 3) d(net)/dw2 = h = 0.6이므로 dL/dw2 = δ_o * h = -0.063*0.6 = -0.0378 입니다."
  optionExplanations={['세 항을 모두 곱한 최종 그라디언트이므로 정답입니다.', 'h를 곱하는 마지막 단계를 빠뜨리면 델타값 -0.063에서 멈춰 틀렸습니다.', '부호를 반대로(t-y로) 계산하면 0.0378이 나와 틀렸습니다.', 'y(1-y) 계산 없이 dL/dy만 h와 곱하지 않고 그대로 사용한 경우 등 중간 단계를 건너뛴 오답입니다.']}
/>

### 문제 12

학습률 $\eta = 0.1$을 적용해 경사하강법으로 w2를 갱신한다.

<Quiz
  questionNumber={12}
  question="w2 = 0.5에서 경사하강법 한 스텝을 적용한 갱신 후 값은 약 얼마인가?"
  options={['0.5038', '0.4962', '0.5378', '0.4622']}
  correctAnswer={1}
  explanation="갱신 공식은 w2 ← w2 - η*(dL/dw2)입니다. 문제 11에서 dL/dw2 ≈ -0.0378이므로, w2 ← 0.5 - 0.1*(-0.0378) = 0.5 + 0.00378 = 0.50378 ≈ 0.5038 입니다. 그라디언트가 음수이므로 빼는 것이 오히려 가중치를 키운다는 점에 주의해야 합니다."
  optionExplanations={['부호를 정확히 처리한 올바른 갱신 결과이므로 정답입니다.', '그라디언트의 부호를 반대로(+0.0378로) 놓고 계산하면 0.4962가 나와 틀렸습니다.', '학습률을 곱하지 않고 그라디언트를 그대로 빼면(η=1로 착각) 0.5378이 나와 틀렸습니다.', '학습률을 1로 놓고 부호까지 반대로 계산하면 0.4622가 나와 틀렸습니다.']}
/>

### 문제 13

이제 은닉층 가중치 w1(입력 x=1과 은닉 뉴런 사이)에 대한 그라디언트를 구한다. 은닉 뉴런의 시그모이드 미분은 $h(1-h) = 0.6 \times 0.4 = 0.24$이고, 문제 11에서 구한 출력층 델타는 $\delta_o = -0.063$이다.

<Quiz
  questionNumber={13}
  question="은닉층 가중치 w1에 대한 손실의 그라디언트 dL/dw1은 약 얼마인가?"
  options={['-0.0076', '-0.063', '-0.0378', '0.0076']}
  correctAnswer={1}
  explanation="은닉층 델타는 출력층 델타를 출력층 가중치와 은닉 뉴런의 미분으로 되돌려 구합니다. δ_h = δ_o * w2 * h*(1-h) = -0.063 * 0.5 * 0.24 = -0.00756. 여기에 입력 x=1을 곱하면 dL/dw1 = δ_h * x = -0.00756 * 1 ≈ -0.0076 입니다."
  optionExplanations={['출력층 델타를 은닉층까지 정확히 역전파한 결과이므로 정답입니다.', '출력층 델타를 그대로 은닉층 그라디언트로 쓰면(역전파를 하지 않으면) -0.063이 나와 틀렸습니다.', '문제 11의 dL/dw2 값을 그대로 재사용하면 -0.0378이 나와 틀렸습니다.', '계산 과정은 맞았지만 부호를 반대로 정리하면 0.0076이 나와 틀렸습니다.']}
/>

### 문제 14

<Quiz
  questionNumber={14}
  question="역전파에서 은닉층 가중치의 그라디언트를 구할 때 반드시 필요한 것은?"
  options={['출력층에서 계산한 델타(오차 항)를 출력층 가중치를 거쳐 은닉층까지 역방향으로 전달한 값', '검증 데이터셋에서 다시 계산한 손실 값', '전혀 다른 입력값으로 순전파를 한 번 더 수행한 결과', '가중치를 무작위로 재초기화한 값']}
  correctAnswer={1}
  explanation="역전파(backpropagation)는 체인 룰을 이용해 출력층의 오차 항(델타)을 가중치를 거슬러 은닉층까지 전달함으로써 은닉층 가중치의 그라디언트를 구합니다. 문제 13처럼 δ_o에 w2와 활성화 함수 미분을 곱해 δ_h를 만드는 과정이 바로 이것입니다."
  optionExplanations={['체인 룰에 의한 델타의 역방향 전달이 역전파의 핵심이므로 정답입니다.', '검증 손실은 모델 평가에 쓰이지 그라디언트 계산에 필요한 값이 아니라 틀렸습니다.', '역전파는 이미 수행한 순전파의 중간값을 재사용하지, 새로운 입력으로 다시 순전파를 할 필요가 없어 틀렸습니다.', '가중치 재초기화는 그라디언트 계산과 무관한 별개의 작업이라 틀렸습니다.']}
/>

### 문제 15

그라디언트 소실(vanishing gradient) 문제에 대한 설명 중 옳지 않은 것을 고르시오.

<Quiz
  questionNumber={15}
  question="그라디언트 소실 문제에 대한 설명으로 옳지 않은 것은?"
  options={['시그모이드 함수의 미분값은 최대 0.25로, 층이 깊어질수록 이 값들이 곱해져 그라디언트가 매우 작아질 수 있다.', 'tanh도 출력이 -1이나 1에 가까울 때 미분값이 0에 가까워져 그라디언트 소실이 발생할 수 있다.', 'ReLU 계열 활성화 함수를 도입하면 그라디언트 소실 문제가 이론적으로 전혀 발생하지 않는다.', '그라디언트 소실이 심하면 입력에 가까운 앞쪽 층의 가중치가 거의 갱신되지 않는다.']}
  correctAnswer={3}
  explanation="ReLU는 시그모이드·tanh보다 그라디언트 소실을 크게 완화하지만, 죽은 ReLU 문제나 매우 깊은 네트워크에서는 여전히 그라디언트가 작아지는 경우가 있어 이론적으로 전혀 발생하지 않는다고 단정할 수 없습니다."
  optionExplanations={['시그모이드 미분의 최댓값이 0.25라는 정확한 사실을 반영한 옳은 설명입니다.', 'tanh 포화 구간에서 미분값이 0에 가까워진다는 옳은 설명입니다.', '전혀 발생하지 않는다는 단정이 과장이라 이 설명이 틀린 지점이며 정답입니다.', '그라디언트 소실이 앞쪽 층 학습을 막는다는 옳은 설명입니다.']}
/>

### 문제 16

문제 11–13에서 출력층 그라디언트(약 -0.063)보다 은닉층 그라디언트(약 -0.0076)의 절대값이 훨씬 작게 나왔다.

<Quiz
  questionNumber={16}
  question="이 결과가 시사하는 바로 가장 적절한 것은?"
  options={['층을 거슬러 올라갈수록 그라디언트가 가중치와 활성화 함수 미분의 곱으로 계속 줄어들어, 깊은 네트워크일수록 앞쪽 층의 학습이 느려지는 그라디언트 소실 경향을 보여준다.', '학습률을 잘못 설정해서 생긴 계산 오류이다.', '모델이 이미 과적합되었다는 증거이다.', '출력층의 오차 계산이 틀렸다는 신호이다.']}
  correctAnswer={1}
  explanation="이 예제는 층을 하나만 거슬러도 그라디언트가 약 1/8로 줄어드는 것을 보여줍니다. 실제로 층이 훨씬 많아지면 이런 곱셈이 반복되어 앞쪽 층의 그라디언트가 매우 작아지는 그라디언트 소실 현상으로 이어집니다."
  optionExplanations={['체인 룰에 의한 그라디언트 감소를 정확히 해석한 설명이므로 정답입니다.', '학습률은 이 예제의 그라디언트 계산 자체에 관여하지 않으므로 틀렸습니다.', '과적합은 훈련-검증 손실의 차이로 판단하는 현상이지 이 계산 결과와는 무관해 틀렸습니다.', '출력층 오차 계산 자체는 문제 11에서 이미 검증된 정상적인 계산이라 틀렸습니다.']}
/>

## 영역 4: 과적합·정규화 (17–22)

### 문제 17

한 모델의 훈련 손실은 학습이 진행될수록 계속 낮아졌지만, 검증 손실은 학습 중반부터 다시 상승하기 시작했다.

<Quiz
  questionNumber={17}
  question="이 모델은 어떤 상태에 해당하는가?"
  options={['과적합(overfitting)', '과소적합(underfitting)', '정상적으로 수렴한 상태', '그라디언트 소실 상태']}
  correctAnswer={1}
  explanation="훈련 손실은 계속 낮아지는데 검증 손실이 어느 시점부터 다시 올라가는 것은 모델이 훈련 데이터의 잡음까지 암기하기 시작했다는 과적합의 전형적인 신호입니다."
  optionExplanations={['훈련-검증 손실이 벌어지는 전형적인 과적합 패턴이므로 정답입니다.', '과소적합이라면 훈련 손실 자체가 충분히 낮아지지 않으므로 이 상황과 맞지 않아 틀렸습니다.', '검증 손실이 다시 상승하는 것은 정상 수렴이 아니라 틀렸습니다.', '그라디언트 소실은 손실 곡선 자체가 아니라 층별 그라디언트 크기로 판단하는 현상이라 틀렸습니다.']}
/>

### 문제 18

가중치 $w = 0.8$, 원래 손실의 그라디언트 $\frac{\partial L_0}{\partial w} = 0.05$, 학습률 $\eta = 0.2$, L2 정규화 강도 $\lambda = 0.1$이다.

<Quiz
  questionNumber={18}
  question="L2 정규화를 적용한 경사하강법 한 스텝 후 가중치 w의 값은?"
  options={['0.774', '0.79', '0.784', '0.826']}
  correctAnswer={1}
  explanation="L2 정규화의 갱신식은 w ← w - η*(dL0/dw + λ*w)입니다. 대입하면 w ← 0.8 - 0.2*(0.05 + 0.1*0.8) = 0.8 - 0.2*(0.05+0.08) = 0.8 - 0.2*0.13 = 0.8 - 0.026 = 0.774 입니다."
  optionExplanations={['정규화 항까지 포함해 정확히 계산한 값이므로 정답입니다.', '정규화 항(λw)을 빠뜨리고 0.8-0.2*0.05만 계산하면 0.79가 나와 틀렸습니다.', '원래 그라디언트 항을 빠뜨리고 0.8-0.2*0.1*0.8만 계산하면 0.784가 나와 틀렸습니다.', '빼는 대신 더하면(0.8+0.026) 0.826이 나와 틀렸습니다.']}
/>

### 문제 19

L1 정규화와 L2 정규화에 대한 설명 중 옳지 않은 것을 고르시오.

<Quiz
  questionNumber={19}
  question="L1 정규화와 L2 정규화에 대한 설명으로 옳지 않은 것은?"
  options={['L1 정규화는 가중치를 정확히 0으로 만들어 희소성을 유도하는 경향이 있다.', 'L2 정규화는 가중치를 0 근처로 고르게 축소하지만 정확히 0이 되는 경우는 드물다.', 'L1과 L2 모두 손실 함수에 벌점 항을 더해 가중치의 크기를 제한한다는 점은 같다.', 'L1 정규화의 미분값은 가중치의 크기에 비례하고, L2 정규화의 미분값은 가중치의 부호에만 의존한다.']}
  correctAnswer={4}
  explanation="실제로는 반대입니다. L2 정규화 항의 미분은 λw로 가중치 크기에 비례하고, L1 정규화 항의 미분은 λ*sign(w)로 가중치의 부호에만 의존하고 크기와는 무관합니다. 이 설명은 두 정규화의 미분 특성을 서로 바꿔 놓았으므로 옳지 않습니다."
  optionExplanations={['L1이 희소성을 유도한다는 옳은 설명입니다.', 'L2가 가중치를 고르게 축소한다는 옳은 설명입니다.', '두 정규화 모두 벌점 항을 더한다는 공통점을 짚은 옳은 설명입니다.', 'L1과 L2의 미분 특성이 서로 뒤바뀌어 있어 이 설명이 틀린 지점이며 정답입니다.']}
/>

### 문제 20

드롭아웃(dropout) 비율 $p = 0.3$을 적용한 은닉층에 노드가 10개 있다.

<Quiz
  questionNumber={20}
  question="학습 시 이 은닉층에서 평균적으로 활성 상태로 남는 노드 수는?"
  options={['7', '3', '5', '10']}
  correctAnswer={1}
  explanation="드롭아웃은 각 노드를 확률 p로 끄고 (1-p)의 확률로 살립니다. 노드 10개에 살아남을 확률 (1-0.3)=0.7을 곱하면 평균 활성 노드 수는 10*0.7=7개입니다."
  optionExplanations={['살아남을 확률 (1-p)를 정확히 곱한 계산이므로 정답입니다.', '꺼지는 노드 수(10*0.3=3)를 활성 노드 수로 착각하면 나오는 오답입니다.', 'p=0.5로 잘못 대입하면 5가 나와 틀렸습니다.', '드롭아웃을 적용하지 않은 경우의 노드 수라 틀렸습니다.']}
/>

### 문제 21

<Quiz
  questionNumber={21}
  question="조기 종료(early stopping)에서 학습을 멈추는 기준으로 가장 적절한 것은?"
  options={['검증 손실(validation loss)이 일정 기간(patience) 동안 개선되지 않을 때', '훈련 손실(training loss)이 정확히 0에 도달했을 때', '미리 정해 둔 에폭(epoch) 수에 도달하면 검증 손실과 무관하게 무조건', '가중치 값이 음수가 될 때']}
  correctAnswer={1}
  explanation="조기 종료는 검증 세트에 대한 손실을 지켜보다가, 일정 기간(patience) 동안 더 나아지지 않으면 그 시점의 가중치를 최종 모델로 채택하고 학습을 멈추는 기법입니다. 훈련 손실만으로 판단하면 과적합 시점을 놓치게 됩니다."
  optionExplanations={['검증 손실 정체를 기준으로 삼는 조기 종료의 정의 그대로이므로 정답입니다.', '훈련 손실이 0에 가까워지는 것은 오히려 과적합의 신호일 수 있어 멈춤 기준으로 부적절해 틀렸습니다.', '검증 손실을 아예 보지 않고 에폭 수만으로 멈추는 것은 조기 종료가 아니라 단순 고정 학습이라 틀렸습니다.', '가중치의 부호는 조기 종료 판단과 무관해 틀렸습니다.']}
/>

### 문제 22

정규화 기법에 대한 설명 중 옳지 않은 것을 고르시오.

<Quiz
  questionNumber={22}
  question="정규화 기법에 대한 설명으로 옳지 않은 것은?"
  options={['드롭아웃은 학습 시에만 적용하고, 추론(inference) 시에는 전체 노드를 사용한다.', 'L2 정규화는 가중치 감쇠(weight decay)라고도 부른다.', '조기 종료를 적용하려면 훈련 데이터와 별도인 검증 세트가 필요하다.', '정규화 기법을 여러 개 동시에 적용하면 항상 성능이 향상된다.']}
  correctAnswer={4}
  explanation="정규화 기법을 여러 개 함께 쓰면 과적합을 더 잘 억제할 수도 있지만, 지나치게 강하게 여러 겹으로 적용하면 오히려 모델이 충분히 학습하지 못하는 과소적합을 유발할 수 있습니다. 따라서 여러 기법을 함께 쓴다고 항상 성능이 향상된다고 단정할 수 없습니다."
  optionExplanations={['드롭아웃은 학습 시에만 노드를 끄고 추론 시에는 전체 노드를 쓴다는 옳은 설명입니다.', 'L2 정규화가 가중치 감쇠라는 다른 이름을 갖는다는 옳은 설명입니다.', '조기 종료가 검증 세트를 필요로 한다는 옳은 설명입니다.', '여러 정규화를 동시에 적용해도 항상 향상된다는 보장은 없으므로 이 설명이 틀린 지점이며 정답입니다.']}
/>

## 참고 자료

- Ian Goodfellow 외, Deep Learning (Deep Learning Book) — https://www.deeplearningbook.org
- 국가평생교육진흥원 독학학위제 — https://bdes.nile.or.kr
