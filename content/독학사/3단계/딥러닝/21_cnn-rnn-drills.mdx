---
title: '21. CNN·RNN·LSTM·GRU 유형 문제 80제'
description: 'CNN 출력 크기·파라미터 수 계산, 대표 CNN 구조 비교, RNN·LSTM·GRU 게이트 계산과 비교를 묻는 22문항 실전 유형 문제 모음'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 문제들은 국가평생교육진흥원 독학학위제 3단계 딥러닝 출제기준(CNN 연산·대표 구조, RNN·LSTM·GRU 영역)을 근거로 새로 구성한 **재구성 문항**입니다. 실제 기출 문항을 그대로 옮긴 것이 아니며, "80제"라는 제목은 11·12·13편에서 다루는 이 영역 전체의 문제 은행 규모를 뜻합니다. 이 문서에는 그중 **출제 빈도가 높은 핵심 유형 22문항**을 골라 전 과정 해설과 함께 싣습니다.
</Callout>

## 문항 구성

| 영역 | 문항 번호 | 문항 수 | 관련 편 |
| --- | --- | --- | --- |
| CNN 연산·크기 계산 | 1–5 | 5 | 11편 |
| 대표 CNN 구조 비교 | 6–10 | 5 | 12편 |
| RNN 구조와 한계 | 11–14 | 4 | 13편 |
| LSTM·GRU 게이트 계산·비교 | 15–22 | 8 | 13편 |

## 영역 1: CNN 연산·크기 계산 (1–5)

가로세로 32, 채널 3(즉 32×32×3)인 컬러 이미지에 5×5 필터 16개를 스트라이드(stride) 1, 패딩(padding) 0으로 합성곱(convolution)한다.

### 문제 1

<Quiz
  questionNumber={1}
  question="이 합성곱을 거친 특징 맵(feature map)의 가로(세로) 크기는?"
  options={['28', '27', '32', '16']}
  correctAnswer={1}
  explanation="합성곱 출력 크기 공식은 O = floor((W - F + 2P) / S) + 1 입니다. 대입하면 O = (32 - 5 + 2*0)/1 + 1 = 27/1 + 1 = 27 + 1 = 28 입니다."
  optionExplanations={['공식대로 정확히 계산한 값이므로 정답입니다.', '마지막에 +1을 빠뜨리면 27이 나와 틀렸습니다.', '필터 크기의 영향을 아예 반영하지 않으면 입력 크기 32가 그대로 나와 틀렸습니다.', '풀링(pooling)의 절반 축소와 혼동하면 16이 나와 틀렸습니다.']}
/>

### 문제 2

<Quiz
  questionNumber={2}
  question="이 합성곱층의 학습 파라미터(가중치+편향) 개수는?"
  options={['1216', '1200', '416', '76']}
  correctAnswer={1}
  explanation="필터 하나의 파라미터 수는 (필터 가로 × 필터 세로 × 입력 채널 수 + 편향 1개) = 5*5*3+1 = 76개입니다. 필터가 16개이므로 전체 파라미터는 76*16 = 1216개입니다."
  optionExplanations={['입력 채널과 편향을 모두 반영한 정확한 계산이므로 정답입니다.', '편향 1개씩을 빠뜨리면 75*16=1200이 나와 틀렸습니다.', '입력 채널 수(3)를 반영하지 않으면 (5*5+1)*16=416이 나와 틀렸습니다.', '필터 1개 분량만 계산하고 필터 개수(16)를 곱하지 않으면 76에서 멈춰 틀렸습니다.']}
/>

### 문제 3

같은 5×5 필터를 스트라이드 1, 패딩(padding) 2로 적용한다.

<Quiz
  questionNumber={3}
  question="패딩을 2로 바꾸면 출력 특징 맵의 크기는?"
  options={['32 (입력과 동일)', '28', '36', '30']}
  correctAnswer={1}
  explanation="O = (32 - 5 + 2*2)/1 + 1 = (32-5+4)+1 = 31+1 = 32 입니다. 패딩을 (F-1)/2 = (5-1)/2 = 2로 맞추면 출력 크기가 입력과 같아지는데, 이를 세임 패딩(same padding)이라 부릅니다."
  optionExplanations={['세임 패딩 조건을 만족해 입력과 같은 크기가 나오므로 정답입니다.', '패딩을 0으로 착각하고 계산하면 28이 나와 틀렸습니다.', '패딩 항을 2배로 잘못 더하면 36이 나와 틀렸습니다.', '패딩을 1로 착각하면 30이 나와 틀렸습니다.']}
/>

### 문제 4

문제 1의 결과물(28×28×16)에 2×2 최대 풀링(max pooling), 스트라이드 2를 적용한다.

<Quiz
  questionNumber={4}
  question="풀링을 거친 후의 크기와 이 풀링 층의 학습 파라미터 개수를 순서대로 짝지은 것은?"
  options={['14×14×16, 파라미터 0개', '14×14×16, 파라미터 1216개', '28×28×16, 파라미터 0개', '14×14×8, 파라미터 0개']}
  correctAnswer={1}
  explanation="풀링의 출력 크기는 O = (28-2)/2+1 = 13+1 = 14이므로 14×14이고, 채널 수(16)는 풀링으로 바뀌지 않습니다. 풀링은 학습되는 가중치가 없는 고정 연산이므로 파라미터는 0개입니다."
  optionExplanations={['크기 계산과 파라미터 0개라는 풀링의 성질을 모두 맞혔으므로 정답입니다.', '풀링에도 합성곱처럼 파라미터가 있다고 착각한 오답입니다.', '풀링을 적용하지 않은 크기를 그대로 쓴 오답입니다.', '풀링이 채널 수까지 절반으로 줄인다고 착각한 오답입니다.']}
/>

### 문제 5

완전연결(fully connected, FC)층과 합성곱(convolutional)층의 차이에 대한 설명 중 옳지 않은 것을 고르시오.

<Quiz
  questionNumber={5}
  question="FC층과 합성곱층의 차이에 대한 설명으로 옳지 않은 것은?"
  options={['합성곱층은 하나의 필터를 이미지 전체 위치에서 공유(weight sharing)하므로 FC층보다 파라미터 수가 훨씬 적다.', '32×32×3 이미지를 100개 노드의 FC층에 바로 연결하면 파라미터가 30만 개를 넘어갈 수 있다.', '합성곱층은 필터가 위치를 이동하며 같은 가중치를 재사용하므로 이미지 내 위치가 달라져도 같은 특징을 인식할 수 있다(평행이동 불변성에 가까운 성질).', 'FC층은 입력의 공간적 구조(가까운 픽셀끼리의 관계)를 합성곱층보다 더 잘 보존한다.']}
  correctAnswer={4}
  explanation="FC층은 입력을 1차원으로 펼쳐(flatten) 모든 픽셀을 독립적인 입력으로 취급하므로 오히려 픽셀 간의 공간적 인접 관계 정보를 잃어버립니다. 공간 구조를 보존하는 쪽은 필터가 인접한 픽셀들을 그대로 훑는 합성곱층입니다. 따라서 이 설명은 반대로 되어 있어 옳지 않습니다."
  optionExplanations={['가중치 공유로 파라미터가 줄어든다는 옳은 설명입니다.', '3072개 입력을 100개 노드에 완전연결하면 3072*100+100=307300개로 30만 개를 넘는다는 옳은 계산입니다.', '가중치 공유가 위치 이동에 강인한 특징 인식을 가능하게 한다는 옳은 설명입니다.', '공간 구조 보존은 합성곱층의 특징인데 이를 FC층의 특징이라고 반대로 서술해 틀린 지점이며 정답입니다.']}
/>

## 영역 2: 대표 CNN 구조 비교 (6–10)

### 문제 6

<Quiz
  questionNumber={6}
  question="LeNet에 대한 설명으로 가장 적절한 것은?"
  options={['합성곱층과 풀링층을 반복해 쌓고 마지막에 완전연결층을 두는 구조로, 손글씨 숫자 인식 등 초기 이미지 인식 문제에 사용된 대표적인 초기 CNN이다.', '최초로 어텐션(attention) 메커니즘을 도입한 CNN이다.', '100개가 넘는 층을 쌓아 잔차 연결(residual connection)로 그라디언트 소실을 해결한 구조이다.', '3×3 필터만 반복해서 쌓아 깊은 네트워크를 구성한 구조이다.']}
  correctAnswer={1}
  explanation="LeNet은 합성곱-풀링을 반복한 뒤 완전연결층으로 분류하는 초기 CNN 구조로, 손글씨 숫자 인식에 쓰인 것으로 널리 알려져 있습니다. 어텐션은 Transformer, 깊은 잔차 구조는 ResNet, 3×3 필터 반복은 VGG의 특징입니다."
  optionExplanations={['LeNet의 구조와 대표 용도를 정확히 설명했으므로 정답입니다.', '어텐션 도입은 훨씬 이후의 Transformer 계열 특징이라 틀렸습니다.', '100층 이상의 잔차 연결 구조는 ResNet의 특징이라 틀렸습니다.', '3×3 필터만 반복해서 쌓는 것은 VGG의 특징이라 틀렸습니다.']}
/>

### 문제 7

AlexNet에 대한 설명 중 옳지 않은 것을 고르시오.

<Quiz
  questionNumber={7}
  question="AlexNet에 대한 설명으로 옳지 않은 것은?"
  options={['ImageNet 이미지 분류 대회에서 딥러닝 기반 CNN의 성능을 크게 부각시킨 구조로 꼽힌다.', '드롭아웃(dropout)을 도입해 완전연결층의 과적합을 줄였다.', '시그모이드 대신 ReLU 활성화 함수를 사용해 학습 속도를 높였다.', '시그모이드 활성화 함수를 도입해 학습 속도를 크게 높인 것이 핵심 기여이다.']}
  correctAnswer={4}
  explanation="AlexNet의 핵심 기여 중 하나는 기존에 널리 쓰이던 시그모이드나 tanh 대신 ReLU를 활성화 함수로 채택해 그라디언트 소실을 줄이고 학습 속도를 높인 것입니다. 시그모이드를 도입했다는 설명은 사실과 반대이므로 옳지 않습니다."
  optionExplanations={['AlexNet이 CNN 기반 이미지 분류의 성능을 크게 끌어올렸다는 옳은 설명입니다.', '드롭아웃 도입이 AlexNet의 실제 기여 중 하나라는 옳은 설명입니다.', 'ReLU 채택이 AlexNet의 실제 기여라는 옳은 설명입니다.', '시그모이드를 도입했다는 설명은 실제로는 반대(ReLU 도입)이므로 틀린 지점이며 정답입니다.']}
/>

### 문제 8

<Quiz
  questionNumber={8}
  question="VGG가 큰 필터(예: 7×7) 대신 3×3 필터를 여러 번 쌓는 방식을 택한 주된 이유로 가장 적절한 것은?"
  options={['작은 필터를 여러 층 쌓으면 같은 수용 영역(receptive field)을 더 적은 파라미터와 더 많은 비선형 활성화 함수로 표현할 수 있기 때문이다.', '3×3 필터가 계산량이 항상 더 크기 때문에 모델을 더 무겁게 만들기 위해서이다.', '3×3 필터를 쓰면 이미지 크기가 줄어들지 않기 때문이다.', '3×3 필터는 편향(bias) 파라미터가 없기 때문이다.']}
  correctAnswer={1}
  explanation="3×3 필터 두 개를 쌓으면 5×5 필터 하나와 같은 수용 영역을 커버하면서도 파라미터 수는 더 적고(3*3*2=18 대 5*5=25, 채널 고려 시 차이는 더 커짐), 활성화 함수를 두 번 거치므로 비선형성이 늘어 표현력이 좋아집니다. 이것이 VGG가 작은 필터를 반복해 쌓은 이유입니다."
  optionExplanations={['수용 영역 유지, 파라미터 절감, 비선형성 증가라는 실제 이유를 정확히 짚었으므로 정답입니다.', '작은 필터를 여러 번 쓰는 것은 오히려 파라미터를 줄이려는 목적이라 이 설명은 틀렸습니다.', '패딩을 어떻게 주느냐에 따라 크기 유지 여부가 결정되지, 필터 크기 자체가 크기 유지를 보장하지 않아 틀렸습니다.', '필터 크기와 편향 유무는 무관한 설계 선택이라 틀렸습니다.']}
/>

### 문제 9

<Quiz
  questionNumber={9}
  question="ResNet의 잔차 연결(residual connection, skip connection)이 해결하고자 한 주된 문제는?"
  options={['네트워크가 매우 깊어질 때 그라디언트가 소실되거나 오히려 성능이 떨어지는 문제(degradation)를 완화하는 것', '이미지의 색상 정보를 흑백으로 변환하는 것', '학습 데이터의 개수를 인위적으로 늘리는 것', '드롭아웃을 대체해 정규화 효과를 완전히 없애는 것']}
  correctAnswer={1}
  explanation="ResNet은 입력을 몇 개 층 건너뛰어 그대로 더해주는 잔차 연결을 도입해, 매우 깊은 네트워크에서도 그라디언트가 지름길(shortcut)을 통해 앞쪽 층까지 잘 전달되게 하고, 층을 깊게 쌓아도 오히려 성능이 떨어지는 열화(degradation) 문제를 완화했습니다."
  optionExplanations={['잔차 연결이 그라디언트 소실·성능 열화를 완화한다는 옳은 설명이므로 정답입니다.', '색상 변환은 CNN 구조 설계와 무관한 전처리 작업이라 틀렸습니다.', '데이터 개수를 늘리는 것은 10편의 데이터 증강에 해당하는 별개의 기법이라 틀렸습니다.', '잔차 연결은 정규화를 없애는 장치가 아니라 그라디언트 흐름을 돕는 구조적 장치라 틀렸습니다.']}
/>

### 문제 10

<Quiz
  questionNumber={10}
  question="32×32×3 이미지를 1차원으로 펼쳐(flatten) 100개 노드의 완전연결층에 바로 연결할 때의 파라미터(가중치+편향) 개수는?"
  options={['307300', '3200', '3072', '300000']}
  correctAnswer={1}
  explanation="펼친 입력 벡터의 길이는 32*32*3=3072입니다. 완전연결층 파라미터는 (입력 개수 × 출력 노드 수) + 출력 노드 수(편향) = 3072*100+100 = 307200+100 = 307300개입니다. 이는 문제 2에서 구한 합성곱층의 1216개와 비교하면 250배가 넘는 규모로, 가중치 공유가 없는 FC층이 얼마나 많은 파라미터를 필요로 하는지 보여줍니다."
  optionExplanations={['입력 크기와 편향을 모두 반영한 정확한 계산이므로 정답입니다.', '입력 크기를 32*100으로 잘못 계산하면 3200이 나와 틀렸습니다.', '편향과 출력 노드 곱을 빠뜨리고 입력 크기만 쓰면 3072가 나와 틀렸습니다.', '편향 100개를 빠뜨리면 307200인데, 반올림하며 300000으로 뭉뚱그리면 틀렸습니다.']}
/>

## 영역 3: RNN 구조와 한계 (11–14)

### 문제 11

<Quiz
  questionNumber={11}
  question="RNN(순환신경망)이 일반적인 순전파 신경망(FFNN)과 구조적으로 가장 크게 다른 점은?"
  options={['이전 시점의 은닉 상태(hidden state)를 현재 시점의 입력과 함께 사용하는 순환 연결이 있어, 같은 가중치를 모든 시점에서 공유한다.', '활성화 함수로 반드시 ReLU만 사용해야 한다.', '입력을 반드시 이미지 형태로만 받을 수 있다.', '역전파를 전혀 사용하지 않고 학습한다.']}
  correctAnswer={1}
  explanation="RNN은 매 시점(time step)마다 이전 은닉 상태를 입력으로 함께 받아 다음 은닉 상태를 계산하고, 이 계산에 쓰이는 가중치 행렬을 모든 시점에서 동일하게 재사용(파라미터 공유)합니다. 이것이 시퀀스(sequence, 순서가 있는 데이터)를 처리할 수 있게 하는 핵심 구조입니다."
  optionExplanations={['순환 연결과 시점 간 가중치 공유라는 RNN의 핵심 구조를 정확히 짚었으므로 정답입니다.', 'RNN도 시그모이드나 tanh 등 다양한 활성화 함수를 쓸 수 있어 틀렸습니다.', 'RNN은 텍스트·음성 등 순서가 있는 데이터 전반에 쓰이며 이미지 전용이 아니라 틀렸습니다.', 'RNN도 시간에 따른 역전파(BPTT)로 학습하므로 틀렸습니다.']}
/>

### 문제 12

<Quiz
  questionNumber={12}
  question="RNN의 시간에 따른 역전파(BPTT, Backpropagation Through Time)에서 시퀀스 길이가 길어질수록 커지는 문제로 가장 적절한 것은?"
  options={['같은 가중치 행렬이 시점마다 반복해서 곱해지므로, 그 값의 크기에 따라 그라디언트가 소실되거나 폭발할 위험이 커진다.', '입력 데이터의 개수가 자동으로 줄어든다.', '모델의 파라미터 개수가 시퀀스 길이에 비례해 늘어난다.', '활성화 함수가 자동으로 ReLU에서 시그모이드로 바뀐다.']}
  correctAnswer={1}
  explanation="BPTT는 매 시점의 그라디언트를 계산할 때 같은 가중치 행렬을 반복해서 곱하는 구조입니다. 이 행렬의 고유값 크기에 따라 여러 번 곱해진 값이 0으로 수렴(그라디언트 소실)하거나 매우 커질(그라디언트 폭발) 수 있고, 시퀀스가 길수록 곱해지는 횟수가 많아져 이 문제가 심해집니다."
  optionExplanations={['가중치 반복 곱셈에 의한 소실·폭발 위험을 정확히 설명했으므로 정답입니다.', 'BPTT는 시퀀스 길이와 무관하게 주어진 데이터를 그대로 사용하므로 틀렸습니다.', 'RNN은 시점마다 같은 가중치를 공유하므로 파라미터 개수는 시퀀스 길이와 무관해 틀렸습니다.', '활성화 함수는 설계 시 정해지는 것이지 시퀀스 길이에 따라 자동으로 바뀌지 않아 틀렸습니다.']}
/>

### 문제 13

<Quiz
  questionNumber={13}
  question="RNN이 긴 시퀀스에서 초반 정보를 잘 기억하지 못하는 장기 의존성(long-term dependency) 문제를 완화하기 위해 고안된 구조로 가장 적절한 것은?"
  options={['게이트(gate) 구조를 도입해 정보를 선택적으로 기억·삭제하는 LSTM이나 GRU', '입력층의 노드 수를 두 배로 늘린 기본 RNN', '풀링층을 추가한 기본 RNN', '활성화 함수를 계단 함수로 바꾼 기본 RNN']}
  correctAnswer={1}
  explanation="LSTM과 GRU는 게이트 구조를 이용해 어떤 정보를 오래 기억하고 어떤 정보를 잊을지 학습을 통해 조절할 수 있게 해, 기본 RNN보다 장기 의존성을 훨씬 잘 다룹니다."
  optionExplanations={['게이트 구조 도입이 장기 의존성 문제를 완화하는 실제 해법이므로 정답입니다.', '입력층 노드 수 증가는 장기 의존성 문제와 직접적인 관련이 없어 틀렸습니다.', '풀링은 CNN에서 주로 쓰이는 연산으로 RNN의 장기 의존성 문제 해결과 무관해 틀렸습니다.', '계단 함수는 미분이 대부분 0이라 오히려 그라디언트 기반 학습에 부적합해 틀렸습니다.']}
/>

### 문제 14

<Quiz
  questionNumber={14}
  question="그라디언트 폭발(exploding gradient) 문제를 완화하기 위해 실무에서 흔히 쓰는 기법은?"
  options={['그라디언트 클리핑(gradient clipping)으로 그라디언트의 크기를 일정 범위로 제한한다.', '학습률을 무한히 크게 키운다.', '모든 활성화 함수를 계단 함수로 바꾼다.', '검증 데이터셋을 삭제한다.']}
  correctAnswer={1}
  explanation="그라디언트 클리핑은 그라디언트 벡터의 크기(노름, norm)가 일정 임계값을 넘으면 그 크기를 임계값으로 잘라내는 기법으로, 그라디언트 폭발로 인한 발산을 막는 데 널리 쓰입니다."
  optionExplanations={['그라디언트 클리핑이 실제로 쓰이는 표준적인 대응책이므로 정답입니다.', '학습률을 키우면 오히려 그라디언트 폭발이 더 심해질 수 있어 틀렸습니다.', '계단 함수는 미분이 거의 0이라 학습 자체가 안 되므로 폭발 문제의 해법이 아니라 틀렸습니다.', '검증 데이터셋 삭제는 모델 평가만 어렵게 할 뿐 그라디언트 폭발과는 무관해 틀렸습니다.']}
/>

## 영역 4: LSTM·GRU 게이트 계산·비교 (15–22)

### 문제 15

<Quiz
  questionNumber={15}
  question="LSTM의 세 가지 게이트와 그 역할을 짝지은 것으로 옳은 것은?"
  options={['망각 게이트(forget gate)는 이전 셀 상태에서 버릴 정보를 정하고, 입력 게이트(input gate)는 새로 추가할 정보를 정하며, 출력 게이트(output gate)는 셀 상태 중 은닉 상태로 내보낼 부분을 정한다.', '망각 게이트는 새 정보를 추가하고, 입력 게이트는 정보를 버리며, 출력 게이트는 학습률을 조절한다.', 'LSTM에는 게이트가 하나뿐이며 그 이름은 순환 게이트이다.', '출력 게이트는 다음 시점의 입력 데이터를 직접 수정한다.']}
  correctAnswer={1}
  explanation="LSTM은 망각 게이트(이전 셀 상태 중 버릴 것 결정), 입력 게이트(새 후보 정보 중 셀 상태에 더할 것 결정), 출력 게이트(셀 상태 중 현재 은닉 상태로 내보낼 것 결정)라는 세 게이트로 셀 상태(cell state)를 조절합니다."
  optionExplanations={['세 게이트의 역할을 정확히 짝지었으므로 정답입니다.', '망각 게이트와 입력 게이트의 역할을 서로 바꿔 설명해 틀렸습니다.', 'LSTM은 세 개의 게이트를 가지므로 하나뿐이라는 설명은 틀렸습니다.', '출력 게이트는 은닉 상태를 만드는 데 관여하지 입력 데이터 자체를 수정하지 않아 틀렸습니다.']}
/>

### 문제 16

어떤 LSTM 셀에서 망각 게이트 출력 $f_t = 0.8$, 이전 셀 상태 $C_{t-1} = 5$, 입력 게이트 출력 $i_t = 0.6$, 후보 셀 상태 $\tilde{C}_t = 2$이다.

<Quiz
  questionNumber={16}
  question="새로운 셀 상태 C_t = f_t * C_(t-1) + i_t * (틸다 C)_t 의 값은?"
  options={['5.2', '4.6', '7', '1.2']}
  correctAnswer={1}
  explanation="공식에 값을 대입하면 C_t = 0.8*5 + 0.6*2 = 4 + 1.2 = 5.2 입니다. 망각 게이트가 이전 정보를 얼마나 남길지, 입력 게이트가 새 정보를 얼마나 더할지를 각각 곱해서 더하는 구조입니다."
  optionExplanations={['두 항을 정확히 계산해 더한 값이므로 정답입니다.', '입력 게이트 항(1.2)을 빠뜨리고 망각 게이트 항(4)만 반올림하듯 계산하면 나오는 오답입니다.', '두 게이트 값을 곱하지 않고 더한 뒤(0.8+0.6=1.4) 다른 값과 섞어 계산하면 나올 수 있는 오답입니다.', '입력 게이트 항(1.2)만 남기고 망각 게이트 항을 빠뜨리면 나오는 오답입니다.']}
/>

### 문제 17

문제 16에서 구한 셀 상태에 대해 $\tanh(C_t) = 0.9$로 주어졌고, 출력 게이트 출력 $o_t = 0.5$이다.

<Quiz
  questionNumber={17}
  question="이 시점의 은닉 상태 h_t = o_t * tanh(C_t)의 값은?"
  options={['0.45', '1.4', '0.9', '0.5']}
  correctAnswer={1}
  explanation="공식에 대입하면 h_t = 0.5*0.9 = 0.45 입니다. 출력 게이트는 셀 상태를 tanh로 압축한 값 중 얼마만큼을 실제 은닉 상태로 내보낼지를 결정합니다."
  optionExplanations={['두 값을 정확히 곱한 결과이므로 정답입니다.', '곱셈 대신 덧셈을 하면(0.5+0.9) 1.4가 나와 틀렸습니다.', 'tanh(C_t) 값을 그대로 은닉 상태로 착각하면 0.9가 나와 틀렸습니다.', '출력 게이트 값을 그대로 은닉 상태로 착각하면 0.5가 나와 틀렸습니다.']}
/>

### 문제 18

<Quiz
  questionNumber={18}
  question="GRU(Gated Recurrent Unit)에 대한 설명으로 옳은 것은?"
  options={['업데이트 게이트와 리셋 게이트라는 두 게이트만 사용하며, LSTM과 달리 별도의 셀 상태 없이 은닉 상태 하나로 정보를 관리한다.', 'GRU는 게이트를 네 개 사용해 LSTM보다 구조가 더 복잡하다.', 'GRU는 순환 구조가 전혀 없는 순전파 신경망이다.', 'GRU는 망각 게이트, 입력 게이트, 출력 게이트를 모두 LSTM과 동일하게 사용한다.']}
  correctAnswer={1}
  explanation="GRU는 업데이트 게이트(update gate)와 리셋 게이트(reset gate) 두 가지만 사용하고, LSTM의 셀 상태와 은닉 상태 구분 없이 은닉 상태 하나로 정보를 유지·갱신합니다. 이 때문에 LSTM보다 구조가 단순하고 파라미터 수가 적습니다."
  optionExplanations={['GRU의 게이트 구성과 셀 상태 통합이라는 핵심 특징을 정확히 설명했으므로 정답입니다.', 'GRU는 두 게이트만 쓰므로 네 개라는 설명은 틀렸습니다.', 'GRU도 RNN 계열로 순환 구조를 가지므로 틀렸습니다.', 'GRU는 LSTM의 세 게이트 구성과 다른 두 게이트 구성을 쓰므로 틀렸습니다.']}
/>

### 문제 19

<Quiz
  questionNumber={19}
  question="LSTM과 GRU를 비교한 설명으로 옳지 않은 것은?"
  options={['GRU는 일반적으로 LSTM보다 게이트 수가 적어 학습 파라미터 수도 더 적은 경향이 있다.', 'LSTM은 셀 상태와 은닉 상태를 구분해서 관리하지만, GRU는 이 둘을 하나의 은닉 상태로 합쳐 관리한다.', 'GRU는 파라미터가 항상 LSTM보다 많아서 계산이 더 오래 걸린다.', '두 구조 모두 게이트를 이용해 정보를 선택적으로 기억·삭제함으로써 기본 RNN보다 장기 의존성을 잘 다룬다.']}
  correctAnswer={3}
  explanation="GRU는 게이트 수가 LSTM보다 적고(2개 대 3개) 셀 상태도 따로 두지 않으므로, 일반적으로 파라미터 수가 LSTM보다 적고 계산도 더 가볍습니다. 항상 더 많다는 설명은 사실과 반대이므로 옳지 않습니다."
  optionExplanations={['게이트 수가 적어 파라미터도 적은 경향이 있다는 옳은 설명입니다.', '셀 상태와 은닉 상태의 통합 여부 차이를 정확히 설명한 옳은 내용입니다.', 'GRU가 LSTM보다 파라미터가 항상 많다는 설명은 실제와 반대이므로 이 설명이 틀린 지점이며 정답입니다.', '두 구조 모두 게이트 기반으로 장기 의존성을 개선한다는 옳은 설명입니다.']}
/>

### 문제 20

<Quiz
  questionNumber={20}
  question="LSTM 셀 상태(cell state)의 역할로 가장 적절한 것은?"
  options={['시점을 거치며 정보를 비교적 그대로 유지·전달할 수 있는 별도의 통로 역할을 해, 장기 의존성 정보를 보존하는 데 기여한다.', '입력 데이터를 이미지로 변환하는 역할을 한다.', '학습률을 시점마다 자동으로 조정하는 역할을 한다.', '드롭아웃을 대체해 과적합을 막는 역할을 한다.']}
  correctAnswer={1}
  explanation="셀 상태는 게이트들의 곱셈·덧셈 연산만으로 갱신되며 활성화 함수의 반복 적용을 거치지 않기 때문에, 기본 RNN의 은닉 상태보다 정보가 덜 왜곡된 채로 먼 시점까지 전달될 수 있습니다. 이것이 LSTM이 장기 의존성을 잘 다루는 핵심 이유입니다."
  optionExplanations={['셀 상태의 정보 보존 역할을 정확히 설명했으므로 정답입니다.', '셀 상태는 데이터 형식 변환과 무관한 내부 상태값이라 틀렸습니다.', '학습률 조정은 08편의 최적화 알고리즘이 다루는 별개의 주제라 틀렸습니다.', '드롭아웃은 별도의 정규화 기법이며 셀 상태의 기능이 아니라 틀렸습니다.']}
/>

### 문제 21

<Quiz
  questionNumber={21}
  question="RNN·LSTM·GRU 계열 모델에 대한 설명으로 옳지 않은 것은?"
  options={['기본 RNN은 은닉 상태 하나만으로 정보를 전달하기 때문에 시퀀스가 길어질수록 초반 정보가 희석되기 쉽다.', 'LSTM과 GRU는 게이트를 도입해 기본 RNN보다 장기 의존성 문제에 강하다.', '기본 RNN, LSTM, GRU 모두 시점마다 서로 다른 가중치 행렬을 새로 학습해야 한다.', 'LSTM·GRU 모두 시그모이드 함수를 게이트 값 계산에 사용해 0과 1 사이의 값으로 정보의 비율을 조절한다.']}
  correctAnswer={3}
  explanation="RNN 계열 모델의 핵심 특징 중 하나는 시점마다 가중치를 새로 두지 않고, 모든 시점에서 동일한 가중치 행렬을 공유(재사용)한다는 점입니다. 시점마다 새로운 가중치를 학습한다는 설명은 이 원칙에 반하므로 옳지 않습니다."
  optionExplanations={['기본 RNN이 장기 정보를 희석시키기 쉽다는 옳은 설명입니다.', 'LSTM·GRU가 장기 의존성에 강하다는 옳은 설명입니다.', '모든 시점에서 같은 가중치를 공유한다는 RNN 계열의 원칙에 어긋나는 설명이라 이 문항이 틀린 지점이며 정답입니다.', '게이트 값 계산에 시그모이드를 쓴다는 옳은 설명입니다.']}
/>

### 문제 22

<Quiz
  questionNumber={22}
  question="LSTM의 게이트 값을 계산할 때 시그모이드 함수를 사용하는 이유로 가장 적절한 것은?"
  options={['출력을 0과 1 사이로 제한해 정보를 얼마나 통과시킬지를 비율(0퍼센트에서 100퍼센트 사이)로 자연스럽게 표현할 수 있기 때문이다.', '시그모이드는 출력이 항상 정수이기 때문이다.', '시그모이드를 쓰면 그라디언트가 항상 1로 일정하게 유지되기 때문이다.', '시그모이드는 미분이 필요 없는 함수이기 때문이다.']}
  correctAnswer={1}
  explanation="게이트는 셀 상태나 입력 정보 중 얼마만큼을 통과시킬지를 결정하는 역할을 하는데, 시그모이드 함수의 출력 범위 (0,1)이 정확히 이 비율 개념과 맞아떨어집니다. 값이 0에 가까우면 거의 차단, 1에 가까우면 거의 통과를 의미합니다."
  optionExplanations={['시그모이드의 출력 범위와 게이트의 비율 조절 역할이 맞아떨어진다는 옳은 설명이므로 정답입니다.', '시그모이드 출력은 0과 1 사이의 연속값이지 정수가 아니라 틀렸습니다.', '시그모이드의 미분값은 06편에서 다뤘듯 σ(1-σ)로 일정하지 않고 0에서 0.25 사이에서 변하므로 틀렸습니다.', 'LSTM은 역전파를 위해 시그모이드의 미분을 반드시 계산해야 하므로 미분이 필요 없다는 설명은 틀렸습니다.']}
/>

## 참고 자료

- Stanford CS231n: Convolutional Neural Networks for Visual Recognition — https://cs231n.stanford.edu
- Ian Goodfellow 외, Deep Learning (Deep Learning Book) — https://www.deeplearningbook.org
