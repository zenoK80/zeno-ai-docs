---
title: '23. 생성 모델·전이학습·평가지표 유형 문제'
description: 'AE·VAE·GAN 구조와 손실 비교, 전이학습 전략, 혼동행렬·F1·ROC/AUC·회귀 지표 계산과 해석을 묻는 22문항 실전 유형 문제 모음'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 문제들은 국가평생교육진흥원 독학학위제 3단계 딥러닝 출제기준(생성 모델·전이학습·평가지표 영역)을 근거로 새로 구성한 **재구성 문항**입니다. 실제 기출 문항을 그대로 옮긴 것이 아니며, 16·17·18·19편에서 다루는 이 영역의 핵심 유형 22문항을 전 과정 해설과 함께 싣습니다.
</Callout>

## 문항 구성

| 영역 | 문항 번호 | 문항 수 | 관련 편 |
| --- | --- | --- | --- |
| 오토인코더(AE)·변분 오토인코더(VAE) | 1–5 | 5 | 16편 |
| GAN 구조·훈련 함정 | 6–10 | 5 | 17편 |
| 전이학습·미세조정 | 11–14 | 4 | 18편 |
| 평가지표 계산과 해석 | 15–22 | 8 | 19편 |

## 영역 1: 오토인코더(AE)·변분 오토인코더(VAE) (1–5)

### 문제 1

<Quiz
  questionNumber={1}
  question="오토인코더(autoencoder)의 기본 구조에 대한 설명으로 가장 적절한 것은?"
  options={['입력을 저차원 잠재공간(latent space)으로 압축하는 인코더와, 그 압축된 표현으로부터 원래 입력을 복원하는 디코더로 구성된다.', '라벨이 있는 데이터에서만 학습할 수 있는 지도학습 전용 구조이다.', '인코더 없이 디코더만으로 이루어진 구조이다.', '입력과 출력의 차원이 항상 서로 달라야 하는 구조이다.']}
  correctAnswer={1}
  explanation="오토인코더는 인코더가 입력을 저차원 잠재 벡터로 압축하고, 디코더가 그 잠재 벡터로부터 원래 입력을 최대한 비슷하게 복원하도록 함께 학습되는 비지도학습 신경망입니다."
  optionExplanations={['인코더-디코더 구조와 잠재공간 압축이라는 오토인코더의 핵심을 정확히 설명했으므로 정답입니다.', '오토인코더는 입력 자체를 목표로 삼아 학습하는 비지도학습 방식이라 틀렸습니다.', '인코더가 없으면 잠재 표현을 만들 수 없어 오토인코더의 구조가 성립하지 않아 틀렸습니다.', '오토인코더는 보통 입력과 출력이 같은 차원이 되도록 복원을 목표로 하므로 틀렸습니다.']}
/>

### 문제 2

<Quiz
  questionNumber={2}
  question="오토인코더의 학습 목표인 재구성 손실(reconstruction loss)이 하는 역할로 가장 적절한 것은?"
  options={['디코더가 복원한 출력과 원래 입력 사이의 차이를 줄이도록 인코더와 디코더를 함께 학습시키는 목적함수 역할을 한다.', '입력 데이터에 라벨을 자동으로 붙여주는 역할을 한다.', '잠재공간의 차원 수를 자동으로 늘려주는 역할을 한다.', '학습률을 자동으로 조절해 주는 역할을 한다.']}
  correctAnswer={1}
  explanation="재구성 손실은 보통 평균제곱오차(MSE)나 교차엔트로피로 정의되며, 복원된 출력이 원본 입력과 얼마나 다른지를 측정해 그 차이를 줄이는 방향으로 인코더·디코더의 가중치를 갱신하는 데 사용됩니다."
  optionExplanations={['원본과 복원 출력의 차이를 줄이는 목적함수라는 실제 역할을 정확히 설명했으므로 정답입니다.', '오토인코더는 비지도학습이며 손실 함수가 라벨을 만들어주는 것이 아니라 틀렸습니다.', '잠재공간 차원은 설계자가 미리 정하는 하이퍼파라미터이지 손실 함수가 바꾸는 값이 아니라 틀렸습니다.', '학습률은 별도의 하이퍼파라미터이며 재구성 손실 자체가 학습률을 조절하지 않아 틀렸습니다.']}
/>

### 문제 3

<Quiz
  questionNumber={3}
  question="변분 오토인코더(Variational Autoencoder, VAE)가 기본 오토인코더와 가장 크게 다른 점으로 적절한 것은?"
  options={['잠재 벡터를 하나의 고정된 값이 아니라 평균과 분산을 갖는 확률분포로 표현하고, 그 분포에서 표본을 뽑아 디코더에 넣는다.', 'VAE는 디코더 없이 인코더만으로 이루어진다.', 'VAE는 반드시 지도학습 라벨이 있어야만 학습할 수 있다.', 'VAE는 재구성 손실을 전혀 사용하지 않는다.']}
  correctAnswer={1}
  explanation="VAE는 인코더가 입력마다 잠재변수의 평균 mu와 표준편차 sigma를 출력하게 하고, 이 정규분포에서 표본을 뽑아 디코더에 전달합니다. 이 확률적 잠재변수 덕분에 잠재공간에서 새로운 표본을 생성하는 것이 자연스러워집니다."
  optionExplanations={['확률분포 기반 잠재변수라는 VAE의 핵심 차이를 정확히 설명했으므로 정답입니다.', 'VAE도 기본 오토인코더처럼 복원을 담당하는 디코더를 가지므로 틀렸습니다.', 'VAE는 오토인코더와 마찬가지로 비지도학습으로 학습되므로 틀렸습니다.', 'VAE도 복원 품질을 위한 재구성 손실 항을 목적함수에 포함하므로 틀렸습니다.']}
/>

### 문제 4

<Quiz
  questionNumber={4}
  question="VAE의 목적함수에서 KL 발산(KL divergence) 항이 하는 역할로 가장 적절한 것은?"
  options={['인코더가 만드는 잠재변수의 분포를 표준정규분포처럼 다루기 쉬운 사전분포에 가깝게 정규화해, 잠재공간이 매끄럽고 연속적인 구조를 갖도록 만든다.', '디코더가 만든 출력을 원본 입력과 픽셀 단위로 정확히 일치시키는 역할을 한다.', 'KL 발산은 학습 속도를 항상 두 배로 빠르게 만드는 역할을 한다.', 'KL 발산은 신경망의 층 수를 자동으로 줄여주는 역할을 한다.']}
  correctAnswer={1}
  explanation="VAE의 목적함수(ELBO, evidence lower bound)는 재구성 손실 항과 KL 발산 항의 합으로 이루어집니다. KL 발산 항은 인코더가 만든 잠재변수 분포를 표준정규분포에 가깝게 규제해, 잠재공간의 특정 영역에만 정보가 몰리지 않고 매끄럽게 퍼지도록 만듭니다."
  optionExplanations={['잠재분포를 표준정규분포에 가깝게 정규화한다는 KL 발산의 실제 역할을 정확히 설명했으므로 정답입니다.', '픽셀 단위 일치를 담당하는 것은 재구성 손실 항이지 KL 발산 항이 아니라 틀렸습니다.', 'KL 발산은 손실 함수의 한 항일 뿐 학습 속도 자체를 결정하는 장치가 아니라 틀렸습니다.', 'KL 발산은 잠재분포를 규제하는 항이며 신경망 층 수와는 무관해 틀렸습니다.']}
/>

### 문제 5

<Quiz
  questionNumber={5}
  question="오토인코더 계열 모델(AE, VAE)의 대표적인 응용 분야로 가장 적절하지 않은 것은?"
  options={['차원 축소와 잡음 제거(denoising), VAE를 이용한 새로운 표본 생성', '이미지 분류를 위한 라벨 자동 채점(사람의 개입 없이 정답을 보장하는 용도)', '이상 탐지(anomaly detection) — 재구성 오차가 큰 데이터를 이상치로 판단', '특징 추출 후 다른 모델의 입력으로 사용하는 표현 학습']}
  correctAnswer={2}
  explanation="AE와 VAE는 비지도학습 기반으로 차원 축소, 잡음 제거, 표본 생성, 이상 탐지, 표현 학습에 널리 쓰입니다. 그러나 정답 라벨을 자동으로 보장하는 채점 도구는 이 모델들의 목적이나 성질과 무관한 설명입니다."
  optionExplanations={['차원 축소·잡음 제거·생성이라는 실제 대표 응용을 설명한 옳은 내용입니다.', '오토인코더는 정답 라벨을 보장하는 채점 도구가 아니므로 이 설명이 적절하지 않아 정답입니다.', '재구성 오차 기반 이상 탐지는 오토인코더의 실제 대표 응용이라 옳은 설명입니다.', '인코더의 잠재 표현을 다른 모델의 입력 특징으로 쓰는 표현 학습은 실제 응용이므로 옳은 설명입니다.']}
/>

## 영역 2: GAN 구조·훈련 함정 (6–10)

### 문제 6

<Quiz
  questionNumber={6}
  question="GAN(Generative Adversarial Network)의 기본 구조에 대한 설명으로 가장 적절한 것은?"
  options={['가짜 데이터를 만들어내는 생성자(generator)와, 진짜와 가짜를 구별하려는 판별자(discriminator)가 서로 경쟁하며 함께 학습된다.', '생성자와 판별자는 서로 협력해 같은 손실을 최소화하는 동일한 목표를 가진다.', 'GAN은 판별자 없이 생성자만으로 학습이 완료되는 구조이다.', 'GAN의 생성자는 반드시 라벨이 있는 데이터로만 학습할 수 있다.']}
  correctAnswer={1}
  explanation="GAN은 생성자가 실제 데이터와 비슷한 가짜 데이터를 만들어 판별자를 속이려 하고, 판별자는 진짜와 가짜를 구별하려 하는 적대적(adversarial) 관계 속에서 두 네트워크가 함께 학습되는 구조입니다."
  optionExplanations={['생성자-판별자의 경쟁 구조라는 GAN의 핵심을 정확히 설명했으므로 정답입니다.', '생성자와 판별자는 서로 반대 방향의 목표를 가지는 적대 관계이지 협력 관계가 아니라 틀렸습니다.', '판별자가 없으면 생성자가 어떤 신호로 학습해야 할지 알 수 없어 GAN 구조가 성립하지 않아 틀렸습니다.', 'GAN의 생성자는 무작위 잡음 벡터로부터 데이터를 생성하는 비지도학습 방식이라 틀렸습니다.']}
/>

### 문제 7

<Quiz
  questionNumber={7}
  question="GAN의 판별자(discriminator)가 학습 과정에서 수행하는 역할로 가장 적절한 것은?"
  options={['입력 데이터가 실제 데이터에서 왔는지 생성자가 만든 가짜 데이터인지를 구별하는 이진 분류기 역할을 한다.', '무작위 잡음 벡터로부터 새로운 이미지를 직접 생성하는 역할을 한다.', '생성자의 가중치를 강제로 고정시켜 학습이 되지 않게 막는 역할을 한다.', '학습 데이터셋에 새로운 라벨을 부여하는 역할을 한다.']}
  correctAnswer={1}
  explanation="판별자는 진짜 데이터와 생성자가 만든 가짜 데이터를 입력받아, 이를 진짜(1)와 가짜(0)로 분류하는 이진 분류기 역할을 합니다. 이 판별 결과가 생성자를 개선시키는 신호로 쓰입니다."
  optionExplanations={['진짜·가짜를 구별하는 이진 분류기라는 판별자의 실제 역할을 정확히 설명했으므로 정답입니다.', '잡음 벡터로부터 데이터를 생성하는 것은 생성자의 역할이지 판별자의 역할이 아니라 틀렸습니다.', '판별자는 생성자와 번갈아 학습되며, 생성자의 학습을 영구히 막는 장치가 아니라 틀렸습니다.', 'GAN은 원본 데이터에 새로운 라벨을 부여하는 과정을 포함하지 않으므로 틀렸습니다.']}
/>

### 문제 8

<Quiz
  questionNumber={8}
  question="GAN 학습에서 흔히 발생하는 '모드 붕괴(mode collapse)' 현상에 대한 설명으로 가장 적절한 것은?"
  options={['생성자가 판별자를 속이기 쉬운 소수의 비슷한 샘플만 반복해서 만들어내며, 실제 데이터가 가진 다양성을 표현하지 못하게 되는 현상이다.', '판별자의 정확도가 항상 100퍼센트에 도달하는 이상적인 현상이다.', '생성자와 판별자의 학습 속도가 완전히 똑같아지는 현상이다.', '학습 데이터셋의 크기가 저절로 줄어드는 현상이다.']}
  correctAnswer={1}
  explanation="모드 붕괴는 생성자가 데이터 분포 전체를 학습하지 못하고, 판별자를 속이기에 유리한 일부 패턴에만 몰려서 비슷한 샘플만 반복 생성하는 현상입니다. 이 경우 생성된 샘플들의 다양성이 크게 떨어집니다."
  optionExplanations={['생성 다양성 손실이라는 모드 붕괴의 실제 정의를 정확히 설명했으므로 정답입니다.', '모드 붕괴는 오히려 생성자와 판별자 사이의 균형이 깨진 결과로 나타나는 문제이며, 판별자가 완벽해지는 것이 이상적 현상은 아니라 틀렸습니다.', '모드 붕괴라는 용어는 두 네트워크의 학습 속도가 같아지는 것과는 무관해 틀렸습니다.', '모드 붕괴는 데이터셋 크기 변화가 아니라 생성 샘플의 다양성 저하를 가리키므로 틀렸습니다.']}
/>

### 문제 9

<Quiz
  questionNumber={9}
  question="GAN 훈련이 다른 신경망 학습보다 불안정하다고 여겨지는 이유로 가장 적절한 것은?"
  options={['생성자와 판별자가 서로의 학습 상태에 따라 목표가 계속 바뀌는 이동 목표(moving target) 상황에서 균형을 맞춰야 하기 때문이다.', 'GAN은 역전파 알고리즘을 사용하지 않기 때문이다.', 'GAN은 항상 손실 함수 없이 학습되기 때문이다.', 'GAN은 미니배치를 전혀 사용할 수 없기 때문이다.']}
  correctAnswer={1}
  explanation="GAN은 생성자와 판별자가 번갈아 학습되며 서로의 파라미터가 계속 바뀌기 때문에, 한쪽이 상대편에 맞춰 최적화하는 동안 상대편의 목표 자체가 계속 움직이는 상황이 됩니다. 이 때문에 두 네트워크 사이의 균형(내시 균형에 가까운 지점)을 안정적으로 찾기가 어렵습니다."
  optionExplanations={['이동 목표 상황에서의 균형 문제라는 실제 불안정성의 원인을 정확히 설명했으므로 정답입니다.', 'GAN도 다른 신경망과 마찬가지로 역전파로 두 네트워크의 가중치를 갱신하므로 틀렸습니다.', 'GAN도 생성자·판별자 각각에 대해 정의된 손실 함수를 사용해 학습하므로 틀렸습니다.', 'GAN도 다른 신경망처럼 미니배치 단위로 학습을 진행할 수 있으므로 틀렸습니다.']}
/>

### 문제 10

<Quiz
  questionNumber={10}
  question="DCGAN(Deep Convolutional GAN)이 기본 GAN 구조에 도입한 변화로 가장 적절한 것은?"
  options={['생성자와 판별자에 완전연결층 대신 합성곱·전치합성곱(transposed convolution) 층을 주로 사용해 이미지 생성 품질과 학습 안정성을 높였다.', '판별자를 완전히 제거하고 생성자만으로 학습하도록 구조를 바꾸었다.', '역전파 알고리즘을 순전파로 대체하였다.', '손실 함수를 전혀 사용하지 않는 학습 방식을 도입하였다.']}
  correctAnswer={1}
  explanation="DCGAN은 이미지 생성에 적합하도록 생성자와 판별자를 합성곱 신경망(CNN) 구조로 설계한 대표적인 GAN 변형입니다. 전치합성곱으로 저차원 잠재 벡터를 점점 큰 이미지로 확장하는 생성자 구조가 특징입니다."
  optionExplanations={['합성곱 기반 구조 도입이라는 DCGAN의 실제 특징을 정확히 설명했으므로 정답입니다.', 'DCGAN도 기본 GAN과 마찬가지로 판별자를 포함하는 구조이므로 틀렸습니다.', 'DCGAN도 다른 신경망처럼 역전파로 학습되므로 순전파로 대체했다는 설명은 틀렸습니다.', 'DCGAN도 생성자·판별자 각각의 손실 함수를 정의해 사용하므로 틀렸습니다.']}
/>

## 영역 3: 전이학습·미세조정 (11–14)

### 문제 11

<Quiz
  questionNumber={11}
  question="전이학습(transfer learning)의 기본 아이디어로 가장 적절한 것은?"
  options={['대규모 데이터로 미리 학습된(pretrained) 모델의 표현을 다른 관련 문제에 재사용해, 적은 데이터로도 좋은 성능을 얻으려는 접근이다.', '모델을 처음부터 무작위 초기화된 상태로만 학습해야 한다는 원칙이다.', '한 번 학습된 모델은 다른 어떤 데이터에도 절대 재사용할 수 없다는 원칙이다.', '전이학습은 반드시 이미지 데이터에만 적용할 수 있는 기법이다.']}
  correctAnswer={1}
  explanation="전이학습은 대규모 데이터셋(예: ImageNet)으로 미리 학습해 유용한 특징을 학습한 모델을 가져와, 데이터가 적은 새로운 문제에 그 표현을 재사용하는 접근입니다. 처음부터 학습하는 것보다 적은 데이터와 시간으로 좋은 성능을 낼 수 있습니다."
  optionExplanations={['사전학습된 표현의 재사용이라는 전이학습의 핵심 아이디어를 정확히 설명했으므로 정답입니다.', '전이학습은 오히려 무작위 초기화 대신 사전학습된 가중치에서 시작하는 방식이라 틀렸습니다.', '전이학습 자체가 학습된 모델을 다른 문제에 재사용하는 것을 목적으로 하므로 이 설명은 정의와 반대되어 틀렸습니다.', '전이학습은 텍스트·음성 등 이미지 이외의 데이터에도 널리 적용되므로 틀렸습니다.']}
/>

### 문제 12

<Quiz
  questionNumber={12}
  question="특징 추출기(feature extractor)로 전이학습을 활용하는 방식에 대한 설명으로 가장 적절한 것은?"
  options={['사전학습된 모델의 앞쪽 층 가중치는 고정(freeze)한 채, 마지막 분류층만 새로운 데이터에 맞게 새로 학습시킨다.', '사전학습된 모델의 모든 가중치를 처음부터 무작위로 다시 초기화한다.', '사전학습된 모델의 마지막 층만 고정하고 나머지 모든 층을 새로 무작위 초기화한다.', '특징 추출기 방식은 새로운 데이터가 아주 많을 때만 사용할 수 있다.']}
  correctAnswer={1}
  explanation="특징 추출기 방식은 사전학습된 모델이 이미 학습한 일반적인 특징(예: 이미지의 모서리, 질감 등)을 그대로 활용하기 위해 앞쪽 층의 가중치를 고정하고, 새로운 문제에 맞는 마지막 분류층(또는 몇 개 층)만 새로 학습시키는 방식입니다."
  optionExplanations={['앞쪽 층 고정과 마지막 층만 재학습이라는 특징 추출기 방식의 실제 정의를 정확히 설명했으므로 정답입니다.', '모든 가중치를 무작위로 재초기화하면 사전학습의 이점을 전혀 살리지 못해 전이학습의 취지와 어긋나 틀렸습니다.', '특징 추출기 방식은 앞쪽 층을 고정하고 뒤쪽 층을 학습하는 것이지 그 반대가 아니라 틀렸습니다.', '특징 추출기 방식은 오히려 새로운 데이터가 적을 때 과적합을 막기 위해 자주 선택되는 전략이라 틀렸습니다.']}
/>

### 문제 13

<Quiz
  questionNumber={13}
  question="전체 미세조정(full fine-tuning)과 특징 추출기 방식을 비교한 설명으로 옳지 않은 것은?"
  options={['전체 미세조정은 사전학습된 모델의 모든 층의 가중치를 새로운 데이터에 맞춰 추가로 갱신한다.', '전체 미세조정은 일반적으로 특징 추출기 방식보다 더 큰 학습률을 사용해야 사전학습된 정보를 보존할 수 있다.', '새로운 데이터셋이 사전학습에 쓰인 데이터와 비슷하고 데이터 양이 적을 때는 특징 추출기 방식이 과적합 위험을 줄이는 데 유리할 수 있다.', '전체 미세조정은 특징 추출기 방식보다 학습해야 할 파라미터 수가 더 많다.']}
  correctAnswer={2}
  explanation="전체 미세조정은 이미 유용한 정보를 담고 있는 사전학습 가중치가 크게 흔들리지 않도록, 보통 처음부터 학습할 때보다 작은 학습률을 사용합니다. 큰 학습률을 쓰면 사전학습된 정보가 오히려 빠르게 손상될 수 있으므로 이 설명은 옳지 않습니다."
  optionExplanations={['전체 미세조정이 모든 층을 갱신한다는 옳은 설명입니다.', '전체 미세조정은 사전학습 정보 보존을 위해 오히려 작은 학습률을 쓰는 것이 일반적이라 이 설명이 틀린 지점이며 정답입니다.', '데이터가 적고 사전학습 데이터와 비슷할 때 특징 추출기 방식이 과적합을 줄이는 데 유리하다는 옳은 설명입니다.', '전체 미세조정은 더 많은 파라미터를 갱신 대상으로 삼으므로 옳은 설명입니다.']}
/>

### 문제 14

<Quiz
  questionNumber={14}
  question="CNN 기반 사전학습 모델을 새로운 이미지 분류 문제에 전이학습으로 적용할 때 흔히 취하는 절차로 가장 적절한 것은?"
  options={['원래 모델의 마지막 분류층(출력 클래스 수에 맞춘 층)을 새로운 클래스 수에 맞게 교체하고, 문제 상황에 따라 일부 또는 전체 층을 새 데이터로 재학습한다.', '입력 이미지의 크기와 채널 수를 사전학습 모델과 다르게 바꾸어도 항상 아무 문제 없이 작동한다.', '사전학습 모델의 합성곱 필터 개수를 반드시 절반으로 줄여야 전이학습을 적용할 수 있다.', '전이학습을 적용하려면 새로운 데이터셋의 클래스 수가 원래 모델과 반드시 같아야 한다.']}
  correctAnswer={1}
  explanation="전이학습을 적용할 때는 보통 새로운 문제의 클래스 수에 맞게 마지막 분류층을 교체하고, 데이터 양과 문제의 유사도에 따라 특징 추출기 방식이나 전체 미세조정 중 하나를 선택해 나머지 층을 다룹니다."
  optionExplanations={['마지막 층 교체와 상황별 재학습 전략이라는 실제 절차를 정확히 설명했으므로 정답입니다.', '입력 크기·채널 수가 사전학습 모델과 다르면 입력 처리 단계에서 문제가 생길 수 있어 항상 문제없다는 설명은 틀렸습니다.', '합성곱 필터 개수를 반드시 절반으로 줄여야 한다는 규칙은 없으므로 틀렸습니다.', '전이학습의 핵심은 오히려 클래스 수가 다른 새로운 문제에 적용하는 것이므로 이 설명은 정의와 반대되어 틀렸습니다.']}
/>

## 영역 4: 평가지표 계산과 해석 (15–22)

어느 이진 분류 모델을 100개의 테스트 데이터에 적용해 다음과 같은 혼동행렬(confusion matrix)을 얻었다.

| 구분 | 예측: 양성 | 예측: 음성 |
| --- | --- | --- |
| 실제: 양성 | TP = 40 | FN = 20 |
| 실제: 음성 | FP = 10 | TN = 30 |

### 문제 15

<Quiz
  questionNumber={15}
  question="위 혼동행렬에서 이 모델의 정확도(accuracy)는 얼마인가?"
  options={['0.70', '0.80', '0.60', '0.50']}
  correctAnswer={1}
  explanation="정확도는 (TP+TN)/(TP+FP+FN+TN)으로 계산합니다. 대입하면 (40+30)/(40+10+20+30) = 70/100 = 0.70입니다."
  optionExplanations={['전체 100개 중 맞힌 70개의 비율을 정확히 계산했으므로 정답입니다.', 'TP만을 (TP+FP)로 나눈 정밀도 값(0.80)과 혼동한 오답입니다.', 'TP를 (TP+FN)으로 나눈 재현율 값(0.667)과 다른 값으로, 임의로 계산을 잘못한 오답입니다.', 'TP와 FP, FN을 단순 평균한 값처럼 근거 없이 계산한 오답입니다.']}
/>

### 문제 16

<Quiz
  questionNumber={16}
  question="위 혼동행렬에서 이 모델의 정밀도(precision)는 얼마인가?"
  options={['0.80', '0.667', '0.70', '0.40']}
  correctAnswer={1}
  explanation="정밀도는 TP/(TP+FP)로 계산합니다. 대입하면 40/(40+10) = 40/50 = 0.80입니다."
  optionExplanations={['양성으로 예측한 것 중 실제 양성 비율을 정확히 계산했으므로 정답입니다.', 'TP/(TP+FN)으로 계산한 재현율 값을 정밀도로 착각한 오답입니다.', '정확도 값(0.70)을 정밀도로 착각한 오답입니다.', 'TP를 전체 100으로 나눈(40/100) 값으로, 분모를 잘못 잡은 오답입니다.']}
/>

### 문제 17

<Quiz
  questionNumber={17}
  question="위 혼동행렬에서 이 모델의 재현율(recall, 민감도)은 약 얼마인가?"
  options={['0.667', '0.80', '0.75', '0.333']}
  correctAnswer={1}
  explanation="재현율은 TP/(TP+FN)로 계산합니다. 대입하면 40/(40+20) = 40/60 ≈ 0.667입니다."
  optionExplanations={['실제 양성 중 모델이 맞힌 비율을 정확히 계산했으므로 정답입니다.', 'TP/(TP+FP)로 계산한 정밀도 값을 재현율로 착각한 오답입니다.', '분모를 (TP+FN)이 아닌 다른 값으로 잘못 잡아 계산한 오답입니다.', 'FN을 (TP+FN)으로 나눈(20/60) 값으로, 분자를 잘못 잡은 오답입니다.']}
/>

### 문제 18

<Quiz
  questionNumber={18}
  question="문제 16, 17의 정밀도 0.80과 재현율 0.667을 이용할 때, 이 모델의 F1 점수는 약 얼마인가?"
  options={['0.727', '0.733', '0.80', '0.667']}
  correctAnswer={1}
  explanation="F1 점수는 정밀도와 재현율의 조화평균으로, F1 = 2 * (정밀도 * 재현율) / (정밀도 + 재현율)로 계산합니다. 대입하면 2 * (0.80 * 0.667) / (0.80 + 0.667) = 2 * 0.5336 / 1.467 ≈ 1.0672 / 1.467 ≈ 0.727입니다."
  optionExplanations={['조화평균 공식에 정밀도·재현율을 정확히 대입한 값이므로 정답입니다.', '정밀도와 재현율을 산술평균((0.80+0.667)/2 ≈ 0.733)으로 잘못 계산한 오답입니다.', '정밀도 값을 그대로 F1으로 착각한 오답입니다.', '재현율 값을 그대로 F1으로 착각한 오답입니다.']}
/>

### 문제 19

<Quiz
  questionNumber={19}
  question="정밀도와 재현율의 관계, 그리고 F1 점수를 사용하는 이유에 대한 설명으로 옳지 않은 것은?"
  options={['정밀도와 재현율은 서로 완전히 독립적이어서, 한쪽을 높이는 임계값(threshold) 조정이 다른 쪽에 전혀 영향을 주지 않는다.', '분류 임계값을 낮추면 양성으로 예측하는 경우가 늘어 재현율은 높아지고 정밀도는 낮아지는 경향이 있다.', 'F1 점수는 정밀도와 재현율 중 어느 한쪽이 극단적으로 낮을 때 이를 산술평균보다 더 크게 반영해 낮은 점수로 나타낸다.', '양성 클래스가 매우 적은 불균형 데이터에서는 정확도만으로 모델 성능을 평가하면 과대평가될 수 있어 정밀도·재현율·F1을 함께 살펴본다.']}
  correctAnswer={1}
  explanation="정밀도와 재현율은 분류 임계값에 따라 서로 트레이드오프 관계를 가집니다. 임계값을 낮추면 더 많은 데이터를 양성으로 예측해 재현율은 올라가지만, 그만큼 잘못된 양성 예측(FP)도 늘어나 정밀도는 낮아지는 경향이 있습니다. 따라서 두 지표가 완전히 독립적이라는 설명은 옳지 않습니다."
  optionExplanations={['정밀도와 재현율은 임계값 조정에 따라 서로 반대 방향으로 움직이는 트레이드오프 관계이므로, 완전히 독립적이라는 이 설명이 틀린 지점이며 정답입니다.', '임계값을 낮추면 재현율이 오르고 정밀도가 내려가는 실제 트레이드오프를 옳게 설명했습니다.', 'F1이 조화평균이라 한쪽이 매우 낮으면 전체 점수가 크게 낮아진다는 옳은 설명입니다.', '불균형 데이터에서 정확도만으로는 성능을 오판할 수 있다는 옳은 설명입니다.']}
/>

### 문제 20

<Quiz
  questionNumber={20}
  question="ROC(Receiver Operating Characteristic) 곡선과 AUC(Area Under the Curve)에 대한 설명으로 가장 적절한 것은?"
  options={['ROC 곡선은 분류 임계값을 바꿔가며 참양성비율(TPR)과 거짓양성비율(FPR)의 변화를 나타내고, AUC는 이 곡선 아래 면적으로 임계값에 관계없는 전반적인 분류 성능을 요약한 값이다.', 'AUC는 항상 0보다 작은 값을 가지며, 값이 작을수록 더 좋은 모델이다.', 'ROC 곡선은 회귀 문제에서만 사용할 수 있는 그래프이다.', 'AUC가 1.0이면 모델이 모든 데이터를 무작위로 예측했다는 뜻이다.']}
  correctAnswer={1}
  explanation="ROC 곡선은 여러 임계값에서 TPR(재현율)과 FPR을 각각 계산해 그린 곡선이며, AUC는 이 곡선 아래 면적으로 0.5(무작위 수준)에서 1.0(완벽한 분류) 사이의 값을 가지는 요약 지표입니다."
  optionExplanations={['ROC 곡선의 정의와 AUC가 요약 지표라는 관계를 정확히 설명했으므로 정답입니다.', 'AUC는 0과 1 사이의 값을 가지며 1에 가까울수록 더 좋은 모델을 의미하므로 틀렸습니다.', 'ROC·AUC는 이진 분류 문제의 평가에 쓰이는 지표이지 회귀 문제 전용이 아니라 틀렸습니다.', 'AUC가 0.5에 가까울수록 무작위 예측에 가깝고, 1.0에 가까울수록 완벽한 분류를 의미하므로 이 설명은 반대라 틀렸습니다.']}
/>

### 문제 21

어떤 회귀 모델이 4개의 테스트 데이터에 대해 다음과 같이 예측했다. 실제값 y = [3, 5, 2, 7], 예측값 y_hat = [2, 5, 4, 6]이다.

<Quiz
  questionNumber={21}
  question="이 예측 결과의 평균절대오차(MAE)와 평균제곱오차(MSE)는 각각 얼마인가?"
  options={['MAE = 1.0, MSE = 1.5', 'MAE = 1.5, MSE = 1.0', 'MAE = 4.0, MSE = 6.0', 'MAE = 0.5, MSE = 0.75']}
  correctAnswer={1}
  explanation="오차는 y - y_hat = 1, 0, -2, 1입니다. 절댓값은 1, 0, 2, 1이며 평균을 내면 MAE = (1+0+2+1)/4 = 4/4 = 1.0입니다. 제곱값은 1, 0, 4, 1이며 평균을 내면 MSE = (1+0+4+1)/4 = 6/4 = 1.5입니다."
  optionExplanations={['절댓값 오차와 제곱 오차의 평균을 각각 정확히 계산했으므로 정답입니다.', 'MAE와 MSE 값을 서로 바꿔 쓴 오답입니다.', '오차의 합(4)과 제곱 오차의 합(6)을 평균 내지 않고 그대로 쓴 오답입니다.', '오차 개수를 4가 아닌 8로 나누는 등 계산 과정에서 분모를 잘못 잡은 오답입니다.']}
/>

### 문제 22

문제 21과 같은 데이터에서, 실제값 y의 평균은 4.25이고 전체 편차제곱합(SS_tot)은 14.75이며, 잔차제곱합(SS_res, 즉 MSE의 합인 6)은 이미 구했다.

<Quiz
  questionNumber={22}
  question="이 모델의 결정계수(R-squared)는 약 얼마인가?"
  options={['0.593', '0.407', '1.407', '0.0']}
  correctAnswer={1}
  explanation="결정계수는 R^2 = 1 - (SS_res / SS_tot)로 계산합니다. 대입하면 1 - (6 / 14.75) = 1 - 0.4068 ≈ 0.593입니다. R^2이 1에 가까울수록 모델이 데이터의 변동을 잘 설명한다는 뜻입니다."
  optionExplanations={['공식에 SS_res와 SS_tot를 정확히 대입해 계산한 값이므로 정답입니다.', 'SS_res/SS_tot 값(약 0.407) 자체를 R^2로 착각하고 1을 빼는 것을 빠뜨린 오답입니다.', '1에서 빼야 할 값을 반대로 더한 오답입니다.', 'SS_res와 SS_tot이 같다고 잘못 가정해 R^2을 0으로 계산한 오답입니다.']}
/>

## 참고 자료

- Ian Goodfellow 외, Deep Learning (Deep Learning Book) — https://www.deeplearningbook.org
- scikit-learn 공식 문서(평가지표) — https://scikit-learn.org/stable/
