---
title: '24. 독학사 3단계 딥러닝 기출 1회분 해설 (최근 A년도)'
description: '전 범위를 아우르는 22문항 객관식과 2문항 서술형으로 구성한 재구성 모의고사, 순전파·역전파·소프트맥스·CNN 출력 크기 계산 전 과정 해설 포함'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 회차는 국가평생교육진흥원 독학학위제 3단계 딥러닝 **최신 출제기준(평가영역) 전체**를 근거로 새로 구성한 **모의 기출 1회분**입니다. 실제 특정 연도의 기출 문항을 그대로 옮기거나 암기해 재현한 것이 아니며, 출제기준에 명시된 영역별 비중을 반영해 객관식 22문항과 서술형 2문항으로 재구성했습니다. 실제 응시 시 문항 수·배점·시간은 그해 공고를 반드시 확인하시기 바랍니다.
</Callout>

## 이 회차의 영역 구성

| 문항 번호 | 출제기준 영역 | 관련 편 | 유형 |
| --- | --- | --- | --- |
| 1–2 | 머신러닝과 딥러닝의 관계 | 03편 | 개념 |
| 3–4 | 퍼셉트론·MLP 구조 | 05편 | 개념 |
| 5–6 | 활성화·손실 함수 | 06편 | 개념·비교 |
| 7–8 | 순전파 계산 | 07편 | 계산 |
| 9 | 역전파 개념 | 07편 | 개념 |
| 10–11 | 최적화 알고리즘 | 08편 | 개념·비교 |
| 12–13 | 과적합·정규화 | 09편 | 개념 |
| 14 | 배치 정규화·초기화 | 10편 | 개념 |
| 15–16 | CNN 출력 크기·파라미터 계산 | 11편 | 계산 |
| 17 | 대표 CNN 구조 | 12편 | 개념 |
| 18 | RNN·LSTM | 13편 | 개념 |
| 19 | 임베딩·Attention | 14편 | 개념 |
| 20 | Transformer 개요 | 15편 | 개념 |
| 21 | 소프트맥스 계산 | 06·15편 | 계산 |
| 22 | 생성 모델(AE/VAE/GAN) | 16·17편 | 개념 |
| 서술 1 | 역전파 1스텝 전 과정 | 07편 | 서술형 |
| 서술 2 | CNN 출력 크기·파라미터 수 종합 | 11편 | 서술형 |

## 객관식 (1–22)

### 문제 1

<Quiz
  questionNumber={1}
  question="전통적인 머신러닝과 딥러닝의 관계에 대한 설명으로 가장 적절한 것은?"
  options={['딥러닝은 머신러닝의 한 하위 분야로, 사람이 직접 설계한 특징 대신 신경망이 데이터로부터 계층적인 특징 표현을 스스로 학습한다는 점이 특징이다.', '딥러닝과 머신러닝은 서로 완전히 별개의 학문 분야로 아무런 공통점이 없다.', '딥러닝은 지도학습만 가능하고 비지도·강화학습은 전통적 머신러닝의 영역이다.', '전통적 머신러닝은 딥러닝이 등장한 이후로 더 이상 사용되지 않는다.']}
  correctAnswer={1}
  explanation="딥러닝은 다층 신경망을 이용해 특징을 자동으로 학습하는 머신러닝의 한 분야입니다. 전통적 머신러닝이 사람이 설계한 특징(feature engineering)에 의존하는 것과 달리, 딥러닝은 원시 데이터로부터 계층적인 표현을 스스로 학습합니다."
  optionExplanations={['딥러닝이 머신러닝의 하위 분야이며 표현학습이 핵심이라는 옳은 설명이므로 정답입니다.', '딥러닝은 머신러닝의 하위 분야로 손실 함수·경사하강법 등 많은 개념을 공유하므로 틀렸습니다.', '딥러닝도 지도·비지도·강화학습 모두에 적용될 수 있으므로 틀렸습니다.', '전통적 머신러닝(트리 기반 모델 등)은 표 형태 데이터 등에서 여전히 널리 쓰이므로 틀렸습니다.']}
/>

### 문제 2

<Quiz
  questionNumber={2}
  question="딥러닝이 전통적 머신러닝에 비해 상대적으로 불리한 점으로 가장 적절한 것은?"
  options={['일반적으로 대량의 데이터와 많은 연산 자원이 필요하며, 모델 내부 동작을 해석하기가 상대적으로 어렵다.', '딥러닝은 항상 데이터가 적을 때 더 좋은 성능을 낸다.', '딥러닝은 이미지나 음성처럼 비정형 데이터를 전혀 다룰 수 없다.', '딥러닝은 하이퍼파라미터가 전혀 없어 튜닝이 필요하지 않다.']}
  correctAnswer={1}
  explanation="딥러닝은 파라미터 수가 많아 충분한 데이터와 연산 자원이 뒷받침되어야 성능을 발휘하며, 다층 구조로 인해 예측 근거를 해석하기 어려운 블랙박스 성격을 갖는다는 한계가 자주 지적됩니다."
  optionExplanations={['데이터·연산 자원 요구와 해석 가능성 문제라는 실제 한계를 정확히 설명했으므로 정답입니다.', '딥러닝은 오히려 데이터가 적으면 과적합 위험이 커지는 경향이 있어 틀렸습니다.', '딥러닝은 이미지·음성·텍스트 등 비정형 데이터 처리에 강점을 보이는 분야이므로 틀렸습니다.', '딥러닝도 학습률·배치 크기·층 수 등 조정해야 할 하이퍼파라미터가 많으므로 틀렸습니다.']}
/>

### 문제 3

<Quiz
  questionNumber={3}
  question="단일 퍼셉트론(perceptron)이 원천적으로 풀 수 없는 문제로 알려진 대표적인 예로 가장 적절한 것은?"
  options={['XOR 논리 연산과 같이 하나의 직선으로 분리할 수 없는 비선형 분리 문제', 'AND 논리 연산과 같이 하나의 직선으로 분리할 수 있는 선형 분리 문제', 'OR 논리 연산과 같이 하나의 직선으로 분리할 수 있는 선형 분리 문제', '입력이 하나뿐인 아주 단순한 회귀 문제']}
  correctAnswer={1}
  explanation="단일 퍼셉트론은 하나의 선형 결정 경계만 만들 수 있어, AND·OR처럼 직선으로 분리 가능한 문제는 풀 수 있지만 XOR처럼 하나의 직선으로는 분리할 수 없는 비선형 문제는 풀 수 없습니다. 이 한계가 다층 퍼셉트론(MLP) 등장의 배경이 되었습니다."
  optionExplanations={['XOR가 단일 퍼셉트론의 대표적 한계 사례라는 옳은 설명이므로 정답입니다.', 'AND는 직선 하나로 분리 가능해 단일 퍼셉트론으로 풀 수 있으므로 틀렸습니다.', 'OR 역시 직선 하나로 분리 가능해 단일 퍼셉트론으로 풀 수 있으므로 틀렸습니다.', '단순한 선형 회귀 문제는 단일 퍼셉트론(선형 유닛)으로도 다룰 수 있는 문제라 틀렸습니다.']}
/>

### 문제 4

<Quiz
  questionNumber={4}
  question="다층 퍼셉트론(MLP)에서 은닉층(hidden layer)에 비선형 활성화 함수를 사용하지 않고 항등함수(identity function)만 사용한다면 일어나는 일로 가장 적절한 것은?"
  options={['여러 층을 쌓아도 전체 네트워크가 결국 하나의 선형 변환과 동일해져, 층을 깊게 쌓는 의미가 사라진다.', '오히려 비선형 함수를 쓸 때보다 항상 더 높은 표현력을 갖게 된다.', '역전파 알고리즘을 전혀 적용할 수 없게 된다.', '입력 데이터의 차원이 자동으로 줄어든다.']}
  correctAnswer={1}
  explanation="선형 변환을 여러 번 합성해도 그 결과는 여전히 하나의 선형 변환입니다. 따라서 은닉층에 비선형 활성화 함수가 없으면, 층을 아무리 깊게 쌓아도 표현력은 단일 선형층과 다르지 않아 깊은 구조의 장점을 잃게 됩니다."
  optionExplanations={['선형 변환의 합성이 여전히 선형이라는 수학적 사실에 근거한 옳은 설명이므로 정답입니다.', '비선형성이 없으면 오히려 표현력이 단일 선형층 수준으로 제한되므로 틀렸습니다.', '항등함수도 미분 가능하므로 역전파 자체는 적용할 수 있어 틀렸습니다.', '항등함수는 입력을 그대로 통과시키므로 차원이 자동으로 줄어들지 않아 틀렸습니다.']}
/>

### 문제 5

<Quiz
  questionNumber={5}
  question="시그모이드(sigmoid) 함수와 비교한 ReLU(Rectified Linear Unit) 함수의 특징으로 가장 적절한 것은?"
  options={['입력이 0보다 클 때 기울기가 항상 1로 일정해, 깊은 신경망에서 시그모이드보다 그라디언트 소실 문제가 덜 발생하는 경향이 있다.', 'ReLU는 출력값이 항상 0과 1 사이로 제한된다.', 'ReLU는 미분이 불가능한 지점이 전혀 없어 시그모이드보다 항상 매끄럽다.', 'ReLU는 입력이 음수일 때도 항상 원래 입력값을 그대로 출력한다.']}
  correctAnswer={1}
  explanation="ReLU는 양수 입력 구간에서 미분값이 항상 1이므로, 시그모이드처럼 입력이 커지거나 작아질수록 기울기가 0에 가까워지는 포화(saturation) 문제가 상대적으로 덜 발생해 깊은 신경망 학습에 유리합니다."
  optionExplanations={['양수 구간 기울기 1과 그라디언트 소실 완화라는 ReLU의 실제 장점을 정확히 설명했으므로 정답입니다.', '출력이 0과 1 사이로 제한되는 것은 시그모이드의 특징이지 ReLU의 특징이 아니라 틀렸습니다.', 'ReLU는 0에서 미분이 정의되지 않는 지점이 있어 완전히 매끄럽다고 할 수 없어 틀렸습니다.', 'ReLU는 입력이 음수이면 0을 출력하므로 원래 입력값을 그대로 출력한다는 설명은 틀렸습니다.']}
/>

### 문제 6

<Quiz
  questionNumber={6}
  question="다중 클래스 분류 문제에서 출력층 활성화 함수로 소프트맥스(softmax)를, 손실 함수로 교차엔트로피(cross-entropy)를 함께 사용하는 이유로 가장 적절한 것은?"
  options={['소프트맥스가 각 클래스에 대한 확률 분포를 만들고, 교차엔트로피가 정답 클래스에 부여된 확률이 낮을수록 큰 손실을 주어 확률을 정답 쪽으로 밀어올리도록 학습시키기 때문이다.', '소프트맥스와 교차엔트로피를 함께 쓰면 항상 학습이 완전히 필요 없어지기 때문이다.', '소프트맥스는 회귀 문제에서만 사용할 수 있는 함수이기 때문이다.', '교차엔트로피는 출력값이 반드시 정수여야만 계산할 수 있기 때문이다.']}
  correctAnswer={1}
  explanation="소프트맥스는 로짓(logit)을 모든 클래스에 대한 확률 합이 1인 분포로 바꾸고, 교차엔트로피는 정답 클래스의 예측 확률이 낮을수록 손실을 크게 주어 그 확률을 높이는 방향으로 그라디언트를 만듭니다. 이 조합은 다중 클래스 분류의 표준 구성입니다."
  optionExplanations={['확률 분포 생성과 정답 확률을 높이는 손실 설계라는 실제 이유를 정확히 설명했으므로 정답입니다.', '이 조합은 학습을 더 효과적으로 만들기 위한 것이지 학습 자체를 없애는 것이 아니라 틀렸습니다.', '소프트맥스는 분류 문제의 출력층에 주로 쓰이므로 회귀 전용이라는 설명은 틀렸습니다.', '교차엔트로피는 확률값(실수)을 대상으로 계산되며 정수 제한이 없으므로 틀렸습니다.']}
/>

### 문제 7

입력 $x_1 = 1$, $x_2 = 2$이고, 가중치 $w_1 = 0.5$, $w_2 = -0.3$, 편향(bias) $b = 0.2$인 하나의 뉴런이 있다.

<Quiz
  questionNumber={7}
  question="이 뉴런의 가중합(weighted sum) $z = w_1 x_1 + w_2 x_2 + b$ 값은?"
  options={['0.1', '0.3', '-0.1', '1.0']}
  correctAnswer={1}
  explanation="가중합은 각 입력과 가중치를 곱해서 더하고 편향을 더한 값입니다. 대입하면 z = 0.5*1 + (-0.3)*2 + 0.2 = 0.5 - 0.6 + 0.2 = 0.1입니다."
  optionExplanations={['가중치와 입력을 정확히 곱해 더한 값이므로 정답입니다.', '부호를 잘못 적용해 -0.6 대신 -0.4를 더한 것과 같은 계산 오류로 나온 오답입니다.', '편향의 부호를 반대로 적용한 오답입니다.', '단순히 입력값들을 더한(1+2 대신 다른 조합) 값처럼 근거 없이 계산한 오답입니다.']}
/>

### 문제 8

문제 7에서 구한 $z = 0.1$을 시그모이드 활성화 함수에 통과시킨다. $e^{-0.1} \approx 0.905$를 이용한다.

<Quiz
  questionNumber={8}
  question="이 뉴런의 최종 출력 $a = \sigma(z)$는 약 얼마인가?"
  options={['0.525', '0.475', '0.905', '1.0']}
  correctAnswer={1}
  explanation="시그모이드 함수는 sigma(z) = 1 / (1 + e^(-z))입니다. 대입하면 1 / (1 + 0.905) = 1 / 1.905 ≈ 0.525입니다."
  optionExplanations={['시그모이드 공식에 정확히 대입한 값이므로 정답입니다.', '1에서 정답 값을 뺀(1-0.525=0.475) 값으로, 분자와 분모를 혼동한 오답입니다.', 'e^(-0.1) 값 자체를 최종 출력으로 착각한 오답입니다.', '시그모이드를 거치지 않은 것처럼 입력이 그대로 1에 가까운 값이 된다고 착각한 오답입니다.']}
/>

### 문제 9

<Quiz
  questionNumber={9}
  question="역전파(backpropagation) 알고리즘의 핵심 아이디어로 가장 적절한 것은?"
  options={['출력층에서 계산한 손실의 그라디언트를 연쇄 법칙(chain rule)을 이용해 입력층 방향으로 층별로 전달하며 각 가중치에 대한 그라디언트를 계산한다.', '입력층에서 출력층 방향으로만 그라디언트를 전달하고 손실은 전혀 계산하지 않는다.', '역전파는 활성화 함수 없이 선형 연산만 있을 때만 정의되는 알고리즘이다.', '역전파는 학습이 끝난 뒤 모델을 저장하는 절차를 의미한다.']}
  correctAnswer={1}
  explanation="역전파는 순전파로 계산한 손실을 출력층에서부터 입력층 방향으로 연쇄 법칙을 적용해 전달하면서, 각 층의 가중치가 손실에 미치는 영향(그라디언트)을 효율적으로 계산하는 알고리즘입니다."
  optionExplanations={['연쇄 법칙 기반의 역방향 그라디언트 전달이라는 역전파의 핵심을 정확히 설명했으므로 정답입니다.', '역전파는 손실의 그라디언트를 출력에서 입력 방향으로 전달하는 것이지, 손실 계산 없이 이루어지지 않으므로 틀렸습니다.', '역전파는 비선형 활성화 함수가 있는 일반적인 신경망에서도 연쇄 법칙으로 그라디언트를 계산할 수 있으므로 틀렸습니다.', '역전파는 학습 중 그라디언트를 계산하는 절차이며 모델 저장과는 무관해 틀렸습니다.']}
/>

### 문제 10

<Quiz
  questionNumber={10}
  question="모멘텀(momentum)을 적용한 경사하강법이 기본 SGD와 다른 점으로 가장 적절한 것은?"
  options={['과거 그라디언트의 방향을 누적한 속도(velocity) 항을 이용해, 일관된 방향으로는 더 빠르게 이동하고 진동은 줄이는 효과를 낸다.', '모멘텀은 그라디언트를 전혀 사용하지 않고 무작위로 파라미터를 갱신한다.', '모멘텀은 학습률을 항상 0으로 만들어 학습을 멈추게 한다.', '모멘텀은 미니배치를 사용할 수 없게 만든다.']}
  correctAnswer={1}
  explanation="모멘텀은 현재 그라디언트뿐 아니라 과거 그라디언트의 누적 방향(속도)을 함께 반영해 파라미터를 갱신합니다. 이 덕분에 일관되게 내려가는 방향으로는 가속이 붙고, 지그재그로 진동하는 방향의 움직임은 상쇄되어 더 안정적으로 수렴하는 경향이 있습니다."
  optionExplanations={['속도 항 누적을 통한 가속과 진동 완화라는 모멘텀의 실제 효과를 정확히 설명했으므로 정답입니다.', '모멘텀도 기본적으로 그라디언트를 이용해 속도 항을 갱신하므로 틀렸습니다.', '모멘텀은 학습률과 별개의 하이퍼파라미터이며 학습률을 0으로 만드는 장치가 아니라 틀렸습니다.', '모멘텀은 SGD와 마찬가지로 미니배치 단위로 적용할 수 있으므로 틀렸습니다.']}
/>

### 문제 11

<Quiz
  questionNumber={11}
  question="Adam 옵티마이저에 대한 설명으로 옳지 않은 것은?"
  options={['Adam은 그라디언트의 1차 모멘트(평균)와 2차 모멘트(분산)를 모두 추정해 각 파라미터마다 다른 크기로 학습률을 조절한다.', 'Adam은 모멘텀 방식의 방향 정보와 RMSprop 방식의 파라미터별 학습률 조절을 결합한 방법으로 볼 수 있다.', 'Adam은 학습 초기의 편향을 보정하기 위한 바이어스 보정(bias correction) 절차를 포함한다.', 'Adam을 사용하면 학습률이라는 하이퍼파라미터를 전혀 설정할 필요가 없다.']}
  correctAnswer={4}
  explanation="Adam도 기본 학습률(예: 0.001)이라는 하이퍼파라미터를 반드시 설정해야 하며, 다만 각 파라미터별로 이 학습률을 적응적으로 조절할 뿐입니다. 학습률 설정이 전혀 필요 없다는 설명은 옳지 않습니다."
  optionExplanations={['1차·2차 모멘트를 모두 추정한다는 Adam의 실제 원리를 옳게 설명했습니다.', '모멘텀과 RMSprop을 결합한 방법이라는 Adam의 실제 성격을 옳게 설명했습니다.', 'Adam이 학습 초기 모멘트 추정치의 편향을 보정하는 절차를 포함한다는 옳은 설명입니다.', 'Adam도 기본 학습률을 사용자가 설정해야 하므로 이 설명이 틀린 지점이며 정답입니다.']}
/>

### 문제 12

<Quiz
  questionNumber={12}
  question="L2 정규화(가중치 감쇠, weight decay)가 과적합을 줄이는 원리로 가장 적절한 것은?"
  options={['손실 함수에 가중치 제곱합에 비례하는 항을 더해, 학습 과정에서 가중치의 크기가 지나치게 커지지 않도록 억제한다.', 'L2 정규화는 학습 데이터의 개수를 자동으로 늘려준다.', 'L2 정규화는 신경망의 층 수를 자동으로 줄여준다.', 'L2 정규화는 활성화 함수를 항등함수로 바꾸어 버린다.']}
  correctAnswer={1}
  explanation="L2 정규화는 손실 함수에 가중치들의 제곱합에 비례하는 페널티 항을 추가합니다. 이 항 때문에 모델은 손실을 줄이는 동시에 가중치 크기도 작게 유지하려 하고, 그 결과 일부 입력에 지나치게 민감하게 반응하는 과적합 경향이 줄어듭니다."
  optionExplanations={['가중치 제곱합 페널티를 통한 크기 억제라는 L2 정규화의 실제 원리를 정확히 설명했으므로 정답입니다.', 'L2 정규화는 손실 함수를 바꾸는 기법이지 데이터 개수와는 무관해 틀렸습니다.', 'L2 정규화는 가중치 값을 조절할 뿐 신경망의 층 수 자체를 바꾸지 않으므로 틀렸습니다.', 'L2 정규화는 손실 함수에 항을 추가하는 기법이며 활성화 함수의 종류를 바꾸지 않으므로 틀렸습니다.']}
/>

### 문제 13

<Quiz
  questionNumber={13}
  question="Dropout에 대한 설명으로 옳지 않은 것은?"
  options={['학습 시 매 순전파마다 일정 확률로 일부 뉴런의 출력을 0으로 만들어, 특정 뉴런에 지나치게 의존하는 것을 막는다.', '여러 개의 서로 다른 부분 네트워크를 앙상블(ensemble)하는 것과 비슷한 효과를 낸다는 해석이 있다.', 'Dropout은 테스트(추론) 시에도 학습 때와 동일한 비율로 뉴런을 무작위로 꺼야 한다.', 'Dropout 비율(drop rate)은 사용자가 정하는 하이퍼파라미터이다.']}
  correctAnswer={3}
  explanation="일반적으로 Dropout은 학습 시에만 적용하고, 테스트(추론) 시에는 모든 뉴런을 사용하는 대신 학습 때 곱했던 비율만큼 출력을 스케일링하여 학습-추론 사이의 기댓값을 맞춰줍니다. 테스트 시에도 동일하게 뉴런을 무작위로 끈다는 설명은 옳지 않습니다."
  optionExplanations={['매 순전파마다 무작위로 뉴런을 끄는 실제 동작 방식을 옳게 설명했습니다.', 'Dropout이 앙상블 효과와 비슷하다는 해석은 실제로 널리 받아들여지는 옳은 설명입니다.', '테스트 시에는 보통 전체 뉴런을 사용하고 출력을 스케일링하는 것이 표준 방식이므로, 이 설명이 틀린 지점이며 정답입니다.', 'Dropout 비율은 사용자가 정하는 하이퍼파라미터라는 옳은 설명입니다.']}
/>

### 문제 14

<Quiz
  questionNumber={14}
  question="배치 정규화(Batch Normalization)의 효과로 가장 적절한 것은?"
  options={['미니배치 단위로 각 층의 입력 분포를 정규화하여 내부 공변량 변화(internal covariate shift)를 줄이고, 더 큰 학습률을 안정적으로 사용할 수 있게 돕는다.', '배치 정규화는 미니배치 크기를 항상 1로 고정시킨다.', '배치 정규화는 활성화 함수를 완전히 제거해 버린다.', '배치 정규화는 데이터 증강을 자동으로 대체하는 기법으로, 함께 쓰면 항상 성능이 떨어진다.']}
  correctAnswer={1}
  explanation="배치 정규화는 각 층의 입력을 미니배치 단위로 평균 0, 분산 1에 가깝게 정규화한 뒤 학습 가능한 스케일·시프트 파라미터를 적용합니다. 이를 통해 층 사이 입력 분포가 학습 중 급격히 바뀌는 문제를 줄이고 학습을 더 안정적이고 빠르게 만듭니다."
  optionExplanations={['내부 공변량 변화 완화와 학습 안정화라는 배치 정규화의 실제 효과를 정확히 설명했으므로 정답입니다.', '배치 정규화는 미니배치 크기와 무관하게 적용되는 기법이며 배치 크기를 강제로 1로 만들지 않아 틀렸습니다.', '배치 정규화는 정규화 이후 일반적으로 활성화 함수를 그대로 적용하므로 틀렸습니다.', '배치 정규화와 데이터 증강은 서로 목적이 다른 별개의 기법이며 함께 사용해 성능이 개선되는 경우가 많으므로 틀렸습니다.']}
/>

### 문제 15

입력 이미지 크기가 32x32이고 5x5 필터를 스트라이드 1, 패딩 0으로 합성곱 연산에 사용한다.

<Quiz
  questionNumber={15}
  question="합성곱 결과로 만들어지는 특징 맵(feature map)의 한 변의 크기는?"
  options={['28', '32', '27', '30']}
  correctAnswer={1}
  explanation="출력 크기는 (입력 크기 - 필터 크기)/스트라이드 + 1 공식으로 계산합니다. 대입하면 (32-5)/1 + 1 = 27+1 = 28입니다."
  optionExplanations={['공식에 값을 정확히 대입해 계산한 값이므로 정답입니다.', '패딩과 스트라이드를 고려하지 않고 입력 크기를 그대로 답으로 쓴 오답입니다.', '+1을 빠뜨리고 (32-5)/1 = 27만 계산한 오답입니다.', '필터 크기를 5가 아닌 3으로 잘못 대입해 계산한 오답입니다.']}
/>

### 문제 16

입력 채널이 3, 필터 크기가 5x5, 출력 채널(필터 개수)이 16, 각 필터마다 편향 1개가 있는 합성곱층이 있다.

<Quiz
  questionNumber={16}
  question="이 합성곱층의 전체 학습 파라미터 개수는?"
  options={['1216', '1200', '400', '76']}
  correctAnswer={1}
  explanation="필터 하나의 파라미터는 (5*5*3)+1(편향) = 75+1 = 76개입니다. 필터가 16개이므로 전체는 76*16 = 1216개입니다."
  optionExplanations={['필터 하나당 파라미터 수에 필터 개수를 곱한 정확한 계산이므로 정답입니다.', '편향을 빠뜨리고 (5*5*3)*16 = 1200만 계산한 오답입니다.', '입력 채널을 곱하지 않고 (5*5)*16 = 400만 계산한 오답입니다.', '필터 하나의 파라미터 수(76)만 답으로 쓰고 필터 개수를 곱하지 않은 오답입니다.']}
/>

### 문제 17

<Quiz
  questionNumber={17}
  question="ResNet의 잔차 연결(residual connection, skip connection)이 해결하고자 한 문제로 가장 적절한 것은?"
  options={['네트워크가 매우 깊어질 때 그라디언트가 소실되어 학습이 잘 되지 않는 문제를, 입력을 몇 개 층 건너뛰어 더해줌으로써 완화한다.', '입력 이미지의 크기를 강제로 절반으로 줄이기 위해 도입되었다.', '완전연결층의 파라미터 수를 늘리기 위해 도입되었다.', '데이터 증강을 대체하기 위해 도입되었다.']}
  correctAnswer={1}
  explanation="ResNet은 F(x)+x 형태로 입력 x를 몇 개 층을 건너뛰어 출력에 더하는 잔차 연결을 도입해, 아주 깊은 네트워크에서도 그라디언트가 소실되지 않고 잘 전달되도록 만들어 깊은 구조의 학습을 가능하게 했습니다."
  optionExplanations={['그라디언트 소실 완화라는 잔차 연결의 실제 도입 목적을 정확히 설명했으므로 정답입니다.', '잔차 연결은 이미지 크기 조절이 아니라 그라디언트 전달을 돕는 구조적 장치라 틀렸습니다.', '잔차 연결은 파라미터를 늘리기 위한 것이 아니라 학습을 안정화하기 위한 구조적 장치라 틀렸습니다.', '잔차 연결과 데이터 증강은 서로 다른 목적을 가진 별개의 기법이라 틀렸습니다.']}
/>

### 문제 18

<Quiz
  questionNumber={18}
  question="LSTM(Long Short-Term Memory)이 기본 RNN의 한계를 보완하기 위해 도입한 요소로 가장 적절한 것은?"
  options={['입력·망각·출력 게이트와 별도의 셀 상태(cell state)를 두어, 오래된 정보를 필요한 만큼 유지하거나 잊을 수 있게 조절한다.', 'LSTM은 게이트 없이 은닉 상태만으로 이루어진 가장 단순한 순환 구조이다.', 'LSTM은 시퀀스 데이터가 아닌 이미지 데이터 전용으로 설계된 구조이다.', 'LSTM은 역전파 없이 학습되는 구조이다.']}
  correctAnswer={1}
  explanation="LSTM은 입력 게이트, 망각 게이트, 출력 게이트와 셀 상태를 도입해, 오래 전 정보를 얼마나 유지할지, 새 정보를 얼마나 반영할지를 게이트로 조절함으로써 기본 RNN의 장기 의존성 문제(그라디언트 소실)를 완화합니다."
  optionExplanations={['게이트와 셀 상태를 통한 장기 의존성 완화라는 LSTM의 핵심을 정확히 설명했으므로 정답입니다.', 'LSTM은 오히려 게이트를 추가한 구조이며, 게이트가 없는 것은 기본 RNN에 가까우므로 틀렸습니다.', 'LSTM은 텍스트·시계열 등 순차 데이터 처리에 주로 쓰이는 구조라 틀렸습니다.', 'LSTM도 다른 신경망처럼 시간에 따른 역전파(BPTT)로 학습되므로 틀렸습니다.']}
/>

### 문제 19

<Quiz
  questionNumber={19}
  question="어텐션(attention) 메커니즘의 기본 아이디어로 가장 적절한 것은?"
  options={['디코더가 매 출력 시점마다 인코더의 여러 시점 정보 중 현재 필요한 정보에 더 큰 가중치를 두어 참고한다.', '어텐션은 인코더의 정보를 전혀 사용하지 않고 디코더 혼자 출력을 생성하게 한다.', '어텐션은 항상 입력 시퀀스의 첫 번째 토큰만 참고하도록 강제한다.', '어텐션은 순환 구조를 반드시 포함해야만 정의될 수 있는 개념이다.']}
  correctAnswer={1}
  explanation="어텐션은 디코더가 매 시점 인코더의 여러 은닉 상태에 서로 다른 가중치를 부여해, 현재 출력 생성에 더 관련 있는 부분에 더 집중해서 참고할 수 있게 하는 메커니즘입니다."
  optionExplanations={['필요한 정보에 가중치를 두어 참고한다는 어텐션의 핵심을 정확히 설명했으므로 정답입니다.', '어텐션은 오히려 인코더의 정보를 적극적으로 활용하기 위한 장치이므로 틀렸습니다.', '어텐션은 매 시점마다 가중치가 달라져 다양한 위치를 참고할 수 있으므로, 항상 첫 토큰만 본다는 설명은 틀렸습니다.', 'Transformer의 셀프 어텐션처럼 순환 구조 없이도 어텐션을 정의할 수 있으므로 틀렸습니다.']}
/>

### 문제 20

<Quiz
  questionNumber={20}
  question="Transformer의 셀프 어텐션(self-attention)이 RNN 계열 모델에 비해 갖는 장점으로 가장 적절한 것은?"
  options={['시점을 순차적으로 처리할 필요가 없어 시퀀스 내 모든 위치를 병렬로 계산할 수 있다.', '셀프 어텐션은 시퀀스 길이가 길어져도 계산량이 전혀 늘어나지 않는다.', '셀프 어텐션은 위치 인코딩 없이도 순서를 완벽하게 인식한다.', '셀프 어텐션은 학습 파라미터가 전혀 없는 연산이다.']}
  correctAnswer={1}
  explanation="RNN은 이전 시점의 은닉 상태가 있어야 다음 시점을 계산할 수 있어 순차 처리가 필수이지만, 셀프 어텐션은 모든 위치 쌍의 관계를 동시에 계산할 수 있어 병렬 처리가 가능합니다. 이 덕분에 학습 속도 측면에서 큰 이점을 가집니다."
  optionExplanations={['순차 처리 제약이 없는 병렬 계산이라는 셀프 어텐션의 실제 장점을 정확히 설명했으므로 정답입니다.', '셀프 어텐션의 계산량은 시퀀스 길이의 제곱에 비례해 늘어나므로 틀렸습니다.', '셀프 어텐션 자체는 순서를 구분하지 못해 위치 인코딩이 별도로 필요하므로 틀렸습니다.', '셀프 어텐션은 쿼리·키·값을 만드는 학습 가능한 가중치 행렬을 포함하므로 틀렸습니다.']}
/>

### 문제 21

어떤 3개 클래스 분류 문제에서 출력층 로짓(logit) 값이 $z = [2, 1, 0]$이다. $e^2 \approx 7.389$, $e^1 \approx 2.718$, $e^0 = 1$을 이용한다.

<Quiz
  questionNumber={21}
  question="소프트맥스를 적용했을 때 첫 번째 클래스의 확률은 약 얼마인가?"
  options={['0.665', '0.245', '0.090', '0.333']}
  correctAnswer={1}
  explanation="소프트맥스는 softmax(z_i) = e^(z_i) / sum(e^(z_j))로 계산합니다. 분모는 7.389+2.718+1 = 11.107이고, 첫 번째 클래스의 확률은 7.389/11.107 ≈ 0.665입니다."
  optionExplanations={['소프트맥스 공식에 정확히 대입한 값이므로 정답입니다.', '두 번째 클래스의 확률(2.718/11.107≈0.245)을 첫 번째로 착각한 오답입니다.', '세 번째 클래스의 확률(1/11.107≈0.090)을 첫 번째로 착각한 오답입니다.', '세 클래스가 균등하다고 잘못 가정한(1/3) 오답입니다.']}
/>

### 문제 22

<Quiz
  questionNumber={22}
  question="오토인코더(AE), 변분 오토인코더(VAE), GAN을 비교한 설명으로 옳지 않은 것은?"
  options={['세 모델 모두 반드시 라벨이 있는 지도학습 데이터로만 학습할 수 있다.', 'AE는 결정적(deterministic)인 잠재 벡터를 사용하는 반면, VAE는 확률분포에서 잠재 벡터를 표본으로 뽑는다.', 'GAN은 생성자와 판별자의 적대적 학습 구조를 사용하는 반면, AE와 VAE는 인코더-디코더 구조에서 재구성 손실을 중심으로 학습한다.', 'AE, VAE, GAN 모두 새로운 데이터를 생성하거나 데이터의 압축된 표현을 얻는 데 활용될 수 있다.']}
  correctAnswer={1}
  explanation="AE, VAE, GAN은 모두 별도의 정답 라벨 없이 데이터 자체의 구조로부터 학습하는 비지도학습(또는 자기지도) 방식입니다. 반드시 라벨이 있어야 한다는 설명은 옳지 않습니다."
  optionExplanations={['세 모델 모두 라벨 없이 학습 가능한 비지도학습 계열이므로, 반드시 라벨이 필요하다는 이 설명이 틀린 지점이며 정답입니다.', 'AE의 결정적 잠재 벡터와 VAE의 확률적 잠재 벡터라는 실제 차이를 옳게 설명했습니다.', 'GAN의 적대적 학습과 AE·VAE의 재구성 손실 중심 학습이라는 옳은 대비 설명입니다.', '세 모델 모두 생성 또는 압축된 표현 학습에 활용된다는 옳은 설명입니다.']}
/>

## 서술형 (1–2)

### 서술형 문제 1: 역전파 1스텝 전 과정

하나의 뉴런에 입력 $x = 1$, 목표값(정답) $y = 1$, 가중치 $w = 0.5$, 편향은 없다고 가정한다. 활성화 함수는 시그모이드이며 손실 함수는 $L = \frac{1}{2}(y-a)^2$이다. 학습률 $\eta = 0.1$일 때, 경사하강법으로 가중치를 1회 갱신하는 전 과정을 순서대로 서술하시오.

**모범답안**

1. **순전파**: $z = w x = 0.5 \times 1 = 0.5$이고, $a = \sigma(z) = \dfrac{1}{1+e^{-0.5}} \approx \dfrac{1}{1.6065} \approx 0.6225$입니다.
2. **손실 계산**: $L = \dfrac{1}{2}(y-a)^2 = \dfrac{1}{2}(1-0.6225)^2 = \dfrac{1}{2}(0.3775)^2 \approx 0.0713$입니다.
3. **출력에 대한 손실의 그라디언트**: $L = \dfrac{1}{2}(y-a)^2$을 $a$로 미분하면 $\dfrac{\partial L}{\partial a} = a - y = 0.6225 - 1 = -0.3775$입니다.
4. **활성화 함수의 국소 그라디언트**: 시그모이드의 미분은 $\sigma'(z) = a(1-a)$이므로 $\dfrac{\partial a}{\partial z} = 0.6225 \times (1-0.6225) = 0.6225 \times 0.3775 \approx 0.235$입니다.
5. **가중치에 대한 국소 그라디언트**: $z = wx$이므로 $\dfrac{\partial z}{\partial w} = x = 1$입니다.
6. **연쇄 법칙으로 전체 그라디언트 계산**: $\dfrac{\partial L}{\partial w} = \dfrac{\partial L}{\partial a} \times \dfrac{\partial a}{\partial z} \times \dfrac{\partial z}{\partial w} = (-0.3775) \times 0.235 \times 1 \approx -0.0887$입니다.
7. **가중치 갱신**: $w_{new} = w - \eta \dfrac{\partial L}{\partial w} = 0.5 - 0.1 \times (-0.0887) = 0.5 + 0.00887 \approx 0.5089$입니다.

**해석**: 예측값(0.6225)이 목표값(1)보다 작아 그라디언트가 음수로 나왔고, 경사하강법은 이 그라디언트의 반대 방향으로 이동하므로 가중치가 소폭 증가했습니다. 이는 다음 순전파에서 예측값을 목표값 방향으로 더 가깝게 만드는 방향입니다.

**자주 틀리는 점**: $\dfrac{\partial L}{\partial a}$를 계산할 때 부호를 $y-a$로 잘못 두는 실수가 흔합니다. $L=\frac{1}{2}(y-a)^2$을 $a$에 대해 미분하면 연쇄 법칙에 의해 $-(y-a) = a-y$가 되어야 합니다. 부호를 반대로 쓰면 가중치가 반대 방향으로 갱신되어 학습이 발산할 수 있습니다.

### 서술형 문제 2: CNN 출력 크기와 파라미터 수 종합 계산

입력 이미지 크기가 28x28이고 채널 수는 1(흑백)이다. 이 입력에 3x3 필터 8개를 스트라이드 1, 패딩 1(same padding)로 합성곱한 뒤, 2x2 최대 풀링(스트라이드 2)을 적용한다. 이 과정에서 합성곱층의 출력 크기, 파라미터 수, 풀링 후 출력 크기를 순서대로 계산하시오.

**모범답안**

1. **합성곱 출력 크기**: 패딩이 1이므로 출력 크기 공식은 $\dfrac{(입력 크기 + 2 \times 패딩 - 필터 크기)}{스트라이드} + 1$입니다. 대입하면 $\dfrac{28 + 2 \times 1 - 3}{1} + 1 = \dfrac{27}{1}+1 = 28$이 되어, 합성곱 결과는 28x28 크기를 유지합니다(패딩 1을 준 이유가 바로 이 same padding 효과입니다).
2. **합성곱층 파라미터 수**: 필터 하나의 파라미터는 (필터 높이 × 필터 너비 × 입력 채널) + 편향 1개 = $(3 \times 3 \times 1) + 1 = 10$개입니다. 필터가 8개이므로 전체 파라미터 수는 $10 \times 8 = 80$개입니다.
3. **채널 수 변화**: 합성곱을 거친 특징 맵은 필터 개수만큼의 채널을 가지므로, 출력은 28x28x8이 됩니다.
4. **풀링 출력 크기**: 2x2 최대 풀링을 스트라이드 2로 적용하면 각 변의 크기가 절반이 됩니다. $28 / 2 = 14$이므로 풀링 후 출력은 14x14x8이 됩니다(채널 수는 풀링으로 바뀌지 않습니다).

**해석**: 패딩을 필터 크기에 맞게(3x3 필터에 패딩 1) 주면 합성곱 전후 가로·세로 크기가 유지되는 same padding이 되고, 그 뒤에 오는 풀링층이 공간 크기를 줄이는 역할을 전담하게 됩니다. 채널 수는 합성곱의 필터 개수에 의해서만 바뀌고, 풀링에서는 바뀌지 않는다는 점이 자주 헷갈리는 부분입니다.

**자주 틀리는 점**: 풀링이 채널 수도 함께 줄인다고 착각하거나, same padding에서 패딩 값을 필터 크기와 무관하게 항상 1로 고정한다고 착각하는 경우가 많습니다. 패딩 값은 필터 크기에 따라 달라지며(홀수 크기 필터에서 same padding을 만들려면 패딩은 (필터 크기-1)/2), 위 예시에서는 필터가 3이므로 패딩 1이 정확히 맞아떨어진 것입니다.

## 참고 자료

- Ian Goodfellow 외, Deep Learning (Deep Learning Book) — https://www.deeplearningbook.org
- Stanford CS231n: Convolutional Neural Networks for Visual Recognition — https://cs231n.stanford.edu
- 국가평생교육진흥원 독학학위제 — https://bdes.nile.or.kr
