---
title: '19. 유형문제: 개요·아키텍처·메모리'
description: '임베디드 시스템 개요, MPU/MCU와 ARM 아키텍처, 레지스터·예외 처리, 인터럽트 지연 계산, 메모리 구조를 국가평생교육진흥원 출제기준 기반으로 재구성한 유형 문제 22개'
---

<Callout type="warning">
  이 문제들은 실제 독학사 기출 문제가 아닙니다. 국가평생교육진흥원 독학학위제 임베디드시스템 출제기준(평가영역)을 바탕으로 03·04·05·06·07편의 개념을 재구성한 유형 문제이며, 실제 시험에 나온 문항을 그대로 옮기지 않았습니다. 최신 출제 범위와 문항 수는 반드시 국가평생교육진흥원 공고로 다시 확인하세요.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="하드 실시간(hard real-time) 시스템에 대한 설명으로 가장 옳은 것은?"
  options={['마감을 놓쳐도 결과값의 품질만 조금 떨어질 뿐 시스템 전체에는 문제가 없다', '마감을 놓치면 시스템 오작동이나 안전 문제로 이어질 수 있어 절대적으로 지켜야 한다', '평균 응답 시간만 짧으면 되고 개별 마감은 중요하지 않다', '실시간성과 무관하게 처리량(throughput)만 최대화하면 된다']}
  correctAnswer={2}
  explanation="하드 실시간 시스템은 정해진 데드라인을 반드시 지켜야 하며, 이를 놓치면 시스템 오작동이나 안전 사고로 이어질 수 있는 시스템이다. 자동차 에어백 제어기가 대표적인 예다."
  optionExplanations={['이는 소프트 실시간(soft real-time)에 대한 설명으로, 마감을 놓쳐도 품질 저하 정도로 끝나는 경우다.', '정답이다. 하드 실시간은 데드라인을 절대적으로 지켜야 하는 시스템이다.', '평균 응답 시간만 중시하는 것은 실시간 시스템이 아니라 일반적인 성능 최적화 관점이다.', '처리량만 최대화하는 것은 범용 시스템의 목표이며, 실시간 시스템은 개별 작업의 마감을 지키는 것이 더 중요하다.']}
/>

<Quiz
  questionNumber={2}
  question="임베디드 시스템의 대표적인 설계 제약으로 옳지 않은 것은?"
  options={['실시간성', '전력 소비', '메모리 용량', '무제한의 개발 예산']}
  correctAnswer={4}
  explanation="임베디드 시스템은 실시간성, 전력, 메모리, 비용이라는 네 가지 제약 속에서 설계된다. 개발 예산이 무제한이라는 것은 임베디드 시스템의 특징이 아니라 오히려 반대로 비용 제약이 강하게 작용하는 분야다."
  optionExplanations={['실시간성은 임베디드 시스템의 핵심 제약 중 하나다.', '전력 소비는 배터리 구동 장치 등에서 핵심 제약이 된다.', '메모리 용량은 범용 컴퓨터에 비해 훨씬 제한적인 대표적 제약이다.', '정답이다. 임베디드 시스템은 오히려 비용 제약이 엄격한 분야이며 무제한 예산은 특징이 아니다.']}
/>

<Quiz
  questionNumber={3}
  question="MPU(Microprocessor Unit)와 MCU(Microcontroller Unit)의 차이에 대한 설명으로 옳은 것은?"
  options={['MPU는 메모리와 주변장치를 칩 하나에 통합하고, MCU는 프로세서 코어만 담고 있다', 'MCU는 프로세서 코어, 메모리, 주변장치를 하나의 칩에 통합하고, MPU는 코어 중심으로 외부에 메모리·주변장치를 별도로 연결한다', 'MPU와 MCU는 완전히 동일한 개념으로 시험에서 구분할 필요가 없다', 'MCU는 항상 MPU보다 처리 성능이 높다']}
  correctAnswer={2}
  explanation="MCU는 프로세서 코어와 메모리, 주변장치를 하나의 칩에 통합해 저비용·저전력의 임베디드 제어에 적합하고, MPU는 코어 위주로 외부에 메모리와 주변장치를 별도 칩으로 연결해 더 높은 성능과 확장성을 갖도록 구성한다."
  optionExplanations={['MPU와 MCU의 통합 정도에 대한 설명이 서로 뒤바뀌어 있다.', '정답이다. MCU는 원칩 통합형, MPU는 코어 중심의 외장 확장형이라는 구분이 핵심이다.', '둘은 통합도와 용도가 다른 별개의 개념이므로 동일하다는 설명은 틀렸다.', '일반적으로는 반대로 MPU가 외부 확장을 통해 더 높은 성능을 낼 수 있는 경우가 많아, MCU가 항상 더 높다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={4}
  question="RISC(Reduced Instruction Set Computer)의 특징으로 가장 옳은 것은?"
  options={['명령어 종류가 많고 각 명령어가 복잡한 동작을 한 번에 수행한다', '명령어 종류를 줄이고 단순화해 하드웨어 구조를 단순하게 만들고 파이프라인 처리에 유리하다', '메모리 접근 명령어와 연산 명령어를 구분하지 않는다', '명령어 길이가 항상 가변적이어서 디코딩이 매우 복잡하다']}
  correctAnswer={2}
  explanation="RISC는 명령어 집합을 단순하고 일정한 형태로 줄여 하드웨어 설계를 단순화하고, 각 명령어가 한 클럭에 가깝게 실행되도록 해 파이프라인 처리 효율을 높이는 설계 철학이다."
  optionExplanations={['명령어 종류가 많고 복잡한 동작을 한 번에 수행하는 것은 CISC(Complex Instruction Set Computer)의 특징이다.', '정답이다. 명령어 단순화와 파이프라인 친화성이 RISC의 핵심 특징이다.', 'RISC는 오히려 메모리 접근을 load/store 명령어로 분리하는 load-store 구조를 특징으로 한다.', 'RISC는 대개 명령어 길이를 고정해 디코딩을 단순하게 만드는 쪽에 가깝다.']}
/>

<Quiz
  questionNumber={5}
  question="ARM Cortex-M 계열의 일반적인 특징으로 옳지 않은 것은?"
  options={['Thumb-2 명령어 집합을 사용해 코드 밀도와 성능의 균형을 맞춘다', '내장 인터럽트 컨트롤러인 NVIC를 갖추고 있다', '주로 고성능 서버·데스크톱 운영체제 구동을 목표로 설계되었다', '저비용·저전력 임베디드 제어 용도로 널리 쓰인다']}
  correctAnswer={3}
  explanation="Cortex-M 계열은 저비용·저전력 임베디드 제어를 목표로 설계된 마이크로컨트롤러용 코어이며, 고성능 서버·데스크톱 운영체제 구동은 Cortex-A 계열이 담당하는 영역이다."
  optionExplanations={['Cortex-M은 Thumb-2 명령어 집합으로 코드 밀도와 성능을 함께 확보한다는 설명이 맞다.', 'NVIC(Nested Vectored Interrupt Controller)는 Cortex-M의 핵심 구성요소로 옳은 설명이다.', '옳지 않은 설명이라 정답이다. 고성능 서버·데스크톱용은 Cortex-A 계열의 목표이며 Cortex-M과 반대되는 설명이다.', 'Cortex-M이 저비용·저전력 임베디드 제어에 널리 쓰인다는 설명은 맞다.']}
/>

<Quiz
  questionNumber={6}
  question="Cortex-M의 동작 모드에 대한 설명으로 옳은 것은?"
  options={['Thread 모드에서만 예외 처리를 수행할 수 있다', 'Handler 모드는 예외(인터럽트 포함) 처리 중에 진입하는 모드이고, Thread 모드는 일반 애플리케이션 코드 실행 모드다', 'Thread 모드와 Handler 모드는 완전히 동일해 구분할 필요가 없다', 'Handler 모드에서는 항상 비특권(unprivileged) 수준으로만 동작한다']}
  correctAnswer={2}
  explanation="Cortex-M은 일반 코드를 실행하는 Thread 모드와, 예외나 인터럽트가 발생했을 때 진입하는 Handler 모드를 구분한다. Handler 모드는 항상 특권(privileged) 수준으로 동작한다."
  optionExplanations={['예외 처리는 Thread 모드가 아니라 Handler 모드에서 수행된다.', '정답이다. Handler 모드는 예외 처리 중 진입하는 모드, Thread 모드는 일반 코드 실행 모드라는 구분이 핵심이다.', '두 모드는 진입 조건과 권한 수준이 다르므로 동일하다는 설명은 틀렸다.', 'Handler 모드는 항상 특권 수준으로 동작하므로 비특권으로만 동작한다는 설명은 반대로 되어 있다.']}
/>

<Quiz
  questionNumber={7}
  question="Cortex-M 레지스터 집합에서 R13에 대응하는 이름과 역할로 옳은 것은?"
  options={['LR(Link Register), 함수 복귀 주소를 저장한다', 'PC(Program Counter), 다음에 실행할 명령의 주소를 가리킨다', 'SP(Stack Pointer), 현재 스택의 꼭대기 주소를 가리킨다', 'PSR(Program Status Register), 연산 결과 플래그를 담는다']}
  correctAnswer={3}
  explanation="Cortex-M의 범용 레지스터 R0~R12 다음에 오는 R13은 SP(Stack Pointer, 스택 포인터)로 지정되어 있으며, 현재 사용 중인 스택의 꼭대기 주소를 가리킨다."
  optionExplanations={['LR은 R13이 아니라 R14에 대응한다.', 'PC는 R13이 아니라 R15에 대응한다.', '정답이다. R13은 SP로 지정되어 스택의 꼭대기 주소를 가리킨다.', 'PSR은 R13과 같은 범용 레지스터 번호 체계가 아니라 별도의 특수 레지스터로 다뤄진다.']}
/>

<Quiz
  questionNumber={8}
  question="Cortex-M에서 예외(인터럽트)가 발생했을 때 자동으로 스택에 저장(스태킹)되는 레지스터 묶음으로 가장 옳은 것은?"
  options={['R4~R11만 자동으로 저장된다', 'R0~R3, R12, LR, PC, PSR이 자동으로 저장된다', '모든 범용 레지스터 R0~R12가 예외 없이 전부 자동 저장된다', '스택에는 아무 레지스터도 자동으로 저장되지 않는다']}
  correctAnswer={2}
  explanation="Cortex-M은 예외 진입 시 하드웨어가 자동으로 R0~R3, R12, LR, PC(반환 주소), PSR을 스택에 저장(스태킹)해 ISR이 이 레지스터들을 자유롭게 사용할 수 있게 하고, 예외 복귀 시 자동으로 복원한다."
  optionExplanations={['R4~R11은 자동 스태킹 대상이 아니라, 필요하면 ISR 코드가 직접 저장·복원해야 하는 레지스터다.', '정답이다. R0~R3, R12, LR, PC, PSR 8개 레지스터가 하드웨어에 의해 자동으로 스택에 저장된다.', 'R4~R11은 자동 저장 대상에 포함되지 않으므로 전부 저장된다는 설명은 틀렸다.', '예외 진입 시 스태킹이 전혀 일어나지 않는다면 ISR이 원래 실행 중이던 코드의 문맥을 보존할 수 없다.']}
/>

<Quiz
  questionNumber={9}
  question="인터럽트 벡터 테이블(interrupt vector table)에 대한 설명으로 가장 옳은 것은?"
  options={['각 인터럽트 번호에 대응하는 ISR의 시작 주소를 모아 둔 표다', '인터럽트가 발생한 횟수를 누적해서 기록하는 통계 표다', '인터럽트 우선순위를 텍스트로 설명해 둔 매뉴얼이다', 'ISR 내부에서 사용하는 지역 변수를 모아 둔 표다']}
  correctAnswer={1}
  explanation="인터럽트 벡터 테이블은 각 인터럽트(예외) 번호에 해당하는 ISR(인터럽트 서비스 루틴)의 시작 주소를 메모리의 정해진 위치에 순서대로 모아 둔 표로, 예외 발생 시 프로세서가 이 표를 참조해 해당 ISR로 분기한다."
  optionExplanations={['정답이다. 벡터 테이블은 인터럽트 번호와 ISR 시작 주소를 매핑한 표다.', '발생 횟수를 기록하는 통계 표는 별도의 소프트웨어 카운터로 구현하는 것이며 벡터 테이블의 역할이 아니다.', '벡터 테이블은 텍스트 설명이 아니라 실제 주소 값을 담은 메모리 상의 데이터 구조다.', 'ISR 내부 지역 변수는 스택에 존재하며 벡터 테이블과는 무관하다.']}
/>

<Quiz
  questionNumber={10}
  question="다음 조건에서 인터럽트 지연시간(interrupt latency)을 사이클 수로 계산하면 얼마인가? 조건: 예외 진입 스태킹에 12사이클, 파이프라인 재적재에 3사이클, 현재 실행 중이던 명령의 완료 대기에 최대 2사이클이 필요하다."
  options={['15사이클', '17사이클', '12사이클', '19사이클']}
  correctAnswer={2}
  explanation="인터럽트 지연시간은 예외 진입에 필요한 각 단계의 사이클을 모두 더한 값이다. 12(스태킹) + 3(파이프라인 재적재) + 2(명령 완료 대기) = 17사이클이다."
  optionExplanations={['파이프라인 재적재 3사이클을 빠뜨리고 스태킹과 명령 완료 대기만 더한 값이다.', '정답이다. 12 + 3 + 2 = 17사이클로 세 단계를 모두 정확히 더한 값이다.', '스태킹 12사이클만 계산하고 나머지 두 단계를 누락한 값이다.', '명령 완료 대기를 2가 아니라 4로 잘못 계산해 더한 값이다.']}
/>

<Quiz
  questionNumber={11}
  question="문제 10과 같은 조건(지연시간 17사이클)에서 클럭 주파수가 48MHz일 때, 인터럽트 지연시간을 시간(마이크로초)으로 환산하면 약 얼마인가?"
  options={['약 0.354마이크로초', '약 3.54마이크로초', '약 35.4마이크로초', '약 0.0354마이크로초']}
  correctAnswer={1}
  explanation="한 사이클의 시간은 1을 클럭 주파수로 나눈 값이다. 48MHz에서 한 사이클은 1/48,000,000초이므로 17사이클은 17/48,000,000초이며, 이를 계산하면 약 0.354마이크로초(3.54×10^-7초)가 된다."
  optionExplanations={['정답이다. 17을 48,000,000으로 나누면 약 3.54×10^-7초, 즉 약 0.354마이크로초다.', '자릿수를 한 자리 크게 계산한 값으로, 실제 값보다 10배 큰 값이다.', '자릿수를 두 자리 크게 계산한 값으로, 실제 값보다 100배 큰 값이다.', '자릿수를 한 자리 작게 계산한 값으로, 실제 값보다 10배 작은 값이다.']}
/>

<Quiz
  questionNumber={12}
  question="파이프라인 구조를 가진 프로세서에서 분기(branch) 명령이 파이프라인 성능에 미치는 영향으로 가장 옳은 것은?"
  options={['분기는 파이프라인 성능에 전혀 영향을 주지 않는다', '분기가 예측과 다르게 이루어지면 이미 페치·디코딩된 뒤따르는 명령들을 버려야 해 파이프라인이 비는 구간(플러시)이 생긴다', '분기 명령은 항상 파이프라인의 처리 속도를 두 배로 높인다', '분기는 메모리 접근 명령에만 영향을 주고 연산 명령에는 영향이 없다']}
  correctAnswer={2}
  explanation="파이프라인은 다음에 실행될 명령이 순차적으로 이어진다고 가정하고 미리 페치·디코딩해 둔다. 분기가 일어나 실제 실행 흐름이 예상과 달라지면, 이미 파이프라인에 들어와 있던 뒤따르는 명령들은 무효화(플러시)해야 하므로 그만큼 빈 사이클이 발생한다."
  optionExplanations={['분기는 파이프라인의 순차적 가정을 깨뜨리므로 성능에 직접적인 영향을 준다.', '정답이다. 예측이 어긋난 분기는 파이프라인을 비우는 플러시를 유발해 지연을 만든다.', '분기가 처리 속도를 두 배로 높인다는 것은 근거 없는 설명이며 오히려 지연을 유발하는 요인이다.', '분기는 명령의 종류와 무관하게 파이프라인에 이미 적재된 모든 뒤따르는 명령에 영향을 준다.']}
/>

<Quiz
  questionNumber={13}
  question="ROM, Flash, SRAM, EEPROM의 특징을 짝지은 것 중 옳지 않은 것은?"
  options={['ROM은 제조 시 내용이 고정되어 이후 변경이 사실상 불가능하다', 'Flash 메모리는 전원이 꺼져도 내용이 유지되며 블록 단위로 지우고 다시 쓸 수 있다', 'SRAM은 전원이 꺼지면 내용이 사라지는 휘발성 메모리로 CPU 캐시나 작업용 데이터 저장에 쓰인다', 'EEPROM은 비휘발성이지만 SRAM보다 접근 속도가 항상 더 빠르다']}
  correctAnswer={4}
  explanation="EEPROM(Electrically Erasable Programmable ROM)은 비휘발성이지만 쓰기·지우기에 시간이 걸려 일반적으로 SRAM보다 접근 속도가 느리다. SRAM보다 항상 빠르다는 설명은 옳지 않다."
  optionExplanations={['ROM은 제조 시 내용이 결정되어 이후 변경이 사실상 불가능하다는 설명은 맞다.', 'Flash 메모리가 비휘발성이며 블록 단위로 지우고 쓸 수 있다는 설명은 맞다.', 'SRAM이 휘발성이며 캐시나 작업용 데이터에 쓰인다는 설명은 맞다.', '옳지 않은 설명이라 정답이다. EEPROM은 비휘발성 저장을 위해 SRAM보다 느린 접근 속도를 감수하는 메모리다.']}
/>

<Quiz
  questionNumber={14}
  question="하버드(Harvard) 구조와 폰노이만(von Neumann) 구조의 차이로 가장 옳은 것은?"
  options={['하버드 구조는 명령어와 데이터를 같은 메모리 공간과 버스로 공유하고, 폰노이만 구조는 이를 분리한다', '폰노이만 구조는 명령어와 데이터를 같은 메모리 공간과 버스로 공유하고, 하버드 구조는 이를 분리해 동시 접근이 가능하다', '두 구조는 이름만 다를 뿐 실제 동작은 완전히 동일하다', '하버드 구조는 반드시 하드웨어적으로 두 개의 물리 칩을 사용해야만 구현할 수 있다']}
  correctAnswer={2}
  explanation="폰노이만 구조는 명령어와 데이터가 하나의 메모리 공간과 버스를 공유해 한 번에 하나씩만 접근할 수 있고, 하버드 구조는 명령어 메모리와 데이터 메모리, 버스를 분리해 동시에 접근할 수 있어 성능에 유리하다."
  optionExplanations={['하버드 구조와 폰노이만 구조의 설명이 서로 뒤바뀌어 있다.', '정답이다. 폰노이만은 공유형, 하버드는 분리형이며 하버드 쪽이 동시 접근에 유리하다.', '두 구조는 명령어·데이터 접근 경로를 공유하는지 분리하는지가 다른, 서로 다른 설계 방식이다.', '하버드 구조는 물리적으로 완전히 분리된 칩이 아니라 논리적인 버스·주소 공간 분리로도 구현할 수 있다.']}
/>

<Quiz
  questionNumber={15}
  question="메모리 맵드 I/O(memory-mapped I/O)에 대한 설명으로 옳은 것은?"
  options={['주변장치의 레지스터에 별도의 전용 명령어 집합으로만 접근할 수 있게 하는 방식이다', '주변장치의 레지스터를 일반 메모리와 같은 주소 공간에 배치해 일반 메모리 접근 명령으로 읽고 쓸 수 있게 하는 방식이다', '주변장치는 항상 CPU와 물리적으로 같은 칩에 내장되어야만 사용할 수 있는 방식이다', 'ROM 영역에만 적용되는 개념이며 SRAM에는 적용할 수 없다']}
  correctAnswer={2}
  explanation="메모리 맵드 I/O는 주변장치의 제어·상태 레지스터를 CPU의 일반 메모리 주소 공간 안에 특정 주소로 배치해, 별도의 전용 명령어 없이 일반적인 메모리 읽기·쓰기 명령으로 주변장치를 제어할 수 있게 하는 방식이다."
  optionExplanations={['별도의 전용 입출력 명령어를 사용하는 방식은 포트 맵드 I/O(port-mapped I/O)에 가까우며, 메모리 맵드 I/O는 일반 메모리 명령을 그대로 사용한다.', '정답이다. 주변장치 레지스터를 메모리 주소 공간에 배치해 일반 메모리 명령으로 접근하는 것이 메모리 맵드 I/O의 핵심이다.', '메모리 맵드 I/O는 물리적 위치가 아니라 주소 공간에 매핑되는 방식의 문제이며 내장 여부와 직접 관련이 없다.', '메모리 맵드 I/O는 특정 메모리 종류에 국한된 개념이 아니라 주소 공간 전반에 적용되는 설계 방식이다.']}
/>

<Quiz
  questionNumber={16}
  question="다음 중 Memory Protection Unit(MPU, 메모리 보호 장치)에 대한 설명으로 옳은 것은?"
  options={['가상 주소를 물리 주소로 변환하는 페이지 테이블 기반 주소 변환 기능을 제공한다', '메모리 영역별로 접근 권한(읽기·쓰기·실행)을 설정해 잘못된 접근을 감지하고 차단하는 기능을 제공한다', '메모리 용량을 물리적으로 확장하는 하드웨어 장치다', 'CPU의 연산 속도를 높이기 위한 캐시 메모리의 다른 이름이다']}
  correctAnswer={2}
  explanation="이 문제에서 MPU는 Memory Protection Unit(메모리 보호 장치)을 가리킨다. MPU는 메모리를 여러 영역으로 나누고 각 영역에 읽기·쓰기·실행 권한을 설정해, 허용되지 않은 접근이 일어나면 예외를 발생시켜 차단하는 기능을 한다."
  optionExplanations={['가상 주소를 물리 주소로 변환하는 페이지 테이블 기반 기능은 MMU(Memory Management Unit)의 역할이며, MPU에는 일반적으로 주소 변환 기능이 없다.', '정답이다. 영역별 접근 권한 설정과 위반 감지가 MPU(Memory Protection Unit)의 핵심 기능이다.', 'MPU는 물리적인 메모리 용량을 늘리는 장치가 아니라 접근을 제어하는 보호 장치다.', 'MPU는 캐시 메모리와 다른 개념이며 캐시의 별칭이 아니다.']}
/>

<Quiz
  questionNumber={17}
  question="MMU(Memory Management Unit)와 MPU(Memory Protection Unit)의 차이로 가장 옳은 것은?"
  options={['MMU는 가상 주소-물리 주소 변환까지 지원하고, MPU는 고정된 물리 주소 영역에 대한 접근 권한만 관리한다', 'MPU가 MMU보다 더 복잡한 주소 변환 기능을 제공한다', '두 장치는 동일한 하드웨어를 부르는 서로 다른 이름일 뿐이다', 'MMU는 저사양 MCU에서 주로 쓰이고 MPU는 고사양 애플리케이션 프로세서에서 주로 쓰인다']}
  correctAnswer={1}
  explanation="MMU는 가상 메모리를 지원하기 위해 페이지 테이블로 가상 주소를 물리 주소로 변환하는 복잡한 기능까지 담당하는 반면, MPU는 주소 변환 없이 정해진 물리 주소 영역에 대한 접근 권한만 검사하는 더 단순한 장치다."
  optionExplanations={['정답이다. MMU는 주소 변환까지 담당하는 더 복잡한 장치이고, MPU는 권한 검사만 담당하는 단순한 장치다.', 'MPU가 아니라 MMU 쪽이 더 복잡한 주소 변환 기능을 제공하므로 이 설명은 반대로 되어 있다.', 'MMU와 MPU는 기능 범위가 다른 별개의 하드웨어이므로 동일한 것을 부르는 이름이 아니다.', '일반적으로 MMU는 운영체제의 가상 메모리를 지원하는 고사양 프로세서에, MPU는 자원이 제한적인 저사양 MCU에 더 흔히 쓰여 이 설명은 반대로 되어 있다.']}
/>

<Quiz
  questionNumber={18}
  question="임베디드 시스템의 메모리 계층을 휘발성과 비휘발성으로 구분할 때, 다음 중 성격이 다른 하나는?"
  options={['SRAM', 'Flash 메모리', 'ROM', 'EEPROM']}
  correctAnswer={1}
  explanation="SRAM은 전원이 꺼지면 내용이 사라지는 휘발성 메모리인 반면, Flash·ROM·EEPROM은 전원이 꺼져도 내용이 유지되는 비휘발성 메모리다."
  optionExplanations={['정답이다. SRAM만 휘발성이며 나머지 세 개는 모두 비휘발성이다.', 'Flash 메모리는 비휘발성으로 SRAM과 성격이 다르다.', 'ROM은 비휘발성으로 SRAM과 성격이 다르다.', 'EEPROM은 비휘발성으로 SRAM과 성격이 다르다.']}
/>

<Quiz
  questionNumber={19}
  question="NVIC(Nested Vectored Interrupt Controller)의 역할에 대한 설명으로 가장 옳은 것은?"
  options={['여러 인터럽트의 우선순위를 관리하고 우선순위가 높은 인터럽트가 낮은 인터럽트 처리 중에도 끼어들 수 있게(중첩) 지원한다', '인터럽트를 발생시키는 각 주변장치의 전원 공급을 직접 제어한다', 'CPU 코어의 파이프라인 단계 수를 동적으로 조절한다', '프로그램 코드를 컴파일 시점에 최적화하는 도구다']}
  correctAnswer={1}
  explanation="NVIC는 Cortex-M에 내장된 인터럽트 컨트롤러로, 여러 인터럽트 소스에 우선순위를 부여하고 관리하며, 이름의 Nested(중첩)가 뜻하듯 우선순위가 높은 인터럽트가 처리 중인 낮은 우선순위 인터럽트에 끼어들 수 있도록 지원한다."
  optionExplanations={['정답이다. 우선순위 관리와 중첩 인터럽트 지원이 NVIC의 핵심 역할이다.', '전원 공급 제어는 NVIC가 아니라 별도의 전력 관리 회로의 역할이다.', '파이프라인 단계 수는 설계 시점에 고정되는 것으로 NVIC가 동적으로 조절하는 대상이 아니다.', 'NVIC는 실행 시점의 하드웨어 인터럽트 컨트롤러이며 컴파일 시점 도구가 아니다.']}
/>

<Quiz
  questionNumber={20}
  question="Cortex-M의 MSP(Main Stack Pointer)와 PSP(Process Stack Pointer)에 대한 설명으로 옳은 것은?"
  options={['MSP와 PSP는 완전히 동일한 스택을 가리키는 중복된 이름이다', '일반적으로 Handler 모드와 초기 부팅은 MSP를 사용하고, RTOS 환경의 태스크 코드는 PSP를 사용하도록 구분해 스택을 분리할 수 있다', 'PSP는 하드웨어 예외 진입 시 자동으로만 사용되고 소프트웨어가 선택할 수 없다', 'MSP는 인터럽트 벡터 테이블의 저장 위치를 가리키는 레지스터다']}
  correctAnswer={2}
  explanation="Cortex-M은 두 개의 스택 포인터(MSP, PSP)를 두어, Handler 모드와 초기 부팅 코드는 MSP를, RTOS의 각 태스크 실행 코드는 PSP를 사용하도록 구분할 수 있다. 이렇게 하면 커널 스택과 태스크 스택을 분리해 관리할 수 있다."
  optionExplanations={['MSP와 PSP는 서로 다른 스택 공간을 가리키도록 분리해 사용할 수 있는 별개의 포인터다.', '정답이다. Handler 모드·부팅은 MSP, RTOS 태스크는 PSP로 구분하는 것이 전형적인 사용 방식이다.', '어떤 스택 포인터를 사용할지는 CONTROL 레지스터 설정으로 소프트웨어가 선택할 수 있다.', '인터럽트 벡터 테이블의 위치는 별도의 벡터 테이블 오프셋 레지스터가 가리키며 MSP의 역할이 아니다.']}
/>

<Quiz
  questionNumber={21}
  question="예외 벡터 테이블에서 인덱스 1번 항목(초기 MSP 값 다음에 오는 첫 번째 항목)이 일반적으로 가리키는 것은?"
  options={['가장 우선순위가 낮은 사용자 인터럽트의 핸들러 주소', '리셋 핸들러(Reset Handler)의 시작 주소', '마지막으로 실행된 명령의 주소', '임의의 값으로 항목마다 의미가 정해져 있지 않다']}
  correctAnswer={2}
  explanation="Cortex-M의 벡터 테이블은 0번 항목에 초기 MSP 값을, 1번 항목에 리셋 핸들러(Reset Handler)의 시작 주소를 담아, 전원이 켜지거나 리셋될 때 이 주소로 실행을 시작하도록 정해져 있다."
  optionExplanations={['특정 사용자 인터럽트 핸들러는 벡터 테이블의 뒤쪽에 정해진 순서대로 배치되며, 1번 항목은 리셋 핸들러로 고정되어 있다.', '정답이다. 벡터 테이블의 1번 항목은 리셋 핸들러의 시작 주소로 고정되어 있다.', '벡터 테이블의 항목은 마지막 실행 명령을 기록하는 곳이 아니라 예외별 핸들러 주소를 미리 정의해 둔 표다.', '벡터 테이블의 각 항목은 예외 번호에 따라 정해진 의미를 가지므로 임의의 값이 아니다.']}
/>

<Quiz
  questionNumber={22}
  question="임베디드 시스템의 개요와 아키텍처에 대한 설명 중 옳지 않은 것은?"
  options={['MCU는 코어·메모리·주변장치를 하나의 칩에 통합해 원가와 전력 면에서 유리하다', '하버드 구조는 명령어와 데이터 버스를 분리해 폰노이만 구조보다 동시 접근성이 높다', '소프트 실시간 시스템은 마감을 놓쳐도 시스템이 즉시 안전 문제로 이어지지 않는 경우가 많다', 'Cortex-M 계열은 항상 MMU를 내장해 완전한 가상 메모리 관리 기능을 제공한다']}
  correctAnswer={4}
  explanation="Cortex-M 계열은 일반적으로 MMU가 아니라 더 단순한 MPU(Memory Protection Unit)를 옵션으로 제공하며, 완전한 가상 메모리 관리를 위한 MMU는 일반적으로 Cortex-A 계열에서 지원한다."
  optionExplanations={['MCU가 원칩 통합형으로 원가·전력에 유리하다는 설명은 옳다.', '하버드 구조가 폰노이만 구조보다 동시 접근성이 높다는 설명은 옳다.', '소프트 실시간 시스템에 대한 설명은 옳다.', '옳지 않은 설명이라 정답이다. Cortex-M은 일반적으로 MMU가 아니라 MPU를 옵션으로 제공하며, MMU 지원은 Cortex-M의 일반적인 특징이 아니다.']}
/>

## 참고 자료

- [Arm Cortex-M resources](https://developer.arm.com/community/arm-community-blogs/b/architectures-and-processors-blog/posts/cortex-m-resources)
- [CMSIS-Core (Cortex-M): Overview](https://arm-software.github.io/CMSIS_6/latest/Core/index.html)
- [Cortex-M4 Technical Reference Manual](https://documentation-service.arm.com/static/5f19da2a20b7cf4bc524d99a)
- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
