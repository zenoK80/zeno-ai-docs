---
title: '26. 독학사 컴퓨터네트워크 예상기출문제 3'
description: '데이터링크 계층의 오류 제어·흐름 제어 계산, 물리 계층 전송 기술, LAN·WAN 비교를 다루는 예상 기출 문제 세트'
---

<Callout type="warning">
  이 문제들은 실제 독학사 기출 문제가 아닙니다. 국가평생교육진흥원 독학학위제 컴퓨터네트워크 출제기준(평가영역)을 바탕으로 재구성한 예상 문제이며, 실제 시험에 나온 문항을 그대로 옮기지 않았습니다. 최신 출제 범위와 문항 수는 반드시 국가평생교육진흥원 공고로 다시 확인하세요.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="데이터링크 계층에서 프레이밍(framing)을 수행하는 목적으로 가장 옳은 것은?"
  options={['비트 스트림을 프레임 단위로 나누어 프레임의 시작과 끝을 구분할 수 있게 한다', '서로 다른 네트워크 사이의 최적 경로를 선택한다', '종단 간 여러 프로세스를 구분해 다중화한다', '아날로그 신호를 디지털 신호로 변조한다']}
  correctAnswer={1}
  explanation="물리 계층이 전달하는 것은 구분 없는 비트의 흐름이다. 데이터링크 계층은 이 비트열에 시작과 끝을 표시하는 구분자나 길이 필드를 붙여 프레임 단위로 나누는 프레이밍을 수행함으로써, 수신측이 하나의 프레임이 어디서 시작해 어디서 끝나는지 알 수 있게 한다."
  optionExplanations={['정답이다. 프레이밍은 비트열을 프레임 단위로 구분하는 것이 핵심 목적이다.', '경로 선택은 네트워크 계층의 라우팅 기능이다. 계층을 혼동한 오답이다.', '프로세스 구분과 다중화는 전송 계층의 포트 번호 기능이다. 계층을 혼동한 오답이다.', '신호 변조는 물리 계층의 기능이며 프레이밍과는 다른 개념이다.']}
/>

<Quiz
  questionNumber={2}
  question="패리티 검사, 체크섬, CRC 등 오류 검출 기법에 대한 설명 중 옳지 않은 것은?"
  options={['패리티 검사는 짝수 개의 비트 오류가 동시에 발생하면 이를 탐지하지 못할 수 있다', '체크섬은 데이터를 일정 크기로 나눈 뒤 그 값을 합산해 오류를 검사한다', 'CRC는 다항식 나눗셈을 이용하며 일반적으로 패리티 검사보다 오류 검출 능력이 뛰어나다', '패리티, 체크섬, CRC는 모두 오류를 검출할 뿐만 아니라 손상된 비트를 스스로 정정할 수 있다']}
  correctAnswer={4}
  explanation="패리티 검사, 체크섬, CRC는 모두 오류 검출(error detection) 기법이다. 오류가 발생했다는 사실은 알 수 있지만 어느 비트가 손상되었는지까지는 알 수 없어 스스로 정정하지 못하며, 오류가 발견되면 일반적으로 재전송을 요청한다."
  optionExplanations={['옳은 설명이다. 짝수 개의 비트가 동시에 뒤집히면 패리티 합이 그대로 유지되어 검출하지 못할 수 있다.', '옳은 설명이다. 체크섬은 합산 방식으로 오류를 검사하는 기법이다.', '옳은 설명이다. CRC는 다항식 나눗셈 기반으로 패리티보다 강력한 오류 검출 능력을 가진다.', '옳지 않은 설명이라 정답이다. 이 세 기법은 오류 검출 전용이며 오류 정정 기능은 없다. 검출과 정정을 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={3}
  question="데이터 1010에 생성 다항식(발생기) 1011(CRC-3, x³+x+1에 해당)을 적용해 CRC 검사 비트를 계산할 때, 데이터 뒤에 덧붙일 3비트의 CRC 값은?"
  options={['011', '001', '111', '100']}
  correctAnswer={1}
  explanation="데이터 1010 뒤에 발생기 차수만큼(3비트) 0을 붙인 1010000을 발생기 1011로 모듈로-2 나눗셈(XOR 연산)한다. 1010000을 1011로 나누면 1010 XOR 1011=0001, 이어서 남은 자리에서 다시 1011과 XOR하면 최종 나머지 011이 남는다. 따라서 CRC 비트는 011이며, 전송 프레임은 1010011이 된다."
  optionExplanations={['정답이다. 모듈로-2 나눗셈을 끝까지 수행하면 나머지 011이 남는다.', '나눗셈 중간 단계에서 XOR을 한 번만 수행하고 멈춰 나머지 자리를 처리하지 않은 오답이다.', '데이터와 발생기의 순서를 바꾸는 등 나눗셈 과정을 잘못 적용해 나온 오답이다.', '나머지 계산 없이 발생기 값의 뒷자리를 그대로 옮겨 적은 오답이다.']}
/>

<Quiz
  questionNumber={4}
  question="7비트 데이터 1011001에 짝수(even) 패리티 비트를 추가한다고 할 때, 추가되는 패리티 비트 값은?"
  options={['0', '1', '계산할 수 없다', '데이터 길이가 짝수여야만 계산할 수 있다']}
  correctAnswer={1}
  explanation="1011001에서 1의 개수를 세면 1, 1, 1, 1로 총 4개다. 이미 짝수 개이므로 전체 1의 개수를 짝수로 유지하려면 패리티 비트는 0이어야 한다."
  optionExplanations={['정답이다. 데이터에 포함된 1의 개수가 이미 짝수(4개)이므로 패리티 비트는 0이다.', '1의 개수를 홀수로 잘못 세어 짝수로 맞추기 위해 1을 추가해야 한다고 착각한 계산 오류다.', '패리티 비트는 1의 개수의 홀짝만 세면 항상 계산할 수 있다. 계산이 불가능하다는 것은 잘못된 설명이다.', '패리티 계산은 데이터 길이의 홀짝과는 무관하게 1의 개수만으로 정해진다. 무관한 조건을 끌어들인 오답이다.']}
/>

<Quiz
  questionNumber={5}
  question="Stop-and-Wait ARQ 방식에 대한 설명 중 옳지 않은 것은?"
  options={['송신측은 하나의 프레임을 보낸 뒤 수신측의 확인 응답(ACK)을 받을 때까지 다음 프레임을 보내지 않는다', '확인 응답이 일정 시간 내에 오지 않으면 타이머 만료로 해당 프레임을 재전송한다', 'Stop-and-Wait 방식은 회선의 대역폭을 매 순간 최대로 활용해 전송 효율이 매우 높다', 'Stop-and-Wait 방식은 구현이 단순하지만 전파 지연이 큰 회선에서는 유휴 시간이 많아 비효율적이다']}
  correctAnswer={3}
  explanation="Stop-and-Wait 방식은 확인 응답을 기다리는 동안 송신측이 아무것도 보내지 못해 회선이 노는 시간이 길다. 특히 전파 지연이 큰 회선에서는 회선 이용률이 크게 떨어지므로, 대역폭을 최대로 활용해 효율이 매우 높다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. Stop-and-Wait의 기본 동작 방식이다.', '옳은 설명이다. 타이머 만료에 의한 재전송은 Stop-and-Wait ARQ의 핵심 오류 복구 메커니즘이다.', '옳지 않은 설명이라 정답이다. 실제로는 확인 응답을 기다리는 유휴 시간 때문에 이용률이 낮다. 효율을 반대로 서술한 오답이다.', '옳은 설명이다. 전파 지연이 큰 환경에서 Stop-and-Wait의 비효율은 대표적인 시험 포인트다.']}
/>

<Quiz
  questionNumber={6}
  question="슬라이딩 윈도우(sliding window) 흐름 제어 방식에 대한 설명으로 가장 옳은 것은?"
  options={['확인 응답을 기다리지 않고 윈도우 크기만큼 여러 프레임을 연속으로 전송할 수 있다', '슬라이딩 윈도우 방식은 Stop-and-Wait 방식보다 회선 이용률이 낮다', '슬라이딩 윈도우 방식도 한 번에 하나의 프레임만 전송할 수 있다는 점에서 Stop-and-Wait와 동일하다', '슬라이딩 윈도우 방식은 흐름 제어 기능이 없고 오류 검출만 수행한다']}
  correctAnswer={1}
  explanation="슬라이딩 윈도우 방식은 확인 응답을 받기 전에도 윈도우 크기만큼의 프레임을 연속으로 전송할 수 있게 하여, 회선이 노는 시간을 줄이고 Stop-and-Wait보다 이용률을 높인다."
  optionExplanations={['정답이다. 윈도우 크기만큼 연속 전송이 가능하다는 점이 슬라이딩 윈도우의 핵심이다.', '슬라이딩 윈도우는 Stop-and-Wait보다 이용률이 높은 것이 핵심 장점이다. 이를 반대로 서술한 오답이다.', '슬라이딩 윈도우의 존재 이유 자체가 여러 프레임을 연속 전송하기 위함이다. Stop-and-Wait와 동일하다는 것은 잘못된 설명이다.', '슬라이딩 윈도우는 흐름 제어를 위한 대표적인 메커니즘이다. 흐름 제어 기능이 없다는 것은 잘못된 설명이다.']}
/>

<Quiz
  questionNumber={7}
  question="대역폭이 2Mbps인 링크에서 한쪽 방향 전파 지연(propagation delay)이 10ms일 때, 대역폭-지연 곱(bandwidth-delay product)은?"
  options={['20,000비트', '2,000비트', '200,000비트', '20비트']}
  correctAnswer={1}
  explanation="대역폭-지연 곱은 대역폭과 지연 시간을 곱한 값으로, 확인 응답 없이 회선에 채울 수 있는 최대 비트 수를 뜻한다. 2Mbps(2,000,000bps)×0.01초(10ms)=20,000비트가 된다."
  optionExplanations={['정답이다. 2,000,000bps×0.01초=20,000비트로 정확히 계산했다.', '10ms를 0.001초로 소수점을 한 자리 잘못 옮겨 계산한 오답이다.', '10ms를 0.1초로 소수점을 한 자리 잘못 옮겨 계산한 오답이다.', '단위 환산 없이 2Mbps의 숫자 2와 지연 시간의 숫자 10만 곱해 계산한 오답이다.']}
/>

<Quiz
  questionNumber={8}
  question="CSMA/CD(Carrier Sense Multiple Access with Collision Detection)의 동작 원리에 대한 설명으로 가장 옳은 것은?"
  options={['전송 전에 회선이 비어 있는지 확인하고, 전송 중에도 충돌을 감지하면 즉시 전송을 멈추고 잼(jam) 신호를 보낸다', '전송 전에 회선 상태와 무관하게 항상 바로 데이터를 전송한다', '충돌이 발생해도 이를 감지하지 않고 전송을 끝까지 마친다', '무선 랜에서 충돌을 물리적으로 감지하기 위해 사용하는 대표적인 방식이다']}
  correctAnswer={1}
  explanation="CSMA/CD는 유선 이더넷에서 사용하는 매체 접근 방식으로, 전송 전 반송파를 감지(carrier sense)해 회선이 비었는지 확인하고, 전송 중에도 충돌을 감지(collision detection)하면 즉시 전송을 멈추고 잼 신호를 보내 다른 장치에도 충돌을 알린다."
  optionExplanations={['정답이다. 반송파 감지와 충돌 감지, 잼 신호 전송이 CSMA/CD의 핵심 동작이다.', '회선이 비었는지 확인하지 않고 바로 전송하면 충돌 가능성이 크게 높아진다. CSMA/CD의 반송파 감지 단계를 무시한 오답이다.', '충돌을 감지하지 못하면 CD(Collision Detection)라는 이름 자체가 무의미해진다. CSMA/CD의 핵심 기능을 부정한 오답이다.', '무선 랜에서는 충돌을 물리적으로 감지하기 어려워 CSMA/CD 대신 CSMA/CA를 사용한다. 유선과 무선 방식을 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={9}
  question="이더넷(Ethernet)에서 프레임의 최소 크기를 64바이트로 정한 이유로 가장 옳은 것은?"
  options={['송신측이 프레임 전송을 마치기 전에 발생 가능한 충돌을 반드시 감지할 수 있도록 하기 위해서다', '프레임 헤더의 모든 필드를 담기 위한 최소 용량이 정확히 64바이트이기 때문이다', '수신측 버퍼 크기가 정확히 64바이트로 고정되어 있기 때문이다', 'IP 계층에서 요구하는 최소 패킷 크기가 64바이트이기 때문이다']}
  correctAnswer={1}
  explanation="CSMA/CD가 충돌을 정상적으로 감지하려면, 송신측이 프레임 전송을 마치기 전에 네트워크 반대편에서 발생한 충돌 신호가 되돌아와야 한다. 최대 왕복 전파 지연을 고려했을 때 이 조건을 만족시키기 위해 필요한 최소 전송 시간에 해당하는 크기가 64바이트로 정해졌다."
  optionExplanations={['정답이다. 충돌 감지가 가능한 최소 전송 시간을 보장하기 위한 크기다.', '헤더 필드 용량과는 무관하며, 64바이트는 헤더가 아니라 프레임 전체의 최소 크기다.', '수신측 버퍼 크기는 이더넷 프레임 최소 크기를 정한 이유와는 관련이 없다.', 'IP는 네트워크 계층의 프로토콜이며 이더넷 프레임 최소 크기는 데이터링크 계층의 충돌 감지 요건에서 비롯된 것이다. 계층을 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={10}
  question="MAC(Media Access Control) 주소의 구조에 대한 설명으로 가장 옳은 것은?"
  options={['총 48비트로 구성되며 앞 24비트는 제조사를 식별하는 OUI, 뒤 24비트는 제조사가 부여한 장치 일련번호다', '총 32비트로 구성되며 네트워크 부분과 호스트 부분으로 나뉜다', '총 48비트로 구성되며 처음 8비트는 항상 클래스를 나타낸다', '총 128비트로 구성되며 8개의 16비트 블록으로 표기한다']}
  correctAnswer={1}
  explanation="MAC 주소는 48비트(6바이트)로 구성되며, 앞의 24비트(3바이트)는 제조사를 식별하는 OUI(Organizationally Unique Identifier), 나머지 24비트는 제조사가 개별 장치에 부여하는 일련번호다."
  optionExplanations={['정답이다. 48비트 구성과 OUI·일련번호 구분이 MAC 주소의 정확한 구조다.', '32비트에 네트워크·호스트 부분 구분은 IPv4 주소의 구조다. IP 주소와 MAC 주소를 혼동한 오답이다.', 'MAC 주소에는 클래스 개념이 없다. IPv4의 클래스 기반 주소 체계와 혼동한 오답이다.', '128비트에 16비트 블록 8개로 표기하는 것은 IPv6 주소의 구조다. IPv6 주소와 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={11}
  question="이더넷 스위치의 자기 학습(self-learning) 기능에 대한 설명으로 가장 옳은 것은?"
  options={['수신된 프레임의 출발지 MAC 주소와 수신 포트를 MAC 주소 테이블에 기록해 이후 해당 주소로 가는 프레임을 그 포트로만 전달한다', '수신된 프레임의 목적지 MAC 주소를 이용해 항상 모든 포트로 프레임을 전달한다', '수신된 프레임의 발신지 IP 주소를 이용해 라우팅 테이블을 구성한다', '스위치는 학습 기능이 없어 모든 프레임을 항상 모든 포트로 플러딩(flooding)한다']}
  correctAnswer={1}
  explanation="스위치는 프레임이 들어올 때마다 출발지 MAC 주소와 수신 포트 번호를 MAC 주소 테이블에 기록한다. 이후 그 MAC 주소를 목적지로 하는 프레임이 오면 모든 포트로 뿌리지 않고 기록된 포트로만 전달해 불필요한 트래픽을 줄인다."
  optionExplanations={['정답이다. 출발지 MAC 주소와 수신 포트를 매핑해 학습하는 것이 스위치의 핵심 동작이다.', '목적지 주소를 모든 포트로 전달하는 것은 주소를 모를 때의 플러딩 동작이며, 학습이 끝난 뒤에는 해당 포트로만 전달한다.', 'MAC 주소 테이블은 IP 주소가 아니라 MAC 주소를 기준으로 구성된다. 라우팅 테이블과 혼동한 오답이다.', '자기 학습 기능이 스위치의 핵심 특징이며, 학습 전(주소를 모를 때)에만 일시적으로 플러딩한다. 학습 기능이 아예 없다는 것은 잘못된 설명이다.']}
/>

<Quiz
  questionNumber={12}
  question="허브, 브리지, 스위치를 비교한 설명 중 옳지 않은 것은?"
  options={['허브는 물리 계층 장비로 수신한 신호를 단순히 재생·중계할 뿐 MAC 주소를 참조하지 않는다', '브리지와 스위치는 데이터링크 계층 장비로 MAC 주소를 참조해 프레임을 전달한다', '스위치는 브리지보다 일반적으로 포트 수가 많고 하드웨어 기반 처리로 더 빠르게 동작한다', '허브를 사용하면 스위치를 사용할 때보다 충돌이 줄어들어 네트워크 성능이 향상된다']}
  correctAnswer={4}
  explanation="허브는 모든 포트가 하나의 충돌 도메인을 공유해 포트 수가 늘수록 충돌 가능성이 커진다. 반면 스위치는 포트마다 별도의 충돌 도메인을 만들어 충돌을 크게 줄인다. 따라서 허브를 쓰면 충돌이 줄어든다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 허브는 물리 계층 장비로 MAC 주소를 참조하지 않는다.', '옳은 설명이다. 브리지와 스위치는 모두 데이터링크 계층에서 MAC 주소 기반으로 동작한다.', '옳은 설명이다. 스위치는 브리지의 발전된 형태로 더 많은 포트와 빠른 하드웨어 처리를 제공하는 것이 일반적이다.', '옳지 않은 설명이라 정답이다. 실제로는 허브가 충돌 도메인을 하나로 묶어 충돌을 늘리고, 스위치가 포트별로 충돌 도메인을 나누어 충돌을 줄인다. 두 장비의 효과를 반대로 서술한 오답이다.']}
/>

<Quiz
  questionNumber={13}
  question="허브와 스위치의 충돌 도메인·브로드캐스트 도메인 차이에 대한 설명으로 옳은 것은?"
  options={['허브는 연결된 모든 포트가 하나의 충돌 도메인을 이루지만, 스위치는 포트마다 별도의 충돌 도메인을 이룬다', '스위치는 포트마다 별도의 브로드캐스트 도메인을 이루어 브로드캐스트 트래픽을 자동으로 차단한다', '허브는 포트마다 별도의 충돌 도메인을 이루어 충돌이 거의 발생하지 않는다', '스위치와 허브 모두 포트 수와 무관하게 항상 하나의 충돌 도메인만 존재한다']}
  correctAnswer={1}
  explanation="허브는 모든 포트가 동일한 물리적 매체를 공유하는 것처럼 동작해 전체가 하나의 충돌 도메인이 된다. 스위치는 포트마다 프레임을 개별적으로 전달하므로 포트별로 별도의 충돌 도메인을 형성한다. 다만 VLAN으로 나누지 않는 한 스위치의 모든 포트는 여전히 하나의 브로드캐스트 도메인을 공유한다."
  optionExplanations={['정답이다. 허브는 전체가 하나의 충돌 도메인, 스위치는 포트별로 별도의 충돌 도메인을 이룬다.', 'VLAN 설정이 없다면 스위치의 모든 포트는 하나의 브로드캐스트 도메인을 공유한다. 브로드캐스트 도메인이 자동으로 나뉜다는 것은 잘못된 설명이다.', '허브는 모든 포트가 하나의 충돌 도메인을 공유하는 장비다. 반대로 서술한 오답이다.', '스위치는 포트별로 별도의 충돌 도메인을 형성하는 것이 허브와의 핵심 차이다. 이를 부정한 오답이다.']}
/>

<Quiz
  questionNumber={14}
  question="VLAN(Virtual LAN)에 대한 설명으로 가장 옳은 것은?"
  options={['물리적인 배치나 연결 위치와 무관하게 논리적으로 브로드캐스트 도메인을 나누는 기술이다', '서로 다른 건물에 있는 스위치는 물리적 거리 때문에 VLAN으로 묶을 수 없다', 'VLAN은 데이터링크 계층이 아니라 물리 계층에서만 동작하는 기술이다', 'VLAN을 사용하면 모든 장치가 하나의 거대한 충돌 도메인으로 통합된다']}
  correctAnswer={1}
  explanation="VLAN은 물리적인 배선이나 위치와 관계없이 논리적으로 브로드캐스트 도메인을 나누어, 같은 스위치에 연결되어 있어도 서로 다른 VLAN에 속하면 별도의 네트워크처럼 분리할 수 있게 하는 기술이다."
  optionExplanations={['정답이다. 물리적 배치와 무관한 논리적 분리가 VLAN의 핵심 개념이다.', 'VLAN은 트렁크 포트 등을 이용해 물리적으로 떨어진 스위치 사이에서도 같은 VLAN으로 묶을 수 있다. 물리적 거리 제약이 있다는 것은 잘못된 설명이다.', 'VLAN은 데이터링크 계층(2계층)에서 동작하는 기술이다. 계층을 혼동한 오답이다.', 'VLAN은 오히려 하나의 물리적 네트워크를 여러 개의 브로드캐스트 도메인으로 나누는 기술이다. 목적을 반대로 서술한 오답이다.']}
/>

<Quiz
  questionNumber={15}
  question="FDM, TDM, WDM 다중화 방식에 대한 설명 중 옳지 않은 것은?"
  options={['FDM은 서로 다른 주파수 대역을 나누어 여러 신호를 동시에 전송한다', 'TDM은 하나의 채널을 시간 슬롯으로 나누어 여러 신호가 번갈아 전송되게 한다', 'WDM은 광섬유에서 서로 다른 파장의 빛을 이용해 다중화하는 방식으로, TDM의 한 종류로 분류된다', '다중화 기법은 하나의 물리적 전송 매체를 여러 논리적 채널로 나누어 자원을 효율적으로 사용하기 위한 것이다']}
  correctAnswer={3}
  explanation="WDM(Wavelength Division Multiplexing)은 서로 다른 파장(주파수)의 빛을 이용해 신호를 나누는 방식으로, 시간을 나누는 TDM이 아니라 주파수를 나누는 FDM 계열의 개념에 가깝다. WDM을 TDM의 한 종류로 분류한 것은 옳지 않다."
  optionExplanations={['옳은 설명이다. FDM은 주파수 대역을 나누어 동시에 전송하는 방식이다.', '옳은 설명이다. TDM은 시간 슬롯을 나누어 번갈아 전송하는 방식이다.', '옳지 않은 설명이라 정답이다. WDM은 파장(주파수)을 나누는 방식으로 FDM 계열에 가까우며 TDM과는 원리가 다르다. 두 다중화 방식의 분류를 혼동한 오답이다.', '옳은 설명이다. 다중화의 공통 목적은 하나의 매체를 여러 채널로 나누어 효율적으로 쓰는 것이다.']}
/>

<Quiz
  questionNumber={16}
  question="스위치의 저장 후 전달(store-and-forward) 방식과 컷스루(cut-through) 방식을 비교한 설명 중 옳지 않은 것은?"
  options={['저장 후 전달 방식은 프레임 전체를 수신해 오류를 검사한 뒤 전달하므로 지연은 크지만 오류 프레임을 걸러낼 수 있다', '컷스루 방식은 목적지 주소만 확인되면 나머지 프레임을 다 받기 전에 바로 전달을 시작해 지연이 작다', '컷스루 방식은 프레임 전체의 오류 검사를 마친 뒤 전달하므로 저장 후 전달 방식보다 오류 검출 능력이 뛰어나다', '저장 후 전달 방식은 컷스루 방식보다 프레임 전달 지연이 크지만 손상된 프레임이 네트워크에 퍼지는 것을 줄일 수 있다']}
  correctAnswer={3}
  explanation="컷스루 방식은 목적지 주소만 확인한 뒤 나머지 프레임을 받기 전에 바로 전달을 시작하기 때문에, 프레임 전체에 대한 오류 검사를 하지 않는다. 오류 검사를 모두 마친 뒤 전달하는 것은 오히려 저장 후 전달 방식이므로, 컷스루가 오류 검출 능력이 더 뛰어나다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 저장 후 전달 방식은 전체 프레임을 확인한 뒤 전달해 오류 프레임을 걸러낼 수 있다.', '옳은 설명이다. 컷스루 방식은 목적지 주소만 보고 바로 전달을 시작해 지연이 작다.', '옳지 않은 설명이라 정답이다. 오류 검사를 완전히 수행하는 쪽은 저장 후 전달 방식이며, 컷스루 방식은 오류 검사를 생략하거나 최소화한다. 두 방식의 오류 검출 능력을 반대로 서술한 오답이다.', '옳은 설명이다. 지연과 오류 확산 방지 사이의 트레이드오프를 정확히 설명하고 있다.']}
/>

<Quiz
  questionNumber={17}
  question="맨체스터(Manchester) 부호화에 대한 설명으로 가장 옳은 것은?"
  options={['각 비트 구간의 중간에서 신호 전이를 발생시켜 별도의 클록 신호 없이도 자체 동기화를 제공한다', 'NRZ 방식보다 더 적은 대역폭으로 같은 데이터를 전송할 수 있어 효율이 더 높다', '아날로그 신호를 디지털 신호로 바꾸는 대표적인 변조 방식이다', '신호 전이가 전혀 없어 잡음의 영향을 거의 받지 않는다']}
  correctAnswer={1}
  explanation="맨체스터 부호화는 각 비트 구간의 중앙에서 반드시 신호 전이(하이에서 로우로, 또는 로우에서 하이로)가 일어나도록 정의되어 있어, 수신측이 이 전이 시점을 이용해 별도의 클록 신호 없이도 송신측과 동기를 맞출 수 있다."
  optionExplanations={['정답이다. 비트 중앙의 신호 전이를 이용한 자체 동기화가 맨체스터 부호화의 핵심 특징이다.', '맨체스터 부호화는 비트마다 전이가 필요해 NRZ보다 오히려 더 넓은 대역폭을 필요로 한다. 효율을 반대로 서술한 오답이다.', '맨체스터 부호화는 디지털 데이터를 디지털 신호로 표현하는 디지털-디지털 부호화 방식이지, 아날로그-디지털 변조 방식이 아니다.', '맨체스터 부호화는 오히려 매 비트마다 신호 전이가 발생하는 것이 특징이다. 전이가 없다는 것은 정반대의 설명이다.']}
/>

<Quiz
  questionNumber={18}
  question="동축 케이블, 꼬임쌍선(UTP) 케이블, 광섬유 등 전송 매체를 비교한 설명 중 옳지 않은 것은?"
  options={['광섬유는 전자기 간섭에 강하고 장거리·고속 전송에 유리하다', '동축 케이블은 꼬임쌍선보다 일반적으로 대역폭이 넓고 외부 간섭에 강하다', '꼬임쌍선 케이블은 두 선을 꼬아 전자기 유도로 인한 간섭(누화)을 줄인다', '광섬유는 전기 신호를 그대로 전달하는 매체로 전자기 간섭의 영향을 크게 받는다']}
  correctAnswer={4}
  explanation="광섬유는 전기 신호가 아니라 빛(광) 신호로 데이터를 전달하기 때문에 전자기 간섭의 영향을 거의 받지 않는다는 것이 큰 장점이다. 전기 신호를 그대로 전달해 간섭에 취약하다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 광섬유는 전자기 간섭에 강하고 장거리·고속 전송에 유리한 매체다.', '옳은 설명이다. 동축 케이블은 꼬임쌍선보다 넓은 대역폭과 나은 차폐 성능을 가진다.', '옳은 설명이다. 꼬임쌍선은 두 선을 꼬아 유도 간섭을 상쇄하는 원리로 누화를 줄인다.', '옳지 않은 설명이라 정답이다. 광섬유는 전기 신호가 아니라 빛 신호를 사용하므로 전자기 간섭에 강하다. 매체의 신호 종류를 잘못 서술한 오답이다.']}
/>

<Quiz
  questionNumber={19}
  question="CSMA/CD와 CSMA/CA에 대한 설명 중 옳지 않은 것은?"
  options={['CSMA/CD는 충돌을 감지하면 즉시 전송을 중단하고 잼(jam) 신호를 보내는 유선 이더넷의 매체 접근 방식이다', 'CSMA/CA는 무선 랜에서 충돌을 물리적으로 감지하기 어려운 특성 때문에 충돌을 사전에 회피하는 데 초점을 둔다', 'CSMA/CA는 전송 전에 매체가 비어 있음을 확인한 뒤에도 임의의 대기 시간(backoff)을 두어 충돌 가능성을 줄인다', 'CSMA/CD와 CSMA/CA는 이름만 다를 뿐 완전히 동일한 방식으로 동작한다']}
  correctAnswer={4}
  explanation="CSMA/CD는 충돌 발생을 감지(Collision Detection)해 대응하는 유선 방식이고, CSMA/CA는 무선 환경에서 충돌 감지가 어려워 충돌을 사전에 회피(Collision Avoidance)하는 데 초점을 둔 방식이다. 목적과 동작 방식이 다르므로 완전히 동일하다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. CSMA/CD의 충돌 감지와 잼 신호 전송 절차를 정확히 설명하고 있다.', '옳은 설명이다. 무선 환경의 특성 때문에 CSMA/CA는 회피 중심으로 설계되었다.', '옳은 설명이다. 임의 백오프 시간을 두어 여러 장치가 동시에 전송을 시도할 가능성을 줄이는 것이 CSMA/CA의 특징이다.', '옳지 않은 설명이라 정답이다. CD(감지)와 CA(회피)는 이름뿐 아니라 동작 원리 자체가 다르다. 두 방식을 동일시한 오답이다.']}
/>

<Quiz
  questionNumber={20}
  question="LAN과 WAN을 비교한 설명 중 옳지 않은 것은?"
  options={['LAN은 일반적으로 한 건물이나 캠퍼스처럼 지리적으로 좁은 범위를 연결한다', 'WAN은 도시나 국가 단위처럼 넓은 지역을 연결하며 통신 사업자의 회선을 임차하는 경우가 많다', 'LAN은 일반적으로 WAN보다 전송 속도가 빠르고 오류율이 낮다', 'WAN은 LAN보다 항상 전송 지연(delay)이 작고 구축 비용이 저렴하다']}
  correctAnswer={4}
  explanation="WAN은 지리적으로 넓은 범위를 연결하기 때문에 전파 거리가 길어 지연이 더 크고, 통신 사업자의 회선을 임차하는 등 구축·운영 비용도 LAN보다 높은 것이 일반적이다. 지연이 작고 비용이 저렴하다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. LAN은 좁은 지리적 범위를 연결하는 것이 일반적이다.', '옳은 설명이다. WAN은 넓은 지역을 연결하며 회선 임차가 흔하다.', '옳은 설명이다. LAN은 짧은 거리와 자체 구축 회선 덕분에 속도가 빠르고 오류율이 낮은 편이다.', '옳지 않은 설명이라 정답이다. 실제로는 WAN이 LAN보다 지연이 크고 비용도 더 드는 경우가 많다. 두 네트워크의 특성을 반대로 서술한 오답이다.']}
/>

<Quiz
  questionNumber={21}
  question="물리 계층에서 신호의 감쇠(attenuation)와 잡음(noise)에 대한 설명 중 옳지 않은 것은?"
  options={['감쇠는 신호가 매체를 통과하며 거리에 따라 세기가 점점 약해지는 현상이다', '잡음은 원치 않는 신호가 전송 신호에 섞여 수신측에서 신호를 왜곡시키는 현상이다', '감쇠는 증폭기(리피터)를 이용해 신호 세기를 보완할 수 있지만, 잡음은 증폭 시 함께 커질 수 있다', '감쇠와 잡음은 동일한 현상을 부르는 서로 다른 이름일 뿐이며 원인과 대응 방법이 완전히 같다']}
  correctAnswer={4}
  explanation="감쇠는 신호 세기가 거리에 따라 약해지는 현상이고, 잡음은 원치 않는 외부 신호가 섞여 들어오는 현상으로, 발생 원인과 대응 방법이 서로 다르다. 감쇠는 증폭으로 어느 정도 보완할 수 있지만, 증폭기는 잡음까지 함께 키울 수 있어 잡음 문제는 감쇠 보완만으로 해결되지 않는다."
  optionExplanations={['옳은 설명이다. 감쇠는 거리에 따른 신호 세기 감소 현상이다.', '옳은 설명이다. 잡음은 외부에서 섞여 들어와 신호를 왜곡시키는 현상이다.', '옳은 설명이다. 증폭기는 신호와 잡음을 함께 증폭시킬 수 있다는 한계가 있다.', '옳지 않은 설명이라 정답이다. 감쇠와 잡음은 원인과 대응 방법이 서로 다른 별개의 현상이다. 두 개념을 동일시한 오답이다.']}
/>

<Quiz
  questionNumber={22}
  question="이더넷 II(Ethernet II) 프레임의 필드 순서로 옳은 것은? (프리앰블은 제외)"
  options={['목적지 MAC 주소 → 출발지 MAC 주소 → 타입 → 데이터 → FCS', '출발지 MAC 주소 → 목적지 MAC 주소 → 타입 → 데이터 → FCS', '타입 → 목적지 MAC 주소 → 출발지 MAC 주소 → 데이터 → FCS', '목적지 MAC 주소 → 출발지 MAC 주소 → 데이터 → 타입 → FCS']}
  correctAnswer={1}
  explanation="이더넷 II 프레임은 프리앰블 다음에 목적지 MAC 주소, 출발지 MAC 주소, 상위 계층 프로토콜을 나타내는 타입 필드, 실제 데이터(페이로드), 마지막으로 오류 검출용 FCS(Frame Check Sequence) 순서로 구성된다."
  optionExplanations={['정답이다. 목적지 주소, 출발지 주소, 타입, 데이터, FCS 순서가 정확하다.', '목적지 주소와 출발지 주소의 순서가 뒤바뀌어 있는 오답이다.', '타입 필드가 주소 필드보다 앞에 오는 것으로 되어 있어 순서가 크게 어긋난 오답이다.', '데이터 필드와 타입 필드의 순서가 뒤바뀌어 있는 오답이다.']}
/>

## 참고 자료

- [IEEE 802.3 표준 개요 자료](https://standards.ieee.org/)
- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
