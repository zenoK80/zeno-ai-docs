---
title: '26. 독학사 3단계 딥러닝 기출 1회분 해설 (최근 B년도)'
description: '전 범위를 재구성한 22문항 객관식과 2문항 서술형 모의고사, Adam 갱신·LSTM 게이트·혼동행렬·회귀 지표 계산 전 과정 해설 포함'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 회차 역시 국가평생교육진흥원 독학학위제 3단계 딥러닝 **최신 출제기준(평가영역) 전체**를 근거로 새로 구성한 **모의 기출 1회분**입니다. 25편과 같은 문항을 반복하지 않도록 다른 숫자·다른 관점으로 재구성했으며, 실제 기출을 그대로 옮긴 것이 아닙니다. 실제 응시 시 문항 수·배점·시간은 그해 공고를 반드시 확인하시기 바랍니다.
</Callout>

## 이 회차의 영역 구성

| 문항 번호 | 출제기준 영역 | 관련 편 | 유형 |
| --- | --- | --- | --- |
| 1–2 | 딥러닝의 위치와 학습 유형 | 02·03편 | 개념 |
| 3–4 | 활성화·손실 함수 선택 | 06편 | 개념 |
| 5 | 순전파 계산 | 07편 | 계산 |
| 6 | 역전파 연쇄 법칙 | 07편 | 개념 |
| 7 | SGD와 미니배치 | 08편 | 개념 |
| 8 | Adam 갱신 계산 | 08편 | 계산 |
| 9 | 모멘텀 하이퍼파라미터 | 08편 | 개념 |
| 10–11 | 정규화(Dropout·Early Stopping) | 09편 | 개념 |
| 12 | 가중치 초기화 | 10편 | 개념 |
| 13 | 데이터 증강 | 10편 | 개념 |
| 14–15 | CNN 풀링·파라미터 계산 | 11편 | 개념·계산 |
| 16 | VGG·AlexNet 비교 | 12편 | 비교 |
| 17 | GRU와 LSTM 비교 | 13편 | 비교 |
| 18 | BPTT | 13편 | 개념 |
| 19 | 임베딩 파라미터 계산 | 14편 | 계산 |
| 20 | 위치 인코딩 | 15편 | 개념 |
| 21 | 혼동행렬 재현율 계산 | 19편 | 계산 |
| 22 | 회귀 지표(RMSE) 계산 | 19편 | 계산 |
| 서술 1 | Adam 갱신 1스텝 전 과정 | 08편 | 서술형 |
| 서술 2 | LSTM 셀 상태·은닉 상태 계산 | 13편 | 서술형 |

## 객관식 (1–22)

### 문제 1

<Quiz
  questionNumber={1}
  question="지도학습(supervised learning), 비지도학습(unsupervised learning), 강화학습(reinforcement learning)을 구분하는 기준으로 가장 적절한 것은?"
  options={['학습에 사용하는 데이터에 정답 라벨이 주어지는지, 정답 없이 구조를 찾는지, 보상 신호를 통해 행동을 학습하는지에 따라 구분한다.', '신경망의 층 수가 몇 개인지에 따라 구분한다.', '사용하는 프로그래밍 언어가 무엇인지에 따라 구분한다.', '모델을 학습시키는 컴퓨터의 연산 속도에 따라 구분한다.']}
  correctAnswer={1}
  explanation="지도학습은 입력-정답 쌍으로 학습하고, 비지도학습은 정답 없이 데이터의 구조나 패턴을 찾으며, 강화학습은 환경과 상호작용해 얻는 보상 신호를 최대화하는 방향으로 행동을 학습합니다. 이 구분은 학습에 주어지는 신호의 종류에 근거합니다."
  optionExplanations={['정답 라벨·구조 탐색·보상 신호라는 세 학습 유형의 실제 구분 기준을 정확히 설명했으므로 정답입니다.', '층 수는 모델 구조의 깊이를 나타낼 뿐 학습 유형 구분과는 무관해 틀렸습니다.', '프로그래밍 언어는 구현 도구일 뿐 학습 유형을 구분하는 개념적 기준이 아니라 틀렸습니다.', '연산 속도는 하드웨어 성능에 관한 것이지 학습 유형 구분 기준이 아니라 틀렸습니다.']}
/>

### 문제 2

<Quiz
  questionNumber={2}
  question="딥러닝이 '표현학습(representation learning)'을 수행한다고 말할 때, 그 의미로 가장 적절한 것은?"
  options={['사람이 특징을 직접 설계하지 않아도, 신경망이 데이터로부터 문제 해결에 유용한 특징을 층을 거치며 스스로 학습한다는 뜻이다.', '신경망이 학습 데이터를 그대로 저장해 두었다가 나중에 검색해서 출력한다는 뜻이다.', '표현학습은 반드시 지도학습에서만 일어난다는 뜻이다.', '표현학습은 딥러닝이 아닌 전통적 통계 기법에서만 쓰이는 용어라는 뜻이다.']}
  correctAnswer={1}
  explanation="표현학습은 원시 데이터로부터 문제 해결에 유용한 특징(표현)을 사람이 수작업으로 설계하지 않고 모델이 학습 과정에서 스스로 찾아내는 것을 말합니다. CNN의 앞쪽 층이 모서리 같은 저수준 특징을, 뒤쪽 층이 더 추상적인 특징을 학습하는 것이 대표적인 예입니다."
  optionExplanations={['특징을 스스로 학습한다는 표현학습의 실제 정의를 정확히 설명했으므로 정답입니다.', '표현학습은 데이터를 그대로 저장하는 것이 아니라 일반화 가능한 특징을 학습하는 것이라 틀렸습니다.', '표현학습은 비지도학습(오토인코더 등)에서도 활발히 쓰이는 개념이라 틀렸습니다.', '표현학습은 딥러닝을 설명할 때 특히 자주 쓰이는 용어이므로 틀렸습니다.']}
/>

### 문제 3

<Quiz
  questionNumber={3}
  question="tanh(하이퍼볼릭 탄젠트) 활성화 함수와 시그모이드 함수를 비교한 설명으로 가장 적절한 것은?"
  options={['tanh는 출력 범위가 -1에서 1 사이로 0을 중심으로 대칭이라, 시그모이드(0~1)보다 그라디언트 흐름 면에서 유리한 경우가 있다.', 'tanh의 출력 범위는 0에서 1 사이로 시그모이드와 완전히 동일하다.', 'tanh는 입력이 커지거나 작아져도 절대 포화(saturation)되지 않는다.', 'tanh는 미분이 불가능한 함수이다.']}
  correctAnswer={1}
  explanation="tanh는 출력이 -1에서 1 사이이며 0을 중심으로 대칭적인 형태를 가집니다. 이 때문에 출력이 항상 양수인 시그모이드보다 다음 층으로 전달되는 그라디언트의 부호가 다양해져, 경우에 따라 학습에 유리하게 작용할 수 있습니다."
  optionExplanations={['출력 범위와 0 중심 대칭이라는 tanh의 실제 특징을 정확히 설명했으므로 정답입니다.', 'tanh의 출력 범위는 -1에서 1이며 시그모이드의 0에서 1과 다르므로 틀렸습니다.', 'tanh도 입력이 매우 크거나 작으면 기울기가 0에 가까워지는 포화 현상이 나타나므로 틀렸습니다.', 'tanh는 모든 구간에서 미분 가능한 매끄러운 함수이므로 틀렸습니다.']}
/>

### 문제 4

<Quiz
  questionNumber={4}
  question="회귀(regression) 문제에서 손실 함수로 평균절대오차(MAE) 대신 평균제곱오차(MSE)를 선택했을 때 나타나는 특징으로 가장 적절한 것은?"
  options={['오차를 제곱하기 때문에 큰 오차에 더 큰 페널티를 주어, 이상치(outlier)에 더 민감하게 반응하는 경향이 있다.', 'MSE는 오차의 부호를 무시하지 않고 항상 그대로 반영한다.', 'MSE를 쓰면 이상치의 영향을 완전히 제거할 수 있다.', 'MSE는 분류 문제에서만 사용할 수 있는 손실 함수이다.']}
  correctAnswer={1}
  explanation="MSE는 오차를 제곱하므로 오차가 큰 데이터(이상치)의 영향이 절대오차보다 훨씬 크게 반영됩니다. 그 결과 MSE로 학습하면 이상치에 더 민감하게 반응해, 이상치가 많은 데이터에서는 MAE보다 불안정할 수 있습니다."
  optionExplanations={['제곱으로 인한 이상치 민감도 증가라는 MSE의 실제 특징을 정확히 설명했으므로 정답입니다.', 'MSE는 오차를 제곱해 부호를 없애 버리므로 부호를 그대로 반영한다는 설명은 틀렸습니다.', 'MSE는 오히려 이상치의 영향을 더 크게 반영하는 손실이라 이 설명은 정반대라 틀렸습니다.', 'MSE는 회귀 문제에서 흔히 쓰이는 손실 함수라 분류 전용이라는 설명은 틀렸습니다.']}
/>

### 문제 5

두 개의 입력 $x_1 = 2$, $x_2 = -1$이고, 가중치 $w_1 = 0.3$, $w_2 = 0.4$, 편향 $b = -0.1$인 뉴런이 있다. 활성화 함수는 ReLU이다.

<Quiz
  questionNumber={5}
  question="이 뉴런의 최종 출력값은?"
  options={['0.1', '0', '0.3', '-0.1']}
  correctAnswer={1}
  explanation="먼저 가중합을 계산하면 z = 0.3*2 + 0.4*(-1) + (-0.1) = 0.6 - 0.4 - 0.1 = 0.1입니다. ReLU는 max(0, z)이므로 max(0, 0.1) = 0.1이 최종 출력입니다."
  optionExplanations={['가중합 계산과 ReLU 적용을 정확히 마친 값이므로 정답입니다.', 'ReLU가 항상 0을 출력한다고 착각한 오답입니다.', '편향을 더하지 않고 0.6-0.4-0.1을 잘못 계산해 0.3이 나온 것으로 보이는 오답입니다.', '부호를 잘못 계산해 z를 -0.1로 잘못 구한 오답입니다.']}
/>

### 문제 6

<Quiz
  questionNumber={6}
  question="다층 신경망에서 연쇄 법칙(chain rule)이 역전파에 반드시 필요한 이유로 가장 적절한 것은?"
  options={['출력층의 손실이 여러 층의 합성 함수를 거쳐 만들어지므로, 각 은닉층 가중치가 손실에 미치는 영향을 구하려면 층마다의 국소 미분을 곱해 나가야 하기 때문이다.', '연쇄 법칙을 쓰면 순전파 계산을 생략할 수 있기 때문이다.', '연쇄 법칙은 손실 함수를 아예 정의하지 않아도 되게 해주기 때문이다.', '연쇄 법칙은 활성화 함수를 사용하지 않을 때만 필요하기 때문이다.']}
  correctAnswer={1}
  explanation="신경망의 출력은 입력이 여러 층의 함수를 순서대로 통과한 합성 함수입니다. 특정 은닉층 가중치가 최종 손실에 미치는 영향을 구하려면, 그 가중치부터 출력까지 거치는 각 층의 국소 미분을 연쇄적으로 곱해야 하며, 이것이 연쇄 법칙이 필요한 이유입니다."
  optionExplanations={['합성 함수 구조에서 국소 미분을 곱해야 하는 이유를 정확히 설명했으므로 정답입니다.', '역전파는 순전파에서 계산해 둔 값(활성화 값 등)을 그대로 활용하므로 순전파를 생략할 수 없어 틀렸습니다.', '역전파는 정의된 손실 함수의 그라디언트를 계산하는 절차이므로 손실 함수가 필요 없다는 설명은 틀렸습니다.', '연쇄 법칙은 비선형 활성화 함수가 있는 일반적인 신경망에서도 그대로 필요하므로 틀렸습니다.']}
/>

### 문제 7

<Quiz
  questionNumber={7}
  question="전체 배치 경사하강법(batch gradient descent)과 비교한 확률적 경사하강법(SGD, 미니배치 포함)의 특징으로 가장 적절한 것은?"
  options={['한 번의 갱신에 전체 데이터가 아닌 일부(또는 한 개) 데이터만 사용해, 갱신 속도는 빠르지만 손실 값의 변화가 더 불규칙(noisy)한 경향이 있다.', 'SGD는 전체 데이터셋을 한 번에 모두 사용해야만 한 번의 가중치 갱신을 할 수 있다.', 'SGD는 데이터의 순서를 절대 섞으면 안 되는 방법이다.', 'SGD는 항상 배치 경사하강법보다 정확히 같은 손실 곡선을 그린다.']}
  correctAnswer={1}
  explanation="SGD(미니배치 포함)는 매 갱신마다 전체 데이터가 아닌 일부 표본만으로 그라디언트를 추정하기 때문에, 배치 전체를 쓰는 것보다 갱신은 자주 할 수 있지만 각 그라디언트 추정에 잡음이 섞여 손실 곡선이 더 들쭉날쭉하게 나타나는 경향이 있습니다."
  optionExplanations={['일부 데이터 사용에 따른 빠른 갱신과 잡음 증가라는 SGD의 실제 특징을 정확히 설명했으므로 정답입니다.', '전체 데이터를 모두 써야 하는 것은 배치 경사하강법의 정의이지 SGD의 정의가 아니라 틀렸습니다.', 'SGD는 오히려 매 에폭마다 데이터 순서를 섞는 것이 일반적인 권장 사항이라 틀렸습니다.', 'SGD는 표본 추출에 따른 잡음 때문에 배치 경사하강법과 손실 곡선의 세부 모양이 달라지므로 틀렸습니다.']}
/>

### 문제 8

어떤 파라미터에 대한 그라디언트가 $g = 0.2$이고, Adam 옵티마이저의 하이퍼파라미터는 $\beta_1 = 0.9$, $\beta_2 = 0.999$, $\eta = 0.01$이다. 이전 시점의 1차·2차 모멘트는 모두 0이며, 지금이 첫 번째 갱신(t=1)이다.

<Quiz
  questionNumber={8}
  question="바이어스 보정(bias correction)까지 마친 뒤 이 파라미터의 갱신량(learning rate가 적용된 이동량)은 약 얼마인가?"
  options={['0.01', '0.002', '0.0002', '0.1']}
  correctAnswer={1}
  explanation="1차 모멘트는 m1 = 0.9*0 + 0.1*0.2 = 0.02이고, 2차 모멘트는 v1 = 0.999*0 + 0.001*0.2^2 = 0.001*0.04 = 0.00004입니다. 바이어스 보정 후 m_hat = 0.02/(1-0.9) = 0.2, v_hat = 0.00004/(1-0.999) = 0.04입니다. 갱신량은 eta*m_hat/(sqrt(v_hat)+epsilon) ≈ 0.01*0.2/0.2 = 0.01입니다."
  optionExplanations={['바이어스 보정과 갱신 공식을 순서대로 정확히 계산한 값이므로 정답입니다.', '바이어스 보정을 하지 않고 m1, v1을 그대로 사용해 0.01*0.02/sqrt(0.00004)를 잘못 계산한 값에 가까운 오답입니다.', '제곱근을 취하지 않고 v_hat(0.04)을 그대로 나눈 값과 비슷하게 계산을 잘못한 오답입니다.', '학습률을 곱하지 않고 m_hat/sqrt(v_hat) 비율(1)에 다른 값을 곱해 근거 없이 계산한 오답입니다.']}
/>

### 문제 9

<Quiz
  questionNumber={9}
  question="모멘텀 계수(momentum coefficient)를 지나치게 크게(예: 0.999에 가깝게) 설정했을 때 나타날 수 있는 문제로 가장 적절한 것은?"
  options={['과거 그라디언트의 영향이 지나치게 오래 남아, 최적점 근처에서도 관성 때문에 진동하거나 최적점을 지나쳐 버리는 현상이 나타날 수 있다.', '모멘텀 계수를 크게 하면 그라디언트를 전혀 사용하지 않게 된다.', '모멘텀 계수를 크게 하면 학습이 항상 더 빠르고 안정적으로 수렴한다.', '모멘텀 계수는 클수록 무조건 좋으므로 상한이 없다.']}
  correctAnswer={1}
  explanation="모멘텀 계수가 너무 크면 과거의 이동 방향이 현재 갱신에 지나치게 강하게 반영되어, 최적점 근처에 도달해도 관성 때문에 멈추지 못하고 지나치거나 진동하는 현상(overshooting)이 나타날 수 있습니다."
  optionExplanations={['관성으로 인한 오버슈팅·진동이라는 실제 부작용을 정확히 설명했으므로 정답입니다.', '모멘텀 계수를 키워도 현재 그라디언트는 여전히 매 스텝 반영되므로 틀렸습니다.', '모멘텀 계수가 지나치게 크면 오히려 불안정해질 수 있으므로, 항상 더 안정적이라는 설명은 틀렸습니다.', '모멘텀 계수도 적절한 범위(대개 0.9 근처)에서 선택해야 하는 하이퍼파라미터이므로 상한이 없다는 설명은 틀렸습니다.']}
/>

### 문제 10

<Quiz
  questionNumber={10}
  question="Early Stopping(조기 종료) 기법에 대한 설명으로 가장 적절한 것은?"
  options={['검증 데이터셋(validation set)에 대한 손실이 더 이상 개선되지 않고 악화되기 시작하는 시점에서 학습을 멈춰 과적합을 방지한다.', 'Early Stopping은 학습 데이터셋의 크기를 자동으로 줄여주는 기법이다.', 'Early Stopping은 반드시 학습을 1 에폭만 진행하고 멈추는 기법이다.', 'Early Stopping은 학습률을 매 에폭마다 두 배로 늘리는 기법이다.']}
  correctAnswer={1}
  explanation="Early Stopping은 학습 중 검증 손실을 지속적으로 관찰하다가, 검증 손실이 더 이상 줄어들지 않고 오히려 늘어나기 시작하는 시점(과적합이 시작되는 시점 근처)에서 학습을 멈추는 정규화 기법입니다."
  optionExplanations={['검증 손실 악화 시점에서 학습을 멈춘다는 Early Stopping의 실제 정의를 정확히 설명했으므로 정답입니다.', 'Early Stopping은 데이터셋 크기가 아니라 학습을 멈추는 시점을 다루는 기법이라 틀렸습니다.', 'Early Stopping이 멈추는 시점은 검증 손실의 추이에 따라 달라지며 항상 1 에폭이 아니라 틀렸습니다.', 'Early Stopping은 학습률 조정과 무관한, 학습 종료 시점을 결정하는 기법이라 틀렸습니다.']}
/>

### 문제 11

<Quiz
  questionNumber={11}
  question="과적합(overfitting)이 의심되는 상황에 대한 설명으로 가장 적절한 것은?"
  options={['학습 데이터에 대한 손실은 계속 낮아지는데, 검증 데이터에 대한 손실은 어느 시점부터 오히려 높아지기 시작하는 상황이다.', '학습 데이터와 검증 데이터 모두에 대한 손실이 함께 계속 낮아지는 상황이다.', '학습 데이터에 대한 손실이 처음부터 전혀 줄어들지 않는 상황이다.', '검증 데이터가 아예 존재하지 않는 상황이다.']}
  correctAnswer={1}
  explanation="과적합은 모델이 학습 데이터의 세부 패턴(잡음 포함)까지 지나치게 맞추어, 학습 손실은 계속 낮아지지만 새로운 데이터(검증 데이터)에 대한 일반화 성능은 오히려 떨어지는 상황을 가리킵니다."
  optionExplanations={['학습 손실 감소와 검증 손실 악화라는 과적합의 대표적 징후를 정확히 설명했으므로 정답입니다.', '학습·검증 손실이 함께 낮아지는 것은 오히려 학습이 잘 되고 있다는 신호이므로 틀렸습니다.', '학습 손실이 전혀 줄지 않는 것은 과소적합(underfitting)에 가까운 상황이라 틀렸습니다.', '검증 데이터가 없으면 과적합 여부를 판단하기 어려울 뿐, 그 자체가 과적합의 정의는 아니라 틀렸습니다.']}
/>

### 문제 12

<Quiz
  questionNumber={12}
  question="ReLU 계열 활성화 함수를 사용하는 신경망에서 He 초기화(He initialization)가 자비어 초기화(Xavier initialization)보다 흔히 권장되는 이유로 가장 적절한 것은?"
  options={['ReLU는 음수 입력을 0으로 만들어 활성화되는 뉴런 비율이 줄어드는 특성을 고려해, 분산 계산에 이를 반영한 초기화 방식이기 때문이다.', 'He 초기화는 가중치를 항상 0으로 초기화하는 방식이기 때문이다.', 'He 초기화는 초기화가 전혀 필요 없게 만들어 주는 방식이기 때문이다.', 'He 초기화는 시그모이드 함수 전용으로 설계되어 ReLU에 더 적합하기 때문이다.']}
  correctAnswer={1}
  explanation="He 초기화는 ReLU가 음수 입력을 0으로 만들어 대략 절반의 뉴런만 활성화되는 특성을 고려해, 가중치 분산을 자비어 초기화보다 조금 더 크게 설정합니다. 이 덕분에 ReLU를 쓰는 깊은 네트워크에서 층을 거칠수록 활성화 값의 분산이 너무 작아지는 문제를 완화합니다."
  optionExplanations={['ReLU의 활성화 비율 특성을 고려한 분산 설계라는 He 초기화의 실제 이유를 정확히 설명했으므로 정답입니다.', '가중치를 모두 0으로 초기화하면 모든 뉴런이 동일하게 학습되는 대칭성 문제가 생겨 오히려 피해야 할 방식이라 틀렸습니다.', '어떤 초기화 방식을 쓰든 초기화 자체는 필요한 절차이므로 틀렸습니다.', 'He 초기화는 오히려 ReLU 계열에 맞춰 설계된 방식이며 시그모이드 전용이 아니라 틀렸습니다.']}
/>

### 문제 13

<Quiz
  questionNumber={13}
  question="이미지 분류 문제에서 데이터 증강(data augmentation)으로 회전·좌우 반전·색상 변형 등을 적용하는 목적으로 가장 적절한 것은?"
  options={['같은 대상이 다양한 형태로 나타나도 모델이 견고하게 인식하도록, 제한된 학습 데이터로도 다양한 변형을 인위적으로 늘려 과적합을 줄이는 것이다.', '데이터 증강은 테스트 데이터의 정답 라벨을 자동으로 만들어 주기 위한 것이다.', '데이터 증강은 항상 원본 데이터를 완전히 대체해 원본을 사용할 수 없게 만드는 절차이다.', '데이터 증강은 모델의 파라미터 개수를 줄이기 위한 기법이다.']}
  correctAnswer={1}
  explanation="데이터 증강은 회전, 반전, 크롭, 색상 변형 등으로 원본 데이터의 인위적인 변형본을 만들어 학습 데이터의 다양성을 늘리는 기법입니다. 이를 통해 모델이 특정 각도나 색상 등 학습 데이터의 우연한 특징에 과적합되는 것을 줄이고 일반화 성능을 높입니다."
  optionExplanations={['다양성 증가를 통한 과적합 완화라는 데이터 증강의 실제 목적을 정확히 설명했으므로 정답입니다.', '데이터 증강은 입력 이미지를 변형할 뿐 정답 라벨을 자동 생성하는 절차가 아니라 틀렸습니다.', '데이터 증강은 원본에 변형을 추가해 함께 사용하는 것이지 원본을 대체해 버리는 것이 아니라 틀렸습니다.', '데이터 증강은 데이터 자체를 다루는 기법이며 모델 파라미터 개수와는 무관해 틀렸습니다.']}
/>

### 문제 14

<Quiz
  questionNumber={14}
  question="최대 풀링(max pooling)과 평균 풀링(average pooling)을 비교한 설명으로 가장 적절한 것은?"
  options={['최대 풀링은 영역 내 가장 두드러진 특징(활성화가 큰 값)을 강조해 전달하는 반면, 평균 풀링은 영역 전체의 정보를 고르게 반영해 전달한다.', '최대 풀링과 평균 풀링은 계산 결과가 항상 완전히 동일하다.', '평균 풀링은 학습 가능한 파라미터를 새로 추가하는 층이다.', '최대 풀링은 반드시 입력 크기를 두 배로 늘리는 연산이다.']}
  correctAnswer={1}
  explanation="최대 풀링은 풀링 영역에서 가장 큰 값을 선택해 두드러진 특징을 강조하고, 평균 풀링은 영역 내 값들의 평균을 내어 정보를 고르게 반영합니다. 두 방식 모두 공간 크기를 줄이는 다운샘플링 역할을 하지만 반영하는 방식이 다릅니다."
  optionExplanations={['최댓값 강조와 평균 반영이라는 두 풀링 방식의 실제 차이를 정확히 설명했으므로 정답입니다.', '두 방식은 계산 방법이 달라 일반적으로 서로 다른 결과를 내므로 틀렸습니다.', '일반적인 풀링층은 학습되는 파라미터가 없는 고정된 연산이므로 틀렸습니다.', '풀링은 보통 공간 크기를 줄이는 다운샘플링 연산이지 두 배로 늘리는 연산이 아니라 틀렸습니다.']}
/>

### 문제 15

입력 채널이 3, 필터 크기가 3x3, 출력 채널(필터 개수)이 32, 각 필터마다 편향이 1개 있는 합성곱층이 있다.

<Quiz
  questionNumber={15}
  question="이 합성곱층의 전체 학습 파라미터 개수는?"
  options={['896', '864', '288', '32']}
  correctAnswer={1}
  explanation="필터 하나의 파라미터는 (3*3*3)+1(편향) = 27+1 = 28개입니다. 필터가 32개이므로 전체는 28*32 = 896개입니다."
  optionExplanations={['필터당 파라미터 수에 필터 개수를 곱한 정확한 계산이므로 정답입니다.', '편향을 빠뜨리고 (3*3*3)*32 = 864만 계산한 오답입니다.', '입력 채널을 곱하지 않고 (3*3)*32 = 288만 계산한 오답입니다.', '필터 개수(32)만 답으로 쓴 오답입니다.']}
/>

### 문제 16

<Quiz
  questionNumber={16}
  question="AlexNet과 비교한 VGG 계열 네트워크의 설계 특징으로 가장 적절한 것은?"
  options={['비교적 큰 필터 대신 3x3처럼 작은 필터를 여러 층 깊게 쌓아, 같은 수용 영역(receptive field)을 더 적은 파라미터로 표현하려 했다.', 'VGG는 합성곱층을 전혀 사용하지 않고 완전연결층만으로 구성된다.', 'VGG는 AlexNet보다 항상 층 수가 더 적은 얕은 구조이다.', 'VGG는 이미지가 아닌 텍스트 데이터 전용으로 설계된 구조이다.']}
  correctAnswer={1}
  explanation="VGG는 11x11이나 5x5처럼 비교적 큰 필터를 쓰던 AlexNet과 달리, 3x3의 작은 필터를 여러 층 깊게 쌓는 설계를 택했습니다. 작은 필터를 여러 번 겹쳐도 큰 필터와 비슷한 수용 영역을 얻을 수 있으면서, 파라미터 수와 비선형성 측면에서 이점이 있습니다."
  optionExplanations={['작은 필터를 깊게 쌓는 VGG의 실제 설계 특징을 정확히 설명했으므로 정답입니다.', 'VGG도 여러 합성곱층으로 특징을 추출한 뒤 완전연결층을 사용하는 CNN 구조이므로 틀렸습니다.', 'VGG는 오히려 AlexNet보다 층 수가 많은 더 깊은 구조로 설계되었으므로 틀렸습니다.', 'VGG는 이미지 분류를 위해 제안된 CNN 구조이므로 텍스트 전용이라는 설명은 틀렸습니다.']}
/>

### 문제 17

<Quiz
  questionNumber={17}
  question="GRU(Gated Recurrent Unit)와 LSTM을 비교한 설명으로 가장 적절한 것은?"
  options={['GRU는 LSTM의 입력·망각 게이트 역할을 업데이트 게이트 하나로 통합하고 별도의 셀 상태 없이 은닉 상태만 사용해 구조가 더 단순하다.', 'GRU는 게이트가 전혀 없는 가장 단순한 순환 구조이다.', 'GRU는 LSTM보다 항상 게이트 개수가 더 많다.', 'GRU와 LSTM은 완전히 같은 구조이며 이름만 다르다.']}
  correctAnswer={1}
  explanation="GRU는 LSTM의 입력 게이트와 망각 게이트 역할을 업데이트 게이트(update gate) 하나로 합치고, 리셋 게이트(reset gate)를 추가로 두며, 별도의 셀 상태 없이 은닉 상태 하나로 정보를 전달합니다. 이 덕분에 LSTM보다 파라미터가 적고 구조가 단순합니다."
  optionExplanations={['게이트 통합과 셀 상태 생략이라는 GRU의 실제 구조적 특징을 정확히 설명했으므로 정답입니다.', 'GRU도 업데이트 게이트와 리셋 게이트라는 게이트를 갖는 구조이므로 틀렸습니다.', 'GRU는 LSTM(3개 게이트)보다 게이트 개수가 적은(2개) 더 단순한 구조이므로 틀렸습니다.', 'GRU와 LSTM은 게이트 구성과 셀 상태 유무 등 구조적 차이가 뚜렷하므로 틀렸습니다.']}
/>

### 문제 18

<Quiz
  questionNumber={18}
  question="시간에 따른 역전파(BPTT, Backpropagation Through Time)에 대한 설명으로 가장 적절한 것은?"
  options={['순환신경망을 시간 축을 따라 펼친 뒤, 각 시점의 출력에서 발생한 손실의 그라디언트를 시간 역방향으로 전달해 공유 가중치를 갱신하는 방법이다.', 'BPTT는 순환 구조가 없는 완전연결 신경망에서만 사용하는 알고리즘이다.', 'BPTT는 그라디언트를 전혀 계산하지 않고 가중치를 무작위로 갱신하는 방법이다.', 'BPTT를 사용하면 그라디언트 소실·폭발 문제가 원천적으로 발생하지 않는다.']}
  correctAnswer={1}
  explanation="BPTT는 RNN을 시간 축으로 펼친 계산 그래프로 보고, 각 시점의 손실에서 나온 그라디언트를 시간 역방향으로 전달하며 모든 시점이 공유하는 가중치에 대한 그라디언트를 누적해 계산하는 역전파 방법입니다."
  optionExplanations={['시간 축 전개와 시간 역방향 그라디언트 전달이라는 BPTT의 실제 정의를 정확히 설명했으므로 정답입니다.', 'BPTT는 이름 그대로 순환 구조(RNN 계열)에서 시간에 따라 적용하는 역전파이므로 틀렸습니다.', 'BPTT도 일반적인 역전파처럼 연쇄 법칙으로 그라디언트를 계산하는 방법이므로 틀렸습니다.', 'BPTT는 오히려 시퀀스가 길어질수록 그라디언트 소실·폭발 문제가 두드러지는 경향이 있어 틀렸습니다.']}
/>

### 문제 19

어휘 집합 크기가 8000이고 임베딩 차원을 64로 설정한 임베딩 레이어가 있다.

<Quiz
  questionNumber={19}
  question="이 임베딩 레이어의 학습 파라미터 개수는?"
  options={['512000', '8064', '64', '8000']}
  correctAnswer={1}
  explanation="임베딩 레이어의 파라미터 수는 (어휘 크기 × 임베딩 차원)입니다. 대입하면 8000 * 64 = 512,000개입니다."
  optionExplanations={['어휘 크기와 임베딩 차원을 정확히 곱한 값이므로 정답입니다.', '곱셈 대신 덧셈(8000+64)을 한 오답입니다.', '임베딩 차원 수만 답으로 쓴 오답입니다.', '어휘 크기만 답으로 쓴 오답입니다.']}
/>

### 문제 20

<Quiz
  questionNumber={20}
  question="Transformer에서 위치 인코딩(positional encoding)을 단어 임베딩에 '더하는(add)' 방식에 대한 설명으로 가장 적절한 것은?"
  options={['위치 인코딩과 단어 임베딩이 같은 차원의 벡터이기 때문에 원소별로 더할 수 있으며, 이렇게 결합된 벡터가 이후 셀프 어텐션 계산에 함께 입력된다.', '위치 인코딩은 단어 임베딩과 차원이 달라 이어붙이기(concatenate)만 가능하고 더할 수는 없다.', '위치 인코딩을 더하면 단어의 의미 정보가 완전히 사라진다.', '위치 인코딩은 셀프 어텐션 계산이 모두 끝난 이후에만 적용할 수 있다.']}
  correctAnswer={1}
  explanation="위치 인코딩은 단어 임베딩과 동일한 차원으로 설계되어, 각 위치의 인코딩 벡터를 해당 위치의 단어 임베딩에 원소별로 더할 수 있습니다. 이렇게 위치 정보가 더해진 벡터가 이후 셀프 어텐션 등 Transformer 블록에 입력됩니다."
  optionExplanations={['같은 차원의 벡터를 더한다는 위치 인코딩 결합 방식을 정확히 설명했으므로 정답입니다.', '위치 인코딩은 임베딩과 같은 차원으로 설계되어 원소별 덧셈이 가능하므로 틀렸습니다.', '덧셈은 의미 정보에 위치 정보를 함께 담는 방식이지 의미 정보를 지우는 것이 아니라 틀렸습니다.', '위치 인코딩은 셀프 어텐션 계산에 들어가기 전, 입력 단계에서 더해지므로 틀렸습니다.']}
/>

### 문제 21

이진 분류 모델을 100개의 테스트 데이터에 적용해 다음 혼동행렬을 얻었다.

| 구분 | 예측: 양성 | 예측: 음성 |
| --- | --- | --- |
| 실제: 양성 | TP = 50 | FN = 15 |
| 실제: 음성 | FP = 5 | TN = 30 |

<Quiz
  questionNumber={21}
  question="이 모델의 재현율(recall)은 약 얼마인가?"
  options={['0.769', '0.909', '0.80', '0.667']}
  correctAnswer={1}
  explanation="재현율은 TP/(TP+FN)로 계산합니다. 대입하면 50/(50+15) = 50/65 ≈ 0.769입니다."
  optionExplanations={['실제 양성 중 모델이 맞힌 비율을 정확히 계산했으므로 정답입니다.', 'TP/(TP+FP) = 50/55 ≈ 0.909로 계산한 정밀도 값을 재현율로 착각한 오답입니다.', '정확도((50+30)/100=0.80)를 재현율로 착각한 오답입니다.', '분모를 다른 값으로 잘못 설정해 계산한 오답입니다.']}
/>

### 문제 22

어떤 회귀 모델이 4개의 테스트 데이터에 대해 실제값 y = [10, 12, 8, 14], 예측값 y_hat = [9, 13, 8, 16]로 예측했다.

<Quiz
  questionNumber={22}
  question="이 예측 결과의 평균제곱근오차(RMSE)는 약 얼마인가?"
  options={['1.22', '1.5', '6.0', '0.75']}
  correctAnswer={1}
  explanation="오차는 y - y_hat = 1, -1, 0, -2입니다. 제곱하면 1, 1, 0, 4이고 평균은 (1+1+0+4)/4 = 6/4 = 1.5로 이것이 MSE입니다. RMSE는 MSE의 제곱근이므로 sqrt(1.5) ≈ 1.22입니다."
  optionExplanations={['MSE를 구한 뒤 제곱근까지 취한 정확한 값이므로 정답입니다.', 'MSE(1.5) 값 자체를 RMSE로 착각해 제곱근을 취하지 않은 오답입니다.', '제곱 오차의 합(6)을 그대로 답으로 쓴 오답입니다.', '절댓값 오차의 평균(MAE, (1+1+0+2)/4=1.0과는 다른 값)을 임의로 계산한 오답입니다.']}
/>

## 서술형 (1–2)

### 서술형 문제 1: Adam 갱신 1스텝 전 과정

파라미터에 대한 그라디언트 $g = 0.4$, 하이퍼파라미터 $\beta_1 = 0.9$, $\beta_2 = 0.999$, 학습률 $\eta = 0.001$, 이전 시점 모멘트 $m_0 = 0$, $v_0 = 0$일 때, 시점 $t=1$에서 Adam이 파라미터를 갱신하는 전 과정을 서술하시오(엡실론은 무시할 수 있을 만큼 작다고 가정).

**모범답안**

1. **1차 모멘트(모멘텀 항) 갱신**: $m_1 = \beta_1 m_0 + (1-\beta_1) g = 0.9 \times 0 + 0.1 \times 0.4 = 0.04$입니다.
2. **2차 모멘트(그라디언트 제곱의 이동평균) 갱신**: $v_1 = \beta_2 v_0 + (1-\beta_2) g^2 = 0.999 \times 0 + 0.001 \times 0.16 = 0.00016$입니다.
3. **1차 모멘트 바이어스 보정**: $\hat{m}_1 = \dfrac{m_1}{1-\beta_1^{1}} = \dfrac{0.04}{1-0.9} = \dfrac{0.04}{0.1} = 0.4$입니다.
4. **2차 모멘트 바이어스 보정**: $\hat{v}_1 = \dfrac{v_1}{1-\beta_2^{1}} = \dfrac{0.00016}{1-0.999} = \dfrac{0.00016}{0.001} = 0.16$입니다.
5. **파라미터 갱신량 계산**: $\Delta\theta = \eta \dfrac{\hat{m}_1}{\sqrt{\hat{v}_1}+\epsilon} \approx 0.001 \times \dfrac{0.4}{\sqrt{0.16}} = 0.001 \times \dfrac{0.4}{0.4} = 0.001$입니다.
6. **파라미터 갱신**: $\theta_1 = \theta_0 - \Delta\theta = \theta_0 - 0.001$입니다.

**해석**: 바이어스 보정을 거치면 학습 초기(t가 작을 때)에 모멘트 추정치가 0에 가깝게 시작해 갱신량이 지나치게 작아지는 문제를 막을 수 있습니다. 이 예시에서는 $\hat{m}_1 / \sqrt{\hat{v}_1}$이 정확히 1이 되어, 실제 갱신량이 학습률 $\eta$와 같아지는 특수한 경우를 보여줍니다.

**자주 틀리는 점**: 바이어스 보정 단계를 생략하고 $m_1$, $v_1$을 그대로 갱신 공식에 대입하는 실수가 흔합니다. 그렇게 하면 $v_1$이 매우 작은 값(0.00016)이어서 제곱근을 취해도 분모가 작아지고, 학습 초기 갱신량이 실제보다 훨씬 크게(또는 왜곡되어) 계산되는 오류가 생깁니다.

### 서술형 문제 2: LSTM 셀 상태·은닉 상태 계산

어떤 LSTM 셀에서 이전 셀 상태 $C_{t-1} = 0.8$이고, 이번 시점에 계산된 망각 게이트 $f_t = 0.6$, 입력 게이트 $i_t = 0.7$, 후보 셀 상태 $\tilde{C}_t = 0.5$, 출력 게이트 $o_t = 0.65$가 주어졌다. 이번 시점의 셀 상태 $C_t$와 은닉 상태 $h_t$를 계산하는 과정을 서술하시오($\tanh(0.83) \approx 0.68$을 이용한다).

**모범답안**

1. **셀 상태 갱신 공식**: LSTM의 셀 상태는 $C_t = f_t \times C_{t-1} + i_t \times \tilde{C}_t$로 갱신됩니다. 이 식은 망각 게이트가 과거 정보를 얼마나 남길지, 입력 게이트가 새 후보 정보를 얼마나 반영할지를 각각 조절한다는 의미를 담고 있습니다.
2. **망각 항 계산**: $f_t \times C_{t-1} = 0.6 \times 0.8 = 0.48$입니다. 이는 과거 셀 상태 중 남기는 부분입니다.
3. **입력 항 계산**: $i_t \times \tilde{C}_t = 0.7 \times 0.5 = 0.35$입니다. 이는 새로 반영하는 후보 정보의 크기입니다.
4. **셀 상태 합산**: $C_t = 0.48 + 0.35 = 0.83$입니다.
5. **은닉 상태 계산**: 은닉 상태는 $h_t = o_t \times \tanh(C_t)$로 계산합니다. $\tanh(0.83) \approx 0.68$이 주어졌으므로 $h_t = 0.65 \times 0.68 \approx 0.442$입니다.

**해석**: 망각 게이트(0.6)와 입력 게이트(0.7)가 모두 1보다 작은 값으로 과거 정보와 새 정보를 적절히 섞어 셀 상태를 갱신했고, 출력 게이트(0.65)는 이 셀 상태 중 일부만 걸러 은닉 상태로 내보냈습니다. 세 게이트가 각각 독립적인 역할(유지·반영·노출)을 한다는 점을 이 계산이 잘 보여줍니다.

**자주 틀리는 점**: 은닉 상태를 계산할 때 $\tanh$를 적용하지 않고 $C_t$ 값 자체에 출력 게이트만 곱하는 실수, 또는 셀 상태 갱신에서 망각 게이트와 입력 게이트를 더하지 않고 곱하는(예: $f_t \times i_t \times \tilde{C}_t$) 실수가 흔합니다. 셀 상태는 두 항의 합이며, 은닉 상태를 만들 때는 반드시 $\tanh$로 셀 상태를 한 번 압축한 뒤 출력 게이트를 곱해야 합니다.

## 참고 자료

- Ian Goodfellow 외, Deep Learning (Deep Learning Book) — https://www.deeplearningbook.org
- Stanford CS231n: Convolutional Neural Networks for Visual Recognition — https://cs231n.stanford.edu
- 국가평생교육진흥원 독학학위제 — https://bdes.nile.or.kr
