---
title: '27. 독학사 3단계 딥러닝 실전 모의고사 1회분'
description: '실제 시험과 같은 시간·문항 구성으로 전 범위를 재구성한 24문항 객관식과 2문항 서술형 실전 모의고사, OMR·답안 작성 연습 안내 포함'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 회차는 국가평생교육진흥원 독학학위제 3단계 딥러닝 **최신 출제기준(평가영역) 전체**를 근거로 새로 구성한 **실전 모의고사**입니다. 실제 기출 문항을 그대로 옮긴 것이 아니며, 25·26편과도 중복되지 않는 새로운 숫자와 관점으로 재구성했습니다. 실제 시험의 정확한 문항 수·배점·시간은 응시하는 해의 공고를 반드시 확인하시기 바랍니다.
</Callout>

## 실전처럼 풀기 위한 안내

<Callout type="info">
  **시험 진행 방법:** 아래 24문항(객관식)과 2문항(서술형)을 실제 시험처럼 **중간에 정답을 확인하지 않고** 끝까지 풀어보세요. 객관식 1문항당 약 1분 30초, 서술형 1문항당 약 5분을 기준으로 전체 약 50분 안에 마치는 것을 목표로 삼습니다(실제 시험의 문항 수·시간은 공고마다 다르므로 이 시간은 연습용 기준입니다). 다 풀고 나서 해설을 보며 채점하고, 틀린 문항은 관련 편으로 돌아가 다시 확인하세요.
</Callout>

> **쉽게 말하면:** 답안지(OMR)에 옮겨 적는 연습도 실전 감각의 일부입니다. 종이에 1번부터 24번까지 번호를 적어두고 정답 번호만 옮기는 연습을 함께 해보면, 실제 시험장에서 답안지를 잘못 옮기는 실수를 줄일 수 있습니다.

## 문항 구성 개요

| 문항 번호 | 영역 | 관련 편 |
| --- | --- | --- |
| 1 | 시험 전략과 문항 유형 | 04편 |
| 2 | 딥러닝 기본 용어 | 02편 |
| 3 | 머신러닝과 딥러닝 관계 | 03편 |
| 4 | 퍼셉트론의 한계 | 05편 |
| 5 | 활성화 함수(Leaky ReLU) | 06편 |
| 6 | 손실 함수(교차엔트로피) | 06편 |
| 7 | 순전파 계산 | 07편 |
| 8 | 역전파와 그라디언트 부호 | 07편 |
| 9 | SGD와 Adam 비교 | 08편 |
| 10 | 모멘텀 개념 | 08편 |
| 11 | L1·L2 정규화 비교 | 09편 |
| 12 | Dropout의 추론 시 처리 | 09편 |
| 13 | 배치 정규화의 학습·추론 차이 | 10편 |
| 14 | 가중치 초기화와 대칭성 문제 | 10편 |
| 15 | CNN 출력 크기 계산 | 11편 |
| 16 | CNN 파라미터 수 계산 | 11편 |
| 17 | ResNet과 VGG 비교 | 12편 |
| 18 | RNN의 그라디언트 소실 | 13편 |
| 19 | LSTM과 GRU의 파라미터 규모 | 13편 |
| 20 | Seq2Seq와 Attention | 14편 |
| 21 | 멀티헤드 어텐션 | 15편 |
| 22 | GAN의 목적함수 | 17편 |
| 23 | 전이학습의 학습률 조정 | 18편 |
| 24 | F1 점수 계산 | 19편 |
| 서술 1 | 소프트맥스·교차엔트로피 손실 계산 | 06·15편 |
| 서술 2 | 혼동행렬 종합 지표 계산 | 19편 |

## 객관식 (1–24)

### 문제 1

<Quiz
  questionNumber={1}
  question="독학사 객관식 문제에서 '옳지 않은 것을 고르시오' 유형을 풀 때 가장 권장되는 접근으로 적절한 것은?"
  options={['보기 4개를 모두 하나씩 옳은 진술인지 확인하고, 그중 틀린 진술 하나를 찾아 표시한다.', '보기 중 가장 길게 쓰인 것을 무조건 정답으로 고른다.', '문제를 끝까지 읽지 않고 첫 번째 보기만 보고 바로 답을 정한다.', '옳지 않은 것을 고르는 문제는 항상 건너뛰고 마지막에 무작위로 찍는다.']}
  correctAnswer={1}
  explanation="옳지 않은 것 고르기 유형은 보기 4개 중 3개는 옳은 진술이고 1개만 틀린 진술입니다. 각 보기를 독립적으로 참·거짓 판단해, 스스로 아는 개념과 충돌하는 보기를 찾아내는 것이 가장 안전한 접근입니다."
  optionExplanations={['각 보기를 개별적으로 검증해 틀린 진술을 찾는다는 실제로 권장되는 접근이므로 정답입니다.', '보기의 길이와 정답 여부는 아무 관련이 없으므로 틀렸습니다.', '문제를 끝까지 읽지 않으면 요구 사항(옳은 것/옳지 않은 것)을 놓칠 위험이 커 틀렸습니다.', '무작위로 찍는 것은 최후의 수단일 뿐 권장되는 풀이 전략이 아니라 틀렸습니다.']}
/>

### 문제 2

<Quiz
  questionNumber={2}
  question="딥러닝에서 '파라미터(parameter)'와 '하이퍼파라미터(hyperparameter)'를 구분하는 기준으로 가장 적절한 것은?"
  options={['파라미터는 학습 과정에서 데이터로부터 자동으로 갱신되는 값이고, 하이퍼파라미터는 학습을 시작하기 전에 사람이 미리 정해두는 값이다.', '파라미터와 하이퍼파라미터는 완전히 같은 의미로 아무 차이가 없다.', '하이퍼파라미터는 학습이 끝난 후에만 생성되는 값이다.', '파라미터는 항상 정수형 값만 가질 수 있다.']}
  correctAnswer={1}
  explanation="가중치·편향처럼 학습 중 그라디언트에 의해 자동으로 갱신되는 값을 파라미터라 하고, 학습률·배치 크기·층 수처럼 학습 시작 전에 사람이 정하는 값을 하이퍼파라미터라 부릅니다."
  optionExplanations={['자동 갱신 여부와 사전 설정 여부라는 실제 구분 기준을 정확히 설명했으므로 정답입니다.', '두 용어는 값이 정해지는 방식이 근본적으로 달라 구분이 필요하므로 틀렸습니다.', '하이퍼파라미터는 학습을 시작하기 전에 미리 정해두는 값이므로 틀렸습니다.', '파라미터(가중치 등)는 일반적으로 실수형 값을 가지므로 틀렸습니다.']}
/>

### 문제 3

<Quiz
  questionNumber={3}
  question="딥러닝을 머신러닝의 하위 분야로 볼 때, 그 관계를 가장 적절하게 설명한 것은?"
  options={['딥러닝은 다층 신경망을 중심으로 하는 머신러닝의 한 갈래로, 표현학습을 통해 특징을 자동으로 추출한다는 점이 특징이다.', '머신러닝은 딥러닝이 등장한 이후 만들어진 더 좁은 개념이다.', '딥러닝과 머신러닝은 서로 대체 가능한 완전히 같은 개념이다.', '딥러닝은 머신러닝에 속하지 않는 독립적인 학문 분야이다.']}
  correctAnswer={1}
  explanation="머신러닝은 데이터로부터 규칙을 학습하는 더 넓은 개념이고, 딥러닝은 다층 신경망과 표현학습을 중심으로 하는 머신러닝의 하위 분야입니다."
  optionExplanations={['다층 신경망과 표현학습이라는 딥러닝의 실제 위치를 정확히 설명했으므로 정답입니다.', '머신러닝이 딥러닝보다 먼저 등장한 더 넓은 개념이므로 틀렸습니다.', '두 용어는 포함 관계에 있는 서로 다른 범위의 개념이므로 완전히 같다는 설명은 틀렸습니다.', '딥러닝은 머신러닝의 하위 분야로 분류되므로 독립적인 별개 분야라는 설명은 틀렸습니다.']}
/>

### 문제 4

<Quiz
  questionNumber={4}
  question="단일 퍼셉트론으로 XOR 문제를 풀 수 없는 근본적인 이유로 가장 적절한 것은?"
  options={['단일 퍼셉트론은 하나의 선형 결정 경계만 만들 수 있는데, XOR의 두 클래스는 하나의 직선으로 나눌 수 없는 배치를 이루기 때문이다.', 'XOR 문제는 입력이 너무 많아서 어떤 모델로도 풀 수 없기 때문이다.', '단일 퍼셉트론은 활성화 함수를 전혀 사용할 수 없기 때문이다.', 'XOR 문제는 원래 정답이 존재하지 않는 문제이기 때문이다.']}
  correctAnswer={1}
  explanation="XOR의 두 클래스(0과 1)는 입력 평면에서 대각선 방향으로 교차 배치되어, 하나의 직선으로는 절대 분리할 수 없습니다. 단일 퍼셉트론은 선형 결정 경계 하나만 만들 수 있으므로 이 문제를 풀 수 없습니다."
  optionExplanations={['선형 분리 불가능성이라는 XOR 문제의 근본 원인을 정확히 설명했으므로 정답입니다.', 'XOR은 입력이 2개뿐인 매우 단순한 문제이며, 다층 퍼셉트론으로는 쉽게 풀 수 있으므로 틀렸습니다.', '단일 퍼셉트론도 계단함수 등 활성화 함수를 사용하므로 틀렸습니다.', 'XOR은 명확한 정답(진리표)이 존재하는 논리 연산이므로 틀렸습니다.']}
/>

### 문제 5

<Quiz
  questionNumber={5}
  question="Leaky ReLU가 기본 ReLU의 어떤 한계를 보완하기 위해 제안되었는지 가장 적절하게 설명한 것은?"
  options={['입력이 음수일 때 출력을 완전히 0으로 만들어 그 뉴런의 그라디언트가 0이 되어 버리는 죽은 뉴런(dying ReLU) 문제를, 작은 기울기를 남겨 완화하려 했다.', 'Leaky ReLU는 출력값을 항상 0과 1 사이로 제한하기 위해 제안되었다.', 'Leaky ReLU는 시그모이드보다 계산량이 훨씬 많아 정확도를 높이기 위해 제안되었다.', 'Leaky ReLU는 양수 입력에 대해서도 출력을 0으로 만들기 위해 제안되었다.']}
  correctAnswer={1}
  explanation="기본 ReLU는 입력이 음수이면 출력과 그 지점의 기울기가 모두 0이 되어, 한 번 그렇게 된 뉴런이 더 이상 학습에 기여하지 못하는 죽은 뉴런 문제가 생길 수 있습니다. Leaky ReLU는 음수 구간에서도 작은 기울기(예: 0.01)를 남겨 이 문제를 완화합니다."
  optionExplanations={['죽은 뉴런 문제와 그 완화 방법이라는 Leaky ReLU의 실제 도입 이유를 정확히 설명했으므로 정답입니다.', 'Leaky ReLU도 ReLU처럼 출력 상한이 없는 함수이므로 0과 1 사이로 제한한다는 설명은 틀렸습니다.', 'Leaky ReLU는 오히려 ReLU와 비슷하게 계산이 단순한 함수이므로 틀렸습니다.', 'Leaky ReLU는 양수 입력에 대해서는 ReLU와 마찬가지로 입력을 그대로 출력하므로 틀렸습니다.']}
/>

### 문제 6

<Quiz
  questionNumber={6}
  question="이진 분류(binary classification)에서 사용하는 이진 교차엔트로피(binary cross-entropy) 손실에 대한 설명으로 가장 적절한 것은?"
  options={['정답이 1인데 모델의 예측 확률이 0에 가까울수록, 또는 정답이 0인데 예측 확률이 1에 가까울수록 손실 값이 매우 커진다.', '이진 교차엔트로피는 예측 확률과 정답이 얼마나 다르든 항상 같은 손실 값을 준다.', '이진 교차엔트로피는 회귀 문제에서만 사용할 수 있는 손실 함수이다.', '이진 교차엔트로피는 예측 확률이 정답과 정확히 같을 때 가장 큰 손실 값을 준다.']}
  correctAnswer={1}
  explanation="이진 교차엔트로피는 정답 클래스에 부여된 예측 확률이 낮을수록 로그 함수 특성에 의해 손실이 급격히 커지도록 설계되어, 모델이 확신을 가지고 틀렸을 때 특히 큰 페널티를 줍니다."
  optionExplanations={['예측이 정답과 크게 어긋날 때 손실이 급격히 커진다는 실제 성질을 정확히 설명했으므로 정답입니다.', '손실 값은 예측 확률과 정답의 차이에 따라 달라지므로 항상 같다는 설명은 틀렸습니다.', '교차엔트로피는 분류 문제의 대표적인 손실 함수이므로 회귀 전용이라는 설명은 틀렸습니다.', '예측 확률이 정답과 정확히 같을 때는 손실이 0에 가까워지므로 이 설명은 정반대라 틀렸습니다.']}
/>

### 문제 7

입력 $x_1 = -1$, $x_2 = 3$이고, 가중치 $w_1 = 0.2$, $w_2 = 0.1$, 편향 $b = 0.05$인 뉴런이 있다. $\tanh(0.15) \approx 0.149$를 이용한다.

<Quiz
  questionNumber={7}
  question="tanh 활성화 함수를 사용할 때 이 뉴런의 최종 출력값은 약 얼마인가?"
  options={['0.149', '0.15', '0.3', '-0.2']}
  correctAnswer={1}
  explanation="가중합은 z = 0.2*(-1) + 0.1*3 + 0.05 = -0.2 + 0.3 + 0.05 = 0.15입니다. tanh(0.15) ≈ 0.149가 최종 출력입니다."
  optionExplanations={['가중합 계산과 tanh 적용을 정확히 마친 값이므로 정답입니다.', '활성화 함수를 적용하지 않고 가중합(z=0.15) 자체를 답으로 쓴 오답입니다.', '두 번째 항(0.1*3=0.3)만 계산해 답으로 쓴 오답입니다.', '첫 번째 항(0.2*-1=-0.2)만 계산해 답으로 쓴 오답입니다.']}
/>

### 문제 8

<Quiz
  questionNumber={8}
  question="어떤 뉴런의 예측값이 목표값보다 작을 때, 평균제곱오차 기반 손실의 그라디언트($\partial L/\partial a = a - y$)의 부호와 그에 따른 경사하강법의 갱신 방향으로 가장 적절한 것은?"
  options={['그라디언트가 음수가 되고, 경사하강법은 그라디언트의 반대 방향으로 이동하므로 예측값이 커지는 방향으로 파라미터가 갱신된다.', '그라디언트가 항상 양수가 되어 예측값이 더 작아지는 방향으로 갱신된다.', '예측값이 목표값보다 작으면 그라디언트는 항상 0이 되어 갱신이 일어나지 않는다.', '경사하강법은 그라디언트와 같은 방향으로 이동해 예측값이 더 작아진다.']}
  correctAnswer={1}
  explanation="예측값 a가 목표값 y보다 작으면 a-y는 음수가 됩니다. 경사하강법은 그라디언트의 반대 방향으로 파라미터를 갱신하므로, 음수 그라디언트에 대해서는 파라미터(따라서 예측값)를 증가시키는 방향으로 이동해 예측을 목표값에 가깝게 만듭니다."
  optionExplanations={['그라디언트 부호와 경사하강법의 반대 방향 이동을 정확히 연결해 설명했으므로 정답입니다.', 'a<y이면 a-y는 음수이므로 그라디언트가 항상 양수라는 설명은 틀렸습니다.', 'a와 y가 다르면 그라디언트는 0이 아닌 값을 가지므로 틀렸습니다.', '경사하강법은 그라디언트의 반대 방향으로 이동하는 것이 정의이므로 같은 방향이라는 설명은 틀렸습니다.']}
/>

### 문제 9

<Quiz
  questionNumber={9}
  question="기본 SGD와 Adam 옵티마이저를 비교한 설명으로 옳지 않은 것은?"
  options={['Adam은 모든 파라미터에 항상 완전히 동일한 크기의 갱신량을 적용해 학습을 단순화한다.', 'Adam은 그라디언트의 1차·2차 모멘트를 추정해 파라미터별로 학습률을 적응적으로 조절한다.', '기본 SGD는 모멘트 추정 없이 현재 그라디언트에 학습률만 곱해 파라미터를 갱신한다.', 'Adam은 일반적으로 SGD보다 초기 학습 단계에서 더 빠르게 손실을 줄이는 경향이 있다고 알려져 있다.']}
  correctAnswer={1}
  explanation="Adam은 오히려 각 파라미터마다 서로 다른 갱신 크기를 적응적으로 적용하는 것이 핵심 특징입니다. 모든 파라미터에 완전히 동일한 갱신량을 적용한다는 설명은 Adam의 실제 동작과 반대이므로 옳지 않습니다."
  optionExplanations={['Adam은 파라미터별로 적응적인 학습률을 쓰므로, 모두 동일한 갱신량이라는 이 설명이 틀린 지점이며 정답입니다.', '1차·2차 모멘트 추정을 통한 적응적 학습률 조절이라는 Adam의 실제 원리를 옳게 설명했습니다.', '기본 SGD가 모멘트 없이 현재 그라디언트만 사용한다는 옳은 설명입니다.', 'Adam이 초기 학습에서 빠르게 수렴하는 경향이 있다는, 실무에서 널리 관찰되는 옳은 설명입니다.']}
/>

### 문제 10

<Quiz
  questionNumber={10}
  question="모멘텀(momentum) 항이 경사하강법에서 '관성'처럼 작용한다고 말할 때, 그 의미로 가장 적절한 것은?"
  options={['과거 여러 스텝의 그라디언트 방향이 누적되어, 현재 그라디언트가 일시적으로 방향을 바꾸어도 갱신 방향이 급격히 바뀌지 않고 이전 방향의 영향을 유지한다.', '모멘텀은 물리적인 질량을 신경망에 실제로 부여하는 기법이다.', '모멘텀을 쓰면 그라디언트 계산 자체가 필요 없어진다.', '모멘텀은 학습이 끝난 뒤 모델을 물리적으로 저장하는 절차이다.']}
  correctAnswer={1}
  explanation="모멘텀은 과거 그라디언트들의 누적 방향(속도 항)을 현재 갱신에 반영하기 때문에, 마치 물리적 관성처럼 이전에 움직이던 방향을 어느 정도 유지하려는 성질을 가집니다. 이 덕분에 일시적인 잡음에 덜 흔들리고 일관된 방향으로는 더 빠르게 이동합니다."
  optionExplanations={['과거 방향의 누적과 급격한 방향 전환 완화라는 관성 비유의 실제 의미를 정확히 설명했으므로 정답입니다.', '관성은 비유적 표현일 뿐 실제 물리적 질량을 신경망에 부여하는 것이 아니라 틀렸습니다.', '모멘텀도 현재 그라디언트를 계산해 속도 항에 반영해야 하므로 틀렸습니다.', '모멘텀은 학습 중 갱신 방식을 다루는 개념이며 모델 저장과는 무관해 틀렸습니다.']}
/>

### 문제 11

<Quiz
  questionNumber={11}
  question="L1 정규화와 L2 정규화를 비교한 설명으로 가장 적절한 것은?"
  options={['L1 정규화는 일부 가중치를 정확히 0으로 만들어 희소(sparse)한 모델을 만드는 경향이 있는 반면, L2 정규화는 가중치를 0에 가깝게 줄이되 정확히 0으로 만들지는 않는 경향이 있다.', 'L1과 L2 정규화는 항상 완전히 동일한 방식으로 가중치를 규제한다.', 'L2 정규화는 가중치의 절댓값 합을 페널티로 사용한다.', 'L1 정규화는 가중치의 제곱합을 페널티로 사용한다.']}
  correctAnswer={1}
  explanation="L1 정규화는 가중치의 절댓값 합을 페널티로 사용해 일부 가중치를 정확히 0으로 만드는 희소성을 유도하는 경향이 있고, L2 정규화는 가중치의 제곱합을 페널티로 사용해 가중치를 전체적으로 작게 줄이되 정확히 0이 되지는 않는 경향이 있습니다."
  optionExplanations={['희소성 유도(L1)와 크기 축소(L2)라는 두 정규화의 실제 차이를 정확히 설명했으므로 정답입니다.', 'L1과 L2는 페널티 항의 형태(절댓값 vs 제곱)가 달라 규제 결과도 다르므로 틀렸습니다.', '절댓값 합을 페널티로 쓰는 것은 L2가 아니라 L1의 정의이므로 틀렸습니다.', '제곱합을 페널티로 쓰는 것은 L1이 아니라 L2의 정의이므로 틀렸습니다.']}
/>

### 문제 12

<Quiz
  questionNumber={12}
  question="Dropout을 적용한 모델을 학습이 끝난 뒤 테스트(추론)할 때 흔히 취하는 처리 방식으로 가장 적절한 것은?"
  options={['모든 뉴런을 사용하는 대신, 학습 시 각 뉴런이 살아있던 비율만큼 출력을 스케일링해 학습 때와 비슷한 기댓값을 유지한다.', '테스트 시에도 학습 때와 똑같이 무작위로 일부 뉴런을 꺼서 매번 다른 결과를 낸다.', '테스트 시에는 Dropout 비율을 학습 때보다 항상 더 높게 설정한다.', '테스트 시에는 모델의 모든 가중치를 0으로 만든다.']}
  correctAnswer={1}
  explanation="테스트(추론) 시에는 일반적으로 모든 뉴런을 사용하지만, 학습 때 곱했던 유지 확률만큼 출력을 스케일링해 학습과 추론 사이의 기댓값을 맞춰주는 방식(inverted dropout 등)을 사용합니다."
  optionExplanations={['전체 뉴런 사용과 스케일링을 통한 기댓값 일치라는 실제 처리 방식을 정확히 설명했으므로 정답입니다.', '테스트 시 무작위로 뉴런을 끄면 예측이 매번 달라져 일관성이 없어지므로 일반적으로 이렇게 하지 않아 틀렸습니다.', 'Dropout 비율은 학습 하이퍼파라미터이며 테스트 시에는 보통 적용하지 않으므로 틀렸습니다.', '모든 가중치를 0으로 만들면 모델이 아무 예측도 할 수 없게 되어 틀렸습니다.']}
/>

### 문제 13

<Quiz
  questionNumber={13}
  question="배치 정규화(Batch Normalization)가 학습 시와 추론 시에 사용하는 평균·분산에 대한 설명으로 가장 적절한 것은?"
  options={['학습 시에는 각 미니배치의 평균·분산을 사용하고, 추론 시에는 학습 중 누적해 온 이동평균(전체 데이터에 대한 추정값)을 사용한다.', '학습 시와 추론 시 모두 항상 하나의 고정된 상수값만 사용한다.', '추론 시에는 매번 새로운 미니배치를 만들어 평균·분산을 다시 계산해야 한다.', '배치 정규화는 추론 시에는 아예 적용하지 않고 완전히 건너뛴다.']}
  correctAnswer={1}
  explanation="배치 정규화는 학습 중에는 각 미니배치의 평균·분산으로 정규화하면서, 동시에 전체 데이터에 대한 평균·분산의 이동평균을 계속 갱신해 둡니다. 추론 시에는 배치 단위 통계 대신 이 누적된 이동평균값을 사용해 정규화합니다."
  optionExplanations={['미니배치 통계(학습)와 이동평균(추론)을 구분해서 사용한다는 실제 동작을 정확히 설명했으므로 정답입니다.', '학습 시에는 배치마다 통계가 달라지므로 고정된 상수만 쓴다는 설명은 틀렸습니다.', '추론 시에는 보통 하나의 샘플씩 들어올 수도 있어 매번 미니배치 통계를 새로 계산하지 않고 누적된 값을 쓰므로 틀렸습니다.', '배치 정규화는 추론 시에도 이동평균을 이용해 정규화를 계속 적용하므로 틀렸습니다.']}
/>

### 문제 14

<Quiz
  questionNumber={14}
  question="신경망의 모든 가중치를 0으로 초기화했을 때 발생하는 문제로 가장 적절한 것은?"
  options={['같은 층의 모든 뉴런이 완전히 동일한 그라디언트를 받아 서로 다른 특징을 학습하지 못하는 대칭성(symmetry) 문제가 발생한다.', '모든 가중치를 0으로 초기화하면 학습이 다른 방법보다 항상 더 빨리 끝난다.', '모든 가중치를 0으로 초기화하면 그라디언트가 항상 매우 커진다.', '모든 가중치를 0으로 초기화해도 아무 문제가 생기지 않는다.']}
  correctAnswer={1}
  explanation="가중치를 모두 0(또는 모두 같은 값)으로 초기화하면, 같은 층의 모든 뉴런이 순전파에서 같은 값을 출력하고 역전파에서도 같은 그라디언트를 받게 되어, 학습이 진행돼도 뉴런들이 서로 다른 특징을 학습하지 못하는 대칭성 문제가 생깁니다."
  optionExplanations={['뉴런 간 대칭성 문제라는 0 초기화의 실제 문제점을 정확히 설명했으므로 정답입니다.', '대칭성 문제 때문에 오히려 학습이 제대로 이루어지지 않으므로 더 빠르다는 설명은 틀렸습니다.', '가중치가 0이면 초기 그라디언트가 오히려 작아지거나 특정 패턴에 갇힐 수 있어 항상 커진다는 설명은 틀렸습니다.', '0 초기화는 대칭성 문제를 일으키는 실제 문제 상황이므로 아무 문제가 없다는 설명은 틀렸습니다.']}
/>

### 문제 15

입력 이미지 크기가 27x27이고, 3x3 필터를 스트라이드 2, 패딩 1로 합성곱한다.

<Quiz
  questionNumber={15}
  question="합성곱 결과로 만들어지는 특징 맵의 한 변의 크기는?"
  options={['14', '13', '27', '12']}
  correctAnswer={1}
  explanation="출력 크기 공식은 (입력 크기 + 2*패딩 - 필터 크기)/스트라이드 + 1입니다. 대입하면 (27 + 2*1 - 3)/2 + 1 = (27+2-3)/2 + 1 = 26/2 + 1 = 13+1 = 14입니다."
  optionExplanations={['공식에 값을 정확히 대입해 계산한 값이므로 정답입니다.', '+1을 빠뜨리고 26/2 = 13만 계산한 오답입니다.', '패딩·스트라이드를 고려하지 않고 입력 크기를 그대로 쓴 오답입니다.', '패딩을 더하지 않고 (27-3)/2+1 = 13을 다시 잘못 조정해 12로 계산한 오답입니다.']}
/>

### 문제 16

입력 채널이 16, 필터 크기가 3x3, 출력 채널(필터 개수)이 64, 각 필터마다 편향이 1개 있는 합성곱층이 있다.

<Quiz
  questionNumber={16}
  question="이 합성곱층의 전체 학습 파라미터 개수는?"
  options={['9280', '9216', '144', '64']}
  correctAnswer={1}
  explanation="필터 하나의 파라미터는 (3*3*16)+1(편향) = 144+1 = 145개입니다. 필터가 64개이므로 전체는 145*64 = 9280개입니다."
  optionExplanations={['필터당 파라미터 수에 필터 개수를 곱한 정확한 계산이므로 정답입니다.', '편향을 빠뜨리고 (3*3*16)*64 = 9216만 계산한 오답입니다.', '필터 하나의 파라미터 수(144, 편향 제외)만 답으로 쓴 오답입니다.', '필터 개수(64)만 답으로 쓴 오답입니다.']}
/>

### 문제 17

<Quiz
  questionNumber={17}
  question="ResNet의 잔차 연결과 VGG의 단순 순차 구조를 비교한 설명으로 가장 적절한 것은?"
  options={['ResNet은 입력을 몇 개 층 건너뛰어 더하는 지름길을 두어 매우 깊은 층에서도 그라디언트가 잘 전달되게 하지만, VGG는 이런 지름길 없이 층을 순서대로만 쌓는다.', 'VGG는 잔차 연결을 이미 포함하고 있으며 ResNet과 구조가 동일하다.', 'ResNet은 합성곱층을 전혀 사용하지 않는다.', 'VGG는 ResNet보다 항상 더 깊은 층수를 가진다.']}
  correctAnswer={1}
  explanation="ResNet의 핵심은 F(x)+x 형태로 입력을 몇 개 층 건너뛰어 출력에 더하는 잔차(지름길) 연결입니다. VGG는 이런 지름길 없이 합성곱-활성화-풀링을 순서대로 깊게 쌓는 단순한 순차 구조를 갖습니다."
  optionExplanations={['잔차 연결의 유무라는 두 구조의 실제 핵심 차이를 정확히 설명했으므로 정답입니다.', '잔차 연결은 ResNet의 고유한 특징이며 VGG에는 없는 구조이므로 틀렸습니다.', 'ResNet도 합성곱층을 기본 구성 요소로 사용하는 CNN이므로 틀렸습니다.', '일반적으로 ResNet 계열이 VGG보다 더 깊은 구조까지 안정적으로 확장 가능하므로 이 설명은 반대라 틀렸습니다.']}
/>

### 문제 18

<Quiz
  questionNumber={18}
  question="기본 RNN이 긴 시퀀스를 학습할 때 그라디언트 소실(vanishing gradient) 문제를 겪는 이유로 가장 적절한 것은?"
  options={['시간 역방향으로 그라디언트를 전달할 때 같은 가중치 행렬과 활성화 함수의 미분값이 반복적으로 곱해지면서, 그 값들이 1보다 작으면 그라디언트가 점점 0에 가까워진다.', '기본 RNN은 애초에 그라디언트를 전혀 계산하지 않기 때문이다.', '기본 RNN은 입력 데이터의 개수가 항상 무한하기 때문이다.', '기본 RNN은 활성화 함수를 사용하지 않기 때문이다.']}
  correctAnswer={1}
  explanation="RNN을 시간 축으로 펼쳐 역전파(BPTT)를 하면, 같은 가중치와 활성화 함수의 미분값이 시점 수만큼 반복해서 곱해집니다. 이 값들이 1보다 작은 경우가 많아 시퀀스가 길어질수록 곱이 지수적으로 작아져 먼 과거 시점으로 그라디언트가 거의 전달되지 않는 그라디언트 소실이 나타납니다."
  optionExplanations={['반복된 곱셈으로 인한 그라디언트 소실의 실제 원인을 정확히 설명했으므로 정답입니다.', 'RNN도 다른 신경망처럼 BPTT로 그라디언트를 계산하므로 틀렸습니다.', '그라디언트 소실은 시퀀스 길이와 관련된 문제이며 데이터 개수가 무한하다는 것과는 무관해 틀렸습니다.', 'RNN도 tanh 등 활성화 함수를 사용하는 구조이므로 틀렸습니다.']}
/>

### 문제 19

<Quiz
  questionNumber={19}
  question="같은 은닉 상태 크기를 가진 LSTM과 GRU를 비교했을 때, 일반적인 파라미터 규모에 대한 설명으로 가장 적절한 것은?"
  options={['LSTM은 입력·망각·출력의 세 게이트를 가지는 반면 GRU는 두 개의 게이트만 가지므로, 같은 은닉 상태 크기에서 LSTM의 파라미터 수가 일반적으로 더 많다.', 'GRU는 게이트가 하나도 없어 파라미터가 전혀 없다.', 'LSTM과 GRU는 게이트 개수와 무관하게 항상 정확히 같은 파라미터 수를 가진다.', 'GRU는 항상 LSTM보다 게이트 개수가 더 많다.']}
  correctAnswer={1}
  explanation="LSTM은 입력·망각·출력 게이트(3개)와 셀 상태 계산용 파라미터를 가지고, GRU는 업데이트·리셋 게이트(2개)만 가지므로, 같은 은닉 상태 크기라면 게이트 수가 적은 GRU가 일반적으로 더 적은 파라미터를 가집니다."
  optionExplanations={['게이트 개수 차이에 따른 파라미터 규모 차이를 정확히 설명했으므로 정답입니다.', 'GRU도 업데이트·리셋 게이트를 가지고 있어 파라미터가 존재하므로 틀렸습니다.', '게이트 개수가 다르면 일반적으로 파라미터 수도 달라지므로 항상 같다는 설명은 틀렸습니다.', 'GRU(2개)가 LSTM(3개)보다 게이트 개수가 적으므로 이 설명은 반대라 틀렸습니다.']}
/>

### 문제 20

<Quiz
  questionNumber={20}
  question="Attention이 없는 기본 Seq2Seq와 Attention을 도입한 Seq2Seq를 비교한 설명으로 가장 적절한 것은?"
  options={['기본 Seq2Seq는 인코더의 마지막 은닉 상태 하나에만 의존하지만, Attention을 도입하면 디코더가 매 시점 인코더의 모든 은닉 상태를 가중합해 참고할 수 있다.', 'Attention을 도입하면 인코더가 아예 필요 없어진다.', 'Attention을 도입하면 디코더가 더 이상 순차적으로 출력을 생성하지 않아도 된다.', '기본 Seq2Seq와 Attention 기반 Seq2Seq는 성능 차이가 전혀 없다.']}
  correctAnswer={1}
  explanation="기본 Seq2Seq는 인코더의 마지막 은닉 상태(고정 크기 문맥 벡터) 하나에 모든 정보를 압축해야 하는 병목이 있지만, Attention을 도입하면 디코더가 매 출력 시점마다 인코더의 모든 시점 정보를 가중합해 그때그때 다른 문맥 벡터로 활용할 수 있습니다."
  optionExplanations={['병목 완화와 모든 시점 활용이라는 Attention 도입의 실제 효과를 정확히 설명했으므로 정답입니다.', 'Attention은 인코더의 출력을 더 적극적으로 활용하는 장치이므로 인코더가 필요 없어진다는 설명은 틀렸습니다.', '기본 디코더 구조(자기회귀적 순차 생성)는 Attention을 도입해도 유지되는 경우가 많으므로 틀렸습니다.', 'Attention 도입은 특히 긴 문장에서 성능을 개선하는 효과가 보고되어 있어 차이가 없다는 설명은 틀렸습니다.']}
/>

### 문제 21

<Quiz
  questionNumber={21}
  question="멀티헤드 어텐션(multi-head attention)에서 여러 개의 헤드를 사용하는 이유로 가장 적절한 것은?"
  options={['각 헤드가 서로 다른 표현 부분공간에서 어텐션을 계산해, 문법적 관계·의미적 관계 등 다양한 종류의 토큰 간 관계를 동시에 포착할 수 있게 하기 위해서이다.', '헤드를 여러 개 사용하면 모델의 파라미터 수를 항상 0으로 만들 수 있기 때문이다.', '헤드를 여러 개 사용하면 위치 인코딩이 필요 없어지기 때문이다.', '헤드를 여러 개 사용하는 것은 순전히 계산을 느리게 만들기 위한 장치이기 때문이다.']}
  correctAnswer={1}
  explanation="멀티헤드 어텐션은 입력을 여러 부분공간으로 나누어 각 헤드가 독립적으로 어텐션을 계산하게 함으로써, 한 헤드로는 포착하기 어려운 다양한 종류의 관계를 동시에 학습할 수 있게 합니다."
  optionExplanations={['다양한 관계를 동시에 포착하기 위한 멀티헤드의 실제 목적을 정확히 설명했으므로 정답입니다.', '멀티헤드 어텐션도 학습 가능한 가중치 행렬을 포함하므로 파라미터가 0이 되지 않아 틀렸습니다.', '멀티헤드 어텐션과 위치 인코딩은 서로 다른 목적을 가진 별개의 장치이므로 틀렸습니다.', '멀티헤드는 계산을 느리게 하기 위한 것이 아니라 표현력을 높이기 위한 설계이므로 틀렸습니다.']}
/>

### 문제 22

<Quiz
  questionNumber={22}
  question="GAN의 목적함수에서 판별자와 생성자가 서로 반대 방향의 목표를 가진다는 것을 가장 적절하게 설명한 것은?"
  options={['판별자는 진짜를 진짜로, 가짜를 가짜로 정확히 구별하는 확률을 높이려 하고, 생성자는 판별자가 가짜를 진짜로 착각하게 만드는 확률을 높이려 한다.', '판별자와 생성자는 완전히 동일한 손실을 함께 최소화하는 협력 관계이다.', '생성자는 판별자의 파라미터를 직접 수정할 수 있는 권한을 가진다.', '판별자는 학습이 끝난 뒤에는 더 이상 존재하지 않는 임시 구조이다.']}
  correctAnswer={1}
  explanation="GAN의 목적함수는 판별자가 진짜·가짜를 정확히 구별할 확률을 최대화하려 하는 동시에, 생성자는 판별자가 가짜 데이터를 진짜로 착각할 확률을 최대화하려는 미니맥스(minimax) 구조로 되어 있습니다."
  optionExplanations={['미니맥스 구조에서 두 네트워크의 반대되는 목표를 정확히 설명했으므로 정답입니다.', '판별자와 생성자는 서로 반대되는 목표를 가진 적대적 관계이므로 협력 관계라는 설명은 틀렸습니다.', '생성자는 자신의 파라미터만 갱신할 수 있으며 판별자의 파라미터를 직접 수정하지 않으므로 틀렸습니다.', '판별자는 생성 품질을 평가하는 데 학습 후에도 활용될 수 있는 구성 요소이므로 틀렸습니다.']}
/>

### 문제 23

<Quiz
  questionNumber={23}
  question="사전학습된 모델을 전체 미세조정(full fine-tuning)할 때 학습률을 처음부터 학습할 때보다 작게 설정하는 이유로 가장 적절한 것은?"
  options={['이미 유용한 정보를 담고 있는 사전학습된 가중치가 큰 학습률로 인해 급격히 흔들려 손상되는 것을 막기 위해서이다.', '학습률을 작게 하면 모델의 파라미터 개수가 줄어들기 때문이다.', '학습률을 작게 하면 새로운 데이터셋의 클래스 수가 자동으로 줄어들기 때문이다.', '학습률과 사전학습 여부는 서로 아무 관련이 없기 때문이다.']}
  correctAnswer={1}
  explanation="사전학습된 가중치는 이미 유용한 표현을 담고 있으므로, 새로운 데이터에 맞춰 미세조정할 때 지나치게 큰 학습률을 쓰면 이 정보가 급격히 손상될 위험이 있습니다. 그래서 처음부터 학습할 때보다 작은 학습률을 쓰는 것이 일반적입니다."
  optionExplanations={['사전학습 정보의 손상을 막기 위한 실제 이유를 정확히 설명했으므로 정답입니다.', '학습률은 파라미터 개수와 무관한 별개의 하이퍼파라미터이므로 틀렸습니다.', '학습률은 데이터셋의 클래스 수와 무관하므로 틀렸습니다.', '전이학습에서 학습률 크기를 사전학습 정보 보존과 연결해 조정하는 것이 실제 관례이므로 틀렸습니다.']}
/>

### 문제 24

이진 분류 모델을 100개의 테스트 데이터에 적용해 다음 혼동행렬을 얻었다.

| 구분 | 예측: 양성 | 예측: 음성 |
| --- | --- | --- |
| 실제: 양성 | TP = 30 | FN = 10 |
| 실제: 음성 | FP = 20 | TN = 40 |

<Quiz
  questionNumber={24}
  question="이 모델의 F1 점수는 약 얼마인가?"
  options={['0.667', '0.75', '0.6', '0.70']}
  correctAnswer={1}
  explanation="정밀도는 TP/(TP+FP) = 30/50 = 0.6이고, 재현율은 TP/(TP+FN) = 30/40 = 0.75입니다. F1은 조화평균이므로 F1 = 2*(0.6*0.75)/(0.6+0.75) = 2*0.45/1.35 = 0.9/1.35 ≈ 0.667입니다."
  optionExplanations={['정밀도·재현율을 각각 구한 뒤 조화평균 공식까지 정확히 계산한 값이므로 정답입니다.', '재현율(0.75) 값을 그대로 F1으로 착각한 오답입니다.', '정밀도(0.6) 값을 그대로 F1으로 착각한 오답입니다.', '정밀도와 재현율을 산술평균((0.6+0.75)/2=0.675에 가까운 값을 반올림)한 오답입니다.']}
/>

## 서술형 (1–2)

### 서술형 문제 1: 소프트맥스·교차엔트로피 손실 계산

3개 클래스 분류 문제에서 어떤 데이터의 출력층 로짓이 $z = [1.2, 0.3, -0.5]$이고 정답 클래스는 첫 번째 클래스이다. $e^{1.2} \approx 3.32$, $e^{0.3} \approx 1.35$, $e^{-0.5} \approx 0.61$을 이용해, 소프트맥스 확률을 구하고 이어서 교차엔트로피 손실을 계산하는 전 과정을 서술하시오($\ln(0.63) \approx -0.462$를 이용한다).

**모범답안**

1. **소프트맥스 분모(정규화 상수) 계산**: 세 지수값을 모두 더하면 $3.32 + 1.35 + 0.61 = 5.28$입니다.
2. **각 클래스의 소프트맥스 확률 계산**: 첫 번째 클래스 확률은 $\dfrac{3.32}{5.28} \approx 0.629$이고, 두 번째는 $\dfrac{1.35}{5.28} \approx 0.256$, 세 번째는 $\dfrac{0.61}{5.28} \approx 0.116$입니다. 세 확률의 합은 약 1(0.629+0.256+0.116=1.001, 근사 오차)로, 확률 분포의 조건을 만족합니다.
3. **교차엔트로피 손실 공식**: 정답 클래스가 하나뿐인 다중 분류에서 교차엔트로피 손실은 $L = -\ln(p_{정답})$입니다. 여기서 $p_{정답}$은 정답 클래스에 대한 모델의 예측 확률입니다.
4. **손실 값 계산**: 정답 클래스는 첫 번째이므로 $p_{정답} \approx 0.629$입니다. $L = -\ln(0.629) \approx -(-0.462) = 0.462$입니다.

**해석**: 정답 클래스에 부여된 확률(0.629)이 1에 비교적 가까울수록 손실이 작아집니다. 만약 모델이 정답 클래스에 아주 낮은 확률(예: 0.05)을 주었다면 $-\ln(0.05) \approx 3.0$처럼 손실이 훨씬 크게 나와, 확신을 갖고 틀렸을 때 더 강하게 페널티를 주는 교차엔트로피의 성질을 확인할 수 있습니다.

**자주 틀리는 점**: 소프트맥스 확률을 구한 뒤 정답 클래스가 아닌 다른 클래스의 확률로 $-\ln$을 계산하는 실수, 또는 자연로그 대신 상용로그(밑이 10인 로그)를 사용하는 실수가 흔합니다. 교차엔트로피 손실은 반드시 자연로그($\ln$, 밑이 $e$)를 사용하고, 정답 클래스에 해당하는 확률만 사용합니다.

### 서술형 문제 2: 혼동행렬 종합 지표 계산

어떤 이진 분류 모델을 100개의 테스트 데이터에 적용해 TP = 45, FP = 15, FN = 5, TN = 35를 얻었다. 정확도, 정밀도, 재현율, F1 점수를 순서대로 계산하고, 이 모델의 성능을 어떻게 해석할 수 있는지 서술하시오.

**모범답안**

1. **정확도(accuracy)**: $\dfrac{TP+TN}{TP+FP+FN+TN} = \dfrac{45+35}{100} = \dfrac{80}{100} = 0.80$입니다.
2. **정밀도(precision)**: $\dfrac{TP}{TP+FP} = \dfrac{45}{45+15} = \dfrac{45}{60} = 0.75$입니다.
3. **재현율(recall)**: $\dfrac{TP}{TP+FN} = \dfrac{45}{45+5} = \dfrac{45}{50} = 0.90$입니다.
4. **F1 점수**: $2 \times \dfrac{0.75 \times 0.90}{0.75+0.90} = 2 \times \dfrac{0.675}{1.65} = \dfrac{1.35}{1.65} \approx 0.818$입니다.

**해석**: 재현율(0.90)이 정밀도(0.75)보다 높다는 것은, 이 모델이 실제 양성 데이터를 놓치는 경우(FN=5)는 상대적으로 적지만, 양성으로 예측한 것 중에는 실제로는 음성인 경우(FP=15)가 어느 정도 섞여 있다는 뜻입니다. 즉 이 모델은 양성을 놓치지 않으려는 경향(재현율 우선)이 있다고 해석할 수 있으며, 질병 진단처럼 양성을 놓치는 비용이 큰 문제에서는 이런 경향이 유리할 수 있습니다.

**자주 틀리는 점**: 정밀도와 재현율의 분모를 서로 바꾸어 계산하는 실수(정밀도의 분모는 예측이 양성인 것 전체 TP+FP, 재현율의 분모는 실제가 양성인 것 전체 TP+FN)가 매우 흔합니다. "예측 기준(정밀도)"과 "실제 기준(재현율)"이라는 기준의 차이를 각 지표의 이름과 함께 기억해 두면 혼동을 줄일 수 있습니다.

## 참고 자료

- Ian Goodfellow 외, Deep Learning (Deep Learning Book) — https://www.deeplearningbook.org
- Stanford CS231n: Convolutional Neural Networks for Visual Recognition — https://cs231n.stanford.edu
- scikit-learn 공식 문서(평가지표) — https://scikit-learn.org/stable/
- 국가평생교육진흥원 독학학위제 — https://bdes.nile.or.kr
