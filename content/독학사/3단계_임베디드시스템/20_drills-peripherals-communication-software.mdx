---
title: '20. 유형문제: 주변장치·통신·소프트웨어'
description: 'GPIO·타이머·PWM·WDT·ADC/DAC·DMA, UART/SPI/I2C/CAN, 부트로더·링커·드라이버·임베디드 C를 국가평생교육진흥원 출제기준 기반으로 재구성한 유형 문제 23개'
---

<Callout type="warning">
  이 문제들은 실제 독학사 기출 문제가 아닙니다. 국가평생교육진흥원 독학학위제 임베디드시스템 출제기준(평가영역)을 바탕으로 08·09·10·11·12편의 개념을 재구성한 유형 문제이며, 실제 시험에 나온 문항을 그대로 옮기지 않았습니다. 마지막 문제(23번)는 14편에서 다루는 RTOS 스케줄링 계산을 미리 확인하는 종합 문제입니다. 최신 출제 범위와 문항 수는 반드시 국가평생교육진흥원 공고로 다시 확인하세요.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="GPIO 핀을 입력 모드로 설정했을 때, 풀업(pull-up) 저항과 풀다운(pull-down) 저항의 역할로 가장 옳은 것은?"
  options={['핀에 아무것도 연결되지 않았을 때 전압이 떠다니지(플로팅) 않도록 기본 상태를 High 또는 Low로 고정한다', '핀의 최대 허용 전류를 두 배로 늘려준다', '핀의 신호를 디지털에서 아날로그로 변환한다', 'GPIO 핀의 클럭 속도를 두 배로 높인다']}
  correctAnswer={1}
  explanation="풀업 저항은 스위치가 열려 있을 때 핀을 전원(High)에 연결해 두고, 풀다운 저항은 그라운드(Low)에 연결해 두어, 외부 신호가 없을 때 핀 전압이 불안정하게 떠다니는 플로팅 상태를 막고 정해진 기본값을 유지하게 한다."
  optionExplanations={['정답이다. 풀업·풀다운은 플로팅 상태를 막고 기본 논리값을 정해 주는 역할을 한다.', '풀업·풀다운 저항은 전류 허용치와 직접 관련이 없다.', '풀업·풀다운은 디지털 신호의 기본값을 정하는 것이며 아날로그 변환과는 무관하다.', '풀업·풀다운 저항은 클럭 속도와 관련이 없는 정적인 전압 고정 소자다.']}
/>

<Quiz
  questionNumber={2}
  question="폴링(polling) 방식과 인터럽트(interrupt) 방식을 비교한 설명으로 옳지 않은 것은?"
  options={['폴링은 CPU가 주기적으로 상태 레지스터를 직접 확인하므로 이벤트가 드물게 발생하면 CPU 자원을 낭비하기 쉽다', '인터럽트는 이벤트가 발생했을 때만 CPU가 반응하므로 CPU가 다른 작업을 할 여유를 확보할 수 있다', '인터럽트는 반드시 폴링보다 응답 지연이 더 크다', '이벤트가 매우 빈번하게 연속으로 발생하는 경우에는 오히려 폴링이 인터럽트 오버헤드보다 유리할 수 있다']}
  correctAnswer={3}
  explanation="일반적으로 인터럽트는 이벤트 발생 즉시 CPU를 깨우므로 폴링보다 응답이 빠른 경우가 많다. 항상 인터럽트가 폴링보다 지연이 크다는 단정은 옳지 않다."
  optionExplanations={['폴링이 드문 이벤트를 계속 확인하느라 CPU 자원을 낭비한다는 설명은 옳다.', '인터럽트가 CPU에 여유를 준다는 설명은 옳다.', '옳지 않은 설명이라 정답이다. 인터럽트는 일반적으로 폴링보다 응답이 빠르며, 이벤트 처리 지연이 항상 더 크다고 단정할 수 없다.', '이벤트가 매우 빈번하면 매번 인터럽트 진입·복귀 오버헤드가 누적되어 폴링이 유리해질 수 있다는 설명은 옳다.']}
/>

<Quiz
  questionNumber={3}
  question="타이머 클럭이 16MHz이고 프리스케일러(prescaler) 설정값이 999(1000분주), ARR(Auto-Reload Register, 자동 재적재 값)이 1999로 설정되어 있을 때, 이 타이머의 오버플로우 주기는?"
  options={['12.5밀리초', '125밀리초', '1.25밀리초', '1.25초']}
  correctAnswer={2}
  explanation="타이머 주기는 (프리스케일러값+1)×(ARR+1)을 타이머 클럭으로 나눈 값이다. (999+1)×(1999+1) = 1000×2000 = 2,000,000이고, 이를 16,000,000Hz로 나누면 0.125초, 즉 125밀리초가 된다."
  optionExplanations={['자릿수를 한 자리 작게 계산한 값이다.', '정답이다. 1000×2000을 16,000,000으로 나누면 0.125초(125밀리초)다.', '자릿수를 한 자리 작게, 그리고 단위를 다르게 계산한 값이다.', '단위를 밀리초가 아니라 초로 잘못 표기하며 자릿수도 10배 크게 계산한 값이다.']}
/>

<Quiz
  questionNumber={4}
  question="PWM(Pulse Width Modulation) 신호에서 한 주기(period)가 1,000마이크로초이고 하이(High) 구간이 300마이크로초일 때, 듀티 사이클(duty cycle)은?"
  options={['70퍼센트', '30퍼센트', '300퍼센트', '3.3퍼센트']}
  correctAnswer={2}
  explanation="듀티 사이클은 한 주기 중 하이 구간이 차지하는 비율이다. 300을 1,000으로 나누면 0.3이므로 백분율로는 30퍼센트다."
  optionExplanations={['하이 구간과 로우(Low) 구간의 비율을 반대로 계산한 값(1000-300=700, 700/1000=70퍼센트)이다.', '정답이다. 300/1000 = 0.3이므로 듀티 사이클은 30퍼센트다.', '비율 계산에서 소수점을 잘못 옮겨 100을 곱하지 않고 그대로 곱한 값이다.', '300과 1,000의 위치를 바꿔 계산한 값이다.']}
/>

<Quiz
  questionNumber={5}
  question="워치독 타이머(Watchdog Timer, WDT)의 역할로 가장 옳은 것은?"
  options={['ADC 변환 완료 시점을 CPU에 알려준다', '소프트웨어가 정해진 시간 안에 리프레시(kick)하지 않으면 시스템을 강제로 리셋해 무한 루프나 멈춤 상태에서 스스로 복구하게 한다', 'PWM 신호의 듀티 사이클을 자동으로 조절한다', 'UART 통신 속도를 자동으로 보정한다']}
  correctAnswer={2}
  explanation="워치독 타이머는 소프트웨어가 살아있다는 신호(리프레시, kick)를 정해진 시간 안에 계속 보내야 하고, 이 신호가 오지 않으면 시스템이 멈추거나 무한 루프에 빠진 것으로 간주해 자동으로 리셋을 발생시켜 스스로 복구하게 한다."
  optionExplanations={['ADC 변환 완료 알림은 ADC 인터럽트의 역할이며 워치독과는 다른 기능이다.', '정답이다. 워치독은 소프트웨어의 응답이 끊기면 강제 리셋으로 자동 복구를 유도하는 안전장치다.', 'PWM 듀티 사이클 조절은 타이머·PWM 컨트롤러의 역할이며 워치독의 기능이 아니다.', 'UART 통신 속도 보정은 워치독과 무관한 통신 컨트롤러의 기능이다.']}
/>

<Quiz
  questionNumber={6}
  question="기준 전압(Vref)이 3.3V인 12비트 ADC에서 변환된 디지털 코드값이 2048일 때, 이에 대응하는 아날로그 입력 전압은?"
  options={['1.65볼트', '3.3볼트', '0.825볼트', '2.048볼트']}
  correctAnswer={1}
  explanation="12비트 ADC의 전체 코드 범위는 0부터 2^12-1인 4095까지, 즉 전체 스텝 수는 4096이다. 전압은 (코드값/4096)×Vref로 계산한다. 2048/4096은 0.5이므로 0.5×3.3V = 1.65V다."
  optionExplanations={['정답이다. 2048은 전체 스텝의 정확히 절반이므로 전압도 기준전압의 절반인 1.65V다.', '이는 코드값이 최댓값(4095에 가까운 값)일 때의 전압으로, 2048에 대응하는 값이 아니다.', '전압 계산에서 절반을 한 번 더 나누는 실수를 한 값이다.', '코드값을 전압 단위로 그대로 착각해 소수점만 옮긴 값이다.']}
/>

<Quiz
  questionNumber={7}
  question="ADC(Analog-to-Digital Converter)와 DAC(Digital-to-Analog Converter)의 관계에 대한 설명으로 옳은 것은?"
  options={['ADC는 아날로그 신호를 디지털 값으로, DAC는 디지털 값을 아날로그 신호로 변환하는 서로 반대 방향의 변환기다', 'ADC와 DAC는 동일한 방향으로 신호를 변환하는 장치다', 'DAC는 온도 센서 신호만 변환할 수 있다', 'ADC는 항상 DAC보다 변환 속도가 빠르다']}
  correctAnswer={1}
  explanation="ADC는 센서 등에서 들어오는 연속적인 아날로그 신호를 이산적인 디지털 값으로 바꾸고, DAC는 반대로 디지털 값을 연속적인 아날로그 신호로 되돌리는, 서로 반대 방향의 변환을 수행하는 장치다."
  optionExplanations={['정답이다. ADC와 DAC는 변환 방향이 정반대인 한 쌍의 장치다.', 'ADC와 DAC는 변환 방향이 반대이므로 동일한 방향이라는 설명은 틀렸다.', 'DAC는 온도 센서에 국한되지 않고 오디오 출력, 전압 제어 등 다양한 아날로그 출력에 쓰인다.', '변환 속도는 두 장치의 방식과 해상도에 따라 다르며 항상 ADC가 빠르다고 단정할 수 없다.']}
/>

<Quiz
  questionNumber={8}
  question="DMA(Direct Memory Access)의 주된 목적으로 가장 옳은 것은?"
  options={['CPU를 거치지 않고 메모리와 주변장치, 또는 메모리와 메모리 사이에서 데이터를 직접 전송해 CPU 부담을 줄인다', 'CPU의 명령어 집합을 확장해 더 많은 연산을 가능하게 한다', '메모리의 물리적 용량을 늘려준다', 'ADC의 변환 정밀도를 높여준다']}
  correctAnswer={1}
  explanation="DMA는 CPU의 개입 없이 전용 컨트롤러가 메모리와 주변장치 사이, 또는 메모리와 메모리 사이의 데이터 전송을 직접 수행해, 대량의 데이터를 옮기는 동안 CPU가 다른 작업을 계속할 수 있게 해 준다."
  optionExplanations={['정답이다. CPU 개입 없이 데이터를 직접 옮겨 CPU 부담을 줄이는 것이 DMA의 목적이다.', 'DMA는 명령어 집합을 확장하는 기능이 아니라 데이터 전송을 대신하는 하드웨어다.', 'DMA는 메모리의 물리적 용량과 무관한 전송 기능이다.', 'DMA는 ADC의 정밀도가 아니라 변환된 데이터를 옮기는 속도와 CPU 부담에 관련된 기능이다.']}
/>

<Quiz
  questionNumber={9}
  question="UART, SPI, I2C, CAN 통신 방식을 비교한 설명으로 옳지 않은 것은?"
  options={['UART는 별도의 클럭 선 없이 송신·수신 두 선으로 비동기 통신을 한다', 'SPI는 별도의 클럭 선을 두고 마스터가 클럭을 생성하는 동기식 통신이다', 'I2C는 두 개의 선(SDA, SCL)만으로 여러 장치를 주소로 구분해 통신할 수 있다', 'CAN은 항상 두 장치 사이의 1대1 연결만 지원하며 다중 노드 통신은 불가능하다']}
  correctAnswer={4}
  explanation="CAN(Controller Area Network)은 여러 노드가 하나의 버스를 공유하며 통신하는 멀티마스터 버스 방식으로, 1대1 연결에 국한되지 않고 다수의 노드가 동시에 참여하는 통신을 지원한다."
  optionExplanations={['UART가 클럭 선 없이 비동기로 통신한다는 설명은 옳다.', 'SPI가 마스터가 클럭을 생성하는 동기식 통신이라는 설명은 옳다.', 'I2C가 SDA, SCL 두 선으로 여러 장치를 주소로 구분한다는 설명은 옳다.', '옳지 않은 설명이라 정답이다. CAN은 여러 노드가 참여하는 멀티마스터 버스 방식이며 1대1 연결에 국한되지 않는다.']}
/>

<Quiz
  questionNumber={10}
  question="UART 통신에서 보레이트(baud rate)가 9,600bps일 때 한 비트를 전송하는 데 걸리는 시간은 약 얼마인가?"
  options={['약 9.6마이크로초', '약 104.2마이크로초', '약 1.042마이크로초', '약 960마이크로초']}
  correctAnswer={2}
  explanation="한 비트의 전송 시간은 보레이트의 역수다. 1을 9,600으로 나누면 약 0.0001042초, 즉 약 104.2마이크로초가 된다."
  optionExplanations={['자릿수를 잘못 계산한 값으로 실제 값보다 훨씬 작다.', '정답이다. 1/9600초는 약 104.2마이크로초다.', '자릿수를 두 자리 작게 계산한 값이다.', '보레이트 값을 그대로 마이크로초 단위로 착각해 곱한 값이다.']}
/>

<Quiz
  questionNumber={11}
  question="SPI(Serial Peripheral Interface)의 CPOL(Clock Polarity)과 CPHA(Clock Phase) 설정에 대한 설명으로 가장 옳은 것은?"
  options={['CPOL과 CPHA는 SPI의 전송 속도(클럭 주파수)를 직접 결정하는 값이다', 'CPOL은 클럭의 유휴(idle) 상태가 High인지 Low인지를, CPHA는 데이터를 클럭의 첫 번째 에지에서 샘플링할지 두 번째 에지에서 샘플링할지를 정한다', 'CPOL과 CPHA는 I2C에서만 사용되는 설정이다', 'CPOL과 CPHA는 항상 동일한 값으로 고정되어 변경할 수 없다']}
  correctAnswer={2}
  explanation="CPOL은 클럭 신호가 통신하지 않을 때(유휴 상태) High인지 Low인지를 정하고, CPHA는 데이터를 클럭의 첫 번째 에지에서 샘플링할지 두 번째 에지에서 샘플링할지를 정해, 이 두 값의 조합(모드 0~3)으로 마스터와 슬레이브의 타이밍을 맞춘다."
  optionExplanations={['CPOL·CPHA는 클럭의 극성과 위상을 정하는 값이며 클럭 주파수 자체를 결정하지 않는다.', '정답이다. CPOL은 유휴 상태의 극성을, CPHA는 샘플링 에지를 정하는 설정이다.', 'CPOL·CPHA는 I2C가 아니라 SPI의 설정이다.', 'CPOL·CPHA는 장치와 상황에 맞게 소프트웨어로 설정을 바꿀 수 있는 값이다.']}
/>

<Quiz
  questionNumber={12}
  question="I2C(Inter-Integrated Circuit) 통신에서 여러 슬레이브 장치를 구분하는 방법으로 옳은 것은?"
  options={['각 슬레이브마다 별도의 데이터 선을 추가로 연결해 구분한다', '각 슬레이브에 부여된 고유 주소를 마스터가 프레임 앞부분에 실어 보내 구분한다', '슬레이브 장치의 전원을 순서대로 켜고 꺼서 구분한다', 'CAN처럼 우선순위 기반 중재(arbitration)로 구분한다']}
  correctAnswer={2}
  explanation="I2C는 SDA, SCL 두 선만 사용하는 버스 방식이므로, 여러 슬레이브를 구분하기 위해 통신 프레임의 앞부분에 목적지 슬레이브의 고유 주소를 실어 보내고 그 주소를 가진 장치만 응답하도록 한다."
  optionExplanations={['I2C는 추가 데이터 선 없이 SDA, SCL 두 선만으로 여러 장치를 구분하는 것이 특징이다.', '정답이다. 프레임 앞부분의 주소로 목적지 슬레이브를 구분하는 것이 I2C의 핵심 방식이다.', '전원을 순서대로 켜고 끄는 방식은 일반적인 I2C 통신 방식이 아니다.', '우선순위 기반 중재는 CAN의 방식이며 I2C의 주소 지정 방식과는 다르다.']}
/>

<Quiz
  questionNumber={13}
  question="CAN(Controller Area Network) 버스의 중재(arbitration) 방식에 대한 설명으로 가장 옳은 것은?"
  options={['먼저 전송을 시작한 노드가 무조건 우선권을 갖는다', '식별자(ID) 값이 낮을수록(우성 비트가 많을수록) 우선순위가 높아 여러 노드가 동시에 전송해도 충돌 없이 우선순위가 높은 메시지가 버스를 차지한다', '중재는 소프트웨어 타이머로만 구현되며 하드웨어와는 무관하다', '모든 노드가 동일한 우선순위를 가지므로 중재 개념이 존재하지 않는다']}
  correctAnswer={2}
  explanation="CAN은 여러 노드가 동시에 버스에 접근해도, 식별자 비트를 하나씩 비교해 우성(dominant) 비트를 가진(일반적으로 값이 낮은) 노드가 버스를 계속 차지하고 나머지 노드는 즉시 전송을 멈추는 방식으로 충돌 없이 우선순위를 가려낸다."
  optionExplanations={['CAN은 먼저 시작한 순서가 아니라 식별자 값에 따른 우선순위로 중재한다.', '정답이다. 낮은 식별자(우성 비트가 많은 쪽)가 우선권을 가지며 충돌 없이 중재가 이루어진다.', 'CAN의 중재는 버스 하드웨어 수준에서 비트 단위로 이루어지는 물리적인 메커니즘이다.', 'CAN은 식별자로 우선순위를 구분하는 것이 핵심 특징이며 중재 개념이 없다는 설명은 틀렸다.']}
/>

<Quiz
  questionNumber={14}
  question="임베디드 소프트웨어 개발에서 크로스 개발(cross development)이 필요한 이유로 가장 옳은 것은?"
  options={['타깃 임베디드 보드 자체의 자원이 부족해 컴파일러·개발 도구를 직접 실행하기 어려운 경우가 많기 때문이다', '타깃 보드가 항상 개발용 PC보다 성능이 더 좋기 때문이다', '크로스 개발은 임베디드 시스템에서 전혀 필요하지 않은 개념이다', '크로스 개발은 오직 그래픽 사용자 인터페이스 개발에만 쓰인다']}
  correctAnswer={1}
  explanation="임베디드 타깃 보드는 메모리와 처리 능력이 제한적이어서 컴파일러 같은 무거운 개발 도구를 직접 실행하기 어려운 경우가 많다. 그래서 성능이 충분한 호스트 PC에서 타깃 아키텍처용 코드를 컴파일(크로스 컴파일)한 뒤 타깃에 옮겨 실행하는 크로스 개발 방식을 사용한다."
  optionExplanations={['정답이다. 타깃 보드의 자원 제약이 크로스 개발을 쓰는 핵심 이유다.', '일반적으로 개발용 PC가 타깃 임베디드 보드보다 훨씬 높은 성능을 갖는 경우가 많아 이 설명은 반대로 되어 있다.', '크로스 개발은 임베디드 소프트웨어 개발에서 널리 쓰이는 핵심적인 개발 방식이다.', '크로스 개발은 GUI 개발에 국한되지 않고 임베디드 소프트웨어 전반의 컴파일·빌드 과정에 쓰인다.']}
/>

<Quiz
  questionNumber={15}
  question="부트로더(bootloader)의 역할로 가장 옳은 것은?"
  options={['전원이 켜진 뒤 가장 먼저 실행되어 하드웨어를 초기화하고 애플리케이션 펌웨어를 메모리에 적재하거나 실행을 넘겨주는 역할을 한다', 'ADC 변환 결과를 실시간으로 필터링하는 역할을 한다', 'RTOS의 태스크 스케줄링을 대신 수행하는 역할을 한다', 'UART 통신 속도를 자동으로 최적화하는 역할을 한다']}
  correctAnswer={1}
  explanation="부트로더는 전원 인가나 리셋 직후 가장 먼저 실행되는 코드로, 최소한의 하드웨어 초기화를 수행하고 애플리케이션 펌웨어를 검증·적재한 뒤 실행 흐름을 넘기거나, 새 펌웨어를 다운로드해 갱신하는 역할을 한다."
  optionExplanations={['정답이다. 하드웨어 초기화와 애플리케이션 적재·실행 이관이 부트로더의 핵심 역할이다.', 'ADC 필터링은 부트로더가 아니라 애플리케이션 코드나 신호 처리 모듈의 역할이다.', 'RTOS 스케줄링은 부트로더가 아니라 RTOS 커널 자체의 역할이다.', 'UART 통신 속도 최적화는 부트로더의 일반적인 역할이 아니다.']}
/>

<Quiz
  questionNumber={16}
  question="링커 스크립트(linker script)와 스타트업 코드(startup code)에 대한 설명으로 옳은 것은?"
  options={['링커 스크립트는 각 섹션(코드, 데이터, 스택 등)을 메모리의 어느 주소에 배치할지 정의하고, 스타트업 코드는 전원 인가 후 C 런타임 환경(스택 설정, 전역 변수 초기화 등)을 준비한 뒤 main 함수를 호출한다', '링커 스크립트는 실행 시점에 동적으로 메모리를 할당하는 프로그램이다', '스타트업 코드는 항상 애플리케이션 개발자가 매번 새로 작성해야 하며 툴체인이 제공하지 않는다', '링커 스크립트와 스타트업 코드는 이름만 다를 뿐 동일한 파일을 가리킨다']}
  correctAnswer={1}
  explanation="링커 스크립트는 빌드 시점에 코드 섹션, 데이터 섹션, 스택 영역 등을 실제 메모리 맵의 어느 주소에 배치할지 정의하는 설정 파일이고, 스타트업 코드는 리셋 직후 스택 포인터 설정, 전역·정적 변수 초기화(.data 복사, .bss 초기화) 등 C 언어가 정상 동작하기 위한 환경을 준비한 뒤 main 함수로 진입시키는 코드다."
  optionExplanations={['정답이다. 링커 스크립트는 메모리 배치를, 스타트업 코드는 C 런타임 준비와 main 진입을 담당한다.', '링커 스크립트는 빌드 시점의 정적인 배치 규칙을 정의하는 것이며 실행 시점에 동적으로 동작하는 프로그램이 아니다.', '스타트업 코드는 대개 툴체인이나 벤더가 기본 템플릿을 제공하며, 필요할 때만 개발자가 수정한다.', '링커 스크립트와 스타트업 코드는 각각 메모리 배치 정의와 초기화 코드라는 서로 다른 역할을 하는 별개의 파일이다.']}
/>

<Quiz
  questionNumber={17}
  question="디바이스 드라이버(device driver)의 역할로 가장 옳은 것은?"
  options={['하드웨어 레지스터를 직접 조작하는 세부 사항을 감추고, 상위 애플리케이션이 일관된 인터페이스로 주변장치를 사용할 수 있게 한다', '애플리케이션 코드를 컴파일하는 도구다', 'CPU의 클럭 주파수를 물리적으로 변경하는 하드웨어 부품이다', 'RTOS 없이는 절대 존재할 수 없는 개념이다']}
  correctAnswer={1}
  explanation="디바이스 드라이버는 특정 주변장치의 레지스터 조작 같은 하드웨어 세부 사항을 캡슐화(추상화)해, 상위 애플리케이션 코드가 일관된 함수 호출만으로 그 장치를 사용할 수 있게 하는 소프트웨어 계층이다."
  optionExplanations={['정답이다. 하드웨어 세부 사항을 감추고 일관된 인터페이스를 제공하는 것이 드라이버의 핵심 역할이다.', '컴파일 도구는 툴체인의 역할이며 디바이스 드라이버와는 다른 개념이다.', '드라이버는 소프트웨어 계층이며 클럭 주파수를 물리적으로 바꾸는 하드웨어 부품이 아니다.', '디바이스 드라이버는 RTOS가 없는 베어메탈(bare-metal) 환경에서도 존재할 수 있는 개념이다.']}
/>

<Quiz
  questionNumber={18}
  question="임베디드 C에서 volatile 키워드를 사용해야 하는 대표적인 경우로 옳은 것은?"
  options={['컴파일러가 최적화 과정에서 값이 바뀌지 않는다고 착각해서는 안 되는, 하드웨어 레지스터나 ISR에서 변경되는 변수에 사용한다', '지역 변수를 스택이 아니라 힙에 할당하고 싶을 때 사용한다', '함수의 반환 값을 항상 부동소수점으로 강제하고 싶을 때 사용한다', '변수의 이름을 프로그램 전역에서 통일하고 싶을 때 사용한다']}
  correctAnswer={1}
  explanation="컴파일러는 최적화를 위해 변수 값이 코드 흐름 안에서 바뀌지 않으면 레지스터에 캐시해 두고 메모리를 다시 읽지 않을 수 있다. 그런데 하드웨어 레지스터나 ISR에서 비동기적으로 값이 바뀌는 변수는 이런 최적화가 적용되면 실제 최신 값을 읽지 못하게 되므로, volatile을 붙여 매번 실제 메모리(레지스터)에서 다시 읽도록 강제해야 한다."
  optionExplanations={['정답이다. 컴파일러 최적화로 인해 오래된 값을 캐시해서는 안 되는 하드웨어 레지스터·ISR 변경 변수에 volatile을 사용한다.', 'volatile은 메모리 할당 위치(스택·힙)를 지정하는 키워드가 아니다.', 'volatile은 자료형이나 반환 값의 타입을 강제하는 키워드가 아니다.', 'volatile은 변수의 이름 규칙과 무관한 키워드다.']}
/>

<Quiz
  questionNumber={19}
  question="32비트 레지스터에서 3번 비트만 1로 설정(다른 비트는 그대로 유지)하는 C 비트 조작 표현으로 옳은 것은?"
  options={['reg = reg 그리고(AND) (1을 3만큼 왼쪽으로 시프트한 값)', 'reg = reg 또는(OR) (1을 3만큼 왼쪽으로 시프트한 값)', 'reg = 1을 3만큼 왼쪽으로 시프트한 값', 'reg = reg 배타적 논리합(XOR) (1을 3만큼 왼쪽으로 시프트한 값)']}
  correctAnswer={2}
  explanation="특정 비트만 1로 설정하면서 나머지 비트는 그대로 유지하려면, 그 비트 위치만 1이고 나머지는 0인 마스크값을 만들어 원래 값과 OR 연산을 한다. 3번 비트를 1로 만드는 마스크는 1을 3만큼 왼쪽으로 시프트한 값이며, OR 연산은 이미 1인 비트는 그대로 1로 두고 목표 비트만 1로 바꾸면서 나머지 비트는 원래 값을 보존한다."
  optionExplanations={['AND 연산은 특정 비트를 0으로 지울 때 사용하는 방식이며, 지우려는 비트를 제외한 나머지가 모두 1인 마스크가 필요해 이 식과는 다르다.', '정답이다. OR 연산으로 목표 비트만 1로 만들고 나머지 비트는 원래 값 그대로 보존한다.', '원래 레지스터 값을 전혀 참조하지 않고 새 값으로 덮어써 다른 비트들이 모두 사라진다.', 'XOR은 해당 비트의 값을 반전(토글)시키는 연산으로, 원래 값이 이미 1이었다면 0으로 바뀌어버려 항상 1로 설정하는 목적과 맞지 않는다.']}
/>

<Quiz
  questionNumber={20}
  question="ISR(Interrupt Service Routine)을 작성할 때 지켜야 할 원칙으로 가장 옳은 것은?"
  options={['ISR 안에서 가능한 한 많은 연산을 처리해 메인 루프의 부담을 없앤다', 'ISR은 가능한 한 짧게 작성하고, 시간이 걸리는 처리는 플래그나 큐를 통해 메인 루프나 다른 태스크로 넘긴다', 'ISR 안에서는 항상 긴 지연(delay) 함수를 호출해 안정성을 높인다', 'ISR은 우선순위와 무관하게 항상 동일한 시간이 걸리도록 설계해야 한다']}
  correctAnswer={2}
  explanation="ISR은 다른 인터럽트의 처리를 지연시킬 수 있으므로 최대한 짧게 작성해야 한다. 시간이 걸리는 실제 처리는 ISR 안에서 플래그를 세우거나 큐에 데이터를 넣어 메인 루프나 별도의 태스크가 이어받아 처리하도록 위임하는 것이 원칙이다."
  optionExplanations={['ISR 안에서 많은 연산을 처리하면 다른 인터럽트의 응답이 지연되어 실시간성이 나빠진다.', '정답이다. ISR은 짧게 유지하고 무거운 처리는 메인 루프나 태스크로 넘기는 것이 원칙이다.', 'ISR 안에서 긴 지연 함수를 호출하면 그 시간 동안 다른 인터럽트가 지연되거나 무시될 수 있어 위험하다.', 'ISR의 실행 시간은 처리 내용에 따라 달라질 수 있으며, 항상 동일한 시간이 걸리도록 강제하는 것이 원칙은 아니다.']}
/>

<Quiz
  questionNumber={21}
  question="펌웨어(firmware) 구조에 대한 설명으로 옳지 않은 것은?"
  options={['펌웨어는 하드웨어에 밀접하게 결합되어 특정 장치의 동작을 제어하는 소프트웨어를 뜻한다', '펌웨어는 부트로더 영역과 애플리케이션 영역으로 나뉘어 관리되는 경우가 많다', '펌웨어 업데이트 기능이 있는 시스템은 부트로더가 새 이미지를 검증한 뒤 적용하는 절차를 거치는 경우가 많다', '펌웨어는 반드시 RTOS 위에서만 동작할 수 있으며 베어메탈 환경에서는 존재할 수 없다']}
  correctAnswer={4}
  explanation="펌웨어는 RTOS의 유무와 무관하게 존재할 수 있는 개념으로, RTOS 없이 무한 루프 하나로 동작하는 베어메탈(bare-metal) 방식의 펌웨어도 널리 쓰인다."
  optionExplanations={['펌웨어가 하드웨어에 밀접하게 결합된 소프트웨어라는 설명은 옳다.', '부트로더 영역과 애플리케이션 영역으로 나뉘는 구조는 실제로 흔히 쓰인다.', '부트로더가 새 이미지를 검증한 뒤 적용하는 절차는 안전한 펌웨어 업데이트의 일반적인 방식이다.', '옳지 않은 설명이라 정답이다. 펌웨어는 RTOS 없이 베어메탈 환경에서도 얼마든지 존재할 수 있다.']}
/>

<Quiz
  questionNumber={22}
  question="타이머, PWM, ADC, DMA 각각의 역할을 짝지은 것 중 옳지 않은 것은?"
  options={['타이머는 정해진 시간 간격을 세거나 이벤트를 주기적으로 발생시키는 데 쓰인다', 'PWM은 디지털 신호의 듀티 사이클을 조절해 평균 전압이나 모터 속도 등을 아날로그처럼 제어하는 데 쓰인다', 'ADC는 아날로그 입력값을 일정 해상도의 디지털 코드로 변환하는 데 쓰인다', 'DMA는 아날로그 신호의 노이즈를 하드웨어적으로 필터링하는 데 쓰인다']}
  correctAnswer={4}
  explanation="DMA는 CPU 개입 없이 메모리와 주변장치 사이의 데이터 전송을 대신 수행하는 기능이며, 아날로그 신호의 노이즈를 필터링하는 기능은 없다. 노이즈 필터링은 아날로그 필터 회로나 디지털 필터링 알고리즘의 역할이다."
  optionExplanations={['타이머가 시간 간격을 세거나 주기적 이벤트를 발생시킨다는 설명은 옳다.', 'PWM이 듀티 사이클로 평균 전압을 조절한다는 설명은 옳다.', 'ADC가 아날로그 입력을 디지털 코드로 변환한다는 설명은 옳다.', '옳지 않은 설명이라 정답이다. DMA는 데이터 전송을 대신하는 기능이며 노이즈 필터링과는 무관하다.']}
/>

<Quiz
  questionNumber={23}
  question="(14편 RTOS 스케줄링 미리보기) 두 개의 주기적 태스크가 있다. 태스크1은 실행 시간 1ms, 주기 4ms이고 태스크2는 실행 시간 2ms, 주기 6ms이다. RM(Rate Monotonic, 최소 주기 우선) 스케줄링의 이용률 상한 검사(Liu & Layland 조건)를 적용했을 때 옳은 결론은?"
  options={['이용률이 상한을 초과해 스케줄 가능성을 보장할 수 없다', '이용률이 상한 이내이므로 RM으로 스케줄 가능함이 보장된다', '이 조건으로는 두 태스크의 스케줄 가능성을 전혀 판단할 수 없다', '실행 시간과 주기의 단위가 달라 계산이 불가능하다']}
  correctAnswer={2}
  explanation="이용률은 각 태스크의 실행 시간을 주기로 나눈 값의 합이다. 태스크1은 1/4=0.25, 태스크2는 2/6≈0.333이므로 합은 약 0.583이다. 태스크 수 n=2일 때 Liu와 Layland의 이용률 상한은 n(2^(1/n)-1)=2×(2^0.5-1)≈2×0.414≈0.828이다. 0.583은 0.828보다 작으므로 이 조건을 만족해 RM으로 스케줄 가능함이 보장된다."
  optionExplanations={['계산된 이용률(약 0.583)은 상한(약 0.828)보다 작으므로 초과했다는 결론은 틀렸다.', '정답이다. 이용률 약 0.583이 상한 약 0.828 이내에 들어오므로 스케줄 가능성이 보장된다.', 'Liu-Layland 이용률 상한 검사는 이용률이 상한 이내이면 스케줄 가능함을 보장하는 판단 기준을 제공하므로, 판단이 불가능하다는 설명은 틀렸다.', '실행 시간과 주기가 모두 밀리초 단위로 통일되어 있으므로 계산에는 문제가 없다.']}
/>

## 참고 자료

- [Arm Cortex-M resources](https://developer.arm.com/community/arm-community-blogs/b/architectures-and-processors-blog/posts/cortex-m-resources)
- [FreeRTOS documentation](https://www.freertos.org/Documentation/00-Overview)
- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
