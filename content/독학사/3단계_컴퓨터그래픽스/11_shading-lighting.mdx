---
title: '11. 조명 모델과 반사 계산'
description: 'Phong 조명 모델의 주변광·확산광·반사광 항을 실제 벡터와 숫자로 대입해 계산하고, 법선·광원·시선·반사 벡터가 밝기 계산에서 하는 역할을 이해한다'
---

<Callout type="info">
  **이번 문서의 목표:** Phong 조명 모델의 주변광(ambient)·확산광(diffuse)·반사광(specular) 세 항을 실제 벡터와 숫자로 대입해 한 점의 밝기를 계산할 수 있고, 법선·광원·시선·반사 벡터 각각이 조명 계산에서 어떤 역할을 하는지 설명할 수 있게 된다.
</Callout>

## 왜 밝기를 수식으로 계산해야 하는가

09·10편에서 곡면을 만들고 어떤 면이 보이는지 걸러냈다면, 이제 남은 문제는 "그 면이 화면에서 얼마나 밝게 보여야 하는가"입니다. 같은 흰색 공이라도 빛을 정면으로 받는 부분은 하얗게 반짝이고, 빛을 비스듬히 받는 부분은 어둡게 보이며, 광원과 반대쪽은 그림자처럼 거의 검게 보입니다. 이 명암 차이를 계산 없이 화가처럼 손으로 칠할 수는 없으므로, 컴퓨터그래픽스는 광원·표면·시점의 기하학적 관계를 수식으로 정리한 **조명 모델**(Lighting Model, Illumination Model)을 사용합니다.

가장 널리 쓰이는 것이 1975년 부이 뜨엉 퐁(Bui Tuong Phong)이 제안한 **Phong 조명 모델**입니다. 이 모델은 실제 물리 법칙을 정밀하게 시뮬레이션하는 대신, 눈에 그럴듯해 보이는 결과를 적은 계산으로 얻도록 설계된 **경험적(empirical) 모델**입니다.

> **쉽게 말하면:** Phong 조명 모델은 한 점의 밝기를 "빛이 없어도 은은하게 보이는 몫" + "빛을 정면으로 받을수록 밝아지는 몫" + "눈부신 하이라이트가 생기는 몫" 세 가지를 더해서 계산하는 공식입니다.

## 1. Phong 모델의 세 가지 항

Phong 모델은 한 점에서의 최종 밝기 $I$를 세 항의 합으로 정의합니다.

$$
I = I_a k_a + I_l k_d (\vec{N} \cdot \vec{L}) + I_l k_s (\vec{R} \cdot \vec{V})^n
$$

- $I$: 계산하려는 점의 최종 밝기(intensity)
- $I_a$: 장면 전체에 고르게 퍼져 있는 주변광(ambient light)의 세기
- $k_a$: 표면이 주변광을 얼마나 반사하는지 나타내는 주변광 반사 계수(0에서 1 사이)
- $I_l$: 광원(light source) 자체의 세기
- $k_d$: 표면이 확산광을 얼마나 반사하는지 나타내는 확산 반사 계수(0에서 1 사이)
- $\vec{N}$: 표면의 법선 벡터(단위 벡터, 크기 1로 정규화됨)
- $\vec{L}$: 표면의 점에서 광원을 향하는 벡터(단위 벡터)
- $k_s$: 표면이 반사광(하이라이트)을 얼마나 반사하는지 나타내는 반사 계수(0에서 1 사이)
- $\vec{R}$: 광원에서 온 빛이 표면에서 정반사(mirror reflection)되어 나가는 방향 벡터(단위 벡터)
- $\vec{V}$: 표면의 점에서 시점(카메라)을 향하는 벡터(단위 벡터)
- $n$: 광택 지수(shininess exponent). 값이 클수록 하이라이트가 작고 날카로워진다

### 주변광(Ambient) 항 — 빛이 안 닿아도 어둡지 않게

> **쉽게 말하면:** 방 안의 형광등을 하나만 켜도 그 빛이 벽에 반사되어 방 전체가 은은하게 밝은 것처럼, 광원이 직접 닿지 않는 면도 완전히 검게 보이지 않도록 더해 주는 항입니다.

실제 장면에서는 빛이 벽이나 다른 물체에 여러 번 반사되어 광원이 직접 비추지 않는 곳도 어느 정도 밝습니다. 이런 간접광을 물리적으로 정밀하게 계산하려면 매우 비싼 전역 조명(global illumination) 계산이 필요하므로, Phong 모델은 이를 장면 전체에 걸린 상수 $I_a k_a$로 단순화해서 대신합니다.

### 확산광(Diffuse) 항 — 빛의 각도가 밝기를 정한다

> **쉽게 말하면:** 손전등을 표면에 똑바로 비추면 밝고, 표면을 스치듯 비스듬히 비추면 어두워지는 것처럼, 빛과 표면이 이루는 각도로 밝기를 정하는 항입니다.

확산광 항은 **람베르트 코사인 법칙**(Lambert's Cosine Law)에 따라 $\vec{N} \cdot \vec{L}$, 즉 법선 벡터와 광원 벡터의 내적으로 계산됩니다. 두 벡터가 모두 단위 벡터일 때 내적은 두 벡터 사이 각도 $\theta$에 대해 $\cos\theta$와 같으므로, 빛이 표면에 수직으로($\theta = 0$) 닿으면 $\cos 0 = 1$로 가장 밝고, 표면과 나란하게($\theta = 90°$) 스치면 $\cos 90° = 0$으로 전혀 밝아지지 않습니다. 이 항은 **거친 무광 표면**(예: 종이, 석고)처럼 빛을 모든 방향으로 고르게 흩어 보내는 반사를 표현하며, 시점 $\vec{V}$의 위치와는 무관하다는 특징이 있습니다.

### 반사광(Specular) 항 — 하이라이트가 생기는 이유

> **쉽게 말하면:** 매끈한 사과 표면에 생기는 반짝이는 하얀 점처럼, 빛이 거울처럼 튕겨 나가는 방향과 눈이 있는 방향이 거의 일치할 때만 눈부시게 밝아지는 항입니다.

반사광 항은 정반사 방향 벡터 $\vec{R}$과 시선 벡터 $\vec{V}$의 내적 $(\vec{R} \cdot \vec{V})$을 광택 지수 $n$만큼 거듭제곱합니다. $\vec{R}$과 $\vec{V}$가 정확히 일치하면($\theta = 0$) 값이 1이 되어 가장 밝고, 조금만 벗어나도 $n$이 클수록 값이 급격히 줄어듭니다. 이 때문에 $n$이 크면 아주 좁고 날카로운 하이라이트(플라스틱·금속처럼 매끈한 표면)가 생기고, $n$이 작으면 넓고 흐릿한 하이라이트(고무처럼 덜 매끈한 표면)가 생깁니다. 확산광과 달리 반사광 항은 **시점 $\vec{V}$의 위치에 따라 값이 달라져**, 같은 물체라도 보는 각도를 바꾸면 하이라이트의 위치가 함께 움직입니다.

## 2. 실제 계산 — Phong 식에 숫자를 대입하기

표면 위의 한 점에서 법선 벡터가 $\vec{N} = (0, 0, 1)$(정면을 바로 향함)이고, 광원이 그 점에서 볼 때 $(1, 1, 1)$ 방향에 있다고 하겠습니다. 먼저 광원 벡터를 단위 벡터로 정규화합니다.

$$
|\vec{L}_{raw}| = \sqrt{1^2 + 1^2 + 1^2} = \sqrt{3} \approx 1.732
$$

$$
\vec{L} = \left(\frac{1}{1.732}, \frac{1}{1.732}, \frac{1}{1.732}\right) \approx (0.577,\ 0.577,\ 0.577)
$$

### 확산광 항 계산

법선과 광원 벡터의 내적을 구합니다.

$$
\vec{N} \cdot \vec{L} = (0 \times 0.577) + (0 \times 0.577) + (1 \times 0.577) = 0.577
$$

주변광 세기 $I_a = 1$, 주변광 반사 계수 $k_a = 0.2$, 광원 세기 $I_l = 1$, 확산 반사 계수 $k_d = 0.6$이라 하고 확산광 항을 계산합니다.

$$
I_l k_d (\vec{N} \cdot \vec{L}) = 1 \times 0.6 \times 0.577 = 0.346
$$

### 반사광 항 계산 — 반사 벡터 구하기

반사 벡터 $\vec{R}$은 다음 공식으로 구합니다.

$$
\vec{R} = 2(\vec{N} \cdot \vec{L})\vec{N} - \vec{L}
$$

- $2(\vec{N} \cdot \vec{L})\vec{N}$: 법선 방향으로 $\vec{L}$을 거울처럼 뒤집기 위한 보정 항
- 이 식은 입사각과 반사각이 법선을 기준으로 같다는 **반사 법칙**을 벡터로 표현한 것이다.

$2(\vec{N} \cdot \vec{L}) = 2 \times 0.577 = 1.154$이므로,

$$
\vec{R} = 1.154 \times (0, 0, 1) - (0.577, 0.577, 0.577) = (-0.577,\ -0.577,\ 0.577)
$$

시점이 표면 바로 정면($\vec{V} = (0, 0, 1)$)에 있다고 하면, 반사 벡터와 시선 벡터의 내적은 다음과 같습니다.

$$
\vec{R} \cdot \vec{V} = (-0.577 \times 0) + (-0.577 \times 0) + (0.577 \times 1) = 0.577
$$

광택 지수 $n = 4$, 반사 계수 $k_s = 0.5$라 하고 이 값을 거듭제곱합니다.

$$
(\vec{R} \cdot \vec{V})^4 = 0.577^4 \approx 0.111
$$

반사광 항을 계산합니다.

$$
I_l k_s (\vec{R} \cdot \vec{V})^n = 1 \times 0.5 \times 0.111 = 0.056
$$

### 세 항을 더해 최종 밝기 구하기

주변광 항을 계산합니다.

$$
I_a k_a = 1 \times 0.2 = 0.2
$$

세 항을 모두 더합니다.

$$
I = 0.2 + 0.346 + 0.056 = 0.602
$$

- 결과: 이 점의 최종 밝기는 약 $0.602$이다(밝기 값이 0에서 1 사이로 정규화되어 있다고 가정할 때, 중간보다 조금 밝은 수준). 주변광 $0.2$가 바탕을 깔고, 법선과 광원이 이루는 각도(약 $54.7°$, $\cos^{-1}(0.577)$)만큼 확산광 $0.346$이 더해지며, 시선이 반사 방향과 정확히 겹치지 않아 반사광은 $0.056$으로 비교적 작게 기여했다.

**자주 틀리는 점:** $\vec{N} \cdot \vec{L}$이 음수로 나오는 경우를 그대로 계산에 넣는 실수가 흔합니다. 내적이 음수라는 것은 광원이 표면의 뒤쪽에 있어 그 표면에 빛이 전혀 닿지 않는다는 뜻이므로, 확산광과 반사광 항은 $0$으로 처리해야 합니다(즉 $\max(0, \vec{N} \cdot \vec{L})$을 사용). 이를 무시하고 음수를 그대로 더하면 밝기가 오히려 줄어드는 잘못된 결과가 나옵니다.

## 3. 조명 계산의 흐름

```mermaid
flowchart TD
  A[표면 점의 법선 N, 광원 방향 L, 시선 방향 V] --> B[N·L 계산]
  B --> C{N·L 이 0보다 큰가?}
  C -->|아니오, 뒤쪽| D[확산광·반사광 = 0]
  C -->|예| E[확산광 항 계산: Il·kd·N·L]
  A --> F[반사 벡터 R = 2(N·L)N - L 계산]
  F --> G[R·V 계산 후 n제곱]
  G --> H[반사광 항 계산: Il·ks·(R·V)^n]
  I[주변광 항: Ia·ka] --> J[최종 밝기 I = 주변광+확산광+반사광]
  E --> J
  H --> J
  D --> J
```

> **비유:** 캄캄한 방에서 손전등 하나로 매끈한 도자기 그릇을 비춰 보는 상황을 떠올려 보세요. 방 안이 완전히 캄캄하지 않은 것은 벽에 반사된 은은한 빛(주변광) 덕분이고, 손전등을 그릇 표면에 똑바로 비출수록 그 부분이 밝아지는 것은 확산광 때문이며, 특정 각도에서만 눈부시게 반짝이는 점이 보이는 것은 반사광(하이라이트) 때문입니다.

## 핵심 정리

- Phong 조명 모델은 밝기 $I$를 주변광($I_a k_a$), 확산광($I_l k_d (\vec{N} \cdot \vec{L})$), 반사광($I_l k_s (\vec{R} \cdot \vec{V})^n$) 세 항의 합으로 계산한다.
- 확산광 항은 법선과 광원 벡터의 내적(코사인 값)으로 계산되며 시점과 무관하고, 반사광 항은 반사 벡터와 시선 벡터의 내적을 광택 지수만큼 거듭제곱해 계산되며 시점에 따라 값이 달라진다.
- 반사 벡터는 $\vec{R} = 2(\vec{N} \cdot \vec{L})\vec{N} - \vec{L}$로 구하며, 이는 입사각과 반사각이 같다는 반사 법칙을 벡터로 표현한 것이다.
- 광택 지수 $n$이 클수록 하이라이트가 좁고 날카로워지고, 작을수록 넓고 흐릿해진다.
- $\vec{N} \cdot \vec{L}$이 음수이면 광원이 표면 뒤에 있다는 뜻이므로 확산광·반사광 항을 0으로 처리해야 한다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="Phong 조명 모델에서 최종 밝기를 이루는 세 항으로 가장 적절한 것은?"
  options={['주변광, 확산광, 반사광', '주변광, 굴절광, 그림자', '확산광, 반사광, 텍스처', '반사광, 굴절광, 산란광']}
  correctAnswer={1}
  explanation="Phong 조명 모델은 밝기를 주변광(ambient), 확산광(diffuse), 반사광(specular) 세 항의 합으로 정의한다."
  optionExplanations={['본문의 정의식 I = 주변광+확산광+반사광과 정확히 일치한다.', '굴절광과 그림자는 Phong 조명 모델의 기본 항에 포함되지 않는다.', '텍스처는 표면의 색상 패턴을 다루는 별개의 개념으로 조명 항이 아니다.', '굴절광과 산란광은 Phong 모델이 다루는 항이 아니다.']}
/>

<Quiz
  questionNumber={2}
  question="확산광 항이 N·L(법선과 광원 벡터의 내적)로 계산되는 이유로 가장 적절한 것은?"
  options={['빛과 표면이 이루는 각도의 코사인 값이 클수록(수직에 가까울수록) 표면이 더 밝게 보이기 때문이다', '시점의 위치가 바뀌면 확산광의 세기도 함께 바뀌어야 하기 때문이다', '법선 벡터의 길이가 길수록 표면이 더 매끈하기 때문이다', '내적을 사용하면 계산 속도만 빨라질 뿐 물리적인 의미는 없기 때문이다']}
  correctAnswer={1}
  explanation="단위 벡터의 내적은 두 벡터 사이 각도의 코사인과 같으며, 람베르트 코사인 법칙에 따라 빛이 표면에 수직으로 닿을수록 밝고 비스듬할수록 어두워지는 것을 N·L이 정확히 표현한다."
  optionExplanations={['본문의 람베르트 코사인 법칙 설명과 정확히 일치한다.', '확산광 항은 시점 V와 무관하게 계산되는 것이 특징이다.', '법선 벡터는 단위 벡터로 정규화되어 항상 길이가 1이므로 표면의 매끈함과는 관련이 없다.', '내적은 실제로 각도에 대한 물리적 의미(코사인 법칙)를 가진다.']}
/>

<Quiz
  questionNumber={3}
  question="법선 벡터 N=(0,0,1)과 광원 벡터 L=(0.577, 0.577, 0.577)의 내적 N·L로 가장 적절한 것은?"
  options={['0.577', '1.0', '0', '1.732']}
  correctAnswer={1}
  explanation="내적은 대응하는 성분끼리 곱해 더하는 것으로, 0x0.577+0x0.577+1x0.577=0.577이다."
  optionExplanations={['z성분끼리의 곱만 남는 계산 결과와 정확히 일치한다.', '1.0은 두 벡터가 완전히 같은 방향일 때 나오는 값으로 이 경우와 다르다.', '0은 두 벡터가 수직일 때 나오는 값으로 이 경우와 다르다.', '1.732는 정규화 전 벡터의 크기(제곱근 3)이며 내적 결과가 아니다.']}
/>

<Quiz
  questionNumber={4}
  question="본문의 예시에서 Ia=1, ka=0.2, Il=1, kd=0.6, N·L=0.577일 때 확산광 항의 값으로 가장 적절한 것은?"
  options={['0.346', '0.2', '0.6', '0.577']}
  correctAnswer={1}
  explanation="확산광 항은 Il x kd x (N·L)이므로 1 x 0.6 x 0.577 = 0.346이다."
  optionExplanations={['공식에 숫자를 대입한 계산 결과와 정확히 일치한다.', '0.2는 주변광 항(Ia x ka)의 값으로 확산광 항이 아니다.', '0.6은 확산 반사 계수 kd 자체이며 곱셈을 하지 않은 값이다.', '0.577은 N·L 값 자체이며 kd를 곱하지 않은 값이다.']}
/>

<Quiz
  questionNumber={5}
  question="반사 벡터 R을 구하는 공식으로 가장 적절한 것은?"
  options={['R = 2(N·L)N - L', 'R = N + L', 'R = N x L (외적)', 'R = L / N (성분별 나눗셈)']}
  correctAnswer={1}
  explanation="반사 벡터는 입사각과 반사각이 법선을 기준으로 같다는 반사 법칙을 표현한 R = 2(N·L)N - L 공식으로 구한다."
  optionExplanations={['본문의 반사 벡터 정의식과 정확히 일치한다.', '단순한 벡터 덧셈은 반사 법칙을 반영하지 않는다.', '외적은 두 벡터에 수직인 벡터를 구하는 연산으로 반사 방향과는 다른 결과를 준다.', '벡터의 성분별 나눗셈은 정의되지 않으며 반사 벡터 계산에 쓰이지 않는다.']}
/>

<Quiz
  questionNumber={6}
  question="광택 지수 n이 커질수록 하이라이트(반사광이 강하게 보이는 영역)에 나타나는 변화로 가장 적절한 것은?"
  options={['하이라이트가 더 좁고 날카로워진다', '하이라이트가 더 넓고 흐릿해진다', '하이라이트의 색이 광원의 색과 반대로 바뀐다', '확산광 항의 값이 함께 커진다']}
  correctAnswer={1}
  explanation="(R·V)^n에서 n이 커질수록 R·V가 1에서 조금만 벗어나도 값이 급격히 작아지므로, 정반사 방향과 거의 일치하는 아주 좁은 영역만 밝게 남아 하이라이트가 좁고 날카로워진다."
  optionExplanations={['본문에서 n이 클수록 좁고 날카로운 하이라이트가 생긴다는 설명과 일치한다.', '이는 n이 작을 때(무광에 가까운 표면) 나타나는 특징이다.', '색상 반전은 Phong 모델의 광택 지수와 관련이 없다.', '확산광 항은 N·L로만 계산되며 광택 지수 n과는 무관하다.']}
/>

## 참고 자료

- [Edinburgh University - Computer Graphics / Visible Surface](https://www.inf.ed.ac.uk/teaching/courses/cg/lectures/cg9_2012.pdf)
- [MIT OCW - Graphics Pipeline and Rasterization](https://ocw.mit.edu/courses/6-837-computer-graphics-fall-2012/53d96abf747a3c82fd3497d2fea540f5_MIT6_837F12_Lec21.pdf)
