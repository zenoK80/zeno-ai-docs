---
title: '25. 독학사 컴퓨터네트워크 예상기출문제 2'
description: 'TCP와 UDP의 구조, 흐름·혼잡 제어 계산, 라우팅 알고리즘, 응용 계층 프로토콜을 다루는 예상 기출 문제 세트'
---

<Callout type="warning">
  이 문제들은 실제 독학사 기출 문제가 아닙니다. 국가평생교육진흥원 독학학위제 컴퓨터네트워크 출제기준(평가영역)을 바탕으로 재구성한 예상 문제이며, 실제 시험에 나온 문항을 그대로 옮기지 않았습니다. 최신 출제 범위와 문항 수는 반드시 국가평생교육진흥원 공고로 다시 확인하세요.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="UDP(User Datagram Protocol)의 특성에 대한 설명으로 옳은 것은?"
  options={['비연결형이며 순서 보장과 오류 복구 기능이 없어 헤더 오버헤드가 작다', '연결형이며 흐름 제어와 혼잡 제어를 수행한다', '3-way handshake로 연결을 설정한 뒤 데이터를 전송한다', '수신 확인(ACK)과 재전송으로 데이터 전달의 신뢰성을 보장한다']}
  correctAnswer={1}
  explanation="UDP는 비연결형(connectionless) 프로토콜로 연결 설정 절차가 없고, 순서 보장이나 재전송 같은 신뢰성 메커니즘도 두지 않아 헤더가 8바이트로 단순하다. 이 단순함 덕분에 오버헤드가 작고 처리 속도가 빠르다."
  optionExplanations={['정답이다. 비연결형과 낮은 오버헤드는 UDP의 핵심 특성이다.', '연결지향성과 흐름·혼잡 제어는 TCP의 특성이다. TCP와 UDP의 특성을 뒤바꾼 오답이다.', '3-way handshake는 TCP의 연결 설정 절차다. UDP는 핸드셰이크 없이 바로 데이터를 보낸다.', '확인 응답과 재전송에 의한 신뢰성 보장은 TCP의 특성이다. UDP는 이러한 신뢰성 메커니즘이 없다.']}
/>

<Quiz
  questionNumber={2}
  question="TCP(Transmission Control Protocol)의 특성에 대한 설명 중 옳지 않은 것은?"
  options={['TCP는 연결지향형 프로토콜로 데이터 전송 전에 3-way handshake를 수행한다', 'TCP는 순서 번호와 확인 응답을 이용해 데이터의 순서를 보장하고 손실을 복구한다', 'TCP는 헤더에 부가 정보가 없어 UDP보다 항상 전송 속도가 빠르다', 'TCP는 흐름 제어와 혼잡 제어 기능으로 네트워크와 수신측 상태에 따라 전송 속도를 조절한다']}
  correctAnswer={3}
  explanation="TCP는 순서 번호, 확인 응답 번호, 윈도우 크기 등을 담기 위해 UDP보다 헤더가 크고(최소 20바이트) 흐름·혼잡 제어 처리 부담도 있다. 신뢰성을 위한 오버헤드 때문에 단순 전송 속도만 보면 UDP보다 느릴 수 있어, 항상 더 빠르다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. TCP는 연결지향형이며 3-way handshake로 연결을 맺는다.', '옳은 설명이다. 순서 번호와 확인 응답은 TCP 신뢰성 보장의 핵심 메커니즘이다.', '옳지 않은 설명이라 정답이다. TCP는 신뢰성 보장을 위한 오버헤드가 있어 항상 더 빠르다고 말할 수 없다. TCP와 UDP의 특성을 뒤바꿔 생각한 오답이다.', '옳은 설명이다. 흐름 제어와 혼잡 제어는 TCP의 핵심 기능이다.']}
/>

<Quiz
  questionNumber={3}
  question="전송 계층에서 포트 번호(port number)가 담당하는 역할로 가장 옳은 것은?"
  options={['한 호스트 안에서 여러 응용 프로세스를 구분해 데이터를 다중화·역다중화한다', '네트워크 간 최적 경로를 선택해 패킷을 전달한다', '같은 링크 안의 장치를 물리적으로 식별한다', '전송 중 발생한 데이터 오류를 검출한다']}
  correctAnswer={1}
  explanation="포트 번호는 같은 IP 주소를 가진 한 호스트 안에서 여러 응용 프로세스(예: 웹 브라우저, 메일 클라이언트)를 구분해 세그먼트를 올바른 프로세스로 전달하는 다중화·역다중화 기준이 된다."
  optionExplanations={['정답이다. 포트 번호는 프로세스 단위의 다중화·역다중화를 담당한다.', '경로 선택은 네트워크 계층의 라우팅 기능이다. 계층을 혼동한 오답이다.', '물리적 장치 식별은 데이터링크 계층의 MAC 주소 기능이다. 계층을 혼동한 오답이다.', '오류 검출은 체크섬 필드의 기능이며 포트 번호와는 무관하다.']}
/>

<Quiz
  questionNumber={4}
  question="TCP 연결을 설정하는 3-way handshake의 순서로 옳은 것은?"
  options={['SYN → SYN+ACK → ACK', 'SYN → ACK → SYN+ACK', 'ACK → SYN → SYN+ACK', 'SYN+ACK → SYN → ACK']}
  correctAnswer={1}
  explanation="클라이언트가 SYN을 보내 연결을 요청하면, 서버는 SYN+ACK로 요청을 수락하며 자신도 연결을 요청하고, 클라이언트가 마지막으로 ACK를 보내 연결이 확립된다."
  optionExplanations={['정답이다. SYN, SYN+ACK, ACK 순서가 3-way handshake의 정확한 절차다.', '두 번째와 세 번째 메시지의 순서가 뒤바뀌어 있다. 서버의 응답 없이 클라이언트가 먼저 ACK를 보내는 것은 잘못된 순서다.', '연결 요청도 하기 전에 ACK를 먼저 보내는 것으로 되어 있어 순서가 완전히 어긋난 오답이다.', '서버가 클라이언트의 요청보다 먼저 응답하는 것으로 되어 있어 순서가 거꾸로 된 오답이다.']}
/>

<Quiz
  questionNumber={5}
  question="TCP 연결을 종료하는 4-way handshake의 순서로 옳은 것은?"
  options={['FIN → ACK → FIN → ACK', 'ACK → FIN → ACK → FIN', 'FIN → FIN → ACK → ACK', 'SYN → FIN → ACK → ACK']}
  correctAnswer={1}
  explanation="연결 종료를 원하는 쪽이 FIN을 보내면 상대는 ACK로 확인하고, 상대도 자신의 전송이 끝나면 FIN을 보내며, 처음 종료를 요청한 쪽이 마지막 ACK를 보내 연결이 완전히 끊어진다."
  optionExplanations={['정답이다. FIN, ACK, FIN, ACK 순서가 4-way handshake의 정확한 절차다.', '첫 메시지로 ACK가 먼저 오는 것으로 되어 있어 종료 요청 없이 응답부터 하는 잘못된 순서다.', '두 FIN이 연속으로 나온 뒤 ACK가 오는 것으로 되어 있어 각 FIN에 대한 개별 확인 절차가 빠진 오답이다.', '연결 설정에 쓰이는 SYN이 종료 절차에 잘못 포함되어 있는 오답이다.']}
/>

<Quiz
  questionNumber={6}
  question="TCP 세그먼트의 순서 번호(sequence number)가 1000이고 페이로드 데이터가 500바이트일 때, 수신측이 다음 세그먼트에 대해 기대하는 확인 응답 번호(ACK number)는?"
  options={['1500', '1000', '1501', '2000']}
  correctAnswer={1}
  explanation="TCP의 ACK 번호는 수신측이 다음에 받기를 기대하는 바이트의 순서 번호다. 이번 세그먼트가 1000번부터 500바이트를 실었으므로 마지막 바이트는 1499번이고, 다음에 받을 바이트는 1500번이다. 따라서 ACK 번호는 1500이다."
  optionExplanations={['정답이다. 시작 순서 번호 1000에 데이터 길이 500을 더해 다음 기대 바이트 번호를 정확히 계산했다.', '데이터 길이 500바이트를 더하지 않고 시작 순서 번호를 그대로 답한 오답이다.', '1000+500 계산에서 1을 더 얹어 1501로 계산한 오답이다.', '더하기 대신 곱하기 개념으로 착각해 2000으로 계산한 오답이다.']}
/>

<Quiz
  questionNumber={7}
  question="TCP의 흐름 제어(flow control)와 혼잡 제어(congestion control)에 대한 설명 중 옳지 않은 것은?"
  options={['흐름 제어는 수신측 버퍼의 여유에 맞춰 송신측의 전송량을 조절한다', '혼잡 제어는 네트워크 경로상의 혼잡 상태에 맞춰 송신측의 전송량을 조절한다', '흐름 제어와 혼잡 제어는 동일한 개념으로 항상 같은 윈도우 값을 사용한다', '실제로 송신 가능한 데이터 양은 수신 윈도우와 혼잡 윈도우 중 더 작은 값으로 제한된다']}
  correctAnswer={3}
  explanation="흐름 제어는 수신 윈도우(rwnd)로 수신측의 버퍼 여유를, 혼잡 제어는 혼잡 윈도우(cwnd)로 네트워크 경로의 혼잡 상태를 반영한다. 두 값은 서로 다른 요인에서 계산되는 별개의 값이며, 실제 송신 가능량은 이 둘 중 더 작은 값으로 제한된다. 따라서 두 개념이 동일하다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 흐름 제어는 수신측 버퍼 상태를 반영한다.', '옳은 설명이다. 혼잡 제어는 네트워크 경로의 혼잡 상태를 반영한다.', '옳지 않은 설명이라 정답이다. 흐름 제어와 혼잡 제어는 서로 다른 목적과 서로 다른 윈도우 값을 사용하는 별개의 메커니즘인데, 이를 같은 개념으로 뒤섞은 오답이다.', '옳은 설명이다. 실제 송신량은 두 윈도우 중 작은 값(min)으로 제한된다.']}
/>

<Quiz
  questionNumber={8}
  question="TCP 슬로우 스타트(slow start)에서 초기 혼잡 윈도우(cwnd)가 1 MSS이고 손실 없이 매 왕복 시간(RTT)마다 혼잡 윈도우가 두 배로 늘어난다고 할 때, 3번째 왕복 시간이 끝난 뒤의 혼잡 윈도우 크기는?"
  options={['8 MSS', '6 MSS', '4 MSS', '3 MSS']}
  correctAnswer={1}
  explanation="슬로우 스타트에서는 확인 응답을 받을 때마다 혼잡 윈도우가 지수적으로 늘어난다. 시작값 1 MSS에서 첫 번째 RTT 후 2 MSS, 두 번째 RTT 후 4 MSS, 세 번째 RTT 후 8 MSS가 된다."
  optionExplanations={['정답이다. 1→2→4→8로 이어지는 지수적 증가를 정확히 계산했다.', '지수 증가가 아니라 왕복마다 2씩 더하는 것으로 착각해 1+2+3=6으로 계산한 오답이다.', '지수 증가를 2번만 적용해 세 번째 왕복을 계산에서 빠뜨린 오답이다.', '왕복 횟수 3을 혼잡 윈도우 값으로 그대로 답한, 계산을 하지 않은 오답이다.']}
/>

<Quiz
  questionNumber={9}
  question="혼잡 윈도우(cwnd)가 16 MSS인 상태에서 재전송 타이머 만료(timeout)로 세그먼트 손실이 감지되었다. 전통적인 TCP Reno 방식에서 새로 설정되는 ssthresh와 cwnd 값으로 옳은 것은?"
  options={['ssthresh=8 MSS, cwnd=1 MSS', 'ssthresh=16 MSS, cwnd=8 MSS', 'ssthresh=8 MSS, cwnd=8 MSS', 'ssthresh=16 MSS, cwnd=1 MSS']}
  correctAnswer={1}
  explanation="타임아웃으로 손실이 감지되면 손실 시점의 cwnd 절반을 새로운 ssthresh로 설정하고(16÷2=8), 혼잡 윈도우는 슬로우 스타트를 처음부터 다시 시작하도록 1 MSS로 초기화한다."
  optionExplanations={['정답이다. ssthresh는 손실 시점 cwnd의 절반(8 MSS), cwnd는 1 MSS로 재설정된다.', 'ssthresh와 cwnd 값이 서로 뒤바뀌어 있다. ssthresh는 절반이 되어야 하는데 원래 값을 그대로 두고 cwnd를 절반으로 계산한 오답이다.', 'cwnd가 1이 아니라 8로 재설정되는 것으로 되어 있어, 타임아웃 이후 슬로우 스타트를 처음부터 다시 시작한다는 점을 놓친 오답이다.', 'ssthresh가 절반으로 줄지 않고 원래 값을 유지하는 것으로 되어 있어, 혼잡 발생 시의 승법 감소(multiplicative decrease)를 적용하지 않은 오답이다.']}
/>

<Quiz
  questionNumber={10}
  question="TCP의 빠른 재전송(fast retransmit)이 일어나는 조건으로 옳은 것은?"
  options={['같은 순서 번호를 요구하는 중복 ACK를 3개 수신했을 때', '수신 윈도우 크기가 0이 되었을 때', '재전송 타이머가 만료되었을 때', '혼잡 윈도우가 최댓값에 도달했을 때']}
  correctAnswer={1}
  explanation="송신측이 동일한 ACK 번호를 요구하는 중복 ACK를 3개(총 4번째 동일 ACK) 수신하면, 타이머 만료를 기다리지 않고 즉시 해당 세그먼트를 재전송하는 빠른 재전송을 수행한다."
  optionExplanations={['정답이다. 3개의 중복 ACK 수신이 빠른 재전송의 표준적인 트리거 조건이다.', '수신 윈도우가 0이 되는 것은 흐름 제어의 지속 타이머(persist timer) 상황과 관련이 있으며 빠른 재전송의 조건이 아니다.', '타이머 만료로 인한 재전송은 일반적인(느린) 재전송이며, 이를 기다리지 않는 것이 빠른 재전송의 취지다. 두 개념을 혼동한 오답이다.', '혼잡 윈도우의 최댓값 도달은 빠른 재전송과 직접적인 관련이 없는 조건이다.']}
/>

<Quiz
  questionNumber={11}
  question="AIMD(Additive Increase Multiplicative Decrease)에 대한 설명으로 옳은 것은?"
  options={['혼잡을 감지하지 못하면 매 왕복 시간마다 혼잡 윈도우를 조금씩 더하고, 혼잡을 감지하면 혼잡 윈도우를 절반으로 줄인다', '혼잡을 감지하지 못하면 혼잡 윈도우를 두 배로 늘리고, 감지하면 1씩 줄인다', '혼잡 윈도우는 항상 일정하게 유지되며 손실이 발생해도 변하지 않는다', '혼잡을 감지하면 혼잡 윈도우를 두 배로 늘려 빠르게 회복한다']}
  correctAnswer={1}
  explanation="AIMD는 혼잡 회피 국면에서 손실이 없으면 매 RTT마다 혼잡 윈도우를 1 MSS씩 더하는 가산 증가(additive increase)를 하고, 혼잡이 감지되면 혼잡 윈도우를 절반으로 줄이는 승법 감소(multiplicative decrease)를 적용하는 원리다."
  optionExplanations={['정답이다. 가산 증가와 승법 감소를 정확히 설명하고 있다.', '두 배로 늘리는 지수적 증가는 슬로우 스타트의 특징이지 혼잡 회피 국면의 가산 증가가 아니다. 두 국면을 혼동한 오답이다.', '혼잡 제어는 손실 여부에 따라 혼잡 윈도우를 계속 조정하는 것이 핵심이다. 이를 부정하는 오답이다.', '혼잡을 감지했을 때는 윈도우를 줄여야 하는데 오히려 늘린다고 서술해 증가와 감소를 반대로 설명한 오답이다.']}
/>

<Quiz
  questionNumber={12}
  question="대역폭 10Mbps인 회선으로 크기 5MB인 파일을 전송할 때 걸리는 전송 시간(transmission time)은? (1MB=8Mb로 환산)"
  options={['4초', '0.5초', '40초', '400초']}
  correctAnswer={1}
  explanation="5MB를 비트로 환산하면 5×8=40Mb다. 전송 시간은 데이터 크기를 대역폭으로 나눈 값이므로 40Mb÷10Mbps=4초가 된다."
  optionExplanations={['정답이다. 바이트를 비트로 환산한 뒤 대역폭으로 정확히 나누었다.', '바이트를 비트로 바꾸는 ×8 과정을 빠뜨리고 5MB를 그대로 5Mb로 취급해 5÷10=0.5초로 계산한 오답이다.', '대역폭 10Mbps를 1Mbps로 잘못 읽어 40Mb÷1Mbps=40초로 계산한 단위 오독 오답이다.', '나눗셈을 해야 할 자리에서 40Mb×10을 곱해버린 연산 혼동 오답이다.']}
/>

<Quiz
  questionNumber={13}
  question="대역폭 1Mbps인 링크로 크기 1,000,000비트(1Mb)인 패킷을 전송한다. 전파 지연(propagation delay)이 20ms일 때, 전송이 시작되어 수신측에 마지막 비트가 도착하기까지 걸리는 총 지연 시간은?"
  options={['1.02초', '1초', '0.02초', '1.2초']}
  correctAnswer={1}
  explanation="전송 시간은 1Mb÷1Mbps=1초이고, 여기에 전파 지연 20ms(0.02초)를 더해야 한다. 총 지연 시간은 1초+0.02초=1.02초다."
  optionExplanations={['정답이다. 전송 시간과 전파 지연을 모두 더해 정확히 계산했다.', '전파 지연 20ms를 더하지 않고 전송 시간만 답한 오답이다.', '전송 시간을 빠뜨리고 전파 지연만 답한 오답이다.', '20ms를 0.02초가 아니라 0.2초로 소수점을 한 자리 잘못 옮겨 계산한 오답이다.']}
/>

<Quiz
  questionNumber={14}
  question="정적 라우팅(static routing)과 동적 라우팅(dynamic routing)에 대한 설명 중 옳지 않은 것은?"
  options={['정적 라우팅은 관리자가 직접 경로를 설정하며 네트워크 변화에 자동으로 대응하지 못한다', '동적 라우팅은 라우팅 프로토콜로 경로 정보를 주고받으며 네트워크 변화에 스스로 대응한다', '정적 라우팅은 대규모이고 자주 변하는 네트워크에서 동적 라우팅보다 관리 부담이 적다', '동적 라우팅은 라우팅 프로토콜 연산 때문에 정적 라우팅보다 라우터의 자원을 더 사용한다']}
  correctAnswer={3}
  explanation="정적 라우팅은 경로마다 관리자가 직접 설정하고 갱신해야 하므로, 네트워크가 크고 변화가 잦을수록 오히려 관리 부담이 커진다. 이런 환경에서는 동적 라우팅이 관리 부담을 줄여주므로, 정적 라우팅이 관리 부담이 적다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 정적 라우팅은 수동 설정이며 변화에 자동으로 대응하지 못한다.', '옳은 설명이다. 동적 라우팅 프로토콜은 경로 정보를 교환해 변화에 스스로 대응한다.', '옳지 않은 설명이라 정답이다. 대규모·잦은 변화 환경에서는 정적 라우팅이 오히려 관리 부담이 크다. 정적과 동적 라우팅의 장단점을 반대로 서술한 오답이다.', '옳은 설명이다. 동적 라우팅은 알고리즘 연산과 정보 교환으로 자원을 더 사용한다.']}
/>

<Quiz
  questionNumber={15}
  question="거리 벡터(distance vector) 방식과 링크 상태(link state) 방식을 비교한 설명 중 옳지 않은 것은?"
  options={['거리 벡터 방식은 이웃 라우터에게만 자신의 라우팅 테이블 전체를 주기적으로 전달한다', '링크 상태 방식은 각 라우터가 네트워크 전체의 링크 정보를 수집해 최단 경로를 스스로 계산한다', '거리 벡터 방식은 링크 상태 방식보다 수렴 속도가 빠르고 라우팅 루프가 발생하지 않는다', '링크 상태 방식의 대표 예로 OSPF가 있고, 거리 벡터 방식의 대표 예로 RIP가 있다']}
  correctAnswer={3}
  explanation="거리 벡터 방식(RIP 등)은 이웃하고만 정보를 교환하기 때문에 네트워크 전체 상태를 반영하는 데 시간이 걸려 수렴이 느리고, 카운트-투-인피니티 같은 라우팅 루프 문제가 발생할 수 있다. 반면 링크 상태 방식(OSPF 등)은 전체 위상을 파악해 계산하므로 수렴이 더 빠르다. 따라서 거리 벡터가 더 빠르고 루프가 없다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 거리 벡터 방식은 이웃에게만 라우팅 테이블 전체를 전달한다.', '옳은 설명이다. 링크 상태 방식은 전체 링크 정보를 바탕으로 각자 최단 경로를 계산한다.', '옳지 않은 설명이라 정답이다. 실제로는 거리 벡터 방식이 수렴이 느리고 라우팅 루프에 더 취약하다. 두 방식의 장단점을 반대로 서술한 오답이다.', '옳은 설명이다. OSPF는 링크 상태, RIP는 거리 벡터의 대표적인 프로토콜이다.']}
/>

<Quiz
  questionNumber={16}
  question="라우터 A, B, C, D로 이루어진 네트워크에서 링크 비용이 A-B=2, A-C=5, B-C=1, B-D=7, C-D=3일 때, 다익스트라(Dijkstra) 알고리즘으로 구한 A에서 D까지의 최단 경로 비용은?"
  options={['6 (A-B-C-D 경로)', '9 (A-B-D 경로)', '8 (A-C-D 경로)', '10 (A-B-C 경로 이후 C-D 대신 B-D 값을 더한 값)']}
  correctAnswer={1}
  explanation="가능한 경로를 모두 비교하면 A-B-D는 2+7=9, A-C-D는 5+3=8, A-B-C-D는 2+1+3=6이다. 세 값 중 가장 작은 6이 A에서 D까지의 최단 경로 비용이며, 경로는 A-B-C-D다."
  optionExplanations={['정답이다. 세 경로 중 A-B-C-D 경로의 합이 6으로 가장 작다.', 'A-B-D 경로의 값이지만, 중간에 C를 거치는 더 짧은 경로(A-B-C-D=6)를 확인하지 않고 첫 번째로 찾은 경로를 최단 경로로 착각한 오답이다.', 'A-C-D 경로의 값이지만, A-B-C-D 경로가 이보다 더 짧다는 것을 놓친 오답이다.', 'C-D 구간 대신 B-D 값을 더하는 계산 실수(2+1+7)로 나온 값으로, 존재하지 않는 경로를 계산한 오답이다.']}
/>

<Quiz
  questionNumber={17}
  question="RIP(Routing Information Protocol)에서 목적지에 도달할 수 없음(infinite)을 나타내는 홉(hop) 수는?"
  options={['16', '15', '24', '32']}
  correctAnswer={1}
  explanation="RIP은 최대 유효 홉 수를 15로 제한하며, 홉 수가 16이 되면 도달 불가능한 목적지로 간주해 무한대로 처리한다."
  optionExplanations={['정답이다. RIP에서 16홉은 도달 불가능(무한대)을 의미한다.', '15는 RIP에서 사용 가능한 최대 유효 홉 수이며, 무한대로 간주하는 값(16)과는 다르다. 두 값을 혼동한 오답이다.', 'RIP의 홉 제한과 관련 없는 임의의 수치를 답한 오답이다.', 'RIP의 홉 제한과 관련 없는 임의의 수치를 답한 오답이다.']}
/>

<Quiz
  questionNumber={18}
  question="DNS(Domain Name System)의 일반적인 이름 질의(query)에 기본적으로 사용하는 전송 계층 프로토콜과 포트 번호로 옳은 것은?"
  options={['UDP 53번', 'TCP 80번', 'UDP 67번', 'TCP 53번']}
  correctAnswer={1}
  explanation="DNS는 일반적인 질의·응답에 UDP 53번 포트를 사용해 빠르게 응답을 주고받는다. 응답 크기가 커서 잘림(truncation)이 발생하거나 영역 전송(zone transfer)이 필요할 때만 TCP 53번을 사용한다."
  optionExplanations={['정답이다. 일반적인 DNS 질의는 UDP 53번을 사용한다.', 'TCP 80번은 HTTP가 사용하는 포트다. 응용 프로토콜과 포트를 혼동한 오답이다.', 'UDP 67번은 DHCP 서버가 사용하는 포트다. 두 프로토콜의 포트를 혼동한 오답이다.', 'DNS도 TCP 53번을 예외적으로 쓰기는 하지만, 일반적인 질의의 기본 프로토콜은 TCP가 아니라 UDP다.']}
/>

<Quiz
  questionNumber={19}
  question="HTTP와 HTTPS의 기본 포트 번호를 바르게 짝지은 것은?"
  options={['HTTP-80, HTTPS-443', 'HTTP-8080, HTTPS-8443', 'HTTP-443, HTTPS-80', 'HTTP-21, HTTPS-22']}
  correctAnswer={1}
  explanation="HTTP는 기본적으로 80번 포트를, TLS로 암호화된 HTTPS는 기본적으로 443번 포트를 사용한다."
  optionExplanations={['정답이다. HTTP 80번, HTTPS 443번은 표준 기본 포트다.', '8080과 8443은 실제로 대체 포트로 종종 쓰이지만 기본(well-known) 포트는 아니다. 기본 포트와 대체 포트를 혼동한 오답이다.', 'HTTP와 HTTPS의 기본 포트가 서로 뒤바뀌어 있는 오답이다.', '21과 22는 각각 FTP 제어 포트와 SSH 포트다. 전혀 다른 프로토콜의 포트와 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={20}
  question="SMTP, POP3, IMAP의 기본 포트 번호를 바르게 짝지은 것은?"
  options={['SMTP-25, POP3-110, IMAP-143', 'SMTP-110, POP3-25, IMAP-143', 'SMTP-25, POP3-143, IMAP-110', 'SMTP-53, POP3-110, IMAP-143']}
  correctAnswer={1}
  explanation="메일 발송을 담당하는 SMTP는 25번, 메일을 내려받는 POP3는 110번, 서버에 메일을 유지하며 열람하는 IMAP은 143번 포트를 기본으로 사용한다."
  optionExplanations={['정답이다. SMTP 25, POP3 110, IMAP 143이 표준 기본 포트다.', 'SMTP와 POP3의 포트가 서로 뒤바뀌어 있는 오답이다.', 'POP3와 IMAP의 포트가 서로 뒤바뀌어 있는 오답이다.', 'SMTP 포트를 DNS 포트(53)와 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={21}
  question="FTP(File Transfer Protocol)가 사용하는 제어 연결과 데이터 연결의 기본 포트 번호로 옳은 것은?"
  options={['제어-21, 데이터-20', '제어-20, 데이터-21', '제어-21, 데이터-80', '제어-23, 데이터-21']}
  correctAnswer={1}
  explanation="FTP는 명령을 주고받는 제어 연결에 21번 포트를, 실제 파일 데이터를 전송하는 데이터 연결에 20번 포트를 기본으로 사용하는 이중 연결 구조를 가진다."
  optionExplanations={['정답이다. 제어 21번, 데이터 20번이 FTP의 표준 기본 포트다.', '제어 포트와 데이터 포트가 서로 뒤바뀌어 있는 오답이다.', '데이터 포트를 HTTP 포트(80)와 혼동한 오답이다.', '제어 포트를 Telnet 포트(23)와 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={22}
  question="DHCP를 통해 클라이언트가 IP 주소를 할당받는 과정(DORA)의 올바른 순서는?"
  options={['DISCOVER → OFFER → REQUEST → ACK', 'OFFER → DISCOVER → ACK → REQUEST', 'DISCOVER → REQUEST → OFFER → ACK', 'REQUEST → DISCOVER → OFFER → ACK']}
  correctAnswer={1}
  explanation="클라이언트가 DHCP 서버를 찾는 DISCOVER를 브로드캐스트하면 서버가 사용 가능한 주소를 제안하는 OFFER로 응답하고, 클라이언트가 그 주소를 쓰겠다는 REQUEST를 보내면 서버가 최종 확인인 ACK를 보내 할당이 완료된다."
  optionExplanations={['정답이다. DISCOVER, OFFER, REQUEST, ACK 순서가 DHCP의 정확한 절차(DORA)다.', '클라이언트의 최초 요청인 DISCOVER보다 서버의 OFFER가 먼저 오는 것으로 되어 있어 순서가 거꾸로 된 오답이다.', 'OFFER와 REQUEST의 순서가 뒤바뀌어 있어, 서버가 제안하기 전에 클라이언트가 먼저 요청하는 것으로 잘못 서술된 오답이다.', '클라이언트의 최초 탐색 단계인 DISCOVER가 맨 앞에 와야 하는데, REQUEST가 먼저 오는 것으로 되어 있어 순서가 어긋난 오답이다.']}
/>

<Quiz
  questionNumber={23}
  question="POP3와 IMAP의 차이에 대한 설명 중 옳지 않은 것은?"
  options={['POP3는 기본적으로 메일을 클라이언트로 내려받은 뒤 서버에서 삭제하는 방식으로 동작한다', 'IMAP은 메일을 서버에 유지한 채 여러 기기에서 동기화하여 열람할 수 있다', 'POP3는 여러 기기에서 메일함 상태를 실시간으로 동기화하는 데 IMAP보다 유리하다', 'IMAP은 서버에 메일을 보관하므로 여러 클라이언트 간 읽음 상태 등을 공유하기에 적합하다']}
  correctAnswer={3}
  explanation="POP3는 메일을 로컬로 내려받고 서버에서는 삭제하는 방식이 기본이라 여러 기기 간 동기화에 취약하다. 반대로 IMAP은 서버에 메일 상태를 유지하며 여러 기기가 같은 상태를 공유하므로 동기화에는 IMAP이 더 유리하다. 이를 반대로 서술했으므로 옳지 않다."
  optionExplanations={['옳은 설명이다. POP3의 기본 동작 방식은 다운로드 후 서버에서 삭제하는 것이다.', '옳은 설명이다. IMAP은 서버 중심으로 여러 기기 간 동기화를 지원한다.', '옳지 않은 설명이라 정답이다. 여러 기기 동기화에 유리한 것은 POP3가 아니라 IMAP이다. 두 프로토콜의 장점을 반대로 서술한 오답이다.', '옳은 설명이다. IMAP은 서버 보관 방식 덕분에 읽음 상태 등을 여러 클라이언트가 공유할 수 있다.']}
/>

<Quiz
  questionNumber={24}
  question="TCP 혼잡 제어의 국면을 순서대로 옳게 나열한 것은? (손실 발생 후 회복까지 포함)"
  options={['느린 시작(slow start) → 혼잡 회피(congestion avoidance) → 빠른 회복(fast recovery)', '혼잡 회피 → 느린 시작 → 빠른 회복', '빠른 회복 → 느린 시작 → 혼잡 회피', '느린 시작 → 빠른 회복 → 혼잡 회피']}
  correctAnswer={1}
  explanation="연결 초기에는 느린 시작으로 혼잡 윈도우를 지수적으로 늘리다가 ssthresh에 도달하면 혼잡 회피 국면으로 전환해 선형적으로 늘린다. 이후 중복 ACK로 손실이 감지되면 빠른 회복 국면으로 들어가 완전히 처음부터 다시 시작하지 않고 회복한다."
  optionExplanations={['정답이다. 느린 시작에서 혼잡 회피로, 손실 시 빠른 회복으로 이어지는 순서가 정확하다.', '혼잡 회피가 느린 시작보다 먼저 오는 것으로 되어 있어 순서가 뒤바뀐 오답이다. 연결 초기에는 항상 느린 시작이 먼저다.', '회복 국면이 맨 앞에 오는 것으로 되어 있어 손실이 발생하기도 전에 회복부터 하는 논리적으로 맞지 않는 순서다.', '혼잡 회피가 빠른 회복 뒤에 오는 것으로 되어 있어, 정상적인 손실 없는 진행 국면인 혼잡 회피의 위치가 잘못된 오답이다.']}
/>

## 참고 자료

- [MDN Web Docs - TCP](https://developer.mozilla.org/)
- [IETF RFC 768 - User Datagram Protocol](https://www.rfc-editor.org/rfc/rfc768)
- [IETF RFC 1058 - RIP](https://www.rfc-editor.org/rfc/rfc1058)
- [IETF RFC 2328 - OSPF](https://www.rfc-editor.org/rfc/rfc2328)
