---
title: '27. 독학사 컴퓨터네트워크 예상기출문제 4'
description: 'NAT·IPv6, 보안 기초, SDN·IoT·클라우드 네트워킹 등 최신 키워드와 전 범위 종합 복습을 다루는 예상 기출 문제 세트'
---

<Callout type="warning">
  이 문제들은 실제 독학사 기출 문제가 아닙니다. 국가평생교육진흥원 독학학위제 컴퓨터네트워크 출제기준(평가영역)을 바탕으로 재구성한 예상 문제이며, 실제 시험에 나온 문항을 그대로 옮기지 않았습니다. 최신 출제 범위와 문항 수는 반드시 국가평생교육진흥원 공고로 다시 확인하세요.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="NAT(Network Address Translation)의 목적과 동작에 대한 설명으로 가장 옳은 것은?"
  options={['사설 IP 주소를 사용하는 내부 호스트가 하나 이상의 공인 IP 주소를 이용해 외부 네트워크와 통신할 수 있도록 주소를 변환한다', '내부 호스트의 MAC 주소를 변환해 최적의 라우팅 경로를 결정한다', '전송 계층에서 발생한 오류를 검출하고 재전송을 요청하는 기능이다', 'NAT를 사용하면 내부 사설 주소가 외부에 그대로 노출되어 오히려 보안이 약화된다']}
  correctAnswer={1}
  explanation="NAT는 내부 네트워크에서 사용하는 사설 IP 주소를 외부와 통신할 때 공인 IP 주소로 바꿔주는 기술이다. 이를 통해 제한된 공인 IP 주소를 절약하고, 부수적으로 내부 주소 구조를 외부에 감추는 효과도 얻을 수 있다."
  optionExplanations={['정답이다. 사설-공인 주소 변환이 NAT의 핵심 목적이다.', 'NAT는 MAC 주소가 아니라 IP 주소(와 포트)를 변환하는 기술이다. 계층과 기능을 혼동한 오답이다.', '오류 검출과 재전송은 전송 계층 프로토콜(TCP)의 기능이며 NAT와는 무관하다.', 'NAT는 오히려 내부 사설 주소를 외부에 직접 노출하지 않아 보안에 도움이 되는 효과가 있다. 이를 반대로 서술한 오답이다.']}
/>

<Quiz
  questionNumber={2}
  question="IPv6 주소 2001:0db8:0000:0000:0000:ff00:0042:8329를 압축 표기법으로 옳게 나타낸 것은?"
  options={['2001:db8::ff00:42:8329', '2001:db8:0:0:ff00:42:8329', '2001::0db8::ff00:0042:8329', '2001:db8:0000:0000:0000:ff00:0042:8329']}
  correctAnswer={1}
  explanation="각 그룹의 앞자리 0을 생략하면 2001:db8:0:0:0:ff00:42:8329가 되고, 연속된 0으로만 이루어진 그룹(세 번째~다섯 번째)은 ::로 한 번만 압축할 수 있다. 따라서 2001:db8::ff00:42:8329가 올바른 압축 표기다."
  optionExplanations={['정답이다. 앞자리 0 생략과 연속된 0 그룹의 :: 압축을 모두 올바르게 적용했다.', '연속된 0 그룹 하나를 실수로 빠뜨려 전체 그룹 수가 7개가 되어 원래 주소와 값이 달라진 오답이다.', ':: 표기는 주소 하나당 한 번만 사용할 수 있는데 두 번 사용해 규칙을 어긴 오답이다.', '앞자리 0도 생략하지 않고 :: 압축도 적용하지 않아 원본 그대로인, 압축이 이루어지지 않은 오답이다.']}
/>

<Quiz
  questionNumber={3}
  question="IPv4와 IPv6을 비교한 설명 중 옳지 않은 것은?"
  options={['IPv6 주소는 128비트로 IPv4의 32비트보다 훨씬 넓은 주소 공간을 제공한다', 'IPv6은 헤더 구조를 단순화하고 확장 헤더를 이용해 추가 기능을 옵션으로 처리한다', 'IPv6은 IPv4와 달리 헤더에 체크섬 필드를 두어 자체적으로 오류를 검사한다', 'IPv6은 주소 공간이 넓어 NAT 없이도 각 기기에 고유한 공인 주소를 부여하기 쉬워진다']}
  correctAnswer={3}
  explanation="IPv6 기본 헤더는 오히려 IPv4에 있던 헤더 체크섬 필드를 제거했다. 오류 검사는 상위 계층(TCP/UDP)이나 데이터링크 계층에 맡기고 기본 헤더를 단순화하는 방향으로 설계되었기 때문에, IPv6이 헤더 체크섬을 둔다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 128비트는 IPv6의 핵심 특징이다.', '옳은 설명이다. IPv6은 기본 헤더를 단순화하고 확장 헤더로 옵션 기능을 분리했다.', '옳지 않은 설명이라 정답이다. IPv6 기본 헤더에는 체크섬 필드가 없다. IPv4의 헤더 체크섬 필드와 혼동한 오답이다.', '옳은 설명이다. 넓은 주소 공간 덕분에 IPv6에서는 NAT의 필요성이 상대적으로 줄어든다.']}
/>

<Quiz
  questionNumber={4}
  question="대칭키 암호와 공개키(비대칭키) 암호를 비교한 설명 중 옳지 않은 것은?"
  options={['대칭키 암호는 암호화와 복호화에 같은 키를 사용하며 일반적으로 공개키 암호보다 연산 속도가 빠르다', '공개키 암호는 공개키로 암호화한 데이터를 그에 대응하는 개인키로만 복호화할 수 있다', '대칭키 암호는 통신 당사자 수가 늘어나도 키를 안전하게 나누어 갖는 키 분배 문제가 전혀 발생하지 않는다', '공개키 암호는 키 분배 문제를 완화할 수 있지만 대칭키 암호보다 연산 부담이 크다']}
  correctAnswer={3}
  explanation="대칭키 암호는 통신 당사자마다 같은 키를 안전하게 공유해야 하므로, 참여자 수가 늘어날수록 필요한 키의 개수와 안전한 분배의 어려움이 함께 커지는 키 분배 문제가 발생한다. 이 문제가 전혀 없다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 대칭키 암호는 연산이 단순해 공개키 암호보다 빠른 것이 일반적이다.', '옳은 설명이다. 공개키로 암호화한 데이터는 짝을 이루는 개인키로만 복호화할 수 있다.', '옳지 않은 설명이라 정답이다. 대칭키 암호는 참여자가 늘어날수록 키 분배 문제가 오히려 커진다. 이를 부정한 오답이다.', '옳은 설명이다. 공개키 암호는 키 분배 문제를 완화하지만 연산량이 많아 속도는 느린 편이다.']}
/>

<Quiz
  questionNumber={5}
  question="TLS와 HTTPS의 관계에 대한 설명으로 가장 옳은 것은?"
  options={['HTTPS는 HTTP 통신 내용을 TLS로 암호화해 기밀성과 무결성을 제공하는 방식이다', 'TLS는 응용 계층이 아니라 항상 물리 계층에서 신호 자체를 암호화하는 프로토콜이다', 'HTTPS를 사용해도 이용하는 기본 포트 번호는 HTTP와 동일하게 80번으로 유지된다', 'TLS는 서버 인증서 없이도 통신 상대에 대한 완전한 신뢰를 보장할 수 있다']}
  correctAnswer={1}
  explanation="HTTPS는 HTTP 트래픽을 TLS(Transport Layer Security)로 감싸 암호화한 것으로, 데이터의 기밀성과 무결성을 함께 제공한다. 이 과정에서 서버 인증서를 이용해 서버의 신원도 함께 확인한다."
  optionExplanations={['정답이다. HTTPS는 HTTP를 TLS로 암호화한 것이라는 관계를 정확히 설명한다.', 'TLS는 응용 계층과 전송 계층 사이에서 동작하는 것으로 보는 것이 일반적이며, 물리 계층에서 신호 자체를 암호화하는 것이 아니다.', 'HTTPS는 기본적으로 443번 포트를 사용한다. HTTP의 80번 포트와 혼동한 오답이다.', 'TLS의 신뢰는 서버 인증서(공개키 인증서)를 검증하는 과정에서 나온다. 인증서 없이 신뢰를 보장한다는 것은 TLS의 핵심 메커니즘을 부정하는 오답이다.']}
/>

<Quiz
  questionNumber={6}
  question="패킷 필터링 방화벽과 상태 기반(stateful) 방화벽을 비교한 설명 중 옳지 않은 것은?"
  options={['패킷 필터링 방화벽은 각 패킷의 헤더 정보(출발지·목적지 주소, 포트 등)만 보고 규칙에 따라 허용·차단을 결정한다', '상태 기반 방화벽은 연결의 상태 정보를 추적해 이미 허용된 연결에 속하는 패킷을 자동으로 허용할 수 있다', '상태 기반 방화벽은 패킷 필터링 방화벽보다 더 단순하며 연결 상태 정보를 전혀 유지하지 않는다', '방화벽은 내부 네트워크와 외부 네트워크 사이의 트래픽을 정책에 따라 통제하는 보안 장비다']}
  correctAnswer={3}
  explanation="상태 기반 방화벽은 이름 그대로 연결의 상태(state) 정보를 표로 유지하면서 판단하기 때문에, 상태 정보 없이 패킷 하나하나만 보는 패킷 필터링 방화벽보다 오히려 더 복잡하다. 상태 정보를 전혀 유지하지 않는다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 패킷 필터링 방화벽은 헤더 정보만으로 판단하는 단순한 방식이다.', '옳은 설명이다. 상태 기반 방화벽은 연결 상태를 추적해 관련 패킷을 자동으로 허용할 수 있다.', '옳지 않은 설명이라 정답이다. 상태 기반 방화벽은 상태 정보를 유지하는 것이 핵심이며, 그 때문에 패킷 필터링 방화벽보다 더 복잡하다. 두 방식의 특징을 반대로 서술한 오답이다.', '옳은 설명이다. 방화벽의 기본 역할은 내부와 외부 트래픽을 정책에 따라 통제하는 것이다.']}
/>

<Quiz
  questionNumber={7}
  question="SDN(Software Defined Networking)의 특징으로 가장 옳은 것은?"
  options={['제어 평면(control plane)과 데이터 평면(data plane)을 분리해 네트워크를 소프트웨어로 중앙에서 제어할 수 있게 한다', '모든 라우터가 각자 독립적으로 경로를 계산하며 중앙 제어 장치를 전혀 두지 않는 방식이다', '데이터 평면 장비가 스스로 정책을 결정하고, 제어기(controller)는 단순히 통계만 수집한다', '하드웨어 장비의 동작을 소프트웨어로는 전혀 제어할 수 없는 전통적인 네트워크 방식이다']}
  correctAnswer={1}
  explanation="SDN은 네트워크 장비의 경로 결정 로직(제어 평면)을 개별 장비에서 분리해 중앙의 소프트웨어 컨트롤러로 옮기고, 장비(데이터 평면)는 컨트롤러의 지시에 따라 단순히 트래픽을 전달하도록 구성하는 구조다."
  optionExplanations={['정답이다. 제어 평면과 데이터 평면의 분리, 소프트웨어 기반 중앙 제어가 SDN의 핵심 특징이다.', 'SDN은 오히려 중앙 컨트롤러를 두어 제어를 집중시키는 방식이다. 이를 반대로 서술한 오답이다.', 'SDN에서는 정책 결정을 컨트롤러가 담당하고 데이터 평면 장비는 단순 전달을 담당한다. 역할을 반대로 서술한 오답이다.', 'SDN이라는 이름 자체가 소프트웨어를 통한 네트워크 정의를 뜻한다. 소프트웨어 제어가 불가능하다는 것은 SDN의 정의를 정반대로 서술한 오답이다.']}
/>

<Quiz
  questionNumber={8}
  question="IoT(Internet of Things) 환경의 시험 수준 특징으로 가장 옳은 것은?"
  options={['IoT 기기는 일반적으로 저전력·저용량 환경에서 동작하도록 설계되어 MQTT, CoAP 같은 경량 프로토콜이 활용된다', 'IoT 기기는 항상 유선 이더넷으로만 연결되며 무선 통신은 사용하지 않는다', 'IoT 환경에서는 연결되는 기기 수가 적어 IPv4 주소만으로 충분하며 IPv6은 필요하지 않다', 'IoT 기기는 일반 서버와 동일한 수준의 연산 능력과 전력을 갖추는 것이 표준이다']}
  correctAnswer={1}
  explanation="IoT 기기는 배터리나 저용량 하드웨어로 동작하는 경우가 많아 자원 소비가 적은 경량 프로토콜(MQTT, CoAP 등)이 널리 쓰인다."
  optionExplanations={['정답이다. 저전력·저용량 특성과 경량 프로토콜 활용이 IoT의 시험 수준 핵심 특징이다.', 'IoT는 다양한 무선 통신(와이파이, 블루투스, 저전력 광역 통신 등)을 폭넓게 활용하는 경우가 많다. 무선을 배제한다는 것은 잘못된 설명이다.', 'IoT는 오히려 방대한 수의 기기가 연결되어 IPv4 주소 공간의 한계를 넘어서기 쉬워 IPv6의 필요성이 커지는 대표적인 분야다.', 'IoT 기기 다수는 자원이 제한된 소형 장치로, 일반 서버 수준의 연산 능력과 전력을 갖추는 것이 표준은 아니다.']}
/>

<Quiz
  questionNumber={9}
  question="클라우드 네트워킹의 시험 수준 개념으로 가장 옳은 것은?"
  options={['가상 네트워크를 소프트웨어로 정의해 물리적 장비 구성을 바꾸지 않고도 네트워크 자원을 유연하게 확장·축소할 수 있다', '클라우드 네트워킹은 물리적 케이블을 새로 포설해야만 네트워크 구성을 바꿀 수 있는 방식이다', '클라우드 환경에는 가상 네트워크 개념이 없어 전통적인 온프레미스 네트워크와 완전히 동일하게 동작한다', '클라우드 네트워킹은 오직 하나의 데이터센터 안에서만 동작하며 여러 지역에 걸친 구성은 불가능하다']}
  correctAnswer={1}
  explanation="클라우드 네트워킹은 가상화 기술로 네트워크를 소프트웨어 수준에서 정의해, 물리적 배선을 바꾸지 않고도 필요에 따라 네트워크 자원을 유연하게 늘리거나 줄일 수 있게 하는 것이 핵심 개념이다."
  optionExplanations={['정답이다. 소프트웨어 기반 가상 네트워크와 탄력적 확장이 클라우드 네트워킹의 핵심이다.', '클라우드 네트워킹의 장점은 오히려 물리적 배선 변경 없이 구성을 바꿀 수 있다는 점이다. 이를 반대로 서술한 오답이다.', '클라우드 네트워킹은 가상 네트워크 개념을 적극적으로 활용하는 것이 특징이며, 전통적인 온프레미스 방식과는 운용 방식이 다르다.', '클라우드 서비스는 여러 지역(리전)에 걸친 네트워크 구성을 지원하는 경우가 많다. 한 데이터센터로 제한된다는 것은 잘못된 설명이다.']}
/>

<Quiz
  questionNumber={10}
  question="IP 주소 172.16.50.10에 서브넷 마스크 255.255.240.0(/20)을 적용했을 때 이 호스트가 속한 네트워크 주소는?"
  options={['172.16.48.0', '172.16.32.0', '172.16.50.0', '172.16.64.0']}
  correctAnswer={1}
  explanation="/20 마스크는 셋째 옥텟에서 블록 크기가 256-240=16이 되도록 만든다. 셋째 옥텟 값 50을 16으로 나누면 3.125이므로 내림해 3×16=48이 된다. 따라서 네트워크 주소는 172.16.48.0이다."
  optionExplanations={['정답이다. 50을 16 단위로 나눈 몫을 정확히 내림해 48을 구했다.', '50을 16으로 나눈 몫을 2로 잘못 계산해 2×16=32로 구한 오답이다.', '/20 마스크를 무시하고 /24 기준으로 계산한 마스크 비트 오산 오답이다.', '몫을 내림이 아니라 올림해 다음 블록(64)의 시작 값으로 착각한 오답이다.']}
/>

<Quiz
  questionNumber={11}
  question="라우터 A, B, C, D, E로 이루어진 네트워크에서 링크 비용이 A-B=1, A-C=4, B-C=2, B-D=5, C-D=1, D-E=3, C-E=8일 때, 다익스트라 알고리즘으로 구한 A에서 E까지의 최단 경로 비용은?"
  options={['7 (A-B-C-D-E 경로)', '9 (A-B-D-E 경로)', '11 (A-B-C-E 경로)', '6 (A-B-C-D까지의 값을 최종 목적지 비용으로 착각)']}
  correctAnswer={1}
  explanation="가능한 경로를 비교하면 A-B-D-E는 1+5+3=9, A-B-C-E는 1+2+8=11, A-B-C-D-E는 1+2+1+3=7이다. 이 중 가장 작은 값인 7이 A에서 E까지의 최단 경로 비용이며, 경로는 A-B-C-D-E다."
  optionExplanations={['정답이다. A-B-C-D-E 경로의 합 1+2+1+3=7이 가장 작은 값이다.', 'A-B-D-E 경로의 값이지만, C를 거쳐 D로 가는 더 짧은 경로(A-B-C-D=4)를 반영하지 않아 최적이 아닌 경로를 답한 오답이다.', 'A-B-C-E처럼 C에서 E로 바로 가는 비싼 간선을 사용해, D를 거치는 더 저렴한 경로를 놓친 오답이다.', 'A에서 D까지의 중간 비용(1+2+1=4가 아니라 6으로 잘못 계산한 값)을 최종 목적지 E까지의 비용으로 착각해 마지막 구간(D-E)을 더하지 않은 오답이다.']}
/>

<Quiz
  questionNumber={12}
  question="TCP 연결 설정 과정에서 클라이언트가 SYN 세그먼트를 보낸 직후, 서버의 응답을 기다리는 동안 클라이언트의 TCP 상태는?"
  options={['SYN_SENT', 'SYN_RCVD', 'ESTABLISHED', 'LISTEN']}
  correctAnswer={1}
  explanation="클라이언트는 SYN을 보낸 뒤 서버의 SYN+ACK 응답을 기다리는 동안 SYN_SENT 상태에 머무른다. 이후 SYN+ACK를 받고 ACK를 보내면 ESTABLISHED 상태로 전이한다."
  optionExplanations={['정답이다. SYN을 보낸 뒤 응답을 기다리는 클라이언트의 상태는 SYN_SENT다.', 'SYN_RCVD는 클라이언트의 SYN을 받은 서버 쪽의 상태다. 클라이언트와 서버의 상태를 혼동한 오답이다.', 'ESTABLISHED는 3-way handshake가 완전히 끝난 뒤의 상태다. 아직 연결이 완료되지 않은 시점을 착각한 오답이다.', 'LISTEN은 서버가 연결 요청을 기다리며 대기하는 상태다. 클라이언트와 서버의 역할을 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={13}
  question="대역폭이 100Mbps인 링크의 실제 회선 이용률(utilization)이 80퍼센트일 때, 이 링크의 실제 처리량(throughput)은?"
  options={['80Mbps', '100Mbps', '20Mbps', '8Mbps']}
  correctAnswer={1}
  explanation="처리량은 대역폭에 실제 이용률을 곱한 값이다. 100Mbps×0.8=80Mbps가 실제 처리량이 된다."
  optionExplanations={['정답이다. 대역폭에 이용률 0.8을 정확히 곱했다.', '이용률을 반영하지 않고 대역폭 자체를 처리량으로 답한 오답이다.', '이용률 80퍼센트를 손실률로 착각해 100-80=20으로 계산한 오답이다.', '100×0.8 계산에서 소수점을 한 자리 잘못 옮겨 8로 계산한 오답이다.']}
/>

<Quiz
  questionNumber={14}
  question="기밀성(confidentiality)과 인증(authentication)에 대한 설명 중 옳지 않은 것은?"
  options={['기밀성은 허가되지 않은 사람이 데이터 내용을 알아볼 수 없도록 보호하는 것이다', '인증은 통신 상대가 주장하는 신원이 실제로 맞는지 확인하는 것이다', '기밀성과 인증은 완전히 같은 개념이며 하나를 만족하면 다른 하나도 자동으로 만족된다', '암호화는 주로 기밀성을 제공하는 수단으로 쓰이며, 그 자체만으로는 상대방의 신원을 보장하지 않는다']}
  correctAnswer={3}
  explanation="기밀성은 데이터 내용을 숨기는 것이고 인증은 상대방의 신원을 확인하는 것으로, 목적과 달성 방법이 서로 다른 별개의 보안 목표다. 데이터를 암호화했다고 해서 상대방이 누구인지 자동으로 증명되는 것은 아니므로, 두 개념이 같다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 기밀성은 데이터 내용을 감추는 것이 목적이다.', '옳은 설명이다. 인증은 신원 확인이 목적이다.', '옳지 않은 설명이라 정답이다. 기밀성과 인증은 서로 다른 보안 목표이며 하나를 만족한다고 다른 하나가 자동으로 보장되지 않는다. 두 개념을 동일시한 오답이다.', '옳은 설명이다. 암호화만으로는 상대방의 신원까지 보장하지 못하며, 별도의 인증 절차(인증서, 서명 등)가 필요하다.']}
/>

<Quiz
  questionNumber={15}
  question="해시 함수(hash function)의 무결성 검증 활용에 대한 설명으로 가장 옳은 것은?"
  options={['임의 길이의 데이터를 고정된 길이의 값으로 변환하며, 데이터가 조금만 달라져도 해시 값이 크게 달라져 데이터 변조 여부를 확인하는 데 활용된다', '해시 값에서 원본 데이터를 역으로 쉽게 복원할 수 있도록 설계하는 것이 해시 함수의 핵심 목표다', '해시 함수는 데이터를 암호화해 기밀성을 제공하는 것이 주된 목적이다', '서로 다른 두 데이터가 반드시 서로 다른 해시 값을 갖도록 수학적으로 완전히 보장된다']}
  correctAnswer={1}
  explanation="해시 함수는 원본 데이터가 조금이라도 바뀌면 결과값(해시 값)이 크게 달라지는 특성을 가지므로, 송수신한 데이터의 해시 값을 비교해 전송 중 변조가 있었는지 확인하는 무결성 검증에 활용된다."
  optionExplanations={['정답이다. 데이터 변화에 민감한 해시 값의 특성을 이용한 무결성 검증이 핵심 활용법이다.', '해시 함수는 원본을 역으로 복원하기 어려워야 하는 일방향성(one-way)을 갖도록 설계된다. 이를 반대로 서술한 오답이다.', '해시 함수는 기밀성을 위한 암호화가 아니라 무결성 검증을 위해 주로 쓰인다. 목적을 혼동한 오답이다.', '서로 다른 데이터가 같은 해시 값을 갖는 충돌(collision)은 이론적으로 존재할 수 있으며, 다만 찾기 어렵도록 설계될 뿐이다. 완전히 보장된다는 것은 과장된 오답이다.']}
/>

<Quiz
  questionNumber={16}
  question="사무실 내 케이블이 물리적으로 끊어져 해당 구간의 모든 통신이 완전히 두절되었다. 문제가 발생한 가능성이 가장 큰 계층은?"
  options={['물리 계층', '응용 계층', '전송 계층', '네트워크 계층']}
  correctAnswer={1}
  explanation="케이블 단선처럼 신호 자체가 전달되지 못하는 문제는 비트를 실어 나르는 매체와 신호를 다루는 물리 계층의 문제로 봐야 한다. 상위 계층 프로토콜은 애초에 신호가 도착하지 않으면 동작할 기회조차 갖지 못한다."
  optionExplanations={['정답이다. 매체 자체의 단선은 물리 계층 문제의 전형적인 사례다.', '응용 계층은 신호가 정상적으로 도달한 이후의 문제(예: 잘못된 요청)를 다루는 계층이라 케이블 단선과는 거리가 멀다.', '전송 계층은 세그먼트의 순서·재전송을 다루는 계층으로, 신호 자체가 전달되지 않는 물리적 단선과는 다른 층위의 문제다.', '네트워크 계층은 경로 선택을 다루는 계층으로, 매체 단선처럼 신호가 아예 전달되지 않는 상황과는 다른 층위의 문제다.']}
/>

<Quiz
  questionNumber={17}
  question="IP 연결은 정상이고 다른 사이트도 잘 열리는데, 특정 도메인 이름을 입력했을 때만 이름 해석에 실패해 웹 페이지가 열리지 않는다. 문제가 발생했을 가능성이 가장 큰 계층은?"
  options={['응용 계층', '물리 계층', '데이터링크 계층', '전송 계층']}
  correctAnswer={1}
  explanation="도메인 이름을 IP 주소로 바꿔주는 DNS는 응용 계층 프로토콜이다. IP 연결 자체는 정상이므로 하위 계층은 문제가 없고, 특정 이름 해석만 실패하는 것은 DNS 조회 과정, 즉 응용 계층 수준의 문제일 가능성이 크다."
  optionExplanations={['정답이다. DNS는 응용 계층 프로토콜이므로 이름 해석 실패는 응용 계층 문제로 보는 것이 타당하다.', '물리 계층에 문제가 있다면 다른 사이트도 열리지 않아야 하는데, 다른 사이트는 정상 동작하므로 물리 계층 문제로 보기 어렵다.', '데이터링크 계층 문제라면 같은 링크의 다른 통신도 영향을 받아야 하는데, 다른 사이트는 정상이므로 데이터링크 계층 문제로 보기 어렵다.', '전송 계층 문제라면 세그먼트 전달 자체가 실패해 다른 통신에도 영향을 주는 경우가 많은데, 이름 해석만 실패하는 상황과는 잘 맞지 않는다.']}
/>

<Quiz
  questionNumber={18}
  question="UDP를 사용하기에 적합한 상황에 대한 설명으로 가장 옳은 것은?"
  options={['실시간 음성·영상 스트리밍처럼 약간의 데이터 손실보다 지연에 더 민감한 서비스는 UDP를 주로 사용한다', '내용이 정확하게 전달되어야 하는 이메일 전송에는 UDP가 TCP보다 더 적합하다', '데이터 손실이 전혀 허용되지 않는 파일 다운로드에는 UDP가 TCP보다 더 적합하다', 'UDP는 연결 설정 절차가 있어 TCP보다 초기 지연이 더 크다']}
  correctAnswer={1}
  explanation="UDP는 순서 보장이나 재전송이 없는 대신 지연이 적다는 장점이 있어, 약간의 손실은 감수하더라도 실시간성이 중요한 음성·영상 스트리밍 같은 서비스에 적합하다."
  optionExplanations={['정답이다. 지연에 민감하고 약간의 손실을 감수할 수 있는 서비스가 UDP에 적합한 대표 사례다.', '이메일은 내용이 정확히 전달되어야 하므로 신뢰성을 보장하는 TCP 기반의 SMTP를 사용한다. UDP와 TCP의 적합한 용도를 반대로 서술한 오답이다.', '손실이 전혀 허용되지 않는 파일 전송에는 신뢰성을 보장하는 TCP가 적합하다. 이를 반대로 서술한 오답이다.', 'UDP는 연결 설정 절차 자체가 없다는 것이 특징이다. 연결 설정이 있다는 것은 TCP와 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={19}
  question="IEEE 802.11 무선 랜(wireless LAN)의 기초 특징으로 가장 옳은 것은?"
  options={['무선 랜은 유선 이더넷과 달리 매체 접근 제어에 주로 CSMA/CA 방식을 사용한다', '무선 랜은 유선 케이블이 없어도 유선 이더넷과 동일한 CSMA/CD를 반드시 사용해야 한다', '무선 랜은 전파를 사용하므로 도청이나 간섭의 위험이 유선 네트워크보다 낮다', '무선 랜에서는 숨은 노드(hidden node) 문제가 전혀 존재하지 않는다']}
  correctAnswer={1}
  explanation="무선 매체에서는 자신이 송신하면서 동시에 충돌을 감지하기가 어려워, 유선의 CSMA/CD 대신 충돌을 사전에 피하는 데 초점을 둔 CSMA/CA 방식을 매체 접근 제어에 주로 사용한다."
  optionExplanations={['정답이다. 무선 랜은 CSMA/CA를 매체 접근 제어의 기본 방식으로 사용한다.', '무선 환경에서는 충돌 감지가 어려워 CSMA/CD 대신 CSMA/CA를 사용하는 것이 일반적이다. 유선과 무선 방식을 혼동한 오답이다.', '무선 랜은 전파가 열린 공간으로 퍼져 나가는 특성 때문에 오히려 도청과 간섭에 더 취약한 편이다. 이를 반대로 서술한 오답이다.', '숨은 노드 문제는 서로의 신호를 감지하지 못하는 두 무선 장치가 같은 수신 장치로 동시에 전송해 충돌이 발생하는, 무선 랜의 대표적인 이슈다. 문제가 없다는 것은 잘못된 설명이다.']}
/>

<Quiz
  questionNumber={20}
  question="SDN, IoT, 클라우드 네트워킹에 대한 설명 중 옳지 않은 것은?"
  options={['SDN은 네트워크 제어 로직을 소프트웨어 컨트롤러로 집중시켜 유연한 정책 적용을 가능하게 한다', 'IoT는 다양한 사물이 인터넷에 연결되어 데이터를 주고받는 환경으로, 기기 수가 많아 주소 체계와 저전력 통신이 중요한 고려사항이다', '클라우드 네트워킹은 가상화 기술을 바탕으로 네트워크 자원을 탄력적으로 할당할 수 있게 한다', 'SDN, IoT, 클라우드 네트워킹은 모두 전통적인 회선 교환 방식만을 기반으로 동작하는 기술이다']}
  correctAnswer={4}
  explanation="SDN, IoT, 클라우드 네트워킹은 모두 패킷 교환 기반의 IP 네트워크 위에서 동작하는 기술이다. 회선 교환은 연결이 유지되는 동안 대역폭을 고정 할당하는 전통적인 전화망 방식으로, 이 최신 기술들의 기반과는 거리가 멀다."
  optionExplanations={['옳은 설명이다. 제어 로직의 소프트웨어 집중이 SDN의 핵심이다.', '옳은 설명이다. IoT는 방대한 기기 수와 저전력 통신이 중요한 고려사항이다.', '옳은 설명이다. 가상화를 통한 탄력적 자원 할당이 클라우드 네트워킹의 핵심이다.', '옳지 않은 설명이라 정답이다. 이 기술들은 회선 교환이 아니라 패킷 교환 기반 IP 네트워크 위에서 동작한다. 교환 방식을 잘못 서술한 오답이다.']}
/>

<Quiz
  questionNumber={21}
  question="OSI 참조 모델과 TCP/IP 모델의 계층 수에 대한 설명으로 가장 옳은 것은?"
  options={['OSI는 7개 계층으로 구성되고, TCP/IP는 이를 단순화해 흔히 4개(응용·전송·인터넷·네트워크 접근) 계층으로 설명한다', 'OSI와 TCP/IP는 계층 수가 정확히 동일하게 7개로 일치한다', 'TCP/IP가 OSI보다 계층 수가 많아 더 세분화된 모델이다', 'OSI 모델은 실제로 구현된 적이 없어 계층 개수 자체가 정해져 있지 않다']}
  correctAnswer={1}
  explanation="OSI 참조 모델은 물리·데이터링크·네트워크·전송·세션·표현·응용의 7개 계층으로 정의되며, TCP/IP는 이를 실용적으로 단순화해 네트워크 접근, 인터넷, 전송, 응용의 4개 계층(교재에 따라 5개로 세분화하기도 함)으로 설명하는 것이 일반적이다."
  optionExplanations={['정답이다. OSI 7계층과 TCP/IP 4계층(또는 5계층)의 구성을 정확히 설명하고 있다.', 'OSI는 7계층, TCP/IP는 4계층(또는 5계층)으로 계층 수가 다르다. 계층 수가 같다는 것은 잘못된 설명이다.', 'TCP/IP는 OSI보다 계층을 단순화해 적은 계층 수로 정리한 모델이다. 계층 수의 많고 적음을 반대로 서술한 오답이다.', 'OSI는 계층별 역할이 명확히 정의된 참조 모델로, 계층 개수(7개)가 명확히 정해져 있다. 개수가 정해져 있지 않다는 것은 잘못된 설명이다.']}
/>

<Quiz
  questionNumber={22}
  question="회선 교환(circuit switching)과 패킷 교환(packet switching)을 비교한 설명 중 옳지 않은 것은?"
  options={['회선 교환은 연결이 유지되는 동안 대역폭이 고정 할당되어 일정한 지연을 보장하기에 유리하다', '패킷 교환은 자원을 동적으로 공유해 회선 이용 효율은 높지만, 트래픽 상황에 따라 지연이 달라질 수 있다', '회선 교환은 연결 설정 없이 즉시 데이터를 보낼 수 있어 패킷 교환보다 초기 지연이 항상 더 작다', '패킷 교환에서는 여러 사용자가 같은 회선을 공유하므로 회선 교환보다 자원 활용률이 높은 경우가 많다']}
  correctAnswer={3}
  explanation="회선 교환은 데이터를 보내기 전에 발신자와 수신자 사이에 전용 경로를 설정하는 호 설정(call setup) 과정이 필요하다. 이 과정 때문에 오히려 초기 지연이 발생할 수 있으므로, 연결 설정 없이 즉시 보낼 수 있어 항상 초기 지연이 더 작다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 고정 대역폭 할당은 회선 교환의 대표적인 장점이다.', '옳은 설명이다. 패킷 교환은 자원을 동적으로 나눠 쓰는 대신 지연이 트래픽 상황에 따라 변할 수 있다.', '옳지 않은 설명이라 정답이다. 회선 교환은 데이터 전송 전에 호 설정 과정을 거쳐야 해 초기 지연이 발생할 수 있다. 연결 설정 절차를 무시한 오답이다.', '옳은 설명이다. 자원을 공유하는 패킷 교환은 전체 활용률 측면에서 유리한 경우가 많다.']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 - 합격결정](https://bdes.nile.or.kr/nile/exam/nExam5_1.do)
- [IETF RFC 8446 - TLS 1.3](https://www.rfc-editor.org/rfc/rfc8446)
- [IETF RFC 8200 - IPv6 Specification](https://www.rfc-editor.org/rfc/rfc8200)
