---
title: '24. 개념·정의형 예상문제 1 (언어 평가·구문·의미)'
description: '국가평생교육진흥원 출제기준에 근거해 재구성한 언어 역사·패러다임, 평가 기준과 설계 원칙, 구문·의미(BNF/EBNF·속성 문법·정적/동적 의미) 영역 개념·정의형 예상문제 24문항'
---

<Callout type="warning">
  **이 예상문제는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학학위제 컴퓨터과학 전공 3단계 「프로그래밍언어론」 평가영역(출제기준)에 근거해, 06~08편에서 다룬 언어의 역사·패러다임, 언어 평가 기준과 설계 원칙, 구문과 의미(BNF/EBNF·속성 문법·정적/동적 의미) 영역의 개념·정의를 확인하도록 새로 구성한 문제입니다. 실제 회차의 문항 수·배점·시험 시간은 매 공고마다 달라질 수 있으므로 **반드시 응시 회차의 공식 공고를 확인**하시기 바랍니다.
</Callout>

이 편은 코드·계산 추론보다는 **용어의 정의와 개념 간의 경계**를 정확히 아는지를 묻는 문제로 구성했습니다. "옳지 않은 것을 고르시오" 유형이 많으므로, 보기 네 개를 하나씩 따로 놓고 그 진술 자체가 참인지 거짓인지 판단한 뒤 답을 고르는 방식으로 복습하는 것을 권장합니다.

## 문항 배분

| 영역 | 문항 수 | 문항 번호 |
|------|---------|-----------|
| 언어의 역사와 패러다임 | 8 | 1–8 |
| 언어 평가 기준과 설계 원칙 | 6 | 9–14 |
| 구문과 의미: BNF/EBNF·속성 문법·정적/동적 의미 | 10 | 15–24 |
| 합계 | 24 | 1–24 |

## 언어의 역사와 패러다임 (1–8번)

<Quiz
  questionNumber={1}
  question="프로그래밍 언어와 그 언어의 구현(implementation)의 관계에 대한 설명으로 옳은 것은?"
  options={['언어는 문법과 의미의 약속이고, 구현은 그 약속을 실제로 동작시키는 프로그램이므로 하나의 언어에 여러 구현이 존재할 수 있다', '언어와 구현은 항상 일대일로 대응하므로 하나의 언어에는 반드시 하나의 구현만 존재한다', '구현이 없어도 언어는 실제 컴퓨터에서 곧바로 실행될 수 있다', '언어는 특정 컴퓨터 기종에 종속되어 정의되는 개념이다']}
  correctAnswer={1}
  explanation="언어는 문법과 의미에 대한 약속이며, 이를 실제로 실행 가능하게 만드는 프로그램이 구현이다. 같은 자바 언어를 여러 JDK 구현이 실행할 수 있는 것처럼, 하나의 언어에 여러 구현이 존재할 수 있다."
  optionExplanations={['정답이다. 언어와 구현은 서로 다른 층위이며 하나의 언어에 여러 구현이 있을 수 있다.', '언어와 구현이 일대일로만 대응해야 한다는 제약은 없다.', '언어가 실제로 동작하려면 반드시 구현이 있어야 한다.', '언어 자체는 특정 컴퓨터 기종에 종속되지 않으며, 구현이 특정 환경에 맞춰지는 것이다.']}
/>

<Quiz
  questionNumber={2}
  question="초기 프로그래밍 언어 발전사에 대한 설명으로 옳지 않은 것은?"
  options={['포트란(Fortran)은 과학·공학 계산을 위한 초기 고급 언어로 개발되었다', 'ALGOL은 이후 여러 언어의 블록 구조와 문법 설계에 영향을 준 언어로 평가된다', 'C 언어는 시스템 프로그래밍을 목적으로 설계되어 하드웨어에 가까운 제어가 가능하다', '자바(Java)는 하드웨어 명령을 직접 처리하는 어셈블리어 계열 언어로 분류된다']}
  correctAnswer={4}
  explanation="자바는 가상 머신(JVM) 위에서 바이트코드로 실행되는 고급 객체지향 언어이며, 하드웨어 명령을 직접 다루는 어셈블리어 계열로 분류되지 않는다."
  optionExplanations={['옳은 설명이다. 포트란은 과학·공학 계산 목적의 초기 고급 언어다.', '옳은 설명이다. ALGOL은 블록 구조 등에서 후속 언어에 큰 영향을 주었다.', '옳은 설명이다. C는 시스템 프로그래밍 목적의 언어로 설계되었다.', '정답이다. 자바는 어셈블리어 계열이 아니라 가상 머신 기반의 고급 객체지향 언어다.']}
/>

<Quiz
  questionNumber={3}
  question="언어 설계에 영향을 준 요인에 대한 설명으로 가장 적절한 것은?"
  options={['하드웨어 성능과 무관하게 언어 설계는 항상 동일한 방향으로 발전해 왔다', '하드웨어 발전과 소프트웨어 개발 트렌드(대규모 협업, 안전성 요구 등)는 모두 언어 설계에 영향을 준 요인이다', '언어 설계는 오직 수학 이론의 발전에 의해서만 결정된다', '언어 설계는 특정 회사의 마케팅 전략과는 전혀 무관하게 이루어진다']}
  correctAnswer={2}
  explanation="언어 설계는 당시 하드웨어의 성능·구조와 소프트웨어 개발 방식의 변화(대규모 협업, 신뢰성 요구 증가 등) 양쪽의 영향을 받아 왔다. 예를 들어 메모리가 부족하던 시절에는 간결한 문법이, 대규모 협업이 흔해진 이후에는 모듈화와 형 안전성이 더 강조되었다."
  optionExplanations={['하드웨어 성능 변화는 실제로 언어 설계 방향에 큰 영향을 주었다.', '정답이다. 하드웨어와 소프트웨어 개발 트렌드 모두가 언어 설계에 영향을 준 요인이다.', '언어 설계는 수학 이론뿐 아니라 실용적 필요, 하드웨어 제약 등 다양한 요인의 영향을 받는다.', '실제로는 특정 기업의 전략적 판단이 언어 설계나 보급에 영향을 준 사례도 있다.']}
/>

<Quiz
  questionNumber={4}
  question="다음 중 명령형(imperative) 패러다임에 속하는 언어로 가장 적절한 것은?"
  options={['하스켈(Haskell)', 'Prolog', 'C', 'ML']}
  correctAnswer={3}
  explanation="C는 대입문과 순차적인 명령 수행을 중심으로 프로그램을 작성하는 명령형 패러다임의 대표적인 언어다. 하스켈과 ML은 함수형, Prolog는 논리형 계열로 분류된다."
  optionExplanations={['하스켈은 순수 함수형 언어의 대표 예다.', 'Prolog는 논리형 언어의 대표 예다.', '정답이다. C는 명령형 패러다임의 대표적인 언어다.', 'ML은 함수형 스타일과 정적 타입을 결합한 언어다.']}
/>

<Quiz
  questionNumber={5}
  question="프로그래밍 언어의 네 가지 주요 패러다임 분류로 옳은 것은?"
  options={['명령형, 객체지향, 함수형, 논리형', '컴파일형, 인터프리트형, 하이브리드형, 스크립트형', '정적형, 동적형, 강형, 약형', '고급 언어, 저급 언어, 어셈블리어, 기계어']}
  correctAnswer={1}
  explanation="프로그래밍 언어의 패러다임은 크게 명령형, 객체지향, 함수형, 논리형 네 가지로 분류하는 것이 이 과목의 기본 분류 체계다. 나머지 보기들은 구현 방식이나 형 시스템 분류, 언어 수준 분류로 패러다임 분류와는 다른 기준이다."
  optionExplanations={['정답이다. 명령형·객체지향·함수형·논리형이 이 과목에서 다루는 패러다임 분류다.', '이는 구현 방식에 따른 분류이지 패러다임 분류가 아니다.', '이는 형 시스템에 따른 분류이지 패러다임 분류가 아니다.', '이는 언어의 추상화 수준에 따른 분류이지 패러다임 분류가 아니다.']}
/>

<Quiz
  questionNumber={6}
  question="도메인 특화 언어(domain-specific language)의 특징에 대한 설명으로 가장 적절한 것은?"
  options={['모든 종류의 응용 분야를 동등하게 다룰 수 있도록 범용으로 설계된다', '특정 응용 분야(예: 웹, 통계 처리 등)의 작업을 효율적으로 표현하도록 설계된다', '반드시 명령형 패러다임만을 따라야 한다', '하드웨어와 완전히 무관하게 추상적인 수학 이론만을 표현한다']}
  correctAnswer={2}
  explanation="도메인 특화 언어는 특정 응용 분야의 문제를 효율적으로 표현하도록 설계된 언어로, 범용 언어와 달리 그 분야의 개념을 문법에 직접 반영하는 경우가 많다."
  optionExplanations={['범용으로 모든 분야를 동등하게 다루는 것은 오히려 범용 언어의 목표이며, 도메인 특화 언어의 정의와 반대된다.', '정답이다. 특정 응용 분야에 맞춘 효율적인 표현이 도메인 특화 언어의 핵심 특징이다.', '도메인 특화 언어가 반드시 명령형 패러다임만 따라야 하는 것은 아니다.', '도메인 특화 언어도 결국 특정 하드웨어·소프트웨어 환경에서 실행되도록 구현된다.']}
/>

<Quiz
  questionNumber={7}
  question="프로그래밍 언어 발전사에서 각 언어와 주된 설계 목적을 짝지은 것으로 옳지 않은 것은?"
  options={['포트란 - 과학·공학 계산', 'C - 시스템 프로그래밍', '자바 - 플랫폼 독립적인 응용 프로그램 개발', 'ALGOL - 대규모 병렬 분산 처리 전용']}
  correctAnswer={4}
  explanation="ALGOL은 알고리즘을 명확하고 구조적으로 표현하기 위한 언어로 설계되어 블록 구조 등 후속 언어의 문법에 큰 영향을 주었으며, 대규모 병렬 분산 처리를 전용 목적으로 설계된 언어가 아니다."
  optionExplanations={['옳은 짝짓기다. 포트란은 과학·공학 계산을 위해 설계되었다.', '옳은 짝짓기다. C는 시스템 프로그래밍을 위해 설계되었다.', '옳은 짝짓기다. 자바는 플랫폼 독립성을 주요 설계 목표로 삼았다.', '정답이다. ALGOL은 알고리즘의 명확한 표현을 위한 언어이지 대규모 병렬 분산 처리 전용 언어가 아니다.']}
/>

<Quiz
  questionNumber={8}
  question="객체지향 패러다임에 대한 설명으로 옳은 것은?"
  options={['상태와 동작을 클래스와 객체로 묶어 표현하고 상속·다형성 같은 기법을 지원한다', '프로그램을 사실과 규칙의 모음으로 표현하고 질의로 답을 탐색한다', '변수의 값 변경을 금지하고 함수의 조합만으로 계산을 표현한다', '컴퓨터에게 수행할 명령을 순서대로 나열하는 것을 유일한 표현 방식으로 삼는다']}
  correctAnswer={1}
  explanation="객체지향 패러다임은 데이터(상태)와 그 데이터를 다루는 동작(메서드)을 클래스와 객체 단위로 묶고, 상속·다형성·캡슐화 같은 기법으로 코드 재사용과 확장을 지원하는 패러다임이다."
  optionExplanations={['정답이다. 클래스·객체로 상태와 동작을 묶고 상속·다형성을 지원하는 것이 객체지향의 핵심이다.', '이는 논리형 패러다임에 대한 설명이다.', '이는 함수형 패러다임에 대한 설명이다.', '이는 명령형 패러다임에 대한 설명이며, 객체지향도 내부적으로 명령형 요소를 포함하지만 이 설명이 객체지향의 정의는 아니다.']}
/>

## 언어 평가 기준과 설계 원칙 (9–14번)

<Quiz
  questionNumber={9}
  question="프로그래밍 언어의 평가 기준 중 가독성(readability)에 대한 설명으로 가장 적절한 것은?"
  options={['작성된 프로그램을 사람이 얼마나 쉽게 읽고 이해할 수 있는가를 나타내는 기준이다', '프로그램이 오류 없이 예상대로 동작하는 정도를 나타내는 기준이다', '컴파일된 프로그램이 실행되는 속도를 나타내는 기준이다', '프로그램을 처음부터 작성하는 데 걸리는 시간을 나타내는 기준이다']}
  correctAnswer={1}
  explanation="가독성은 작성된 프로그램을 다른 사람(또는 나중의 자신)이 얼마나 쉽게 읽고 이해할 수 있는지를 나타내는 언어 평가 기준이다."
  optionExplanations={['정답이다. 가독성은 이해의 용이성을 나타내는 기준이다.', '이는 신뢰성(reliability)에 대한 설명이다.', '이는 효율성(efficiency)에 대한 설명이다.', '이는 작성 용이성(writability)에 대한 설명이다.']}
/>

<Quiz
  questionNumber={10}
  question="직교성(orthogonality)에 대한 설명으로 옳은 것은?"
  options={['언어의 기능이 서로 독립적이면서도 일관되게 조합될 수 있는 성질을 뜻한다', '프로그램이 여러 대의 컴퓨터에서 동시에 실행될 수 있는 성질을 뜻한다', '컴파일 시점에 모든 형 오류를 발견할 수 있는 성질을 뜻한다', '언어에 예약어가 전혀 없는 성질을 뜻한다']}
  correctAnswer={1}
  explanation="직교성은 언어의 몇 가지 기본 기능이 서로 독립적이면서도 예측 가능하게 조합될 수 있는 성질을 말한다. 직교성이 높으면 적은 수의 기본 개념으로도 다양한 표현이 가능해 가독성과 작성 용이성이 함께 좋아지는 경향이 있다."
  optionExplanations={['정답이다. 기능의 독립적이고 일관된 조합 가능성이 직교성의 정의다.', '이는 분산 시스템이나 병렬 실행과 관련된 개념으로 직교성의 정의가 아니다.', '이는 정적 타입 검사와 관련된 설명으로 직교성의 정의가 아니다.', '예약어 유무는 직교성의 정의와 직접 관련이 없다.']}
/>

<Quiz
  questionNumber={11}
  question="언어 설계의 트레이드오프(trade-off)에 대한 설명으로 옳지 않은 것은?"
  options={['형 안전성을 강화하면 대체로 유연성이 다소 줄어드는 경향이 있다', '정적 검사를 늘리면 컴파일 시점에 발견하는 오류는 늘지만 컴파일 시간이 길어질 수 있다', '가독성과 간결성은 항상 같은 방향으로만 향상되며 서로 상충하지 않는다', '효율성을 높이기 위한 저수준 제어를 허용하면 가독성이나 안전성이 떨어질 수 있다']}
  correctAnswer={3}
  explanation="가독성과 간결성은 항상 같은 방향으로만 향상되는 것이 아니라 서로 상충할 수 있다. 지나치게 간결한 표기는 오히려 읽는 사람이 의미를 파악하기 어렵게 만들어 가독성을 떨어뜨릴 수 있다."
  optionExplanations={['옳은 설명이다. 형 안전성 강화는 유연성 감소라는 대가를 동반하는 경우가 많다.', '옳은 설명이다. 정적 검사 강화는 오류 조기 발견과 컴파일 시간 증가 사이의 트레이드오프다.', '정답이다. 가독성과 간결성은 항상 같은 방향으로만 향상되는 관계가 아니며 서로 상충할 수 있다.', '옳은 설명이다. 저수준 제어 허용은 효율성 향상과 가독성·안전성 저하 사이의 트레이드오프다.']}
/>

<Quiz
  questionNumber={12}
  question="신뢰성(reliability)에 영향을 주는 언어 설계 요소로 가장 거리가 먼 것은?"
  options={['형 검사(type checking)의 엄격함', '예외 처리 지원 여부', '변수 초기화를 강제하는지 여부', '프로그램 소스 코드 파일의 이름 규칙']}
  correctAnswer={4}
  explanation="신뢰성은 프로그램이 예상대로 오류 없이 동작하는 정도를 뜻하며, 형 검사의 엄격함, 예외 처리 지원, 변수 초기화 강제 여부 등이 신뢰성에 직접 영향을 준다. 소스 코드 파일의 이름 규칙은 언어의 신뢰성과 관련이 없는 관례적 요소다."
  optionExplanations={['형 검사가 엄격할수록 형 오류로 인한 오작동을 미리 막아 신뢰성이 높아진다.', '예외 처리를 지원하면 오류 상황에서도 프로그램이 통제된 방식으로 대응할 수 있어 신뢰성에 기여한다.', '변수 초기화를 강제하면 초기화되지 않은 값으로 인한 오류를 막을 수 있어 신뢰성에 기여한다.', '정답이다. 파일 이름 규칙은 언어의 신뢰성과 직접 관련이 없다.']}
/>

<Quiz
  questionNumber={13}
  question="작성 용이성(writability)에 대한 설명으로 가장 적절한 것은?"
  options={['프로그램을 실행했을 때 자원을 얼마나 적게 쓰는가를 나타낸다', '주어진 문제를 해결하는 프로그램을 얼마나 쉽고 간결하게 작성할 수 있는가를 나타낸다', '다른 컴퓨터로 프로그램을 옮겼을 때 그대로 동작하는 정도를 나타낸다', '프로그램이 악의적인 공격으로부터 얼마나 안전한가를 나타낸다']}
  correctAnswer={2}
  explanation="작성 용이성은 특정 문제를 해결하는 프로그램을 언어의 기능을 활용해 얼마나 쉽고 간결하게 작성할 수 있는지를 나타내는 평가 기준이다."
  optionExplanations={['이는 효율성에 대한 설명이다.', '정답이다. 작성 용이성은 문제 해결 프로그램을 쉽게 작성할 수 있는 정도를 뜻한다.', '이는 이식성(portability)에 대한 설명이다.', '이는 보안성에 대한 설명이다.']}
/>

<Quiz
  questionNumber={14}
  question="정적 검사(static checking)와 동적 검사(dynamic checking)를 비교한 설명으로 옳지 않은 것은?"
  options={['정적 검사는 컴파일 시점에 오류를 발견하려 시도한다', '동적 검사는 프로그램이 실제로 실행되는 도중에 오류를 발견한다', '정적 검사를 많이 지원하는 언어일수록 실행 전에 잡아낼 수 있는 오류의 범위가 넓어지는 경향이 있다', '동적 검사는 실행 전에 오류를 모두 발견하므로 정적 검사보다 항상 더 안전하다']}
  correctAnswer={4}
  explanation="동적 검사는 프로그램이 실제로 그 코드를 실행하는 순간에야 오류를 발견하므로, 실행되지 않은 경로에 숨어 있는 오류는 발견하지 못할 수 있다. 실행 전에 오류를 모두 발견한다는 설명은 동적 검사의 정의와 어긋난다."
  optionExplanations={['옳은 설명이다. 정적 검사는 컴파일 시점에 이루어진다.', '옳은 설명이다. 동적 검사는 실행 중에 이루어진다.', '옳은 설명이다. 정적 검사가 강화될수록 실행 전에 발견 가능한 오류 범위가 넓어지는 경향이 있다.', '정답이다. 동적 검사는 실행된 경로에서만 오류를 발견하므로 실행 전에 오류를 모두 발견한다는 설명은 옳지 않다.']}
/>

## 구문과 의미: BNF/EBNF·속성 문법·정적/동적 의미 (15–24번)

<Quiz
  questionNumber={15}
  question="구문(syntax)과 의미(semantics)의 차이에 대한 설명으로 옳은 것은?"
  options={['구문은 문장이 올바른 형태를 갖췄는지를 다루고, 의미는 그 문장이 실제로 무엇을 뜻하는지를 다룬다', '구문과 의미는 완전히 같은 개념이며 시험에서 구분할 필요가 없다', '구문은 프로그램의 실행 속도를 다루고, 의미는 프로그램의 형태를 다룬다', '의미는 컴파일 시점에만 존재하고 구문은 실행 시점에만 존재한다']}
  correctAnswer={1}
  explanation="구문은 문장이 언어의 문법 규칙에 맞는 올바른 형태를 갖추고 있는지를 다루고, 의미는 문법에 맞게 작성된 문장이 실제로 어떤 동작이나 값을 뜻하는지를 다룬다. 이 둘은 서로 다른 층위의 개념이다."
  optionExplanations={['정답이다. 형태를 다루는 구문과 뜻을 다루는 의미는 서로 다른 층위다.', '구문과 의미는 명확히 구분되는 서로 다른 개념이다.', '실행 속도는 구문·의미 어느 쪽의 정의도 아니다.', '구문 검사는 대개 컴파일 시점, 의미는 정적 의미와 동적 의미로 나뉘어 각각 컴파일 시점과 실행 시점 모두와 관련된다.']}
/>

<Quiz
  questionNumber={16}
  question="BNF(Backus-Naur Form)에서 생성 규칙 다음 표기가 뜻하는 바로 옳은 것은? (표기: 비단말기호 문장은 비단말기호 조건문 또는 비단말기호 반복문으로 정의된다는 생성 규칙이 세로 막대로 연결되어 있다)"
  options={['문장은 조건문과 반복문을 모두 동시에 포함해야 한다는 뜻이다', '문장은 조건문이거나 반복문 중 하나로 대체될 수 있다는 선택을 뜻한다', '문장이라는 비단말기호가 더 이상 존재하지 않는다는 뜻이다', '조건문과 반복문 사이에 반드시 특수 문자가 와야 한다는 뜻이다']}
  correctAnswer={2}
  explanation="BNF에서 세로 막대는 선택을 나타내는 기호로, 왼쪽 비단말기호가 여러 대안 중 하나로 대체될 수 있음을 뜻한다. 문장이 조건문 또는 반복문 중 하나로 대체될 수 있다는 것이 이 규칙의 의미다."
  optionExplanations={['세로 막대는 동시 포함이 아니라 선택을 나타내는 기호다.', '정답이다. 세로 막대는 여러 대안 중 하나를 선택할 수 있다는 뜻이다.', '이 표기는 오히려 문장이라는 비단말기호를 조건문 또는 반복문으로 정의하는 생성 규칙이다.', '세로 막대 자체가 선택을 나타내는 기호이며 별도의 특수 문자가 필요한 것은 아니다.']}
/>

<Quiz
  questionNumber={17}
  question="EBNF(Extended BNF)가 BNF와 비교해 갖는 차이로 옳은 것은?"
  options={['EBNF는 단말기호와 비단말기호를 구분하지 않는다', 'EBNF는 반복이나 선택 사항을 나타내는 중괄호나 대괄호 표기 등을 추가로 지원해 문법을 더 간결하게 표현할 수 있다', 'EBNF는 문맥 자유 문법과 전혀 관계가 없는 별개의 이론이다', 'EBNF는 오직 자연어 문법만을 표현하기 위해 고안되었다']}
  correctAnswer={2}
  explanation="EBNF는 BNF의 기본 개념을 확장해, 반복이나 선택적 요소를 나타내는 표기를 추가로 지원함으로써 같은 문법을 더 간결하게 표현할 수 있게 한 표기법이다."
  optionExplanations={['EBNF도 BNF와 마찬가지로 단말기호와 비단말기호를 구분한다.', '정답이다. 반복·선택 표기 지원으로 더 간결한 문법 표현이 가능한 것이 EBNF의 특징이다.', 'EBNF는 BNF와 마찬가지로 문맥 자유 문법을 표현하는 표기법이다.', 'EBNF는 프로그래밍 언어를 포함한 형식 언어의 문법을 표현하기 위해 고안되었다.']}
/>

<Quiz
  questionNumber={18}
  question="구문 도표(syntax diagram)에 대한 설명으로 가장 적절한 것은?"
  options={['언어의 문법 규칙을 도형과 화살표로 시각화해 표현한 것이다', '프로그램이 실행되는 동안 변수 값이 바뀌는 과정을 표시한 도표다', '컴파일러 내부의 목적 코드 생성 과정을 표시한 도표다', '프로세스의 상태 전이를 표시하기 위한 도표다']}
  correctAnswer={1}
  explanation="구문 도표는 BNF나 EBNF로 표현된 문법 규칙을 상자와 화살표 같은 도형으로 시각화해, 문장이 어떤 순서로 구성 요소를 거쳐 만들어지는지 그림으로 보여 주는 표기 방식이다."
  optionExplanations={['정답이다. 문법 규칙을 시각적으로 표현하는 것이 구문 도표의 목적이다.', '변수 값 변화 과정은 구문 도표가 아니라 실행 추적표 등으로 표현한다.', '목적 코드 생성 과정은 구문 도표와 관련이 없다.', '프로세스 상태 전이는 운영체제 과목에서 다루는 별개의 개념이다.']}
/>

<Quiz
  questionNumber={19}
  question="속성 문법(attribute grammar)에 대한 설명으로 옳은 것은?"
  options={['문맥 자유 문법에 각 문법 요소가 가지는 속성과 그 속성을 계산하는 규칙을 덧붙여 정적 의미까지 함께 표현하는 방법이다', '프로그램의 실행 속도를 빠르게 하기 위한 컴파일러 최적화 기법이다', '변수의 이름을 자동으로 생성해 주는 프로그램 자동화 도구다', '오직 동적 의미만을 표현하기 위해 고안된 표기법이다']}
  correctAnswer={1}
  explanation="속성 문법은 BNF 같은 문맥 자유 문법에 각 문법 요소(비단말기호)가 가지는 속성(attribute)과 그 속성 값을 계산하는 규칙을 추가함으로써, 형 일치 검사처럼 문맥 자유 문법만으로는 표현하기 어려운 정적 의미 규칙까지 함께 나타낼 수 있게 한 방법이다."
  optionExplanations={['정답이다. 문법에 속성과 계산 규칙을 덧붙여 정적 의미를 표현하는 것이 속성 문법의 핵심이다.', '속성 문법은 실행 속도 최적화 기법이 아니라 의미를 기술하는 형식적 방법이다.', '속성 문법은 변수 이름 자동 생성과 관련이 없다.', '속성 문법은 정적 의미(형 검사 등)를 표현하는 데 주로 쓰이며 동적 의미 전용 표기법이 아니다.']}
/>

<Quiz
  questionNumber={20}
  question="정적 의미(static semantics)에 해당하는 예로 가장 적절한 것은?"
  options={['프로그램 실행 중 배열의 인덱스가 범위를 벗어나 발생하는 오류', '변수를 선언하지 않고 사용했을 때 컴파일 시점에 발견되는 오류', '실행 중 0으로 나누기를 시도해 발생하는 오류', '실행 중 사용 가능한 메모리가 부족해 발생하는 오류']}
  correctAnswer={2}
  explanation="정적 의미는 프로그램을 실제로 실행하지 않고도 컴파일 시점에 검사할 수 있는 의미 규칙을 뜻한다. 변수를 선언하지 않고 사용하는 오류는 컴파일 시점에 발견되는 대표적인 정적 의미 위반 사례다."
  optionExplanations={['배열 인덱스 범위 초과는 실행 중에야 알 수 있는 동적 의미 오류다.', '정답이다. 선언되지 않은 변수 사용은 컴파일 시점에 발견되는 정적 의미 오류다.', '0으로 나누기는 실행 중 값에 따라 결정되는 동적 의미 오류다.', '메모리 부족은 실행 중 자원 상태에 따라 발생하는 동적 오류다.']}
/>

<Quiz
  questionNumber={21}
  question="동적 의미(dynamic semantics)에 대한 설명으로 옳지 않은 것은?"
  options={['프로그램이 실제로 실행되었을 때 각 문장이 어떤 동작을 하는지를 규정한다', '변수를 선언하지 않고 사용하는 오류처럼 실행 전에 발견되는 오류를 다룬다', '같은 구문이라도 언어마다 동적 의미가 다르게 정의될 수 있다', '동적 의미를 기술하는 방법에는 자연어 서술, 형식적 의미론 등이 있다']}
  correctAnswer={2}
  explanation="변수를 선언하지 않고 사용하는 오류는 컴파일 시점에 발견되는 정적 의미 오류다. 동적 의미는 프로그램이 실제로 실행되는 동안 나타나는 동작과 관련된 의미를 다룬다."
  optionExplanations={['옳은 설명이다. 실행되었을 때의 동작을 규정하는 것이 동적 의미의 정의다.', '정답이다. 이는 정적 의미에 대한 설명이며, 동적 의미와는 구분된다.', '옳은 설명이다. 같은 형태의 구문이라도 언어마다 실행 결과(동적 의미)가 다를 수 있다.', '옳은 설명이다. 동적 의미를 기술하는 방법에는 자연어 서술을 비롯한 여러 방식이 있다.']}
/>

<Quiz
  questionNumber={22}
  question="다음 중 문맥 자유 문법(context-free grammar)의 특징으로 가장 적절한 것은?"
  options={['한 비단말기호를 다른 기호들로 바꾸는 생성 규칙의 왼쪽에는 정확히 하나의 비단말기호만 올 수 있다', '생성 규칙 왼쪽에는 반드시 여러 개의 비단말기호가 함께 와야 한다', '단말기호만으로 이루어진 문법을 뜻한다', '실행 시점에만 적용되는 문법 규칙을 뜻한다']}
  correctAnswer={1}
  explanation="문맥 자유 문법은 생성 규칙의 왼쪽에 정확히 하나의 비단말기호만 오도록 제한한 문법으로, 그 비단말기호가 나타나는 문맥(주변 상황)과 무관하게 항상 같은 규칙으로 대체될 수 있다는 뜻에서 문맥 자유라 부른다. BNF는 이 문맥 자유 문법을 표기하는 대표적인 방법이다."
  optionExplanations={['정답이다. 생성 규칙 왼쪽에 비단말기호가 하나만 오는 것이 문맥 자유 문법의 정의다.', '왼쪽에 여러 비단말기호가 함께 오는 것은 오히려 문맥 자유 문법의 정의에 어긋난다.', '문맥 자유 문법은 단말기호와 비단말기호를 모두 포함하는 생성 규칙으로 구성된다.', '문맥 자유 문법은 구문(형태)을 정의하는 규칙이며 특정 시점(실행 시점)에만 적용되는 것이 아니다.']}
/>

<Quiz
  questionNumber={23}
  question="BNF에서 비단말기호(nonterminal)와 단말기호(terminal)에 대한 설명으로 옳지 않은 것은?"
  options={['비단말기호는 생성 규칙에 의해 다른 기호들로 더 대체될 수 있는 기호다', '단말기호는 더 이상 다른 기호로 대체되지 않는, 실제 문장에 나타나는 최종 기호다', '비단말기호는 대개 꺾쇠 괄호 없이 그냥 일반 단어로만 표기해야 한다', '하나의 생성 규칙은 비단말기호 하나를 단말기호와 비단말기호가 섞인 나열로 정의한다']}
  correctAnswer={3}
  explanation="비단말기호는 표기법에 따라 다양하게 나타낼 수 있으며, 전통적인 BNF 표기에서는 비단말기호를 꺾쇠 괄호 형태의 표기로 감싸 단말기호와 구분하는 방식이 흔히 쓰인다. 반드시 꺾쇠 괄호 표기를 쓰지 않아야 한다는 규칙이 있는 것은 아니다."
  optionExplanations={['옳은 설명이다. 비단말기호는 생성 규칙을 통해 계속 대체될 수 있는 기호다.', '옳은 설명이다. 단말기호는 더 이상 대체되지 않는 최종 기호다.', '정답이다. 비단말기호를 특정 괄호로 감싸 표기하는 관례가 있으며, 반드시 일반 단어로만 표기해야 한다는 규칙은 없다.', '옳은 설명이다. 생성 규칙은 비단말기호 하나를 기호들의 나열로 정의한다.']}
/>

<Quiz
  questionNumber={24}
  question="다음 상황을 정적 의미 오류와 동적 의미 오류로 올바르게 구분한 것은? (상황 1: 함수에 선언된 매개변수 개수보다 적은 개수의 인자를 넘겨 호출해 컴파일이 실패한 경우. 상황 2: 정수 배열에서 배열의 크기를 넘는 인덱스로 접근해 실행 중 오류가 발생한 경우)"
  options={['상황 1과 상황 2 모두 정적 의미 오류다', '상황 1은 정적 의미 오류이고, 상황 2는 동적 의미 오류다', '상황 1은 동적 의미 오류이고, 상황 2는 정적 의미 오류다', '상황 1과 상황 2 모두 동적 의미 오류다']}
  correctAnswer={2}
  explanation="상황 1은 인자 개수 불일치를 컴파일 시점에 확인해 컴파일이 실패하므로 정적 의미 오류다. 상황 2는 배열 인덱스가 실제로 실행되는 동안 어떤 값을 갖는지에 따라 결정되므로 실행 중에야 드러나는 동적 의미 오류다."
  optionExplanations={['상황 2는 실행 중에야 발견되는 오류이므로 정적 의미 오류로 볼 수 없다.', '정답이다. 컴파일 시점에 발견되는 상황 1은 정적 의미 오류, 실행 중에 발견되는 상황 2는 동적 의미 오류다.', '상황 1은 컴파일이 실패한 경우이므로 정적 의미 오류이며 동적 의미 오류가 아니다.', '상황 1은 컴파일 시점에 발견되는 정적 의미 오류이므로 동적 의미 오류로 분류할 수 없다.']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
