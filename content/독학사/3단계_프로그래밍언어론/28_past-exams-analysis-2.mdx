---
title: '28. 독학사 프로그래밍언어론 기출 분석 2 (코드·모델 중심)'
description: '식·할당·단락 평가, 활성화 레코드, 세마포어·모니터·메시지 전달, 예외 전파, 함수형·논리형 언어를 코드·모델 추적으로 재구성한 24문항과 풀이 전략'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 편은 국가평생교육진흥원 독학학위제 컴퓨터과학 전공 3단계 「프로그래밍언어론」의 **최신 출제기준(평가영역)** 중 식·할당·단락 평가, 하위프로그램 구현, 병행성, 예외 처리, 함수형·논리형 언어 영역을 근거로 새로 구성한 **코드·모델 중심 재구성 문항 24개**입니다. 실제 특정 회차의 기출 문항을 그대로 옮기거나 암기해 재현한 것이 아니며, 12~15·19~23편에서 다룬 개념을 실행 결과 추적 형태로 재구성했습니다. 실제 응시 시 문항 수·배점·시험 시간은 그해 시행 공고를 반드시 확인하시기 바랍니다. 아래 의사코드는 특정 프로그래밍언어의 실제 문법이 아니라 개념 비교를 위한 교육용 표기입니다.
</Callout>

## 풀이 전략과 이 편의 구성

코드·모델형 문제는 답을 외워서 맞히는 것이 불가능합니다. **실행 순서를 한 줄씩 손으로 따라가며 변수·스택·메시지의 상태를 직접 적어 보는 습관**이 유일한 대비법입니다. 아래 표는 24개 문항이 다루는 영역과, 풀이 시 반드시 확인해야 할 확인 포인트를 정리한 것입니다.

| 문항 번호 | 출제기준 영역 | 관련 편 | 풀이 시 확인 포인트 |
| --- | --- | --- | --- |
| 1–2 | 연산자 우선순위·결합법칙 | 12편 | 우선순위표를 암기가 아니라 유도로 확인 |
| 3–4 | l-value vs r-value, 할당 모델 | 12편 | 대입문 왼쪽·오른쪽의 역할 구분 |
| 5–7 | 단락 평가(short-circuit)와 부작용 | 12편 | 어느 피연산자가 실제로 평가되는지 |
| 8–9 | 활성화 레코드·재귀 스택 추적 | 15편 | 스택에 몇 개의 레코드가 동시에 있는지 |
| 10–11 | 세마포어·모니터 시나리오 | 19편 | P·V 순서, 조건변수 대기·신호 시점 |
| 12 | 메시지 전달 동기화 | 19편 | 메시지 전송·수신 순서 |
| 13–14 | 예외 처리(try-catch-finally)·전파 | 20편 | finally 실행 여부, 어느 핸들러가 잡는지 |
| 15 | 이벤트 핸들링 | 20편 | 콜백이 언제 호출되는지 |
| 16–17 | 람다·순수 함수·고차 함수 | 22편 | 부작용 유무, 함수를 값으로 전달하는지 |
| 18–19 | 불변성(immutability) | 22편 | 재할당 시도 시 어떤 일이 일어나는지 |
| 20–21 | Prolog 사실·규칙·단일화·백트래킹 | 23편 | 질의가 어떤 순서로 해를 찾는지 |
| 22–23 | 컴파일러·인터프리터의 오류 발견 시점 | 21편 | 정적 오류 vs 런타임 오류 |
| 24 | 재귀 vs 반복 종합 | 15·22편 | 스택 사용량과 실행 방식 비교 |

## 문제 1

<Quiz
  questionNumber={1}
  question="식 2 + 3 * 4에서 곱셈 연산자가 덧셈 연산자보다 우선순위가 높은 일반적인 언어 규칙을 적용할 때 이 식의 값은?"
  options={['14', '20', '9', '24']}
  correctAnswer={1}
  explanation="곱셈의 우선순위가 덧셈보다 높으므로 3 * 4를 먼저 계산해 12를 얻고, 그 다음 2를 더해 2 + 12 = 14가 됩니다."
  optionExplanations={['우선순위 규칙대로 곱셈을 먼저 계산한 값이므로 정답입니다.', '왼쪽에서 오른쪽으로 우선순위 없이 순서대로만 계산해 (2+3)*4를 구한 값으로, 우선순위 규칙을 무시한 오답입니다.', '2+3만 계산하고 뒤의 *4를 반영하지 않은 오답입니다.', '2*3*4처럼 덧셈을 곱셈으로 잘못 바꿔 계산한 오답입니다.']}
/>

## 문제 2

<Quiz
  questionNumber={2}
  question="뺄셈 연산자가 왼쪽 결합(left-associative)일 때, 식 10 - 3 - 2의 값은?"
  options={['5', '9', '-5', '15']}
  correctAnswer={1}
  explanation="왼쪽 결합은 같은 우선순위의 연산자를 왼쪽에서 오른쪽 순서로 묶어 계산한다는 뜻입니다. 즉 (10 - 3) - 2로 계산되어 먼저 10 - 3 = 7을 구하고, 다시 7 - 2 = 5가 됩니다."
  optionExplanations={['왼쪽 결합 규칙에 따라 (10-3)-2 순서로 계산한 값이므로 정답입니다.', '10 - (3 - 2)처럼 오른쪽 결합으로 잘못 계산한 값으로, 왼쪽 결합이라는 조건을 반영하지 않은 오답입니다.', '부호를 반대로 계산한 오답입니다.', '뺄셈을 덧셈으로 잘못 바꿔 10+3+2를 계산한 오답입니다.']}
/>

## 문제 3

다음 대입문에서 각 위치의 역할을 생각해 보자.

```text
x = y + 1
```

<Quiz
  questionNumber={3}
  question="이 대입문에서 l-value와 r-value에 대한 설명으로 가장 적절한 것은?"
  options={['x는 값이 저장될 메모리 위치를 가리키는 l-value로 쓰이고, y + 1은 계산되어 그 위치에 저장될 값을 나타내는 r-value로 쓰인다.', 'x와 y + 1은 완전히 같은 역할을 하므로 l-value·r-value 구분이 필요 없다.', 'l-value는 오직 상수만을 의미하고 r-value는 오직 변수만을 의미한다.', 'y + 1도 반드시 어떤 메모리 위치를 가리키는 l-value로만 쓰일 수 있다.']}
  correctAnswer={1}
  explanation="대입문의 왼쪽에 오는 x는 값이 저장될 저장 공간(메모리 위치)을 나타내야 하므로 l-value(left-value)로 쓰이고, 오른쪽의 y + 1은 계산되어 만들어지는 값 자체를 나타내므로 r-value(right-value)로 쓰입니다. l-value는 대입문의 왼쪽에, r-value는 오른쪽에 올 수 있는 것이 일반적인 구분입니다."
  optionExplanations={['왼쪽(저장 위치, l-value)과 오른쪽(계산된 값, r-value)의 역할 차이를 정확히 설명했으므로 정답입니다.', 'x는 저장 위치를, y+1은 계산된 값을 나타내는 서로 다른 역할을 하므로 완전히 같다는 설명은 틀렸습니다.', 'l-value·r-value 구분은 상수·변수 여부가 아니라 저장 위치를 나타내는지 값을 나타내는지에 관한 것이므로 틀렸습니다.', 'y + 1은 계산 결과인 값(r-value)이지 저장 위치를 가리키는 것이 아니므로 l-value로만 쓰인다는 설명은 틀렸습니다.']}
/>

## 문제 4

<Quiz
  questionNumber={4}
  question="일반적인 대입문에서 대입 연산자의 왼쪽에 y + 1처럼 계산된 값(r-value)만 놓았을 때 어떤 일이 일어나는가?"
  options={['값을 저장할 구체적인 메모리 위치가 지정되지 않으므로 컴파일 오류가 발생하는 것이 일반적이다.', '아무 문제 없이 정상적으로 실행되고 y + 1의 결과에 값이 성공적으로 저장된다.', '항상 y의 값이 자동으로 1만큼 증가한다.', 'y + 1은 자동으로 새로운 변수 이름으로 바뀐다.']}
  correctAnswer={1}
  explanation="대입문의 왼쪽에는 값을 저장할 수 있는 메모리 위치(l-value)가 와야 합니다. y + 1처럼 계산되어 나온 임시 값에는 다시 값을 저장할 고정된 위치가 없으므로, 대부분의 언어에서는 이런 코드를 컴파일 시점에 오류로 처리합니다."
  optionExplanations={['l-value가 아닌 계산된 값을 대입문 왼쪽에 놓아 저장 위치가 없다는 문제를 정확히 짚었으므로 정답입니다.', 'l-value 요구 조건을 만족하지 못하므로 정상적으로 실행되지 않는 것이 일반적이므로 틀렸습니다.', 'y + 1을 왼쪽에 놓는 것과 y의 값을 증가시키는 것은 서로 다른 문제이므로 자동으로 증가한다는 설명은 근거가 없어 틀렸습니다.', '언어가 임의로 표현식을 새로운 변수 이름으로 바꾸어 주지 않으므로 이 설명은 근거 없는 오답입니다.']}
/>

## 문제 5

`increment()`는 호출될 때마다 전역 변수 count를 1씩 늘리고 true를 반환하는 하위프로그램이라고 하자.

```text
전역 변수 count = 0

function increment():
    count = count + 1
    true를 반환한다

조건 = false and increment()   // 단락 평가를 지원하는 언어
```

<Quiz
  questionNumber={5}
  question="이 언어가 단락 평가(short-circuit evaluation)를 지원하는 and 연산자를 쓸 때, 위 문장을 실행한 뒤 count의 값은?"
  options={['0 (increment가 호출되지 않는다)', '1 (increment가 한 번 호출된다)', '2', '실행할 때마다 값이 달라진다']}
  correctAnswer={1}
  explanation="단락 평가를 지원하는 and 연산자는 왼쪽 피연산자가 false이면 이미 전체 결과가 false로 확정되므로, 오른쪽 피연산자는 아예 평가(실행)하지 않습니다. false and increment()에서 왼쪽이 false이므로 increment()는 호출되지 않고, count는 초기값 0 그대로 남습니다."
  optionExplanations={['단락 평가로 인해 오른쪽 피연산자가 평가되지 않아 increment가 호출되지 않는다는 것이 정답입니다.', '이 값은 increment가 호출되었다고 가정한 것으로, 단락 평가가 오른쪽 평가를 건너뛴다는 점을 반영하지 않은 오답입니다.', 'increment가 두 번 호출된다고 볼 근거가 코드 어디에도 없는 오답입니다.', '단락 평가 규칙은 왼쪽 피연산자의 값에 따라 결정적으로 정해지므로 값이 매번 달라지지 않아 오답입니다.']}
/>

## 문제 6

문제 5와 동일한 코드이지만, 이번에는 이 언어의 and 연산자가 단락 평가를 지원하지 않고 항상 양쪽 피연산자를 모두 평가한다고 가정한다.

<Quiz
  questionNumber={6}
  question="단락 평가를 지원하지 않는 and 연산자를 쓸 때, 위 문장을 실행한 뒤 count의 값은?"
  options={['1 (양쪽 피연산자가 모두 평가되어 increment가 호출된다)', '0', '2', '컴파일 오류가 발생해 실행되지 않는다']}
  correctAnswer={1}
  explanation="단락 평가를 지원하지 않으면 and 연산자는 왼쪽 결과가 이미 false로 확정되었더라도 오른쪽 피연산자인 increment()를 그대로 평가(호출)합니다. 따라서 increment가 한 번 호출되어 count가 0에서 1로 늘어납니다. 다만 이때도 and 연산의 최종 결과 자체는 false입니다(왼쪽이 false이므로)."
  optionExplanations={['단락 평가가 없으므로 오른쪽 피연산자가 그대로 호출되어 count가 1이 된다는 것이 정답입니다.', '이 값은 단락 평가가 있을 때(문제 5)의 결과이므로, 단락 평가가 없다고 가정한 이 문제의 답으로는 틀렸습니다.', 'increment는 이 문장에서 정확히 한 번만 나타나므로 두 번 호출된다고 볼 근거가 없어 틀렸습니다.', '단락 평가 여부는 문법 오류와 관계없는 의미론적 차이일 뿐이므로 컴파일 오류가 발생한다는 설명은 틀렸습니다.']}
/>

## 문제 7

<Quiz
  questionNumber={7}
  question="문제 5와 문제 6의 결과 차이가 실무에서 중요한 이유로 가장 적절한 것은?"
  options={['배열의 경계를 먼저 확인하고 그 다음에 배열 원소에 접근하는 조건문처럼, 뒤쪽 피연산자의 평가가 앞쪽 조건에 의존하는 코드에서는 단락 평가 여부에 따라 프로그램이 안전하게 동작할 수도, 오류(예: 잘못된 접근)가 발생할 수도 있기 때문이다.', '단락 평가 여부는 실행 결과에 어떠한 영향도 주지 않으므로 실무적으로 전혀 중요하지 않다.', '단락 평가는 오직 문자열 연산에서만 의미가 있는 개념이다.', '단락 평가 여부는 정수형 변수에서는 절대로 문제가 되지 않는다.']}
  correctAnswer={1}
  explanation="예를 들어 인덱스가 배열 범위 안에 있는지 먼저 확인한 뒤에만 배열 원소에 접근하도록 and로 연결한 조건문은, 단락 평가가 있어야 앞 조건이 거짓일 때 뒤의 위험한 접근을 건너뛸 수 있습니다. 단락 평가가 없는 언어에서 이런 패턴을 쓰면 범위를 벗어난 접근이 실제로 일어나 오류로 이어질 수 있습니다. 이 때문에 단락 평가 지원 여부는 코드의 안전성에 직접적인 영향을 줍니다."
  optionExplanations={['범위 확인 후 접근하는 대표적 패턴을 들어 단락 평가가 프로그램 안전성에 미치는 실제 영향을 정확히 설명했으므로 정답입니다.', '문제 5·6에서 count 값이 실제로 달라졌으므로 단락 평가 여부가 실행 결과에 영향을 주지 않는다는 설명은 틀렸습니다.', '단락 평가는 논리 연산 전반(불리언 조건 결합)에 적용되는 개념이며 문자열 연산에 국한되지 않으므로 틀렸습니다.', '단락 평가로 보호해야 하는 뒤쪽 연산은 배열 접근·나눗셈 등 자료형과 무관하게 다양하게 나타날 수 있으므로 정수형에는 문제가 되지 않는다는 설명은 틀렸습니다.']}
/>

## 문제 8

다음 재귀 함수는 n부터 1까지의 합을 구한다.

```text
function sumTo(n):
    만약 n <= 0이면 0을 반환한다
    아니면 n + sumTo(n - 1)을 반환한다

sumTo(3)을 호출한다
```

<Quiz
  questionNumber={8}
  question="sumTo(3) 호출 과정을 활성화 레코드가 쌓이는 순서대로 나열했을 때, sumTo(0)의 활성화 레코드가 반환값 0을 갖고 제거된 직후 상태에 대한 설명으로 가장 적절한 것은?"
  options={['스택에는 sumTo(1), sumTo(2), sumTo(3)의 활성화 레코드가 이 순서로 여전히 남아 있고, sumTo(1)의 활성화 레코드는 이제 1 + 0 = 1을 계산해 반환할 차례이다.', '스택에 있던 모든 활성화 레코드가 한꺼번에 동시에 제거된다.', 'sumTo(0)이 반환되면 sumTo(3)의 활성화 레코드도 함께 즉시 제거된다.', '스택에는 sumTo(3)의 활성화 레코드만 남고 나머지는 이미 모두 제거되어 있다.']}
  correctAnswer={1}
  explanation="sumTo(3)은 sumTo(2)를, sumTo(2)는 sumTo(1)을, sumTo(1)은 sumTo(0)을 차례로 호출하며 매번 새 활성화 레코드가 쌓입니다. sumTo(0)이 0을 반환하며 자신의 활성화 레코드만 제거되면, 그 아래에 쌓여 있던 sumTo(1), sumTo(2), sumTo(3)의 활성화 레코드는 그대로 스택에 남아 각자 자신의 반환값을 계산할 차례를 기다립니다. sumTo(1)은 이제 1 + (sumTo(0)의 결과인 0) = 1을 계산해 반환합니다."
  optionExplanations={['가장 안쪽 호출 하나만 제거되고 나머지 호출들이 스택에 순서대로 남아 각자 계산을 이어간다는 재귀 스택의 실제 동작을 정확히 설명했으므로 정답입니다.', '재귀 호출의 반환은 가장 안쪽부터 하나씩 순서대로 이루어지는 것이지 모든 레코드가 한꺼번에 제거되지 않으므로 틀렸습니다.', 'sumTo(3)은 sumTo(2)의 결과를 받아야 자신의 계산(3 + sumTo(2))을 마칠 수 있으므로, sumTo(0)이 반환되었다고 sumTo(3)까지 곧바로 제거되지 않아 틀렸습니다.', 'sumTo(1), sumTo(2)의 활성화 레코드는 아직 자신의 반환값을 계산하지 못했으므로 스택에 남아 있어야 하며, 이미 모두 제거되었다는 설명은 틀렸습니다.']}
/>

## 문제 9

<Quiz
  questionNumber={9}
  question="문제 8의 sumTo(3) 호출이 최종적으로 반환하는 값과, 그 값이 계산되는 순서로 가장 적절한 것은?"
  options={['최종 반환값은 6이며, 가장 안쪽 호출인 sumTo(0)의 결과(0)부터 시작해 sumTo(1)=1, sumTo(2)=1+2=3, sumTo(3)=3+3=6 순서로 바깥쪽 호출이 안쪽 호출의 결과를 받아 차례로 계산을 완성한다.', '최종 반환값은 3이며, 가장 바깥쪽 호출부터 값을 미리 정해 안쪽으로 전달한다.', '최종 반환값은 0이며, sumTo(0)의 결과만 그대로 사용된다.', '최종 반환값은 9이며, n의 값들을 모두 곱해서 계산한다.']}
  correctAnswer={1}
  explanation="재귀 호출은 가장 안쪽(기저 조건)부터 값이 확정되고, 그 값이 하나씩 바깥쪽 호출로 전달되며 계산이 완성됩니다. sumTo(0)=0, sumTo(1)=1+sumTo(0)=1, sumTo(2)=2+sumTo(1)=3, sumTo(3)=3+sumTo(2)=6이 되어 최종 반환값은 6입니다."
  optionExplanations={['안쪽에서 바깥쪽으로 값이 전달되며 1, 3, 6 순서로 계산이 완성되어 최종 6이 된다는 것을 정확히 계산했으므로 정답입니다.', '재귀는 안쪽 호출의 결과가 확정되어야 바깥쪽 호출이 계산을 마칠 수 있는 구조이므로 바깥쪽이 먼저 값을 정한다는 설명은 순서가 뒤바뀌어 틀렸습니다.', 'sumTo(0)의 결과 0은 sumTo(1)의 계산에 쓰이는 중간값일 뿐 최종 반환값이 아니므로 틀렸습니다.', 'sumTo 함수는 n부터 1까지 더하는 함수(덧셈)이지 곱하는 함수가 아니므로 곱셈으로 계산한 이 값은 틀렸습니다.']}
/>

## 문제 10

두 프로세스 A, B가 공유 세마포어 S(초기값 1)를 이용해 임계 구역에 접근한다고 하자.

```text
프로세스 A:
    P(S)
    임계 구역 실행
    V(S)

프로세스 B:
    P(S)
    임계 구역 실행
    V(S)
```

<Quiz
  questionNumber={10}
  question="A가 먼저 P(S)를 실행해 임계 구역에 진입한 상태에서 B가 P(S)를 시도하면 어떤 일이 일어나는가?"
  options={['S의 값이 이미 0이 되어 있으므로 B는 A가 V(S)를 실행해 S의 값을 다시 1로 늘릴 때까지 대기한다.', 'B는 S의 값과 무관하게 즉시 임계 구역에 함께 진입할 수 있다.', 'A의 P(S) 실행이 자동으로 취소되고 B가 대신 진입한다.', 'S의 값이 즉시 2로 증가해 두 프로세스 모두 자유롭게 진입한다.']}
  correctAnswer={1}
  explanation="세마포어 S의 초기값이 1이고 A가 P(S)를 실행하면 S는 0이 됩니다. 이 상태에서 B가 P(S)를 시도하면 S를 0 이하로 더 감소시켜야 하므로, 세마포어 규칙에 따라 B는 A가 V(S)로 S를 다시 1로 늘릴 때까지 대기하게 됩니다. 이것이 세마포어를 이용한 상호 배제의 핵심 동작입니다."
  optionExplanations={['S가 0인 상태에서 B가 대기하게 된다는 세마포어의 상호 배제 동작을 정확히 설명했으므로 정답입니다.', '세마포어의 목적 자체가 동시 진입을 막는 것이므로 B가 즉시 함께 진입할 수 있다는 설명은 상호 배제의 정의와 모순되어 틀렸습니다.', '세마포어 연산은 정해진 규칙(P는 감소, V는 증가)에 따라 결정적으로 동작할 뿐, 다른 프로세스의 진입을 이유로 임의로 취소되지 않으므로 틀렸습니다.', 'S는 P 연산으로 감소만 하고, V 연산이 실행되기 전까지는 값이 임의로 증가하지 않으므로 틀렸습니다.']}
/>

## 문제 11

모니터 안에 조건 변수(condition variable) `notFull`이 있고, 생산자 프로세스가 버퍼가 가득 찬 경우 `notFull.wait()`으로 대기하고, 소비자 프로세스가 버퍼에서 항목을 꺼낸 뒤 `notFull.signal()`로 대기 중인 생산자를 깨운다고 하자.

<Quiz
  questionNumber={11}
  question="생산자가 notFull.wait()을 호출했을 때 일반적으로 일어나는 일로 가장 적절한 것은?"
  options={['생산자는 모니터에 대한 배타적 점유를 내려놓고 대기 상태로 들어가, 나중에 다른 프로세스가 notFull.signal()을 호출할 때까지 재개되지 않는다.', '생산자는 대기하지 않고 즉시 다음 문장을 실행한다.', '생산자가 wait을 호출하면 모니터 전체가 영구적으로 잠기고 어떤 프로세스도 더 이상 모니터에 진입할 수 없다.', 'wait 호출은 소비자 프로세스를 즉시 종료시킨다.']}
  correctAnswer={1}
  explanation="모니터의 조건 변수에 대한 wait 호출은 호출한 프로세스가 그 조건이 만족될 때까지 기다리도록 대기시키는 연산입니다. 이때 중요한 점은, 대기하는 동안 모니터에 대한 배타적 점유(다른 프로세스가 모니터에 진입하지 못하게 막는 잠금)를 내려놓아, 다른 프로세스(여기서는 소비자)가 모니터에 들어와 필요한 작업을 하고 signal을 호출할 수 있게 해 준다는 것입니다."
  optionExplanations={['점유를 내려놓고 대기하며, signal이 호출될 때까지 재개되지 않는다는 조건 변수 wait의 실제 동작을 정확히 설명했으므로 정답입니다.', 'wait의 목적 자체가 조건이 만족될 때까지 대기하는 것이므로 즉시 다음 문장을 실행한다는 설명은 wait의 정의와 모순되어 틀렸습니다.', 'wait 호출 시 점유를 내려놓아야 다른 프로세스가 모니터에 들어와 signal을 호출할 수 있으므로, 모니터가 영구적으로 잠긴다는 설명은 모니터 동기화가 아예 작동하지 못하게 만들어 틀렸습니다.', 'wait은 호출한 프로세스 자신을 대기시키는 연산이며 다른 프로세스를 종료시키는 연산이 아니므로 틀렸습니다.']}
/>

## 문제 12

프로세스 P가 프로세스 Q에게 메시지를 보내고 응답을 기다리는 동기(synchronous) 메시지 전달 방식이 있다고 하자.

```text
프로세스 P:
    Q에게 요청 메시지를 보낸다
    Q로부터 응답 메시지를 받을 때까지 기다린다
    응답을 처리한다

프로세스 Q:
    P로부터 요청 메시지를 받을 때까지 기다린다
    요청을 처리한다
    P에게 응답 메시지를 보낸다
```

<Quiz
  questionNumber={12}
  question="이 동기 메시지 전달 방식에서 P가 요청 메시지를 보낸 직후의 상태로 가장 적절한 것은?"
  options={['P는 Q로부터 응답이 올 때까지 다음 문장(응답 처리)으로 넘어가지 않고 대기한다.', 'P는 메시지를 보내자마자 Q의 응답을 기다리지 않고 곧바로 응답을 처리하는 문장을 실행한다.', 'P가 메시지를 보내는 순간 Q는 자동으로 종료된다.', '메시지를 보낸 이후에는 Q가 그 메시지를 받을 필요가 없어진다.']}
  correctAnswer={1}
  explanation="동기 메시지 전달에서는 송신자(P)가 수신자(Q)의 응답을 받을 때까지 자신의 다음 동작을 진행하지 않고 대기하는 것이 일반적인 정의입니다. 이 덕분에 P는 Q가 실제로 요청을 처리하고 응답을 보낼 때까지 안전하게 기다렸다가 그 응답을 바로 이어서 처리할 수 있습니다."
  optionExplanations={['동기 방식에서 송신자가 응답을 받을 때까지 대기한다는 정의를 정확히 설명했으므로 정답입니다.', '응답을 기다리지 않고 곧바로 다음 문장을 실행하는 것은 비동기(asynchronous) 메시지 전달의 특징이므로, 동기 방식을 가정한 이 문제에는 맞지 않아 틀렸습니다.', '메시지를 보내는 것과 수신자 프로세스의 종료는 서로 별개의 사건이므로 이런 인과관계는 성립하지 않아 틀렸습니다.', 'Q는 P로부터 요청 메시지를 받아야 그에 따른 처리와 응답을 할 수 있으므로 받을 필요가 없어진다는 설명은 틀렸습니다.']}
/>

## 문제 13

다음과 같은 예외 처리 구조가 있다고 하자.

```text
try:
    1을 출력한다
    0으로 나누기를 시도해 예외를 발생시킨다
    2를 출력한다   // 예외 때문에 이 줄은 실행되지 않는다
catch (DivideByZeroException e):
    3을 출력한다
finally:
    4를 출력한다
5를 출력한다
```

<Quiz
  questionNumber={13}
  question="이 코드를 실행했을 때 출력되는 순서로 가장 적절한 것은?"
  options={['1, 3, 4, 5', '1, 2, 3, 4, 5', '1, 4, 5', '1, 3, 5']}
  correctAnswer={1}
  explanation="try 블록에서 1을 출력한 뒤 0으로 나누기에서 예외가 발생하므로, 그 아래의 2를 출력하는 문장은 건너뜁니다. 발생한 예외 형과 일치하는 catch 블록이 실행되어 3을 출력합니다. finally 블록은 예외 발생 여부와 관계없이 항상 실행되므로 4를 출력합니다. 그 다음 try-catch-finally 구조 이후의 문장인 5를 출력합니다."
  optionExplanations={['예외 발생으로 2를 건너뛰고, catch에서 3, finally에서 4, 이후 5까지 순서대로 출력된다는 전체 흐름을 정확히 추적했으므로 정답입니다.', '2를 출력하는 문장은 예외가 발생한 이후에 있으므로 실행되지 않아야 하는데 포함시킨 오답입니다.', 'catch 블록이 실행되지 않았다고 본 오답으로, 예외와 일치하는 catch가 있으므로 3이 출력되어야 함을 놓쳤습니다.', 'finally 블록이 실행되지 않았다고 본 오답으로, finally는 예외 발생 여부와 관계없이 항상 실행되어야 합니다.']}
/>

## 문제 14

다음은 세 겹으로 중첩된 하위프로그램 호출에서 예외가 발생하는 상황이다.

```text
procedure outer():
    try:
        call middle()
    catch (MyException e):
        A를 출력한다

procedure middle():
    call inner()   // middle에는 이 예외를 처리하는 catch가 없다

procedure inner():
    MyException을 발생시킨다
```

<Quiz
  questionNumber={14}
  question="inner에서 발생한 예외가 최종적으로 처리되는 위치와 출력 결과로 가장 적절한 것은?"
  options={['inner와 middle에는 이 예외를 처리할 핸들러가 없으므로 예외가 호출 체인을 따라 outer까지 전파되고, outer의 catch에서 처리되어 A가 출력된다.', 'inner 자신이 예외를 발생시켰으므로 inner 안에서 즉시 처리되어야 하며 middle과 outer에는 아무 영향이 없다.', '예외는 middle에서 처리되지 않으면 그대로 소멸되어 outer까지 전파되지 않는다.', '예외가 발생하면 outer, middle, inner가 모두 동시에 A를 출력한다.']}
  correctAnswer={1}
  explanation="inner에서 발생한 예외를 inner 자신이 처리하지 않고, 이를 호출한 middle에도 이 예외를 처리하는 catch가 없으므로, 예외는 호출 체인을 따라 middle을 호출한 outer까지 전파됩니다. outer에는 MyException을 처리하는 catch가 있으므로 여기서 비로소 처리되어 A가 출력됩니다. 이는 문제 6편(27편) 문제 22에서 다룬 예외 전파의 대표적인 사례입니다."
  optionExplanations={['핸들러가 없는 단계를 거쳐 outer까지 전파되어 그곳에서 처리된다는 예외 전파의 실제 동작을 정확히 설명했으므로 정답입니다.', '예외를 처리할 핸들러가 없는 inner 자신이 반드시 처리해야 한다는 규칙은 없으며, 오히려 상위로 전파되는 것이 예외 처리의 핵심 메커니즘이므로 틀렸습니다.', '예외 전파는 처리할 핸들러를 찾을 때까지 계속 상위로 전달되는 과정이므로, 중간에 핸들러가 없다고 예외가 소멸된다는 설명은 틀렸습니다.', '예외는 하나의 핸들러에서 한 번만 처리되는 것이 일반적이므로 세 하위프로그램이 동시에 출력한다는 설명은 예외 처리 모델과 맞지 않아 틀렸습니다.']}
/>

## 문제 15

버튼 클릭 시 실행될 콜백 함수를 등록해 두는 이벤트 기반 프로그램이 있다고 하자.

```text
버튼.onClick(콜백함수)로 등록해 둔다
(프로그램은 여기서 사용자의 입력을 기다린다)
... 시간이 흐른 뒤 사용자가 버튼을 클릭한다 ...
```

<Quiz
  questionNumber={15}
  question="이벤트 기반 프로그래밍에서 콜백함수가 실제로 실행되는 시점으로 가장 적절한 것은?"
  options={['등록 코드가 실행되는 시점이 아니라, 실제로 그 이벤트(버튼 클릭)가 발생한 시점에 이벤트 처리기(런타임)에 의해 호출된다.', '콜백함수는 onClick으로 등록하는 바로 그 순간에 즉시 실행되고, 이후 버튼 클릭과는 무관해진다.', '콜백함수는 프로그램이 완전히 종료된 이후에만 실행될 수 있다.', '콜백함수는 등록한 순서와 관계없이 항상 무작위한 시점에 실행된다.']}
  correctAnswer={1}
  explanation="이벤트 핸들링에서 콜백함수를 등록하는 코드(onClick 호출)는 함수를 나중에 실행하도록 예약해 두는 것일 뿐, 그 자리에서 즉시 실행하는 것이 아닙니다. 실제 실행은 프로그램의 이벤트 처리 메커니즘이 해당 이벤트(버튼 클릭)가 실제로 발생했음을 감지했을 때 이루어집니다."
  optionExplanations={['등록과 실행이 분리되어, 실제 이벤트 발생 시점에 실행된다는 이벤트 핸들링의 핵심을 정확히 설명했으므로 정답입니다.', '등록 시점에는 콜백을 나중에 실행하도록 연결해 두는 것뿐이며 즉시 실행되지 않으므로 틀렸습니다.', '콜백함수는 프로그램이 실행되는 중 이벤트가 발생했을 때 호출되는 것이 일반적이므로 프로그램 종료 이후로 국한된다는 설명은 틀렸습니다.', '콜백함수는 그 이벤트가 실제로 발생하는 시점에 대응해 결정적으로 호출되는 것이지 무작위 시점에 실행되는 것이 아니므로 틀렸습니다.']}
/>

## 문제 16

<Quiz
  questionNumber={16}
  question="다음 두 함수 중 순수 함수(pure function)로 볼 수 있는 것은? (double은 입력값을 2배로 만들어 반환하고, addAndLog는 입력값에 1을 더해 반환하면서 동시에 전역 로그 목록에 실행 기록을 추가한다)"
  options={['double(x)는 입력값만으로 결과가 결정되고 외부 상태를 바꾸지 않으므로 순수 함수이지만, addAndLog(x)는 전역 로그 목록이라는 외부 상태를 변경하는 부작용이 있으므로 순수 함수가 아니다.', 'double과 addAndLog 둘 다 반환값이 있으므로 둘 다 순수 함수이다.', 'addAndLog가 로그를 남기는 것은 부작용이 아니라 필수적인 계산의 일부이므로 순수 함수로 인정된다.', 'double은 곱셈을 사용하므로 순수 함수가 될 수 없다.']}
  correctAnswer={1}
  explanation="double은 같은 입력에 항상 같은 출력을 내고 외부 상태를 전혀 바꾸지 않으므로 순수 함수의 조건을 만족합니다. addAndLog는 계산 결과를 반환하는 것 외에 전역 로그 목록이라는 외부 상태를 변경하는 부작용이 있으므로, 반환값이 결정적이더라도 순수 함수가 아닙니다."
  optionExplanations={['외부 상태 변경 여부를 기준으로 double은 순수, addAndLog는 비순수로 정확히 구분했으므로 정답입니다.', '반환값이 있다는 것만으로 순수 함수가 되는 것이 아니라 부작용이 없어야 하므로, addAndLog까지 순수하다고 본 것은 틀렸습니다.', '전역 상태(로그 목록)를 변경하는 것은 그 자체로 부작용의 정의에 해당하므로, 계산의 일부라 해도 순수 함수 조건을 깨뜨려 틀렸습니다.', '곱셈 같은 순수한 계산 연산 자체는 부작용이 아니므로, 곱셈을 쓴다고 순수 함수가 될 수 없다는 설명은 근거가 없어 틀렸습니다.']}
/>

## 문제 17

고차 함수(higher-order function)의 대표적인 예로 리스트의 각 원소에 함수를 적용하는 map 연산이 있다. 리스트 [1, 2, 3]에 double 함수를 map으로 적용한다고 하자.

<Quiz
  questionNumber={17}
  question="map(double, [1, 2, 3])의 결과로 가장 적절한 것은? (double은 입력을 2배로 만들어 반환하는 순수 함수이다)"
  options={['[2, 4, 6]', '[1, 2, 3]', '12', '[1, 4, 9]']}
  correctAnswer={1}
  explanation="map은 주어진 함수(double)를 리스트의 각 원소에 하나씩 적용해 새로운 리스트를 만드는 고차 함수입니다. 1, 2, 3에 각각 double을 적용하면 2, 4, 6이 되므로 결과는 [2, 4, 6]입니다. map 자체는 함수를 인자로 받는다는 점에서 고차 함수의 대표 사례입니다."
  optionExplanations={['각 원소에 double을 적용한 결과를 정확히 계산했으므로 정답입니다.', '이 값은 double을 전혀 적용하지 않은 원래 리스트이므로 map 연산이 반영되지 않은 오답입니다.', '이 값은 리스트 원소를 모두 더하고 2를 곱한 것과 같은 값으로, map이 리스트를 반환한다는 사실과 맞지 않는 오답입니다.', '이 값은 각 원소를 제곱한 결과로, double(2배)이 아니라 다른 함수를 적용한 것으로 착각한 오답입니다.']}
/>

## 문제 18

불변(immutable) 값을 사용하는 함수형 스타일의 언어에서 다음과 같은 코드를 작성했다고 하자.

```text
x = 5   // 한 번 값이 정해지면 다시 바꿀 수 없는 불변 바인딩
x = x + 1   // 같은 이름 x에 다시 대입을 시도
```

<Quiz
  questionNumber={18}
  question="완전히 불변(immutable) 바인딩만 허용하는 언어에서 두 번째 줄과 같은 재대입을 시도하면 일반적으로 어떤 일이 일어나는가?"
  options={['이미 값이 정해진 이름에 다시 값을 대입하려는 시도이므로 오류로 처리되거나, 언어에 따라 새로운 이름을 도입하는 것으로 다르게 해석된다.', '아무 문제 없이 x의 값이 5에서 6으로 자유롭게 바뀐다.', '불변 언어에서는 애초에 변수라는 개념 자체를 전혀 쓸 수 없다.', 'x = x + 1은 항상 무한 루프에 빠진다.']}
  correctAnswer={1}
  explanation="불변 바인딩은 한 번 값이 정해지면 그 이름에 대해 다시 값을 바꿀 수 없다는 원칙입니다. 완전히 불변만 허용하는 언어에서 같은 이름에 다시 대입을 시도하면 오류로 처리되는 경우가 많고, 일부 언어는 이를 재대입이 아니라 같은 이름을 가리는 새로운 바인딩을 만드는 것으로 다르게 취급하기도 합니다. 어느 쪽이든 기존 값 5가 그 자리에서 그대로 6으로 바뀌는 전통적 의미의 변수 대입과는 다릅니다."
  optionExplanations={['불변 바인딩에 대한 재대입 시도가 오류이거나 새로운 바인딩으로 다르게 처리된다는 것을 정확히 설명했으므로 정답입니다.', '불변 바인딩의 정의 자체가 값을 자유롭게 바꿀 수 없다는 것이므로, 아무 문제 없이 값이 바뀐다는 설명은 정의와 모순되어 틀렸습니다.', '불변 언어에서도 이름과 값을 연결하는 바인딩이라는 개념 자체는 존재하며, 다만 그 값을 나중에 바꿀 수 없다는 제약이 있을 뿐이므로 틀렸습니다.', 'x = x + 1이라는 표현 자체가 반복 실행 구조를 갖고 있지 않으므로 무한 루프와는 관계가 없어 틀렸습니다.']}
/>

## 문제 19

<Quiz
  questionNumber={19}
  question="불변성(immutability)이 병행성(concurrency) 프로그래밍에서 갖는 장점으로 가장 적절한 것은?"
  options={['여러 스레드가 같은 값을 동시에 읽기만 할 뿐 아무도 그 값을 바꿀 수 없으므로, 값이 변경되어 생기는 경쟁 상태(race condition)를 원천적으로 줄일 수 있다.', '불변성은 병행성과 아무런 관련이 없는 개념이다.', '불변성을 적용하면 여러 스레드가 항상 서로의 실행을 완전히 막아 순차적으로만 실행되게 만든다.', '불변 값은 병행 프로그램에서 오직 한 번만 읽을 수 있고 두 번째 읽기부터는 오류가 발생한다.']}
  correctAnswer={1}
  explanation="여러 스레드가 같은 데이터에 동시에 접근할 때 문제가 되는 대표적 원인은 누군가 그 값을 바꾸는 동안 다른 누군가가 읽거나 쓰는 경쟁 상태입니다. 값이 아예 바뀌지 않는 불변 데이터라면 여러 스레드가 동시에 읽어도 항상 같은 값을 보게 되어 이런 경쟁 상태의 원인 자체가 크게 줄어듭니다. 이 때문에 함수형 언어의 불변성 원칙이 병행성 프로그래밍에서 자주 장점으로 언급됩니다."
  optionExplanations={['값이 바뀌지 않아 경쟁 상태의 원인이 줄어든다는 불변성과 병행성의 실제 관계를 정확히 설명했으므로 정답입니다.', '불변성은 오히려 병행 프로그래밍의 안전성 논의에서 자주 등장하는 중요한 개념이므로 관련이 없다는 설명은 틀렸습니다.', '불변성은 값이 바뀌지 않는다는 것이지 스레드의 실행 순서 자체를 강제로 순차화하는 것이 아니므로 틀렸습니다.', '불변 값은 여러 번 자유롭게 읽을 수 있는 것이 일반적이며, 다만 다시 쓸(변경할) 수 없을 뿐이므로 한 번만 읽을 수 있다는 설명은 틀렸습니다.']}
/>

## 문제 20

다음과 같은 Prolog 스타일의 사실과 규칙이 있다고 하자.

```text
사실: 부모(철수, 영희).
사실: 부모(영희, 민수).
규칙: 조부모(X, Y) :- 부모(X, Z), 부모(Z, Y).

질의: ?- 조부모(철수, 민수).
```

<Quiz
  questionNumber={20}
  question="이 질의 조부모(철수, 민수)에 대한 추론 과정과 결과로 가장 적절한 것은?"
  options={['조부모(X, Y)가 참이 되려면 어떤 Z에 대해 부모(X, Z)와 부모(Z, Y)가 모두 참이어야 하는데, X=철수, Y=민수, Z=영희로 두면 부모(철수, 영희)와 부모(영희, 민수)가 모두 사실로 존재하므로 단일화(unification)에 성공해 질의는 참이다.', '이 질의는 사실 목록에 조부모라는 사실이 직접 없으므로 무조건 거짓으로 실패한다.', '규칙은 사실과 결합해 추론하는 데 쓰일 수 없고 오직 사실끼리만 비교할 수 있다.', 'Z라는 변수는 규칙 안에서 어떤 값과도 단일화될 수 없는 고정된 상수이다.']}
  correctAnswer={1}
  explanation="Prolog의 추론 엔진은 질의 조부모(철수, 민수)를 규칙 조부모(X, Y) :- 부모(X, Z), 부모(Z, Y)와 맞추어(X=철수, Y=민수로 단일화) 몸체인 부모(철수, Z)와 부모(Z, 민수)를 모두 만족하는 Z를 찾습니다. 사실 목록에서 부모(철수, 영희)와 부모(영희, 민수)가 있으므로 Z=영희로 두면 두 조건이 모두 만족되어 단일화에 성공하고, 질의는 참으로 판정됩니다."
  optionExplanations={['변수 X, Y, Z에 대한 단일화 과정을 통해 규칙의 몸체가 사실들로 만족됨을 정확히 추적했으므로 정답입니다.', 'Prolog는 사실뿐 아니라 규칙을 이용한 추론으로도 질의를 참으로 판정할 수 있으므로, 사실 목록에 직접 없다고 무조건 거짓이라는 설명은 틀렸습니다.', '이 예시 자체가 규칙(조부모)이 여러 사실(부모)을 결합해 새로운 관계를 추론하는 데 쓰인 사례이므로, 규칙이 사실과 결합될 수 없다는 설명은 틀렸습니다.', 'Z는 규칙 안에서 특정 값(여기서는 영희)에 단일화될 수 있는 논리 변수이므로, 어떤 값과도 단일화될 수 없는 고정 상수라는 설명은 틀렸습니다.']}
/>

## 문제 21

이번에는 사실이 다음과 같이 추가되어 영희가 두 사람의 부모인 경우를 생각한다.

```text
사실: 부모(철수, 영희).
사실: 부모(영희, 민수).
사실: 부모(영희, 지수).
규칙: 조부모(X, Y) :- 부모(X, Z), 부모(Z, Y).

질의: ?- 조부모(철수, W).
```

<Quiz
  questionNumber={21}
  question="변수 W를 포함한 질의 조부모(철수, W)를 실행하면, Prolog의 백트래킹(backtracking)을 통해 얻을 수 있는 결과로 가장 적절한 것은?"
  options={['먼저 W=민수라는 해를 찾아 제시하고, 추가 해를 요구하면 백트래킹으로 다른 가능성을 다시 탐색해 W=지수라는 두 번째 해도 찾아낼 수 있다.', 'Prolog는 질의당 정확히 하나의 해만 찾을 수 있고 절대로 두 번째 해를 찾을 수 없다.', 'W는 반드시 철수와 같은 값으로만 단일화될 수 있다.', '사실이 여러 개로 늘어나면 오히려 이전에 찾은 해(민수)조차 더 이상 찾을 수 없게 된다.']}
  correctAnswer={1}
  explanation="영희가 민수와 지수 두 사람의 부모이므로, 조부모(철수, W) 질의는 Z=영희를 거쳐 W=민수, W=지수라는 두 가지 해를 모두 가질 수 있습니다. Prolog는 먼저 찾은 해(W=민수)를 제시한 뒤, 추가 해를 요구받으면 이전 선택 지점으로 되돌아가(백트래킹) 부모(영희, Z2)에서 다른 사실(부모(영희, 지수))을 시도해 두 번째 해 W=지수를 찾아낼 수 있습니다."
  optionExplanations={['백트래킹을 통해 첫 번째 해(민수)에 이어 두 번째 해(지수)까지 찾아낼 수 있다는 Prolog의 실제 동작을 정확히 설명했으므로 정답입니다.', 'Prolog는 백트래킹을 통해 조건을 만족하는 여러 해를 순차적으로 찾아낼 수 있으므로 하나의 해만 가능하다는 설명은 틀렸습니다.', 'W는 질의에서 아직 값이 정해지지 않은 변수로, 조건을 만족하는 민수나 지수와 단일화될 수 있으므로 철수로 고정된다는 설명은 틀렸습니다.', '사실이 추가되었다고 이전에 성립하던 부모(철수, 영희)와 부모(영희, 민수)라는 사실이 사라지는 것이 아니므로 첫 번째 해(민수)를 여전히 찾을 수 있어 틀렸습니다.']}
/>

## 문제 22

<Quiz
  questionNumber={22}
  question="정적 타입 언어에서 형이 맞지 않는 연산(예: 문자열과 정수의 부적절한 덧셈)을 컴파일러가 처리하는 방식과, 동적 타입 언어에서 같은 상황을 인터프리터가 처리하는 방식을 비교한 것으로 가장 적절한 것은?"
  options={['정적 타입 언어의 컴파일러는 실행 전 컴파일 시점에 형 불일치를 정적 오류로 검출해 실행 자체를 막는 경향이 있는 반면, 동적 타입 언어의 인터프리터는 그 코드가 실제로 실행되는 순간에야 값의 형을 보고 오류를 검출하는 런타임 오류로 처리하는 경향이 있다.', '정적 타입 언어와 동적 타입 언어는 형 오류를 검출하는 시점에 있어 완전히 동일하게 동작한다.', '동적 타입 언어는 어떤 형 오류도 절대로 검출할 수 없다.', '정적 타입 언어는 실행 중에는 어떠한 오류도 발생할 수 없도록 완전히 보장한다.']}
  correctAnswer={1}
  explanation="정적 타입 언어는 컴파일 시점에 변수와 식의 형을 분석해 형이 맞지 않는 연산을 정적 오류로 미리 찾아내는 경향이 있습니다. 반면 동적 타입 언어는 형이 실행 중 값에 따라 결정되므로, 형이 맞지 않는 연산은 그 코드가 실제로 실행되는 순간에야 런타임 오류로 드러나는 경향이 있습니다. 이는 09~10편에서 다룬 정적/동적 형 시스템의 차이가 오류 검출 시점에 그대로 반영된 사례입니다."
  optionExplanations={['컴파일 시점 정적 오류 검출(정적 타입)과 실행 시점 런타임 오류 검출(동적 타입)이라는 실제 차이를 정확히 설명했으므로 정답입니다.', '검출 시점(컴파일 전 vs 실행 중)이 서로 다르므로 완전히 동일하게 동작한다는 설명은 틀렸습니다.', '동적 타입 언어도 실행 중에 형이 맞지 않는 연산을 감지해 런타임 오류를 발생시킬 수 있으므로 절대 검출할 수 없다는 설명은 틀렸습니다.', '정적 타입 언어도 논리적 오류나 자원 부족 같은 다른 종류의 실행 중 오류는 얼마든지 발생할 수 있으므로 어떠한 오류도 발생할 수 없다는 설명은 지나친 단정이라 틀렸습니다.']}
/>

## 문제 23

<Quiz
  questionNumber={23}
  question="같은 문제(예: n까지의 합 구하기)를 재귀로 구현했을 때와 반복문(for나 while)으로 구현했을 때의 일반적인 차이로 가장 적절한 것은?"
  options={['재귀 구현은 호출마다 새로운 활성화 레코드를 스택에 쌓으므로 n이 매우 커지면 스택 사용량이 커져 스택 오버플로 위험이 있는 반면, 반복문 구현은 일반적으로 반복마다 새로운 활성화 레코드를 만들지 않고 같은 지역 변수들을 갱신하며 진행하므로 추가적인 스택 사용량이 거의 늘지 않는다.', '재귀와 반복문은 실행 시 스택 사용량 측면에서 항상 완전히 동일하다.', '반복문은 활성화 레코드라는 개념 자체와 전혀 관련이 없는 별도의 실행 모델을 사용한다.', '재귀는 항상 반복문보다 스택을 적게 사용한다.']}
  correctAnswer={1}
  explanation="재귀 호출은 호출할 때마다(문제 8~9에서 본 것처럼) 새로운 활성화 레코드를 스택에 추가로 쌓으므로, 호출 깊이(예: n)가 커지면 스택 사용량이 그에 비례해 늘어나 스택 오버플로 위험이 커집니다. 반면 반복문은 하나의 활성화 레코드(현재 실행 중인 하위프로그램의 레코드) 안에서 지역 변수 값을 계속 갱신하며 반복하므로, 반복 횟수가 늘어나도 추가적인 활성화 레코드가 쌓이지 않아 스택 사용량이 거의 일정하게 유지됩니다."
  optionExplanations={['재귀의 스택 누적과 반복문의 일정한 스택 사용량이라는 실제 차이를 정확히 설명했으므로 정답입니다.', '재귀는 호출 깊이만큼 스택을 추가로 사용하지만 반복문은 그렇지 않으므로 완전히 동일하다는 설명은 틀렸습니다.', '반복문도 그 반복문이 속한 하위프로그램의 활성화 레코드 안에서 실행되므로 활성화 레코드 개념과 무관하다는 설명은 틀렸습니다.', '오히려 재귀가 반복문보다 호출 깊이만큼 더 많은 스택을 사용하는 경향이 있으므로 재귀가 항상 스택을 적게 사용한다는 설명은 실제와 반대라 틀렸습니다.']}
/>

## 문제 24

<Quiz
  questionNumber={24}
  question="이 편에서 다룬 식·할당·단락 평가, 활성화 레코드, 병행성, 예외 처리, 함수형·논리형 언어를 종합했을 때 옳지 않은 설명은?"
  options={['단락 평가를 지원하는 and 연산자는 왼쪽 피연산자가 이미 결과를 확정지으면 오른쪽 피연산자를 평가하지 않을 수 있다.', '예외가 발생한 하위프로그램 자신에게 처리할 핸들러가 없으면 예외는 호출 체인을 따라 상위로 전파될 수 있다.', 'Prolog의 백트래킹은 하나의 해를 찾은 뒤에는 다른 가능한 해를 절대로 다시 탐색할 수 없다는 원칙을 따른다.', '재귀 호출은 호출 깊이만큼 활성화 레코드가 스택에 추가로 쌓이므로 반복문에 비해 스택 사용량이 늘어나는 경향이 있다.']}
  correctAnswer={3}
  explanation="Prolog의 백트래킹은 오히려 하나의 해를 찾은 뒤에도 추가 해를 요구받으면 이전 선택 지점으로 되돌아가 다른 가능성을 다시 탐색할 수 있다는 것이 핵심 특징입니다(문제 21 참고). 다른 해를 절대로 다시 탐색할 수 없다는 설명은 백트래킹의 정의와 정반대이므로 옳지 않습니다."
  optionExplanations={['단락 평가의 동작 방식은 문제 5~7에서 확인한 옳은 설명입니다.', '예외 전파의 동작 방식은 문제 14에서 확인한 옳은 설명입니다.', '백트래킹이 오히려 여러 해를 순차적으로 다시 탐색할 수 있게 해 주는 메커니즘이므로, 절대 다시 탐색할 수 없다는 이 설명이 옳지 않아 정답입니다.', '재귀와 반복문의 스택 사용량 차이는 문제 23에서 확인한 옳은 설명입니다.']}
/>

## 참고 자료

- 국가평생교육진흥원 독학학위제 — https://bdes.nile.or.kr
- SWI-Prolog Reference Manual, Unification and backtracking — https://www.swi-prolog.org/pldoc/man?section=unify
