---
title: '22. 독학사형 종합 연습 ① 관계모델·관계대수·기본 SQL'
description: '국가평생교육진흥원 출제기준에 근거해 재구성한 관계모델·키·무결성, 관계대수 기본·중급 연산, 단일 테이블·기본 조인 SQL 혼합 문제 22선'
---

<Callout type="warning">
  **이 문제 세트는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학학위제 4단계 데이터베이스 평가영역(출제기준) 중 관계 데이터 모델·관계대수·SQL 기본 항목을 근거로, 다루는 개념·계산 유형·오답 구성 방식을 참고해 지문과 데이터를 모두 새로 구성한 예상문제입니다. 실제 회차의 문항 수·배점·시험 시간은 매 공고마다 달라질 수 있으므로 **반드시 응시 회차의 공식 공고를 확인**하시기 바랍니다. 이 편은 06~08편, 12~13편에서 다룬 관계모델·관계대수·기본 SQL을 섞은 혼합 문제 22개로 구성했습니다.
</Callout>

관계대수 결과의 튜플 개수를 세는 문항과 조인·조건절 SQL의 실행 결과를 계산하는 문항은 해설에 계산 과정을 전부 풀어 두었습니다. 정답만 확인하지 말고, 표에 있는 데이터를 직접 손으로 짚어 가며 검산하는 방식으로 복습하시기 바랍니다.

## 문항 배분

| 영역 | 문항 수 | 문항 번호 |
|------|---------|-----------|
| 관계모델과 키·무결성 | 6 | 1–6 |
| 관계대수 기본 연산 | 7 | 7–13 |
| 관계대수 중급(그룹화·나누기·집합대응) | 3 | 14–16 |
| SQL 기본(단일 테이블·기본 조인, Oracle 문법) | 6 | 17–22 |
| 합계 | 22 | 1–22 |

다음 두 릴레이션은 7번부터 16번까지 관계대수 문항에서 공통으로 사용합니다.

**학생**(학번, 이름, 학년)

| 학번 | 이름 | 학년 |
|------|------|------|
| 1 | 정민 | 3 |
| 2 | 수아 | 2 |
| 3 | 도윤 | 3 |
| 4 | 하은 | 1 |

**수강**(학번, 과목)

| 학번 | 과목 |
|------|------|
| 1 | DB |
| 1 | OS |
| 2 | DB |
| 3 | NET |

## 관계모델과 키·무결성 (1–6번)

<Quiz
  questionNumber={1}
  question="키(key) 개념에 대한 설명으로 옳지 않은 것은?"
  options={['슈퍼키는 릴레이션 내의 각 튜플을 유일하게 식별할 수 있는 속성 또는 속성 집합이다.', '후보키는 슈퍼키 중에서 어떤 속성을 하나라도 제거하면 유일성이 깨지는 최소성을 만족하는 키다.', '대체키는 후보키 중 기본키로 선택되지 않은 나머지 후보키를 말한다.', '기본키로 선택된 속성은 서로 다른 튜플끼리 값이 같아도 무방하다.']}
  correctAnswer={4}
  explanation="기본키(primary key)는 후보키 중 하나를 대표로 선택한 것으로, 후보키의 정의인 유일성을 그대로 물려받는다. 따라서 기본키 값은 어떤 두 튜플 사이에서도 같을 수 없으며, 값이 같아도 무방하다는 설명은 기본키의 정의에 정면으로 어긋난다."
  optionExplanations={['옳은 설명이다. 슈퍼키의 정확한 정의다.', '옳은 설명이다. 후보키는 슈퍼키 중 최소성(minimality)까지 만족하는 키다.', '옳은 설명이다. 대체키는 후보키에서 기본키를 제외한 나머지다.', '정답. 기본키는 유일성을 반드시 만족해야 하므로 값이 같아도 된다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={2}
  question="외래키(foreign key)에 대한 설명으로 옳은 것은?"
  options={['외래키 컬럼명은 반드시 참조하는 테이블의 기본키 컬럼명과 동일해야 한다.', '외래키 값은 참조하는 테이블에 실제로 존재하는 기본키 값이거나 NULL이어야 한다.', '외래키가 설정된 컬럼에는 자동으로 유일성(UNIQUE) 제약도 함께 적용된다.', '하나의 테이블에는 외래키를 하나만 정의할 수 있다.']}
  correctAnswer={2}
  explanation="외래키는 다른 릴레이션의 기본키를 참조하는 속성으로, 그 값은 참조 대상 릴레이션에 실제로 존재하는 기본키 값과 일치하거나 NULL이어야 한다는 참조 무결성 제약을 따른다."
  optionExplanations={['외래키와 참조되는 기본키의 컬럼명이 같을 필요는 없다. 데이터 타입과 값의 의미만 대응되면 된다.', '정답. 참조 무결성 제약의 정확한 정의다.', '외래키 자체는 중복 값을 가질 수 있다(예: 여러 사원이 같은 부서번호를 외래키로 가짐). 유일성이 자동으로 붙지 않는다.', '한 테이블은 여러 개의 외래키를 가질 수 있다(예: 주문 테이블이 고객번호·상품번호를 각각 외래키로 가짐).']}
/>

<Quiz
  questionNumber={3}
  question="무결성 제약에 대한 설명으로 옳지 않은 것은?"
  options={['엔티티 무결성은 기본키를 구성하는 어떤 속성도 NULL 값을 가질 수 없다는 제약이다.', '참조 무결성은 외래키 값이 참조 대상 릴레이션에 존재하는 기본키 값이거나 NULL이어야 한다는 제약이다.', '도메인 무결성은 각 속성의 값이 그 속성에 정의된 데이터 타입과 값의 범위를 만족해야 한다는 제약이다.', '참조 무결성은 기본키에만 적용되는 제약이며 외래키와는 관련이 없다.']}
  correctAnswer={4}
  explanation="참조 무결성은 외래키와 그것이 참조하는 기본키 사이의 관계에 관한 제약이다. 외래키가 없다면 참조 무결성이라는 개념 자체가 성립하지 않으므로, 외래키와 관련이 없다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 엔티티 무결성의 정확한 정의다.', '옳은 설명이다. 참조 무결성의 정확한 정의다.', '옳은 설명이다. 도메인 무결성의 정확한 정의다.', '정답. 참조 무결성은 외래키가 있어야 성립하는 개념이므로 외래키와 무관하다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={4}
  question="다음 중 사용자 정의 무결성(user-defined integrity) 제약의 예로 가장 적절한 것은?"
  options={['급여 속성은 반드시 숫자 타입이어야 한다.', '학번 속성은 NULL 값을 가질 수 없다.', '재고 수량은 0 이상이어야 하며 특정 창고에 보관하는 상품은 항상 최소 10개 이상을 유지해야 한다는 업무 규칙을 만족해야 한다.', '외래키 값은 참조 테이블에 존재하는 값이어야 한다.']}
  correctAnswer={3}
  explanation="사용자 정의 무결성은 도메인·엔티티·참조 무결성 같은 일반적인 제약만으로는 표현할 수 없는, 해당 업무(도메인)에 특화된 규칙을 말한다. 특정 창고 상품의 최소 재고 수량처럼 조직의 업무 규칙을 반영한 제약이 대표적인 예다."
  optionExplanations={['데이터 타입 제약은 도메인 무결성의 예다.', 'NULL 금지는 엔티티 무결성의 예다(학번이 기본키인 경우).', '정답. 조직 고유의 업무 규칙을 반영한 제약이 사용자 정의 무결성의 대표적인 예다.', '외래키 값 존재 조건은 참조 무결성의 예다.']}
/>

<Quiz
  questionNumber={5}
  question="학생 테이블에 학번, 주민등록번호, 이름, 학과 속성이 있다. 학번과 주민등록번호는 각각 모든 튜플에서 유일한 값을 가지며, 이름과 학과는 여러 학생이 같은 값을 가질 수 있다. 이 테이블에서 후보키가 될 수 있는 속성으로 옳은 것은?"
  options={['이름', '학과', '학번', '이름과 학과를 합친 속성 집합']}
  correctAnswer={3}
  explanation="후보키는 릴레이션의 각 튜플을 유일하게 식별할 수 있어야 한다. 학번은 모든 튜플에서 값이 유일하므로 단독으로 후보키가 될 수 있다(주민등록번호도 마찬가지로 후보키가 될 수 있지만, 보기에는 학번만 제시되어 있다)."
  optionExplanations={['이름은 여러 학생이 같은 값을 가질 수 있으므로 단독으로 유일성을 보장하지 못해 후보키가 될 수 없다.', '학과 역시 여러 학생이 공유하는 값이므로 후보키가 될 수 없다.', '정답. 학번은 모든 튜플에서 값이 유일하므로 후보키의 조건을 만족한다.', '이름과 학과를 합쳐도 같은 학과의 동명이인이 존재할 수 있어 유일성이 보장되지 않는다.']}
/>

<Quiz
  questionNumber={6}
  question="부서 테이블의 부서번호를 사원 테이블이 외래키로 참조하고 있다. 그 부서번호를 사용하는 사원 레코드가 남아 있는 상태에서 해당 부서 레코드를 DELETE 문으로 삭제하려고 할 때 일어나는 일에 대한 설명으로 옳은 것은? (별도의 삭제 옵션은 지정하지 않았다고 가정한다)"
  options={['참조하는 사원 레코드가 있어도 데이터베이스는 이를 무시하고 부서 레코드 삭제를 정상적으로 수행한다.', 'Oracle은 참조 무결성 제약 위반 오류를 발생시키고 부서 레코드 삭제를 거부한다.', '외래키 제약이 있으면 부서 레코드를 삭제할 때 관련된 사원 레코드까지 항상 자동으로 함께 삭제된다.', '참조 무결성 제약은 삽입 연산에만 적용되고 삭제 연산에는 적용되지 않는다.']}
  correctAnswer={2}
  explanation="외래키 제약에 삭제 시 동작(ON DELETE CASCADE, ON DELETE SET NULL 등)을 별도로 지정하지 않으면 기본 동작은 참조하는 행이 남아 있는 부모 행의 삭제를 거부하는 것이다. Oracle은 이 경우 참조 무결성 제약 위반 오류를 발생시킨다."
  optionExplanations={['참조 무결성 제약이 걸려 있으면 삭제가 거부되므로 정상 수행된다는 설명은 옳지 않다.', '정답. 별도 옵션이 없으면 참조하는 자식 행이 있는 부모 행의 삭제는 거부된다.', '자동으로 자식 레코드까지 함께 삭제되는 것은 ON DELETE CASCADE를 명시적으로 지정했을 때만 일어나는 동작이다.', '참조 무결성은 삽입·수정·삭제 연산 모두에 적용되는 제약이다.']}
/>

## 관계대수 기본 연산 (7–13번)

<Quiz
  questionNumber={7}
  question="위 학생 릴레이션에 대해 셀렉션 연산 시그마(σ)를 이용한 σ학년=3(학생)을 수행한 결과의 튜플 개수는?"
  options={['1건', '2건', '3건', '4건']}
  correctAnswer={2}
  explanation="셀렉션 연산 σ학년=3(학생)은 학생 릴레이션에서 학년 속성 값이 3인 튜플만 골라낸다. 표에서 학년이 3인 튜플은 정민(학번 1)과 도윤(학번 3) 두 건이다."
  optionExplanations={['학년이 3인 튜플을 하나만 세면 나오는 값으로, 실제로는 두 건이 있다.', '정답. 정민과 도윤 두 튜플이 학년=3 조건을 만족한다.', '전체 4건 중 조건을 만족하지 않는 튜플까지 포함해 잘못 셌을 때 나오는 값이다.', '전체 튜플 수를 그대로 답으로 착각했을 때 나오는 값이다.']}
/>

<Quiz
  questionNumber={8}
  question="위 학생 릴레이션에 대해 프로젝션 연산 파이(π)를 이용한 π학년(학생)을 수행한 결과 릴레이션에 남는 튜플(값)의 개수는?"
  options={['2개', '3개', '4개', '1개']}
  correctAnswer={2}
  explanation="프로젝션은 지정한 속성만 남기고, 그 결과에서 중복된 튜플은 하나로 합친다. 학생 릴레이션의 학년 값은 3, 2, 3, 1이며, 여기서 중복(3이 두 번)을 제거하면 남는 값은 1, 2, 3 세 가지다."
  optionExplanations={['중복 제거를 지나치게 많이 해서 값 하나를 빠뜨렸을 때 나오는 값이다.', '정답. 중복된 3을 하나로 합치면 1, 2, 3 세 개의 서로 다른 값이 남는다.', '중복 제거를 하지 않고 원래 튜플 수를 그대로 답했을 때 나오는 값이다.', '거의 모든 값을 하나로 합쳐 버렸을 때 나오는 값이다.']}
/>

<Quiz
  questionNumber={9}
  question="합집합(∪)·교집합(∩)·차집합(−) 연산에 대한 설명으로 옳지 않은 것은?"
  options={['두 릴레이션에 합집합·교집합·차집합 연산을 적용하려면 두 릴레이션의 차수(속성 개수)가 같아야 한다.', '두 릴레이션에 합집합·교집합·차집합 연산을 적용하려면 대응되는 속성끼리 도메인이 같아야 한다.', '이 조건을 만족하는 두 릴레이션을 합집합 호환(union-compatible)이라고 부른다.', '카티전 곱(×) 연산도 합집합·교집합·차집합과 마찬가지로 두 릴레이션의 차수가 반드시 같아야 수행할 수 있다.']}
  correctAnswer={4}
  explanation="카티전 곱은 두 릴레이션의 모든 튜플 조합을 만드는 연산으로, 두 릴레이션의 차수(속성 개수)나 도메인이 서로 달라도 아무 제약 없이 수행할 수 있다. 차수가 같아야 한다는 조건은 합집합·교집합·차집합에만 해당한다."
  optionExplanations={['옳은 설명이다. 차수가 같아야 한다는 것이 합집합 호환의 조건 중 하나다.', '옳은 설명이다. 도메인이 같아야 한다는 것도 합집합 호환의 조건이다.', '옳은 설명이다. 이 두 조건을 만족하는 관계를 합집합 호환이라 부른다.', '정답. 카티전 곱은 차수가 달라도 수행할 수 있는 연산이므로 이 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={10}
  question="학생 릴레이션(4건)과 수강 릴레이션(4건)에 카티전 곱 연산 학생×수강을 수행하면 결과 릴레이션의 튜플 개수는?"
  options={['8건', '12건', '16건', '20건']}
  correctAnswer={3}
  explanation="카티전 곱의 결과 튜플 수는 두 릴레이션의 튜플 수를 곱한 값이다. 학생이 4건, 수강이 4건이므로 4×4=16건이다."
  optionExplanations={['두 릴레이션의 튜플 수를 더했을 때(4+4=8) 나오는 값으로, 곱셈이 아니라 덧셈으로 잘못 계산한 결과다.', '한쪽 튜플 수를 3으로 잘못 세었을 때(4×3) 나오는 값이다.', '정답. 4×4=16이다.', '한쪽 튜플 수를 5로 잘못 세었을 때(4×5) 나오는 값이다.']}
/>

<Quiz
  questionNumber={11}
  question="세타조인·동등조인·자연조인에 대한 설명으로 옳지 않은 것은?"
  options={['세타조인은 조인 조건에 등호(=)뿐 아니라 부등호 등 다양한 비교 연산자를 사용할 수 있는 가장 일반적인 형태의 조인이다.', '동등조인은 세타조인의 특수한 경우로, 조인 조건에 등호(=) 연산자만 사용하는 조인이다.', '자연조인은 두 릴레이션에서 이름이 같은 속성을 기준으로 동등조인을 수행한 뒤, 중복되는 속성을 결과에서 하나만 남기는 조인이다.', '세타조인은 조인 조건에 항상 등호(=) 연산자만 사용할 수 있으며 다른 비교 연산자는 허용되지 않는다.']}
  correctAnswer={4}
  explanation="세타(theta)조인의 세타는 등호뿐 아니라 부등호를 포함한 일반적인 비교 연산자를 뜻한다. 등호만 사용할 수 있다는 설명은 세타조인이 아니라 동등조인의 정의이며, 세타조인의 일반성을 정반대로 서술한 것이다."
  optionExplanations={['옳은 설명이다. 세타조인은 다양한 비교 연산자를 허용하는 가장 일반적인 조인이다.', '옳은 설명이다. 동등조인은 세타조인 중 등호만 쓰는 특수한 경우다.', '옳은 설명이다. 자연조인은 동등조인 후 중복 속성을 제거하는 조인이다.', '정답. 세타조인은 등호뿐 아니라 다양한 비교 연산자를 쓸 수 있으므로 등호만 허용된다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={12}
  question="위 학생 릴레이션과 수강 릴레이션을 학번 속성 기준으로 자연조인(⋈)한 결과 릴레이션의 튜플 개수는?"
  options={['3건', '4건', '5건', '6건']}
  correctAnswer={2}
  explanation="자연조인은 학번이 같은 튜플끼리 결합한다. 학번 1은 수강 릴레이션에 DB, OS 두 건이 있어 2건이 만들어지고, 학번 2는 DB 한 건, 학번 3은 NET 한 건이 만들어지며, 학번 4는 수강 릴레이션에 대응하는 행이 없어 결과에 포함되지 않는다. 따라서 2+1+1+0=4건이다."
  optionExplanations={['학번 1에서 만들어지는 두 건 중 하나를 빠뜨렸을 때 나오는 값이다.', '정답. 2(학번1)+1(학번2)+1(학번3)+0(학번4)=4건이다.', '수강 기록이 없는 학번 4도 결과에 포함된다고 잘못 계산했을 때(자연조인이 아니라 외부조인처럼 취급) 나오는 값이다.', '학번 1의 조인 결과를 실제보다 많이 세었을 때 나오는 값이다.']}
/>

<Quiz
  questionNumber={13}
  question="관계대수 연산의 성질에 대한 설명으로 옳지 않은 것은?"
  options={['셀렉션 연산은 교환 법칙이 성립하여 σc1(σc2(R))과 σc2(σc1(R))의 결과는 항상 같다.', '여러 개의 셀렉션 조건은 하나의 셀렉션 연산으로 합쳐 표현할 수 있다.', '프로젝션을 먼저 수행해 특정 속성을 제거한 뒤, 제거된 속성을 조건으로 사용하는 셀렉션을 그 결과에 적용해도 항상 정상적으로 동작한다.', '자연조인은 교환 법칙과 결합 법칙이 성립한다.']}
  correctAnswer={3}
  explanation="프로젝션으로 특정 속성을 먼저 제거하면, 그 속성은 결과 릴레이션에 더 이상 존재하지 않는다. 이후 셀렉션이 그 속성을 조건으로 사용하려 하면 속성 자체가 없으므로 연산이 성립하지 않는다. 즉, 프로젝션과 셀렉션의 순서는 셀렉션 조건에 쓰인 속성이 프로젝션 결과에 남아 있을 때만 바꿀 수 있다."
  optionExplanations={['옳은 설명이다. 셀렉션은 교환 법칙이 성립하는 대표적인 연산이다.', '옳은 설명이다. σc1(σc2(R))은 σ(c1 AND c2)(R)로 합칠 수 있다.', '정답. 프로젝션에서 제거된 속성을 이후 셀렉션 조건으로 쓰면 그 속성이 존재하지 않아 오류가 발생하므로, 항상 정상 동작한다는 설명은 옳지 않다.', '옳은 설명이다. 자연조인은 교환 법칙·결합 법칙이 모두 성립하는 연산이다.']}
/>

## 관계대수 중급: 그룹화·나누기·집합 대응 (14–16번)

<Quiz
  questionNumber={14}
  question="관계대수의 나누기(division, ÷) 연산에 대한 설명으로 옳은 것은?"
  options={['나누기 연산은 두 릴레이션의 모든 튜플 조합을 만들어 내는 연산이다.', '나누기 연산은 한 릴레이션에서 다른 릴레이션에 있는 값을 모두 만족(포함)하는 튜플만 골라내는 연산으로, 흔히 모든 항목을 만족하는 대상을 찾는 질의에 쓰인다.', '나누기 연산은 두 릴레이션 중 하나에만 있는 튜플을 찾는 차집합과 동일한 연산이다.', '나누기 연산은 항상 두 릴레이션의 차수가 같아야 수행할 수 있다.']}
  correctAnswer={2}
  explanation="나누기 연산은 예를 들어 수강 릴레이션을 특정 과목 집합으로 나누면, 그 과목들을 전부 수강한 학번만 결과로 남기는 방식으로 동작한다. 즉 모든 조건 값을 빠짐없이 만족하는 튜플을 찾을 때 쓰이는 연산이다."
  optionExplanations={['모든 튜플 조합을 만드는 것은 카티전 곱에 대한 설명이다.', '정답. 나누기는 모든 항목을 만족하는 대상을 찾는 전형적인 용도로 쓰인다.', '나누기는 차집합과 다른 연산으로, 한쪽 릴레이션의 모든 값을 만족하는지를 검사한다.', '나누기는 두 릴레이션의 차수가 다르며, 나누는 릴레이션의 속성이 나누어지는 릴레이션 속성의 부분집합이면 된다.']}
/>

<Quiz
  questionNumber={15}
  question="관계대수의 확장 연산인 그룹화-집계 연산(그리스 문자 감마, γ)에 대한 설명으로 옳지 않은 것은?"
  options={['특정 속성을 기준으로 튜플을 그룹으로 묶은 뒤, 각 그룹에 대해 합계·평균·개수 같은 집계 함수를 적용한다.', 'SQL의 GROUP BY 절과 집계 함수 조합에 대응되는 관계대수 연산이다.', '그룹화 기준 속성을 지정하지 않으면 릴레이션 전체를 하나의 그룹으로 보고 집계 함수를 적용할 수 있다.', '그룹화-집계 연산은 기본 연산(셀렉션·프로젝션·조인 등)만으로는 표현할 수 없으므로 관계대수에서 전혀 정의되지 않는다.']}
  correctAnswer={4}
  explanation="그룹화-집계 연산은 기본 연산만으로는 표현하기 어렵기 때문에 오히려 별도의 확장 연산(γ)으로 관계대수에 추가로 정의된 것이다. 정의되지 않는다는 설명은 사실과 반대다."
  optionExplanations={['옳은 설명이다. 그룹화 후 집계 함수를 적용하는 것이 이 연산의 핵심이다.', '옳은 설명이다. SQL의 GROUP BY와 대응된다.', '옳은 설명이다. 그룹화 기준이 없으면 전체를 하나의 그룹으로 취급한다.', '정답. 그룹화-집계는 표현이 어렵기 때문에 확장 연산으로 별도 정의된 것이지, 정의되지 않는 것이 아니다.']}
/>

<Quiz
  questionNumber={16}
  question="Oracle SQL의 집합 연산자와 관계대수 연산의 대응 관계에 대한 설명으로 옳지 않은 것은?"
  options={['UNION은 관계대수의 합집합(∪) 연산에 대응된다.', 'INTERSECT는 관계대수의 교집합(∩) 연산에 대응된다.', 'Oracle에서는 표준 SQL의 EXCEPT 대신 전통적으로 MINUS 연산자를 사용해 관계대수의 차집합(−) 연산을 표현해 왔다.', 'MINUS는 관계대수의 카티전 곱(×) 연산에 대응된다.']}
  correctAnswer={4}
  explanation="MINUS는 두 결과 집합에서 한쪽에만 있는 행을 남기는 차집합 연산이며, 관계대수의 차집합(−)에 대응된다. 카티전 곱은 조인 없이 두 릴레이션의 모든 튜플 조합을 만드는 전혀 다른 연산으로, MINUS와는 대응되지 않는다."
  optionExplanations={['옳은 설명이다. UNION과 합집합의 대응은 정확하다.', '옳은 설명이다. INTERSECT와 교집합의 대응도 정확하다.', '옳은 설명이다. Oracle은 오랫동안 EXCEPT 대신 MINUS라는 이름을 사용해 왔다.', '정답. MINUS는 카티전 곱이 아니라 차집합에 대응되므로 이 설명은 옳지 않다.']}
/>

## SQL 기본: 단일 테이블·기본 조인 (17–22번, Oracle 문법)

다음 두 테이블은 17번부터 22번까지 SQL 문항에서 공통으로 사용합니다.

**사원**(사번, 이름, 급여, 부서번호)

| 사번 | 이름 | 급여 | 부서번호 |
|------|------|------|----------|
| 1 | 철수 | 3000 | 10 |
| 2 | 영희 | 4500 | 20 |
| 3 | 민수 | 2800 | 10 |
| 4 | 지은 | 5200 | 20 |
| 5 | 현우 | 3900 | 30 |
| 6 | 수빈 | 3300 | 40 |

**부서**(부서번호, 부서명)

| 부서번호 | 부서명 |
|----------|--------|
| 10 | 영업 |
| 20 | 개발 |
| 30 | 인사 |

<Quiz
  questionNumber={17}
  question="Oracle에서 조회 결과 중 상위 몇 건만 가져오는 방법에 대한 설명으로 옳지 않은 것은?"
  options={['ROWNUM 의사 컬럼을 WHERE 절에서 활용해 상위 몇 건만 걸러낼 수 있다.', '12c 이상 버전에서는 FETCH FIRST n ROWS ONLY 구문으로 상위 n건만 조회할 수 있다.', 'Oracle은 표준 문법으로 LIMIT 절을 지원하며, 이를 SELECT문 맨 끝에 붙이면 상위 몇 건만 조회된다.', '정렬 기준 없이 ROWNUM만 사용하면 정렬 순서가 보장되지 않은 임의의 몇 건이 반환될 수 있다.']}
  correctAnswer={3}
  explanation="LIMIT 절은 MySQL·PostgreSQL 등에서 지원되는 문법이며, Oracle은 전통적으로 이를 지원하지 않는다. Oracle에서 상위 N건을 가져오려면 ROWNUM 의사 컬럼을 사용하거나, 12c 이상에서는 FETCH FIRST n ROWS ONLY 구문을 사용해야 한다."
  optionExplanations={['옳은 설명이다. ROWNUM은 Oracle에서 상위 N건을 제한하는 전통적인 방법이다.', '옳은 설명이다. FETCH FIRST n ROWS ONLY는 Oracle 12c 이상에서 지원하는 표준 방식이다.', '정답. Oracle은 LIMIT 절을 지원하지 않으므로 이 설명은 옳지 않다.', '옳은 설명이다. ORDER BY 없이 ROWNUM만 쓰면 정렬이 보장되지 않는 임의의 행이 먼저 걸릴 수 있다.']}
/>

<Quiz
  questionNumber={18}
  question="Oracle에서 NULL 값 비교와 산술 연산에 대한 설명으로 옳지 않은 것은?"
  options={['컬럼값이 NULL인 행을 찾으려면 WHERE 컬럼명 IS NULL 조건을 사용해야 한다.', 'NULL이 아닌 값을 찾으려면 WHERE 컬럼명 IS NOT NULL 조건을 사용해야 한다.', 'WHERE 컬럼명 = NULL 조건으로 NULL 값을 가진 행을 정상적으로 찾을 수 있다.', 'NULL 값이 포함된 산술식(예: NULL 더하기 1000)의 결과는 일반적으로 NULL이 된다.']}
  correctAnswer={3}
  explanation="Oracle을 비롯한 대부분의 관계형 DBMS에서 등호(=) 연산자는 NULL과의 비교에 사용할 수 없다. NULL은 값이 아니라 알 수 없음을 나타내므로, WHERE 컬럼명 = NULL 조건은 아무 행도 찾아내지 못한다. NULL을 찾으려면 반드시 IS NULL을 사용해야 한다."
  optionExplanations={['옳은 설명이다. IS NULL이 NULL 값을 찾는 올바른 방법이다.', '옳은 설명이다. IS NOT NULL이 NULL이 아닌 값을 찾는 올바른 방법이다.', '정답. 등호(=)로는 NULL을 비교할 수 없으므로 이 조건으로는 행을 찾지 못한다.', '옳은 설명이다. NULL이 포함된 산술식의 결과는 일반적으로 NULL로 전파된다.']}
/>

<Quiz
  questionNumber={19}
  question="INNER JOIN과 OUTER JOIN에 대한 설명으로 옳지 않은 것은?"
  options={['INNER JOIN은 두 테이블에서 조인 조건을 만족하는 행만 결과로 반환한다.', 'LEFT OUTER JOIN은 왼쪽 테이블의 모든 행을 반환하며, 오른쪽 테이블에 대응하는 값이 없으면 그 부분을 NULL로 채운다.', 'FULL OUTER JOIN은 양쪽 테이블 중 어느 한쪽에서라도 대응하는 행이 없어도 양쪽의 모든 행을 결과에 포함한다.', 'INNER JOIN은 조인 조건을 만족하지 않아도 양쪽 테이블의 모든 행을 결과에 포함한다.']}
  correctAnswer={4}
  explanation="INNER JOIN의 핵심은 조인 조건을 만족하는 행만 결과에 남기는 것이다. 조건을 만족하지 않아도 모든 행을 포함하는 것은 INNER JOIN이 아니라 카티전 곱이나 OUTER JOIN의 특징이다."
  optionExplanations={['옳은 설명이다. INNER JOIN의 정확한 정의다.', '옳은 설명이다. LEFT OUTER JOIN의 정확한 동작이다.', '옳은 설명이다. FULL OUTER JOIN의 정확한 동작이다.', '정답. INNER JOIN은 조건을 만족하는 행만 남기므로 모든 행을 포함한다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={20}
  question="위 사원 테이블에 대해 다음 질의를 실행하면 반환되는 행 수는? SELECT COUNT 함수를 이용해 급여가 3000을 초과하는 사원의 수를 세는 질의다."
  options={['2명', '3명', '4명', '5명']}
  correctAnswer={3}
  explanation="급여가 3000을 초과하는 사원을 표에서 찾으면 영희(4500), 지은(5200), 현우(3900), 수빈(3300)까지 총 4명이다. 다만 이 문항은 급여 3000 초과 조건만 적용한 결과이므로, 3000 정확히 초과하는 인원은 영희·지은·현우·수빈 4명이 맞다. 3000인 철수는 초과가 아니라 같은 값이므로 제외되고, 2800인 민수도 제외된다."
  optionExplanations={['조건을 만족하는 사원 중 두 명만 세었을 때 나오는 값으로, 실제로는 4명이 조건을 만족한다.', '조건을 만족하는 사원 중 한 명을 빠뜨렸을 때 나오는 값이다.', '정답. 영희(4500), 지은(5200), 현우(3900), 수빈(3300) 네 명의 급여가 3000을 초과한다.', '급여가 3000인 철수까지 포함해 잘못 세었을 때 나오는 값이다(철수는 초과가 아니라 동일이므로 제외되어야 한다).']}
/>

<Quiz
  questionNumber={21}
  question="위 사원 테이블과 부서 테이블을 부서번호 기준으로 INNER JOIN한 결과의 행 수는? (수빈의 부서번호 40은 부서 테이블에 존재하지 않는다)"
  options={['4건', '5건', '6건', '7건']}
  correctAnswer={2}
  explanation="INNER JOIN은 두 테이블에서 부서번호가 일치하는 행만 결과에 남긴다. 사원 6명 중 철수(10)·영희(20)·민수(10)·지은(20)·현우(30)는 부서 테이블에 대응하는 부서번호가 있어 조인되지만, 수빈(40)은 부서 테이블에 40번 부서가 없어 결과에서 제외된다. 따라서 6명 중 5명만 조인 결과에 남는다."
  optionExplanations={['조건을 만족하는 행을 하나 더 빠뜨렸을 때 나오는 값이다.', '정답. 부서번호가 일치하지 않는 수빈만 제외되므로 6명 중 5명이 남는다.', '수빈까지 포함해 전체 사원 수를 그대로 답했을 때 나오는 값으로, INNER JOIN에서는 대응하는 부서가 없는 행은 제외되어야 한다.', '실제 사원 수보다 많은 값으로, 계산 과정에서 존재하지 않는 행을 추가했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={22}
  question="위 사원 테이블에서 특정 사원의 급여 컬럼 값이 NULL이라고 할 때, 급여에 1000을 더하는 산술식(급여+1000)을 SELECT하면 어떤 값이 반환되는가?"
  options={['NULL', '1000', '오류가 발생하여 질의 실행이 중단된다.', '0']}
  correctAnswer={1}
  explanation="Oracle에서 NULL은 알 수 없는 값을 의미하며, NULL이 포함된 산술 연산의 결과는 일반적으로 NULL로 전파된다. 급여가 NULL이면 급여에 얼마를 더하든 결과는 여전히 NULL이다."
  optionExplanations={['정답. NULL이 포함된 산술 연산의 결과는 NULL로 전파된다.', 'NULL을 0으로 취급해 더했을 때 나오는 값으로, 실제로는 NULL은 0이 아니라 알 수 없음을 뜻한다.', 'NULL이 포함된 산술식은 오류를 내지 않고 NULL을 반환하며 질의 실행 자체는 정상적으로 끝난다.', 'NULL을 0으로 취급했을 때 나오는 값으로, NULL과 0은 서로 다른 개념이다.']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
- [Oracle SQL Language Reference](https://docs.oracle.com/en/database/oracle/oracle-database/index.html)
