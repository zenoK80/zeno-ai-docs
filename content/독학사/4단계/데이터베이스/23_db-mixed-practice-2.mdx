---
title: '23. 독학사형 종합 연습 ② 정규화·ER 사상·고급 SQL'
description: '국가평생교육진흥원 출제기준에 근거해 재구성한 함수적 종속·정규화(1NF~BCNF·4NF·5NF), ER/EER→관계 사상, 그룹화·뷰·권한·고급 서브쿼리 혼합 문제 22선'
---

<Callout type="warning">
  **이 문제 세트는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학학위제 4단계 데이터베이스 평가영역(출제기준) 중 함수적 종속·정규화·ER/EER 사상·SQL 고급 항목을 근거로, 다루는 개념·계산 유형·오답 구성 방식을 참고해 지문과 데이터를 모두 새로 구성한 예상문제입니다. 실제 회차의 문항 수·배점·시험 시간은 매 공고마다 달라질 수 있으므로 **반드시 응시 회차의 공식 공고를 확인**하시기 바랍니다. 이 편은 10·11편(ER 사상), 15·17편(FD·정규화), 13·14편(고급 SQL)에서 다룬 내용을 섞은 혼합 문제 22개로 구성했습니다.
</Callout>

정규화 문항은 폐포(closure) 계산과 분해 절차를 해설에 전부 풀어 두었고, SQL 문항은 실행 결과를 표의 데이터로 직접 검산할 수 있게 구성했습니다. 정답 번호만 확인하지 말고, 표의 데이터를 손으로 짚어 가며 검산하시기 바랍니다.

## 문항 배분

| 영역 | 문항 수 | 문항 번호 |
|------|---------|-----------|
| 함수적 종속과 이상 현상 | 4 | 1–4 |
| 정규화 I(1NF–BCNF) | 5 | 5–9 |
| 정규화 II(4NF·5NF) | 2 | 10–11 |
| ER/EER → 관계 사상 | 5 | 12–16 |
| SQL 고급(그룹화·뷰·서브쿼리·DCL, Oracle 문법) | 6 | 17–22 |
| 합계 | 22 | 1–22 |

## 함수적 종속과 이상 현상 (1–4번)

다음 릴레이션은 1번부터 4번, 9번까지 정규화 문항에서 공통으로 사용합니다.

**도서대출(대출번호, 회원번호, 회원이름, 도서번호, 도서명, 저자, 대출일)**

| 대출번호 | 회원번호 | 회원이름 | 도서번호 | 도서명 | 저자 | 대출일 |
|----------|----------|----------|----------|--------|------|--------|
| L1 | M1 | 김하늘 | B01 | 데이터베이스개론 | 이일산 | 2024-03-02 |
| L2 | M1 | 김하늘 | B02 | 자료구조 | 박준서 | 2024-03-05 |
| L3 | M2 | 정수아 | B01 | 데이터베이스개론 | 이일산 | 2024-03-10 |
| L4 | M3 | 오지훈 | B03 | 운영체제 | 최민규 | 2024-03-12 |
| L5 | M2 | 정수아 | B03 | 운영체제 | 최민규 | 2024-03-15 |

이 릴레이션에는 다음 함수적 종속(FD) 집합 $F$가 성립합니다.

$$
F = \{\ \text{대출번호} \rightarrow \{\text{회원번호}, \text{도서번호}, \text{대출일}\},\ \ \text{회원번호} \rightarrow \text{회원이름},\ \ \text{도서번호} \rightarrow \{\text{도서명}, \text{저자}\}\ \}
$$

<Quiz
  questionNumber={1}
  question="위 도서대출 릴레이션의 FD 집합 F가 주어졌을 때, 이 릴레이션의 후보키로 옳은 것은?"
  options={['{회원번호, 도서번호}', '{대출번호}', '{도서번호}', '{대출번호, 회원번호}']}
  correctAnswer={2}
  explanation="{대출번호}의 폐포를 계산하면 대출번호 → 회원번호, 도서번호, 대출일이 먼저 추가되고, 다시 회원번호 → 회원이름, 도서번호 → 도서명, 저자가 추가되어 최종적으로 릴레이션의 전체 속성이 됩니다. 더 작은 부분집합이 없으므로(대출번호는 단일 속성) {대출번호}가 후보키입니다."
  optionExplanations={['{회원번호, 도서번호}의 폐포는 회원이름·도서명·저자만 추가될 뿐 대출번호나 대출일을 결정하지 못해 후보키가 아닙니다', '정답. {대출번호}의 폐포가 전체 속성과 같아 후보키입니다', '{도서번호}의 폐포는 {도서번호, 도서명, 저자}에 그쳐 나머지 속성을 결정하지 못합니다', '{대출번호, 회원번호}는 대출번호 하나만으로 이미 전체를 결정하므로 회원번호가 불필요한 속성이 포함된 슈퍼키일 뿐 최소성을 만족하지 못해 후보키가 아닙니다']}
/>

<Quiz
  questionNumber={2}
  question="도서 B01의 저자 정보가 '이일산'에서 '이도현'으로 바뀌었다고 하자. 이 릴레이션에서 저자 정보를 올바르게 수정하려면 L1, L3 두 행을 모두 고쳐야 하고, 하나만 고치면 같은 도서인데 저자가 다르게 저장되는 문제가 생긴다. 이 문제에 해당하는 이상 현상은?"
  options={['삽입 이상', '삭제 이상', '갱신 이상', '참조 무결성 위반']}
  correctAnswer={3}
  explanation="같은 사실(B01의 저자)이 여러 행(L1, L3)에 중복 저장되어 있어, 일부만 고치면 모순이 생기는 것은 전형적인 갱신 이상입니다."
  optionExplanations={['삽입 이상은 정보를 넣고 싶어도 넣을 수 없는 문제로, 이 사례와 다릅니다', '삭제 이상은 행을 지웠더니 무관한 정보까지 사라지는 문제로, 이 사례와 다릅니다', '정답. 여러 곳에 중복된 정보 중 일부만 고쳐 모순이 생기는 갱신 이상입니다', '이 사례는 함수적 종속·이상 현상 개념이며 외래키 참조 무결성과는 무관합니다']}
/>

<Quiz
  questionNumber={3}
  question="위 FD 집합 F에서 {회원번호}의 폐포(closure)를 계산한 결과로 옳은 것은?"
  options={['{회원번호}', '{회원번호, 회원이름}', '{회원번호, 회원이름, 도서번호}', '릴레이션의 전체 속성']}
  correctAnswer={2}
  explanation="{회원번호}에서 시작해 F에 있는 FD를 적용하면 회원번호 → 회원이름 하나만 적용할 수 있어 회원이름이 추가되고, 더 이상 적용할 FD가 없어 폐포는 {회원번호, 회원이름}에서 멈춥니다."
  optionExplanations={['자기 자신만 남긴 초기 상태이며 회원번호 → 회원이름을 아직 적용하지 않은 상태입니다', '정답. 회원번호 → 회원이름만 적용되고 더 확장되지 않습니다', '도서번호는 회원번호로부터 결정되지 않으므로 폐포에 포함되지 않습니다', '회원번호만으로는 대출번호·도서번호 계열 속성을 전혀 결정할 수 없으므로 전체 속성이 될 수 없습니다']}
/>

<Quiz
  questionNumber={4}
  question="F에 포함된 도서번호 → 도서명과 도서번호 → 저자로부터 Armstrong 공리의 합집합률(union rule)을 이용해 유도할 수 있는 함수적 종속은?"
  options={['도서명 → 저자', '도서번호 → {도서명, 저자}', '저자 → 도서번호', '{도서명, 저자} → 도서번호']}
  correctAnswer={2}
  explanation="합집합률은 X → Y이고 X → Z이면 X → YZ가 성립한다는 규칙입니다. 도서번호 → 도서명과 도서번호 → 저자로부터 도서번호 → {도서명, 저자}를 바로 유도할 수 있습니다."
  optionExplanations={['도서명 → 저자는 주어진 FD로부터 합집합률로 유도할 수 있는 관계가 아닙니다', '정답. 합집합률에 따라 도서번호 → {도서명, 저자}가 유도됩니다', '저자 → 도서번호는 방향이 반대이며 주어진 FD로 유도되지 않습니다', '{도서명, 저자} → 도서번호도 방향이 반대이며 유도되지 않습니다']}
/>

## 정규화 I: 1NF~BCNF (5–9번)

<Quiz
  questionNumber={5}
  question="어느 테이블의 '취미' 컬럼에 한 학생당 '축구, 등산'처럼 여러 값을 콤마로 묶어 한 칸에 저장하고 있다. 이 설계가 위반하는 정규형은?"
  options={['제1정규형(1NF)', '제2정규형(2NF)', '제3정규형(3NF)', 'BCNF']}
  correctAnswer={1}
  explanation="관계형 모델의 각 속성 값은 더 이상 쪼갤 수 없는 원자값이어야 한다는 것이 제1정규형의 요구 조건입니다. 한 칸에 여러 값을 콤마로 묶어 저장하는 것은 원자값 조건을 위반하므로 1NF조차 만족하지 못합니다."
  optionExplanations={['정답. 한 칸에 여러 값을 넣는 것은 원자값 조건을 어겨 1NF부터 위반합니다', '2NF는 1NF를 만족한 상태에서 부분 함수적 종속을 다루는 정규형이라 이 사례와 다릅니다', '3NF는 1NF·2NF를 만족한 상태에서 이행적 종속을 다루는 정규형이라 이 사례와 다릅니다', 'BCNF는 결정자가 슈퍼키인지를 다루는 정규형이라 이 사례와 다릅니다']}
/>

<Quiz
  questionNumber={6}
  question="수강성적(학번, 과목코드, 학생이름, 성적) 릴레이션의 기본키는 {학번, 과목코드}이고, 학번 → 학생이름이 성립하며 학번만으로는 성적을 결정할 수 없다. 이 릴레이션에 대한 설명으로 옳은 것은?"
  options={['학생이름이 기본키 전체에 완전 함수적으로 종속하므로 2NF를 만족한다', '학생이름이 기본키의 진부분집합인 학번에만 종속하는 부분 함수적 종속이 있어 2NF를 위반한다', '이 릴레이션은 이미 3NF와 BCNF를 모두 만족한다', '성적이 학번에만 종속하므로 2NF를 위반한다']}
  correctAnswer={2}
  explanation="2NF는 기본키가 아닌 모든 속성이 기본키 전체에 완전 함수적으로 종속해야 한다는 조건입니다. 학생이름이 기본키의 진부분집합인 학번만으로 결정되는 부분 함수적 종속이 있으므로 이 릴레이션은 2NF를 위반합니다."
  optionExplanations={['학생이름은 학번(진부분집합)만으로 결정되므로 완전 함수적 종속이 아니라 부분 함수적 종속입니다', '정답. 부분 함수적 종속이 존재해 2NF를 위반합니다', '2NF조차 위반하므로 3NF·BCNF는 논할 수 없습니다', '성적은 학번만으로는 결정되지 않고 {학번, 과목코드} 전체가 있어야 결정되므로 완전 함수적 종속입니다']}
/>

<Quiz
  questionNumber={7}
  question="앞서 제시한 도서대출(대출번호, 회원번호, 회원이름, 도서번호, 도서명, 저자, 대출일) 릴레이션이 3NF를 만족하는지에 대한 설명으로 옳은 것은? (후보키는 {대출번호}이다)"
  options={['비기본 속성 사이에 이행적 종속이 없으므로 3NF를 만족한다', '대출번호 → 회원번호 → 회원이름, 대출번호 → 도서번호 → 도서명·저자라는 이행적 종속이 있어 3NF를 위반한다', '이 릴레이션은 애초에 후보키가 두 개 이상이라 3NF를 판별할 수 없다', '기본키가 복합키가 아니므로 3NF 위반 여부를 따질 필요가 없다']}
  correctAnswer={2}
  explanation="후보키 대출번호가 비기본 속성인 회원번호·도서번호를 결정하고, 그 회원번호·도서번호가 다시 회원이름·도서명·저자를 결정하는 이행적 종속 관계가 있습니다. 3NF는 비기본 속성이 후보키에 이행적으로 종속하지 않아야 한다는 조건이므로 이 릴레이션은 3NF를 위반합니다."
  optionExplanations={['이행적 종속이 실제로 존재하므로 이 설명은 옳지 않습니다', '정답. 회원번호·도서번호를 매개로 한 이행적 종속이 3NF 위반의 원인입니다', '후보키는 {대출번호} 하나뿐입니다', '기본키가 단일 속성이어도 비기본 속성 사이의 이행적 종속으로 3NF를 위반할 수 있습니다']}
/>

<Quiz
  questionNumber={8}
  question="BCNF(Boyce-Codd 정규형)의 판별 기준으로 옳은 것은?"
  options={['릴레이션에 함수적 종속이 하나도 없어야 한다', '모든 함수적 종속 X → Y에 대해 X가 그 릴레이션의 슈퍼키여야 한다', '모든 속성이 원자값을 가져야 한다', '모든 비기본 속성이 기본키에 완전 함수적으로 종속해야 한다']}
  correctAnswer={2}
  explanation="BCNF는 릴레이션에 성립하는 모든(자명하지 않은) 함수적 종속 X → Y에 대해 결정자 X가 반드시 그 릴레이션의 슈퍼키여야 한다는 조건입니다."
  optionExplanations={['함수적 종속이 아예 없어야 한다는 조건이 아니라, 있는 함수적 종속의 결정자가 슈퍼키이면 됩니다', '정답. BCNF의 정확한 판별 기준입니다', '이는 1NF의 조건이지 BCNF의 조건이 아닙니다', '이는 2NF의 조건이지 BCNF의 조건이 아닙니다']}
/>

<Quiz
  questionNumber={9}
  question="도서대출 릴레이션을 이행적 종속 기준으로 무손실·의존성 보존 분해했을 때, 결과로 가장 적절한 스키마 구성은?"
  options={['도서대출(대출번호, 회원번호, 도서번호, 대출일) 하나만 남기고 나머지 속성은 모두 버린다', '대출(대출번호, 회원번호, 도서번호, 대출일), 회원(회원번호, 회원이름), 도서(도서번호, 도서명, 저자) 세 릴레이션으로 나눈다', '회원이름과 도서명을 하나의 릴레이션에 합치고 나머지를 별도 릴레이션으로 나눈다', '대출번호를 제거하고 회원번호와 도서번호만으로 기본키를 구성한다']}
  correctAnswer={2}
  explanation="이행적 종속의 원인인 회원번호 → 회원이름과 도서번호 → 도서명·저자를 각각 별도 릴레이션으로 분리하고, 원래 릴레이션에는 대출 사실 자체(대출번호, 회원번호, 도서번호, 대출일)만 남기면 세 릴레이션 모두 이행적 종속이 사라져 BCNF를 만족하며 자연조인으로 원본을 복원할 수 있습니다."
  optionExplanations={['회원이름·도서명·저자 정보를 버리면 정보 손실이 발생해 무손실 분해가 아닙니다', '정답. 이행적 종속의 결정자(회원번호, 도서번호)를 기준으로 나눈 표준적인 분해 결과입니다', '서로 관련 없는 회원이름과 도서명을 한 릴레이션에 합치면 오히려 새로운 무관한 종속이 생길 수 있어 부적절합니다', '대출번호를 제거하면 서로 다른 대출 건을 구분할 수 없어 정보가 손실됩니다']}
/>

## 정규화 II: 4NF·5NF (10–11번)

<Quiz
  questionNumber={10}
  question="강사담당(강사명, 담당과목, 보유자격증) 릴레이션에서 한 강사가 여러 과목을 담당하고 동시에 여러 자격증을 보유하며, 담당과목과 보유자격증이 서로 완전히 독립적이어서 강사명이 같은 행끼리는 모든 조합이 테이블에 나타난다. 이 릴레이션에 대한 설명으로 옳은 것은?"
  options={['자명하지 않은 함수적 종속이 없으므로 BCNF를 만족하며 4NF도 자동으로 만족한다', '강사명 →→ 담당과목이라는 자명하지 않은 다중값 종속이 있고 강사명이 슈퍼키가 아니므로 4NF를 위반한다', '담당과목과 보유자격증이 독립적이라는 사실은 정규화와 무관한 정보이다', '이 릴레이션은 이미 5NF까지 만족한다']}
  correctAnswer={2}
  explanation="담당과목과 보유자격증이 강사명에 대해 서로 독립적으로 조합되어 나타나는 것은 강사명 →→ 담당과목(그리고 대칭적으로 강사명 →→ 보유자격증)이라는 자명하지 않은 다중값 종속의 전형적인 예입니다. 후보키는 세 속성 전체이므로 강사명은 슈퍼키가 아니고, 따라서 이 릴레이션은 BCNF는 만족해도 4NF는 위반합니다."
  optionExplanations={['자명하지 않은 FD가 없어 BCNF는 만족하지만, MVD 기준의 4NF까지 자동으로 만족하는 것은 아닙니다', '정답. 독립적으로 조합되는 다중값 속성 구조는 4NF 위반의 전형적인 사례입니다', '독립적 조합 구조는 다중값 종속이라는 정규화 개념으로 정확히 설명되는 정보입니다', '4NF조차 위반하므로 더 엄격한 5NF는 만족할 수 없습니다']}
/>

<Quiz
  questionNumber={11}
  question="조인 종속(JD)과 다중값 종속(MVD)의 관계에 대한 설명으로 옳지 않은 것은?"
  options={['MVD는 두 조각으로만 나누는 조인 종속의 특수한 경우이다', '모든 MVD는 JD로 표현할 수 있지만, 모든 JD가 MVD로 표현되는 것은 아니다', '세 조각 이상으로 나눠야만 무손실 조인이 되는 경우는 MVD만으로는 설명할 수 없다', 'JD는 MVD보다 좁은 개념이라 4NF를 만족하면 5NF는 자동으로 보장된다']}
  correctAnswer={4}
  explanation="JD는 MVD보다 더 넓은 개념이며, 4NF(MVD 기준)를 만족해도 삼진 이상의 조인 종속(JD) 문제가 남아 있으면 5NF(JD 기준)는 위반할 수 있습니다. 4NF가 5NF를 자동으로 보장한다는 설명은 옳지 않습니다."
  optionExplanations={['옳은 설명입니다. MVD는 JD의 두 조각짜리 특수 사례입니다', '옳은 설명입니다. MVD는 JD의 특수 사례이므로 이 방향의 포함 관계는 성립합니다', '옳은 설명입니다. 삼진 조인 종속은 MVD로 표현되지 않는 대표적인 경우입니다', '옳지 않은 것을 찾는 문제의 정답. JD가 MVD보다 넓은 개념이므로 4NF가 5NF를 보장하지 않습니다']}
/>

## ER/EER → 관계 사상 (12–16번)

다음 EER 다이어그램 설명은 12번부터 16번까지 공통으로 사용합니다. "병원(강한 엔티티, 병원코드가 기본키)은 여러 의사를 고용한다(1:N, employs). 의사(강한 엔티티, 의사번호가 기본키)는 여러 환자를 진료하고 환자도 여러 의사에게 진료받을 수 있다(M:N, treats, 관계 속성으로 진료일·진단내용을 가짐). 환자(강한 엔티티, 환자번호가 기본키)는 보호자연락처(약한 엔티티, 부분 키는 연락처이름)를 가진다. 의료진(ISA 슈퍼클래스)은 의사와 간호사(ISA 서브클래스, 두 서브클래스는 서로 겹치지 않고 모든 의료진이 둘 중 하나에 반드시 속함)로 나뉜다."

<Quiz
  questionNumber={12}
  question="강한 엔티티 집합을 관계 스키마로 변환하는 규칙에 대한 설명으로 옳은 것은?"
  options={['엔티티의 키 속성은 테이블의 외래키로 지정한다', '엔티티 집합 하나를 테이블 하나로 만들고 키 속성을 기본키로 지정한다', '강한 엔티티는 반드시 다른 테이블과 결합해야만 테이블로 만들 수 있다', '강한 엔티티의 속성은 테이블 컬럼이 아니라 별도의 참조 테이블에 저장해야 한다']}
  correctAnswer={2}
  explanation="강한 엔티티는 엔티티 집합 하나를 테이블 하나로 만들고, 단순 속성을 컬럼으로, 키 속성을 기본키로 그대로 옮기는 것이 표준 사상 규칙입니다."
  optionExplanations={['키 속성은 외래키가 아니라 기본키로 지정합니다', '정답. 강한 엔티티 변환의 기본 규칙입니다', '강한 엔티티는 독립적으로 테이블 하나가 될 수 있습니다', '단순 속성은 그 엔티티의 테이블에 컬럼으로 직접 저장합니다']}
/>

<Quiz
  questionNumber={13}
  question="환자의 약한 엔티티인 보호자연락처(부분 키: 연락처이름)를 관계 스키마로 변환할 때 기본키 구성으로 옳은 것은?"
  options={['연락처이름만 단독으로 기본키로 지정한다', '환자번호만 단독으로 기본키로 지정한다', '환자번호를 외래키로 추가한 뒤 연락처이름과 합쳐 복합 기본키로 지정한다', '보호자연락처는 기본키 없이 저장해도 무방하다']}
  correctAnswer={3}
  explanation="약한 엔티티는 소유 엔티티(환자)의 기본키를 외래키로 받아, 그 외래키와 부분 키(연락처이름)를 합쳐 복합 기본키로 지정해야 서로 다른 환자의 동명 연락처를 구분할 수 있습니다."
  optionExplanations={['연락처이름만으로는 서로 다른 환자에게 같은 이름의 보호자가 있을 때 기본키가 중복됩니다', '환자번호만으로는 한 환자의 여러 보호자연락처를 구분할 수 없습니다', '정답. 소유 엔티티의 외래키와 부분 키를 합친 복합 기본키가 표준 규칙입니다', '관계형 모델의 모든 테이블은 유일성을 위한 기본키가 있어야 합니다']}
/>

<Quiz
  questionNumber={14}
  question="병원(1) – 의사(N) 관계(employs)를 관계 스키마로 사상하는 방법으로 옳은 것은?"
  options={['병원 테이블에 의사번호를 외래키로 추가한다', '의사 테이블에 병원코드를 외래키로 추가한다', '별도의 연결 테이블을 만들어 병원코드와 의사번호를 모두 외래키로 둔다', '병원과 의사를 하나의 테이블로 합친다']}
  correctAnswer={2}
  explanation="1대다 관계는 '다' 쪽 테이블(의사)에 '1' 쪽 테이블(병원)의 기본키를 외래키로 추가하는 방식으로 표현합니다. 의사 한 명은 병원 하나에만 속하므로 의사 테이블의 한 행에 병원코드 하나만 적으면 됩니다."
  optionExplanations={['병원 하나가 여러 의사를 가지므로 병원 테이블에 의사번호를 넣으면 한 칸에 여러 값을 넣어야 하는 모순이 생깁니다', '정답. 다 쪽인 의사 테이블에 1 쪽인 병원코드를 외래키로 추가합니다', '연결 테이블은 다대다 관계에서 필요한 방식입니다', '두 엔티티를 하나로 합치면 별도의 개념인 병원과 의사 정보가 뒤섞여 이상 현상이 생깁니다']}
/>

<Quiz
  questionNumber={15}
  question="의사(M) – 환자(N) 진료 관계(treats, 관계 속성: 진료일·진단내용)를 관계 스키마로 사상하는 방법으로 옳은 것은?"
  options={['의사 테이블에 환자번호와 진료일·진단내용 컬럼을 모두 추가한다', '연결 테이블을 만들어 의사번호·환자번호를 외래키로 받고 둘을 합쳐 기본키로 삼은 뒤 진료일·진단내용을 컬럼으로 추가한다', '진료일·진단내용은 관계에 속한 정보이므로 의사·환자 어느 테이블에도 저장할 수 없어 버려야 한다', '환자 테이블에 의사번호 목록을 콤마로 구분해 저장한다']}
  correctAnswer={2}
  explanation="다대다 관계는 별도의 연결 테이블을 만들어 양쪽 엔티티의 기본키를 각각 외래키로 받고, 두 외래키를 합쳐 기본키로 삼습니다. 관계 자체가 가진 속성(진료일, 진단내용)은 이 연결 테이블에 컬럼으로 추가합니다."
  optionExplanations={['의사 한 명이 여러 환자를 진료하므로 의사 테이블에 환자번호를 넣으면 한 칸에 여러 값이 들어가야 하는 모순이 생깁니다', '정답. 다대다 관계의 표준 사상 규칙입니다', '관계 속성은 버리지 않고 연결 테이블의 컬럼으로 정상적으로 저장할 수 있습니다', '콤마로 여러 값을 저장하는 것은 제1정규형을 위반하는 잘못된 설계입니다']}
/>

<Quiz
  questionNumber={16}
  question="의료진(ISA 슈퍼클래스) – 의사·간호사(ISA 서브클래스, 분리적이며 전체 참여)를 관계 스키마로 사상할 때 가장 적합한 전략은?"
  options={['슈퍼클래스와 서브클래스를 모두 각각의 테이블로 만들고 서브클래스가 슈퍼클래스를 외래키로 참조하게 한다', '서브클래스별로 공통 속성을 복제한 테이블만 만들고 슈퍼클래스 테이블은 만들지 않는다', '슈퍼클래스 테이블 하나만 만들고 서브클래스 구분은 아예 하지 않는다', 'ISA 관계는 관계형 모델로 표현할 수 없으므로 임의로 한 서브클래스만 선택해서 저장한다']}
  correctAnswer={2}
  explanation="분리적(overlap 없음)이면서 전체 참여(모든 의료진이 반드시 의사 또는 간호사)인 경우, 서브클래스별 테이블만 만들고 슈퍼클래스의 공통 속성을 각 서브클래스 테이블에 포함시키는 전략을 안전하게 쓸 수 있습니다. 이 조건이 아니면 정보 손실·중복 위험이 있어 이 전략을 쓸 수 없습니다."
  optionExplanations={['이 전략도 항상 안전하지만, 서브클래스가 여러 개이고 중복적일 때 특히 유리한 방식이며 이 문제의 조건(분리적+전체 참여)에서는 서브클래스만 만드는 전략이 더 간결합니다', '정답. 분리적이고 전체 참여인 조건에서 안전하게 쓸 수 있는 전략입니다', '구분을 하지 않으면 의사·간호사 각각의 고유 속성을 저장할 곳이 없어집니다', 'ISA 관계도 정해진 규칙에 따라 관계형 테이블로 사상할 수 있습니다']}
/>

## SQL 고급: 그룹화·뷰·서브쿼리·DCL (17–22번, Oracle 문법)

다음 두 테이블은 17번부터 22번까지 공통으로 사용합니다.

**고객**(고객번호, 고객명, 등급)

| 고객번호 | 고객명 | 등급 |
|----------|--------|------|
| C1 | 김도윤 | VIP |
| C2 | 이서준 | 일반 |
| C3 | 박서연 | VIP |

**주문**(주문번호, 고객번호, 상품코드, 수량, 단가)

| 주문번호 | 고객번호 | 상품코드 | 수량 | 단가 |
|----------|----------|----------|------|------|
| O1 | C1 | P01 | 2 | 15000 |
| O2 | C1 | P02 | 1 | 30000 |
| O3 | C2 | P01 | 3 | 15000 |
| O4 | C3 | P03 | NULL | 20000 |
| O5 | C2 | P02 | 2 | 30000 |

O4는 아직 수량이 확정되지 않아 `NULL`로 남아 있는 주문입니다.

<Quiz
  questionNumber={17}
  question={"다음 질의를 실행했을 때 결과에 남는 고객번호는?\n\n```sql\nSELECT 고객번호\nFROM 주문\nGROUP BY 고객번호\nHAVING SUM(수량 * 단가) > 60000;\n```"}
  options={['C1', 'C2', 'C1, C2', 'C1, C2, C3']}
  correctAnswer={2}
  explanation="C1의 합계는 (2×15000)+(1×30000)=60000으로 60000을 초과하지 않아 제외됩니다. C2의 합계는 (3×15000)+(2×30000)=45000+60000=105000으로 60000을 초과해 포함됩니다. C3는 수량이 NULL이라 수량×단가 값 자체가 NULL이 되고, SUM 결과도 NULL이 되어 NULL이 60000보다 크다는 비교는 참으로 평가되지 않으므로 제외됩니다."
  optionExplanations={['C1의 합계는 정확히 60000이라 초과 조건을 만족하지 못합니다', '정답. C2만 105000으로 60000을 초과합니다', 'C1은 조건을 만족하지 못하므로 결과에 포함될 수 없습니다', 'C3는 NULL 값 때문에 HAVING 조건 자체를 만족한다고 판정할 수 없어 제외됩니다']}
/>

<Quiz
  questionNumber={18}
  question="상관 서브쿼리(correlated subquery)를 이용한 EXISTS·NOT EXISTS 사용법에 대한 설명으로 옳지 않은 것은?"
  options={['EXISTS는 서브쿼리가 하나 이상의 행을 반환하면 참이 된다', 'NOT EXISTS는 서브쿼리가 아무 행도 반환하지 않으면 참이 된다', '상관 서브쿼리는 외부 질의의 컬럼 값을 참조해 내부 질의의 조건을 매 행마다 다시 평가한다', 'EXISTS는 항상 서브쿼리 SELECT 절에 지정한 컬럼 값을 실제로 비교에 사용한다']}
  correctAnswer={4}
  explanation="EXISTS는 서브쿼리가 행을 반환하는지 여부(존재 여부)만으로 참·거짓을 판단하며, SELECT 절에 어떤 컬럼을 나열하든 그 값 자체는 비교에 사용되지 않습니다. 그래서 관례적으로 EXISTS의 서브쿼리에는 SELECT 1이나 SELECT * 를 흔히 씁니다."
  optionExplanations={['EXISTS의 정확한 정의입니다', 'NOT EXISTS의 정확한 정의입니다', '상관 서브쿼리의 정확한 동작 방식입니다', '옳지 않은 것을 찾는 문제의 정답. EXISTS는 반환된 값이 아니라 행의 존재 여부만 판단합니다']}
/>

<Quiz
  questionNumber={19}
  question="다음과 같이 고객과 주문을 조인한 뷰를 정의했다. VIPORDER 뷰에 대한 설명으로 옳지 않은 것은? CREATE VIEW VIPORDER AS SELECT c.고객명, o.주문번호, o.수량, o.단가 FROM 고객 c JOIN 주문 o ON c.고객번호 = o.고객번호 WHERE c.등급 = 'VIP';"
  options={['이 뷰는 두 테이블을 조인해서 만들었으므로 일반적으로 갱신이 제한된다', '이 뷰를 조회하면 등급이 VIP인 고객의 주문만 나타난다', '이 뷰는 실제 데이터를 별도로 복제해 저장하며 원본 테이블이 바뀌어도 뷰의 내용은 자동으로 갱신되지 않는다', '이 뷰는 원본 테이블에 직접 권한을 주지 않고도 필요한 정보만 제한적으로 노출하는 보안 용도로 쓸 수 있다']}
  correctAnswer={3}
  explanation="뷰는 SELECT문을 저장해 둔 가상 테이블로, 실제 데이터를 별도로 복제해 저장하지 않습니다. 뷰를 조회할 때마다 저장된 SELECT문이 다시 실행되므로 원본 테이블의 변경 사항이 항상 즉시 반영됩니다."
  optionExplanations={['둘 이상의 테이블을 조인한 뷰는 일반적으로 갱신 불가능한 뷰로 분류되므로 옳은 설명입니다', 'WHERE 절 조건대로 VIP 등급 고객의 주문만 나타나므로 옳은 설명입니다', '옳지 않은 것을 찾는 문제의 정답. 뷰는 데이터를 복제 저장하지 않고 조회 시마다 다시 계산합니다', '원본 테이블에 권한을 주지 않고 뷰에만 권한을 부여하는 보안 활용은 옳은 설명입니다']}
/>

<Quiz
  questionNumber={20}
  question={"다음 SQL 문을 순서대로 실행했다. 세 번째 문장 실행 결과에 대한 설명으로 옳은 것은?\n\n```sql\nGRANT SELECT ON 주문 TO park WITH GRANT OPTION;\nGRANT SELECT ON 주문 TO choi; -- (park이 choi에게 부여)\nREVOKE SELECT ON 주문 FROM park;\n```\n\n(choi의 SELECT 권한은 park으로부터 전파받은 것이라고 가정한다)"}
  options={['park의 SELECT 권한만 회수되고 choi의 권한은 그대로 유지된다', 'park의 SELECT 권한이 회수되면 park이 전파한 choi의 권한도 함께 회수되는 연쇄 회수가 일어난다', 'REVOKE 문은 WITH GRANT OPTION으로 부여된 권한에는 적용되지 않아 아무 효과가 없다', 'choi의 권한만 회수되고 park의 권한은 그대로 유지된다']}
  correctAnswer={2}
  explanation="WITH GRANT OPTION으로 받은 권한을 회수하면, 그 권한을 근거로 전파된 하위 권한도 함께 회수되는 것이 표준적인 동작(연쇄 회수, cascading revoke)입니다. park의 권한이 사라지면 park이 choi에게 넘겨준 권한의 근거도 사라지므로 choi의 권한도 함께 회수됩니다."
  optionExplanations={['연쇄 회수로 인해 choi의 권한도 함께 회수되므로 이 설명은 옳지 않습니다', '정답. WITH GRANT OPTION으로 전파된 권한은 원본 권한 회수 시 연쇄적으로 함께 회수됩니다', 'REVOKE는 WITH GRANT OPTION으로 부여된 권한에도 정상적으로 적용됩니다', 'REVOKE 대상은 park이므로 park의 권한이 회수되는 것이 맞습니다']}
/>

<Quiz
  questionNumber={21}
  question="다음 두 질의의 차집합(Oracle의 MINUS)을 계산한 결과의 행 수는? 질의1: 상품코드가 'P01'인 주문의 고객번호 목록, 질의2: 상품코드가 'P02'인 주문의 고객번호 목록. (SELECT 고객번호 FROM 주문 WHERE 상품코드='P01') MINUS (SELECT 고객번호 FROM 주문 WHERE 상품코드='P02')"
  options={['0건', '1건', '2건', '3건']}
  correctAnswer={1}
  explanation="P01을 주문한 고객번호는 {C1, C2}이고, P02를 주문한 고객번호도 {C1, C2}로 동일합니다. MINUS는 첫 번째 결과에서 두 번째 결과에 있는 값을 모두 제외하므로, {C1, C2} - {C1, C2} = 공집합이 되어 결과는 0건입니다."
  optionExplanations={['정답. 두 집합이 완전히 같아 차집합 결과는 공집합입니다', 'P01과 P02를 주문한 고객이 겹치지 않는다고 잘못 계산했을 때 나올 수 있는 값입니다', 'MINUS가 아니라 UNION처럼 계산했을 때 나올 수 있는 값입니다', 'P01 주문 고객 수(2건)를 그대로 답했을 때 나올 수 있는 값으로, MINUS 연산을 적용하지 않은 결과입니다']}
/>

<Quiz
  questionNumber={22}
  question={"다음 질의를 실행하면 반환되는 행 수는?\n\n```sql\nSELECT *\nFROM 주문 O1\nWHERE O1.수량 > (SELECT AVG(수량) FROM 주문 O2);\n```"}
  options={['0건', '1건', '2건', '4건']}
  correctAnswer={2}
  explanation="주문 테이블에서 수량이 NULL이 아닌 값은 2, 1, 3, 2이며 평균은 (2+1+3+2)/4=2입니다. 이 평균 2보다 수량이 큰 주문은 O3(수량 3) 하나뿐입니다. O4는 수량 자체가 NULL이라 비교 결과가 알 수 없음(UNKNOWN)으로 처리되어 결과에서 제외됩니다."
  optionExplanations={['O3가 조건을 만족하므로 0건은 오답입니다', '정답. 평균 2를 초과하는 수량은 O3의 3뿐입니다', 'O1(수량 2)까지 포함해 잘못 세었을 때 나올 수 있는 값으로, 2는 평균과 같을 뿐 초과가 아닙니다', 'NULL인 O4를 포함해 전체 행을 세었을 때 나올 수 있는 값으로, 서브쿼리 결과를 실제로 적용하지 않은 오답입니다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
- [Oracle SQL Language Reference](https://docs.oracle.com/en/database/oracle/oracle-database/index.html)
- [GeeksforGeeks - Database Normalization](https://www.geeksforgeeks.org/normal-forms-in-dbms/)
