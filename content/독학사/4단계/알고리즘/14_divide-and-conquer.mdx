---
title: '14. 분할정복 설계 기법과 대표 문제'
description: '분할정복의 분할·정복·결합 3단계 패턴을 빠른 거듭제곱 계산으로 직접 추적하고, 마스터 정리로 점화식을 O 표기로 정리하는 방법과 시험에서 자주 나오는 함정을 정리한다.'
---

<Callout type="info">
  **이번 문서의 목표:** 이 문서를 다 읽으면 분할정복 알고리즘의 분할·정복·결합 3단계를 구체적인 예제로 설명할 수 있고, 분할정복 알고리즘의 점화식을 세운 뒤 마스터 정리로 시간 복잡도를 구할 수 있으며, 분할정복이 유리한 경우와 그렇지 않은 경우를 구분할 수 있다.
</Callout>

## 왜 분할정복이 따로 필요한가

07편에서 다룬 퀵 정렬·병합 정렬은 사실 "큰 문제를 작은 문제로 쪼개 각각 풀고 합친다"는 하나의 공통된 설계 원리를 공유한다. 이 원리를 정렬에만 한정하지 않고 일반화하면, 정렬 말고도 거듭제곱 계산, 최근접 점 쌍 찾기, 행렬 곱셈처럼 전혀 다른 문제에도 똑같이 적용할 수 있다. **분할정복**(divide and conquer)은 이렇게 "문제를 작게 나누고, 작은 문제를 풀고, 결과를 합친다"는 패턴 자체를 하나의 설계 기법으로 다루는 관점이다.

> **쉽게 말하면:** 분할정복은 큰 문제를 그대로 풀기 어려우니, 작은 조각으로 쪼개 각 조각을 풀고 그 답들을 이어 붙여 원래 문제의 답을 만드는 전략이다.

## 분할정복의 세 단계

분할정복 알고리즘은 항상 다음 세 단계로 구성된다.

1. **분할**(divide): 원래 문제를 같은 종류의 더 작은 부분 문제 여러 개로 나눈다.
2. **정복**(conquer): 각 부분 문제를 재귀적으로 푼다. 부분 문제가 충분히 작아지면(기저 사례, base case) 더 나누지 않고 직접 답을 구한다.
3. **결합**(combine): 부분 문제들의 답을 모아 원래 문제의 답을 만든다.

07편의 병합 정렬로 이 세 단계를 다시 확인하면, 분할은 "배열을 절반으로 나누는 것", 정복은 "각 절반을 재귀적으로 정렬하는 것", 결합은 "정렬된 두 절반을 하나로 병합하는 것"이다. 반면 퀵 정렬은 분할 단계(피벗 기준으로 나누기)에서 대부분의 일을 끝내고 결합 단계는 사실상 아무 일도 하지 않는다는 점이 병합 정렬과 대조적이다.

## 대표 예제 1 — 빠른 거듭제곱 계산

거듭제곱 $a^n$을 계산하는 가장 단순한 방법은 $a$를 $n$번 곱하는 것으로, $O(n)$번의 곱셈이 필요하다. 분할정복을 적용하면 이를 $O(\log n)$으로 줄일 수 있다.

핵심 아이디어는 $a^n$을 절반 크기의 거듭제곱으로 표현하는 것이다.

$$
a^n = \begin{cases} 1 & n = 0 \text{일 때} \\ \left(a^{n/2}\right)^2 & n \text{이 짝수일 때} \\ a \times \left(a^{(n-1)/2}\right)^2 & n \text{이 홀수일 때} \end{cases}
$$

- $a^{n/2}$: 지수를 절반으로 줄인 부분 문제. 이 값을 한 번만 계산하고 제곱해서 재사용하는 것이 핵심이다(만약 $a^{n/2}$을 두 번 따로 계산하면 나이브 방식과 다를 게 없어진다).
- $n$이 홀수일 때는 짝을 맞추기 위해 $a$ 하나를 따로 곱해 준다.

```
Power(a, n):
  만약 n == 0이면 1을 반환한다  (기저 사례)
  절반값 = Power(a, n을 2로 나눈 몫)   (분할 + 정복: 절반 크기 부분 문제를 재귀로 품)
  만약 n이 짝수이면 절반값 × 절반값을 반환한다        (결합)
  그렇지 않으면(n이 홀수) a × 절반값 × 절반값을 반환한다  (결합)
```

### 작은 예시로 추적하기

$3^{13}$을 계산하는 과정을 재귀 호출 트리로 추적한다.

| 호출 | n의 홀짝 | 재귀 결과 이용 | 계산 | 반환값 |
|------|----------|-----------------|------|--------|
| Power(3, 0) | - (기저 사례) | - | - | 1 |
| Power(3, 1) | 홀수 | Power(3, 0) = 1 | $3 \times 1^2$ | 3 |
| Power(3, 3) | 홀수 | Power(3, 1) = 3 | $3 \times 3^2$ | 27 |
| Power(3, 6) | 짝수 | Power(3, 3) = 27 | $27^2$ | 729 |
| Power(3, 13) | 홀수 | Power(3, 6) = 729 | $3 \times 729^2$ | 1,594,323 |

호출 순서를 보면 $13 \to 6 \to 3 \to 1 \to 0$으로, 매 호출마다 $n$이 대략 절반으로 줄어든다. 실제로 $3^{13} = 1{,}594{,}323$이 맞는지는 $3^{13} = 3^8 \times 3^4 \times 3^1$로 검산할 수 있다. 나이브 방식이라면 곱셈을 12번 해야 했을 자리를, 분할정복은 재귀 호출 4번(그리고 각 단계에서 곱셈 1~2번)만으로 끝낸다.

> **쉽게 말하면:** 지수를 계속 반으로 접어 나가면서, "절반을 한 번만 계산하고 제곱해서 재사용"하는 것이 곱셈 횟수를 크게 줄이는 비결이다.

## 대표 문제 2 — 최근접 점 쌍 문제(개요)

평면 위에 점이 $n$개 있을 때 서로 가장 가까운 두 점을 찾는 **최근접 점 쌍**(closest pair of points) 문제도 분할정복으로 풀 수 있는 대표적인 문제다. 모든 점 쌍의 거리를 다 재는 나이브 방식은 $O(n^2)$이 걸리지만, 분할정복은 다음과 같은 흐름으로 이를 줄인다.

- **분할**: 점들을 x좌표 기준으로 정렬한 뒤, 중간을 기준으로 왼쪽 절반과 오른쪽 절반으로 나눈다.
- **정복**: 각 절반에서 재귀적으로 최근접 점 쌍을 구한다.
- **결합**: 왼쪽과 오른쪽 최솟값 중 더 작은 값을 $d$라 하고, 경계선 근처 폭 $2d$ 안에 있는 점들만 따로 모아 그 안에서만 추가로 거리를 비교한다(경계 안의 점들만 보면 되므로 이 결합 단계가 $O(n)$에 끝난다는 것이 이 알고리즘의 정교한 부분이다).

이 문제는 독학사 시험에서 알고리즘을 직접 손코딩하기보다는 "분할정복으로 $O(n \log n)$에 풀 수 있는 대표적인 기하 문제"라는 존재와 흐름 정도를 알아 두면 충분하다.

## 점화식 세우기와 마스터 정리

분할정복 알고리즘의 시간 복잡도는 대부분 다음과 같은 형태의 점화식으로 나타난다(04편에서 다룬 점화식 기초를 분할정복에 적용한다).

$$
T(n) = a \times T(n/b) + O(n^d)
$$

- $a$: 한 번의 호출에서 만들어지는 부분 문제의 개수.
- $n/b$: 부분 문제 하나의 크기(원래 크기를 $b$로 나눈 것).
- $O(n^d)$: 분할과 결합에 드는 비용(재귀 호출 자체를 제외한 비용).

**마스터 정리**(master theorem)는 $a$, $b$, $d$ 세 값만으로 $T(n)$의 점근적 표기를 바로 구할 수 있게 해 준다. $\log_b a$(밑이 $b$인 $a$의 로그, "부분 문제 개수가 부분 문제 크기의 몇 제곱에 해당하는 속도로 늘어나는가"를 나타내는 지수)와 $d$를 비교한다.

$$
T(n) = \begin{cases} O(n^d) & \log_b a < d \text{일 때 (분할·결합 비용이 지배적)} \\ O(n^d \log n) & \log_b a = d \text{일 때 (둘의 크기가 같은 차수)} \\ O(n^{\log_b a}) & \log_b a > d \text{일 때 (재귀 호출 자체가 지배적)} \end{cases}
$$

### 적용 예 1 — 병합 정렬

병합 정렬(07편)의 점화식은 $T(n) = 2T(n/2) + O(n)$이다. 여기서 $a=2$, $b=2$, $d=1$이다.

$$
\log_b a = \log_2 2 = 1
$$

$\log_b a = 1$이고 $d = 1$이므로 두 값이 같은 경우(두 번째 경우)에 해당해, $T(n) = O(n^1 \log n) = O(n \log n)$이다. 이는 07편에서 재귀 트리로 직접 유도했던 결과와 정확히 일치한다.

### 적용 예 2 — 빠른 거듭제곱

빠른 거듭제곱의 점화식은 $T(n) = T(n/2) + O(1)$이다. 여기서 $a=1$, $b=2$, $d=0$(분할·결합에 드는 비용이 지수·곱셈 한 번이라는 상수 시간이므로 $n^0$).

$$
\log_b a = \log_2 1 = 0
$$

$\log_b a = 0$이고 $d = 0$이므로 역시 두 번째 경우에 해당해, $T(n) = O(n^0 \log n) = O(\log n)$이다. 앞서 추적표에서 호출 4번($13\to6\to3\to1\to0$)만으로 끝난 것이 바로 이 $O(\log n)$의 실제 모습이다.

> **자주 틀리는 점:** 마스터 정리를 적용할 때 $d$를 "재귀 호출 자체를 포함한 전체 비용"으로 잘못 넣는 실수가 잦다. $d$는 **분할과 결합에만 드는 비용**(재귀 호출 부분을 뺀 나머지)이어야 한다. 병합 정렬에서 $d=1$인 것은 "두 배열을 병합하는 데 드는 $O(n)$"만을 가리키며, 재귀로 절반씩 정렬하는 비용은 $a \times T(n/b)$ 항이 이미 따로 표현하고 있다.

## 분할정복의 장단점과 시험 포인트

| 항목 | 내용 |
|------|------|
| 장점 | 부분 문제가 서로 독립적이면 병렬 처리가 자연스럽고, 문제를 작게 쪼개면 사람이 이해·증명하기도 쉬워진다. 대부분 $O(n \log n)$ 수준의 효율적인 알고리즘으로 이어진다. |
| 단점 | 부분 문제를 나누고 결과를 합치는 과정 자체에 비용(예: 병합 정렬의 병합 단계)이 들며, 재귀 호출로 인한 함수 호출 오버헤드·스택 공간 소모가 있다. 부분 문제들이 겹치는 경우(16편의 동적계획법이 다루는 상황)에는 같은 부분 문제를 반복해서 풀게 되어 비효율적일 수 있다. |
| 시험 포인트 | "이 알고리즘의 점화식을 세우고 마스터 정리로 시간 복잡도를 구하라"는 유형이 자주 나온다. $a$, $b$, $d$를 정확히 뽑아내는 것이 관건이며, 분할정복과 동적계획법(16편)을 구분하는 기준(부분 문제가 겹치는지 여부)도 함께 자주 출제된다. |

> **자주 틀리는 점:** "분할정복은 항상 효율적이다"라고 단정하면 안 된다. 부분 문제들이 서로 겹쳐서(예: 피보나치 수를 단순 재귀로 계산하면 같은 부분 문제를 지수적으로 반복 계산하게 됨) 오히려 비효율적인 경우도 있으며, 이런 경우는 16편의 동적계획법으로 부분 문제의 답을 저장해 재사용하는 편이 낫다.

## 핵심 정리

- 분할정복은 분할(작은 부분 문제로 나누기), 정복(부분 문제를 재귀로 풀기), 결합(부분 문제의 답을 합치기)의 세 단계로 이루어진다.
- 빠른 거듭제곱 $a^n$은 절반 크기의 거듭제곱을 한 번만 계산해 제곱함으로써 $O(\log n)$에 계산할 수 있다.
- 분할정복 알고리즘의 점화식 $T(n) = aT(n/b) + O(n^d)$은 마스터 정리로 $\log_b a$와 $d$를 비교해 $O(n^d)$, $O(n^d \log n)$, $O(n^{\log_b a})$ 중 하나로 정리된다.
- 부분 문제가 서로 독립적일 때는 분할정복이 유리하지만, 부분 문제가 겹칠 때는 동적계획법이 더 효율적일 수 있다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="분할정복 알고리즘을 구성하는 세 단계로 옳은 것은?"
  options={['정렬, 탐색, 병합', '분할, 정복, 결합', '초기화, 반복, 종료', '입력, 처리, 출력']}
  correctAnswer={2}
  explanation="분할정복은 원래 문제를 더 작은 부분 문제로 나누는 분할, 부분 문제를 재귀적으로 푸는 정복, 부분 문제의 답을 모아 원래 문제의 답을 만드는 결합의 세 단계로 이루어진다."
  optionExplanations={['정렬·탐색·병합은 특정 알고리즘의 구성 요소일 뿐 분할정복 일반의 단계 이름이 아니다.', '정답. 분할·정복·결합이 분할정복의 표준 3단계다.', '이는 일반적인 프로그램 실행 흐름이며 분할정복 특유의 구조를 나타내지 않는다.', '이는 일반적인 프로그램의 입출력 구조일 뿐 분할정복의 단계를 나타내지 않는다.']}
/>

<Quiz
  questionNumber={2}
  question="빠른 거듭제곱 계산에서 지수 n이 짝수일 때 a^n을 (a^(n/2))^2로 계산하는 것이 나이브 방식(a를 n번 곱하기)보다 빠른 이유로 가장 적절한 것은?"
  options={['a^(n/2)를 두 번 각각 계산하기 때문이다', 'a^(n/2)를 한 번만 계산한 뒤 그 결과를 제곱해 재사용하기 때문에 곱셈 횟수가 지수 규모가 아닌 로그 규모로 줄어들기 때문이다', '컴퓨터가 짝수 계산을 홀수 계산보다 항상 빠르게 처리하기 때문이다', 'a의 값이 작아지기 때문이다']}
  correctAnswer={2}
  explanation="a^(n/2)를 한 번만 계산하고 그 값을 제곱해서 재사용하면, 지수가 절반씩 줄어드는 재귀 호출이 O(log n)번만 일어난다. 만약 a^(n/2)를 두 번 각각 새로 계산한다면 나이브 방식과 다를 바 없이 느려진다."
  optionExplanations={['두 번 각각 계산하면 오히려 나이브 방식과 비슷하거나 더 느려질 수 있다. 빠른 거듭제곱의 핵심은 한 번만 계산해 재사용하는 것이다.', '정답. 한 번 계산한 결과를 재사용해 곱셈 횟수를 로그 규모로 줄이는 것이 핵심이다.', '짝수·홀수는 계산 속도와 무관하며, 재귀 호출 구조의 차이일 뿐이다.', 'a의 값 자체는 변하지 않는다. 줄어드는 것은 지수 n이다.']}
/>

<Quiz
  questionNumber={3}
  question="분할정복 알고리즘의 점화식이 T(n) = 4T(n/2) + O(n)일 때, 마스터 정리를 적용하기 위한 log_b(a) 값과 d 값으로 옳은 것은?"
  options={['log_b(a) = 1, d = 1', 'log_b(a) = 2, d = 1', 'log_b(a) = 4, d = 2', 'log_b(a) = 0, d = 1']}
  correctAnswer={2}
  explanation="점화식에서 a=4, b=2이므로 log_2(4) = 2이고, 분할·결합 비용이 O(n)이므로 d=1이다."
  optionExplanations={['a=4, b=2에서 log_2(4)는 1이 아니라 2다.', '정답. log_2(4)=2이고 분할·결합 비용 O(n)에서 d=1이다.', 'a와 b값을 그대로 log_b(a)에 대입하는 것은 잘못된 계산이다. log_2(4)=2로 계산해야 한다.', 'a=4, b=2에서 log_2(4)는 0이 아니라 2다.']}
/>

<Quiz
  questionNumber={4}
  question="병합 정렬의 점화식 T(n) = 2T(n/2) + O(n)에 마스터 정리를 적용했을 때의 결과로 옳은 것은?"
  options={['O(n)', 'O(n log n)', 'O(n^2)', 'O(log n)']}
  correctAnswer={2}
  explanation="a=2, b=2이므로 log_2(2)=1이고 d=1이다. log_b(a)와 d가 같으므로 마스터 정리의 두 번째 경우에 해당해 O(n^d log n) = O(n log n)이다."
  optionExplanations={['O(n)은 분할·결합 비용만을 나타내며 재귀 호출 전체를 반영한 결과가 아니다.', '정답. log_b(a)=d=1인 경우이므로 O(n log n)이다.', 'O(n^2)은 log_b(a)가 d보다 클 때 나올 수 있는 형태이며 이 점화식에는 해당하지 않는다.', 'O(log n)은 a=1일 때 나올 수 있는 형태이며 a=2인 이 점화식에는 해당하지 않는다.']}
/>

<Quiz
  questionNumber={5}
  question="분할정복 기법의 한계 또는 주의점으로 옳지 않은 것은?"
  options={['부분 문제들이 서로 겹치면 같은 계산을 반복하게 되어 비효율적일 수 있다', '재귀 호출로 인한 함수 호출 오버헤드와 스택 공간 소모가 있을 수 있다', '결합 단계 자체에도 비용이 들 수 있으며 이 비용이 전체 복잡도에 영향을 준다', '분할정복으로 설계된 알고리즘은 항상 나이브 알고리즘보다 빠르다']}
  correctAnswer={4}
  explanation="분할정복이 항상 더 빠른 것은 아니다. 부분 문제가 매우 작거나 분할·결합 비용이 크면 오히려 나이브 방식보다 느릴 수 있고, 부분 문제가 겹치는 경우에는 동적계획법이 더 유리할 수 있다."
  optionExplanations={['옳은 설명이다. 부분 문제가 겹치는 대표적인 예가 단순 재귀 피보나치 수 계산이다.', '옳은 설명이다. 재귀 호출은 추가적인 오버헤드를 동반한다.', '옳은 설명이다. 병합 정렬의 병합 단계처럼 결합 비용이 전체 복잡도의 일부를 차지한다.', '정답(옳지 않은 설명). 분할정복이 항상 더 빠르다는 보장은 없으며, 상황에 따라 다른 기법이 더 유리할 수 있다.']}
/>

<Quiz
  questionNumber={6}
  question="퀵 정렬과 병합 정렬을 분할정복의 세 단계 관점에서 비교한 설명으로 가장 적절한 것은?"
  options={['퀵 정렬은 결합 단계에서 대부분의 일을 하고 분할 단계는 거의 하지 않는다', '병합 정렬은 분할 단계에서 대부분의 일을 하고 결합 단계는 거의 하지 않는다', '퀵 정렬은 분할(피벗 기준 나누기) 단계에서 대부분의 일을 하고 결합 단계는 거의 하지 않는 반면, 병합 정렬은 분할이 단순하고 결합(병합) 단계에서 대부분의 일을 한다', '두 알고리즘 모두 분할·정복·결합에 동일한 비중으로 일을 분배한다']}
  correctAnswer={3}
  explanation="퀵 정렬은 피벗을 기준으로 배열을 나누는 분할 단계에서 실질적인 정렬 작업이 끝나고, 정렬된 두 부분을 그대로 이어 붙이기만 하면 되어 결합 단계는 사실상 없다. 반대로 병합 정렬은 배열을 절반으로 나누는 분할은 단순하지만, 두 정렬된 배열을 하나로 합치는 병합(결합) 단계에서 실질적인 작업이 이루어진다."
  optionExplanations={['퀵 정렬은 결합이 아니라 분할 단계에서 대부분의 일을 한다.', '병합 정렬은 분할이 아니라 결합(병합) 단계에서 대부분의 일을 한다.', '정답. 두 알고리즘이 분할정복의 세 단계 중 어디에 무게를 두는지가 서로 대조적이다.', '두 알고리즘은 단계별 작업 비중이 서로 뚜렷하게 다르다.']}
/>

## 참고 자료

- [설계기법 강의노트 - Divide and Conquer (GeeksforGeeks)](https://www.geeksforgeeks.org/dsa/divide-and-conquer/) — 분할정복의 일반 패턴, 대표 예제, 마스터 정리 적용법을 정리한 자료.
- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr) — 독학사 4단계 알고리즘 과목의 최신 출제기준·평가영역 확인용 공식 안내.
