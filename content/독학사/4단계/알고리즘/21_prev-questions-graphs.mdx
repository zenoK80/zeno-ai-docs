---
title: '21. 예상·기출 변형: 그래프·최단경로·MST'
description: 'DFS/BFS, 위상 정렬, 최소 신장 트리(프림·크루스칼), 최단경로(다익스트라·벨만-포드·플로이드-워셜)를 트레이싱·복잡도 비교 중심으로 재구성한 예상 문항 모음'
---

<Callout type="warning">
  이 편의 문항은 실제 독학사 기출 문제를 그대로 옮긴 것이 아닙니다. 국가평생교육진흥원이 공개한 4단계 알고리즘 과목 출제기준(평가영역)과 공개·복원된 기출의 유형 분포를 바탕으로, 동일한 출제 의도를 갖도록 새로 재구성한 예상·유사 문항입니다. 실제 시험의 문항 수·배점·시간은 매 회차 공고를 통해 반드시 다시 확인하세요.
</Callout>

아래 11개 문항 중 그래프 순회·탐색(1~5번)은 다음 무방향 그래프를 공통으로 사용합니다.

- 정점: A, B, C, D, E, F
- 간선: A–B, A–C, B–D, B–E, C–F, E–F

인접 리스트를 알파벳 오름차순으로 저장했다고 가정합니다(즉 A의 인접 리스트는 B, C 순서).

<Quiz
  questionNumber={1}
  question="위 그래프 표현 방식에 대한 설명으로 옳지 않은 것은?"
  options={['인접 행렬은 정점 수를 n이라 할 때 O(n^2)의 공간을 차지한다', '인접 리스트는 간선 수를 m이라 할 때 O(n + m)의 공간을 차지한다', '두 정점 사이에 간선이 있는지 확인하는 데는 인접 리스트가 인접 행렬보다 항상 더 빠르다', '희소 그래프(간선이 적은 그래프)에서는 인접 리스트가 공간 효율이 더 좋다']}
  correctAnswer={3}
  explanation="인접 행렬에서 두 정점 i, j 사이의 간선 존재 여부는 배열 원소 하나를 바로 읽으면 되므로 O(1)이다. 반면 인접 리스트에서는 정점 i의 리스트를 처음부터 훑어야 하므로 최악의 경우 O(차수) 시간이 걸린다. 따라서 간선 존재 여부 확인은 인접 행렬이 인접 리스트보다 항상 빠르거나 같으며, 인접 리스트가 항상 더 빠르다는 진술은 옳지 않다. 나머지 세 진술은 각 표현법의 표준적인 공간·성능 특성을 정확히 설명한다."
  optionExplanations={['옳은 설명이다. 인접 행렬은 n행 n열 배열이므로 O(n^2) 공간이 필요하다', '옳은 설명이다. 각 정점마다 자신과 연결된 간선만 저장하므로 전체 공간이 O(n + m)이다', '옳지 않은 설명이다. 간선 존재 여부 확인은 배열 인덱싱 한 번이면 되는 인접 행렬 쪽이 O(1)로 더 빠르거나 같다', '옳은 설명이다. 간선이 적은 그래프에서는 인접 행렬의 대부분 칸이 낭비되므로 인접 리스트가 유리하다']}
/>

<Quiz
  questionNumber={2}
  question="위 그래프에서 정점 A를 시작점으로 깊이 우선 탐색(DFS)을 수행하되, 각 정점에서 인접 리스트에 저장된 순서(알파벳 오름차순)대로 방문한다고 할 때, 방문 순서로 옳은 것은?"
  options={['A, B, D, E, F, C', 'A, B, C, D, E, F', 'A, C, F, E, B, D', 'A, B, E, F, C, D']}
  correctAnswer={1}
  explanation="DFS는 한 방향으로 갈 수 있는 만큼 깊이 내려간 뒤 막히면 되돌아온다(재귀 호출 스택 또는 명시적 스택 사용). A를 방문하고 인접 리스트의 첫 원소인 B로 이동한다. B에서는 A(이미 방문)를 건너뛰고 D로 이동한다. D의 인접 정점은 B뿐이고 이미 방문했으므로 더 갈 곳이 없어 B로 되돌아온다(백트래킹). B의 다음 인접 정점 E로 이동한다. E의 인접 리스트는 B(방문), F 순서이므로 F로 이동한다. F의 인접 리스트는 C, E 순서인데 E는 이미 방문했으므로 C로 이동한다. C의 인접 리스트는 A(방문), F(방문)뿐이므로 더 갈 곳이 없다. 모든 정점을 방문했으므로 종료한다. 최종 방문 순서는 A, B, D, E, F, C이다."
  optionExplanations={['정답이다. 재귀적으로 깊이 내려가다 막히면 되돌아오는 DFS의 동작을 그대로 따른 결과다', 'BFS의 방문 순서와 혼동한 경우다. DFS는 한 갈래를 끝까지 파고든 뒤에야 다른 갈래로 넘어간다', 'C를 먼저 방문한 것으로 착각한 경우다. A의 인접 리스트에서 B가 C보다 앞에 있으므로 B를 먼저 방문해야 한다', 'D보다 E를 먼저 방문한 것으로 착각한 경우다. B에서 A는 이미 방문했으므로 다음 순서인 D를 먼저 방문해야 한다']}
/>

<Quiz
  questionNumber={3}
  question="같은 그래프에서 정점 A를 시작점으로 너비 우선 탐색(BFS)을 수행할 때(인접 리스트 순서대로 큐에 삽입), 방문 순서로 옳은 것은?"
  options={['A, B, C, D, E, F', 'A, B, D, E, F, C', 'A, C, B, F, E, D', 'A, B, C, F, D, E']}
  correctAnswer={1}
  explanation="BFS는 큐(선입선출)를 사용해 가까운 정점부터 차례로 방문한다. A를 방문하고 큐에 B, C를 순서대로 넣는다. 큐에서 B를 꺼내 방문하고, B의 인접 정점 중 아직 방문하지 않은 D, E를 큐에 넣는다(A는 이미 방문). 큐에서 C를 꺼내 방문하고, C의 인접 정점 중 아직 방문하지 않은 F를 큐에 넣는다(A는 이미 방문). 이제 큐는 D, E, F 순서다. D를 꺼내 방문한다(인접 정점 B는 이미 방문). E를 꺼내 방문한다(인접 정점 B는 방문, F는 아직 큐에 있으나 미방문 상태이므로 방문 표시만 확인하고 중복 삽입하지 않는다). F를 꺼내 방문한다. 최종 방문 순서는 A, B, C, D, E, F이다."
  optionExplanations={['정답이다. 레벨 순서대로(A의 이웃 전체, 그다음 이웃의 이웃 전체) 방문한 결과다', 'DFS의 방문 순서와 혼동한 경우다. BFS는 큐를 쓰므로 A의 이웃 B, C를 모두 방문한 뒤에야 다음 단계로 넘어간다', 'C를 B보다 먼저 방문한 것으로 착각한 경우다. A의 인접 리스트에서 B가 C보다 먼저 큐에 들어간다', '큐 순서를 잘못 적용한 경우다. B와 C를 모두 방문한 뒤에 D, E, F 순서로 방문해야 한다']}
/>

<Quiz
  questionNumber={4}
  question="정점 수를 V, 간선 수를 E라 할 때 DFS와 BFS의 시간 복잡도에 대한 설명으로 옳은 것은? (인접 리스트로 표현했다고 가정)"
  options={['DFS와 BFS 모두 O(V + E)이다. 각 정점을 한 번씩 방문하고 각 간선을 최대 한 번(무방향이면 두 번) 검사하기 때문이다', 'DFS는 O(V^2), BFS는 O(V + E)이다. DFS는 재귀 호출 오버헤드 때문에 항상 더 느리다', 'DFS와 BFS 모두 O(V^2)이다. 인접 리스트를 쓰더라도 모든 정점 쌍을 비교해야 하기 때문이다', 'DFS는 O(V + E), BFS는 O(E^2)이다. 큐에 간선을 반복 삽입하기 때문이다']}
  correctAnswer={1}
  explanation="DFS와 BFS 모두 '모든 정점을 한 번씩 방문하고, 방문한 정점에서 인접 리스트를 훑으며 각 간선을 확인'하는 동일한 골격을 가진다. 정점 방문에 O(V), 인접 리스트 전체를 합쳐서 훑는 데 O(E)(무방향 그래프라면 각 간선이 양쪽 정점의 리스트에 한 번씩 나타나므로 실제로는 O(2E)이지만 상수는 무시)가 걸리므로 총 시간은 O(V + E)로 동일하다. 인접 행렬로 표현했다면 각 정점에서 모든 정점을 확인해야 하므로 O(V^2)이 되지만, 이 문제는 인접 리스트를 전제로 하므로 O(V + E)가 맞다."
  optionExplanations={['정답이다. 인접 리스트 기준으로 DFS, BFS 모두 정점 방문과 간선 확인을 합쳐 O(V + E)이다', 'DFS가 항상 더 느리다는 것은 근거 없는 오해다. 재귀 호출도 상수 시간 오버헤드일 뿐 점근적 복잡도를 바꾸지 않는다', '인접 리스트를 사용하면 각 정점에서 실제 인접한 정점만 확인하므로 모든 정점 쌍을 비교할 필요가 없다. O(V^2)은 인접 행렬을 사용할 때의 복잡도다', 'BFS가 O(E^2)이라는 것은 근거가 없다. 큐에는 각 정점이 최대 한 번만 들어가고 간선도 한 번씩만 확인한다']}
/>

<Quiz
  questionNumber={5}
  question="다음 중 그래프의 연결 요소(connected component) 개수를 구하는 방법으로 옳은 것은?"
  options={['방문하지 않은 정점이 남아 있으면 그 정점에서 새로 DFS(또는 BFS)를 시작하고, 이렇게 시작한 횟수를 세면 연결 요소 개수가 된다', '전체 정점 수에서 전체 간선 수를 빼면 연결 요소 개수가 된다', '위상 정렬이 가능한 그래프인지 확인하면 연결 요소 개수를 알 수 있다', '인접 행렬의 대각선 원소 합을 구하면 연결 요소 개수가 된다']}
  correctAnswer={1}
  explanation="연결 요소란 그래프 안에서 서로 경로로 도달 가능한 정점들의 최대 부분집합이다. 임의의 미방문 정점에서 DFS(또는 BFS)를 수행하면 그 정점과 같은 연결 요소에 속한 모든 정점이 한 번에 방문된다. 방문이 끝난 뒤에도 아직 방문하지 않은 정점이 남아 있다면 그것은 다른 연결 요소에 속한다는 뜻이므로, 그 정점에서 다시 탐색을 시작한다. 이 과정을 모든 정점이 방문될 때까지 반복하면서 탐색을 시작한 횟수를 세면 그것이 곧 연결 요소의 개수다. 앞선 예시 그래프는 A에서 시작한 한 번의 DFS로 모든 정점(A~F)이 방문되므로 연결 요소가 1개다."
  optionExplanations={['정답이다. 미방문 정점에서 탐색을 새로 시작하는 횟수가 곧 연결 요소 개수다', '정점 수와 간선 수의 차이는 트리 구조에서나 의미가 있는 값이며, 일반적인 그래프의 연결 요소 개수와는 무관하다', '위상 정렬은 방향 비순환 그래프(DAG)에서 선후 관계를 정하는 것으로, 연결 요소 개수와는 별개의 개념이다. 위상 정렬이 불가능해도 연결 요소는 여러 개일 수도 하나일 수도 있다', '인접 행렬의 대각선 원소는 보통 자기 자신으로의 간선(자기 루프) 여부를 나타낼 뿐 연결 요소 개수와는 무관하다']}
/>

다음 3문항(6~8)은 위상 정렬 문제입니다. 다음 방향 비순환 그래프(DAG)를 사용합니다.

- 정점: 1, 2, 3, 4, 5, 6
- 간선(방향): 1→2, 1→3, 2→4, 3→4, 4→5, 3→6, 5→6

<Quiz
  questionNumber={6}
  question="위상 정렬(topological sort)에 대한 설명으로 옳지 않은 것은?"
  options={['위상 정렬은 방향 비순환 그래프(DAG)에서만 정의된다', '위상 정렬 결과에서 간선 u→v가 있다면 u는 반드시 v보다 앞에 온다', '하나의 DAG에 대해 위상 정렬 결과는 항상 유일하다', '사이클이 있는 방향 그래프는 위상 정렬을 수행할 수 없다']}
  correctAnswer={3}
  explanation="위상 정렬은 방향 비순환 그래프에서 모든 간선 u→v에 대해 u가 v보다 앞서도록 정점을 일렬로 나열하는 것이다. 그런데 그래프에 분기(여러 정점의 진입 차수가 동시에 0이 되는 지점)가 있으면, 그 순간 큐에 여러 후보가 동시에 들어가고 어떤 것을 먼저 꺼내도 정당한 위상 순서가 되므로 결과가 여러 개 존재할 수 있다. 예를 들어 위 그래프에서 정점 2와 3은 둘 다 정점 1 하나에만 의존하므로 1, 2, 3, 4, 5, 6 순서와 1, 3, 2, 4, 5, 6 순서가 모두 올바른 위상 정렬 결과가 된다. 따라서 결과가 항상 유일하다는 진술은 옳지 않다."
  optionExplanations={['옳은 설명이다. 사이클이 있으면 서로 먼저 와야 하는 모순이 생겨 순서를 정할 수 없다', '옳은 설명이다. 이것이 위상 정렬의 정의 그 자체다', '옳지 않은 설명이다. 진입 차수가 동시에 0이 되는 정점이 여러 개면 그 순서를 바꿔도 모두 올바른 위상 정렬이 되므로 결과가 여러 개일 수 있다', '옳은 설명이다. 사이클이 있으면 순환 관계에 있는 정점들 사이의 선후 관계를 정할 수 없다']}
/>

<Quiz
  questionNumber={7}
  question="Kahn 알고리즘(진입 차수 기반 위상 정렬)으로 위 DAG를 처리한다고 하자. 먼저 각 정점의 진입 차수를 구하면 1은 0, 2는 1, 3은 1, 4는 2, 5는 1, 6은 2이다. 진입 차수가 0인 정점을 큐에 넣고, 정점을 꺼낼 때마다 그 정점에서 나가는 간선을 제거하며 진입 차수가 0이 되는 정점을 새로 큐에 넣는 방식으로 진행할 때, 나올 수 있는 위상 정렬 결과로 옳은 것은?"
  options={['1, 2, 3, 4, 5, 6', '1, 3, 2, 5, 4, 6', '2, 1, 3, 4, 5, 6', '1, 4, 2, 3, 5, 6']}
  correctAnswer={1}
  explanation="진입 차수가 0인 정점은 1뿐이므로 먼저 1을 꺼낸다. 1에서 나가는 간선 1→2, 1→3을 제거하면 2와 3의 진입 차수가 각각 0이 되어 큐에 들어간다(순서상 2, 3). 2를 꺼내면 간선 2→4가 제거되어 4의 진입 차수가 2에서 1로 줄지만 아직 0이 아니다. 3을 꺼내면 간선 3→4, 3→6이 제거되어 4의 진입 차수가 1에서 0이 되고 6의 진입 차수가 2에서 1로 줄어든다. 이제 4가 큐에 들어간다. 4를 꺼내면 간선 4→5가 제거되어 5의 진입 차수가 0이 되어 큐에 들어간다. 5를 꺼내면 간선 5→6이 제거되어 6의 진입 차수가 1에서 0이 되어 큐에 들어간다. 마지막으로 6을 꺼낸다. 결과 순서는 1, 2, 3, 4, 5, 6이며, 모든 간선 u→v에서 u가 v보다 앞에 있는지 검산해도 조건을 만족한다."
  optionExplanations={['정답이다. 진입 차수 0부터 차례로 제거해 나가는 표준 Kahn 알고리즘 절차를 그대로 따른 결과다', '3을 2보다 먼저 꺼낸 경우인데, 두 정점이 동시에 큐에 들어갈 때 어느 쪽을 먼저 꺼내도 무방하지만 이 보기의 이후 순서(5, 4)는 간선 4→5를 위반한다. 4가 5보다 먼저 와야 한다', '정점 2의 진입 차수는 처음부터 1이므로 시작 시점에는 큐에 들어갈 수 없다. 1이 먼저 처리되어야 2의 진입 차수가 0이 된다', '정점 4는 2와 3에서 들어오는 간선이 모두 제거되어야 진입 차수가 0이 되므로, 2 하나만 처리된 시점에는 아직 큐에 들어갈 수 없다']}
/>

<Quiz
  questionNumber={8}
  question="Kahn 알고리즘을 수행하는 도중, 모든 간선을 다 제거했는데도 큐가 비어 아직 방문하지 못한 정점이 남아 있다면 이는 무엇을 의미하는가?"
  options={['원래 그래프에 사이클이 존재한다는 뜻이다', '그래프가 완전 그래프라는 뜻이다', '그래프에 고립된 정점(간선이 하나도 없는 정점)이 있다는 뜻이다', '그래프의 간선에 음수 가중치가 있다는 뜻이다']}
  correctAnswer={1}
  explanation="Kahn 알고리즘은 진입 차수가 0인 정점만 큐에 넣고 처리한다. 만약 사이클이 있다면 그 사이클에 속한 정점들은 서로가 서로의 진입 차수를 유지시켜 주기 때문에(예를 들어 X→Y→Z→X 사이클에서 X의 진입 차수는 Z가 처리되기 전까지 0이 될 수 없고, Z의 진입 차수는 Y가, Y의 진입 차수는 X가 처리되어야 줄어드는데 아무도 먼저 처리될 수 없다) 진입 차수가 영원히 0이 되지 않는다. 따라서 알고리즘이 종료된 시점에 처리된 정점 수가 전체 정점 수보다 적다면, 처리되지 못하고 남은 정점들이 사이클을 이루고 있다는 뜻이며, 이것이 바로 Kahn 알고리즘을 이용한 사이클 판별법이다."
  optionExplanations={['정답이다. 사이클에 속한 정점들은 서로 맞물려 진입 차수가 0이 될 기회를 얻지 못하므로 큐에 들어가지 못한 채 남는다', '완전 그래프라도 방향 비순환 구조라면 정상적으로 위상 정렬이 끝까지 진행된다. 완전성과 이 현상은 무관하다', '고립된 정점은 진입 차수가 처음부터 0이므로 오히려 가장 먼저 큐에 들어가 쉽게 처리된다', 'Kahn 알고리즘의 진입 차수 계산은 가중치와 무관하게 간선의 존재 여부만 센다. 음수 가중치는 이 현상과 관계가 없다']}
/>

다음 5문항(9~13)은 최소 신장 트리(MST) 문제입니다. 다음 무방향 가중치 그래프를 사용합니다.

- 정점: A, B, C, D, E
- 간선(가중치): A–C(1), B–C(2), D–E(2), A–B(4), B–D(5), B–E(6), C–D(8), C–E(10)

<Quiz
  questionNumber={9}
  question="최소 신장 트리(MST)의 성질에 대한 설명으로 옳지 않은 것은?"
  options={['정점이 n개인 연결 그래프의 MST는 항상 n - 1개의 간선을 가진다', 'MST에는 사이클이 존재하지 않는다', '그래프에 가중치가 같은 간선이 여러 개 있어도 MST를 구성하는 간선의 총 가중치는 항상 유일하게 정해진다', '같은 그래프에 대해 프림 알고리즘과 크루스칼 알고리즘은 항상 완전히 동일한 간선 집합을 선택한다']}
  correctAnswer={4}
  explanation="MST는 모든 정점을 연결하면서 총 가중치의 합이 최소가 되는 트리이므로 트리의 정의상 간선이 n - 1개이고 사이클이 없다. 또한 MST의 총 가중치 자체는(간선 가중치가 같은 것이 있어도) 항상 유일하게 정해진다는 사실이 증명되어 있다(최소 가중치 값 자체는 유일). 그러나 가중치가 같은 간선이 여러 개 존재하면, 어떤 간선을 선택하느냐에 따라 총 가중치는 같지만 서로 다른 간선 구성을 갖는 MST가 여러 개 나올 수 있다. 프림과 크루스칼은 서로 다른 방식으로 간선을 고르기 때문에, 동점 간선이 있을 때 실제로 선택하는 간선 집합이 달라질 수 있다(총 가중치는 같음). 따라서 두 알고리즘이 항상 완전히 동일한 간선 집합을 선택한다는 진술은 옳지 않다."
  optionExplanations={['옳은 설명이다. 트리의 정의(사이클 없이 모든 정점이 연결됨)에서 간선 수는 항상 정점 수보다 1 적다', '옳은 설명이다. 사이클이 있으면 간선 하나를 제거해도 연결성이 유지되면서 가중치 합이 줄어들어 최소성에 모순된다', '옳은 설명이다. MST를 이루는 구체적인 간선 구성은 여러 개일 수 있어도 그 총 가중치의 최솟값 자체는 유일하다', '옳지 않은 설명이다. 동점 간선이 존재하면 두 알고리즘이 서로 다른 간선을 선택해 총 가중치는 같지만 간선 구성이 다른 MST를 만들어낼 수 있다']}
/>

<Quiz
  questionNumber={10}
  question="위 그래프에서 크루스칼(Kruskal) 알고리즘을 수행한다고 하자. 간선을 가중치 오름차순으로 정렬하면 A–C(1), B–C(2), D–E(2), A–B(4), B–D(5), B–E(6), C–D(8), C–E(10) 순서다. 사이클을 만들지 않는 간선만 골라 트리를 완성할 때, 선택되는 간선 집합과 총 가중치로 옳은 것은?"
  options={['A–C, B–C, D–E, B–D (총 가중치 10)', 'A–C, B–C, D–E, A–B (총 가중치 9)', 'A–C, B–C, A–B, B–D (총 가중치 12)', 'A–C, D–E, B–D, B–E (총 가중치 15)']}
  correctAnswer={1}
  explanation="가중치가 작은 간선부터 순서대로 검토하되, 두 끝점이 이미 같은 그룹(같은 집합)에 속해 있으면 사이클이 생기므로 건너뛴다. A–C(1)를 추가한다(A, C가 한 그룹). B–C(2)를 추가한다(B가 합류해 A, B, C가 한 그룹). D–E(2)를 추가한다(D, E가 별도 그룹). A–B(4)는 A와 B가 이미 같은 그룹이므로 사이클이 생겨 건너뛴다. B–D(5)를 검토하면 B는 (A, B, C) 그룹, D는 (D, E) 그룹으로 서로 다르므로 추가한다. 이제 모든 정점이 하나로 연결되고 간선 수도 4개(정점 5개이므로 n - 1 = 4)가 되어 종료한다. 선택된 간선은 A–C, B–C, D–E, B–D이고 총 가중치는 1 + 2 + 2 + 5 = 10이다."
  optionExplanations={['정답이다. 사이클을 만들지 않는 간선만 오름차순으로 골라 n - 1개를 모은 결과다', 'A–B(4)를 넣어 계산한 값인데, 이 시점에는 A와 B가 이미 B–C, A–C를 통해 같은 그룹에 속해 있으므로 A–B를 추가하면 사이클이 생겨 선택할 수 없다', 'B–D 대신 A–B를 넣고 총 가중치를 잘못 더한 경우다. A–B는 사이클을 만들기 때문에 애초에 선택 대상이 아니다', 'A–B(4) 대신 B–E(6)를 넣어 계산한 값인데, B–E를 넣지 않아도 이미 B–D로 D, E 그룹이 연결되므로 B–E는 필요 없는 간선이다']}
/>

<Quiz
  questionNumber={11}
  question="크루스칼 알고리즘에서 어떤 간선의 두 끝점이 이미 같은 그룹(집합)에 속해 있는지, 즉 그 간선을 추가하면 사이클이 생기는지를 판정할 때 표준적으로 사용하는 자료구조는?"
  options={['유니온-파인드(Union-Find, 서로소 집합)', '최소 힙(min-heap)', '연결 리스트(linked list)', '해시 테이블(hash table)']}
  correctAnswer={1}
  explanation="유니온-파인드(서로소 집합, disjoint set)는 여러 원소를 그룹으로 묶고, 두 원소가 같은 그룹에 속하는지(find 연산)와 두 그룹을 하나로 합치는 것(union 연산)을 매우 빠르게 처리하는 자료구조다. 경로 압축과 랭크(또는 크기) 기준 합치기를 함께 쓰면 각 연산이 평균적으로 거의 상수 시간(역 아커만 함수 수준)에 처리된다. 크루스칼 알고리즘은 간선을 가중치 순으로 하나씩 검토하면서 두 끝점이 이미 같은 그룹인지(find)를 확인하고, 다르면 간선을 선택하고 두 그룹을 합치는(union) 과정을 반복하므로 유니온-파인드가 정확히 이 요구에 들어맞는다. 최소 힙은 오히려 프림 알고리즘에서 다음에 추가할 최소 가중치 간선을 빠르게 꺼내는 데 주로 쓰인다."
  optionExplanations={['정답이다. 그룹 소속 확인(find)과 그룹 합치기(union)를 빠르게 지원하는 자료구조가 유니온-파인드다', '최소 힙은 프림 알고리즘에서 다음 후보 간선을 빠르게 꺼내는 용도로 자주 쓰이며, 사이클 판정 자체와는 직접적인 관련이 없다', '연결 리스트만으로는 두 원소가 같은 그룹인지 빠르게 판정하기 어렵다', '해시 테이블은 키-값 검색에는 유용하지만 그룹 합치기(union) 연산을 효율적으로 지원하지 않는다']}
/>

<Quiz
  questionNumber={12}
  question="같은 그래프에서 프림(Prim) 알고리즘을 정점 A에서 시작해 수행한다고 하자. 매 단계마다 현재 트리에 포함된 정점들과 연결된 간선 중 가중치가 가장 작은 것을 고른다고 할 때, 간선이 선택되는 순서로 옳은 것은?"
  options={['A–C(1), B–C(2), B–D(5), D–E(2)', 'A–C(1), B–C(2), D–E(2), B–D(5)', 'A–B(4), B–C(2), B–D(5), D–E(2)', 'A–C(1), C–D(8), D–E(2), B–C(2)']}
  correctAnswer={1}
  explanation="시작 정점은 A뿐이다. A와 연결된 간선은 A–B(4), A–C(1)이고 이 중 최소는 A–C(1)이므로 선택하고 C를 트리에 편입한다. 이제 트리는 (A, C)이고, 트리와 바깥을 잇는 간선 후보는 A–B(4), C–B(2), C–D(8), C–E(10)이다. 이 중 최소는 C–B(2)이므로 선택하고 B를 편입한다. 트리는 (A, B, C)이고, 후보 간선은 B–D(5), B–E(6), C–D(8), C–E(10)이다. 최소는 B–D(5)이므로 선택하고 D를 편입한다. 트리는 (A, B, C, D)이고, 후보는 D–E(2), C–E(10), B–E(6)이다. 최소는 D–E(2)이므로 선택하고 E를 편입한다. 모든 정점이 편입되어 종료한다. 선택 순서는 A–C(1), B–C(2), B–D(5), D–E(2)이며, 총 가중치는 크루스칼과 같은 10이다(간선 선택 순서는 다르지만 총 가중치는 동일)."
  optionExplanations={['정답이다. 매 단계에서 트리와 바깥을 잇는 최소 가중치 간선만 고르는 프림 알고리즘의 절차를 그대로 따른 결과다', 'D–E를 B–D보다 먼저 선택했는데, D–E는 D 또는 E가 아직 트리에 편입되지 않은 시점에는 트리와 바깥을 잇는 간선이 아니므로 후보에 들어올 수 없다(D가 먼저 편입되어야 D–E가 후보가 된다)', 'A–B(4)를 A–C(1)보다 먼저 선택했는데, 첫 단계에서는 가중치가 더 작은 A–C(1)를 선택해야 한다', 'C–D(8)를 두 번째로 선택했는데, 이 시점에 가중치 2인 C–B가 후보에 있으므로 그보다 가중치가 큰 C–D를 먼저 고르는 것은 프림 알고리즘의 최소 선택 규칙에 어긋난다']}
/>

<Quiz
  questionNumber={13}
  question="프림 알고리즘과 크루스칼 알고리즘의 선택 기준으로 옳은 것은?"
  options={['정점이 많고 간선이 상대적으로 적은 희소 그래프에서는 간선을 정렬해 처리하는 크루스칼이 유리하고, 간선이 매우 많은 밀집 그래프에서는 인접 정점 갱신 방식인 프림이 유리한 경우가 많다', '프림과 크루스칼은 항상 정확히 같은 시간 복잡도를 가지므로 어떤 그래프에도 선택 기준이 존재하지 않는다', '크루스칼은 방향 그래프에서만 동작하고 프림은 무방향 그래프에서만 동작한다', '프림 알고리즘은 반드시 인접 행렬로만 구현할 수 있고 인접 리스트로는 구현할 수 없다']}
  correctAnswer={1}
  explanation="크루스칼은 모든 간선을 가중치 순으로 정렬한 뒤 유니온-파인드로 사이클을 검사하므로 시간 복잡도가 대략 O(E log E)이며, 간선 수 E가 상대적으로 적은 희소 그래프에서 효율적이다. 프림은(우선순위 큐를 함께 쓰면) 트리에 인접한 간선들을 계속 갱신하며 최소값을 꺼내는 방식으로 O(E log V)이며, 간선이 매우 많아 정렬 비용이 커지는 밀집 그래프에서도 상대적으로 안정적인 성능을 낸다. 두 알고리즘 모두 무방향 가중치 그래프를 대상으로 하며, 프림은 인접 리스트와 우선순위 큐 조합으로도 흔히 구현된다."
  optionExplanations={['정답이다. 간선 밀도에 따라 정렬 비용(크루스칼)과 우선순위 큐 갱신 비용(프림)의 상대적 유불리가 갈린다', '두 알고리즘의 시간 복잡도 표현식(O(E log E) 대 O(E log V))이 다르고 실제 성능도 그래프 밀도에 따라 달라지므로 이 진술은 옳지 않다', 'MST 알고리즘은 무방향 가중치 연결 그래프를 대상으로 정의된다. 방향 그래프 전용이라는 구분은 옳지 않다', '프림 알고리즘은 인접 리스트와 우선순위 큐(최소 힙)를 함께 쓰는 구현이 오히려 더 일반적이다']}
/>

다음 4문항(14~17)은 최단경로 알고리즘 문제입니다. 다음 방향 가중치 그래프에서 정점 S를 출발점으로 합니다.

- 정점: S, A, B, C, D
- 간선(방향, 가중치): S→A(4), S→B(2), B→A(1), B→C(8), B→D(10), A→C(5), C→D(2)

<Quiz
  questionNumber={14}
  question="다익스트라(Dijkstra) 알고리즘을 위 그래프에 정점 S에서 적용한다고 하자. 알고리즘을 진행하는 각 단계에서 아직 확정되지 않은 정점 중 현재까지의 최단 거리 추정값이 가장 작은 정점을 확정(처리)하고, 그 정점을 거쳐 갈 수 있는 인접 정점의 거리를 갱신한다. 이 과정을 반복했을 때 S에서 각 정점까지의 최종 최단 거리로 옳은 것은?"
  options={['S=0, B=2, A=3, C=8, D=10', 'S=0, A=4, B=2, C=9, D=11', 'S=0, B=2, A=4, C=10, D=20', 'S=0, A=3, B=2, C=8, D=9']}
  correctAnswer={1}
  explanation="초기값은 S=0, 나머지는 무한대다. S를 확정하고 인접 정점을 갱신하면 A=4(S→A), B=2(S→B)가 된다. 아직 미확정 정점 중 가장 작은 값은 B(2)이므로 B를 확정한다. B의 인접 정점을 갱신하면 A는 min(4, 2+1) = 3이 되어 3으로 갱신되고, C는 min(무한대, 2+8) = 10, D는 min(무한대, 2+10) = 12로 갱신된다. 다음으로 가장 작은 값은 A(3)이므로 A를 확정한다. A의 인접 정점 C는 min(10, 3+5) = 8로 갱신된다. 다음으로 가장 작은 값은 C(8)이므로 C를 확정한다. C의 인접 정점 D는 min(12, 8+2) = 10으로 갱신된다. 마지막으로 D(10)를 확정한다. 최종 결과는 S=0, B=2, A=3, C=8, D=10이다."
  optionExplanations={['정답이다. 매 단계 최소값 정점을 확정하며 완화(relax)를 반복 적용한 전 과정의 결과다', 'B를 거쳐 A로 가는 더 짧은 경로(2+1=3)를 반영하지 않고 S에서 A로 바로 가는 값(4)을 그대로 최종값으로 둔 오류다', 'B를 거쳐 A로 가는 경로를 반영했지만(4로 잘못 유지) C, D 갱신에서 A를 거치는 더 짧은 경로(A→C=5)를 반영하지 않은 오류다', 'A를 3으로는 올바르게 갱신했지만 C를 A 경유로 갱신하지 않고 B 경유 값(10)을 그대로 둔 오류다']}
/>

<Quiz
  questionNumber={15}
  question="다익스트라 알고리즘이 요구하는 전제 조건과, 이를 어겼을 때 생기는 문제로 옳은 것은?"
  options={['모든 간선의 가중치가 음수가 아니어야 한다. 음수 가중치가 있으면 이미 확정한 정점의 거리값이 나중에 더 줄어들 수 있어 알고리즘의 정확성이 깨진다', '그래프에 사이클이 없어야 한다. 사이클이 있으면 무한 루프에 빠져 알고리즘이 종료되지 않는다', '모든 정점의 차수가 같아야 한다. 차수가 다르면 우선순위 큐의 크기를 예측할 수 없다', '간선의 가중치가 모두 정수여야 한다. 실수 가중치는 우선순위 큐에 저장할 수 없다']}
  correctAnswer={1}
  explanation="다익스트라 알고리즘은 '한 번 확정한 정점의 최단 거리는 이후에 절대 줄어들지 않는다'는 전제 위에서 동작한다. 이 전제가 성립하려면 모든 간선의 가중치가 0 이상이어야 한다. 만약 음수 가중치 간선이 존재하면, 이미 확정해 버린 정점을 나중에 다른 경로(음수 간선을 포함하는 경로)로 거치는 것이 실제로는 더 짧을 수 있는데, 다익스트라는 한 번 확정한 정점을 다시 검토하지 않으므로 이 경우를 놓쳐 잘못된 결과를 낼 수 있다. 이런 경우에는 음수 가중치를 허용하는 벨만-포드 알고리즘을 사용해야 한다. 다익스트라는 사이클이 있는 그래프에도 적용할 수 있으며(양수 가중치 사이클이라면 문제 없음), 정수·실수 가중치 여부나 정점 차수의 균일성과는 무관하다."
  optionExplanations={['정답이다. 확정된 최단 거리가 다시 줄어들지 않는다는 다익스트라의 핵심 전제는 비음수 가중치를 필요로 한다', '다익스트라는 사이클이 있어도(가중치가 비음수인 한) 정상적으로 동작하며 무한 루프에 빠지지 않는다. 각 정점은 최대 한 번만 확정된다', '정점 차수는 알고리즘의 정확성과 무관하다. 우선순위 큐는 차수와 상관없이 필요한 만큼만 원소를 담는다', '가중치가 실수여도 크기 비교만 가능하면 우선순위 큐에 문제없이 저장하고 비교할 수 있다']}
/>

<Quiz
  questionNumber={16}
  question="위와 같은 방향 그래프에서 S→A 간선의 가중치가 4가 아니라 -3(음수)이라고 바꿔 생각해보자. 다익스트라 알고리즘을 그대로 적용하면 어떤 문제가 발생하는가?"
  options={['S 다음으로 B(2)가 먼저 확정되어 B→A(1)를 거친 A값(3)만 반영되고, 더 짧을 수 있는 S→A(-3)를 나중에 발견해도 이미 확정된 값이 갱신되지 않아 실제 최단 거리 -3을 찾지 못할 수 있다', '음수 가중치가 있으면 인접 리스트 자체를 만들 수 없어 알고리즘을 시작조차 할 수 없다', '음수 가중치는 우선순위 큐에서 항상 가장 먼저 꺼내지므로 오히려 더 정확한 결과가 나온다', '다익스트라는 음수 가중치를 만나면 자동으로 벨만-포드 방식으로 전환되므로 문제가 없다']}
  correctAnswer={1}
  explanation="다익스트라는 매 단계에서 '현재까지 추정값이 가장 작은 정점'을 확정하고 다시는 그 값을 갱신하지 않는다. S→B(2)가 S→A(-3)보다 이 시점의 추정값 비교에서 어떻게 처리되는지와 무관하게, 핵심 문제는 일단 어떤 정점이 확정된 뒤에 더 작은 값을 만드는 음수 간선이 뒤늦게 발견되어도 이미 확정된 값은 절대 다시 검토하지 않는다는 점이다. 이 예에서는 S→A가 직접 -3이므로 오히려 일찍 반영될 수도 있지만, 일반적으로 음수 간선이 그래프 뒤쪽에 숨어 있으면(예: 다른 정점을 거쳐야 음수 간선에 도달하는 구조) 이미 확정한 정점의 실제 최단 거리를 놓치는 일이 발생한다. 이것이 다익스트라가 음수 가중치에서 정확성을 보장하지 못하는 근본 이유이며, 실제 시험에서는 이 원리(확정 후 재검토 없음)를 설명할 수 있는지를 묻는다."
  optionExplanations={['정답이다. 확정된 정점을 다시 검토하지 않는 다익스트라의 구조적 특성이 음수 가중치 아래에서 정확성을 깨뜨리는 근본 원인이다', '가중치의 부호는 인접 리스트를 구성하는 데 아무 문제가 되지 않는다. 인접 리스트는 단순히 (인접 정점, 가중치) 쌍을 저장할 뿐이다', '우선순위 큐는 저장된 값을 기준으로 최솟값을 꺼낼 뿐이며, 음수라고 해서 항상 가장 먼저 꺼내진다는 보장은 없다(다른 값이 더 작을 수도 있음)', '다익스트라 알고리즘 자체에는 벨만-포드로 자동 전환되는 로직이 없다. 이는 알고리즘 설계자가 상황에 맞게 직접 선택해야 하는 별개의 알고리즘이다']}
/>

<Quiz
  questionNumber={17}
  question="벨만-포드(Bellman-Ford) 알고리즘에 대한 설명으로 옳지 않은 것은?"
  options={['정점 수가 V일 때 모든 간선에 대한 완화(relaxation)를 V - 1번 반복하면 음수 사이클이 없는 그래프에서 모든 최단 거리가 정확히 계산된다', '음수 가중치 간선이 있어도 음수 사이클만 없다면 정확한 최단 거리를 구할 수 있다', 'V - 1번 반복이 끝난 후 한 번 더(V번째) 완화를 시도했을 때 값이 갱신되는 간선이 있다면 그래프에 음수 사이클이 존재한다는 뜻이다', '벨만-포드의 시간 복잡도는 다익스트라(우선순위 큐 사용시 O(E log V))보다 항상 더 빠르다']}
  correctAnswer={4}
  explanation="벨만-포드는 매 반복마다 그래프의 모든 간선을 한 번씩 검사하며 완화를 시도하고, 이를 V - 1번 반복한다. 최단 경로는 사이클이 없다면 최대 V - 1개의 간선으로 이루어지므로, V - 1번 반복하면 모든 최단 거리가 안정적으로 확정된다는 것이 이 알고리즘의 원리다. 시간 복잡도는 O(V * E)로, 간선 하나하나를 V - 1번 반복해서 검사하기 때문이다. 반면 다익스트라는 우선순위 큐를 사용하면 O(E log V)로 대부분의 그래프에서 벨만-포드보다 빠르다. 따라서 벨만-포드가 다익스트라보다 항상 더 빠르다는 진술은 옳지 않다. 벨만-포드의 장점은 속도가 아니라 음수 가중치를 허용하고 음수 사이클을 탐지할 수 있다는 점이다."
  optionExplanations={['옳은 설명이다. 사이클이 없는 최단 경로의 간선 수는 최대 V - 1개이므로 이만큼 반복하면 충분하다', '옳은 설명이다. 이것이 벨만-포드가 다익스트라와 달리 음수 가중치를 다룰 수 있는 이유다', '옳은 설명이다. 이미 안정되었어야 할 값이 V번째 반복에서도 계속 줄어든다면 무한히 줄어들 수 있는 음수 사이클이 있다는 신호다', '옳지 않은 설명이다. 벨만-포드는 O(V * E)로 일반적으로 다익스트라(O(E log V), 우선순위 큐 사용시)보다 느리며, 대신 음수 가중치를 다룰 수 있다는 점이 장점이다']}
/>

다음 4문항(18~21)은 벨만-포드와 플로이드-워셜의 트레이싱 문제입니다. 벨만-포드는 다음 방향 그래프를 사용합니다.

- 정점: S, A, B, C
- 간선(방향, 가중치): S→A(4), S→B(5), A→B(-2), A→C(3), B→C(4)

<Quiz
  questionNumber={18}
  question="벨만-포드 알고리즘을 정점 S에서 시작해 위 그래프에 적용한다. 간선을 S→A, S→B, A→B, A→C, B→C 순서로 완화한다고 할 때, 1회차 반복(모든 간선을 한 번씩 검사)이 끝난 직후 각 정점의 거리값으로 옳은 것은?"
  options={['S=0, A=4, B=2, C=6', 'S=0, A=4, B=5, C=7', 'S=0, A=4, B=2, C=7', 'S=0, A=4, B=5, C=9']}
  correctAnswer={1}
  explanation="초기값은 S=0, A=B=C=무한대다. 1회차 반복을 순서대로 진행한다. 간선 S→A(4)를 검사하면 0+4=4가 무한대보다 작으므로 A=4로 갱신한다. 간선 S→B(5)를 검사하면 0+5=5가 무한대보다 작으므로 B=5로 갱신한다. 간선 A→B(-2)를 검사하면 A(4) + (-2) = 2가 현재 B값 5보다 작으므로 B=2로 다시 갱신한다(같은 반복 회차 안에서도 방금 갱신된 A값을 즉시 사용하는 것이 벨만-포드의 표준 동작이다). 간선 A→C(3)를 검사하면 4+3=7이 무한대보다 작으므로 C=7로 갱신한다. 간선 B→C(4)를 검사하면 방금 갱신된 B(2) + 4 = 6이 현재 C값 7보다 작으므로 C=6으로 다시 갱신한다. 1회차가 끝난 시점의 값은 S=0, A=4, B=2, C=6이다."
  optionExplanations={['정답이다. 같은 회차 안에서 이미 갱신된 값을 이어서 사용하는 완화 순서를 정확히 반영한 결과다', 'A→B(-2) 완화를 반영하지 않아 B가 5에 머문 경우다. A가 먼저 갱신된 뒤에는 A→B를 검사할 때 반드시 2로 다시 낮아져야 한다', 'B는 올바르게 2로 갱신했지만, B→C 완화에서 갱신된 B(2)를 쓰지 않고 이전 C값(7)을 그대로 둔 경우다', 'A→B와 B→C 완화를 모두 반영하지 않고 S→B, A→C 값만 그대로 사용한 경우다']}
/>

<Quiz
  questionNumber={19}
  question="위 1회차 결과(S=0, A=4, B=2, C=6)에 대해 2회차 반복을 같은 순서로 다시 진행하면 어떤 일이 일어나는가?"
  options={['어떤 간선을 완화해도 값이 더 이상 줄어들지 않는다. 즉 1회차만으로 이미 모든 최단 거리가 확정되었다', 'B값이 다시 줄어들어 음수가 된다', 'C값이 다시 늘어나 무한대로 돌아간다', 'S값이 음수로 바뀐다']}
  correctAnswer={1}
  explanation="2회차에서 같은 간선들을 같은 순서로 다시 검사해 본다. S→A: 0+4=4, 현재 A(4)와 같아 갱신 없음. S→B: 0+5=5, 현재 B(2)보다 크므로 갱신 없음. A→B: 4+(-2)=2, 현재 B(2)와 같아 갱신 없음. A→C: 4+3=7, 현재 C(6)보다 크므로 갱신 없음. B→C: 2+4=6, 현재 C(6)와 같아 갱신 없음. 모든 간선에서 값이 줄어들지 않으므로 이미 1회차 만에 최단 거리가 안정되었다는 뜻이다(이 그래프는 정점이 4개이므로 이론상 최대 3회 반복이 필요할 수 있지만, 실제로는 그보다 일찍 안정될 수 있다). 이는 음수 사이클이 없다는 근거이기도 하다."
  optionExplanations={['정답이다. 완화를 반복해도 어떤 값도 줄어들지 않으면 이미 최단 거리가 확정된 것이다', '이 그래프에는 음수 사이클이 없으므로 값이 무한정 줄어드는 일은 일어나지 않는다. B는 2로 안정된다', '벨만-포드에서 한 번 줄어든 거리값은 더 짧은 경로가 없는 한 다시 늘어나지 않는다. 완화는 항상 더 작은 값으로만 갱신한다', 'S는 출발점으로 고정된 0이며, 다른 어떤 완화로도 변하지 않는다']}
/>

<Quiz
  questionNumber={20}
  question="위 그래프에 간선 C→A(-10)을 추가로 넣는다고 하자. 이렇게 하면 A→B(-2), B→C(4), C→A(-10)로 이어지는 사이클의 총 가중치는 -2 + 4 + (-10) = -8로 음수가 된다. 이 상태에서 벨만-포드 알고리즘을 V - 1 = 3회 반복한 뒤 4번째(V번째) 완화를 한 번 더 시도하면 어떤 일이 일어나는가?"
  options={['A, B, C 중 사이클에 포함된 정점의 거리값이 4번째 완화에서도 계속 줄어들며, 이는 음수 사이클이 존재한다는 것을 알려주는 신호다', 'S의 거리값이 처음으로 갱신된다', '알고리즘이 즉시 종료되며 아무 값도 변하지 않는다', '거리값이 모두 0으로 초기화된다']}
  correctAnswer={1}
  explanation="음수 사이클이 존재하면 그 사이클을 돌 때마다 총 거리가 계속 줄어들 수 있으므로 '최단 거리'라는 개념 자체가 정의되지 않는다(무한히 작아질 수 있음). 벨만-포드는 이런 상황을 V - 1번의 정상 반복만으로는 알아채지 못할 수 있지만, V - 1번 반복 후 값이 이미 안정되었어야 함에도 V번째 완화에서 여전히 값이 줄어드는 간선이 있다면, 이는 사이클을 한 바퀴 더 돌아서 값이 더 작아질 수 있다는 뜻이므로 음수 사이클이 존재한다고 판정한다. 이 문제에서는 C→A(-10) 간선이 추가되어 A→B→C→A 사이클의 총 가중치가 -8이 되므로, 이 사이클에 포함된 A, B, C의 거리값이 4번째 완화에서도 계속 줄어드는 현상이 관찰된다."
  optionExplanations={['정답이다. 안정되었어야 할 값이 V번째 완화에서도 줄어드는 것이 음수 사이클 존재의 표준적인 판정 기준이다', '출발점 S의 거리는 0으로 고정되며, S로 들어오는 간선이 없는 한 다른 값으로 갱신될 근거가 없다', '음수 사이클이 있을 때는 오히려 값이 계속 갱신되는 것이 정상이며, 알고리즘이 즉시 멈추는 것은 아니다(다만 판정 후 종료 처리는 구현자가 정한다)', '완화는 기존 값보다 더 작은 값을 발견했을 때만 갱신하는 것이지, 임의로 0으로 초기화하는 연산이 아니다']}
/>

다음 2문항(21~22)은 플로이드-워셜(Floyd-Warshall) 알고리즘 문제입니다. 다음 방향 그래프의 직접 연결된 거리(연결이 없으면 무한대)를 사용합니다.

- 정점: A, B, C, D
- A→B(5), A→D(10), B→C(3), C→D(1) (그 외 직접 간선 없음)

<Quiz
  questionNumber={21}
  question="플로이드-워셜 알고리즘에 대한 설명으로 옳은 것은?"
  options={['모든 정점 쌍의 최단 거리를 구하는 동적계획법 알고리즘으로, k번째 단계에서는 정점 1부터 k까지만 경유지로 허용했을 때의 최단 거리를 갱신하며, 시간 복잡도는 O(V^3)이다', '단일 출발점에서 다른 모든 정점까지의 최단 거리만 구할 수 있으며 다익스트라보다 항상 빠르다', '음수 가중치가 하나라도 있으면 시작할 수 없는 알고리즘이다', '그래프를 인접 리스트로 표현해야만 동작하며 인접 행렬로는 구현할 수 없다']}
  correctAnswer={1}
  explanation="플로이드-워셜은 모든 정점 쌍(all-pairs) 사이의 최단 거리를 한 번에 구하는 동적계획법 알고리즘이다. dist[i][j]를 정점 i에서 j까지의 현재까지 알려진 최단 거리라 하면, k = 1부터 V까지 순서대로 '정점 k를 경유했을 때 더 짧아지는가(dist[i][j]가 dist[i][k] + dist[k][j]보다 큰가)'를 모든 (i, j) 쌍에 대해 검사하며 갱신한다. k, i, j 세 개의 중첩 반복문을 사용하므로 시간 복잡도는 O(V^3)이다. 단일 출발점 최단경로에 특화된 다익스트라와 달리 모든 쌍을 구하는 데 특화되어 있고, 음수 가중치가 있어도(음수 사이클만 없다면) 정확히 동작하며, 보통 인접 행렬 형태의 dist 배열로 구현한다."
  optionExplanations={['정답이다. k를 경유지 후보로 하나씩 늘려가며 모든 쌍의 최단 거리를 갱신하는 동적계획법 구조를 정확히 설명한다', '플로이드-워셜은 단일 출발점이 아니라 모든 정점 쌍의 최단 거리를 구하는 알고리즘이며, 다익스트라보다 항상 빠르다는 것도 사실이 아니다(단일 출발점만 필요하면 다익스트라가 보통 더 빠르다)', '플로이드-워셜은 음수 가중치가 있어도(음수 사이클이 없는 한) 정확한 결과를 낸다는 점이 다익스트라와의 중요한 차이다', '플로이드-워셜은 dist[i][j] 형태의 2차원 배열(인접 행렬과 같은 구조)로 구현하는 것이 표준적이다']}
/>

<Quiz
  questionNumber={22}
  question="위 그래프에 플로이드-워셜을 적용한다고 하자. 초기 dist 값은 A→B=5, A→D=10, B→C=3, C→D=1이고 그 외는 무한대다(자기 자신은 0). 경유지 k를 A, B, C, D 순서로 검토할 때, k=B와 k=C 단계에서 새로 갱신되는 값으로 옳은 것은?"
  options={['k=B 단계에서 A→C가 5+3=8로 갱신되고, k=C 단계에서 A→D가 8+1=9로, B→D가 3+1=4로 갱신된다', 'k=B 단계에서 A→D가 곧바로 9로 갱신되고, k=C 단계에서는 아무것도 갱신되지 않는다', 'k=A 단계에서 이미 모든 값이 최종 확정되어 k=B, k=C 단계에서는 아무 갱신도 일어나지 않는다', 'k=B 단계에서 C→A가 갱신되고, k=C 단계에서 D→A가 갱신된다']}
  correctAnswer={1}
  explanation="k=A 단계에서는 A로 들어오는 간선이 없으므로(다른 정점에서 A로 가는 경로가 없음) A를 경유해 더 짧아지는 쌍이 없어 갱신이 일어나지 않는다. k=B 단계에서는 B를 경유하는 경로를 검토한다. A→B(5)와 B→C(3)를 이어 붙이면 A→C 경로가 5+3=8이 되는데, 기존 A→C는 무한대였으므로 8로 갱신된다. k=C 단계에서는 C를 경유하는 경로를 검토한다. 방금 갱신된 A→C(8)와 기존 C→D(1)를 이어 붙이면 A→D가 8+1=9가 되어, 기존 A→D(10)보다 작으므로 9로 갱신된다. 또한 B→C(3)와 C→D(1)를 이어 붙이면 B→D가 3+1=4가 되는데 기존 B→D는 무한대였으므로 4로 갱신된다. k=D 단계에서는 D로 나가는 간선이 없으므로 추가 갱신이 없다. 최종적으로 A→B=5, A→C=8, A→D=9, B→C=3, B→D=4, C→D=1이 된다."
  optionExplanations={['정답이다. k를 B, C로 늘려가며 경유지를 통한 더 짧은 경로를 순차적으로 반영한 전 과정의 결과다', 'A→D는 A→B→C→D(9)를 거쳐야 갱신되므로 k=C 단계까지 지나야 확정되며, k=B 단계에서 곧바로 9가 되지는 않는다(이 시점에는 아직 A→C가 8로 갱신된 직후이고 C→D를 더하는 것은 k=C 차례다)', 'A→C, A→D, B→D는 모두 k=A 단계에서는 갱신되지 않고 이후 k=B, k=C 단계에서 경유지를 통해 처음으로 짧아진다', '이 그래프에는 C에서 A로, D에서 A로 가는 간선 자체가 존재하지 않으므로 그런 경로가 새로 생길 수 없다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [RFC 문서 저장소 (그래프 알고리즘 실제 활용 사례 참고)](https://www.rfc-editor.org)
