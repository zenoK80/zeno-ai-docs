---
title: '22. 예상·기출 변형: 계산 복잡도·NP-완전'
description: 'P, NP, NP-완전, NP-난해, 다항시간 환원 개념을 판별·서술형 중심으로 재구성한 예상 문항 모음'
---

<Callout type="warning">
  이 편의 문항은 실제 독학사 기출 문제를 그대로 옮긴 것이 아닙니다. 국가평생교육진흥원이 공개한 4단계 알고리즘 과목 출제기준(평가영역)과 공개·복원된 기출의 유형 분포를 바탕으로, 동일한 출제 의도를 갖도록 새로 재구성한 예상·유사 문항입니다. 이 과목의 계산 복잡도 파트는 정의·판별 수준까지만 출제되며 증명 테크닉의 세부 전개는 범위 밖이므로, 문항도 그 수준에 맞추어 구성했습니다. 실제 시험의 문항 수·배점·시간은 매 회차 공고를 통해 반드시 다시 확인하세요.
</Callout>

<Quiz
  questionNumber={1}
  question="계산 복잡도 이론에서 다루는 결정 문제(decision problem)에 대한 설명으로 옳은 것은?"
  options={['입력에 대해 예 또는 아니오 중 하나로만 답이 정해지는 문제다. 예를 들어 최적화 문제인 배낭 문제는 최댓값을 구하는 대신 어떤 목표값 K가 주어졌을 때 그 이상의 가치를 담을 수 있는지 예 또는 아니오로 답하는 결정 문제로 바꿀 수 있다', '입력값에 상관없이 항상 참인 답만 존재하는 문제다', '반드시 그래프로 표현되어야만 하는 문제다', '최적화 문제와 결정 문제는 서로 완전히 무관해서 하나를 다른 것으로 바꿀 수 없다']}
  correctAnswer={1}
  explanation="결정 문제는 답이 예 또는 아니오(참 또는 거짓) 둘 중 하나로만 정해지는 문제를 말한다. 계산 복잡도 이론(P, NP 등)은 주로 이런 결정 문제를 기준으로 클래스를 정의한다. 그런데 실제로 우리가 관심 있는 문제는 대개 최댓값이나 최적해를 구하는 최적화 문제(optimization problem)인 경우가 많다. 이런 최적화 문제는 목표값 K를 매개변수로 추가해 '가치가 K 이상인 조합이 존재하는가'와 같은 결정 문제로 바꿀 수 있다. 예를 들어 배낭 문제의 최적화 버전(최대 가치를 구하라)은 '가치가 K 이상인 조합이 존재하는가'라는 결정 버전으로 변환할 수 있으며, 결정 버전이 어렵다면(다항시간에 풀 수 없다면) 그보다 어려운 최적화 버전도 다항시간에 풀 수 없다는 논리로 이어진다. 이 때문에 이론적으로는 결정 문제를 기준으로 어려움을 분석하는 것이 표준이다."
  optionExplanations={['정답이다. 결정 문제의 정의와, 최적화 문제를 결정 문제로 변환하는 표준적인 방법을 정확히 설명한다', '결정 문제는 입력에 따라 답이 예일 수도 아니오일 수도 있다. 항상 참만 나온다면 애초에 판별할 필요가 없는 자명한 문제가 된다', '결정 문제는 그래프 문제에 국한되지 않는다. 정렬 가능 여부, 소수 판별, 만족 가능성 판별 등 다양한 영역에 존재한다', '최적화 문제는 목표값을 매개변수로 추가함으로써 그에 대응하는 결정 문제로 바꿀 수 있으며, 계산 복잡도 이론에서는 이 변환이 핵심적인 도구로 널리 쓰인다']}
/>

<Quiz
  questionNumber={2}
  question="복잡도 클래스 P에 대한 설명으로 옳은 것은?"
  options={['입력 크기 n에 대해 다항식 시간(예: O(n), O(n^2), O(n^3) 등) 안에 답을 구할 수 있는 알고리즘이 존재하는 결정 문제들의 집합이다', '입력 크기와 무관하게 항상 상수 시간에 풀리는 문제들의 집합이다', '어떤 알고리즘으로도 풀 수 없는 문제들의 집합이다', '반드시 지수 시간이 걸리는 문제들의 집합이다']}
  correctAnswer={1}
  explanation="P(Polynomial time)는 다항식 시간 알고리즘, 즉 입력 크기 n의 다항식 함수(n^k 형태, k는 상수)로 표현되는 시간 복잡도 안에 정답을 구할 수 있는 결정 문제들의 클래스다. 정렬(O(n log n)), 두 정점 사이 최단경로 찾기(다익스트라, O(E log V)), 최대공약수 구하기(유클리드 호제법) 등이 P에 속하는 대표적인 문제다. 다항시간 알고리즘은 입력이 커져도 실행 시간이 상대적으로 감당할 수 있는 수준으로 늘어나기 때문에 '효율적으로 풀 수 있는 문제'의 기준으로 널리 받아들여진다. 이와 대비되는 것이 지수 시간(O(2^n) 등)이 필요한 것으로 알려진(또는 그렇게 추측되는) 문제들이다."
  optionExplanations={['정답이다. 다항식 시간 알고리즘으로 풀 수 있는 결정 문제의 집합이라는 P의 표준 정의를 정확히 설명한다', 'P에 속하는 문제도 입력 크기가 커지면 실행 시간이 늘어난다(다항식 함수를 따름). 항상 상수 시간이라는 것은 P의 정의와 다르다', 'P는 오히려 알고리즘으로 효율적으로 풀 수 있다고 알려진 문제들의 모임이다. 풀 수 없는 문제(계산 불가능 문제, undecidable)와는 다른 개념이다', 'P는 지수 시간이 아니라 다항식 시간 안에 풀리는 문제들의 집합이다. 지수 시간이 필요한 문제는 P에 속하지 않는 것으로 여겨진다']}
/>

<Quiz
  questionNumber={3}
  question="복잡도 클래스 NP에 대한 설명으로 옳은 것은?"
  options={['어떤 해(인증서, certificate)가 주어졌을 때 그것이 실제로 올바른 해인지를 다항시간 안에 검증(verify)할 수 있는 결정 문제들의 집합이다', 'NP는 Not Polynomial(다항시간이 아닌)의 약자로, 다항시간에 풀 수 없다고 증명된 문제들만 모은 집합이다', 'NP에 속하는 문제는 항상 P에 속하는 문제보다 반드시 더 오래 걸린다는 것이 증명되어 있다', 'NP는 비결정적(non-deterministic) 컴퓨터가 실제로 존재해야만 의미를 갖는 물리적 개념이다']}
  correctAnswer={1}
  explanation="NP(Nondeterministic Polynomial time)는 어떤 후보 해(인증서)가 주어졌을 때, 그 후보가 정말로 문제의 조건을 만족하는 올바른 해인지 확인하는 작업을 다항시간 안에 수행할 수 있는 결정 문제들의 집합이다. 예를 들어 해밀턴 사이클 문제(그래프의 모든 정점을 정확히 한 번씩 방문하고 시작점으로 돌아오는 경로가 있는지)는 그런 경로 자체를 처음부터 찾아내는 것은 어려워 보이지만, 만약 누군가 후보 경로를 하나 제시한다면 그 경로가 실제로 모든 정점을 한 번씩 지나는지 순서대로 확인하는 것은 간선 수에 비례하는 다항시간 안에 쉽게 할 수 있다. 이처럼 '검증의 용이성'이 NP의 핵심 정의다. NP라는 이름은 역사적으로 비결정적 튜링 기계라는 이론적 계산 모델에서 유래했지만, 실제 시험에서 중요한 것은 물리적 실재 여부가 아니라 '검증 가능성'이라는 정의 자체다."
  optionExplanations={['정답이다. 후보 해의 정당성을 다항시간에 검증할 수 있다는 것이 NP의 핵심 정의다', 'NP는 Not Polynomial의 약자가 아니라 Nondeterministic Polynomial time의 약자이며, 다항시간에 풀 수 없다고 증명된 문제만 모은 집합도 아니다(오히려 P는 NP의 부분집합으로 알려져 있다)', 'P가 NP의 부분집합이라는 것은 알려져 있지만(P에 속하는 문제는 풀이 자체가 검증이 될 수 있으므로 항상 NP에도 속한다), NP에 속하는 모든 문제가 P보다 반드시 더 오래 걸린다는 것은 증명된 바 없다(이것이 바로 P=NP 문제가 미해결인 이유다)', 'NP라는 이름의 유래는 비결정적 튜링 기계라는 이론적 모델과 관련 있지만, 실제로 그런 컴퓨터가 물리적으로 존재해야 개념이 성립하는 것은 아니다. NP는 어디까지나 수학적으로 정의된 문제 클래스다']}
/>

<Quiz
  questionNumber={4}
  question="P와 NP의 관계에 대한 설명으로 옳은 것은?"
  options={['P에 속하는 모든 문제는 NP에도 속한다(P는 NP의 부분집합이다). 다항시간에 풀 수 있는 문제는 그 풀이 과정 자체를 인증서로 삼아 다항시간에 검증할 수 있기 때문이다. 반대로 NP의 모든 문제가 P에 속하는지(P = NP인지)는 아직 증명도 반증도 되지 않은 미해결 문제다', 'P와 NP는 서로 전혀 겹치지 않는 완전히 별개의 집합이다', 'NP가 P의 부분집합이라는 것이 이미 증명되어 있다', 'P = NP라는 것이 이미 수학적으로 증명되어 있다']}
  correctAnswer={1}
  explanation="P에 속하는 문제는 다항시간 안에 해를 직접 구할 수 있으므로, 그 계산 과정 자체를 인증서로 활용해 '이 해가 맞는지'도 당연히 다항시간 안에 확인할 수 있다(사실 계산을 다시 한 번 하는 셈이니 검증이 항상 가능하다). 따라서 P의 모든 문제는 NP에도 속하며, 이를 P가 NP의 부분집합(P가 NP에 포함됨)이라고 표현한다. 문제는 그 반대 방향이다. NP에 속하는 문제, 즉 '검증은 쉽지만 실제로 해를 찾는 것도 항상 다항시간에 가능한가(P = NP인가)'는 컴퓨터 과학에서 가장 유명한 미해결 문제 중 하나로, 아직 누구도 증명하거나 반증하지 못했다. 많은 학자들은 P가 NP보다 진짜로 작다고(P ≠ NP) 추측하지만 이는 아직 증명된 사실이 아니라 추측일 뿐이다."
  optionExplanations={['정답이다. P가 NP의 부분집합이라는 것은 증명된 사실이고, 그 역(P = NP 여부)은 미해결이라는 현재 상태를 정확히 설명한다', 'P의 문제도 검증 가능성 조건을 만족하므로 NP에 포함된다. 두 집합이 서로 겹치지 않는다는 것은 알려진 사실과 반대다', '알려진 것은 그 반대 방향, 즉 P가 NP의 부분집합이라는 것이다. NP가 P의 부분집합이라는 것(즉 P = NP)은 아직 증명되지 않았다', 'P = NP는 아직 증명도 반증도 되지 않은 미해결 문제이며, 이를 증명(또는 반증)하는 사람에게는 밀레니엄 난제 상금이 걸려 있을 정도로 유명한 열린 문제다']}
/>

<Quiz
  questionNumber={5}
  question="NP-완전(NP-complete)의 정의로 옳은 것은?"
  options={['어떤 문제가 (1) NP에 속하고, (2) NP에 속하는 모든 문제를 그 문제로 다항시간 안에 환원(reduction)할 수 있을 때, 그 문제를 NP-완전이라 한다', 'NP-완전 문제는 다항시간 안에 풀 수 있다는 것이 이미 증명된 문제만을 말한다', 'NP-완전 문제는 NP에 속하지 않아도 되고 검증조차 다항시간에 되지 않아도 된다', 'NP-완전은 그래프 문제에만 붙일 수 있는 이름이다']}
  correctAnswer={1}
  explanation="NP-완전은 NP 클래스 안에서도 '가장 어려운' 문제들을 가리키는 개념이다. 어떤 문제 X가 NP-완전이려면 두 조건을 모두 만족해야 한다. 첫째, X 자체가 NP에 속해야 한다(즉 후보 해를 다항시간에 검증할 수 있어야 한다). 둘째, NP에 속하는 다른 모든 문제를 다항시간 알고리즘을 이용해 X 문제로 변환(환원)할 수 있어야 한다. 이 두 번째 조건이 의미하는 바는, 만약 X를 다항시간에 풀 수 있는 방법이 하나라도 발견된다면 그 환원 절차를 거쳐 NP에 속하는 모든 문제를 다항시간에 풀 수 있게 되어 결국 P = NP가 증명된다는 것이다. 그래서 NP-완전 문제는 'NP 안에서 가장 풀기 어려운 문제들의 대표'로 여겨지며, 지금까지 수천 개의 서로 다른 문제가 NP-완전으로 밝혀졌다."
  optionExplanations={['정답이다. NP에 속함과 NP의 모든 문제로부터 다항시간 환원이 가능함이라는 두 조건이 NP-완전의 정확한 정의다', 'NP-완전 문제가 다항시간에 풀린다는 것이 증명된 적은 없다. 오히려 많은 이들이 다항시간에 풀리지 않을 것이라 추측하는 어려운 문제들이다(다항시간에 풀린다면 P = NP가 증명되는 것이므로)', 'NP-완전이 되려면 반드시 NP에 속해야 한다(검증이 다항시간에 가능해야 함)는 것이 정의의 첫 번째 조건이다. NP에 속하지 않아도 된다는 것은 오히려 NP-난해(NP-hard)에 대한 오해와 관련이 있다', 'NP-완전은 문제의 영역(그래프, 논리식, 수치 조합 등)과 무관하게 정의되는 복잡도 개념이다. 실제로 그래프 문제(정점 커버, 해밀턴 사이클)뿐 아니라 논리식 만족 문제(SAT), 수 분할 문제 등 다양한 영역에 NP-완전 문제가 존재한다']}
/>

<Quiz
  questionNumber={6}
  question="NP-완전(NP-complete)과 NP-난해(NP-hard)의 차이로 옳은 것은?"
  options={['NP-완전은 NP에 속하면서 NP의 모든 문제로부터 환원 가능한 문제를 말하고, NP-난해는 NP의 모든 문제로부터 환원 가능하다는 조건은 같지만 그 문제 자체가 반드시 NP에 속할 필요는 없다(따라서 결정 문제가 아니거나 NP보다 더 어려운 문제도 NP-난해일 수 있다)', 'NP-완전과 NP-난해는 완전히 같은 뜻으로 아무 차이 없이 섞어 써도 되는 말이다', 'NP-난해는 항상 NP-완전보다 쉬운 문제만을 가리킨다', 'NP-완전은 결정 문제가 아니어도 되고, NP-난해는 반드시 결정 문제여야 한다']}
  correctAnswer={1}
  explanation="NP-완전은 앞 문항에서 본 것처럼 (1) NP에 속함, (2) NP의 모든 문제로부터 다항시간 환원이 가능함이라는 두 조건을 모두 만족해야 한다. NP-난해는 이 중 두 번째 조건(NP의 모든 문제로부터 환원 가능함)만 요구하고, 그 문제 자체가 NP에 속해야 한다는 첫 번째 조건은 요구하지 않는다. 그 결과 NP-난해에는 NP-완전 문제뿐 아니라, NP에 속하는지조차 불분명하거나 아예 결정 문제가 아닌 최적화 문제(예: 외판원 문제의 최적 경로 길이를 구하는 버전 자체), 심지어 NP보다 훨씬 어려운 것으로 알려진 문제(예: 계산 불가능한 정지 문제)까지 포함될 수 있다. 즉 NP-완전은 'NP 안에서 가장 어려운 문제들'이고, NP-난해는 'NP-완전만큼 어렵거나 그보다 더 어려운 모든 문제들'을 아우르는 더 넓은 개념이다."
  optionExplanations={['정답이다. 환원 가능성 조건은 공유하되 NP 소속 여부라는 조건에서 갈리는 두 개념의 정확한 관계를 설명한다', '두 용어는 명확히 다른 조건을 가리키며, NP-완전은 NP-난해의 특수한 경우(NP에도 속하는 경우)로 이해하는 것이 정확하다', 'NP-난해는 오히려 NP-완전을 포함하면서 그보다 더 어려울 수도 있는 문제까지 아우르는 더 넓은(또는 같거나 더 어려운) 범주다. 항상 더 쉽다는 것은 정반대의 설명이다', '실제로는 그 반대에 가깝다. NP-완전은 반드시 결정 문제이면서 NP에 속해야 하고, NP-난해는 결정 문제가 아니어도(최적화 문제여도) 괜찮다']}
/>

<Quiz
  questionNumber={7}
  question="다항시간 환원(polynomial-time reduction)의 의미로 옳은 것은?"
  options={['문제 A의 임의의 입력을 다항시간 안에 문제 B의 입력으로 변환할 수 있고, 그렇게 변환된 B의 입력에 대한 답이 원래 A의 입력에 대한 답과 같아지도록 만드는 것이다. 이를 통해 B를 다항시간에 풀 수 있는 알고리즘이 있다면 그것을 이용해 A도 다항시간에 풀 수 있게 된다', '문제 A와 문제 B를 각각 독립적으로 다항시간에 푼 뒤 두 결과를 단순히 더하는 것이다', '문제 A를 그래프로, 문제 B를 배열로 표현을 통일하는 것을 말한다', '다항시간 환원은 항상 문제의 크기를 절반으로 줄이는 과정을 의미한다']}
  correctAnswer={1}
  explanation="다항시간 환원(A를 B로 환원한다, 기호로 A ≤p B)은 A의 임의의 입력 인스턴스를 다항시간 알고리즘으로 B의 입력 인스턴스로 바꾸는 변환 함수가 존재하고, 그 변환된 B의 인스턴스에 대한 답(예 또는 아니오)이 원래 A의 인스턴스에 대한 답과 정확히 일치하도록 만드는 것을 말한다. 이 관계가 성립하면, B를 다항시간에 푸는 알고리즘이 있다고 가정할 때, A의 입력이 주어지면 그것을 다항시간에 B의 입력으로 바꾼 뒤 B의 알고리즘을 돌리고 그 결과를 그대로 A의 답으로 사용하면 되므로, 결국 A도 다항시간에 풀 수 있게 된다(변환 시간과 B를 푸는 시간이 모두 다항시간이면 전체도 다항시간이기 때문이다). 이 원리가 NP-완전성을 증명하는 데 핵심적으로 쓰인다. 이미 NP-완전이라고 알려진 문제를 새로운 문제로 환원할 수 있음을 보이면, 새로운 문제도 최소한 그만큼 어렵다는 것이 증명된다."
  optionExplanations={['정답이다. 입력 변환과 답의 일치라는 환원의 정확한 정의, 그리고 그것이 왜 어려움의 전이를 보장하는지를 정확히 설명한다', '환원은 두 문제를 독립적으로 풀어 결과를 합치는 것이 아니라, 한 문제를 다른 문제의 입력 형태로 바꾸어 그 문제의 알고리즘을 그대로 재사용하려는 것이다', '환원은 자료구조의 표현 형식을 통일하는 것이 아니라 문제 인스턴스 사이의 답을 보존하는 변환을 만드는 것이다', '환원 과정에서 변환된 입력의 크기가 원래 입력보다 커지는 경우도 흔하다(다항시간 안에만 변환되면 되고 크기가 줄어들어야 한다는 조건은 없다)']}
/>

<Quiz
  questionNumber={8}
  question="쿡-레빈 정리(Cook-Levin theorem)의 의미로 옳은 것은?"
  options={['불 논리식 만족 가능성 문제(SAT, Boolean satisfiability problem)가 최초로 NP-완전임이 증명된 문제이며, 이후 다른 문제들의 NP-완전성은 대부분 SAT(또는 이미 SAT로부터 NP-완전임이 증명된 다른 문제)로부터의 환원을 통해 증명되었다', '쿡-레빈 정리는 P = NP임을 증명한 정리다', '쿡-레빈 정리는 모든 그래프 문제가 다항시간에 풀린다는 것을 보인 정리다', '쿡-레빈 정리는 정렬 알고리즘의 하한이 O(n log n)임을 증명한 정리다']}
  correctAnswer={1}
  explanation="1971년 스티븐 쿡과 독립적으로 레오니드 레빈이 증명한 쿡-레빈 정리는 SAT(불 논리식이 참이 되게 만드는 변수 값 대입이 존재하는지 판별하는 문제)가 NP-완전임을 보인 최초의 결과다. 이 정리가 중요한 이유는, 그 이전에는 NP-완전이라는 개념 자체가 있어도 실제로 그 조건을 만족하는 구체적인 문제가 알려져 있지 않았기 때문이다. SAT가 NP-완전임이 증명된 이후로는, 새로운 문제 X가 NP-완전임을 보이고 싶을 때 매번 'NP의 모든 문제로부터 X로 환원 가능함'을 처음부터 증명할 필요 없이, 이미 NP-완전으로 알려진 문제(SAT나 그로부터 파생된 3-SAT, 정점 커버 등)를 X로 다항시간에 환원할 수 있음만 보이면 충분하다는 표준적인 증명 절차가 확립되었다. 정렬 하한(O(n log n))이나 P=NP 증명과는 직접적인 관련이 없다."
  optionExplanations={['정답이다. 최초의 NP-완전 문제로서 SAT를 확립했다는 쿡-레빈 정리의 역사적·이론적 의의를 정확히 설명한다', '쿡-레빈 정리는 P = NP 여부와는 무관하며, 오히려 그 미해결 문제의 배경이 되는 NP-완전 개념을 확립한 정리다', '쿡-레빈 정리는 그래프 문제가 아니라 불 논리식 만족 가능성 문제(SAT)에 대한 결과이며, 모든 그래프 문제의 다항시간 해결 가능성과는 무관하다', '비교 기반 정렬의 O(n log n) 하한은 결정 트리 모델을 이용한 별개의 증명이며 쿡-레빈 정리와는 관련이 없다']}
/>

<Quiz
  questionNumber={9}
  question="다음 중 대표적인 NP-완전 문제로 알려져 있지 않은 것은?"
  options={['정렬된 배열에서 특정 값을 찾는 이진 탐색 문제', 'SAT(불 논리식 만족 가능성 문제)', '정점 커버 문제(그래프의 모든 간선을 덮는 최소 정점 집합이 존재하는지, 목표 크기 K 이하로 존재하는지 판별)', '외판원 문제의 결정 버전(모든 도시를 한 번씩 방문하고 돌아오는 경로 중 총 거리가 K 이하인 것이 존재하는지 판별)']}
  correctAnswer={1}
  explanation="이진 탐색 문제는 정렬된 배열에서 특정 값의 위치를 찾는 문제로, O(log n)이라는 매우 빠른 다항시간(사실은 다항시간보다도 훨씬 빠른 로그 시간) 알고리즘이 이미 잘 알려져 있는 P 클래스의 대표적인 문제다. 반면 SAT, 정점 커버 문제, 외판원 문제의 결정 버전은 모두 대표적인 NP-완전 문제로 알려져 있다. 이 문제들은 후보 해(변수 대입, 정점 집합, 경로)가 주어지면 그것이 조건을 만족하는지 다항시간에 검증할 수 있지만(그래서 NP에 속함), 처음부터 그런 해를 효율적으로 찾아내는 다항시간 알고리즘은 아직 발견되지 않았으며 대부분의 학자들은 존재하지 않을 것으로 추측한다."
  optionExplanations={['정답이다. 이진 탐색은 이미 O(log n)의 매우 효율적인 다항시간 알고리즘이 알려져 있는 P 클래스의 문제이며 NP-완전이 아니다', '옳지 않은 선택이다. SAT는 쿡-레빈 정리로 증명된 최초이자 대표적인 NP-완전 문제다', '옳지 않은 선택이다. 정점 커버 문제는 SAT로부터의 환원을 통해 NP-완전임이 증명된 대표적인 그래프 이론 NP-완전 문제다', '옳지 않은 선택이다. 외판원 문제의 최적화 버전(최단 경로 길이 자체를 구하는 것)은 NP-난해이며, 목표 거리 K를 매개변수로 추가한 결정 버전은 NP-완전으로 알려져 있다']}
/>

<Quiz
  questionNumber={10}
  question="해밀턴 사이클 문제(그래프의 모든 정점을 정확히 한 번씩 방문하고 시작 정점으로 돌아오는 사이클이 존재하는지 판별)를 예로 들어, 'NP에 속한다'는 것이 '검증이 쉽다'는 뜻이지 '풀기가 쉽다'는 뜻이 아님을 보여 주는 설명으로 옳은 것은?"
  options={['어떤 후보 방문 순서(정점을 나열한 목록)가 주어지면, 그 순서가 실제로 모든 정점을 정확히 한 번씩 지나면서 인접한 정점끼리만 연결되어 있는지 차례로 확인하는 데는 정점·간선 수에 비례하는 다항시간이면 충분하다. 그러나 애초에 그런 순서가 존재하는지, 존재한다면 무엇인지를 처음부터 찾아내는 일반적으로 효율적인 다항시간 알고리즘은 알려져 있지 않다', '해밀턴 사이클 문제는 검증과 해결 모두 다항시간에 가능하므로 P에 속한다', '해밀턴 사이클 문제는 검증조차 지수 시간이 걸리므로 NP에 속하지 않는다', '해밀턴 사이클 문제는 정점이 3개 이하인 그래프에서만 정의된다']}
  correctAnswer={1}
  explanation="NP의 정의에서 핵심은 '증명(인증서)이 주어졌을 때 그것을 검증하는 일'의 난이도이지, '증명을 처음부터 찾아내는 일'의 난이도가 아니다. 해밀턴 사이클 문제에서 후보 방문 순서 하나(예: 정점을 나열한 리스트)가 주어지면, 목록의 길이가 정점 수와 같은지, 목록에 모든 정점이 중복 없이 나타나는지, 그리고 목록에서 연속한 두 정점 사이에 실제로 간선이 있는지를 차례로 확인하기만 하면 된다. 이 확인 작업은 정점 수와 간선 수에 비례하는 시간, 즉 다항시간이면 충분하다. 그래서 해밀턴 사이클 문제는 NP에 속한다. 하지만 이것이 그런 사이클을 처음부터 찾아내는 것도 쉽다는 뜻은 전혀 아니다. 실제로 해밀턴 사이클 문제는 NP-완전으로 알려져 있어서, 일반적인 그래프에 대해 그런 사이클을 효율적으로 찾아내는(또는 없다고 판별하는) 다항시간 알고리즘은 알려져 있지 않다."
  optionExplanations={['정답이다. 검증의 용이성(다항시간)과 해결(탐색)의 어려움을 명확히 구분해 NP의 정의를 정확히 설명한다', '해밀턴 사이클 문제는 검증은 다항시간에 가능하지만(NP에 속함), 해를 찾는 다항시간 알고리즘은 알려져 있지 않아 P에 속한다고 알려져 있지 않다(오히려 NP-완전으로 알려져 있다)', '검증 자체는 목록을 한 번 훑어보며 인접성만 확인하면 되므로 다항시간에 충분히 가능하다. 검증이 지수 시간이 걸린다는 것은 NP의 정의와 반대되는 설명이다', '해밀턴 사이클 문제는 임의의 크기의 그래프에 대해 정의되는 일반적인 그래프 이론 문제이며 정점 수에 특별한 제한이 없다']}
/>

<Quiz
  questionNumber={11}
  question="어떤 새로운 문제 X가 NP-완전임을 증명하는 표준적인 절차로 옳은 것은?"
  options={['(1) X가 NP에 속함을 보인다(후보 해를 다항시간에 검증할 수 있음을 보임). (2) 이미 NP-완전으로 알려진 문제 Y를 X로 다항시간에 환원할 수 있음을 보인다(Y ≤p X)', '(1) X를 실제로 다항시간에 풀어 보인다. (2) 그 알고리즘이 항상 정확한 답을 낸다는 것을 증명한다', 'X가 그래프로 표현될 수 있음을 보이기만 하면 자동으로 NP-완전이 증명된다', 'X에 대한 무작위 입력 100개를 실행해 보고 모두 빠르게 풀리면 NP-완전이 아니라고 결론짓는다']}
  correctAnswer={1}
  explanation="새로운 문제 X가 NP-완전임을 보이는 표준적인 두 단계 절차가 있다. 첫째, X가 NP에 속함을 보인다. 즉 X에 대한 어떤 후보 해가 주어졌을 때 그것이 올바른지 다항시간 안에 검증할 수 있는 절차가 있음을 보인다. 둘째, 이미 NP-완전임이 알려진 문제 Y를 하나 골라, Y의 임의의 입력을 다항시간 안에 X의 입력으로 변환하는 방법(환원)을 제시하고, 그 변환이 답을 보존함을 보인다(Y ≤p X). 이렇게 되면 Y가 NP-완전이므로 NP의 모든 문제가 Y로 환원되고, Y는 다시 X로 환원되므로, 결국 NP의 모든 문제가 X로도 환원됨이 증명되어 X도 NP-완전이 된다. 이 절차는 매번 처음부터 'NP의 모든 문제로부터' 증명하는 대신 이미 증명된 결과를 재사용하는 실용적인 방법이다."
  optionExplanations={['정답이다. NP 소속 증명과 이미 알려진 NP-완전 문제로부터의 환원이라는 표준 2단계 절차를 정확히 설명한다', 'X를 다항시간에 실제로 풀 수 있다면 그것은 X가 NP-완전이 아니라 P에 속한다는 것을 보이는 것이며(더 나아가 P = NP 증명으로 이어질 수도 있다), NP-완전 증명 절차와는 정반대 방향의 결과다', '그래프로 표현 가능하다는 사실 자체는 NP-완전성과 아무 논리적 연결 고리가 없다. 그래프로 표현되면서도 P에 속하는 문제(최단경로 등)가 많다', '무작위 입력 몇 개가 빠르게 풀린다는 실험적 관찰은 그 알고리즘이 모든 입력(특히 최악의 경우)에서도 빠르다는 것을 수학적으로 보장하지 않으므로 복잡도 클래스에 대한 증명이 될 수 없다']}
/>

<Quiz
  questionNumber={12}
  question="P = NP 문제(P와 NP가 사실은 같은 집합인지를 묻는 미해결 문제)의 현재 상태에 대한 설명으로 옳은 것은?"
  options={['현재까지 P = NP인지 P ≠ NP인지 수학적으로 증명된 바 없는 컴퓨터 과학의 대표적인 미해결 문제이며, 많은 연구자들이 P ≠ NP일 것이라고 추측하지만 이는 어디까지나 추측이다', 'P = NP는 1990년대에 이미 증명되어 현재는 정설로 받아들여지고 있다', 'P ≠ NP는 반증되어 현재는 P = NP가 확립된 사실이다', 'P와 NP의 관계는 각 개별 프로그래밍 언어에 따라 달라지는 구현 세부 사항일 뿐 수학적으로 정의된 질문이 아니다']}
  correctAnswer={1}
  explanation="P = NP 문제는 클레이 수학연구소가 지정한 7개의 밀레니엄 난제 중 하나로, 아직까지 그 누구도 P와 NP가 같은 집합이라는 것도, 서로 다른 집합이라는 것도 엄밀하게 증명하지 못했다. 직관적으로는 'NP-완전 문제들을 다항시간에 푸는 효율적인 알고리즘이 오랜 세월 동안 발견되지 않았다'는 경험적 사실 때문에 많은 연구자들이 P ≠ NP일 것이라고 강하게 추측하지만, 추측은 증명이 아니다. 독학사 시험에서 이 주제가 나온다면 'P = NP가 참인지 거짓인지 확정하라'는 식의 문항이 아니라, '현재 미해결 상태임을 알고 있는가', 'NP-완전 문제가 다항시간에 풀린다면 어떤 결론이 따라오는가(모든 NP 문제가 다항시간에 풀리게 되어 P = NP가 증명됨)' 같은 개념·판별 수준으로 출제될 가능성이 높다."
  optionExplanations={['정답이다. 여전히 미해결이며 추측과 증명을 명확히 구분한 현재 상태를 정확히 설명한다', 'P = NP는 아직 증명된 바 없다. 이런 증명이 실제로 나온다면 컴퓨터 과학과 암호학 전반에 엄청난 영향을 미칠 만큼 중대한 사건이 될 것이다', 'P ≠ NP 역시 아직 증명되지 않았다. 많은 이들이 그렇게 추측할 뿐 수학적으로 확립된 사실은 아니다', 'P = NP 문제는 특정 프로그래밍 언어나 구현과 무관하게 계산 모델(튜링 기계) 수준에서 정의되는 순수한 수학적·이론적 질문이다']}
/>

<Quiz
  questionNumber={13}
  question="어떤 문제가 NP-완전임이 밝혀졌을 때, 실무에서 그 문제를 다루기 위해 흔히 선택하는 전략으로 옳지 않은 것은?"
  options={['근사 알고리즘(approximation algorithm)이나 휴리스틱(heuristic)을 사용해 최적해는 아니더라도 실용적으로 충분히 좋은 해를 다항시간 안에 구한다', '입력 크기가 작거나 특별한 구조를 가진 경우에 한해 지수 시간 알고리즘이나 분기한정 기법을 적용해 실제 실행 시간을 감당할 수 있는 수준으로 줄인다', '무작위성을 활용한 근사 알고리즘이나 메타 휴리스틱(유전 알고리즘, 담금질 기법 등)으로 실용적인 해를 찾는다', '문제가 NP-완전이라는 사실 자체를 무시하고 항상 완전 탐색으로 최적해를 구하며, 이 방법은 입력 크기와 무관하게 항상 실용적인 시간 안에 끝난다']}
  correctAnswer={4}
  explanation="NP-완전 문제는 (현재까지 알려진 한) 입력 크기가 커지면 최악의 경우 지수적으로 시간이 늘어나는 완전 탐색 외에는 항상 정확한 최적해를 다항시간에 구하는 방법이 알려져 있지 않다. 따라서 '항상 완전 탐색으로 최적해를 구하며 이것이 항상 실용적인 시간 안에 끝난다'는 것은 NP-완전 문제의 성질과 정면으로 모순되는 설명이다. 실제로는 정확한 최적해를 포기하는 대신 근사 알고리즘(이론적으로 최적해의 몇 배 이내임이 보장되는 해를 다항시간에 구함)이나 휴리스틱·메타 휴리스틱(이론적 보장은 약하지만 실전에서 빠르고 좋은 해를 자주 찾는 방법), 또는 입력 크기가 작을 때만 분기한정·동적계획법으로 정확한 해를 구하는 등의 절충 전략을 사용하는 것이 표준적인 실무 대응이다."
  optionExplanations={['옳은 전략이다. 이론적 보장이 있는 근사 알고리즘으로 최적에 가까운 해를 효율적으로 구하는 것은 NP-완전 문제에 대한 표준적인 대응이다', '옳은 전략이다. 입력이 작으면 지수 시간이라도 실제로는 감당할 만한 시간에 끝날 수 있고, 분기한정으로 실질적인 탐색량을 크게 줄일 수도 있다', '옳은 전략이다. 유전 알고리즘, 담금질 기법 등의 메타 휴리스틱은 이론적 최적성 보장은 약하지만 실전에서 유용한 해를 빠르게 찾는 데 널리 쓰인다', '옳지 않은 전략이다. 완전 탐색은 최악의 경우 지수 시간이 걸리므로 입력이 조금만 커져도 실용적인 시간 안에 끝난다는 보장이 전혀 없다. 이것이 바로 NP-완전 문제를 다룰 때 다른 전략이 필요한 이유다']}
/>

<Quiz
  questionNumber={14}
  question="다음 서술 중 계산 복잡도 개념에 대한 흔한 오개념을 바르게 지적한 것은?"
  options={['어떤 문제에 대해 현재까지 다항시간 알고리즘이 발견되지 않았다고 해서, 그 문제가 곧 NP-완전이라고 단정할 수는 없다. NP-완전이려면 NP에 속함과 NP의 모든 문제로부터의 환원 가능성이 모두 별도로 증명되어야 한다', '다항시간 알고리즘이 아직 발견되지 않은 문제는 자동으로 NP-완전으로 분류된다', 'NP에 속하는 문제는 모두 그래프로 표현할 수 있어야 한다', 'P에 속하는 문제는 상수 시간 알고리즘이 존재해야 한다']}
  correctAnswer={1}
  explanation="시험에서 자주 나오는 함정 중 하나가 '다항시간 알고리즘이 아직 없다 = NP-완전이다'라는 잘못된 등식이다. 실제로는 그렇지 않다. 예를 들어 정지 문제(halting problem, 임의의 프로그램이 주어진 입력에 대해 언젠가 멈출지 판별하는 문제)는 다항시간 알고리즘이 없는 정도가 아니라 어떤 알고리즘으로도 절대 풀 수 없다는 것이 증명된 계산 불가능(undecidable) 문제이며, 이는 NP에 속하지도 않는다(다항시간 검증조차 불가능하기 때문이다). 또한 어떤 문제는 단지 아직 아무도 효율적인 알고리즘을 찾지 못했을 뿐, NP-완전이라는 것이 증명되지 않은 채로 남아 있을 수도 있다. NP-완전으로 분류되려면 정의에 따라 (1) NP 소속, (2) 이미 알려진 NP-완전 문제로부터의 환원 가능성을 모두 명시적으로 증명해야 하며, 단순히 '어렵다는 느낌' 만으로는 부족하다."
  optionExplanations={['정답이다. 다항시간 알고리즘 부재와 NP-완전성 사이에 자동적인 등식이 성립하지 않는다는 것, 그리고 NP-완전 판정에 필요한 두 조건을 정확히 짚었다', '이것이 바로 지적해야 할 오개념 그 자체다. 다항시간 알고리즘 부재는 NP-완전의 필요조건도 충분조건도 아니며, 정지 문제처럼 NP에 속하지도 않는 계산 불가능 문제도 존재한다', 'NP는 문제의 표현 형식(그래프, 논리식, 수치 등)과 무관하게 정의되는 복잡도 개념이며 그래프로 표현 가능해야 한다는 제약은 없다', 'P는 다항식 시간(상수가 아니라 n의 다항식) 안에 풀리는 문제들의 집합이다. 상수 시간 알고리즘이 존재해야 한다는 것은 P보다 훨씬 강한(그리고 대부분의 P 문제가 만족하지 못하는) 조건이다']}
/>

<Quiz
  questionNumber={15}
  question="정지 문제(halting problem)에 대한 설명과 NP와의 관계로 옳은 것은?"
  options={['정지 문제는 임의의 프로그램과 입력이 주어졌을 때 그 프로그램이 언젠가 멈추는지를 판별하는 문제로, 어떤 알고리즘으로도 일반적으로 풀 수 없음(계산 불가능, undecidable)이 증명되어 있다. 이는 다항시간에 풀리지 않는 정도가 아니라 아예 어떤 시간 안에도 풀 수 없다는 뜻이므로 NP는 물론 NP-난해보다도 더 근본적으로 다루기 어려운 문제로 분류된다', '정지 문제는 대표적인 NP-완전 문제 중 하나다', '정지 문제는 이미 O(n log n) 알고리즘으로 해결되었다', '정지 문제는 P에 속하는 문제로 알려져 있다']}
  correctAnswer={1}
  explanation="정지 문제는 앨런 튜링이 증명한 계산 이론의 근본적인 결과 중 하나로, 임의의 프로그램 P와 입력 I가 주어졌을 때 'P가 I를 입력으로 실행하면 언젠가 멈추는가'를 모든 경우에 대해 정확히 판별하는 알고리즘은 존재할 수 없다는 것이 증명되어 있다(대각선 논법을 이용한 증명이 유명하다). 이것은 '느리다'거나 '다항시간이 아니다'라는 수준의 문제가 아니라, 어떤 시간이 걸리든 상관없이 원리적으로 모든 경우를 올바르게 판별하는 알고리즘 자체가 존재하지 않는다는 뜻이다. 이런 문제를 계산 불가능(undecidable) 문제라 부르며, NP(다항시간 검증 가능)에도 속하지 않고 오히려 P, NP, NP-완전, NP-난해를 포함하는 '계산 가능한 문제들의 세계' 자체를 벗어나는 문제로 취급된다."
  optionExplanations={['정답이다. 계산 불가능성과 NP 이하의 복잡도 클래스들과의 근본적인 차이를 정확히 설명한다', '정지 문제는 NP-완전이 되기 위한 전제 조건인 NP 소속(다항시간 검증 가능)조차 만족하지 못한다. NP-완전보다 훨씬 더 근본적인 의미에서 다루기 어려운 문제다', '정지 문제는 어떤 다항시간 알고리즘은커녕 어떤 알고리즘으로도 일반적인 경우에 대해 해결할 수 없다는 것이 증명되어 있다. 특정 복잡도의 알고리즘으로 해결되었다는 것은 사실과 다르다', '정지 문제는 P에 속하기는커녕 애초에 계산 가능한 문제조차 아니다(계산 불가능 문제로 분류된다)']}
/>

<Quiz
  questionNumber={16}
  question="다음 중 옳지 않은 것을 고르시오."
  options={['배낭 문제(0/1 knapsack)의 결정 버전(목표 가치 K 이상을 담을 수 있는지 판별)은 NP-완전으로 알려져 있지만, 물건 수와 배낭 용량의 곱에 비례하는 시간이 걸리는 동적계획법으로도 풀 수 있으므로 이는 P에 속함을 완전히 증명한 것이다', '동일한 문제라도 최적화 버전(최댓값을 구하라)과 결정 버전(목표값 이상이 가능한지 판별하라)은 서로 변환 가능하며, 계산 복잡도 논의에서는 주로 결정 버전을 기준으로 삼는다', '배낭 문제에 대한 동적계획법의 시간 복잡도는 물건 수(n)와 배낭 용량(W)의 곱에 비례하는 O(nW)인데, 이 값은 W가 매우 큰 수로 주어지면(예: W가 입력에서 큰 정수로 주어짐) 입력을 인코딩하는 데 필요한 비트 수에 대해서는 지수적으로 커질 수 있어, 이런 알고리즘을 유사 다항시간(pseudo-polynomial time) 알고리즘이라 부른다', '유사 다항시간 알고리즘이 존재한다는 사실은 NP-완전성 증명과 모순되지 않는다. NP-완전성은 입력의 비트 수를 기준으로 한 진짜 다항시간 알고리즘이 알려져 있지 않다는 것과 관련이 있다']}
  correctAnswer={1}
  explanation="이 문제는 함정을 정확히 짚어야 한다. 배낭 문제의 동적계획법은 시간이 O(nW)로 물건 수 n과 배낭 용량 W의 곱에 비례하지만, 계산 복잡도 이론에서 '다항시간'을 판단하는 기준은 입력을 표현하는 데 필요한 비트 수(입력 인코딩 길이)를 기준으로 한다. 용량 W라는 숫자를 이진수로 표현하는 데 필요한 비트 수는 log2(W)이므로, W 자체는 입력 크기에 대해 지수적으로 클 수 있다(예를 들어 W가 100비트 숫자라면 W의 값 자체는 2의 100제곱 수준으로 어마어마하게 크다). 따라서 O(nW) 알고리즘은 W의 '값'에는 다항식이지만 W를 표현하는 '비트 수'에는 지수적이므로, 진짜 의미의 다항시간(입력 비트 수의 다항식) 알고리즘이 아니다. 이런 알고리즘을 유사 다항시간 알고리즘이라 부르며, 이것이 존재한다고 해서 배낭 문제가 P에 속한다는 것이 증명된 것은 아니다(실제로 배낭 문제의 결정 버전은 NP-완전으로 알려져 있다). 이 구분을 못 하면 'NP-완전 문제인데 다항시간 알고리즘이 있다'는 모순된 결론에 빠지기 쉽다."
  optionExplanations={['옳지 않은 설명이므로 이것이 정답이다. O(nW) 동적계획법은 W의 값에는 다항식이지만 W를 인코딩하는 비트 수에는 지수적인 유사 다항시간 알고리즘일 뿐, 진짜 다항시간 알고리즘이 아니므로 P 소속을 증명하지 못한다', '옳은 설명이다. 목표값 매개변수를 통해 최적화 문제와 결정 문제를 상호 변환할 수 있고, 이론에서는 결정 문제를 표준으로 다룬다', '옳은 설명이다. 값 자체에는 다항식이지만 비트 수 기준으로는 지수적인 시간 복잡도를 유사 다항시간이라 부르는 정확한 정의다', '옳은 설명이다. 유사 다항시간 알고리즘의 존재는 진짜 다항시간 알고리즘의 존재와는 다른 개념이므로 NP-완전성과 논리적으로 모순되지 않는다']}
/>

<Quiz
  questionNumber={17}
  question="다음 중 최단경로 문제(다익스트라로 풀리는, 음수 가중치 없는 단일 출발점 최단경로)와 해밀턴 경로 문제(그래프의 모든 정점을 정확히 한 번씩 방문하는 경로가 존재하는지 판별)를 복잡도 클래스 관점에서 비교한 설명으로 옳은 것은?"
  options={['최단경로 문제는 P에 속하는 대표적인 문제이고, 해밀턴 경로 문제는 NP-완전으로 알려진 문제다. 겉보기에는 둘 다 그래프에서 정점들을 잇는 경로를 다루는 비슷한 유형처럼 보이지만 실제 난이도는 크게 다르다', '두 문제 모두 P에 속하므로 난이도가 동일하다', '두 문제 모두 NP-완전이므로 난이도가 동일하다', '최단경로 문제가 오히려 해밀턴 경로 문제보다 더 어렵다고 알려져 있다']}
  correctAnswer={1}
  explanation="이 비교는 '그래프에서 경로를 다룬다'는 겉모습의 유사성만으로 난이도를 짐작해서는 안 된다는 것을 보여 주는 대표적인 예다. 단일 출발점에서 음수 가중치가 없는 최단경로 문제는 다익스트라 알고리즘으로 O(E log V) 시간에 풀리는 P 클래스의 문제다. 반면 해밀턴 경로 문제(그래프의 모든 정점을 정확히 한 번씩 방문하는 경로가 존재하는지 판별하는 문제)는 NP-완전으로 알려져 있어서, 일반적인 그래프에 대해 이를 다항시간에 판별하는 알고리즘은 알려져 있지 않다. 이 두 문제의 차이는 '경로에 포함되는 정점 수에 대한 제약'에 있다. 최단경로 문제는 '어떤 경로든 상관없이 총 가중치만 최소화'하면 되지만, 해밀턴 경로 문제는 '모든 정점을 빠짐없이 정확히 한 번씩'이라는 매우 엄격한 조합적 제약이 걸려 있어 훨씬 다루기 어렵다."
  optionExplanations={['정답이다. 겉모습이 비슷한 그래프 경로 문제라도 실제 복잡도 클래스는 전혀 다를 수 있다는 것을 정확히 짚었다', '해밀턴 경로 문제는 P에 속한다고 알려져 있지 않다. 다항시간 알고리즘이 발견되지 않았고 NP-완전으로 여겨진다', '최단경로 문제는 다익스트라 알고리즘으로 다항시간에 풀리므로 P에 속하며 NP-완전이 아니다', '오히려 그 반대다. 최단경로 문제는 P에 속해 효율적으로 풀리고, 해밀턴 경로 문제가 훨씬 다루기 어려운 NP-완전 문제로 알려져 있다']}
/>

<Quiz
  questionNumber={18}
  question="다음 중 계산 복잡도 이론에 대한 설명으로 옳지 않은 것은?"
  options={['NP-완전 문제라도 특정 입력 구조(예: 그래프가 트리 형태이거나 입력 크기가 매우 작은 경우)에서는 다항시간에 풀리는 특수한 경우가 존재할 수 있다', 'NP-완전 문제로 증명된 문제는 어떤 입력에 대해서도 절대로 빠르게 풀릴 수 없다', '어떤 문제가 NP에 속하는지 판별하는 것과 그 문제가 NP-완전인지 판별하는 것은 서로 다른 작업이다(NP 소속 증명이 더 쉬운 경우가 많다)', 'NP-완전성은 최악의 경우(worst-case) 복잡도를 기준으로 정의되는 개념이며, 평균적인 경우나 특정 입력 분포에서의 실행 속도와는 별개다']}
  correctAnswer={2}
  explanation="NP-완전이라는 분류는 '일반적인 모든 입력에 대해 항상 빠르게(다항시간에) 푸는 알고리즘이 알려져 있지 않다'는 뜻이지, '어떤 입력에 대해서도 절대 빠르게 풀릴 수 없다'는 뜻이 아니다. 실제로 NP-완전 문제라도 입력이 특별한 구조를 가지면(예: 그래프가 트리이거나 평면 그래프인 경우, 변수 수가 적은 SAT 인스턴스인 경우) 다항시간 알고리즘이 존재하는 특수한 부분 문제들이 많이 알려져 있다. 또한 입력 크기가 충분히 작으면 지수 시간 알고리즘도 실제로는 순식간에 끝날 수 있다. NP-완전성은 어디까지나 '최악의 경우, 모든 가능한 입력에 대해 일반적으로 통하는 다항시간 알고리즘이 없다(고 여겨진다)'는 의미이며, 평균적인 실행 속도나 특정 사례에서의 성능과는 다른 이야기다."
  optionExplanations={['옳은 설명이다. 트리나 평면 그래프 같은 특수 구조에서는 일반 그래프에서 NP-완전인 문제도 다항시간에 풀리는 경우가 실제로 많다', '옳지 않은 설명이므로 이것이 정답이다. NP-완전은 일반적인 모든 입력에 대한 효율적 해법 부재를 뜻하는 것이지, 모든 개별 입력에서 절대 빠르게 풀릴 수 없다는 뜻이 아니다', '옳은 설명이다. NP 소속 증명(검증 절차 제시)은 비교적 간단한 경우가 많지만, NP-완전성 증명은 다른 NP-완전 문제로부터의 환원을 구성해야 하므로 더 까다로운 경우가 많다', '옳은 설명이다. NP-완전성은 최악의 경우 복잡도 개념이며, 평균 복잡도나 실제 응용에서의 체감 속도는 또 다른 분석 대상이다']}
/>

<Quiz
  questionNumber={19}
  question="만약 어떤 연구자가 3-SAT 문제(각 절이 정확히 세 개의 리터럴로 이루어진 SAT의 특수 형태로, NP-완전으로 알려져 있음)를 다항시간에 정확히 푸는 알고리즘을 발견하고 그 정확성을 증명했다고 하자. 이로부터 논리적으로 이끌어낼 수 있는 결론으로 옳은 것은?"
  options={['NP에 속하는 모든 문제를 3-SAT로 다항시간 환원할 수 있으므로, 3-SAT를 다항시간에 풀 수 있다면 NP의 모든 문제도 다항시간에 풀 수 있게 되어 결국 P = NP가 증명된다', '이 발견은 3-SAT 하나에만 국한된 결과이며 다른 어떤 문제에도 영향을 주지 않는다', '이 발견은 P ≠ NP를 증명하는 결과가 된다', '이 발견은 정지 문제도 풀 수 있게 됨을 의미한다']}
  correctAnswer={1}
  explanation="3-SAT는 NP-완전 문제이므로 정의상 NP에 속하는 모든 문제가 다항시간 환원을 통해 3-SAT로 변환될 수 있다. 만약 3-SAT를 다항시간에 정확히 푸는 알고리즘이 실제로 존재한다면, NP에 속하는 임의의 문제 Y가 주어졌을 때 Y의 입력을 다항시간에 3-SAT의 입력으로 변환하고(환원), 그 변환된 입력을 방금 발견된 다항시간 알고리즘으로 풀면 되므로, Y도 결국 다항시간에 풀리게 된다. 이 논리가 NP에 속하는 모든 문제 Y에 대해 성립하므로, NP 전체가 P에 포함된다는 것이 증명되고, 이미 P가 NP의 부분집합이라는 것은 알려져 있으므로 결국 P = NP가 증명된다. 이것이 바로 NP-완전 문제 하나만 다항시간에 풀어도 계산 복잡도 이론 전체를 뒤흔드는 결과로 이어지는 이유이며, 그만큼 NP-완전 문제에 대한 효율적인 알고리즘 발견이 극히 어렵고 중요하게 여겨진다."
  optionExplanations={['정답이다. NP-완전 문제 하나의 다항시간 해법이 환원 관계를 통해 NP 전체로 전파되어 P = NP 증명으로 이어지는 논리를 정확히 짚었다', '실제로는 정반대다. NP-완전 문제는 정의상 NP의 모든 문제와 환원 관계로 연결되어 있으므로, 하나의 다항시간 해법 발견이 NP 전체에 영향을 미친다', 'P = NP가 증명되는 것이지 그 반대인 P ≠ NP가 증명되는 것이 아니다. 다항시간 알고리즘의 발견은 P = NP 방향의 증거이지 그 반대 방향의 증거가 아니다', '정지 문제는 NP에 속하지 않는 계산 불가능 문제이므로, NP 안에서의 환원 관계망과는 완전히 별개다. 3-SAT를 다항시간에 푼다고 해서 계산 불가능 문제가 풀리게 되는 것은 아니다']}
/>

<Quiz
  questionNumber={20}
  question="독학사 4단계 알고리즘 시험에서 계산 복잡도(P, NP, NP-완전) 파트가 주로 요구하는 학습 수준으로 옳은 것은?"
  options={['각 개념의 정의를 정확히 이해하고, 주어진 문제가 P·NP·NP-완전·NP-난해 중 어디에 해당하는지 판별하거나 관련 진술의 참·거짓을 가리는 수준이며, 새로운 문제의 NP-완전성을 처음부터 엄밀하게 증명해 내는 것까지는 요구하지 않는다', '모든 대표적인 NP-완전 문제에 대한 환원 증명을 처음부터 끝까지 스스로 유도해 낼 수 있어야 한다', 'P = NP 문제를 실제로 증명하거나 반증할 수 있어야 한다', '계산 복잡도 이론은 시험 범위에서 완전히 제외되어 있다']}
  correctAnswer={1}
  explanation="이 과목의 학습 방향과 출제기준에 따르면 계산 복잡도 개요(P, NP, NP-완전, NP-난해, 환원 개념)는 시험 범위에 포함되지만, 그 수준은 '정의를 정확히 알고, 주어진 진술이나 문제 상황이 어느 개념에 해당하는지 판별할 수 있는가'이다. 즉 결정 문제와 최적화 문제의 구분, P와 NP의 관계, NP-완전과 NP-난해의 차이, 검증과 해결의 차이, 유사 다항시간 알고리즘 같은 함정 개념을 정확히 짚어낼 수 있으면 충분하며, 특정 문제에 대해 처음부터 완전히 새로운 NP-완전성 증명(환원 함수를 직접 설계하고 정당성을 논증하는 것)을 스스로 구성해 내는 연구 수준의 증명 능력까지는 요구하지 않는다. 이는 학습 방향 문서에서 명시한 '증명 테크닉 세부 전개는 하지 않는다'는 범위 설정과 일치한다."
  optionExplanations={['정답이다. 정의 이해와 판별 능력을 요구하되 엄밀한 증명 구성까지는 요구하지 않는다는 이 과목의 출제 수준 설정을 정확히 반영한다', '이 과목의 학습 방향에서 명시적으로 제외한 연구 수준 NP 증명 세부 전개에 해당하는 요구 수준이며, 시험 범위를 벗어난다', 'P = NP는 전 세계 어떤 연구자도 아직 해결하지 못한 미해결 문제이며, 독학사 시험이 요구하는 학습 수준을 크게 벗어난다', '계산 복잡도 개요는 이 과목의 목차에 명시적으로 포함된 정규 출제 범위이며, 완전히 제외되어 있다는 것은 사실과 다르다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [NIST 계산 이론·복잡도 클래스 참고 자료](https://csrc.nist.gov)
