---
title: '22. 예상·기출 변형: 설계 기법(DP·탐욕·분할정복·백트래킹)'
description: '분할정복, 동적계획법, 탐욕 알고리즘, 백트래킹·분기한정을 판별·트레이싱 중심으로 재구성한 예상 문항 모음'
---

<Callout type="warning">
  이 편의 문항은 실제 독학사 기출 문제를 그대로 옮긴 것이 아닙니다. 국가평생교육진흥원이 공개한 4단계 알고리즘 과목 출제기준(평가영역)과 공개·복원된 기출의 유형 분포를 바탕으로, 동일한 출제 의도를 갖도록 새로 재구성한 예상·유사 문항입니다. 실제 시험의 문항 수·배점·시간은 매 회차 공고를 통해 반드시 다시 확인하세요.
</Callout>

<Quiz
  questionNumber={1}
  question="분할정복(divide and conquer) 기법의 일반적인 구조로 옳은 것은?"
  options={['문제를 더 작은 여러 개의 독립적인 부분 문제로 나누고(분할), 각 부분 문제를 재귀적으로 해결한 뒤(정복), 그 결과를 합쳐서(결합) 원래 문제의 답을 얻는다', '문제를 한 번에 가장 이득이 커 보이는 선택으로만 채워 나가며 다시 되돌아보지 않는다', '가능한 모든 경우를 조건 없이 나열한 뒤 그중 정답을 찾는다', '작은 부분 문제의 답을 표에 저장해 두고 반복해서 재사용한다']}
  correctAnswer={1}
  explanation="분할정복은 분할(divide), 정복(conquer), 결합(combine)의 세 단계로 이루어진다. 원래 문제를 더 작고 같은 형태의 부분 문제로 나누고(분할), 부분 문제가 충분히 작아지면 직접 풀거나 재귀적으로 해결하며(정복), 부분 문제들의 해를 조합해 원래 문제의 해를 만든다(결합). 병합 정렬(배열을 반으로 나누어 각각 정렬한 뒤 병합), 퀵 정렬(피벗 기준으로 나눈 뒤 각 부분을 재귀 정렬), 이진 탐색이 대표 예다. 두 번째 보기는 탐욕 알고리즘, 세 번째 보기는 완전 탐색(브루트포스), 네 번째 보기는 동적계획법의 메모이제이션에 대한 설명이다."
  optionExplanations={['정답이다. 분할-정복-결합의 세 단계가 분할정복 기법의 정의 그 자체다', '탐욕 알고리즘에 대한 설명이다. 탐욕은 매 단계에서 지역적으로 최선인 선택을 하고 다시 검토하지 않는다', '완전 탐색(브루트포스)에 대한 설명이다. 분할정복은 문제를 체계적으로 축소해 나가는 전략이지 모든 경우를 나열하는 것이 아니다', '동적계획법의 메모이제이션 기법에 대한 설명이다. 분할정복도 재귀를 쓰지만 부분 문제의 결과를 저장해 재사용하는 것이 핵심은 아니다']}
/>

<Quiz
  questionNumber={2}
  question="병합 정렬의 시간 복잡도를 점화식으로 나타내면 T(n) = 2T(n/2) + cn (배열을 반으로 나누는 데 상수, 병합에 O(n))이다. 이 점화식을 재귀 트리로 전개해 복잡도를 유도하는 과정으로 옳은 것은?"
  options={['깊이 k에서 부분 문제 개수는 2^k개이고 각 크기는 n/2^k이므로 각 깊이의 총 병합 비용은 2^k * c(n/2^k) = cn으로 항상 동일하다. 트리의 깊이는 log2(n)이므로 전체 비용은 cn * log2(n), 즉 O(n log n)이다', '깊이가 늘어날수록 각 깊이의 총 병합 비용이 절반씩 줄어들어 전체 합이 등비수열이 되므로 총 비용은 O(n)이다', '부분 문제 개수가 매 깊이 두 배로 늘어나기만 하고 크기는 줄어들지 않는다고 가정하면 총 비용은 O(2^n)이다', '병합 비용은 부분 문제 크기와 무관하게 항상 상수이므로 총 비용은 트리의 깊이에만 비례해 O(log n)이다']}
  correctAnswer={1}
  explanation="재귀 트리의 각 깊이 k(0부터 시작)에는 부분 문제가 2^k개 있고, 각 부분 문제의 크기는 n/2^k이다. 각 부분 문제를 병합하는 데 그 크기에 비례하는 비용 c * (n/2^k)이 들므로, 깊이 k 전체의 병합 비용 합은 2^k개 * c(n/2^k) = cn이 된다. 즉 어느 깊이에서든 그 층의 총 병합 비용은 항상 cn으로 동일하다. 트리는 부분 문제 크기가 1이 될 때까지, 즉 n/2^k = 1이 될 때(k = log2(n))까지 내려가므로 깊이는 log2(n) + 1개 층이다. 각 층의 비용이 cn으로 동일하므로 전체 비용은 cn * log2(n)이 되고, 상수를 무시하면 O(n log n)이다. 이것이 병합 정렬이 O(n log n)인 이유를 점화식에서 유도하는 표준적인 방법이다."
  optionExplanations={['정답이다. 각 층의 총 병합 비용이 cn으로 일정하고 층 수가 log2(n)이라는 사실로부터 O(n log n)을 정확히 유도한다', '실제로는 각 층의 병합 비용 합이 줄어들지 않고 cn으로 일정하게 유지되므로 등비수열로 줄어든다는 전제가 틀렸다', '병합 정렬은 부분 문제 크기가 매 깊이 절반으로 줄어드는 것이 핵심이며, 크기가 줄지 않는다는 가정 자체가 병합 정렬의 실제 동작과 다르다', '병합 비용은 부분 문제 크기 n/2^k에 비례하지 상수가 아니다. 다만 그 크기별 비용을 모두 합친 층별 총합이 우연히 cn으로 일정할 뿐이다']}
/>

<Quiz
  questionNumber={3}
  question="마스터 정리(master theorem)를 이용해 점화식 T(n) = 4T(n/2) + n의 복잡도를 구하려고 한다. a = 4, b = 2이므로 n^(log_b a) = n^(log_2 4) = n^2이다. f(n) = n을 n^2과 비교했을 때의 결론으로 옳은 것은?"
  options={['f(n) = n은 n^2보다 점근적으로 작으므로(다항식 차수만큼 작음) 마스터 정리의 첫 번째 경우에 해당하고, T(n) = Θ(n^2)이다', 'f(n)과 n^2이 같은 차수이므로 T(n) = Θ(n^2 log n)이다', 'f(n) = n이 n^2보다 크므로 T(n) = Θ(n)이다', '마스터 정리로는 이 점화식을 판정할 수 없다']}
  correctAnswer={1}
  explanation="마스터 정리는 T(n) = aT(n/b) + f(n) 형태의 점화식에서 f(n)을 n^(log_b a)와 비교해 세 가지 경우로 나눈다. f(n)이 n^(log_b a)보다 다항식 차수만큼 작으면(첫 번째 경우) T(n) = Θ(n^(log_b a))이고, 두 값이 같은 차수이면(두 번째 경우) T(n) = Θ(n^(log_b a) * log n)이며, f(n)이 다항식 차수만큼 크면서 정칙 조건을 만족하면(세 번째 경우) T(n) = Θ(f(n))이다. 이 문제에서는 n^(log_2 4) = n^2이고 f(n) = n = n^1이므로 f(n)의 차수(1)가 n^2의 차수(2)보다 작다. 즉 첫 번째 경우에 해당하며, 결론은 T(n) = Θ(n^2)이다."
  optionExplanations={['정답이다. f(n)의 차수가 n^(log_b a)의 차수보다 작을 때 적용하는 마스터 정리 첫 번째 경우를 정확히 적용한 결과다', '두 값이 같은 차수일 때(f(n)이 n^2 수준일 때) 적용되는 두 번째 경우와 혼동한 것이다. 이 문제에서 f(n) = n은 n^2보다 작은 차수다', 'f(n)이 n^(log_b a)보다 클 때 적용되는 세 번째 경우와 혼동한 것이다. 이 문제에서는 f(n)이 더 작은 차수이므로 해당하지 않는다', '이 점화식은 마스터 정리의 표준 형태(T(n) = aT(n/b) + f(n), a >= 1, b > 1)를 그대로 만족하므로 판정할 수 있다']}
/>

<Quiz
  questionNumber={4}
  question="분할정복과 동적계획법(DP)의 근본적인 차이에 대한 설명으로 옳은 것은?"
  options={['분할정복은 나뉜 부분 문제들이 서로 겹치지 않는(독립적인) 경우에 적합하고, 동적계획법은 부분 문제가 반복해서 겹치는(중복 부분 문제) 경우에 그 결과를 저장해 재사용함으로써 효율을 높인다', '분할정복은 항상 동적계획법보다 빠르다', '동적계획법은 재귀를 전혀 사용하지 않는 기법이다', '분할정복과 동적계획법은 이름만 다를 뿐 완전히 동일한 기법이다']}
  correctAnswer={1}
  explanation="두 기법 모두 '문제를 부분 문제로 나누어 해결한다'는 공통점이 있지만 결정적인 차이는 부분 문제들이 겹치는지 여부다. 병합 정렬처럼 나뉜 부분 문제들이 서로 완전히 독립적이어서 각각 한 번씩만 풀면 되는 경우는 분할정복으로 충분하다. 반면 피보나치 수열이나 배낭 문제처럼 같은 부분 문제가 재귀 호출 과정에서 여러 번 반복해서 등장하는 경우(중복 부분 문제), 매번 다시 계산하면 매우 비효율적이므로 한 번 계산한 결과를 표(배열)에 저장해 두고 재사용하는 동적계획법이 필요하다. 동적계획법은 메모이제이션(재귀 + 저장)이나 타뷸레이션(반복문으로 표를 아래에서부터 채움) 방식으로 구현되며, 메모이제이션은 재귀를 사용한다."
  optionExplanations={['정답이다. 부분 문제의 독립성 여부(중복 부분 문제의 유무)가 두 기법을 가르는 핵심 기준이다', '항상 더 빠르다는 일반화는 성립하지 않는다. 오히려 중복 부분 문제가 있는 문제에 분할정복만 적용하면 지수 시간이 걸려 훨씬 느려질 수 있다', '동적계획법의 두 가지 구현 방식 중 메모이제이션은 재귀 호출을 그대로 사용하며 결과만 저장해 재사용한다. 재귀를 쓰지 않는 것은 타뷸레이션 방식뿐이다', '두 기법은 겹치는 부분(부분 문제로 나눈다)이 있지만, 부분 문제의 독립성 여부와 결과 재사용 방식에서 명확히 구분되는 별개의 설계 기법이다']}
/>

<Quiz
  questionNumber={5}
  question="어떤 문제에 동적계획법을 적용할 수 있는지 판별하는 두 가지 필요조건으로 옳은 것은?"
  options={['최적 부분 구조(optimal substructure)와 중복 부분 문제(overlapping subproblems)', '탐욕 선택 속성(greedy choice property)과 정렬 가능성', '완전 이진 트리 구조와 균형 잡힌 분할', '사이클이 없는 그래프 구조와 위상 정렬 가능성']}
  correctAnswer={1}
  explanation="동적계획법을 적용하려면 첫째, 최적 부분 구조가 성립해야 한다. 이는 전체 문제의 최적해가 부분 문제들의 최적해로부터 구성될 수 있다는 성질이다(부분 문제를 최적으로 풀면 그것들을 조합해 전체도 최적으로 풀 수 있음). 둘째, 중복 부분 문제가 존재해야 한다. 즉 재귀적으로 문제를 나눌 때 같은 부분 문제가 여러 경로에서 반복해서 나타나야, 그 결과를 저장해 재사용하는 동적계획법의 이점이 생긴다. 이 두 조건이 함께 성립할 때 동적계획법이 효과적이며, 최적 부분 구조만 있고 중복이 없다면 분할정복으로 충분하다. 탐욕 선택 속성은 탐욕 알고리즘이 성립하기 위한 별개의 조건이다."
  optionExplanations={['정답이다. 이 두 조건이 동적계획법 적용 가능성을 판별하는 표준 기준이다', '탐욕 선택 속성은 탐욕 알고리즘이 최적해를 보장하기 위한 조건이며 동적계획법의 필요조건이 아니다', '완전 이진 트리나 균형 잡힌 분할은 특정 자료구조·분할정복 알고리즘의 특성일 뿐 동적계획법 적용 여부의 판별 기준이 아니다', '사이클 없는 그래프나 위상 정렬 가능성은 특정 그래프 문제에 국한된 조건이며 동적계획법 일반의 필요조건이 아니다']}
/>

<Quiz
  questionNumber={6}
  question="0/1 배낭 문제(각 물건을 통째로 넣거나 아예 넣지 않음)를 동적계획법으로 풀 때, 배낭 용량 5, 물건이 (무게 2, 가치 3), (무게 3, 가치 4), (무게 4, 가치 5) 세 개라고 하자. dp[i][w]를 처음 i개의 물건만 고려하고 용량이 w일 때 얻을 수 있는 최대 가치라 정의하고 표를 채운다면, 최종 최대 가치 dp[3][5]로 옳은 것은?"
  options={['7 (물건 1과 물건 2를 함께 넣어 무게 5, 가치 3 + 4)', '8 (물건 2와 물건 3을 함께 넣어 무게 7, 가치 4 + 5)', '5 (물건 3만 넣어 무게 4, 가치 5)', '12 (세 물건을 모두 넣어 무게 9, 가치 12)']}
  correctAnswer={1}
  explanation="점화식은 dp[i][w] = max(dp[i-1][w], value_i + dp[i-1][w - weight_i]) (단 w가 weight_i 이상일 때만 후자를 고려)이다. 물건 1(무게2, 가치3)만 고려한 dp[1]행: 용량 2 이상이면 항상 3을 얻을 수 있으므로 dp[1] = [0, 0, 3, 3, 3, 3](용량 0~5 순서). 물건 2(무게3, 가치4)까지 고려한 dp[2]행: 용량 3에서는 max(dp[1][3]=3, 4+dp[1][0]=4)=4, 용량 4에서는 max(dp[1][4]=3, 4+dp[1][1]=4)=4, 용량 5에서는 max(dp[1][5]=3, 4+dp[1][2]=4+3=7)=7이므로 dp[2] = [0,0,3,4,4,7]. 물건 3(무게4, 가치5)까지 고려한 dp[3]행: 용량 4에서는 max(dp[2][4]=4, 5+dp[2][0]=5)=5, 용량 5에서는 max(dp[2][5]=7, 5+dp[2][1]=5+0=5)=7이므로 dp[3][5] = 7이다. 이는 무게 제한 5 안에서 물건 1과 물건 2를 함께 넣었을 때(무게 2+3=5, 가치 3+4=7)와 정확히 일치한다. 물건 3을 포함하면 무게 4를 넘는 조합을 만들기 어려워 오히려 가치가 낮아진다."
  optionExplanations={['정답이다. dp 표를 물건 1, 2, 3 순서로 채워 나간 결과 dp[3][5] = 7이 최댓값이다', '물건 2와 물건 3을 함께 넣으면 무게가 3 + 4 = 7로 배낭 용량 5를 초과하므로 애초에 선택할 수 없는 조합이다', '물건 3만 넣으면 가치 5를 얻지만, 물건 1과 물건 2를 함께 넣는 조합(가치 7)이 용량 제한 안에서 더 크므로 최댓값이 아니다', '세 물건을 모두 넣으면 무게가 2 + 3 + 4 = 9로 배낭 용량 5를 크게 초과하므로 애초에 불가능한 조합이다']}
/>

<Quiz
  questionNumber={7}
  question="두 문자열 ACD와 CAD의 최장 공통 부분수열(LCS)의 길이를 동적계획법으로 구하려고 한다. dp[i][j]를 X의 앞 i글자와 Y의 앞 j글자 사이의 LCS 길이라 하고, X[i] = Y[j]이면 dp[i][j] = dp[i-1][j-1] + 1, 다르면 dp[i][j] = max(dp[i-1][j], dp[i][j-1])로 채운다고 하자. 최종 LCS 길이 dp[3][3]으로 옳은 것은?"
  options={['2 (예: CD 또는 AD가 공통 부분수열이 된다)', '3 (ACD와 CAD가 완전히 같은 부분수열을 이룬다)', '1 (D 하나만 공통이다)', '0 (공통 부분수열이 존재하지 않는다)']}
  correctAnswer={1}
  explanation="X = A, C, D(i=1,2,3), Y = C, A, D(j=1,2,3)로 표를 채운다. dp[1][1]: A와 C가 달라 max(dp[0][1]=0, dp[1][0]=0)=0. dp[1][2]: A와 A가 같아 dp[0][1]+1=1. dp[1][3]: A와 D가 달라 max(dp[0][3]=0, dp[1][2]=1)=1. dp[2][1]: C와 C가 같아 dp[1][0]+1=1. dp[2][2]: C와 A가 달라 max(dp[1][2]=1, dp[2][1]=1)=1. dp[2][3]: C와 D가 달라 max(dp[1][3]=1, dp[2][2]=1)=1. dp[3][1]: D와 C가 달라 max(dp[2][1]=1, dp[3][0]=0)=1. dp[3][2]: D와 A가 달라 max(dp[2][2]=1, dp[3][1]=1)=1. dp[3][3]: D와 D가 같아 dp[2][2]+1=1+1=2. 따라서 LCS 길이는 2이며, 실제로 CD(두 문자열에서 각각 순서를 지키며 뽑을 수 있음) 또는 AD가 그 예다."
  optionExplanations={['정답이다. 표를 행렬로 채워 나가 최종 dp[3][3] = 2를 얻는 전 과정이다', 'ACD와 CAD는 문자 순서가 다르므로 문자열 전체가 공통 부분수열이 될 수 없다. 완전히 같은 문자열일 때만 성립하는 경우다', '문자 D 하나만 공통이라고 착각한 경우인데, 실제로는 C와 D, 또는 A와 D처럼 순서를 지키는 두 글자짜리 공통 부분수열이 존재한다', 'C, A, D가 각각 두 문자열에 모두 등장하므로 공통 부분수열이 전혀 없다는 것은 표 계산 없이 성급하게 판단한 오류다']}
/>

<Quiz
  questionNumber={8}
  question="동전 종류가 1원, 3원, 4원이고 목표 금액이 6원일 때, 최소 동전 개수를 구하는 동적계획법 dp[k] = min(dp[k - c] + 1) (c는 사용 가능한 동전, k - c가 0 이상)를 적용해 dp[0]부터 dp[6]까지 채운다면, dp[6]의 값과 그 근거로 옳은 것은?"
  options={['2개. dp[3] = 1(동전 3 하나)이 이미 최소이므로, dp[6] = dp[3] + 1 = 2(동전 3을 두 번 사용, 즉 3원 + 3원)이다', '3개. dp[6] = dp[2] + 1이며 dp[2] = 2이므로 총 3개(1원 두 개와 4원 하나)이다', '6개. 모든 금액을 1원짜리로만 채우는 것이 항상 최적이다', '1개. 6원짜리 동전이 있다고 가정하면 1개로 충분하다']}
  correctAnswer={1}
  explanation="dp[0] = 0에서 시작한다. dp[1] = dp[0] + 1 = 1(1원 동전). dp[2] = dp[1] + 1 = 2(1원 두 번). dp[3]은 1원 세 번(dp[2]+1=3) 또는 3원 동전 하나(dp[0]+1=1) 중 최소이므로 dp[3] = 1. dp[4]는 1원 네 번(dp[3]+1=2), 3원 + 1원(dp[1]+1=2), 4원 하나(dp[0]+1=1) 중 최소이므로 dp[4] = 1. dp[5]는 dp[4]+1=2(1원), dp[2]+1=3(3원), dp[1]+1=2(4원) 중 최소이므로 dp[5] = 2. dp[6]은 dp[5]+1=3(1원), dp[3]+1=2(3원), dp[2]+1=3(4원) 중 최소이므로 dp[6] = 2이며, 이는 3원 동전을 두 번 사용하는 조합(3+3=6)에서 나온다."
  optionExplanations={['정답이다. dp 표를 0부터 6까지 차례로 채워 얻은 최솟값이 2이며, 그 근거(3원을 두 번 사용)까지 정확하다', 'dp[2] + 1을 사용하는 조합은 1원 두 개에 4원 하나를 더하는 것으로 금액이 2 + 4 = 6은 맞지만 개수가 3개가 되어, dp[3] + 1로 얻는 2개보다 많으므로 최솟값이 아니다', '1원짜리로만 채우는 것은 dp[6] = 6이 되어, 3원 동전을 활용하는 조합(dp[6] = 2)보다 훨씬 비효율적이다', '이 문제의 동전 종류는 1원, 3원, 4원뿐이며 6원짜리 동전은 주어지지 않았다. 주어지지 않은 동전을 가정하는 것은 문제 조건을 벗어난다']}
/>

<Quiz
  questionNumber={9}
  question="같은 동전 종류(1원, 3원, 4원)와 목표 금액 6원에 대해, 탐욕(greedy) 알고리즘으로 '가장 큰 액면부터 최대한 사용'하는 방식을 적용하면 4원 하나(남은 금액 2원), 다시 1원 두 개를 써서 총 3개(4+1+1=6)가 된다. 이 결과를 앞 문항의 동적계획법 결과(2개, 3+3=6)와 비교했을 때 알 수 있는 사실로 옳은 것은?"
  options={['동전 액면이 1, 3, 4처럼 표준적인 체계(예: 1, 5, 10, 50, 100원처럼 배수 관계로 잘 맞물리는 체계)가 아니면, 큰 액면부터 쓰는 탐욕 전략이 항상 최적해를 보장하지는 않는다', '탐욕 알고리즘은 이 경우에도 동적계획법과 정확히 같은 결과(2개)를 낸다', '탐욕 알고리즘이 낸 3개가 오히려 정답이고 동적계획법 계산이 틀렸다', '이 결과는 동전 문제와 무관하게 항상 탐욕이 동적계획법보다 더 적은 개수를 낸다는 것을 보여준다']}
  correctAnswer={1}
  explanation="탐욕 알고리즘은 매 단계에서 지역적으로 가장 이득이 커 보이는 선택(가장 큰 액면 동전)을 하고 이후에 이 선택을 다시 검토하지 않는다. 그런데 동전 액면이 1원, 3원, 4원처럼 서로 배수 관계로 깔끔하게 맞물리지 않는 체계에서는, 처음에 4원을 선택한 것이 전체적으로 보면 손해가 되는 선택일 수 있다(4원을 선택하는 대신 3원을 두 번 선택했다면 더 적은 개수로 같은 금액을 만들 수 있었다). 이는 탐욕 선택 속성(매 단계의 지역 최적 선택이 전체 최적해로 이어진다는 성질)이 이 동전 조합에서는 성립하지 않는다는 것을 보여주는 대표적인 반례다. 반면 한국의 실제 동전·지폐 체계처럼 액면이 서로 배수 관계로 잘 맞물리는 표준 체계에서는 탐욕 전략이 최적해를 보장하는 것으로 알려져 있다."
  optionExplanations={['정답이다. 이 예는 탐욕 알고리즘이 모든 문제에서 최적해를 보장하지 않는다는 것을 보여주는 표준적인 반례이며, 동전 체계의 성질에 따라 탐욕의 성공 여부가 갈린다는 것을 알려준다', '앞선 계산에서 탐욕은 3개, 동적계획법은 2개로 서로 다른 결과를 냈으므로 이 진술은 계산 결과와 모순된다', '동적계획법은 가능한 모든 조합을 체계적으로 고려해 최솟값을 구하므로 2개가 실제 최적해이며, 탐욕의 3개는 최적이 아닌 결과다', '이 예는 특정 동전 조합(1, 3, 4)에서 탐욕이 실패하는 사례를 보여줄 뿐이며, 모든 상황에서 탐욕이 항상 더 나쁘다는 일반적 결론을 내릴 수는 없다. 표준 동전 체계에서는 탐욕이 최적일 수 있다']}
/>

<Quiz
  questionNumber={10}
  question="탐욕 알고리즘이 항상 최적해를 보장하기 위해 필요한 두 가지 성질로 옳은 것은?"
  options={['탐욕 선택 속성(매 단계의 지역적으로 최선인 선택이 전체 최적해의 일부가 될 수 있다는 성질)과 최적 부분 구조', '중복 부분 문제와 최적 부분 구조', '사이클이 없는 그래프 구조와 정렬 가능성', '분할정복 가능성과 재귀 종료 조건']}
  correctAnswer={1}
  explanation="탐욕 알고리즘이 항상 최적해를 낸다는 것을 보장하려면 두 조건이 성립해야 한다. 첫째, 탐욕 선택 속성이다. 이는 전체 문제의 최적해를 구성할 때, 맨 처음 단계에서 지역적으로 최선으로 보이는 선택을 해도 그것이 어떤 전체 최적해의 일부로 남을 수 있다는 성질이다. 둘째, 최적 부분 구조다. 이는 동적계획법과 공유하는 조건으로, 탐욕 선택을 한 뒤 남은 부분 문제의 최적해와 처음의 탐욕 선택을 합치면 전체 문제의 최적해가 된다는 성질이다. 활동 선택 문제(가장 일찍 끝나는 활동을 먼저 고르는 전략)는 이 두 성질이 모두 증명되어 있어 탐욕이 항상 최적이지만, 앞 문항의 비표준 동전 문제는 탐욕 선택 속성이 성립하지 않아 탐욕이 최적을 보장하지 못한다."
  optionExplanations={['정답이다. 이 두 조건이 함께 성립할 때만 탐욕 알고리즘의 최적성이 수학적으로 보장된다', '중복 부분 문제는 오히려 동적계획법이 필요하다는 신호이지, 탐욕 알고리즘의 최적성을 보장하는 조건이 아니다', '사이클 없는 그래프나 정렬 가능성은 특정 문제(위상 정렬 등)에 국한된 조건이며 탐욕 알고리즘 일반의 최적성 조건이 아니다', '분할정복 가능성과 재귀 종료 조건은 분할정복 기법이 성립하기 위한 조건과 관련 있으며 탐욕 알고리즘의 최적성 조건과는 다르다']}
/>

<Quiz
  questionNumber={11}
  question="활동 선택 문제(activity selection problem)에서 활동 A(시작 1, 종료 4), B(시작 3, 종료 5), C(시작 0, 종료 6), D(시작 5, 종료 7), E(시작 6, 종료 8)가 주어졌다고 하자. 탐욕 알고리즘으로 '종료 시각이 가장 빠른 활동부터, 이미 선택한 활동과 겹치지 않는 것만' 차례로 선택할 때 뽑히는 활동과 그 개수로 옳은 것은?"
  options={['A(1,4)와 D(5,7)를 선택해 총 2개를 뽑는다. C나 B를 먼저 선택해도 이보다 더 많은 활동을 겹치지 않게 선택할 수는 없다', 'C(0,6) 하나만 선택해 총 1개를 뽑는다', 'A, B, C, D, E를 모두 선택해 총 5개를 뽑는다', 'B(3,5)와 C(0,6)를 함께 선택해 총 2개를 뽑는다']}
  correctAnswer={1}
  explanation="종료 시각 오름차순으로 정렬하면 A(1,4), B(3,5), C(0,6), D(5,7), E(6,8) 순서다. 먼저 A(1,4)를 선택한다. 다음으로 시작 시각이 A의 종료 시각(4) 이상인 활동을 찾는다. B는 시작이 3으로 4보다 작아 A와 겹치므로 건너뛴다. C는 시작이 0으로 역시 겹치므로 건너뛴다. D는 시작이 5로 4 이상이어서 겹치지 않으므로 선택한다. E는 시작이 6으로 D의 종료 시각(7)보다 작아 D와 겹치므로 건너뛴다. 결과적으로 A와 D 두 개가 선택된다. 다른 조합(B와 D, 또는 C 하나만 등)을 시도해도 서로 겹치지 않게 3개 이상을 뽑을 수는 없으므로(그래프상 A, D 두 구간 사이에 낄 수 있는 세 번째 구간이 없음), 종료 시각 기준 탐욕 선택이 만든 2개가 실제로 최적이다."
  optionExplanations={['정답이다. 종료 시각이 빠른 활동부터 차례로 겹치지 않는 것만 고르는 표준 탐욕 절차를 그대로 따른 결과이며, 이 조합이 실제 최적(최대 개수)이다', 'C 하나만 선택하는 것은 다른 활동을 배제하는 지나치게 보수적인 선택이며, A와 D처럼 서로 겹치지 않는 두 활동을 함께 선택하는 것이 더 낫다', '다섯 활동을 시간 순으로 나열해 보면 서로 겹치는 구간이 많아(예: B와 C, C와 D 등이 겹침) 모두 동시에 선택할 수 없다', 'B(3,5)와 C(0,6)는 시작·종료 구간이 겹치므로(3부터 5는 0부터 6 안에 포함됨) 동시에 선택할 수 없는 조합이다']}
/>

<Quiz
  questionNumber={12}
  question="백트래킹(backtracking)에 대한 설명으로 옳은 것은?"
  options={['해를 부분적으로 만들어 가다가 현재까지의 부분해가 조건을 만족할 수 없다고 판단되면(유망하지 않으면) 그 지점에서 더 진행하지 않고 이전 선택 지점으로 되돌아가 다른 선택을 시도하는 탐색 기법이다', '모든 경우의 수를 끝까지 만들어 본 뒤 마지막에 한꺼번에 조건을 검사하는 기법이다', '문제를 절대 재귀로 표현하지 않고 반복문만으로 구현하는 기법이다', '해가 하나 발견되면 그 즉시 이전 선택으로 되돌아가지 않고 탐색을 종료하는 기법이다(첫 해만 찾음을 항상 전제로 한다)']}
  correctAnswer={1}
  explanation="백트래킹은 해를 구성하는 과정을 트리 형태로 생각하고, 한 단계씩 후보를 선택해 부분해를 늘려 가다가 그 부분해가 이미 조건(제약)을 위반해 더 이상 완전한 해로 이어질 수 없음이 확인되면(유망성 판단, promising test), 그 지점 이후의 가능성을 더 탐색하지 않고 가지치기(pruning)하여 바로 이전 선택 지점으로 되돌아간다(백트랙, backtrack). 이렇게 하면 완전 탐색처럼 모든 경우를 끝까지 만들어 보는 것보다 탐색 공간을 크게 줄일 수 있다. 대표 예로 N-Queen 문제(퀸을 한 줄씩 놓아가며 서로 공격하지 않는지 검사), 순열 생성, 부분집합 합 문제가 있다. 백트래킹은 보통 재귀 함수로 구현되며, 여러 개의 해를 모두 찾도록 구현할 수도 있고 첫 번째 해만 찾고 멈추도록 구현할 수도 있다."
  optionExplanations={['정답이다. 부분해의 유망성을 중간에 확인해 가지치기하고 되돌아가는 것이 백트래킹의 핵심 동작이다', '모든 경우를 끝까지 만든 뒤 검사하는 것은 완전 탐색이며, 백트래킹은 중간에 가망이 없으면 미리 포기해 불필요한 탐색을 줄인다는 점이 다르다', '백트래킹은 일반적으로 재귀 함수로 구현하는 것이 자연스럽고 표준적이다. 반복문만으로 구현하는 것이 정의는 아니다', '백트래킹이 첫 해만 찾고 멈출지, 모든 해를 다 찾을지는 구현자가 목적에 따라 정하는 것이지 백트래킹의 정의에 포함된 고정 규칙이 아니다']}
/>

<Quiz
  questionNumber={13}
  question="4-Queens 문제(4×4 체스판에 서로 공격하지 않도록 퀸 4개를 놓기)를 백트래킹으로 풀 때, 1행에 1열, 2행에 4열을 놓았다고 하자(같은 열, 같은 대각선에는 놓을 수 없음). 이어서 3행에 놓을 수 있는 열을 검토하면 1열과 4열은 이미 사용되어 제외되고, 2열과 3열이 후보로 남는다. 2열을 놓아 보면 1행(1열)과는 행 차이 2, 열 차이 1로 대각선이 아니라 안전하고, 2행(4열)과는 행 차이 1, 열 차이 2로 역시 안전해 3행에 2열을 놓을 수 있다. 그런데 이어서 4행을 검토하면 남은 열은 3열뿐인데 3행(2열)과 행 차이 1, 열 차이 1로 대각선 공격이 성립해 놓을 수 없다. 이 상황에서 백트래킹 알고리즘이 다음으로 해야 할 일은?"
  options={['4행에서 더 이상 놓을 열이 없으므로 3행의 선택(2열)이 막다른 길이었다고 판단해 3행으로 되돌아가 3열을 다시 시도하고, 그것도 실패하면 2행으로 더 되돌아가 다른 열을 시도한다', '문제 자체가 해가 없다고 결론짓고 즉시 탐색을 종료한다', '이미 놓은 1행, 2행, 3행의 퀸을 모두 지우고 처음부터 무작위로 다시 배치한다', '4행의 대각선 조건을 무시하고 3열에 그냥 놓아 임시로 해를 완성한다']}
  correctAnswer={1}
  explanation="백트래킹의 핵심은 현재 단계(4행)에서 더 이상 유망한 후보가 없을 때, 문제 전체가 해결 불가능하다고 바로 포기하는 것이 아니라 바로 이전 선택 지점(3행)으로 돌아가 그 지점에서 아직 시도하지 않은 다른 후보(3열)를 시도해 보는 것이다. 이 경우 3행에 3열을 놓아 보면 1행(1열)과는 행 차이 2, 열 차이 2로 대각선 공격이 성립해 3열도 실패한다. 3행에서 시도할 수 있는 후보(2열, 3열)를 모두 소진했으므로 다시 2행으로 되돌아가 2행에 다른 열(1열은 1행과 같아 불가, 3열을 시도)을 놓아 보는 식으로 계속 되돌아가며 탐색한다. 실제로 이 문제는 1행 2열부터 다시 시작하면 2행 4열, 3행 1열, 4행 3열이라는 해를 찾을 수 있다. 이렇게 실패한 가지를 가지치기하고 상위 선택 지점으로 되돌아가는 것이 백트래킹의 표준 동작이다."
  optionExplanations={['정답이다. 현재 단계가 막히면 바로 위 단계로 되돌아가 다른 후보를 시도하는 것이 백트래킹의 표준 절차다', '한 경로가 막혔다고 해서 문제 전체에 해가 없다고 결론지을 수 없다. 다른 경로(다른 열 선택)에서 해가 존재할 수 있으므로 계속 탐색해야 한다', '전부 지우고 무작위로 다시 시작하는 것은 백트래킹이 아니라 비효율적인 임의 재시도이며, 이전까지의 탐색 정보를 전혀 활용하지 못한다', '제약 조건(대각선 공격 금지)을 무시하고 배치하면 애초에 문제의 정의(서로 공격하지 않는 배치)를 어기게 되어 올바른 해가 아니다']}
/>

<Quiz
  questionNumber={14}
  question="분기한정(branch and bound)과 백트래킹의 차이에 대한 설명으로 옳은 것은?"
  options={['백트래킹은 주로 제약 조건 위반 여부만으로 가지를 쳐내는 반면, 분기한정은 각 분기(부분해)에서 얻을 수 있는 이론적 상한(또는 하한) 값을 계산해 그것이 이미 찾은 최선의 해보다 나쁘면 그 가지를 계산 없이 잘라내는 최적화 문제 전용 기법이다', '분기한정은 그래프 알고리즘에서만 쓰이고 백트래킹은 문자열 문제에서만 쓰인다', '두 기법은 이름만 다를 뿐 완전히 동일한 절차를 가진다', '분기한정은 재귀를 쓰지 않고 백트래킹은 재귀만 사용한다']}
  correctAnswer={1}
  explanation="백트래킹은 주로 제약 충족 문제(constraint satisfaction, 예: N-Queen처럼 조건을 만족하는 해를 찾는 문제)에서 조건 위반 여부로 가지치기를 한다. 분기한정은 주로 최적화 문제(예: 배낭 문제처럼 최댓값·최솟값을 찾는 문제)에서, 현재 부분해로부터 도달 가능한 결과의 이론적 한계값(상한 또는 하한, 흔히 제약을 완화한 근사 계산으로 빠르게 구함)을 계산한다. 만약 이 한계값이 이미 확보한 최선의 해보다 더 나쁘다면, 그 가지를 더 파고들어도 결코 더 나은 해를 찾을 수 없다는 것이 확실하므로 그 가지 전체를 탐색하지 않고 잘라낸다. 예를 들어 0/1 배낭 문제에서 남은 물건들을 쪼갤 수 있다고 가정한 분수 배낭 문제의 최적값을 상한으로 사용해, 그 상한이 이미 찾은 정수해보다 작으면 해당 분기를 포기한다."
  optionExplanations={['정답이다. 조건 위반 검사(백트래킹)와 이론적 한계값 비교를 통한 가지치기(분기한정)라는 서로 다른 가지치기 기준을 정확히 구분한다', '두 기법 모두 특정 문제 영역에 국한되지 않고 다양한 조합 최적화·탐색 문제에 적용될 수 있으며, 이런 이분법적 구분은 사실이 아니다', '가지치기 기준(제약 위반 대 한계값 비교)이 서로 다르므로 이름만 다를 뿐 동일한 절차라는 것은 옳지 않다', '두 기법 모두 일반적으로 재귀(또는 이를 흉내 낸 명시적 스택·큐)를 이용한 트리 탐색으로 구현되는 경우가 많다']}
/>

<Quiz
  questionNumber={15}
  question="0/1 배낭 문제(용량 5, 물건 (2,3), (3,4), (4,5)는 앞 문항과 동일)를 분기한정으로 풀 때, 가치 대 무게 비율이 큰 순서(물건1: 1.5, 물건2: 약 1.33, 물건3: 1.25)로 물건을 정렬한 뒤 물건을 쪼갤 수 있다고 가정한 분수 배낭 상한을 계산하면, 물건1을 전부(무게2, 가치3) 담고 남은 용량 3에 물건2를 전부(무게3, 가치4) 담아 상한 값 7을 얻는다(용량이 정확히 소진됨). 이 상한값 7의 의미로 옳은 것은?"
  options={['이 분기에서 만들 수 있는 정수해(물건을 통째로만 담는 실제 0/1 배낭 해)는 상한값 7을 절대 넘을 수 없으므로, 만약 다른 분기에서 이미 가치 7 이상인 해를 찾았다면 이 분기는 더 탐색할 필요 없이 잘라낼 수 있다', '상한값은 항상 실제 최적해보다 작게 나오므로 이 값을 신뢰할 수 없다', '상한값 7은 이 배낭 문제와 무관한 임의의 참고용 숫자일 뿐이다', '상한값을 계산했으므로 이제 더 이상 어떤 가지치기도 필요 없다']}
  correctAnswer={1}
  explanation="분기한정에서 상한(upper bound)은 '이 분기 아래에서 아무리 잘해도 얻을 수 있는 값의 이론적 최댓값'을 뜻한다. 물건을 쪼갤 수 있다고 완화한 분수 배낭 문제의 최적값은 항상 실제 0/1 배낭 문제(정수해)의 최적값보다 크거나 같다(제약을 완화했으므로 선택의 폭이 더 넓어 값이 작아질 수 없음). 따라서 이 분기에서 나올 수 있는 실제 정수해는 상한값 7을 절대 넘어설 수 없다. 만약 탐색 도중 다른 분기에서 이미 가치 7 이상인 완전한 정수해를 찾아 두었다면, 이 분기를 계속 파고들어 봤자 상한값 7을 넘는 해를 만들 수 없으므로 더 탐색할 이유가 없어져 가지를 통째로 잘라낼 수 있다. 이것이 분기한정이 완전 탐색보다 효율적인 이유다."
  optionExplanations={['정답이다. 상한값은 그 분기에서 나올 수 있는 정수해의 이론적 한계이므로, 이미 그만큼 좋은 해가 확보되어 있으면 그 분기를 잘라낼 근거가 된다', '분수 배낭으로 계산한 상한은 제약을 완화한 값이므로 실제 정수해보다 크거나 같은 것이 원칙이다(이 예에서는 우연히 정확히 7로 실제 최적해와 같아졌다). 작게 나온다는 진술은 상한의 정의와 모순된다', '상한값은 바로 이 배낭 문제 인스턴스에서 도달 가능한 값의 한계를 계산한 것으로, 문제와 무관한 임의의 값이 아니다', '상한값 하나를 계산했다고 가지치기가 끝나는 것이 아니라, 탐색이 진행되며 새로운 분기마다 상한을 다시 계산해 현재까지의 최선해와 비교하는 과정을 계속 반복해야 한다']}
/>

<Quiz
  questionNumber={16}
  question="설계 기법을 어떤 문제에 적용할지 판별하는 문제다. '동전을 거슬러 줄 때 사용할 동전 개수를 최소화하되, 어떤 동전 액면 조합이 주어질지 미리 알 수 없어 액면 사이의 특별한 배수 관계를 가정할 수 없는 경우'에 가장 적합한 설계 기법은?"
  options={['동적계획법 (모든 금액에 대해 최소 동전 개수를 표로 채워, 액면 관계에 상관없이 항상 정확한 최적해를 보장한다)', '탐욕 알고리즘 (액면이 무엇이든 큰 액면부터 쓰면 항상 최소 개수가 보장된다)', '단순 분할정복 (금액을 반으로 나누어 각각 독립적으로 거슬러 준다)', '위상 정렬 (동전들 사이의 선후 관계를 정해 순서대로 사용한다)']}
  correctAnswer={1}
  explanation="앞의 9번 문항에서 확인했듯이, 동전 액면 사이에 특정한 배수·구조적 관계가 보장되지 않으면 큰 액면부터 쓰는 탐욕 전략은 최적해를 보장하지 못한다(1, 3, 4원 예에서 탐욕은 3개, 실제 최적은 2개). 반면 동적계획법은 dp[k] = 모든 사용 가능한 동전 c에 대해 min(dp[k-c] + 1)이라는 점화식으로 0원부터 목표 금액까지 차례로 표를 채우기 때문에, 액면 사이의 관계가 어떻든 항상 정확한 최소 개수를 보장한다. 다만 시간 복잡도가 목표 금액과 동전 종류 수에 비례해 커지므로(탐욕보다 느림) 상황에 따라 트레이드오프를 고려해야 하지만, 정확성을 요구하는 이 문제 설정에서는 동적계획법이 정답이다. 분할정복(부분 문제가 서로 독립적이지 않음)이나 위상 정렬(순서 관계가 없는 문제)은 이 문제의 구조와 맞지 않는다."
  optionExplanations={['정답이다. 액면 관계를 가정할 수 없는 일반적인 동전 교환 문제에서는 모든 부분 금액에 대한 최적해를 체계적으로 계산하는 동적계획법이 정확성을 보장하는 유일한 표준 접근이다', '탐욕은 액면이 표준적인 배수 관계를 가질 때만 최적을 보장하며, 그렇지 않은 임의의 액면 조합에서는 최적해를 놓칠 수 있다는 것이 9번 문항에서 확인한 반례의 핵심이다', '동전 금액을 반으로 나누어 독립적으로 거슬러 준다는 개념 자체가 이 문제의 구조(부분 금액들의 결과가 서로 겹치고 재사용되는 구조)에 맞지 않는다', '동전 사이에는 선후 관계나 의존 관계가 존재하지 않으므로 위상 정렬이라는 개념 자체가 이 문제에 적용될 수 없다']}
/>

<Quiz
  questionNumber={17}
  question="다음 중 설계 기법과 그 시간 복잡도상의 특징을 짝지은 것으로 옳지 않은 것은?"
  options={['완전 탐색(브루트포스)은 일반적으로 문제 크기에 따라 지수 시간이 걸릴 수 있지만 항상 정답을 보장한다', '동적계획법은 부분 문제의 개수와 하나의 부분 문제를 계산하는 비용의 곱으로 전체 시간 복잡도가 결정되는 경우가 많다', '탐욕 알고리즘은 매 단계에서 다시 모든 경우를 재귀적으로 탐색하므로 일반적으로 동적계획법보다 시간 복잡도가 더 높다', '백트래킹·분기한정은 가지치기를 통해 최악의 경우에도 지수 시간이 걸릴 수 있는 완전 탐색의 실제 실행 시간을 줄이는 것이 목적이지, 이론적 최악 시간 복잡도 자체를 항상 다항 시간으로 낮추는 것은 아니다']}
  correctAnswer={3}
  explanation="탐욕 알고리즘은 정확히 그 반대 특징을 가진다. 탐욕은 각 단계에서 재귀적으로 여러 경우를 탐색하지 않고, 그 순간 가장 좋아 보이는 단 하나의 선택만 하고 넘어가므로 일반적으로 반복문 한 번(또는 정렬 비용)으로 끝나 동적계획법보다 시간 복잡도가 낮은 경우가 많다(다만 최적해를 보장하는 문제에 한해서만 유효하다는 대가가 있다). 완전 탐색은 모든 경우를 나열하므로 지수 시간이 걸리지만 항상 정답을 찾을 수 있고, 동적계획법은 (부분 문제 개수) 곱하기 (부분 문제 하나를 계산하는 비용)으로 전체 복잡도가 결정되는 경우가 흔하며(예: 0/1 배낭의 dp[i][w]는 부분 문제 개수가 대략 (물건 수) 곱하기 (용량)), 백트래킹·분기한정은 가지치기로 실제 탐색량을 줄이지만 이론적인 최악 시간 복잡도 자체는 여전히 지수적일 수 있다."
  optionExplanations={['옳은 설명이다. 완전 탐색은 모든 경우를 나열하므로 느리지만 확실하게 정답을 찾는다', '옳은 설명이다. 예를 들어 0/1 배낭 dp[i][w]는 부분 문제 개수가 물건 수와 용량의 곱에 비례하고 각 칸을 계산하는 비용이 상수이므로 전체가 그 곱으로 결정된다', '옳지 않은 설명이다. 탐욕은 오히려 재귀적으로 여러 경우를 다시 탐색하지 않고 한 번의 선택만 하고 지나가므로, 최적성이 보장되는 문제에서는 동적계획법보다 시간 복잡도가 더 낮은 경우가 많다', '옳은 설명이다. 가지치기는 실제 실행 시간을 실질적으로 줄여 주지만, 최악의 경우(가지치기가 거의 일어나지 않는 입력)에는 여전히 지수 시간이 걸릴 수 있다']}
/>

<Quiz
  questionNumber={18}
  question="동적계획법에서 메모이제이션(memoization)과 타뷸레이션(tabulation)의 차이로 옳은 것은?"
  options={['메모이제이션은 재귀 함수를 그대로 사용하되 이미 계산한 부분 문제의 결과를 저장해 두었다가 같은 부분 문제가 다시 필요할 때 재계산 없이 바로 꺼내 쓰는 방식(하향식)이고, 타뷸레이션은 가장 작은 부분 문제부터 반복문으로 차례로 표를 채워 올라가는 방식(상향식)이다', '메모이제이션과 타뷸레이션은 완전히 같은 코드이며 이름만 다르다', '메모이제이션은 재귀를 쓰지 않고, 타뷸레이션은 반드시 재귀를 사용해야 한다', '메모이제이션은 그래프 문제에서만, 타뷸레이션은 문자열 문제에서만 사용할 수 있다']}
  correctAnswer={1}
  explanation="메모이제이션은 하향식(top-down) 접근이다. 원래 문제에서 시작해 재귀적으로 더 작은 부분 문제를 호출하되, 각 부분 문제의 결과를 계산한 직후 캐시(배열이나 해시맵)에 저장해 두고, 이후 같은 부분 문제가 다시 호출되면 저장된 값을 즉시 반환해 중복 계산을 막는다. 타뷸레이션은 상향식(bottom-up) 접근이다. 가장 작은 부분 문제(보통 기저 조건에 해당하는 값)부터 시작해 반복문으로 차례차례 더 큰 부분 문제의 값을 표에 채워 나가며, 결국 원래 문제의 답에 도달한다. 두 방식 모두 같은 점화식을 사용하지만, 메모이제이션은 재귀 호출 스택을 사용하고 필요한 부분 문제만 계산하는 반면, 타뷸레이션은 반복문만으로 구현되고 보통 표의 모든 칸을 순서대로 채운다는 차이가 있다."
  optionExplanations={['정답이다. 재귀 기반의 하향식(메모이제이션)과 반복문 기반의 상향식(타뷸레이션)이라는 구현 방향의 차이를 정확히 설명한다', '두 방식은 같은 문제를 풀지만 구현 방향(하향식 재귀 대 상향식 반복문)이 명확히 다른 별개의 구현 기법이다', '실제로는 정반대다. 메모이제이션이 재귀를 사용하고, 타뷸레이션은 반복문만으로 구현되어 재귀를 쓰지 않는 것이 일반적이다', '두 기법 모두 특정 문제 유형에 국한되지 않고 배낭 문제, 최장 공통 부분수열, 최단경로 등 다양한 동적계획법 문제에 공통으로 적용될 수 있다']}
/>

<Quiz
  questionNumber={19}
  question="다음 중 '탐욕 알고리즘이 최적해를 보장하는 문제'와 '탐욕 알고리즘이 최적해를 보장하지 못하는 문제'를 옳게 짝지은 것은?"
  options={['활동 선택 문제(종료 시각 기준 선택)는 탐욕이 최적을 보장하고, 임의의 배수 관계가 없는 동전 액면으로 최소 동전 개수를 구하는 문제는 탐욕이 최적을 보장하지 못한다', '활동 선택 문제와 임의의 동전 교환 문제 모두 탐욕이 항상 최적을 보장한다', '활동 선택 문제와 임의의 동전 교환 문제 모두 탐욕으로는 절대 풀 수 없다', '동전 교환 문제는 항상 탐욕이 최적을 보장하고, 활동 선택 문제는 탐욕이 최적을 보장하지 못한다']}
  correctAnswer={1}
  explanation="11번 문항에서 확인했듯이 활동 선택 문제는 종료 시각이 가장 빠른 활동을 먼저 선택하는 탐욕 전략에 대해 탐욕 선택 속성과 최적 부분 구조가 모두 수학적으로 증명되어 있어, 항상 최대 개수의 겹치지 않는 활동 조합(최적해)을 보장한다. 반면 9번 문항에서 확인했듯이 동전 액면 사이에 배수 관계 같은 특별한 구조가 없는 임의의 동전 조합(예: 1원, 3원, 4원)에서는 큰 액면부터 쓰는 탐욕 전략이 최적 개수를 보장하지 못하는 반례가 존재한다(탐욕 3개, 실제 최적 2개). 이처럼 같은 '탐욕적으로 접근 가능해 보이는' 문제라도 문제의 구조에 따라 탐욕의 최적성 보장 여부가 완전히 달라진다는 것이 독학사 시험에서 자주 묻는 핵심 포인트다."
  optionExplanations={['정답이다. 앞선 두 문항(9번, 11번)의 결론을 정확히 짝지은 것이다', '동전 교환 문제는 액면 구조에 따라 탐욕이 실패할 수 있다는 것이 9번 문항의 핵심 결론이므로, 항상 최적을 보장한다는 진술은 옳지 않다', '두 문제 모두 탐욕적 접근 자체는 가능하다(알고리즘을 적용할 수는 있다). 다만 그 결과가 항상 최적인지 여부가 문제에 따라 다를 뿐이다', '실제로는 정반대다. 활동 선택 문제는 탐욕이 최적을 보장하고, 임의의 동전 교환 문제는 그렇지 않다는 것이 정확한 관계다']}
/>

<Quiz
  questionNumber={20}
  question="다음 중 각 설계 기법에 대한 설명으로 옳지 않은 것은?"
  options={['분할정복은 부분 문제가 서로 독립적일 때 효율적이며 부분 문제의 결과를 저장해 재사용하는 것이 핵심 아이디어다', '동적계획법은 최적 부분 구조와 중복 부분 문제가 모두 있는 문제에 적합하다', '탐욕 알고리즘은 매 단계의 지역적 최선 선택을 이후에 번복하지 않는다', '백트래킹은 유망하지 않은 부분해를 만나면 그 지점에서 탐색을 중단하고 이전 단계로 되돌아간다']}
  correctAnswer={1}
  explanation="분할정복의 핵심 아이디어는 '부분 문제의 결과를 저장해 재사용하는 것'이 아니라 '문제를 독립적인 더 작은 부분 문제로 나누어 각각 해결한 뒤 결합하는 것'이다. 부분 문제의 결과를 저장해 재사용하는 것은 동적계획법의 핵심 아이디어이며, 이는 부분 문제가 서로 독립적이지 않고 반복해서 겹칠 때(중복 부분 문제) 필요해지는 기법이다. 분할정복이 다루는 부분 문제들은 애초에 서로 겹치지 않으므로 저장해서 재사용할 대상 자체가 거의 없다(있다 해도 그것을 활용하는 것이 분할정복의 정의는 아니다). 나머지 세 진술은 동적계획법, 탐욕, 백트래킹 각각의 핵심 특징을 정확히 설명한다."
  optionExplanations={['옳지 않은 설명이다. 부분 문제 결과 저장·재사용은 동적계획법의 특징이며, 분할정복은 부분 문제가 서로 독립적이라 애초에 겹치는 결과를 재사용할 필요가 크지 않다는 점에서 대비된다', '옳은 설명이다. 5번 문항에서 확인한 동적계획법의 두 가지 필요조건을 정확히 요약한다', '옳은 설명이다. 탐욕은 한 번 선택한 것을 되돌아보지 않는다는 것이 정의적 특징이다', '옳은 설명이다. 12번, 13번 문항에서 확인한 백트래킹의 가지치기·복귀 동작을 정확히 설명한다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [NIST 계산 이론·알고리즘 설계 기법 참고 자료](https://csrc.nist.gov)
