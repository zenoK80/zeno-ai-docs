---
title: '26. 기출·모의 ③: 웹·DB·네트워크·자료구조 연동 문제'
description: 'SQL·JDBC·JSP/Servlet·소켓 프로그래밍과 자료구조 연산을 코드 빈칸·실행 결과·짧은 구현 중심으로 재구성한 유형 문제 22문항'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 문제집은 국가평생교육진흥원 독학학위제 4단계 통합프로그래밍 출제기준의 평가영역(데이터베이스 프로그래밍, 웹 프로그래밍, 네트워크 프로그래밍, 자료구조 활용) 범위와 문항 유형을 분석해 새로 구성한 **재구성 문항**입니다. 실제 기출 문항을 그대로 옮긴 것이 아니며, 회차별 정확한 문항 수·배점·시험 시간은 시행처 공고를 확인해야 합니다. 여기서는 학습 편의를 위해 22문항을 표준 분량으로 구성했습니다.
</Callout>

## 출제 범위와 문항 배분

이 편은 13~21편(자료구조 구현, 정렬·탐색, DB·SQL, JDBC, 웹, JSP/Servlet, 소켓)에서 다룬 개념을 실행 결과 추적·코드 빈칸·짧은 구현 형태로 재확인합니다. 23편(통합 실습 ②)에서 다룬 여러 영역 연결 감각도 함께 점검합니다. SQL 예제는 이 사이트의 SQLD 과목과 문법을 맞추기 위해 Oracle 문법을 기준으로 합니다.

| 영역 | 문항 수 | 문항 번호 | 관련 편 |
| --- | --- | --- | --- |
| SQL 기본·조인 | 3 | 1–3 | 17 |
| JDBC 연동 코드 구조 | 4 | 4–7 | 18 |
| JSP·Servlet·MVC | 4 | 8–11 | 20 |
| 소켓 프로그래밍 | 4 | 12–15 | 21 |
| 자료구조 연동(스택·큐·트리·그래프) | 4 | 16–19 | 13, 14 |
| 웹 프런트 기초(HTML·CSS·JS) | 3 | 20–22 | 19 |

## SQL 기본·조인 (1–3)

### 문제 1

다음은 EMP(사원) 테이블과 DEPT(부서) 테이블이다.

| EMPNO | ENAME | DEPTNO |
| --- | --- | --- |
| 100 | KIM | 10 |
| 101 | LEE | 20 |
| 102 | PARK | 10 |

| DEPTNO | DNAME |
| --- | --- |
| 10 | SALES |
| 20 | DEV |

```sql
SELECT e.ename, d.dname
FROM emp e
JOIN dept d ON e.deptno = d.deptno
WHERE d.dname = 'SALES';
```

<Quiz
  questionNumber={1}
  question="위 조인 조회의 실행 결과로 조회되는 행의 개수와 이름으로 가장 적절한 것은?"
  options={['KIM, PARK 두 명이 조회된다', 'LEE 한 명만 조회된다', '세 명 모두 조회된다', '조회 결과가 없다']}
  correctAnswer={1}
  explanation="EMP 테이블과 DEPT 테이블을 DEPTNO(부서 번호) 값이 같은 행끼리 연결하는 내부 조인(INNER JOIN)을 수행한 뒤, WHERE 절에서 부서 이름이 SALES인 행만 걸러낸다. DEPTNO가 10인 사원은 KIM과 PARK이고, DEPTNO 10에 대응하는 부서 이름이 SALES이므로 이 둘이 조건을 만족한다. LEE는 DEPTNO가 20으로 DEV 부서에 속하므로 조건에서 제외된다."
  optionExplanations={['부서 번호 10에 속한 KIM과 PARK이 SALES 조건을 만족하므로 정답이다.', 'LEE는 DEPTNO가 20인 DEV 부서 소속이라 SALES 조건에서 제외된다.', 'PARK과 KIM만 SALES 부서이고 LEE는 DEV 부서이므로 세 명 모두가 조회되지는 않는다.', 'DEPTNO 10과 20이 각각 DEPT 테이블에 존재하고 조인 조건과 WHERE 조건을 만족하는 행이 있으므로 결과가 비어 있지 않다.']}
/>

### 문제 2

```sql
SELECT deptno, COUNT(*) AS cnt
FROM emp
GROUP BY deptno
HAVING COUNT(*) >= 2;
```

<Quiz
  questionNumber={2}
  question="문제 1의 EMP 테이블 데이터를 기준으로 위 GROUP BY와 HAVING을 사용한 집계 조회의 결과로 가장 적절한 것은?"
  options={['DEPTNO가 10이고 CNT가 2인 행 하나만 조회된다', 'DEPTNO 10과 20이 모두 조회된다', '조회 결과가 없다', '모든 사원이 한 행씩 그대로 조회된다']}
  correctAnswer={1}
  explanation="GROUP BY deptno는 같은 부서 번호를 가진 행끼리 하나의 그룹으로 묶고, COUNT(*)는 각 그룹에 속한 행의 개수를 센다. DEPTNO가 10인 그룹에는 KIM, PARK 두 명이 속해 개수가 2이고, DEPTNO가 20인 그룹에는 LEE 한 명만 속해 개수가 1이다. HAVING절은 그룹으로 묶은 뒤에 조건을 적용하는 절로, 개수가 2 이상인 그룹만 남기라는 조건이므로 DEPTNO가 20인 그룹(개수 1)은 제외되고 DEPTNO가 10인 그룹(개수 2)만 결과에 남는다."
  optionExplanations={['개수가 2 이상인 그룹은 DEPTNO 10 하나뿐이므로 정답이다.', 'DEPTNO 20 그룹은 사원이 한 명뿐이라 개수 2 이상 조건을 만족하지 못해 결과에서 제외된다.', 'DEPTNO 10 그룹은 조건을 만족하므로 결과가 비어 있지 않다.', 'GROUP BY로 부서별로 묶여 집계되므로 사원별로 한 행씩 나오지 않는다.']}
/>

### 문제 3

```sql
SELECT ename,
       CASE
           WHEN deptno = 10 THEN 'A'
           ELSE 'B'
       END AS grp
FROM emp
ORDER BY ename;
```

<Quiz
  questionNumber={3}
  question="CASE 표현식으로 부서 번호에 따라 그룹 문자를 부여하고 이름 기준으로 정렬한 위 조회 결과 순서로 가장 적절한 것은?"
  options={['KIM(A), LEE(B), PARK(A) 순으로 정렬되어 조회된다', 'PARK(A), LEE(B), KIM(A) 순으로 조회된다', 'LEE(B), KIM(A), PARK(A) 순으로 조회된다', '정렬 없이 입력 순서 그대로 조회된다']}
  correctAnswer={1}
  explanation="CASE WHEN 조건 THEN 값 ELSE 값 END 형태의 CASE 표현식은 조건을 위에서부터 순서대로 검사해, DEPTNO가 10이면 A, 그렇지 않으면 B를 grp 값으로 부여한다. KIM과 PARK은 DEPTNO가 10이므로 A, LEE는 DEPTNO가 20이므로 B가 된다. ORDER BY ename은 이름을 알파벳 오름차순으로 정렬하라는 뜻이므로, K로 시작하는 KIM, L로 시작하는 LEE, P로 시작하는 PARK 순서가 된다."
  optionExplanations={['CASE 조건에 따라 KIM은 A, LEE는 B, PARK은 A가 되고 이름 알파벳 순으로 정렬하면 KIM, LEE, PARK 순서가 되므로 정답이다.', '이름 알파벳 순 정렬 기준으로 PARK이 가장 앞에 오지 않으므로 이 순서는 틀렸다.', 'ORDER BY ename으로 정렬했으므로 이름 알파벳 순서상 LEE가 가장 앞에 오지 않는다.', 'ORDER BY 절이 명시적으로 있으므로 입력 순서가 아니라 이름 기준 정렬 순서로 조회된다.']}
/>

## JDBC 연동 코드 구조 (4–7)

### 문제 4

```java
import java.sql.*;

public class JdbcExample {
    public static void main(String[] args) throws Exception {
        Class.forName("oracle.jdbc.driver.OracleDriver");
        Connection conn = DriverManager.getConnection(
            "jdbc:oracle:thin:@localhost:1521:xe", "scott", "tiger");
        Statement stmt = conn.createStatement();
        ResultSet rs = stmt.executeQuery("SELECT ename FROM emp");
        while (rs.next()) {
            System.out.println(rs.getString("ename"));
        }
        rs.close();
        stmt.close();
        conn.close();
    }
}
```

<Quiz
  questionNumber={4}
  question="위 JDBC 코드에서 Class.forName 코드가 하는 역할로 가장 적절한 것은?"
  options={['지정한 이름의 JDBC 드라이버 클래스를 메모리에 로딩해 DriverManager에 등록되게 한다', 'DB 서버에 실제로 접속해 커넥션을 맺는다', 'SQL 문을 실제로 실행한다', 'ResultSet에서 데이터를 한 행씩 읽어 온다']}
  correctAnswer={1}
  explanation="Class.forName(문자열)은 그 문자열이 가리키는 이름의 클래스를 찾아 메모리에 적재(로딩)하는 자바 표준 기능이다. JDBC 드라이버 클래스는 로딩되는 시점에 내부적으로 자기 자신을 DriverManager(어떤 데이터베이스에 접속할지에 맞는 드라이버를 찾아 연결해 주는 관리자 역할의 클래스)에 등록하도록 작성되어 있다. 그래서 이 코드는 실제 DB 접속보다 앞서, 이후 DriverManager.getConnection이 올바른 드라이버를 찾아 쓸 수 있도록 준비하는 단계다. 실제 접속은 그 다음 줄인 getConnection 호출에서 이루어진다."
  optionExplanations={['드라이버 클래스를 로딩해 DriverManager에 등록하는 준비 단계라는 설명이 정확하므로 정답이다.', '실제 DB 접속은 그 다음 줄의 DriverManager.getConnection 호출에서 이루어진다.', 'SQL 실행은 Statement의 executeQuery 호출에서 이루어지며 Class.forName과는 무관하다.', 'ResultSet에서 데이터를 읽는 것은 rs.next()와 rs.getString 호출의 역할이다.']}
/>

### 문제 5

```java
Statement stmt = conn.createStatement();
ResultSet rs = stmt.executeQuery("SELECT ename FROM emp");
while (rs.next()) {
    System.out.println(rs.getString("ename"));
}
```

<Quiz
  questionNumber={5}
  question="ResultSet의 next 메서드를 while문의 조건으로 사용하는 방식에 대한 설명으로 가장 적절한 것은?"
  options={['next는 커서를 다음 행으로 이동시키고, 이동할 행이 있으면 참을, 더 이상 행이 없으면 거짓을 반환해 반복이 자연스럽게 끝나게 한다', 'next는 항상 참만 반환하므로 이 코드는 무한 반복에 빠진다', 'next는 결과 집합의 첫 번째 행만 가리키며 반복할 때마다 항상 같은 행을 가리킨다', 'next는 SQL문을 다시 실행해 매번 새로운 데이터를 가져온다']}
  correctAnswer={1}
  explanation="ResultSet은 쿼리 실행 결과를 표현하며, 내부적으로 커서(현재 가리키고 있는 행의 위치)를 유지한다. 처음에는 커서가 첫 번째 행보다 앞에 위치해 있고, next()를 호출할 때마다 커서가 한 행씩 앞으로 이동한다. 이동한 위치에 실제로 읽을 행이 있으면 true를, 결과 집합의 끝을 지나 더 이상 행이 없으면 false를 반환한다. 그래서 while(rs.next())는 결과 집합의 모든 행을 하나씩 순회하다가 행이 소진되면 자연스럽게 반복을 멈추는 전형적인 패턴이다."
  optionExplanations={['커서를 이동시키고 행의 존재 여부를 참·거짓으로 반환해 반복을 자연스럽게 끝낸다는 설명이 정확하므로 정답이다.', '결과 집합의 끝에 도달하면 next는 거짓을 반환하므로 무한 반복에 빠지지 않는다.', 'next를 호출할 때마다 커서가 다음 행으로 이동하므로 매번 같은 행을 가리키지 않는다.', 'next는 이미 실행되어 메모리에 있는 결과 집합의 커서만 이동시킬 뿐, SQL을 다시 실행하지 않는다.']}
/>

### 문제 6

다음 JDBC 코드에는 자원 정리와 관련된 문제가 있다.

```java
Connection conn = DriverManager.getConnection(url, id, pw);
Statement stmt = conn.createStatement();
ResultSet rs = stmt.executeQuery("SELECT ename FROM emp");

while (rs.next()) {
    if (rs.getString("ename").equals("KIM")) {
        throw new RuntimeException("stop here");
    }
}

rs.close();
stmt.close();
conn.close();
```

<Quiz
  questionNumber={6}
  question="반복 도중 예외가 발생할 수 있는 위 코드의 자원 관리 문제로 가장 적절한 것은?"
  options={['예외가 발생하면 코드 흐름이 rs.close(), stmt.close(), conn.close()를 건너뛰어 커넥션 등 자원이 반납되지 않을 수 있으므로 try-finally나 try-with-resources로 반드시 자원을 정리해야 한다', '예외가 발생해도 Java는 항상 자동으로 모든 자원을 정리해 주므로 문제가 없다', 'RuntimeException은 반드시 컴파일 오류로 처리되어 이 코드는 실행조차 되지 않는다', 'ResultSet은 예외와 무관하게 항상 자동으로 닫힌다']}
  correctAnswer={1}
  explanation="예외가 발생하면 그 시점에서 현재 실행 흐름을 즉시 중단하고, 그 예외를 처리할 catch 블록을 찾아 위로 전파(propagation)된다. 이 코드에는 catch가 없으므로 예외가 그대로 main 밖으로 전파되며, 그 과정에서 rs.close(), stmt.close(), conn.close()처럼 예외 발생 지점보다 뒤에 있는 코드는 전혀 실행되지 않는다. 그 결과 데이터베이스 커넥션이 반납되지 않고 계속 점유된 채로 남을 수 있는데, 이는 커넥션 누수(connection leak)라 불리는 대표적인 실무 오류다. 이런 문제를 막기 위해 finally 블록이나 try-with-resources 구문으로 반드시 자원을 정리해야 한다."
  optionExplanations={['예외 발생 시 뒤에 있는 close 코드들이 건너뛰어져 자원이 반납되지 않을 수 있다는 지적이 정확하므로 정답이다.', 'Java는 명시적으로 닫지 않은 커넥션·스트림 등의 외부 자원을 자동으로 정리해 주지 않으므로 이 설명은 틀렸다.', 'RuntimeException을 던지는 것은 문법적으로 정상이며 컴파일 오류가 아니라 실행 시점의 예외다.', 'ResultSet의 close는 명시적으로 호출해야 하며, 예외로 인해 그 호출이 건너뛰어지면 닫히지 않는다.']}
/>

### 문제 7

```java
PreparedStatement pstmt = conn.prepareStatement(
    "SELECT * FROM emp WHERE ename = ?");
pstmt.setString(1, "KIM");
ResultSet rs = pstmt.executeQuery();
```

<Quiz
  questionNumber={7}
  question="Statement 대신 PreparedStatement와 물음표(placeholder)를 사용하는 방식의 장점으로 가장 적절한 것은?"
  options={['SQL 문자열에 사용자 입력값을 직접 이어 붙이지 않아 SQL 삽입(SQL Injection) 공격을 막는 데 도움이 되고, 같은 SQL을 반복 실행할 때 성능상 이점도 있다', 'PreparedStatement는 SELECT문에서만 사용할 수 있다', '물음표는 항상 정수형 값만 받을 수 있다', 'PreparedStatement를 사용하면 executeQuery 호출이 필요 없어진다']}
  correctAnswer={1}
  explanation="Statement로 SQL을 만들 때 사용자 입력을 문자열 그대로 이어 붙이면, 입력값 안에 악의적인 SQL 구문을 끼워 넣어 원래 의도와 다른 쿼리가 실행되게 하는 SQL 삽입 공격에 취약해진다. PreparedStatement는 SQL 구조를 먼저 물음표(placeholder, 나중에 채워질 값의 자리를 표시하는 기호)가 포함된 형태로 미리 준비(precompile)해 두고, setString·setInt 같은 메서드로 값만 안전하게 그 자리에 바인딩(binding)한다. 이 값은 SQL 구문의 일부로 재해석되지 않고 순수한 데이터로만 취급되므로 삽입 공격을 막는 데 효과적이며, 미리 준비된 SQL 구조를 재사용할 수 있어 반복 실행 시 성능에도 유리하다."
  optionExplanations={['SQL 삽입 공격 방지와 준비된 구문 재사용에 따른 성능 이점을 모두 설명하므로 정답이다.', 'PreparedStatement는 SELECT뿐 아니라 INSERT, UPDATE, DELETE 등 모든 SQL 문에 사용할 수 있다.', 'setString처럼 자료형에 맞는 다양한 set 메서드가 있어 문자열·정수·날짜 등 다양한 값을 바인딩할 수 있다.', 'PreparedStatement도 결과를 조회하려면 executeQuery를 호출해야 하며, 이 과정이 생략되지 않는다.']}
/>

## JSP·Servlet·MVC (8–11)

### 문제 8

```java
import javax.servlet.http.*;
import java.io.*;

public class HelloServlet extends HttpServlet {
    protected void doGet(HttpServletRequest req, HttpServletResponse resp)
            throws IOException {
        resp.setContentType("text/html");
        PrintWriter out = resp.getWriter();
        out.println("Hello Servlet");
    }
}
```

<Quiz
  questionNumber={8}
  question="위 Servlet 클래스에서 doGet 메서드가 호출되는 시점으로 가장 적절한 것은?"
  options={['클라이언트가 이 서블릿에 매핑된 URL로 HTTP GET 방식의 요청을 보낼 때 서블릿 컨테이너가 자동으로 호출한다', '서블릿 클래스 파일이 컴파일되는 시점에 한 번 호출된다', '개발자가 main 메서드에서 직접 doGet을 호출해야만 실행된다', 'doGet은 서버가 꺼질 때 정리 작업을 위해 호출된다']}
  correctAnswer={1}
  explanation="서블릿(Servlet)은 웹 컨테이너(톰캣 등 서블릿을 실행·관리하는 프로그램) 위에서 동작하도록 설계된 자바 클래스로, 개발자가 직접 이 메서드들을 호출하지 않는다. 클라이언트(웹 브라우저 등)가 이 서블릿에 매핑된 URL로 HTTP 요청을 보내면, 서블릿 컨테이너가 요청의 HTTP 메서드 종류를 확인해 GET 방식이면 doGet을, POST 방식이면 doPost를 자동으로 호출하는 생명주기(lifecycle)를 따른다. doGet 안에서는 요청 정보를 담은 HttpServletRequest와 응답을 작성할 HttpServletResponse를 이용해 클라이언트에게 돌려줄 내용을 만든다."
  optionExplanations={['GET 방식 HTTP 요청이 들어올 때 컨테이너가 자동으로 doGet을 호출한다는 설명이 정확하므로 정답이다.', '컴파일 시점이 아니라 실제 요청이 들어오는 실행 시점에 호출되는 메서드다.', '서블릿 컨테이너가 생명주기에 따라 자동으로 호출하므로 개발자가 직접 호출할 필요가 없다.', '서버 종료 시 정리 작업은 destroy 메서드의 역할이며 doGet과는 다르다.']}
/>

### 문제 9

```jsp
<%
    int count = 0;
    for (int i = 0; i < 5; i++) {
        count += i;
    }
%>
합계: <%= count %>
```

<Quiz
  questionNumber={9}
  question="JSP의 스크립틀릿(scriptlet, 퍼센트 기호로 감싼 자바 코드 블록)과 표현식 태그를 사용한 위 코드의 출력 결과로 가장 적절한 것은?"
  options={['합계: 10', '합계: 0', '합계: 5', '컴파일 오류']}
  correctAnswer={1}
  explanation="스크립틀릿 안의 for문은 i가 0부터 4까지(조건이 i가 5보다 작을 동안이므로 4까지 포함) 반복하며 count에 i 값을 누적한다. 계산 과정은 0 더하기 0은 0, 0 더하기 1은 1, 1 더하기 2는 3, 3 더하기 3은 6, 6 더하기 4는 10이 된다. 반복이 끝난 뒤 count는 10이 되고, 표현식 태그(퍼센트와 등호로 시작하는 태그)는 그 안의 자바 표현식 결과를 문자열로 변환해 그 위치에 그대로 출력하므로, 최종적으로 합계: 10이 화면에 나타난다."
  optionExplanations={['0부터 4까지의 합을 누적하면 10이 되고 이를 표현식 태그로 출력하므로 정답이다.', '0은 반복이 시작되기 전 count의 초기값일 뿐이며 반복이 끝난 뒤의 최종 값이 아니다.', '5는 반복 횟수이지 0부터 4까지 더한 합계가 아니다.', '스크립틀릿과 표현식 태그는 JSP의 정상 문법이므로 컴파일 오류가 발생하지 않는다.']}
/>

### 문제 10

<Quiz
  questionNumber={10}
  question="웹 애플리케이션의 MVC(Model-View-Controller) 패턴에서 각 구성 요소의 역할을 짝지은 것으로 가장 적절한 것은?"
  options={['Model은 데이터와 비즈니스 로직, View는 화면 출력, Controller는 요청을 받아 Model을 조작하고 알맞은 View로 연결하는 역할을 한다', 'Model이 화면 출력을 전담하고 View는 데이터베이스 접근을 전담한다', 'Controller는 화면에 보이는 HTML만 작성하는 역할을 한다', 'MVC 패턴에서는 Model과 View가 서로 직접 통신하며 Controller는 사용되지 않는다']}
  correctAnswer={1}
  explanation="MVC는 애플리케이션의 책임을 세 부분으로 나누어 설계하는 대표적인 아키텍처 패턴이다. Model은 데이터 구조와 데이터베이스 접근, 비즈니스 로직(핵심 처리 규칙)을 담당한다. View는 Model이 가진 데이터를 사용자가 볼 수 있는 화면(HTML 등)으로 표현하는 역할만 담당하며 로직을 직접 갖지 않는 것이 이상적이다. Controller는 사용자의 요청(서블릿이 받는 HTTP 요청 등)을 받아 어떤 Model 로직을 실행할지 결정하고, 그 처리 결과를 어떤 View로 보여줄지 연결하는 중개자 역할을 한다. 이렇게 역할을 나누면 화면과 데이터 처리 로직이 서로 영향을 덜 주고받아 유지보수가 쉬워진다."
  optionExplanations={['Model-View-Controller 각각의 표준적인 책임 분담을 정확히 설명하므로 정답이다.', 'Model은 데이터·로직을, View는 화면 출력을 담당하는 것이 표준 구조이며 이 보기는 역할이 뒤바뀌어 있다.', 'Controller는 요청 처리와 흐름 제어를 담당하며, 화면을 직접 그리는 역할은 View의 몫이다.', 'MVC 패턴의 핵심은 Controller가 Model과 View를 중개하는 구조이므로, Controller가 없다는 설명은 패턴의 정의와 어긋난다.']}
/>

### 문제 11

```jsp
<%
    request.setAttribute("userName", "Kim");
%>
<jsp:forward page="result.jsp" />
```

<Quiz
  questionNumber={11}
  question="request 영역에 값을 저장한 뒤 다른 JSP로 forward(제어를 넘기는 것)하는 위 코드에 대한 설명으로 가장 적절한 것은?"
  options={['request에 저장한 속성은 forward된 result.jsp에서도 그대로 접근할 수 있는데, forward는 클라이언트에게 새 요청을 다시 보내게 하지 않고 서버 안에서 같은 요청 객체를 다음 자원으로 넘기기 때문이다', 'forward가 일어나면 클라이언트의 주소창 URL이 result.jsp로 바뀐다', 'request에 저장한 값은 forward된 페이지에서는 사라지고 접근할 수 없다', 'jsp:forward는 항상 새로운 HTTP 요청을 클라이언트가 다시 보내도록 강제한다']}
  correctAnswer={1}
  explanation="request는 하나의 HTTP 요청이 처리되는 동안 유지되는 영역(scope)으로, setAttribute로 값을 저장하면 그 요청이 완전히 끝날 때까지 같은 요청 객체를 참조하는 모든 곳에서 그 값을 꺼내 쓸 수 있다. jsp:forward는 클라이언트에게 다시 요청하라고 응답하는 리다이렉트와 달리, 서버 내부에서 같은 요청과 응답 객체를 그대로 유지한 채 처리를 다음 자원(result.jsp)에게 넘기는 방식이다. 따라서 forward 이전에 request에 저장해 둔 userName 속성은 forward된 result.jsp에서도 request.getAttribute로 그대로 꺼낼 수 있으며, 클라이언트의 주소창 URL도 forward 이전 상태 그대로 유지된다."
  optionExplanations={['forward는 같은 요청 객체를 서버 내부에서 넘기는 방식이라 request에 저장한 속성이 유지된다는 설명이 정확하므로 정답이다.', 'forward는 서버 내부에서 처리되는 방식이라 클라이언트의 주소창 URL은 바뀌지 않는다(주소창이 바뀌는 것은 리다이렉트의 특징이다).', 'forward는 같은 요청을 그대로 넘기므로 request에 저장한 값이 사라지지 않고 이어서 사용할 수 있다.', 'forward는 클라이언트가 새 요청을 보내는 것이 아니라 서버 내부에서 처리를 이어가는 방식이다.']}
/>

## 소켓 프로그래밍 (12–15)

### 문제 12

```java
import java.net.*;
import java.io.*;

public class EchoServer {
    public static void main(String[] args) throws IOException {
        ServerSocket serverSocket = new ServerSocket(5000);
        Socket socket = serverSocket.accept();
        BufferedReader in = new BufferedReader(
            new InputStreamReader(socket.getInputStream()));
        String line = in.readLine();
        System.out.println("received: " + line);
    }
}
```

<Quiz
  questionNumber={12}
  question="ServerSocket의 accept 메서드가 하는 동작으로 가장 적절한 것은?"
  options={['지정한 포트로 클라이언트의 연결 요청이 들어올 때까지 실행을 멈춘 채 대기하다가, 연결이 성사되면 그 클라이언트와 통신할 새로운 Socket 객체를 반환한다', '즉시 반환되며 클라이언트 연결 여부와 무관하게 항상 null을 돌려준다', '서버가 시작될 때 한 번만 호출할 수 있으며 이후에는 재사용할 수 없다', '클라이언트에게 데이터를 곧바로 전송하는 역할을 한다']}
  correctAnswer={1}
  explanation="ServerSocket은 지정된 포트 번호(여기서는 5000)에서 클라이언트의 연결 요청을 기다리는 역할을 한다. accept 메서드를 호출하면, 실제로 어떤 클라이언트가 그 포트로 연결을 시도해 올 때까지 그 지점에서 실행이 멈춘 채 기다리는 블로킹(blocking) 방식으로 동작한다. 클라이언트의 연결 요청이 들어와 연결이 성사되면, accept는 그 특정 클라이언트와 데이터를 주고받는 데 사용할 새로운 Socket 객체를 만들어 반환한다. 이렇게 반환된 소켓의 입력 스트림(getInputStream)을 통해 클라이언트가 보낸 데이터를 읽을 수 있다."
  optionExplanations={['연결 요청이 들어올 때까지 대기하다가 통신용 Socket을 반환하는 블로킹 동작을 정확히 설명하므로 정답이다.', 'accept는 연결이 성사될 때까지 대기하는 블로킹 메서드이며, 즉시 null을 반환하지 않는다.', 'accept는 하나의 서버 소켓에서 여러 번 반복 호출해 여러 클라이언트의 연결을 순차적으로 받아들일 수 있다.', '데이터 전송은 반환된 Socket의 스트림을 통해 이루어지며, accept 자체가 데이터를 보내는 것은 아니다.']}
/>

### 문제 13

```java
import java.net.*;
import java.io.*;

public class EchoClient {
    public static void main(String[] args) throws IOException {
        Socket socket = new Socket("localhost", 5000);
        PrintWriter out = new PrintWriter(socket.getOutputStream(), true);
        out.println("hello server");
        socket.close();
    }
}
```

<Quiz
  questionNumber={13}
  question="클라이언트에서 new Socket 하고 문자열 localhost와 포트 번호 5000을 전달하는 위 코드가 의미하는 바로 가장 적절한 것은?"
  options={['같은 컴퓨터에서 실행 중인, 포트 번호 5000으로 대기 중인 서버 프로그램에 연결을 시도한다', '5000번이라는 이름의 파일을 로컬 디스크에서 연다', 'localhost라는 이름의 원격 웹사이트에 HTTP 요청을 보낸다', '포트 번호는 반드시 5000번 하나만 사용할 수 있다']}
  correctAnswer={1}
  explanation="Socket 생성자에 전달하는 첫 번째 인자는 연결하려는 상대 컴퓨터의 주소(호스트명 또는 IP 주소)이고, 두 번째 인자는 그 컴퓨터에서 서버 프로그램이 대기하고 있는 포트 번호다. localhost는 지금 이 프로그램이 실행되고 있는 바로 그 컴퓨터 자신을 가리키는 특별한 호스트 이름이다. 따라서 이 코드는 같은 컴퓨터 안에서 실행 중인, 포트 5000번으로 클라이언트의 연결을 기다리고 있는 서버(문제 12의 EchoServer 같은 프로그램)에 연결을 시도하는 것이다. 연결이 성사되면 이 소켓의 출력 스트림을 통해 서버로 데이터를 보낼 수 있다."
  optionExplanations={['localhost는 자기 자신의 컴퓨터를, 5000은 그 컴퓨터의 포트 번호를 가리켜 같은 컴퓨터의 서버에 연결한다는 설명이 정확하므로 정답이다.', 'Socket 생성자에 전달하는 두 번째 인자는 파일 이름이 아니라 네트워크 포트 번호다.', 'localhost는 원격 웹사이트가 아니라 자기 자신의 컴퓨터를 가리키는 이름이다.', '포트 번호는 프로그램이 자유롭게 정할 수 있는 값으로, 이 예제에서 5000을 사용했을 뿐 다른 번호도 사용할 수 있다.']}
/>

### 문제 14

<Quiz
  questionNumber={14}
  question="TCP와 UDP 소켓 통신 방식의 차이에 대한 설명으로 가장 적절한 것은?"
  options={['TCP는 연결을 먼저 맺고 순서·전달을 보장하는 방식이고, UDP는 연결 설정 없이 데이터그램(datagram) 단위로 보내며 순서·전달을 보장하지 않는 대신 오버헤드가 적다', 'TCP와 UDP는 완전히 같은 방식으로 동작하며 이름만 다르다', 'UDP는 반드시 데이터의 전달을 보장하고 TCP는 전달을 보장하지 않는다', 'TCP는 포트 번호를 사용하지 않고 UDP만 포트 번호를 사용한다']}
  correctAnswer={1}
  explanation="TCP(Transmission Control Protocol)는 데이터를 주고받기 전에 먼저 연결(connection)을 맺는 과정을 거치고, 보낸 데이터가 순서대로, 손실 없이 도착하도록 확인 응답과 재전송 메커니즘을 갖춘 프로토콜이다. 반면 UDP(User Datagram Protocol)는 별도의 연결 설정 과정 없이 데이터그램이라는 독립된 단위로 데이터를 그냥 보내며, 상대가 잘 받았는지 확인하지 않으므로 순서가 바뀌거나 일부가 유실될 수 있다. 대신 확인·재전송 과정이 없어 오버헤드(부가적으로 드는 처리 비용)가 적고 속도가 빠른 특성이 있어, 실시간성이 중요한 스트리밍 등에 활용된다."
  optionExplanations={['연결 설정 여부와 전달 보장 유무, 오버헤드 차이를 정확히 대비해 설명하므로 정답이다.', 'TCP는 연결 지향적이고 신뢰성을 보장하는 반면 UDP는 비연결형이라는 근본적인 차이가 있어 같은 방식이 아니다.', '전달을 보장하는 쪽은 UDP가 아니라 TCP이며, 이 설명은 두 프로토콜의 특성이 뒤바뀌어 있다.', 'TCP와 UDP 모두 상대 프로그램을 식별하기 위해 포트 번호를 사용한다.']}
/>

### 문제 15

```java
Socket socket = new Socket("localhost", 5000);
BufferedReader in = new BufferedReader(
    new InputStreamReader(socket.getInputStream()));
PrintWriter out = new PrintWriter(socket.getOutputStream(), true);

out.println("hello");
String response = in.readLine();
```

<Quiz
  questionNumber={15}
  question="위 코드에서 in.readLine() 호출 지점의 동작 특성으로 가장 적절한 것은?"
  options={['서버가 응답으로 한 줄의 데이터를 보내 줄 때까지 그 지점에서 실행이 멈춰 대기하는 블로킹 호출이다', '서버의 응답 여부와 무관하게 항상 즉시 빈 문자열을 반환한다', 'readLine은 소켓 통신에서는 사용할 수 없는 메서드다', '이 호출은 서버로 데이터를 보내는 역할을 한다']}
  correctAnswer={1}
  explanation="BufferedReader의 readLine 메서드는 한 줄(개행 문자로 끝나는 텍스트)이 입력 스트림에 도착할 때까지 기다렸다가 그 줄을 문자열로 반환하는 방식으로 동작한다. 소켓 통신에서 이 입력 스트림은 상대방(서버)이 데이터를 보내 줄 때 채워지므로, 서버가 아직 아무 응답도 보내지 않았다면 readLine 호출은 그 자리에서 실행을 멈추고 서버의 응답이 도착할 때까지 대기하는 블로킹(blocking) 동작을 보인다. 이런 특성 때문에 소켓 프로그램을 설계할 때는 클라이언트와 서버가 서로 언제 보내고 언제 받을지 순서를 맞추는 프로토콜 설계가 중요하다."
  optionExplanations={['데이터가 도착할 때까지 실행을 멈추고 기다리는 블로킹 성질을 정확히 설명하므로 정답이다.', 'readLine은 실제로 데이터가 도착할 때까지 기다리며, 응답 여부와 무관하게 즉시 반환되지 않는다.', 'readLine은 문자 기반 스트림을 다루는 표준 메서드로 소켓의 입력 스트림에서도 정상적으로 사용할 수 있다.', '데이터를 서버로 보내는 역할은 out.println이 담당하며, in.readLine은 데이터를 받는 역할이다.']}
/>

## 자료구조 연동: 스택·큐·트리·그래프 (16–19)

### 문제 16

```java
import java.util.Stack;

public class Main {
    public static void main(String[] args) {
        Stack<Integer> stack = new Stack<Integer>();
        stack.push(1);
        stack.push(2);
        stack.push(3);
        stack.pop();
        stack.push(4);
        System.out.println(stack.peek());
    }
}
```

<Quiz
  questionNumber={16}
  question="push, pop, peek 연산이 섞인 위 코드의 실행 과정을 단계별로 추적했을 때 마지막 peek()의 결과로 가장 적절한 것은?"
  options={['4', '3', '2', '1']}
  correctAnswer={1}
  explanation="스택은 나중에 넣은 값이 가장 먼저 나오는(LIFO, Last In First Out) 자료구조다. 연산을 순서대로 추적하면, push(1) 후 스택은 아래부터 [1], push(2) 후 [1, 2], push(3) 후 [1, 2, 3]이 된다. pop()은 맨 위(가장 나중에 넣은) 원소인 3을 꺼내 제거하므로 스택은 [1, 2]가 된다. 이어서 push(4)를 하면 스택은 [1, 2, 4]가 되고, peek()는 스택을 변경하지 않고 맨 위 원소만 조회하는 연산이므로 맨 위에 있는 4를 반환한다."
  optionExplanations={['pop으로 3을 제거한 뒤 4를 새로 push했으므로 맨 위 원소는 4가 되어 정답이다.', '3은 pop 연산으로 이미 제거된 값이라 더 이상 스택의 맨 위에 있지 않다.', '2는 스택에서 3 아래에 있는 값으로, 4가 push된 이후에는 맨 위가 아니다.', '1은 가장 먼저 넣은 값으로 스택의 맨 아래에 있어 peek의 대상이 아니다.']}
/>

### 문제 17

```java
import java.util.LinkedList;
import java.util.Queue;

public class Main {
    public static void main(String[] args) {
        Queue<Integer> queue = new LinkedList<Integer>();
        queue.offer(1);
        queue.offer(2);
        queue.offer(3);
        queue.poll();
        queue.offer(4);
        System.out.println(queue.peek());
    }
}
```

<Quiz
  questionNumber={17}
  question="큐(Queue)의 offer(삽입), poll(꺼내며 제거), peek(조회) 연산이 섞인 위 코드에서 마지막 peek()의 결과로 가장 적절한 것은?"
  options={['2', '1', '3', '4']}
  correctAnswer={1}
  explanation="큐는 먼저 넣은 값이 먼저 나오는(FIFO, First In First Out) 자료구조다. offer(1), offer(2), offer(3)을 순서대로 실행하면 큐는 앞에서부터 [1, 2, 3] 순서로 쌓인다. poll()은 큐의 맨 앞(가장 먼저 넣은) 원소인 1을 꺼내 제거하므로 큐는 [2, 3]이 된다. offer(4)를 하면 맨 뒤에 추가되어 큐는 [2, 3, 4]가 되고, peek()는 큐를 변경하지 않고 맨 앞의 원소만 조회하므로 2를 반환한다."
  optionExplanations={['poll로 1이 빠져나간 뒤 맨 앞에 남은 원소가 2이므로 정답이다.', '1은 poll 연산으로 이미 큐에서 빠져나간 값이라 더 이상 맨 앞에 있지 않다.', '3은 큐의 중간에 위치하며 맨 앞이 아니다.', '4는 가장 나중에 offer된 값으로 큐의 맨 뒤에 위치해 peek의 대상이 아니다.']}
/>

### 문제 18

다음은 이진 탐색 트리(BST)에 7, 3, 9, 1, 5를 순서대로 삽입하는 과정을 나타낸다.

| 삽입 값 | 비교 과정 | 삽입 위치 |
| --- | --- | --- |
| 7 | 트리가 비어 있음 | 루트 |
| 3 | 7보다 작음 | 7의 왼쪽 자식 |
| 9 | 7보다 큼 | 7의 오른쪽 자식 |
| 1 | 7보다 작음, 3보다 작음 | 3의 왼쪽 자식 |
| 5 | 7보다 작음, 3보다 큼 | 3의 오른쪽 자식 |

<Quiz
  questionNumber={18}
  question="위 표와 같이 만들어진 이진 탐색 트리를 중위 순회(in-order traversal, 왼쪽-루트-오른쪽 순서로 방문)했을 때 방문 순서로 가장 적절한 것은?"
  options={['1, 3, 5, 7, 9', '7, 3, 1, 5, 9', '1, 5, 3, 9, 7', '9, 7, 5, 3, 1']}
  correctAnswer={1}
  explanation="중위 순회는 각 노드에서 왼쪽 서브트리를 먼저 전부 방문하고, 그 다음 자기 자신을 방문하고, 마지막으로 오른쪽 서브트리를 방문하는 규칙을 재귀적으로 적용한다. 루트 7에서 시작해 왼쪽 서브트리(3을 루트로 하는 부분)를 먼저 본다. 3의 왼쪽 서브트리인 1을 먼저 방문하고, 3 자신을 방문하고, 3의 오른쪽 서브트리인 5를 방문해 1, 3, 5 순서가 나온다. 이후 루트인 7 자신을 방문하고, 마지막으로 7의 오른쪽 서브트리인 9를 방문한다. 전체 순서는 1, 3, 5, 7, 9가 되며, 이는 이진 탐색 트리의 중위 순회 결과가 항상 오름차순 정렬이 된다는 성질과도 일치한다."
  optionExplanations={['왼쪽-루트-오른쪽 순서를 재귀적으로 적용하면 1, 3, 5, 7, 9의 오름차순이 나오므로 정답이다.', '이 순서는 루트를 가장 먼저 방문하는 전위 순회(preorder)의 결과에 가깝고 중위 순회의 결과가 아니다.', '5를 3보다 먼저 방문하는 것은 왼쪽-루트-오른쪽 순서 규칙에 어긋난다.', '이 순서는 오른쪽 서브트리부터 방문하는 역순 형태로 중위 순회의 정의와 맞지 않는다.']}
/>

### 문제 19

다음은 정점 A, B, C, D와 간선 A-B, A-C, B-D, C-D로 이루어진 그래프를 인접 리스트로 표현한 것이다.

| 정점 | 인접한 정점 목록 |
| --- | --- |
| A | B, C |
| B | A, D |
| C | A, D |
| D | B, C |

<Quiz
  questionNumber={19}
  question="A에서 시작해 너비 우선 탐색(BFS, Breadth-First Search)을 수행할 때, 각 정점의 인접 목록에 나열된 순서대로 방문한다고 가정하면 방문 순서로 가장 적절한 것은?"
  options={['A, B, C, D', 'A, B, D, C', 'A, C, B, D', 'A, D, B, C']}
  correctAnswer={1}
  explanation="BFS는 큐(Queue)를 이용해 시작 정점과 가까운 정점부터 한 단계씩 넓게 탐색하는 방법이다. A를 방문하고 큐에 A를 넣은 뒤 꺼내면서 A의 인접 목록인 B, C를 순서대로 큐에 넣고 방문 처리한다. 이 시점까지 방문 순서는 A, B, C다. 다음으로 큐에서 B를 꺼내 B의 인접 목록(A, D)을 확인하는데 A는 이미 방문했으므로 건너뛰고 D를 새로 방문해 큐에 넣는다. 이어서 큐에서 C를 꺼내 C의 인접 목록(A, D)을 확인하는데 둘 다 이미 방문했거나 큐에 있으므로 추가로 방문할 정점이 없다. 마지막으로 큐에서 D를 꺼내지만 D의 인접 목록(B, C)이 모두 이미 방문되어 더 방문할 정점이 없다. 따라서 전체 방문 순서는 A, B, C, D가 된다."
  optionExplanations={['A의 인접 목록 순서대로 B, C를 먼저 방문하고 그 다음 단계에서 D를 방문하므로 A, B, C, D가 정답이다.', 'D는 B와 C 모두와 인접하지만 A와 같은 단계(너비)의 정점이 아니라 그 다음 단계이므로 C보다 먼저 방문되지 않는다.', 'BFS는 A의 인접 목록에 나열된 순서(B가 C보다 먼저)를 따르므로 C가 B보다 먼저 방문되지 않는다.', 'D는 A와 직접 연결되어 있지 않으므로 A 바로 다음 순서로 방문될 수 없다.']}
/>

## 웹 프런트 기초: HTML·CSS·JS (20–22)

### 문제 20

```html
<form action="/register" method="post">
  <input type="text" name="userName" />
  <input type="submit" value="가입" />
</form>
```

<Quiz
  questionNumber={20}
  question="위 form 요소에서 method 속성을 post로 지정했을 때의 동작으로 가장 적절한 것은?"
  options={['입력값이 HTTP 요청의 본문(body)에 담겨 서버로 전송되며, URL 주소창에는 노출되지 않는다', '입력값이 URL의 쿼리 문자열에 그대로 노출되어 전송된다', 'method는 화면에 보이는 버튼의 색상을 결정하는 속성이다', 'post 방식은 서버에 아무 데이터도 전달하지 않는다']}
  correctAnswer={1}
  explanation="HTML form의 method 속성은 폼 데이터를 서버로 보낼 때 사용할 HTTP 메서드를 지정한다. get으로 지정하면 입력값들이 요청 URL의 뒤에 물음표와 함께 쿼리 문자열 형태로 덧붙여져 전송되므로 주소창에 그대로 노출되지만, post로 지정하면 입력값이 HTTP 요청의 본문(body) 부분에 담겨 전송되어 주소창에는 나타나지 않는다. 그래서 비밀번호나 긴 데이터를 다루는 폼은 보통 post 방식을 사용한다."
  optionExplanations={['post는 데이터를 요청 본문에 담아 보내 주소창에 노출되지 않는다는 설명이 정확하므로 정답이다.', 'URL 쿼리 문자열로 노출되는 것은 get 방식의 특징이며 post와는 다르다.', 'method 속성은 데이터 전송 방식(HTTP 메서드)을 지정하는 것으로, 버튼의 색상과는 무관하다.', 'post 방식도 get과 마찬가지로 서버에 데이터를 전달하며, 전달 위치만 다를 뿐이다.']}
/>

### 문제 21

```css
.box {
  width: 100px;
  padding: 10px;
  border: 5px solid black;
  margin: 20px;
}
```

<Quiz
  questionNumber={21}
  question="CSS 박스 모델(box model)에서 위 클래스가 적용된 요소가 차지하는 전체 너비(기본 box-sizing 기준)에 대한 설명으로 가장 적절한 것은?"
  options={['content 너비 100px에 좌우 padding 20px, 좌우 border 10px을 더한 130px이 실제 테두리까지의 너비이며, margin 40px은 바깥 여백으로 별도로 차지한다', '전체 너비는 항상 width에 지정한 100px 그대로 고정된다', 'margin은 요소 자신의 크기 계산에 포함되어 실제 테두리 너비가 140px이 된다', 'padding과 border는 요소의 크기에 전혀 영향을 주지 않는다']}
  correctAnswer={1}
  explanation="기본 박스 모델(box-sizing이 content-box인 기본값)에서 width에 지정한 값은 내용(content) 영역만의 너비를 의미하며, 그 바깥으로 padding(안쪽 여백)과 border(테두리 두께)가 추가로 더해져 실제 테두리까지의 너비가 결정된다. 좌우 padding이 각각 10px이므로 합쳐서 20px, 좌우 border가 각각 5px이므로 합쳐서 10px이 content 너비 100px에 더해져 100 더하기 20 더하기 10인 130px이 테두리까지의 실제 너비가 된다. margin(바깥 여백)은 요소 자신의 크기에는 포함되지 않고, 이 요소와 주변 다른 요소 사이의 간격을 만드는 별도의 공간이다."
  optionExplanations={['content, padding, border를 모두 더한 130px이 테두리까지의 너비이고 margin은 별도의 바깥 여백이라는 설명이 정확하므로 정답이다.', '기본 박스 모델에서는 padding과 border가 content 너비 바깥에 추가되므로 전체 너비가 width 값 그대로 고정되지 않는다.', 'margin은 요소 자신의 테두리까지의 크기 계산에 포함되지 않고 바깥 여백으로만 작용한다.', 'padding과 border는 기본 박스 모델에서 요소가 차지하는 실제 크기를 늘리는 요소들이다.']}
/>

### 문제 22

```html
<button id="btn">클릭</button>
<p id="msg"></p>

<script>
  var count = 0;
  document.getElementById("btn").addEventListener("click", function () {
    count = count + 1;
    document.getElementById("msg").innerText = "클릭 횟수: " + count;
  });
</script>
```

<Quiz
  questionNumber={22}
  question="버튼에 addEventListener로 클릭 이벤트를 등록한 위 JavaScript 코드에서 버튼을 세 번 클릭했을 때 p 요소에 표시되는 최종 내용으로 가장 적절한 것은?"
  options={['클릭 횟수: 3', '클릭 횟수: 1', '클릭 횟수: 0', '아무 내용도 표시되지 않는다']}
  correctAnswer={1}
  explanation="addEventListener는 지정한 요소에 특정 이벤트(여기서는 click)가 발생할 때마다 등록된 콜백 함수(이벤트가 발생했을 때 실행되도록 미리 넘겨준 함수)를 실행하도록 등록하는 함수다. 버튼을 클릭할 때마다 콜백 함수가 실행되어 count 변수를 1씩 증가시키고, id가 msg인 문단 요소의 innerText(화면에 보이는 텍스트 내용을 나타내는 속성)를 클릭 횟수를 포함한 문자열로 갱신한다. count는 함수 바깥에 선언되어 클릭 사이에도 값이 유지되므로, 첫 클릭 후 1, 두 번째 클릭 후 2, 세 번째 클릭 후 3이 되어 최종적으로 문단에는 클릭 횟수: 3이라는 내용이 표시된다."
  optionExplanations={['count가 클릭마다 누적 증가해 세 번 클릭 후 3이 되고 이를 반영한 문자열이 표시되므로 정답이다.', '1은 첫 번째 클릭 직후의 값일 뿐이며, 세 번 클릭한 뒤의 최종 값이 아니다.', 'count는 클릭할 때마다 콜백 함수 안에서 증가하도록 작성되어 있으므로 0에서 멈추지 않는다.', '이벤트 리스너가 정상적으로 등록되어 클릭할 때마다 문단 내용을 갱신하므로 내용이 표시되지 않을 이유가 없다.']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [Oracle Database SQL Language Reference](https://docs.oracle.com/en/database/oracle/oracle-database/index.html)
- [Oracle Java Servlet 및 JSP 관련 문서(Jakarta EE)](https://jakarta.ee/specifications/)
