---
title: '26. 독학사형 종합 연습 ③ 트랜잭션·동시성·회복·기타'
description: '국가평생교육진흥원 출제기준에 근거해 재구성한 트랜잭션·ACID·직렬성, 잠금·2PL·타임스탬프·교착, 회복·로그·체크포인트·WAL, 저장 구조·분산/객체 DB 혼합 문제 22선'
---

<Callout type="warning">
  **이 문제 세트는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 국가평생교육진흥원이 공개한 독학학위제 4단계 데이터베이스 평가영역(출제기준) 중 트랜잭션·동시성 제어·회복·저장 구조·분산 및 객체 데이터베이스 항목을 근거로, 다루는 개념·계산 유형·오답 구성 방식을 참고해 지문과 데이터를 모두 새로 구성한 예상문제입니다. 실제 회차의 문항 수·배점·시험 시간은 매 공고마다 달라질 수 있으므로 **반드시 응시 회차의 공식 공고를 확인**하시기 바랍니다. 이 편은 18~23편에서 다룬 트랜잭션·동시성·회복·저장 구조·분산/객체 DB를 섞은 혼합 문제 22개로 구성했습니다.
</Callout>

직렬 가능성 판정(선행 그래프), 교착상태 탐지(대기 그래프), 회복 절차(REDO·UNDO 목록), 인덱스 비용 계산 문항은 해설에 계산 과정을 전부 풀어 두었습니다. 정답 번호만 확인하지 말고 표·그래프를 직접 그려 가며 검산하시기 바랍니다.

## 문항 배분

| 영역 | 문항 수 | 문항 번호 |
|------|---------|-----------|
| 트랜잭션·ACID·직렬성 | 5 | 1–5 |
| 동시성 제어(잠금·2PL·타임스탬프·교착) | 6 | 6–11 |
| 회복(로그·체크포인트·WAL·REDO/UNDO) | 6 | 12–17 |
| 저장 구조·분산/객체 DB | 5 | 18–22 |
| 합계 | 22 | 1–22 |

## 트랜잭션·ACID·직렬성 (1–5번)

<Quiz
  questionNumber={1}
  question="온라인 쇼핑몰에서 주문 트랜잭션이 재고 차감과 결제 승인 두 연산으로 이루어져 있다. 결제 승인이 실패하면 이미 처리된 재고 차감도 반드시 취소되어야 한다는 요구는 ACID 중 어떤 특성과 가장 직접 관련되는가?"
  options={['일관성(Consistency)', '원자성(Atomicity)', '고립성(Isolation)', '지속성(Durability)']}
  correctAnswer={2}
  explanation="트랜잭션에 속한 여러 연산이 전부 반영되거나 전혀 반영되지 않아야 한다는 요구는 원자성의 정의 그 자체입니다. 결제 실패 시 재고 차감까지 함께 취소되어야 하는 것이 원자성이 지키는 약속입니다."
  optionExplanations={['일관성은 트랜잭션 실행 전후로 무결성 제약이 유지되는지를 다루는 성질로, 이 사례의 핵심과는 다릅니다', '정답. 전부 반영되거나 전혀 반영되지 않아야 한다는 요구가 원자성입니다', '고립성은 동시에 실행되는 다른 트랜잭션과의 간섭 문제를 다루므로 이 사례와는 다른 축입니다', '지속성은 커밋 이후 장애에도 결과가 보존되는지를 다루므로 이 사례와는 다른 축입니다']}
/>

<Quiz
  questionNumber={2}
  question="트랜잭션의 상태 전이에 대한 설명으로 옳지 않은 것은?"
  options={['활성 상태에서 오류가 발생하면 실패 상태로 전이될 수 있다', '커밋 상태에 도달한 트랜잭션의 결과는 이후 장애가 나도 사라지지 않아야 한다', '중단 상태는 커밋을 완료한 뒤 도달하는 최종 상태다', '실패 상태의 트랜잭션은 롤백을 거쳐 중단 상태로 전이된다']}
  correctAnswer={3}
  explanation="중단(aborted) 상태는 실패한 트랜잭션이 롤백을 완료해 도달하는 상태이지, 커밋을 완료한 뒤에 도달하는 상태가 아닙니다. 커밋을 완료하면 커밋 상태로 종료됩니다."
  optionExplanations={['활성 상태에서 오류가 발생하면 실패 상태로 전이될 수 있다는 것은 옳은 설명이다', '지속성의 요구와 일치하는 옳은 설명이다', '옳지 않은 것을 찾는 문제의 정답. 중단은 실패 후 롤백을 거쳐 도달하는 상태이지 커밋 후 도달하는 상태가 아니다', '실패 상태에서 롤백을 완료하면 중단 상태로 전이된다는 것은 옳은 설명이다']}
/>

<Quiz
  questionNumber={3}
  question="다음 스케줄 S에서 선행 그래프를 그렸을 때의 판정으로 옳은 것은? (X, Y는 서로 다른 데이터 항목) t1: T1 R(X) / t2: T2 R(Y) / t3: T1 W(Y) / t4: T3 R(X) / t5: T2 W(X) / t6: T3 W(Y)"
  options={['간선은 T1→T3 하나만 존재하며 사이클이 없어 충돌 직렬 가능하다', 'T1→T2와 T2→T1 간선이 모두 만들어져 사이클이 생기므로 충돌 직렬 가능하지 않다', '이 스케줄에는 서로 다른 트랜잭션 간 충돌이 전혀 없어 판정 자체가 불필요하다', 'T2→T3 간선만 존재하며 사이클이 없어 충돌 직렬 가능하다']}
  correctAnswer={2}
  explanation="X에 대한 접근(t1 T1R, t4 T3R, t5 T2W)에서 t1과 t5는 read-write 충돌로 T1→T2, t4와 t5는 read-write 충돌로 T3→T2가 생깁니다. Y에 대한 접근(t2 T2R, t3 T1W, t6 T3W)에서 t2와 t3는 read-write 충돌로 T2→T1, t2와 t6은 read-write 충돌로 T2→T3, t3와 t6은 write-write 충돌로 T1→T3가 생깁니다. 이 중 T1→T2와 T2→T1이 함께 존재해 사이클을 이루므로 충돌 직렬 가능하지 않습니다."
  optionExplanations={['T1→T3 간선 외에도 T1→T2, T2→T1, T3→T2, T2→T3 간선이 함께 생기며, 그중 T1↔T2 사이클이 존재합니다', '정답. T1→T2와 T2→T1 간선이 사이클을 이루어 충돌 직렬 가능하지 않습니다', '두 트랜잭션이 같은 데이터 항목(X 또는 Y)에 접근하는 지점마다 충돌이 발생합니다', '이 간선 외에도 여러 간선이 존재하며, 특히 T1↔T2 사이클 때문에 직렬 가능하지 않습니다']}
/>

<Quiz
  questionNumber={4}
  question="트랜잭션 T2가 T1이 아직 커밋하지 않은 상태에서 쓴 값을 읽었고, 그 뒤 T2가 먼저 커밋을 완료했으며 T1은 그 이후에 커밋했다. 이 스케줄에 대한 설명으로 옳은 것은?"
  options={['회복 가능 스케줄의 조건을 만족한다', '회복 가능 스케줄의 조건을 만족하지 못한다', '엄격 스케줄의 조건을 만족한다', '연쇄 없는 스케줄의 조건을 만족한다']}
  correctAnswer={2}
  explanation="회복 가능 스케줄은 Tj가 Ti가 쓴 값을 읽었다면 Ti의 커밋이 Tj의 커밋보다 반드시 먼저 일어나야 한다는 조건입니다. 이 사례는 T2(Tj)가 T1(Ti)보다 먼저 커밋했으므로 이 조건을 위반해 회복 가능하지 않습니다. 만약 이 상태에서 T1이 실패해 롤백된다면, 이미 커밋되어 되돌릴 수 없는 T2까지 잘못된 값에 근거해 결과를 확정한 셈이 됩니다."
  optionExplanations={['T1의 커밋이 T2의 커밋보다 나중이므로 회복 가능 조건(값을 쓴 트랜잭션이 먼저 커밋)을 위반합니다', '정답. Tj가 Ti보다 먼저 커밋해 회복 불가능한 상태입니다', '회복 가능 조건조차 위반하므로 이보다 엄격한 조건(엄격 스케줄)도 당연히 만족하지 못합니다', '연쇄 없는 스케줄도 회복 가능 스케줄의 부분집합이므로, 회복 가능성부터 위반하면 연쇄 없는 스케줄일 수 없습니다']}
/>

<Quiz
  questionNumber={5}
  question="트랜잭션·스케줄 관련 개념에 대한 설명으로 옳지 않은 것은?"
  options={['고립성은 동시에 실행되는 트랜잭션들이 서로의 중간 결과에 영향을 주지 않아야 한다는 성질이다', '지속성은 커밋된 트랜잭션의 결과가 이후 장애에도 사라지지 않아야 한다는 성질이다', '직렬 스케줄은 트랜잭션 사이에 연산이 전혀 끼어들지 않으므로 항상 회복 가능 스케줄이다', '충돌 직렬 가능한 스케줄은 항상 엄격 스케줄이기도 하다']}
  correctAnswer={4}
  explanation="충돌 직렬 가능성(고립성 판정 기준)과 엄격 스케줄 여부(회복 관련 판정 기준)는 서로 독립적인 기준입니다. 충돌 직렬 가능하다고 해서 반드시 엄격 스케줄인 것은 아니며, 이 둘을 동일시하는 설명은 옳지 않습니다."
  optionExplanations={['고립성의 정확한 정의다', '지속성의 정확한 정의다', '직렬 스케줄은 한 트랜잭션이 끝나야 다음이 시작하므로 커밋되지 않은 값을 읽는 상황 자체가 생기지 않아 항상 회복 가능하다는 것은 옳은 설명이다', '옳지 않은 것을 찾는 문제의 정답. 직렬 가능성과 엄격성은 서로 다른 독립적인 판정 기준이다']}
/>

## 동시성 제어: 잠금·2PL·타임스탬프·교착 (6–11번)

<Quiz
  questionNumber={6}
  question="트랜잭션 T1이 데이터 항목 A에 공유 잠금(S-lock)을 가지고 있다. 트랜잭션 T2가 같은 A에 S-lock을 요청하면 어떻게 되는가?"
  options={['잠금 호환성 표에 따라 즉시 허용된다', 'X-lock과 동일하게 취급되어 거부된다', 'T1의 S-lock이 자동으로 X-lock으로 승격된다', 'T2는 반드시 T1이 커밋할 때까지 대기해야 한다']}
  correctAnswer={1}
  explanation="잠금 호환성 표에서 S-lock끼리는 유일하게 공존이 허용되는 조합입니다. 읽기 전용 잠금끼리는 서로 방해하지 않으므로 T2의 S-lock 요청은 즉시 허용됩니다."
  optionExplanations={['정답. S-S 조합은 호환성 표에서 허용되는 유일한 조합이다', 'S-lock끼리는 공존 가능하므로 X-lock처럼 거부되지 않는다', '승격은 트랜잭션 자신이 쓰기를 시도할 때 필요에 따라 요청하는 것이지, 다른 트랜잭션의 S-lock 요청으로 자동 승격되지 않는다', 'S-lock 요청은 다른 트랜잭션의 S-lock과 충돌하지 않으므로 대기할 필요가 없다']}
/>

<Quiz
  questionNumber={7}
  question="다음 잠금 이력에서 T1과 T2가 2PL(2단계 잠금 프로토콜)을 따르는지 판정한 것으로 옳은 것은? t1: T1 X(A) / t2: T2 S(B) / t3: T2 S(C) / t4: T1 U(A) / t5: T2 U(B) / t6: T2 S(D)"
  options={['T1과 T2 모두 2PL을 따른다', 'T1은 2PL을 따르지만 T2는 위반한다', 'T1은 위반하지만 T2는 2PL을 따른다', 'T1과 T2 모두 2PL을 위반한다']}
  correctAnswer={2}
  explanation="T1은 X(A) 획득(t1) 후 U(A) 해제(t4)로 끝나 획득 뒤 해제 한 번뿐이므로 확장 국면과 수축 국면이 명확히 분리되어 2PL을 따릅니다. T2는 S(B)·S(C) 획득(t2, t3) 후 U(B) 해제(t5)로 수축 국면에 들어갔는데, 그 뒤 t6에서 S(D)를 새로 획득해 다시 확장 국면으로 돌아갔으므로 2PL을 위반합니다."
  optionExplanations={['T2가 해제 이후 새 잠금을 획득해 국면이 뒤섞였으므로 이 설명은 틀렸다', '정답. T1은 국면이 분리되어 있고 T2는 해제 후 재획득으로 위반한다', 'T1은 획득 후 해제 순서를 지켜 2PL을 따르므로 이 설명은 틀렸다', 'T1은 2PL을 따르므로 둘 다 위반한다는 설명은 틀렸다']}
/>

<Quiz
  questionNumber={8}
  question="2PL의 변형(기본 2PL, strict 2PL, strong strict 2PL)에 대한 설명으로 옳지 않은 것은?"
  options={['strict 2PL은 X-lock을 트랜잭션의 커밋·중단 시점까지 유지한다', 'strong strict 2PL은 S-lock과 X-lock을 모두 커밋·중단 시점까지 유지한다', '기본 2PL은 충돌 직렬 가능성조차 보장하지 못하는 프로토콜이다', 'strict 2PL은 연쇄 롤백을 방지하는 데 도움이 된다']}
  correctAnswer={3}
  explanation="기본 2PL도 확장·수축 국면을 지키기만 하면 충돌 직렬 가능성은 보장합니다. 기본 2PL이 보장하지 못하는 것은 연쇄 롤백 방지이지, 충돌 직렬 가능성 자체가 아닙니다."
  optionExplanations={['strict 2PL의 정의로 옳은 설명이다', 'strong strict 2PL의 정의로 옳은 설명이다', '옳지 않은 것을 찾는 문제의 정답. 기본 2PL도 충돌 직렬 가능성은 보장하며, 보장하지 못하는 것은 연쇄 롤백 방지다', 'X-lock을 커밋 시점까지 유지하면 다른 트랜잭션이 커밋 전 값을 읽지 못해 연쇄 롤백을 막는 데 도움이 된다는 것은 옳은 설명이다']}
/>

<Quiz
  questionNumber={9}
  question="타임스탬프 순서 프로토콜에서 T1의 타임스탬프는 10, T2의 타임스탬프는 20이다. 데이터 항목 D의 초기값은 R_TS(D)=0, W_TS(D)=0이다. 다음 순서로 연산이 들어온다: ① T2: write(D) ② T1: write(D). Thomas의 쓰기 규칙을 적용하면 ②는 어떻게 처리되는가?"
  options={['T1은 롤백되고 더 큰 새 타임스탬프로 재시작된다', 'T1의 쓰기는 무시(no-op)되고 T1은 계속 진행된다', 'T1의 쓰기가 정상적으로 수행되어 W_TS(D)가 10으로 갱신된다', 'T2가 대신 롤백된다']}
  correctAnswer={2}
  explanation="①에서 T2: write(D)는 TS(T2)=20이 R_TS(D)=0, W_TS(D)=0보다 크거나 같아 허용되고 W_TS(D)=20이 됩니다. ②에서 T1: write(D)는 TS(T1)=10이 R_TS(D)=0보다는 작지 않지만 W_TS(D)=20보다는 작습니다(10이 20보다 작음). 기본 규칙이라면 롤백 대상이지만, 이 조건은 R_TS 위반이 아니라 W_TS 위반이므로 Thomas의 쓰기 규칙이 적용되어, 이 쓰기 결과는 어차피 더 최신 값(T2가 쓴 값)에 덮어써질 것이므로 롤백 대신 조용히 무시하고 T1은 계속 진행됩니다."
  optionExplanations={['이는 Thomas의 쓰기 규칙을 적용하지 않은 기본 규칙의 처리 방식이다', '정답. W_TS 위반이므로 Thomas의 쓰기 규칙에 따라 무시 처리된다', 'W_TS(D)를 10으로 되돌리면 이미 반영된 T2의 최신 값(20 기준)이 손상되므로 옳지 않다', 'Thomas의 쓰기 규칙은 T1의 쓰기를 무시하는 것이지 T2를 롤백시키는 규칙이 아니다']}
/>

<Quiz
  questionNumber={10}
  question="네 트랜잭션의 잠금 대기 상황이 다음과 같다: T1은 A를 갖고 B를 기다린다. T2는 B를 갖고 C를 기다린다. T3는 C를 갖고 D를 기다린다. T4는 D를 갖고 A를 기다린다. 이 상황에 대한 설명으로 옳은 것은?"
  options={['대기 그래프에 사이클이 없어 교착상태가 아니다', 'T1→T2→T3→T4→T1의 사이클이 생겨 네 트랜잭션 모두 교착상태에 빠져 있다', 'T1과 T3만 교착상태에 빠져 있고 T2, T4는 정상 진행된다', '대기 그래프는 이 상황을 표현할 수 없어 판정이 불가능하다']}
  correctAnswer={2}
  explanation="각 트랜잭션이 기다리는 대상을 간선으로 그리면 T1→T2(B를 가진 T2를 기다림), T2→T3(C를 가진 T3를 기다림), T3→T4(D를 가진 T4를 기다림), T4→T1(A를 가진 T1을 기다림)이 되어 네 트랜잭션을 모두 포함하는 하나의 사이클이 만들어집니다. 사이클에 포함된 네 트랜잭션 모두 교착상태입니다."
  optionExplanations={['네 간선이 모두 이어져 하나의 사이클을 이루므로 사이클이 없다는 설명은 틀렸다', '정답. 네 트랜잭션을 모두 포함하는 사이클이 존재해 전부 교착상태다', '사이클에 포함된 모든 트랜잭션이 교착상태이며, 일부만 교착상태라는 설명은 틀렸다', '대기 그래프는 이런 다자간 순환 대기도 정확히 표현하고 탐지할 수 있다']}
/>

<Quiz
  questionNumber={11}
  question="트랜잭션 Ti(타임스탬프 5, 먼저 시작)가 트랜잭션 Tj(타임스탬프 15, 나중에 시작)가 가진 잠금을 요청했다. wound-wait 기법을 적용하면 어떤 일이 일어나는가?"
  options={['Ti가 잠금을 얻을 때까지 대기한다', 'Ti가 스스로 중단(die)된다', 'Tj가 강제로 중단(wound)되고 Ti가 그 잠금을 차지한다', 'Tj가 잠금을 얻을 때까지 대기한다']}
  correctAnswer={3}
  explanation="wound-wait에서는 요청자 Ti가 상대 Tj보다 오래된(타임스탬프가 더 작은) 경우, Ti가 Tj를 강제로 중단시키고 그 잠금을 빼앗습니다. Ti(5)가 Tj(15)보다 먼저 시작했으므로 이 규칙이 적용됩니다."
  optionExplanations={['대기하는 쪽은 wait-die 기법에서 오래된 트랜잭션이 취하는 동작이다', 'die(스스로 중단)는 요청자가 상대보다 나중에 시작했을 때(wait-die)의 동작이며, 이 상황에는 해당하지 않는다', '정답. 오래된 요청자가 젊은 보유자를 강제 중단시키는 것이 wound-wait의 핵심이다', 'Tj가 대기하는 것이 아니라 강제로 중단되는 것이 wound-wait의 특징이다']}
/>

## 회복: 로그·체크포인트·WAL (12–17번)

<Quiz
  questionNumber={12}
  question="장애 유형과 대응 방법에 대한 설명으로 옳지 않은 것은?"
  options={['트랜잭션 장애는 논리적 오류 등으로 발생하며 해당 트랜잭션만 롤백하면 된다', '시스템 장애는 주기억장치 내용은 손실되지만 디스크 내용은 보존된다는 전제를 따른다', '미디어 장애는 로그 기반 REDO·UNDO만으로 항상 완전히 복구할 수 있다', '시스템 장애의 대응에는 로그를 이용한 REDO와 UNDO가 모두 쓰일 수 있다']}
  correctAnswer={3}
  explanation="미디어 장애는 디스크 자체가 물리적으로 손상되는 경우이므로, 로그만으로는 복구할 수 없고 별도의 백업본이 반드시 필요합니다."
  optionExplanations={['트랜잭션 장애의 대응으로 옳은 설명이다', '시스템 장애 회복의 핵심 전제로 옳은 설명이다', '옳지 않은 것을 찾는 문제의 정답. 미디어 장애는 백업이 반드시 필요하다', '시스템 장애 대응으로 옳은 설명이다']}
/>

<Quiz
  questionNumber={13}
  question="갱신 로그 레코드가 &lt;T5, E, 800, 950&gt; 형태로 기록되었다. 이 레코드의 의미로 옳은 것은?"
  options={['트랜잭션 T5가 데이터 항목 E를 800에서 950으로 갱신했으며, UNDO 시에는 950을, REDO 시에는 800을 사용한다', '트랜잭션 T5가 데이터 항목 E를 800에서 950으로 갱신했으며, UNDO 시에는 800을, REDO 시에는 950을 사용한다', '트랜잭션 800이 데이터 항목 E를 5에서 950으로 갱신했다는 뜻이다', '이 레코드만으로는 갱신 전후 값을 알 수 없다']}
  correctAnswer={2}
  explanation="갱신 로그 레코드 &lt;Ti, Xj, V1, V2&gt;에서 V1은 갱신 전(이전) 값으로 UNDO에, V2는 갱신 후(이후) 값으로 REDO에 사용됩니다. 이 레코드는 T5가 E를 800에서 950으로 바꿨다는 뜻이며, 되돌릴 때는 이전 값 800을, 다시 반영할 때는 이후 값 950을 사용합니다."
  optionExplanations={['UNDO·REDO에 사용하는 값이 반대로 설명되어 있다', '정답. 이전 값(800)은 UNDO, 이후 값(950)은 REDO에 사용된다는 정의와 정확히 일치한다', 'T5는 트랜잭션 식별자이지 800이 트랜잭션 식별자가 아니다', '로그 레코드 형식(Ti, Xj, V1, V2)에서 V1, V2가 각각 갱신 전후 값을 명확히 나타낸다']}
/>

<Quiz
  questionNumber={14}
  question="즉시 갱신(immediate update)과 지연 갱신(deferred update)에 대한 설명으로 옳지 않은 것은?"
  options={['즉시 갱신은 커밋 전에도 데이터베이스에 값을 반영할 수 있다', '지연 갱신은 커밋되기 전까지 데이터베이스에 값을 반영하지 않는다', '지연 갱신 방식에서는 커밋 전이라도 갱신이 일어나는 즉시 로그에 기록한다', '즉시 갱신 방식은 undo 없이 redo만으로 회복할 수 있는 no-undo 방식이다']}
  correctAnswer={4}
  explanation="undo 없이 redo만으로 충분한 no-undo 방식은 지연 갱신입니다. 즉시 갱신은 커밋 전에도 데이터베이스에 값을 반영할 수 있으므로 undo와 redo가 모두 필요합니다."
  optionExplanations={['즉시 갱신의 정의로 옳은 설명이다', '지연 갱신의 정의로 옳은 설명이다', '지연 갱신도 로그 기록 자체는 갱신 즉시 이루어지고 데이터베이스 반영만 커밋 이후로 미룬다는 것은 옳은 설명이다', '옳지 않은 것을 찾는 문제의 정답. no-undo 방식은 지연 갱신이며 즉시 갱신은 undo·redo가 모두 필요하다']}
/>

<Quiz
  questionNumber={15}
  question="쓰기 전 로그(Write-Ahead Logging, WAL) 원칙에 대한 설명으로 옳은 것은?"
  options={['데이터 항목의 새 값이 디스크에 반영된 뒤에 그 변경의 로그 레코드를 기록해도 된다', '데이터 항목에 대한 갱신 로그 레코드는 그 데이터의 새 값이 디스크에 반영되기 전에 먼저 디스크에 기록되어야 한다', 'WAL 원칙은 커밋 로그 레코드에는 적용되지 않는다', 'WAL 원칙을 지키지 않아도 회복 절차의 정확성에는 영향이 없다']}
  correctAnswer={2}
  explanation="WAL 원칙은 데이터의 실제 변경보다 그 변경을 되돌릴 수 있는 로그 레코드의 기록이 항상 먼저(ahead) 이루어져야 한다는 규칙입니다. 이를 통해 '데이터는 바뀌었는데 되돌릴 로그가 없는' 상황을 원천적으로 막습니다."
  optionExplanations={['순서가 반대로 서술되어 있다. 로그가 먼저 기록되어야 한다', '정답. WAL 원칙의 핵심 순서를 정확히 설명한다', 'WAL 원칙은 커밋 로그 레코드의 기록 순서에도 적용된다', 'WAL 원칙을 지키지 않으면 되돌릴 근거가 사라져 회복이 불가능한 상황이 생길 수 있다']}
/>

<Quiz
  questionNumber={16}
  question="체크포인트(checkpoint)에 대한 설명으로 옳지 않은 것은?"
  options={['체크포인트를 수행하면 메모리에 남아 있는 로그와 수정된 페이지를 디스크에 강제로 기록한다', '체크포인트 레코드에는 그 시점에 아직 진행 중인 트랜잭션 목록이 포함된다', '체크포인트가 있으면 회복 시 로그 전체가 아니라 가장 최근 체크포인트부터 훑으면 된다', '체크포인트 이전에 커밋을 완료한 트랜잭션도 장애 발생 시 항상 다시 REDO 대상에 포함해야 한다']}
  correctAnswer={4}
  explanation="체크포인트 자체가 그 시점까지 커밋된 트랜잭션의 변경은 이미 디스크에 안전하게 반영되었음을 보장하는 절차이므로, 체크포인트 이전에 커밋된 트랜잭션은 REDO 대상에 다시 포함시킬 필요가 없습니다."
  optionExplanations={['체크포인트 수행 절차로 옳은 설명이다', '체크포인트 레코드의 구성으로 옳은 설명이다', '체크포인트의 목적으로 옳은 설명이다', '옳지 않은 것을 찾는 문제의 정답. 체크포인트 이전 커밋 트랜잭션은 REDO 대상이 아니다']}
/>

<Quiz
  questionNumber={17}
  question="다음 로그가 기록된 뒤 마지막 줄에서 시스템 장애가 발생했다. REDO·UNDO 목록으로 옳은 것은? 1.&lt;T1 start&gt; 2.&lt;T1,A,500,600&gt; 3.&lt;T2 start&gt; 4.&lt;T2,B,100,150&gt; 5.&lt;T2 commit&gt; 6.&lt;checkpoint, 활성 트랜잭션 목록 T1&gt; 7.&lt;T3 start&gt; 8.&lt;T1,C,300,250&gt; 9.&lt;T1 commit&gt; 10.&lt;T3,D,700,800&gt; -- 장애 발생 --"
  options={['REDO = {T1, T3}, UNDO = { }', 'REDO = {T1}, UNDO = {T3}', 'REDO = {T3}, UNDO = {T1}', 'REDO = { }, UNDO = {T1, T3}']}
  correctAnswer={2}
  explanation="6번 줄 체크포인트 시점의 활성 트랜잭션 목록은 {T1}이므로 undo-list={T1}, redo-list={ }로 시작합니다. 7번 줄에서 T3가 시작해 undo-list={T1, T3}가 됩니다. 9번 줄에서 T1이 커밋해 undo-list에서 제거되고 redo-list에 추가되어 undo-list={T3}, redo-list={T1}이 됩니다. 10번 줄은 T3의 갱신이며 그 뒤 커밋·중단 없이 장애가 났으므로 T3는 그대로 undo-list에 남습니다. T2는 체크포인트 이전(5번 줄)에 이미 커밋했으므로 두 목록 어디에도 들어가지 않습니다."
  optionExplanations={['T3는 커밋되지 않았으므로 REDO가 아니라 UNDO 대상이다', '정답. 체크포인트 이후 커밋한 T1은 REDO, 끝내 커밋하지 못한 T3는 UNDO 대상이다', 'T1과 T3의 REDO·UNDO 배정이 뒤바뀐 오답이다', 'T1은 커밋을 완료했으므로 UNDO가 아니라 REDO 대상이다']}
/>

## 저장 구조·분산/객체 데이터베이스 (18–22번)

<Quiz
  questionNumber={18}
  question="한 블록의 크기가 4,800바이트이고 레코드 하나의 크기가 240바이트이며, 레코드가 블록 경계를 넘어 나뉘지 않는다고 가정한다. 전체 레코드가 100,000개일 때 이 테이블이 차지하는 블록 수는?"
  options={['2,500블록', '4,000블록', '5,000블록', '20,000블록']}
  correctAnswer={3}
  explanation="블록당 레코드 수는 4,800 ÷ 240 = 20개입니다. 전체 블록 수는 전체 레코드 수를 블록당 레코드 수로 나눈 뒤 올림한 값이므로 100,000 ÷ 20 = 5,000블록입니다."
  optionExplanations={['블록당 레코드 수를 다르게(예: 40개) 계산했을 때 나올 수 있는 값이다', '블록당 레코드 수를 25개로 잘못 계산했을 때 나올 수 있는 값이다', '정답. 100,000 ÷ 20 = 5,000블록이다', '블록당 레코드 수 계산 없이 전체 레코드 수를 그대로 5로 나눈 값과 혼동했을 때 나올 수 있는 값이다']}
/>

<Quiz
  questionNumber={19}
  question="차수(order) n = 50인 B+트리에 N = 125,000개의 키가 저장되어 있다. 이 트리에서 특정 키를 찾기 위한 트리 높이(블록 I/O 횟수)로 가장 적절한 것은?"
  options={['약 2번', '약 3번', '약 50번', '약 2,500번']}
  correctAnswer={2}
  explanation="트리 높이는 h = 올림(log_n N) 공식으로 구합니다. 50의 3제곱(50×50×50)이 125,000이므로 log_50(125,000) = 3이고, 트리 높이는 3단계입니다."
  optionExplanations={['50의 2제곱은 2,500으로 125,000에 미치지 못하므로 2단계로는 부족하다', '정답. 50의 3제곱이 125,000과 같아 트리 높이는 3단계다', '차수 50은 각 노드의 최대 자식 수이지 트리 높이가 아니다', '이 값은 125,000 ÷ 50으로 계산했을 때 나오는 값으로, 로그 스케일 계산이 아니라 단순 나눗셈을 적용한 오답이다']}
/>

<Quiz
  questionNumber={20}
  question="밀집 인덱스·희소 인덱스·2차 인덱스에 대한 설명으로 옳지 않은 것은?"
  options={['희소 인덱스는 데이터가 검색 키 순서로 정렬되어 있음을 전제로 한다', '밀집 인덱스는 모든 검색 키 값에 대해 인덱스 항목을 하나씩 둔다', '2차 인덱스는 데이터의 물리적 정렬 순서와 무관한 속성에 만들 수 있으며 희소 인덱스로 구현하는 것이 가장 효율적이다', '2차 인덱스는 정렬 기준이 아니므로 대표 값 하나로 여러 레코드를 대신 가리킬 수 없어 밀집 인덱스여야 한다']}
  correctAnswer={3}
  explanation="희소 인덱스는 데이터가 정렬되어 있다는 전제 아래 대표 값만 두는 방식입니다. 2차 인덱스는 물리적 정렬 기준과 무관한 속성에 만들므로 대표 값 하나로 여러 레코드를 대표할 수 없어, 희소 인덱스가 아니라 반드시 밀집 인덱스로 구현해야 합니다."
  optionExplanations={['희소 인덱스의 전제 조건으로 옳은 설명이다', '밀집 인덱스의 정의로 옳은 설명이다', '옳지 않은 것을 찾는 문제의 정답. 2차 인덱스는 희소가 아니라 밀집 인덱스로 구현해야 한다', '2차 인덱스가 밀집 인덱스여야 하는 이유로 옳은 설명이다']}
/>

<Quiz
  questionNumber={21}
  question="분산 데이터베이스의 데이터 분할·복제·투명성에 대한 설명으로 옳지 않은 것은?"
  options={['수평 분할은 행 단위로, 수직 분할은 열 단위로 테이블을 나눈다', '복제는 읽기 성능과 장애 대응력을 높이지만 갱신 시 모든 복제본에 변경을 반영해야 하는 비용이 따른다', '위치 투명성은 조각이나 복제본이 물리적으로 어느 사이트에 있는지를 사용자가 몰라도 되게 하는 것이다', '수직 분할된 조각에는 기본 키를 포함시키지 않는 것이 원래 테이블 복원에 유리하다']}
  correctAnswer={4}
  explanation="수직 분할된 조각에는 오히려 기본 키를 공통으로 포함시켜야, 나중에 조각들을 기본 키 기준으로 조인해 원래 테이블을 정확히 복원할 수 있습니다. 기본 키를 포함시키지 않는 것이 유리하다는 설명은 옳지 않습니다."
  optionExplanations={['수평·수직 분할의 정의로 옳은 설명이다', '복제의 장단점으로 옳은 설명이다', '위치 투명성의 정의로 옳은 설명이다', '옳지 않은 것을 찾는 문제의 정답. 수직 분할 조각은 기본 키를 공통으로 포함해야 복원이 가능하다']}
/>

<Quiz
  questionNumber={22}
  question="관계형 DBMS(RDBMS), 객체관계형 DBMS(ORDBMS), 객체지향 DBMS(OODB)의 객체 식별 방식에 대한 설명으로 옳은 것은?"
  options={['RDBMS의 튜플은 시스템이 부여한 객체 식별자(OID)만으로 식별되며 속성 값은 식별에 관여하지 않는다', 'OODB의 객체는 속성 값이 완전히 같으면 항상 같은 객체로 취급된다', 'RDBMS의 튜플은 기본 키 값으로 식별되고, OODB의 객체는 값과 무관하게 시스템이 부여하는 OID로 식별된다', 'ORDBMS는 OID 개념을 전혀 사용하지 않고 순수한 기본 키 식별 방식만 지원한다']}
  correctAnswer={3}
  explanation="관계형 모델의 튜플은 기본 키 값을 기준으로 식별되지만, 객체지향 모델의 객체는 속성 값과 무관하게 시스템이 부여하는 고유한 OID(Object Identifier)로 식별됩니다. 값이 완전히 같아도 OID가 다르면 서로 다른 객체로 구분됩니다."
  optionExplanations={['RDBMS는 기본 키(속성 값 기반)로 튜플을 식별하며 OID 개념을 쓰지 않는다', 'OID 기반 식별에서는 속성 값이 같아도 OID가 다르면 별개의 객체로 취급된다', '정답. 두 모델의 식별 방식 차이를 정확히 설명한다', 'ORDBMS는 관계형 모델을 기반으로 하되 사용자 정의 타입 등 일부 객체지향 개념을 확장해 받아들인 절충 모델이다']}
/>

## 참고 자료

- [Oracle Database Concepts - Transaction Management and Recovery](https://docs.oracle.com/en/database/oracle/oracle-database/index.html)
- [GeeksforGeeks - Concurrency Control in DBMS](https://www.geeksforgeeks.org/dbms/concurrency-control-in-dbms/)
- [GeeksforGeeks - Log-Based Recovery in DBMS](https://www.geeksforgeeks.org/dbms/log-based-recovery-in-dbms/)
- 국가평생교육진흥원 독학학위제: [https://bdes.nile.or.kr](https://bdes.nile.or.kr)
