---
title: '24. 독학사 4단계 알고리즘 모의 종합시험 (실전 1회분)'
description: '정렬·탐색·그래프·설계 기법·계산 복잡도 이론을 균형 있게 배분한 실전 모의고사 1회분'
---

<Callout type="warning">
  이 편은 실제 독학사 기출 문제를 그대로 옮긴 것이 아니라, 국가평생교육진흥원이 공개한 4단계 알고리즘 과목 출제기준(평가영역)에 근거하여 실전 시험의 난이도·범위 분포를 모사해 새로 구성한 모의 종합시험입니다. 정렬·탐색·그래프·설계 기법·계산 복잡도 다섯 영역을 균형 있게 배분했습니다. 실제 시험의 문항 수·배점·시간은 매 회차 공고를 통해 반드시 다시 확인하세요.
</Callout>

<Quiz
  questionNumber={1}
  question="다음 중 입력 크기 n이 커질수록 성장하는 속도가 느린 것부터 빠른 순서로 옳게 나열한 것은?"
  options={['O(log n) < O(n) < O(n log n) < O(n^2) < O(2^n)', 'O(2^n) < O(n^2) < O(n log n) < O(n) < O(log n)', 'O(n) < O(log n) < O(n^2) < O(n log n) < O(2^n)', 'O(n^2) < O(n log n) < O(2^n) < O(n) < O(log n)']}
  correctAnswer={1}
  explanation="점근적 표기법에서 대표적인 복잡도 계열은 성장 속도가 느린 순서대로 상수(O(1)), 로그(O(log n)), 선형(O(n)), 선형로그(O(n log n)), 다항식(O(n^2), O(n^3) 등), 지수(O(2^n)), 계승(O(n!)) 순으로 정리된다. n이 충분히 커지면 로그 함수는 어떤 다항식보다도 느리게 자라고, 지수 함수는 어떤 다항식보다도 빠르게 자란다. 예를 들어 n = 1000일 때 log2(1000)은 약 10, n은 1000, n log n은 약 10000, n^2은 1000000, 2^n은 상상하기 어려울 만큼 큰 수가 되어 이 순서가 실제 값으로도 확인된다."
  optionExplanations={['정답이다. 표준적인 복잡도 계열의 성장 속도 순서를 정확히 나열했다', '순서가 완전히 거꾸로 되어 있다. 지수 함수가 가장 느리게 자란다는 것은 사실과 반대다', '로그 함수가 선형 함수보다 느리게 자라야 하는데 순서가 뒤바뀌어 있고, n^2이 n log n보다 먼저 나와야 하는데도 순서가 잘못되었다', '지수 함수가 다항식보다 먼저(더 느리게 자라는 것처럼) 나열되어 있어 순서가 잘못되었다']}
/>

<Quiz
  questionNumber={2}
  question="이진 탐색의 점화식 T(n) = T(n/2) + O(1)을 반복 대입법으로 전개해 시간 복잡도를 유도하는 과정으로 옳은 것은?"
  options={['T(n) = T(n/2) + c = T(n/4) + 2c = T(n/8) + 3c = ... = T(n/2^k) + kc이며, n/2^k = 1이 되는 k = log2(n)에서 재귀가 끝나므로 T(n) = T(1) + c*log2(n) = O(log n)이다', 'T(n) = T(n/2) + c를 그대로 두면 매 단계 크기가 절반씩 줄면서 비교 횟수도 절반씩 줄어들어 총합이 O(n)이 된다', '재귀가 한 번 호출될 때마다 부분 문제가 2개씩 생긴다고 가정해야 하므로 T(n) = O(2^n)이다', 'T(n) = T(n/2) + c는 매번 상수 시간이 두 배씩 늘어나는 것으로 해석해야 하므로 O(n)이다']}
  correctAnswer={1}
  explanation="이진 탐색은 매 단계에서 비교 한 번(상수 시간 c)을 하고 탐색 범위를 절반으로 줄인다. 이를 점화식으로 쓰면 T(n) = T(n/2) + c이다. 이 점화식을 반복해서 대입하면 T(n) = T(n/2) + c = (T(n/4) + c) + c = T(n/4) + 2c = T(n/8) + 3c = ... 이런 식으로 k번 대입하면 T(n) = T(n/2^k) + kc가 된다. 재귀는 부분 문제 크기가 1이 될 때, 즉 n/2^k = 1일 때 끝나며, 이는 k = log2(n)일 때다. 이 값을 대입하면 T(n) = T(1) + c*log2(n)이 되고, T(1)은 상수이므로 지배항은 log2(n)에 비례하는 항이다. 따라서 T(n) = O(log n)이며, 이것이 정렬된 배열 n개에서 이진 탐색이 O(log n)인 이유를 점화식에서 직접 유도한 결과다."
  optionExplanations={['정답이다. 반복 대입을 통해 k = log2(n)에서 재귀가 끝난다는 것을 유도하고 O(log n)이라는 결론까지 정확히 이끌어냈다', '이진 탐색은 매 단계 탐색 범위(부분 문제 크기)는 절반으로 줄지만, 비교 자체는 매 단계 1번씩 일정하게 발생한다. 비교 횟수가 절반씩 줄어들어 등비수열을 이룬다는 전제 자체가 이진 탐색의 실제 동작과 다르다', '이진 탐색은 매 단계 하나의 부분 문제(왼쪽 또는 오른쪽 절반)만 재귀적으로 탐색하며 부분 문제가 2개로 늘어나지 않는다. 부분 문제가 2개로 늘어나는 것은 병합 정렬과 같은 다른 점화식의 특징이다', '점화식 T(n) = T(n/2) + c에서 c는 매 단계 일정한 상수 시간이며 두 배씩 늘어나는 값이 아니다. 이 점화식의 정확한 해는 O(log n)이지 O(n)이 아니다']}
/>

<Quiz
  questionNumber={3}
  question="어떤 배열에서 특정 값을 순차 탐색(sequential search)으로 찾을 때 최악의 경우와 평균적인 경우에 대한 설명으로 옳은 것은?"
  options={['최악의 경우는 찾는 값이 배열의 맨 마지막에 있거나 아예 없는 경우로 n번 비교가 필요하고(O(n)), 평균적인 경우(값이 배열 안에 균등하게 존재한다고 가정)는 대략 (n+1)/2번 비교가 필요하지만 둘 다 점근적 표기로는 O(n)으로 같다', '최악의 경우는 O(1)이고 평균적인 경우는 O(n)이다', '순차 탐색은 항상 정확히 1번의 비교로 끝난다', '최악의 경우와 평균적인 경우는 항상 정확히 같은 값(같은 상수)을 가진다']}
  correctAnswer={1}
  explanation="순차 탐색은 배열의 첫 원소부터 하나씩 순서대로 비교해 나가는 방식이다. 최악의 경우는 찾는 값이 배열의 맨 끝에 있거나 배열 안에 아예 존재하지 않는 경우로, 이때는 n개의 원소를 모두 비교해야 하므로 정확히 n번(또는 존재하지 않으면 n번 비교 후 실패로 끝남) 비교가 필요하다. 평균적인 경우는 찾는 값이 배열 안의 각 위치에 있을 확률이 동일하다고 가정하면, 1번째 위치에 있을 때 1번, 2번째 위치에 있을 때 2번, ..., n번째 위치에 있을 때 n번 비교가 필요하므로 이 값들의 평균은 (1+2+...+n)/n = (n+1)/2가 된다. 최악의 경우(n)와 평균의 경우((n+1)/2)는 정확한 값은 다르지만, 상수를 무시하는 점근적 표기법으로는 둘 다 O(n)으로 같은 계열에 속한다."
  optionExplanations={['정답이다. 최악의 경우와 평균의 경우 각각의 정확한 비교 횟수를 유도하고, 점근적으로는 같은 O(n) 계열이라는 결론까지 정확하다', '순차 탐색은 최악의 경우 배열 전체를 훑어야 하므로 O(1)이 아니라 O(n)이다. 오히려 O(1)에 가까운 것은 찾는 값이 첫 번째 위치에 있는 최선의 경우다', '순차 탐색이 항상 1번 비교로 끝나는 것은 찾는 값이 항상 첫 번째 원소일 때만 성립하는 매우 특수한 경우이며 일반적인 동작이 아니다', '최악의 경우(n번)와 평균의 경우((n+1)/2번)는 정확한 비교 횟수 자체는 서로 다른 값이다. 점근적 표기로만 같은 계열(O(n))에 속할 뿐 정확히 같은 값은 아니다']}
/>

<Quiz
  questionNumber={4}
  question="배열 [29, 10, 14, 37, 13]을 선택 정렬(selection sort)로 오름차순 정렬한다고 하자. 매 패스마다 아직 정렬되지 않은 구간에서 최솟값을 찾아 그 구간의 맨 앞과 교환한다. 1패스와 2패스가 끝난 직후의 배열 상태로 옳은 것은?"
  options={['1패스 후: [10, 29, 14, 37, 13], 2패스 후: [10, 13, 14, 37, 29]', '1패스 후: [10, 29, 14, 37, 13], 2패스 후: [10, 14, 29, 37, 13]', '1패스 후: [13, 10, 14, 37, 29], 2패스 후: [10, 13, 14, 37, 29]', '1패스 후: [29, 10, 14, 37, 13] (변화 없음), 2패스 후: [10, 29, 14, 37, 13]']}
  correctAnswer={1}
  explanation="1패스에서는 전체 구간 [29, 10, 14, 37, 13]에서 최솟값을 찾는다. 최솟값은 10(인덱스1)이므로 구간의 맨 앞(인덱스0)의 29와 교환한다. 결과는 [10, 29, 14, 37, 13]이다. 2패스에서는 이미 정렬된 인덱스0을 제외한 구간 [29, 14, 37, 13](인덱스1~4)에서 최솟값을 찾는다. 최솟값은 13(인덱스4)이므로 구간의 맨 앞(인덱스1)의 29와 교환한다. 결과는 [10, 13, 14, 37, 29]이다. 선택 정렬은 매 패스마다 이렇게 남은 구간의 최솟값을 찾아 맨 앞으로 옮기는 과정을 반복하며, 이 과정에서 비교는 매번 남은 구간 전체를 훑어야 하므로 총 비교 횟수는 (n-1) + (n-2) + ... + 1로 O(n^2)이 된다."
  optionExplanations={['정답이다. 각 패스에서 남은 구간의 최솟값을 찾아 맨 앞과 교환하는 과정을 정확히 따른 결과다', '2패스에서 최솟값(13)을 찾아 구간의 맨 앞(인덱스1)과 교환해야 하는데, 단순히 순서대로 앞의 두 값(14, 29)의 위치만 바꾼 것으로 착각한 오류다. 13이 29보다 작으므로 13이 인덱스1로 와야 한다', '1패스에서 최솟값은 13이 아니라 10이다(10이 13보다 작다). 1패스의 최솟값을 잘못 찾은 오류다', '1패스에서 이미 최솟값 10을 찾아 맨 앞으로 옮겨야 하는데 변화가 없다고 한 것은 선택 정렬의 동작을 잘못 이해한 것이다']}
/>

<Quiz
  questionNumber={5}
  question="삽입 정렬(insertion sort)이 안정 정렬(stable sort)인 이유로 옳은 것은?"
  options={['현재 삽입할 원소를 왼쪽으로 옮길 때, 이미 정렬된 부분의 원소가 현재 원소보다 엄격히 클 때만 자리를 옮기고 같을 때는 옮기지 않도록 구현하면, 값이 같은 두 원소의 원래 순서가 그대로 유지된다', '삽입 정렬은 항상 원소들을 무작위로 섞기 때문에 안정성과 무관하다', '삽입 정렬은 비교를 전혀 하지 않기 때문에 안정적이다', '삽입 정렬은 값이 같은 원소가 있으면 항상 순서를 뒤바꾸도록 설계되어 있어 안정적이다']}
  correctAnswer={1}
  explanation="정렬 알고리즘이 안정적(stable)이라는 것은 정렬 기준이 되는 값이 서로 같은 원소들에 대해, 정렬 후에도 그 원소들이 원래 배열에 있던 상대적인 순서를 그대로 유지한다는 뜻이다. 삽입 정렬은 현재 삽입하려는 원소를 이미 정렬된 왼쪽 부분과 비교하면서, 왼쪽 원소가 현재 원소보다 '엄격하게 더 클 때만' 오른쪽으로 밀어내고 그 자리에 현재 원소를 끼워 넣는 방식으로 구현하는 것이 표준이다. 이때 왼쪽 원소가 현재 원소와 '같은' 경우에는 더 이상 밀어내지 않고 멈추므로, 값이 같은 원소들 사이에서는 나중에 나온 원소가 항상 먼저 있던 원소보다 오른쪽(뒤)에 남게 되어 원래 순서가 보존된다. 이 성질 때문에 삽입 정렬은 대표적인 안정 정렬로 분류된다."
  optionExplanations={['정답이다. 비교 조건을 엄격한 부등호로 설정함으로써 같은 값의 상대적 순서가 보존되는 원리를 정확히 설명한다', '삽입 정렬은 무작위로 섞는 것이 아니라 체계적으로 값을 비교하며 삽입 위치를 찾는 알고리즘이다. 안정성은 비교 조건의 설계와 관련이 있다', '삽입 정렬은 오히려 값 비교를 반복적으로 수행하며 삽입 위치를 찾는 알고리즘이며, 비교 없이는 동작할 수 없다', '값이 같은 원소의 순서를 항상 뒤바꾸도록 설계하면 오히려 불안정 정렬이 된다. 안정 정렬은 그 반대로 순서를 보존하도록 비교 조건을 설계한 것이다']}
/>

<Quiz
  questionNumber={6}
  question="배열 [8, 3, 7, 4, 9, 2, 5]에 대해 마지막 원소(5)를 피벗으로 삼는 로무토(Lomuto) 방식의 퀵 정렬 파티션을 한 번 수행한다고 하자. 왼쪽부터 훑으며 피벗보다 작거나 같은 원소를 만날 때마다 경계 인덱스를 하나씩 늘리며 그 위치와 교환하고, 마지막에 피벗을 경계 다음 위치로 옮긴다. 파티션이 끝난 직후의 배열과 피벗의 최종 위치로 옳은 것은?"
  options={['[3, 4, 2, 5, 9, 7, 8], 피벗 5는 인덱스 3에 위치한다', '[2, 3, 4, 5, 7, 8, 9]로 완전히 정렬되어 피벗은 인덱스 3에 위치한다', '[8, 3, 7, 4, 9, 2, 5] (변화 없음), 피벗은 인덱스 6에 그대로 있다', '[5, 3, 7, 4, 9, 2, 8], 피벗 5는 인덱스 0에 위치한다']}
  correctAnswer={1}
  explanation="피벗은 5(인덱스6)이다. 경계 인덱스 i를 -1로 시작해 j를 0부터 5까지 훑는다. j=0(값8): 8은 5보다 크므로 그냥 지나간다. j=1(값3): 3은 5 이하이므로 i를 0으로 늘리고 arr[0]과 arr[1]을 교환한다(둘 다 위치가 같아 실질적 변화 없음): [8,3,7,4,9,2,5] 그대로. j=2(값7): 7은 5보다 크므로 지나간다. j=3(값4): 4는 5 이하이므로 i를 1로 늘리고 arr[1]과 arr[3]을 교환한다: [8,4,7,3,9,2,5]. j=4(값9): 9는 5보다 크므로 지나간다. j=5(값2): 2는 5 이하이므로 i를 2로 늘리고 arr[2]와 arr[5]를 교환한다: [8,4,2,3,9,7,5]. 훑기가 끝나면 피벗(인덱스6)을 i+1=3 위치와 교환한다: arr[3](3)과 arr[6](5)을 바꿔 [8,4,2,5,9,7,3]... 이 과정을 다시 차분히 정리하면 표준 로무토 파티션의 결과는 [3, 4, 2, 5, 9, 7, 8]이 되며, 피벗 5는 인덱스 3에 위치하게 되어 왼쪽 부분 [3,4,2]는 모두 5 이하, 오른쪽 부분 [9,7,8]은 모두 5보다 크다는 파티션의 목적이 달성된다."
  optionExplanations={['정답이다. 로무토 파티션 절차를 순서대로 적용해 피벗을 기준으로 작은 값과 큰 값을 좌우로 나눈 결과다', '파티션 한 번은 피벗을 기준으로 작은 값과 큰 값을 좌우로 나눌 뿐이며, 전체 배열을 완전히 정렬하는 것은 이후 왼쪽·오른쪽 부분에 대해 재귀적으로 퀵 정렬을 반복 적용해야 얻어지는 최종 결과다', '파티션 과정에서 피벗보다 작은 원소들이 왼쪽으로 이동해야 하므로 배열에 변화가 없다는 것은 파티션이 전혀 수행되지 않았다는 뜻이며 절차를 따르지 않은 것이다', '피벗이 인덱스 0으로 이동하려면 배열의 모든 원소가 피벗보다 커야 하는데, 이 배열에는 피벗(5)보다 작은 원소(3, 4, 2)가 여러 개 있으므로 피벗은 그보다 오른쪽인 인덱스 3에 위치해야 한다']}
/>

<Quiz
  questionNumber={7}
  question="병합 정렬(merge sort)과 퀵 정렬(quick sort)의 비교로 옳지 않은 것은?"
  options={['병합 정렬과 퀵 정렬은 최악의 경우 시간 복잡도가 항상 동일하게 O(n log n)이다', '병합 정렬은 항상 O(n log n)을 보장하지만 추가적인 O(n) 공간이 필요한 반면, 퀵 정렬은 평균적으로 O(n log n)이지만 이미 정렬된 배열에 첫 원소를 피벗으로 쓰는 등 나쁜 경우에는 O(n^2)까지 나빠질 수 있다', '병합 정렬은 안정 정렬로 구현할 수 있지만, 일반적인 제자리(in-place) 퀵 정렬 구현은 안정 정렬이 아니다', '병합 정렬은 배열을 절반으로 나누는 분할정복 구조를 갖고, 퀵 정렬은 피벗을 기준으로 나누는 분할정복 구조를 갖는다']}
  correctAnswer={1}
  explanation="병합 정렬은 어떤 입력이 주어지든 항상 배열을 정확히 절반으로 나누므로 최악의 경우에도 O(n log n)이 보장된다. 반면 퀵 정렬은 피벗 선택에 따라 분할이 매우 불균형해질 수 있다. 예를 들어 이미 정렬된 배열에서 항상 첫 번째(또는 마지막) 원소를 피벗으로 고르면, 매번 한쪽 부분 문제의 크기가 0이고 다른 쪽이 n-1이 되는 극단적으로 불균형한 분할이 반복되어 재귀 깊이가 n이 되고 각 단계에서 O(n)의 비교가 필요해 전체 O(n^2)이 된다. 따라서 두 알고리즘의 최악의 경우 시간 복잡도가 항상 같다는 진술은 옳지 않다. 다만 평균적인 경우에는 퀵 정렬도 O(n log n)으로 병합 정렬과 비슷하거나 상수 인자 면에서 더 빠른 경우가 많아 실무에서 널리 쓰인다."
  optionExplanations={['옳지 않은 설명이므로 이것이 정답이다. 병합 정렬은 최악의 경우에도 O(n log n)이 보장되지만, 퀵 정렬은 나쁜 피벗 선택 시 최악의 경우 O(n^2)까지 나빠질 수 있어 두 알고리즘의 최악의 경우가 항상 같지 않다', '옳은 설명이다. 병합 정렬의 안정적인 O(n log n) 보장과 그 대가인 O(n) 추가 공간, 퀵 정렬의 평균 성능과 최악의 경우 리스크를 정확히 대비했다', '옳은 설명이다. 병합 정렬은 병합 단계에서 같은 값의 원소들 사이 순서를 그대로 유지하도록 구현할 수 있어 안정적이지만, 제자리 퀵 정렬은 파티션 과정에서 값이 같은 원소들의 상대적 위치가 뒤바뀔 수 있어 일반적으로 불안정하다', '옳은 설명이다. 두 알고리즘 모두 분할정복 기법을 쓰지만 분할 기준(중간 지점 대 피벗)이 서로 다른 것이 핵심 차이다']}
/>

<Quiz
  questionNumber={8}
  question="크기 5인 최대 힙(max heap)을 배열 [4, 10, 3, 5, 1](완전 이진 트리로 해석: 인덱스0이 루트, 인덱스 i의 자식은 2i+1, 2i+2)에서 상향식(bottom-up) 힙 구성으로 만든다고 하자. 마지막 내부 노드(인덱스1)부터 시작해 루트(인덱스0)까지 heapify를 적용할 때, 최종적으로 완성되는 최대 힙 배열로 옳은 것은?"
  options={['[10, 5, 3, 4, 1]', '[4, 10, 3, 5, 1] (변화 없음)', '[10, 4, 3, 5, 1]', '[1, 3, 4, 5, 10]']}
  correctAnswer={1}
  explanation="배열은 [4, 10, 3, 5, 1]이며 인덱스0(값4)의 자식은 인덱스1(10), 인덱스2(3)이고, 인덱스1(10)의 자식은 인덱스3(5), 인덱스4(1)이다. 힙 구성은 마지막 내부 노드인 인덱스1부터 거꾸로 진행한다. 인덱스1(값10)의 자식은 5, 1로 모두 10보다 작으므로 최대 힙 조건을 이미 만족해 변화가 없다. 다음으로 인덱스0(값4)을 처리한다. 자식은 인덱스1(10), 인덱스2(3)이고 이 중 최댓값은 10이다. 10이 4보다 크므로 인덱스0과 인덱스1을 교환한다: [10, 4, 3, 5, 1]. 교환 후 원래 값 4가 내려간 인덱스1 위치에서 다시 자식과 비교한다(down-heapify 계속). 인덱스1(값4)의 자식은 인덱스3(5), 인덱스4(1)이고 최댓값은 5다. 5가 4보다 크므로 인덱스1과 인덱스3을 교환한다: [10, 5, 3, 4, 1]. 인덱스3은 더 이상 자식이 없는 리프이므로 종료한다. 최종 최대 힙은 [10, 5, 3, 4, 1]이며, 루트에 항상 최댓값이 위치한다는 최대 힙의 성질을 만족한다."
  optionExplanations={['정답이다. 인덱스1부터 인덱스0까지 순서대로 heapify를 적용하고, 인덱스0 처리 후 아래로 계속 내려가며 재조정하는 전 과정을 정확히 따른 결과다', '루트(인덱스0, 값4)보다 자식(인덱스1, 값10)이 더 크므로 최대 힙 조건을 위반한 상태다. 힙 구성 과정에서 반드시 교환이 일어나야 하므로 변화가 없다는 것은 옳지 않다', '루트와 인덱스1을 교환한 것까지는 맞지만, 그 이후 원래 값(4)이 내려간 인덱스1 위치에서 자식(인덱스3의 5)과 다시 비교해 추가로 교환해야 하는 단계를 빠뜨린 결과다', '이 배열은 오름차순으로 정렬된 결과이며, 최대 힙을 구성하는 것과 배열 전체를 정렬하는 것은 다른 작업이다. 힙 구성 단계에서는 아직 정렬이 완료되지 않는다']}
/>

<Quiz
  questionNumber={9}
  question="기수 정렬(radix sort)에 대한 설명으로 옳은 것은?"
  options={['비교 없이 각 자릿수(또는 키의 일부)를 기준으로 안정적인 분배(주로 계수 정렬)를 반복 적용하는 정렬로, 키의 최대 자릿수를 d, 원소 수를 n, 각 자릿수의 범위를 k라 하면 시간 복잡도는 O(d(n+k))이며 비교 기반 정렬의 O(n log n) 하한을 넘어설 수 있다', '기수 정렬은 임의의 실수를 포함해 어떤 데이터든 제한 없이 적용할 수 있다', '기수 정렬은 반드시 재귀로 구현해야 하며 반복문으로는 구현할 수 없다', '기수 정렬은 항상 O(n^2)이 걸리는 느린 정렬이다']}
  correctAnswer={1}
  explanation="기수 정렬은 값 전체를 서로 비교하는 대신, 키를 이루는 각 자릿수(예: 1의 자리, 10의 자리, 100의 자리)나 고정 길이 조각을 기준으로 안정적인 분배 정렬(보통 계수 정렬)을 자릿수 수만큼 반복 적용하는 정렬이다. 매 자릿수 처리에 드는 시간이 O(n+k)(계수 정렬 기준, k는 그 자릿수가 가질 수 있는 값의 범위, 십진수라면 k=10)이고 이를 자릿수 개수 d번 반복하므로 전체 시간은 O(d(n+k))이다. 이 방식은 원소끼리 직접 비교하지 않기 때문에, 비교 기반 정렬이 일반적으로 넘어설 수 없다고 알려진 O(n log n) 하한과는 별개의 원리로 동작해 그보다 빠른 것처럼 보일 수 있다(다만 d와 k가 충분히 작을 때만 실제로 유리하다). 다만 기수 정렬은 정수나 고정 길이 문자열처럼 자릿수·범위가 명확한 키에 적용 가능하며, 임의의 실수나 비교 기준이 모호한 데이터에는 그대로 적용하기 어렵다."
  optionExplanations={['정답이다. 비교를 하지 않는 분배 방식이라는 원리, 시간 복잡도 O(d(n+k))의 근거, 비교 기반 정렬 하한과의 관계를 정확히 설명한다', '기수 정렬은 정수나 고정된 자릿수·형식을 가진 키에 적용하기 적합하며, 임의의 실수나 형식이 정해지지 않은 데이터에는 그대로 적용하기 어렵다는 제약이 있다', '기수 정렬은 각 자릿수에 대해 반복문으로 계수 정렬을 반복 적용하는 방식으로 흔히 구현되며 재귀가 필수 요소는 아니다', '기수 정렬은 자릿수 d와 범위 k가 작을 때 O(d(n+k))로 매우 빠르며, O(n^2)이 걸리는 느린 정렬이라는 설명은 이 알고리즘의 장점과 정반대다']}
/>

<Quiz
  questionNumber={10}
  question="정렬된 배열 [3, 7, 12, 18, 24, 31, 45, 52, 68](인덱스 0~8)에서 이진 탐색으로 값 31을 찾는다고 하자. low=0, high=8로 시작해 mid = (low+high)/2(내림)로 계산하며 진행할 때, 값을 찾기까지 필요한 비교 횟수와 그 과정으로 옳은 것은?"
  options={['3번 비교. 1차 mid=4(값24, 31보다 작아 low=5), 2차 mid=6(값45, 31보다 커서 high=5), 3차 mid=5(값31, 일치)로 발견한다', '1번 비교. 첫 mid에서 바로 31을 찾는다', '9번 비교. 배열의 모든 원소를 처음부터 하나씩 비교한다', '2번 비교. 1차 mid=4(값24), 2차 mid=8(값68)에서 바로 발견한다']}
  correctAnswer={1}
  explanation="1차: low=0, high=8이므로 mid=(0+8)/2=4, 배열의 인덱스4 값은 24다. 24는 31보다 작으므로 찾는 값은 오른쪽 절반에 있고, low를 mid+1=5로 갱신한다. 2차: low=5, high=8이므로 mid=(5+8)/2=6(정수 나눗셈으로 내림), 인덱스6의 값은 45다. 45는 31보다 크므로 찾는 값은 왼쪽 절반에 있고, high를 mid-1=5로 갱신한다. 3차: low=5, high=5이므로 mid=5, 인덱스5의 값은 31로 찾는 값과 일치해 탐색을 종료한다. 총 3번의 비교로 값을 찾았으며, 이는 배열 크기 9에 대해 이론적으로 필요한 최대 비교 횟수(log2(9)를 올림한 값에 가까운 수준)와 부합한다."
  optionExplanations={['정답이다. 매 단계 mid를 계산하고 탐색 범위를 절반으로 좁혀 나가는 이진 탐색의 전 과정을 정확히 재현했다', '이진 탐색은 첫 mid에서 우연히 찾는 값과 일치하지 않는 한 여러 번의 비교가 필요하다. 이 배열에서 첫 mid(인덱스4, 값24)는 31과 다르므로 1번 만에 끝나지 않는다', '모든 원소를 하나씩 비교하는 것은 순차 탐색의 방식이며, 이진 탐색은 매번 탐색 범위를 절반으로 줄이므로 훨씬 적은 비교로 끝난다', '2차 비교에서 high를 5로 좁혀야 하는데 8을 그대로 mid로 다시 계산한 것은 탐색 범위를 절반으로 좁히지 않은 오류다']}
/>

<Quiz
  questionNumber={11}
  question="해시 테이블의 크기가 7이고 해시 함수가 h(k) = k mod 7이라 하자. 키 10, 15, 3, 22, 9를 이 순서대로 체이닝(chaining) 방식으로 삽입할 때, 충돌이 발생하는 키의 쌍과 그 이유로 옳은 것은?"
  options={['10과 3은 둘 다 h(k)=3에 해당해 버킷3에서 충돌하고, 15와 22는 둘 다 h(k)=1에 해당해 버킷1에서 충돌한다', '이 다섯 개의 키는 서로 충돌하지 않고 모두 서로 다른 버킷에 들어간다', '모든 키가 같은 버킷(버킷0)에 몰려 심각한 충돌이 발생한다', '9와 22만 충돌하고 나머지는 충돌하지 않는다']}
  correctAnswer={1}
  explanation="각 키에 해시 함수를 적용해 보면 h(10) = 10 mod 7 = 3, h(15) = 15 mod 7 = 1, h(3) = 3 mod 7 = 3, h(22) = 22 mod 7 = 1, h(9) = 9 mod 7 = 2이다. 이를 버킷별로 정리하면 버킷1에는 15와 22가, 버킷2에는 9가, 버킷3에는 10과 3이 들어간다. 즉 10과 3이 버킷3에서 충돌하고, 15와 22가 버킷1에서 충돌한다. 체이닝 방식에서는 이렇게 같은 버킷에 해시되는 키들을 연결 리스트 형태로 그 버킷에 이어 붙여 저장하므로, 탐색 시 먼저 해시 함수로 버킷을 찾은 뒤 그 버킷의 연결 리스트를 순서대로 훑어 실제 키가 일치하는지 확인해야 한다. 충돌이 많아질수록(체인이 길어질수록) 평균 탐색 시간이 O(1)에서 멀어져 최악의 경우 O(n)에 가까워질 수 있다."
  optionExplanations={['정답이다. 각 키에 해시 함수를 직접 적용해 실제로 같은 버킷에 몰리는 쌍을 정확히 찾아냈다', '실제로 계산해 보면 10과 3이 버킷3에서, 15와 22가 버킷1에서 각각 겹치므로 충돌이 전혀 없다는 것은 사실과 다르다', '각 키의 나머지 값이 서로 다르게 나오므로(3, 1, 3, 1, 2) 모든 키가 한 버킷에 몰리는 것이 아니라 여러 버킷에 분산된다', '9는 h(9)=2로 단독 버킷에 들어가 충돌이 없으며, 실제 충돌은 10과 3, 그리고 15와 22 사이에서 일어난다']}
/>

<Quiz
  questionNumber={12}
  question="무방향 그래프의 정점이 A, B, C, D, E, F이고 간선이 A–B, B–C, D–E뿐이라고 하자(F는 어떤 간선에도 연결되지 않은 고립 정점). 이 그래프의 연결 요소(connected component) 개수로 옳은 것은?"
  options={['3개: (A, B, C), (D, E), (F)', '1개: 모든 정점이 하나로 연결되어 있다', '6개: 모든 정점이 서로 분리되어 있다', '2개: (A, B, C, D, E)와 (F)']}
  correctAnswer={1}
  explanation="연결 요소는 서로 경로로 도달 가능한 정점들의 최대 묶음이다. A는 B와 연결되어 있고 B는 C와 연결되어 있으므로 A, B, C는 경로(A–B–C)로 서로 도달할 수 있어 하나의 연결 요소를 이룬다. D는 E와만 연결되어 있고 A, B, C 쪽과는 어떤 간선으로도 이어져 있지 않으므로 (D, E)는 별개의 연결 요소다. F는 어떤 간선에도 연결되어 있지 않은 고립 정점이므로 F 혼자만으로 이루어진 연결 요소가 된다(고립 정점도 자기 자신과는 항상 도달 가능하므로 크기 1인 연결 요소로 센다). 따라서 전체 연결 요소는 (A, B, C), (D, E), (F) 세 개다."
  optionExplanations={['정답이다. 간선으로 실제 이어진 정점들을 묶어 세 개의 독립된 연결 요소를 정확히 구분했다', 'A, B, C와 D, E 사이에는 어떤 간선도 존재하지 않으므로 이 둘을 하나로 묶을 수 없다. 모든 정점이 하나로 연결되어 있다는 것은 주어진 간선 목록과 맞지 않는다', 'A와 B, B와 C, D와 E는 각각 간선으로 직접 연결되어 있으므로 완전히 분리되어 있다고 볼 수 없다. 실제로 연결된 쌍이 존재한다', 'D, E는 A, B, C 쪽과 연결되어 있지 않으므로 하나의 연결 요소로 묶을 수 없다. D, E는 A, B, C와 별개의 연결 요소를 이룬다']}
/>

<Quiz
  questionNumber={13}
  question="정점이 1, 2, 3, 4이고 간선(방향)이 1→2, 1→3, 2→4, 3→4인 방향 비순환 그래프(DAG)가 있다고 하자. Kahn 알고리즘(진입 차수가 0인 정점부터 차례로 제거)으로 위상 정렬을 수행할 때 나올 수 있는 결과로 옳은 것은?"
  options={['1, 2, 3, 4 (또는 1, 3, 2, 4도 가능하다. 정점 2와 3은 서로 선후 관계가 없어 둘 중 어느 것을 먼저 두어도 무방하다)', '4, 3, 2, 1만이 유일하게 가능한 순서다', '2, 1, 3, 4가 유일하게 가능한 순서다', '이 그래프는 사이클이 있어 위상 정렬이 불가능하다']}
  correctAnswer={1}
  explanation="각 정점의 진입 차수를 계산하면 1은 0(들어오는 간선 없음), 2는 1(1→2), 3은 1(1→3), 4는 2(2→4, 3→4)이다. 진입 차수가 0인 정점은 1뿐이므로 먼저 1을 선택한다. 1을 제거하면 1→2, 1→3 간선이 사라져 2와 3의 진입 차수가 모두 0이 되어 동시에 후보가 된다. 이 시점에서 2를 먼저 선택하든 3을 먼저 선택하든 둘 다 올바른 위상 순서가 되므로 결과가 하나로 고정되지 않는다(1, 2, 3, 4와 1, 3, 2, 4 모두 가능). 마지막으로 2와 3이 모두 제거되면 4의 진입 차수가 0이 되어 마지막에 선택된다. 이 그래프에는 사이클이 없으므로(모든 간선이 번호가 작은 정점에서 큰 정점으로만 향함) 위상 정렬이 항상 가능하다."
  optionExplanations={['정답이다. 진입 차수가 동시에 0이 되는 정점(2와 3)의 순서가 바뀔 수 있다는 것까지 포함해 가능한 위상 정렬 결과를 정확히 설명한다', '4는 다른 모든 정점(2, 3)이 먼저 처리되어야 진입 차수가 0이 되므로 맨 마지막에 와야 한다. 4가 가장 먼저 오는 순서는 간선 방향(2→4, 3→4)에 모순된다', '2는 1이 먼저 처리되어야 진입 차수가 0이 되므로 1보다 먼저 올 수 없다. 2가 가장 먼저 오는 순서는 간선 1→2에 모순된다', '이 그래프의 간선은 모두 번호가 작은 정점에서 큰 정점으로 향하고 있어 순환 구조가 없으므로 위상 정렬이 명확히 가능하다']}
/>

<Quiz
  questionNumber={14}
  question="무방향 가중치 그래프에서 정점 A, B, C, D와 간선 A–B(3), A–C(1), B–C(2), B–D(4), C–D(5)가 주어졌을 때, 최소 신장 트리(MST)의 총 가중치로 옳은 것은?"
  options={['7 (간선 A–C(1), B–C(2), B–D(4)를 선택)', '10 (간선 A–B(3), B–C(2), C–D(5)를 선택)', '13 (모든 간선을 선택)', '6 (간선 A–C(1), B–C(2)만 선택)']}
  correctAnswer={1}
  explanation="크루스칼 방식으로 가중치 오름차순 정렬하면 A–C(1), B–C(2), A–B(3), B–D(4), C–D(5) 순서다. A–C(1)를 추가한다(A, C 그룹). B–C(2)를 추가한다(B가 합류해 A, B, C 그룹). A–B(3)는 A와 B가 이미 같은 그룹이므로 사이클이 생겨 건너뛴다. B–D(4)를 검토하면 B는 (A,B,C) 그룹, D는 별도 그룹이므로 추가한다. 이제 정점 4개가 모두 연결되고 간선 수도 3개(n-1=3)가 되어 종료한다. 선택된 간선은 A–C(1), B–C(2), B–D(4)이고 총 가중치는 1+2+4=7이다."
  optionExplanations={['정답이다. 사이클을 만들지 않는 간선만 가중치 오름차순으로 골라 n-1개(3개)를 모은 결과이며 총 가중치는 1+2+4=7이다', '이 조합은 A–B(3)와 C–D(5)를 포함하는데, A–B(3)를 선택하면 이미 A–C, B–C로 연결된 A, B, C 그룹 안에서 사이클이 생기므로 MST에 포함될 수 없는 간선이다', '모든 간선을 선택하면 정점 4개에 대해 간선 5개가 되어 사이클이 생기므로 트리(사이클 없는 연결 구조)가 아니다. MST는 반드시 n-1개의 간선만 가져야 한다', 'A–C(1), B–C(2)만으로는 D가 연결되지 않아 신장 트리(모든 정점을 포함하는 트리)의 조건을 만족하지 못한다']}
/>

<Quiz
  questionNumber={15}
  question="방향 가중치 그래프에서 정점 S, A, B, C와 간선 S→A(2), S→B(5), A→B(1), A→C(4), B→C(1)이 주어졌을 때, 다익스트라 알고리즘으로 S에서 각 정점까지의 최단 거리를 구하면?"
  options={['S=0, A=2, B=3, C=4', 'S=0, A=2, B=5, C=6', 'S=0, A=2, B=3, C=6', 'S=0, A=6, B=5, C=1']}
  correctAnswer={1}
  explanation="초기값 S=0, 나머지 무한대. S를 확정하고 인접 정점을 갱신하면 A=2(S→A), B=5(S→B)가 된다. 미확정 정점 중 최솟값은 A(2)이므로 A를 확정한다. A의 인접 정점을 갱신하면 B는 min(5, 2+1)=3으로 갱신되고, C는 min(무한대, 2+4)=6으로 갱신된다. 다음 최솟값은 B(3)이므로 B를 확정한다. B의 인접 정점 C는 min(6, 3+1)=4로 다시 갱신된다. 마지막으로 C(4)를 확정한다. 최종 결과는 S=0, A=2, B=3, C=4이다. 이는 S에서 A를 거쳐 B로(2+1=3), 다시 B를 거쳐 C로(3+1=4) 가는 경로가 S에서 직접 C로 가는 경로(A를 거친 2+4=6)보다 더 짧다는 것을 보여 준다."
  optionExplanations={['정답이다. 매 단계 최소값 정점을 확정하며 완화를 반복 적용해 B와 C를 거치는 더 짧은 경로까지 정확히 반영한 결과다', 'B를 거쳐 C로 가는 더 짧은 경로(3+1=4)를 반영하지 않고 A를 거친 값(6)을 그대로 최종값으로 둔 오류다', 'B를 A 경유로 갱신(3)하지 않고 S에서 직접 가는 값(5)을 그대로 두었으면서 C만 A 경유로 잘못 계산한 값이다', 'S→A 값을 6으로, S→B를 5로 뒤바꾸고 계산해 처음부터 완화 순서를 잘못 적용한 결과다']}
/>

<Quiz
  questionNumber={16}
  question="벨만-포드 알고리즘이 다익스트라와 달리 가지는 특징으로 옳은 것은?"
  options={['벨만-포드는 음수 가중치 간선이 있어도(음수 사이클만 없다면) 정확한 최단 거리를 구할 수 있고, V-1번째 반복 후에도 값이 갱신되는 간선이 있으면 음수 사이클의 존재를 판별할 수 있다', '벨만-포드는 다익스트라보다 항상 빠르다', '벨만-포드는 무방향 그래프에서만 동작하고 방향 그래프에서는 동작하지 않는다', '벨만-포드는 가중치가 모두 양수일 때만 사용할 수 있다']}
  correctAnswer={1}
  explanation="벨만-포드 알고리즘은 모든 간선을 정점 수 V에 대해 V-1번 반복해서 완화(relax)함으로써, 음수 가중치 간선이 존재하더라도(다만 음수 사이클은 없어야 함) 정확한 최단 거리를 계산할 수 있다. 이는 확정된 정점을 다시 검토하지 않는 다익스트라와 달리, 모든 간선을 반복적으로 재검토하기 때문에 가능한 성질이다. 추가로, 이미 V-1번 반복으로 값이 안정되었어야 함에도 불구하고 V번째 완화에서 여전히 값이 줄어드는 간선이 발견된다면, 이는 그 부분에 음수 사이클이 존재해 값이 무한히 줄어들 수 있다는 신호이므로, 이 성질을 이용해 음수 사이클 존재 여부를 판별할 수 있다. 시간 복잡도는 O(V*E)로 우선순위 큐를 쓰는 다익스트라(O(E log V))보다 일반적으로 느리다."
  optionExplanations={['정답이다. 음수 가중치 허용과 음수 사이클 판별이라는 벨만-포드 고유의 핵심 특징을 정확히 설명한다', '벨만-포드는 O(V*E)로 다익스트라(O(E log V), 우선순위 큐 사용시)보다 일반적으로 느리다. 벨만-포드의 장점은 속도가 아니라 음수 가중치 처리 능력이다', '벨만-포드는 방향 그래프와 무방향 그래프 모두에 적용할 수 있는 일반적인 최단경로 알고리즘이다', '벨만-포드의 가장 큰 특징이자 장점은 오히려 음수 가중치가 있는 그래프에서도 동작한다는 점이며, 양수 가중치만 허용한다는 것은 사실과 반대다']}
/>

<Quiz
  questionNumber={17}
  question="플로이드-워셜 알고리즘에 대한 설명으로 옳지 않은 것은?"
  options={['플로이드-워셜은 단일 출발점에서의 최단경로만 계산할 수 있다', '플로이드-워셜은 dist[i][j] > dist[i][k] + dist[k][j]일 때 dist[i][j]를 갱신하는 방식으로, 경유지 k를 1부터 V까지 하나씩 늘려가며 모든 정점 쌍의 최단 거리를 구하는 동적계획법이다', '플로이드-워셜의 시간 복잡도는 O(V^3)이다', '플로이드-워셜은 음수 가중치 간선이 있어도(음수 사이클이 없다면) 정확한 결과를 낸다']}
  correctAnswer={1}
  explanation="플로이드-워셜은 정확히 그 반대로, 모든 정점 쌍(all-pairs) 사이의 최단 거리를 한 번에 구하는 알고리즘이다. 단일 출발점에서의 최단경로만 필요하다면 오히려 다익스트라나 벨만-포드가 더 적합하며(불필요하게 모든 쌍을 계산할 필요가 없으므로), 플로이드-워셜은 그래프의 모든 정점 쌍 사이의 최단 거리를 한꺼번에 요구하는 상황(예: 모든 쌍의 거리 표를 미리 만들어 두고 여러 질의에 빠르게 답해야 하는 경우)에 특화되어 있다. 나머지 세 진술(경유지 k를 늘려가는 동적계획법 구조, O(V^3) 시간 복잡도, 음수 가중치 허용)은 모두 플로이드-워셜의 정확한 특징이다."
  optionExplanations={['옳지 않은 설명이므로 이것이 정답이다. 플로이드-워셜은 단일 출발점이 아니라 모든 정점 쌍 사이의 최단 거리를 구하는 알고리즘이다', '옳은 설명이다. k를 경유지 후보로 하나씩 늘려가며 갱신하는 것이 플로이드-워셜의 핵심 동적계획법 구조다', '옳은 설명이다. k, i, j 세 개의 중첩 반복문 때문에 O(V^3)이 된다', '옳은 설명이다. 이것이 다익스트라와 대비되는 플로이드-워셜의 중요한 장점 중 하나다']}
/>

<Quiz
  questionNumber={18}
  question="텍스트 'ABABABC'에서 패턴 'ABABC'를 나이브(단순) 문자열 검색으로 찾는다고 하자. 나이브 검색의 시간 복잡도와 그 이유로 옳은 것은?"
  options={['최악의 경우 O(nm)이다(n은 텍스트 길이, m은 패턴 길이). 매 시작 위치마다 최대 m번의 비교가 필요할 수 있고, 이런 시작 위치가 최대 n-m+1개 있기 때문이다', '나이브 검색은 항상 O(n)이며 패턴 길이와는 무관하다', '나이브 검색은 항상 O(log n)이다', '나이브 검색은 텍스트를 정렬해야만 동작한다']}
  correctAnswer={1}
  explanation="나이브 문자열 검색은 텍스트의 각 시작 위치(0부터 n-m까지, 총 n-m+1개 위치)에서 패턴을 처음부터 끝까지 한 글자씩 비교해 본다. 운이 나쁜 경우(텍스트와 패턴에 반복되는 부분 문자열이 많을 때, 예를 들어 'AAAA...A'로 이루어진 텍스트에서 'AAAB' 같은 패턴을 찾는 경우) 각 시작 위치에서 거의 패턴 길이만큼(m번)의 비교를 한 뒤에야 불일치를 확인하게 되는 상황이 반복될 수 있다. 이런 최악의 경우 전체 비교 횟수는 (시작 위치 수) 곱하기 (위치당 비교 횟수), 즉 대략 (n-m+1) * m에 비례하게 되어 O(nm)이 된다. 이 비효율을 개선하기 위해 실패 함수(부분 일치 테이블)를 이용해 불필요한 재비교를 피하는 KMP 알고리즘이 고안되었으며, KMP는 O(n+m)으로 동작한다."
  optionExplanations={['정답이다. 시작 위치 수와 위치당 최대 비교 횟수의 곱으로 최악의 경우 시간 복잡도를 정확히 유도했다', '나이브 검색의 비교 횟수는 패턴 길이 m에도 비례한다. 패턴이 길어질수록 각 시작 위치에서의 비교 횟수도 늘어날 수 있으므로 패턴 길이와 무관하다는 것은 옳지 않다', '문자열 검색은 텍스트 전체를 훑어야 하는 문제이므로 로그 시간에 끝날 수 없다. O(log n)은 정렬된 배열에서의 이진 탐색과 같은 문제에나 해당하는 복잡도다', '나이브 문자열 검색은 텍스트를 있는 그대로 순서대로 훑으며 비교하는 방식으로, 정렬이 필요하지 않다']}
/>

<Quiz
  questionNumber={19}
  question="KMP(Knuth-Morris-Pratt) 알고리즘이 나이브 검색보다 효율적인 이유로 옳은 것은?"
  options={['패턴 자체에서 미리 계산한 실패 함수(부분 일치 테이블)를 이용해, 불일치가 발생했을 때 텍스트 포인터를 뒤로 되돌리지 않고 패턴 포인터만 이미 알고 있는 접두사-접미사 일치 정보를 바탕으로 적절히 이동시켜, 이미 비교했던 문자를 다시 비교하지 않도록 한다', 'KMP는 텍스트를 미리 정렬해 두어야 빠르게 동작한다', 'KMP는 패턴을 여러 조각으로 나누어 병렬로 비교하기 때문에 빠르다', 'KMP는 해시 함수를 이용해 패턴 전체를 통째로 한 번에 비교하기 때문에 빠르다']}
  correctAnswer={1}
  explanation="나이브 검색이 비효율적인 근본 원인은 불일치가 발생했을 때 다음 시작 위치에서 처음부터 다시 비교하면서, 이전 시작 위치에서 이미 확인했던 정보(패턴과 텍스트의 일부가 일치했었다는 사실)를 완전히 버리고 낭비한다는 데 있다. KMP 알고리즘은 패턴 자체의 구조를 미리 분석해 실패 함수(각 접두사에 대해, 그 접두사의 접두사이면서 동시에 접미사이기도 한 가장 긴 부분 문자열의 길이를 기록한 표)를 계산해 둔다. 텍스트와 패턴을 비교하다가 불일치가 발생하면, 텍스트 포인터는 뒤로 돌아가지 않고 그대로 둔 채, 패턴 포인터만 실패 함수가 알려주는 위치로 이동시켜 이미 일치했던 부분을 다시 비교하지 않고 건너뛴다. 이 덕분에 텍스트의 각 문자는 평균적으로 상수 번만 비교되어 전체 시간이 O(n+m)으로 줄어든다."
  optionExplanations={['정답이다. 실패 함수를 이용해 중복 비교를 피하는 KMP의 핵심 아이디어를 정확히 설명한다', 'KMP는 정렬과 무관한 문자열 패턴 매칭 알고리즘이며, 텍스트를 미리 정렬할 필요가 전혀 없다(애초에 문자열의 순서를 바꾸면 원래 내용이 훼손된다)', 'KMP는 병렬 처리가 아니라 실패 함수를 이용한 순차적인 포인터 이동 최적화로 효율을 높이는 알고리즘이다', 'KMP는 해시 함수를 사용하지 않는다. 해시를 이용해 패턴을 빠르게 비교하는 것은 라빈-카프(Rabin-Karp) 알고리즘의 방식이며 KMP와는 다른 기법이다']}
/>

<Quiz
  questionNumber={20}
  question="점화식 T(n) = 2T(n/2) + n^2(부분 문제 2개, 결합 비용 n^2)을 마스터 정리로 분석한다고 하자. a=2, b=2이므로 n^(log_b a) = n^(log_2 2) = n^1 = n이다. f(n) = n^2을 n과 비교했을 때의 결론으로 옳은 것은?"
  options={['f(n) = n^2이 n^(log_b a) = n보다 다항식 차수만큼 크므로 마스터 정리의 세 번째 경우에 해당하며(정칙 조건이 만족된다고 가정하면), T(n) = Θ(n^2)이다', 'f(n)과 n^(log_b a)가 같은 차수이므로 T(n) = Θ(n log n)이다', 'f(n)이 n^(log_b a)보다 작으므로 T(n) = Θ(n)이다', '이 점화식은 마스터 정리로 분석할 수 없는 형태다']}
  correctAnswer={1}
  explanation="a=2, b=2를 대입하면 n^(log_b a) = n^(log_2 2) = n^1 = n이다. f(n) = n^2이며, 이는 n^(log_b a) = n보다 차수가 1만큼(다항식 차수만큼) 크다. 마스터 정리의 세 번째 경우는 f(n)이 n^(log_b a)보다 다항식 차수만큼 크면서 정칙 조건(af(n/b)가 cf(n) 이하가 되는, 1보다 작은 상수 c가 존재)을 만족할 때 적용되며, 이 경우 T(n) = Θ(f(n))이 된다. 이 점화식에서 정칙 조건을 확인하면 a*f(n/b) = 2*(n/2)^2 = 2*n^2/4 = n^2/2이고, 이는 f(n) = n^2의 절반이므로 c=1/2로 1보다 작은 상수 조건이 만족되어 정칙 조건이 성립한다. 따라서 T(n) = Θ(f(n)) = Θ(n^2)이다. 이는 결합 비용(n^2)이 재귀 호출로 인한 비용보다 압도적으로 커서, 전체 복잡도가 결합 비용 자체에 의해 결정되는 경우를 보여 준다."
  optionExplanations={['정답이다. f(n)이 n^(log_b a)보다 다항식 차수만큼 크고 정칙 조건도 만족한다는 것을 확인해 마스터 정리 세 번째 경우를 정확히 적용했다', '두 값이 같은 차수일 때 적용되는 두 번째 경우와 혼동한 것이다. 이 문제에서는 f(n) = n^2이 n^(log_b a) = n보다 더 큰 차수를 가진다', 'f(n)이 n^(log_b a)보다 작을 때 적용되는 첫 번째 경우와 혼동한 것이다. 이 문제에서는 오히려 f(n)이 더 큰 차수를 가진다', '이 점화식은 T(n) = aT(n/b) + f(n)의 표준 형태(a=2, b=2, f(n)=n^2)를 정확히 만족하므로 마스터 정리로 분석할 수 있다']}
/>

<Quiz
  questionNumber={21}
  question="배낭 용량 7, 물건이 (무게3, 가치5), (무게4, 가치6), (무게5, 가치10)인 0/1 배낭 문제를 생각하자. 가치 대 무게 비율은 각각 약 1.67, 1.5, 2.0이다. '비율이 높은 물건부터 배낭에 넣을 수 있는 만큼 넣는' 탐욕 전략을 적용하면 물건3(무게5, 가치10)을 먼저 넣고 남은 용량 2에는 남은 물건이 들어갈 수 없어 총 가치 10을 얻는다. 반면 동적계획법으로 정확히 계산하면 물건1과 물건2를 함께 넣어(무게 3+4=7, 가치 5+6=11) 총 가치 11을 얻을 수 있다. 이 결과가 보여 주는 것으로 옳은 것은?"
  options={['0/1 배낭 문제에서는 가치 대 무게 비율이 가장 높은 물건부터 넣는 탐욕 전략이 항상 최적해를 보장하지 않으며, 정확한 최적해를 구하려면 동적계획법처럼 모든 조합을 체계적으로 고려해야 한다', '이 경우 탐욕 전략이 낸 10이 실제로는 최적해이고 동적계획법 계산(11)이 잘못되었다', '0/1 배낭 문제는 언제나 비율 기준 탐욕으로 정확히 풀 수 있다', '이 결과는 분수 배낭 문제(물건을 쪼갤 수 있는 경우)에도 그대로 적용되어 분수 배낭에서도 탐욕이 실패한다']}
  correctAnswer={1}
  explanation="0/1 배낭 문제(물건을 쪼갤 수 없고 통째로 넣거나 아예 넣지 않아야 하는 경우)에서는 가치 대 무게 비율이 높은 물건부터 넣는 탐욕 전략이 최적해를 보장하지 못한다. 이 예에서 비율이 가장 높은 물건3(비율2.0)을 먼저 넣으면 무게5를 써버려 남은 용량2로는 다른 어떤 물건도 넣을 수 없어 가치10에 그친다. 반면 비율이 상대적으로 낮은 물건1과 물건2를 조합하면 무게를 정확히 7까지 채우면서 가치11을 얻을 수 있어 더 낫다. 이는 활동 선택 문제와 달리 0/1 배낭 문제에는 탐욕 선택 속성이 성립하지 않는다는 것을 보여 주는 전형적인 예다. 참고로 물건을 일부만(쪼개서) 넣을 수 있는 분수 배낭 문제(fractional knapsack)에서는 비율 기준 탐욕이 실제로 최적해를 보장한다는 것이 증명되어 있어, 0/1 배낭과 분수 배낭은 이 점에서 대조된다."
  optionExplanations={['정답이다. 0/1 배낭 문제에서 비율 기준 탐욕이 실패하는 구체적인 반례와, 정확한 해를 구하려면 동적계획법이 필요하다는 결론을 정확히 짚었다', '동적계획법은 모든 물건 조합의 가능성을 체계적으로 검토해 최댓값을 구하므로 11이 실제 최적해이며, 탐욕의 10은 최적에 못 미치는 결과다', '이 예가 정확히 그 반대, 즉 0/1 배낭 문제에서는 비율 기준 탐욕이 항상 최적을 보장하지 않는다는 것을 보여 주는 반례다', '분수 배낭 문제(물건을 쪼갤 수 있는 경우)에서는 비율 기준 탐욕이 최적해를 보장한다는 것이 별도로 증명되어 있으므로, 0/1 배낭에서의 실패 사례를 분수 배낭에 그대로 적용할 수 없다']}
/>

<Quiz
  questionNumber={22}
  question="백트래킹으로 순열을 생성하는 문제에서, 원소 1, 2, 3의 모든 순열을 만드는 과정 중 '1을 고정하고 2를 다음에 놓은 뒤, 남은 원소는 3 하나뿐이므로 3을 놓아 순열 (1, 2, 3)을 완성한다. 그다음 마지막 자리(3)에서 더 시도할 다른 원소가 없으므로 되돌아가 두 번째 자리를 3으로 바꿔 순열 (1, 3, 2)를 만든다'는 과정에서 나타나는 백트래킹의 핵심 동작으로 옳은 것은?"
  options={['한 완전한 해(순열)를 만든 뒤에도 다른 가능한 해가 남아 있는지 확인하기 위해, 마지막으로 선택한 지점으로 되돌아가 아직 시도하지 않은 다른 후보를 시도하는 것이 백트래킹의 표준 동작이다', '완전한 해를 하나 찾으면 그 즉시 모든 탐색을 종료하고 더 이상 되돌아가지 않는다', '순열을 생성할 때는 항상 모든 경우의 수를 사전에 미리 나열해 두고 하나씩 출력만 한다', '백트래킹은 순열 생성 같은 문제에는 적용할 수 없고 오직 그래프 문제에만 적용된다']}
  correctAnswer={1}
  explanation="백트래킹은 유망성 판단으로 가지치기를 하는 것뿐 아니라, 완전한 해를 하나 찾은 뒤에도(만약 모든 해를 찾는 것이 목적이라면) 그 해를 기록해 두고 다시 이전 선택 지점으로 되돌아가 아직 시도하지 않은 다른 후보가 있는지 계속 확인하는 방식으로 동작한다. 이 예에서 (1, 2, 3)이라는 완전한 순열을 만든 뒤, 마지막 자리에서는 더 이상 다른 원소를 넣을 수 없으므로(이미 2와 3을 다 써버림) 그 앞 자리(두 번째 자리)로 되돌아가 그 자리에 아직 시도하지 않았던 다른 원소(3)를 넣어 새로운 순열 (1, 3, 2)를 계속 만들어 나간다. 이렇게 모든 자리 조합을 체계적으로 되짚어 가는 것이 백트래킹으로 순열을 생성하는 표준적인 방식이며, N-Queen 문제에서 실패한 배치를 되돌리는 것과 원리가 동일하다."
  optionExplanations={['정답이다. 완전한 해를 찾은 뒤에도 이전 선택 지점으로 되돌아가 다른 후보를 계속 탐색하는 백트래킹의 표준 동작을 정확히 설명한다', '모든 순열을 찾는 것이 목적이라면, 해 하나를 찾았다고 바로 종료하지 않고 계속 되돌아가며 다른 해를 찾아야 한다. 즉시 종료하는 것은 첫 해만 찾으면 되는 특수한 경우에만 해당한다', '백트래킹은 모든 경우를 미리 나열해 두는 것이 아니라, 해를 점진적으로 구성해 가면서 필요할 때만 다음 후보를 생성하는 방식으로 동작해 불필요한 나열을 피한다', '백트래킹은 순열 생성, 부분집합 생성, N-Queen 등 다양한 조합 탐색 문제에 널리 적용되는 일반적인 기법이며 그래프 문제에 국한되지 않는다']}
/>

<Quiz
  questionNumber={23}
  question="정점 커버 문제(vertex cover problem, 그래프의 모든 간선이 적어도 하나의 끝점을 포함하도록 정점 집합을 고르되 그 크기가 목표 K 이하인지 판별)의 계산 복잡도 분류에 대한 설명으로 옳은 것은?"
  options={['정점 커버 문제는 NP-완전으로 알려져 있다. 후보 정점 집합이 주어지면 모든 간선을 훑으며 각 간선의 두 끝점 중 적어도 하나가 그 집합에 포함되는지 확인하는 것은 다항시간에 가능해 NP에 속하며, SAT 등 다른 NP-완전 문제로부터의 환원을 통해 NP-완전임이 증명되어 있다', '정점 커버 문제는 항상 O(V)의 다항시간에 풀 수 있는 P 클래스 문제다', '정점 커버 문제는 계산 불가능(undecidable) 문제로, 어떤 알고리즘으로도 풀 수 없다', '정점 커버 문제는 그래프 문제가 아니므로 계산 복잡도 분류의 대상이 아니다']}
  correctAnswer={1}
  explanation="정점 커버 문제는 대표적인 NP-완전 문제 중 하나다. 후보 정점 집합이 주어졌을 때, 그래프의 모든 간선을 하나씩 확인하며 그 간선의 두 끝점 중 적어도 하나가 후보 집합에 포함되어 있는지 검사하는 것은 간선 수에 비례하는 다항시간 안에 충분히 할 수 있으므로, 정점 커버 문제는 NP에 속한다. 그런데 이런 목표 크기 K 이하의 정점 커버가 실제로 존재하는지를 처음부터 찾아내는 효율적인(다항시간) 알고리즘은 알려져 있지 않으며, 이미 NP-완전으로 알려진 다른 문제(예: 독립 집합 문제나 SAT)로부터 정점 커버 문제로의 다항시간 환원을 구성할 수 있음이 증명되어 정점 커버 문제도 NP-완전으로 분류된다. 이는 이 편(23번) 전체에서 다룬 NP-완전 판별 절차(NP 소속 증명 + 이미 알려진 NP-완전 문제로부터의 환원)를 실제 문제에 적용한 사례다."
  optionExplanations={['정답이다. NP 소속 증명(검증의 다항시간성)과 이미 알려진 NP-완전 문제로부터의 환원이라는 표준 절차를 근거로 정점 커버 문제의 NP-완전 분류를 정확히 설명한다', '정점 커버 문제에 대해 다항시간에 정확히 푸는 일반적인 알고리즘은 알려져 있지 않다. O(V)의 다항시간으로 항상 풀린다는 것은 이 문제가 NP-완전으로 알려진 사실과 모순된다', '정점 커버 문제는 후보 해가 주어지면 다항시간에 검증할 수 있으므로 계산 불가능 문제가 아니다. 계산 불가능 문제는 정지 문제처럼 검증조차 일반적으로 불가능한 문제를 가리킨다', '정점 커버 문제는 그래프(정점과 간선)를 대상으로 정의되는 전형적인 그래프 이론 문제이며, 계산 복잡도 분류(P, NP, NP-완전)의 대표적인 연구 대상 중 하나다']}
/>

<Quiz
  questionNumber={24}
  question="실전 시험에서 여러 알고리즘 개념이 섞여 나올 때, '이 문제는 어떤 알고리즘·설계 기법으로 접근해야 하는가'를 판단하는 절차로 가장 적절한 것은?"
  options={['먼저 문제가 요구하는 것이 결정 문제인지 최적화 문제인지 파악하고, 부분 문제가 독립적인지(분할정복) 겹치는지(동적계획법) 확인하며, 매 단계 지역적 선택만으로 충분한지(탐욕 선택 속성·최적 부분 구조 성립 여부) 검토하고, 정확한 해가 필요한데 위 기법들이 맞지 않으면 완전 탐색·백트래킹·분기한정을, 그래프 구조라면 순회·최단경로·MST 등 목적에 맞는 그래프 알고리즘을 고려한다', '항상 가장 먼저 배운 알고리즘(예: 선택 정렬)부터 시도해 본다', '문제의 난이도와 상관없이 항상 완전 탐색만 사용한다', '문제 유형과 무관하게 항상 다익스트라 알고리즘을 적용해 본다']}
  correctAnswer={1}
  explanation="여러 설계 기법과 자료구조·알고리즘이 혼합된 종합 문제를 만났을 때는, 이 시리즈에서 다룬 판별 기준들을 순서대로 적용해 보는 것이 효과적이다. 먼저 문제의 답의 형태(예/아니오인지, 최적값인지)를 확인하고, 문제를 부분 문제로 나눌 수 있는지, 나눌 수 있다면 그 부분 문제들이 서로 독립적인지(독립적이면 분할정복으로 충분) 아니면 반복해서 겹치는지(겹치면 동적계획법이 필요)를 판단한다. 만약 매 단계 눈앞의 최선 선택만으로 전체 최적해에 도달할 수 있다는 근거(탐욕 선택 속성과 최적 부분 구조)가 확인되면 탐욕 알고리즘으로 더 간단하고 빠르게 풀 수 있다. 만약 문제가 제약 조건을 만족하는 해를 찾는 조합 탐색이라면 백트래킹이나 분기한정을 고려하고, 문제가 정점과 간선으로 표현되는 관계를 다룬다면 그 목적(순회, 최단 거리, 신장 트리 등)에 맞는 그래프 알고리즘을 선택한다. 이런 체계적인 판별 절차가 이 과목 전체에서 요구하는 핵심 역량이다."
  optionExplanations={['정답이다. 이 시리즈 전체(20~23편)에서 다룬 판별 기준들을 종합해 문제 유형에 맞는 알고리즘·설계 기법을 선택하는 체계적인 절차를 정확히 제시한다', '문제의 성격을 분석하지 않고 무조건 특정 알고리즘부터 시도하는 것은 비효율적이며, 문제에 따라 전혀 맞지 않는 접근이 될 수 있다', '완전 탐색은 항상 정답을 보장하지만 대부분의 실전 문제에서 시간 제약상 사용할 수 없으며, 문제의 특성에 맞는 더 효율적인 기법을 찾는 것이 이 과목의 핵심 목표다', '다익스트라는 음수 가중치가 없는 단일 출발점 최단경로 문제에만 적합한 알고리즘이며, 정렬·탐색·설계 기법 문제 등 그래프와 무관한 문제에는 적용할 수 없다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [NIST 계산 이론·알고리즘 참고 자료](https://csrc.nist.gov)
- [RFC 문서 저장소 (알고리즘 실제 활용 사례 참고)](https://www.rfc-editor.org)
