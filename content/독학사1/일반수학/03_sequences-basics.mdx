---
title: '03. 수열과 급수 입문'
description: '수열의 기본 용어, 등차·등비수열의 감각, 부분합과 급수 개념, 시그마 기호 읽는 법을 정리한다'
---

<Callout type="info">
  **이번 문서의 목표:** 이 파일을 다 읽으면 수열의 항·일반항이 무엇인지 설명하고, 등차·등비수열을 구분하며, 시그마 기호로 쓰인 합을 풀어서 읽을 수 있다.
</Callout>

## 왜 수열의 용어부터 정리하나

08편(등차·등비수열 공식)과 09편(급수와 수렴)에서는 $a_n$, $S_n$, $\sum$ 같은 기호가 쉬지 않고 등장한다. 이 편에서 기호의 뜻과 읽는 법을 먼저 익혀 두면, 뒤에서는 "이 아래첨자가 뭐였지"에 걸리지 않고 바로 공식과 계산에 집중할 수 있다.

> **쉽게 말하면:** 수열은 순서를 매겨 나열한 숫자들의 목록이고, 급수는 그 숫자들을 전부 더한 값이다.

## 수열이란 무엇인가

**수열**(sequence)은 순서를 가지고 나열된 수들의 목록이다. 예를 들어 $2, 4, 6, 8, 10, \dots$은 하나의 수열이다.

- **항(term)**: 수열을 이루는 각각의 수. 첫 번째 항을 **첫째항**이라 부른다.
- **번호(항번호, index)**: 항이 몇 번째인지 나타내는 순번. 아래첨자로 표시한다.
- **일반항(general term)**: $n$번째 항을 $n$의 식으로 나타낸 것. $a_n$으로 쓴다.

표기법을 정리하면 다음과 같다.

| 표기 | 읽는 법 | 뜻 |
|------|---------|----|
| $a_1$ | "에이 원", "첫째항" | 수열의 1번째 항 |
| $a_2$ | "에이 투", "둘째항" | 수열의 2번째 항 |
| $a_n$ | "에이 엔", "일반항" | 수열의 $n$번째 항. $n$에 숫자를 넣으면 그 항의 값이 나오는 식 |
| $\{a_n\}$ | "에이 엔의 수열" | $a_1, a_2, a_3, \dots$ 전체를 하나의 수열로 가리키는 표기 |

**예시:** 수열 $2, 4, 6, 8, 10, \dots$은 $a_1=2, a_2=4, a_3=6, a_4=8, a_5=10$이다. 이 수열의 일반항은 $a_n = 2n$이다. 실제로 $n=1$을 넣으면 $a_1 = 2 \times 1 = 2$, $n=3$을 넣으면 $a_3 = 2 \times 3 = 6$으로 값이 정확히 맞는다.

> **비유:** 수열은 아파트 각 동·호수에 붙은 우편함과 비슷하다. $a_n$은 "$n$번째 우편함에 들어 있는 편지"이고, 일반항 공식은 "몇 번째 우편함인지 알려주면 그 안의 내용을 바로 계산해서 알려주는 규칙"이다.

## 등차수열: 일정하게 더해 가는 수열

**등차수열**(arithmetic sequence)은 이웃한 두 항의 차이가 항상 일정한 수열이다. 이 일정한 차이를 **공차**(common difference)라 하고 보통 $d$로 쓴다.

**예시:** $3, 7, 11, 15, 19, \dots$

- $7 - 3 = 4$, $11 - 7 = 4$, $15 - 11 = 4$, $19 - 15 = 4$ — 차이가 항상 $4$로 일정하다.
- 이 수열은 공차 $d = 4$인 등차수열이고, 첫째항은 $a_1 = 3$이다.

이 수열의 일반항을 직접 손으로 확인해 보자. 각 항은 첫째항에 공차를 몇 번 더한 것인지 세어 보면 규칙이 보인다.

- $a_1 = 3$ (공차를 0번 더함)
- $a_2 = 3 + 4 = 7$ (공차를 1번 더함)
- $a_3 = 3 + 4 + 4 = 11$ (공차를 2번 더함)
- $a_4 = 3 + 4 + 4 + 4 = 15$ (공차를 3번 더함)

즉 $n$번째 항은 첫째항에 공차를 $(n-1)$번 더한 값이다. 등차수열의 일반항 공식과 계차·합 공식의 유도 과정은 08편에서 자세히 다룬다. 여기서는 "공차가 무엇인지", "일반항이 왜 $(n-1)$번 더하는 형태인지"에 대한 감각만 잡아 둔다.

> **자주 틀리는 점:** $n$번째 항을 계산할 때 공차를 $n$번 더하는 것으로 착각하기 쉽다. 첫째항 자체가 이미 "0번 더한 상태"이므로, $n$번째 항은 공차를 $(n-1)$번만 더한 것임에 주의해야 한다.

## 등비수열: 일정하게 곱해 가는 수열

**등비수열**(geometric sequence)은 이웃한 두 항의 비율이 항상 일정한 수열이다. 이 일정한 비율을 **공비**(common ratio)라 하고 보통 $r$로 쓴다.

**예시:** $2, 6, 18, 54, 162, \dots$

- $6 \div 2 = 3$, $18 \div 6 = 3$, $54 \div 18 = 3$, $162 \div 54 = 3$ — 비율이 항상 $3$으로 일정하다.
- 이 수열은 공비 $r = 3$인 등비수열이고, 첫째항은 $a_1 = 2$이다.

등차수열과 마찬가지로 각 항을 "첫째항에 공비를 몇 번 곱했는지"로 확인할 수 있다.

- $a_1 = 2$ (공비를 0번 곱함)
- $a_2 = 2 \times 3 = 6$ (공비를 1번 곱함)
- $a_3 = 2 \times 3 \times 3 = 18$ (공비를 2번 곱함)
- $a_4 = 2 \times 3 \times 3 \times 3 = 54$ (공비를 3번 곱함)

즉 $n$번째 항은 첫째항에 공비를 $(n-1)$번 곱한 값이다. 정확한 지수 형태의 공식과 등비수열의 합 공식은 마찬가지로 08편에서 다룬다.

등차수열과 등비수열을 구분하는 기준을 표로 정리한다.

| 구분 | 이웃 항 사이의 관계 | 일정하게 유지되는 값 | 값이 변하는 방식 |
|------|---------------------|----------------------|-------------------|
| 등차수열 | 차이가 일정 | 공차 $d$ | 일정한 크기로 더해지며 변한다(등속) |
| 등비수열 | 비율이 일정 | 공비 $r$ | 일정한 배율로 곱해지며 변한다(가속·감속) |

> **비유:** 등차수열은 매달 정해진 금액을 저금통에 추가로 넣는 것과 같고(예: 매달 1만 원씩 추가), 등비수열은 예금에 일정한 이자율이 붙어 잔액이 불어나는 것과 같다(예: 매년 3퍼센트씩 증가). 더하는 저금은 늘어나는 속도가 일정하지만, 이자가 붙는 예금은 원금이 커질수록 불어나는 양도 커진다.

## 부분합: 앞에서부터 몇 개를 더한 값

**부분합**(partial sum)은 수열의 앞부분 항들을 순서대로 더한 값이다. 첫째항부터 $n$번째 항까지 더한 값을 $S_n$으로 쓴다.

$$
S_n = a_1 + a_2 + a_3 + \cdots + a_n
$$

- $S_n$: "에스 엔"으로 읽으며, 첫째항부터 $n$번째 항까지의 합을 뜻한다.

**예시:** 앞의 등차수열 $3, 7, 11, 15, 19, \dots$에서 $S_3$은 첫째항부터 셋째항까지의 합이다.

$$
S_3 = a_1 + a_2 + a_3 = 3 + 7 + 11 = 21
$$

$S_4$는 여기에 넷째항 $15$까지 더한 값이다.

$$
S_4 = S_3 + a_4 = 21 + 15 = 36
$$

이처럼 $S_n$은 이전 부분합에 새 항을 하나씩 얹어 가는 방식으로도 계산할 수 있다. 부분합을 빠르게 구하는 공식(등차수열의 합 공식, 등비수열의 합 공식)은 08편에서 유도 과정과 함께 다룬다.

## 시그마 기호: 합을 짧게 쓰는 법

항이 많아지면 $a_1 + a_2 + \cdots + a_{10}$처럼 더하기 기호를 계속 나열하기가 번거롭다. 이때 쓰는 것이 **시그마(sigma, $\sum$)** 기호다.

$$
\sum_{k=1}^{n} a_k = a_1 + a_2 + a_3 + \cdots + a_n
$$

- $\sum$: "시그마"라고 읽으며, "모두 더하라"는 뜻이다.
- $k$: 더해 나가는 동안 $1$부터 $n$까지 하나씩 바뀌는 변수. **항번호(index)** 역할을 한다.
- $k=1$ (시그마 아래): 더하기를 $k=1$부터 시작한다는 뜻.
- $n$ (시그마 위): 더하기를 $k=n$까지 한다는 뜻.
- $a_k$: 각 $k$에 대응하는 수열의 항. 더할 대상.

**시그마 읽는 순서:** "$k$를 $1$부터 $n$까지 하나씩 바꿔 가며 $a_k$를 전부 더하라"로 읽는다.

### 예시로 직접 풀어 확인하기

$\displaystyle\sum_{k=1}^{5} 2k$를 계산해 보자. 이 식은 $2k$에 $k=1$부터 $5$까지 차례로 넣어 나온 값을 모두 더하라는 뜻이다.

$$
\sum_{k=1}^{5} 2k = 2(1) + 2(2) + 2(3) + 2(4) + 2(5)
$$

각 항의 값을 순서대로 구하면 $2, 4, 6, 8, 10$이다. 이를 더한다.

$$
\sum_{k=1}^{5} 2k = 2 + 4 + 6 + 8 + 10 = 30
$$

또 다른 예시로 $\displaystyle\sum_{k=1}^{4} k^2$을 계산해 보자. $k^2$에 $k=1,2,3,4$를 각각 넣는다.

$$
\sum_{k=1}^{4} k^2 = 1^2 + 2^2 + 3^2 + 4^2 = 1 + 4 + 9 + 16
$$

이를 더한다.

$$
\sum_{k=1}^{4} k^2 = 1 + 4 + 9 + 16 = 30
$$

> **자주 틀리는 점:** 시그마 아래위의 숫자(범위)를 무시하고 무작정 일반항만 계산하는 실수가 잦다. $k=1$부터 시작하는지, $k=0$이나 $k=2$부터 시작하는지에 따라 항의 개수와 값이 완전히 달라지므로, 범위를 가장 먼저 확인해야 한다.

## 급수: 수열을 끝없이 더한 값

**급수**(series)는 수열의 모든 항을 (원칙적으로 끝없이) 더한 것을 말한다. 부분합 $S_n$이 항의 개수를 유한하게 정해 놓고 더한 값이라면, 급수는 $n$을 한없이 키워 갈 때 부분합이 어떤 값에 다가가는지를 다룬다.

$$
\sum_{k=1}^{\infty} a_k
$$

- $\infty$: "무한대"라고 읽으며, 더하기를 끝없이 계속한다는 뜻이다.

예를 들어 $\dfrac{1}{2} + \dfrac{1}{4} + \dfrac{1}{8} + \dfrac{1}{16} + \cdots$처럼 항이 계속 작아지며 끝없이 이어지는 합을 급수라 부른다. 이 합이 특정한 값으로 다가가는지(수렴), 한없이 커지거나 일정한 값 없이 흔들리는지(발산)를 판별하는 방법은 09편(급수와 수렴 판별)에서 등비급수를 중심으로 자세히 다룬다.

> **쉽게 말하면:** 부분합은 "지금까지 더한 값"이고, 급수는 "끝까지 계속 더하면 결국 어디로 가는가"를 묻는 것이다.

## 핵심 정리

- 수열은 순서가 있는 수들의 목록이며, $a_n$은 $n$번째 항, 일반항은 $a_n$을 $n$의 식으로 나타낸 것이다.
- 등차수열은 이웃 항의 차(공차 $d$)가 일정하고, 등비수열은 이웃 항의 비(공비 $r$)가 일정하다.
- $n$번째 항은 첫째항에 공차를(등차) $(n-1)$번 더하거나, 공비를(등비) $(n-1)$번 곱한 값이다.
- 부분합 $S_n$은 첫째항부터 $n$번째 항까지의 합이며, 시그마 $\sum$는 "범위 안의 항을 모두 더하라"는 뜻의 기호다.
- 급수는 수열을 끝없이 더한 값이며, 수렴·발산 여부는 09편에서 다룬다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="수열 5, 8, 11, 14, 17, ...의 공차는?"
  options={['2', '3', '5', '8']}
  correctAnswer={2}
  explanation="이웃한 항의 차이를 확인하면 8-5=3, 11-8=3, 14-11=3으로 항상 3이다. 따라서 공차 d=3인 등차수열이다."
  optionExplanations={['이웃 항의 차이는 2가 아니라 3이다', '정답. 모든 이웃 항의 차이가 3으로 일정하다', '5는 첫째항의 값이지 공차가 아니다', '8은 둘째항의 값이지 이웃 항 사이의 차이가 아니다']}
/>

<Quiz
  questionNumber={2}
  question="수열 3, 6, 12, 24, ...의 공비는?"
  options={['2', '3', '6', '12']}
  correctAnswer={1}
  explanation="이웃한 항의 비율을 확인하면 6/3=2, 12/6=2, 24/12=2로 항상 2다. 따라서 공비 r=2인 등비수열이다."
  optionExplanations={['정답. 모든 이웃 항의 비율이 2로 일정하다', '3은 첫째항의 값이지 공비가 아니다', '6은 둘째항의 값이지 비율이 아니다', '12는 넷째항 이전의 항 값이지 공비가 아니다']}
/>

<Quiz
  questionNumber={3}
  question="시그마 기호 Σ(k=1부터 4까지) 3k 의 값은?"
  options={['12', '24', '30', '18']}
  correctAnswer={3}
  explanation="3k에 k=1,2,3,4를 차례로 대입하면 3, 6, 9, 12가 되고, 이를 모두 더하면 3+6+9+12=30이다."
  optionExplanations={['3+6+9+12를 다 더하지 않고 일부만 더한 값이거나 계산 실수다. 전 항을 모두 더하면 30이다', '3+6+9+12=30이며 24는 이 합과 다르다', '정답. 3+6+9+12를 순서대로 더하면 30이다', '3+6+9+12=30이며 18은 이 합과 다르다']}
/>

<Quiz
  questionNumber={4}
  question="부분합 Sn에 대한 설명으로 옳은 것은?"
  options={['Sn은 수열의 n번째 항 하나만을 가리킨다', 'Sn은 첫째항부터 n번째 항까지를 모두 더한 값이다', 'Sn은 수열이 무한히 이어질 때의 합만을 가리킨다', 'Sn은 공차나 공비와 같은 뜻이다']}
  correctAnswer={2}
  explanation="Sn(부분합)은 첫째항 a1부터 n번째 항 an까지를 순서대로 더한 값, 즉 a1+a2+...+an이다."
  optionExplanations={['n번째 항 하나만을 가리키는 것은 an이지 Sn이 아니다', '정답. Sn은 첫째항부터 n번째 항까지의 합을 뜻한다', '무한히 이어질 때의 합은 급수의 개념이며, 부분합 Sn은 유한한 n개까지만 더한 값이다', '공차·공비는 이웃 항 사이의 관계를 나타내는 값이며 Sn(합)과는 다른 개념이다']}
/>

<Quiz
  questionNumber={5}
  question="등차수열에서 n번째 항이 첫째항에 공차를 몇 번 더한 값인지 옳게 설명한 것은?"
  options={['n번 더한 값이다', '(n-1)번 더한 값이다', '(n+1)번 더한 값이다', '항상 1번만 더한 값이다']}
  correctAnswer={2}
  explanation="첫째항 a1은 공차를 0번 더한 상태이고, 둘째항은 1번, 셋째항은 2번 더한 상태다. 따라서 n번째 항은 공차를 (n-1)번 더한 값이다."
  optionExplanations={['n번 더하면 실제 값보다 공차 한 번만큼 더 많아진다. 예를 들어 둘째항은 1번만 더한 값이지 2번이 아니다', '정답. 첫째항이 이미 0번 더한 상태이므로 n번째 항은 (n-1)번 더한 값이다', '(n+1)번은 실제보다 두 번 더 많이 더한 것이 되어 값이 맞지 않는다', '더하는 횟수는 항번호에 따라 달라지므로 항상 1번이라는 설명은 틀렸다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr)
- [Wolfram MathWorld — Sequence](https://mathworld.wolfram.com)
