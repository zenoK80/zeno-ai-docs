---
title: '26. 플립플롭·카운터·순차회로 집중 기출·유형 풀이'
description: '플립플롭 특성식·여기표·변환부터 타이밍·레이스·해저드, 순차회로 설계, 동기·비동기 카운터, 레지스터·시프트 레지스터까지 순차논리 전 범위를 계산 전 과정으로 재구성한 24문항'
---

<Callout type="warning">
  이 문서의 문제는 실제 독학사 기출문제를 그대로 옮긴 것이 아니라, 국가평생교육진흥원이 공개한 2단계 논리회로 **출제기준(평가영역)을 바탕으로 새로 재구성**한 유형 문제다. 18~22편(플립플롭·여기표·타이밍·순차회로 설계·카운터·레지스터)의 계산 유형을 집중적으로 모았으며, 실제 회차별 문항 수·난이도는 매년 시행공고에 따라 달라질 수 있으므로 최신 공고를 함께 확인해야 한다.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="SR 플립플롭에서 S=1, R=0일 때 다음 상태 Q(t+1)로 옳은 것은?"
  options={['0으로 리셋', '1로 세트', '이전 상태 유지', '부정(정의되지 않음)']}
  correctAnswer={2}
  explanation="SR 플립플롭 특성표에서 S=1, R=0은 세트 입력이며, 현재 상태와 관계없이 다음 상태는 항상 1이 된다."
  optionExplanations={['R=1일 때의 동작이며 R=0인 이 경우와 다르다', '정답이다. S=1, R=0은 세트 조건이므로 Q(t+1)=1이다', 'S=0, R=0일 때의 동작이며 이 경우와 다르다', 'S=1, R=1일 때의 금지 조건에 대한 설명이며 이 경우와 다르다']}
/>

<Quiz
  questionNumber={2}
  question="SR 플립플롭의 특성식 Q(t+1) = S + R′·Q(t)에 S=0, R=1, Q(t)=1을 대입한 결과로 옳은 것은?"
  options={['0', '1', 'Q(t)와 같은 값(1)', '부정(정의되지 않음)']}
  correctAnswer={1}
  explanation="R′=0이므로 R′·Q(t)=0×1=0이고, S=0이므로 전체 합은 0+0=0이다. 이는 R=1인 리셋 조건과 일치한다."
  optionExplanations={['정답이다. S=0이고 R′·Q(t)=0이므로 전체 식은 0이 된다', 'R=1(리셋)이 걸려 있으므로 결과가 1이 될 수 없다', 'R=1일 때는 이전 상태를 그대로 유지하지 않고 리셋되므로 Q(t)와 같은 값이 나오지 않는다', 'S와 R이 동시에 1인 경우가 아니므로 부정(금지 조건)에 해당하지 않는다']}
/>

<Quiz
  questionNumber={3}
  question="D 플립플롭에서 현재 상태 Q(t)=1에서 다음 상태 Q(t+1)=0으로 바뀌기 위해 필요한 D 값은?"
  options={['D=1', 'D=0', 'D=X(무관)', 'D 플립플롭으로는 불가능하다']}
  correctAnswer={2}
  explanation="D 플립플롭의 특성식은 Q(t+1)=D이므로, 원하는 다음 상태를 그대로 D에 넣으면 된다. Q(t+1)=0이 되려면 D=0이어야 한다."
  optionExplanations={['D=1이면 다음 상태가 1이 되어 원하는 전이(1에서 0으로)와 반대가 된다', '정답이다. D 플립플롭은 D값이 곧 다음 상태이므로 D=0이 필요하다', 'D 플립플롭의 여기표에는 don t care가 전혀 없으며 이 전이에도 D값이 명확히 정해진다', 'D 플립플롭으로도 이 전이는 D=0을 넣으면 정상적으로 만들 수 있다']}
/>

<Quiz
  questionNumber={4}
  question="JK 플립플롭의 특성식 Q(t+1) = J·Q(t)′ + K′·Q(t)에 J=1, K=0, Q(t)=0을 대입한 결과로 옳은 것은?"
  options={['0', '1', 'Q(t)와 같은 값(0)', '토글(반전)']}
  correctAnswer={2}
  explanation="J·Q(t)′ = 1×1 = 1이고 K′·Q(t) = 1×0 = 0이므로 전체 합은 1+0=1이다. 이는 J=1, K=0의 세트 조건과 일치한다."
  optionExplanations={['J·Q(t)′ 항이 1이 되어 전체 합이 0이 될 수 없다', '정답이다. 세트 조건(J=1,K=0)이므로 Q(t)와 관계없이 다음 상태는 1이다', '세트 조건에서는 이전 상태(0)를 그대로 유지하지 않고 1로 바뀐다', '토글은 J=K=1일 때의 동작이며 이 조합(J=1,K=0)과는 다르다']}
/>

<Quiz
  questionNumber={5}
  question="T 플립플롭에서 현재 상태 Q(t)=0에서 다음 상태 Q(t+1)=1로 바뀌기 위해 필요한 T 값은?"
  options={['T=0', 'T=1', 'T=X(무관)', '이 전이는 T 플립플롭으로 불가능하다']}
  correctAnswer={2}
  explanation="0에서 1로 상태가 바뀌는 것은 반전(토글)에 해당하므로, T 플립플롭에서는 T=1을 넣어야 한다."
  optionExplanations={['T=0은 상태를 그대로 유지시키는 값이며, 0에서 1로 바뀌는 반전을 만들지 못한다', '정답이다. 상태가 바뀌었으므로 반전 동작이 필요하고, 이는 T=1에 해당한다', 'T 플립플롭의 여기표에는 don t care가 없으며 이 전이에는 T=1이 명확히 정해진다', 'T 플립플롭은 토글 동작 하나로 0에서 1, 1에서 0 모두 만들 수 있다']}
/>

<Quiz
  questionNumber={6}
  question="JK 플립플롭의 여기표에서 현재 상태 Q(t)=0에서 다음 상태 Q(t+1)=1로 가기 위한 J, K 값으로 옳은 것은?"
  options={['J=0, K=0(확정)', 'J=1, K=X(무관)', 'J=X, K=1', 'J=0, K=X']}
  correctAnswer={2}
  explanation="0에서 1로 가려면 J=1,K=0(세트)이거나 J=1,K=1(토글)이면 되므로, J는 반드시 1이어야 하고 K는 0이든 1이든 상관없는 don't care다."
  optionExplanations={['J=0,K=0은 상태 유지 조건이므로 0에서 1로 가는 전이를 만들지 못한다', '정답이다. J는 1로 확정되고, K는 상관없는 don t care다', 'J와 K의 확정·무관 여부가 서로 뒤바뀐 오답이다. 실제로는 J가 확정, K가 무관이다', 'J=0으로는 0에서 1로 가는 전이를 만들 수 없다']}
/>

<Quiz
  questionNumber={7}
  question="SR 플립플롭을 D 플립플롭처럼 동작시키기 위한 변환식으로 옳은 것은?"
  options={["S=D′, R=D", 'S=D, R=D′', 'S=D, R=D', "S=D′, R=D′"]}
  correctAnswer={2}
  explanation="D=1이면 세트(S=1,R=0), D=0이면 리셋(S=0,R=1)이 되어야 D 플립플롭처럼 동작한다. 이를 만족하는 식은 S=D, R=D′이다."
  optionExplanations={['S와 R에 D 대신 D의 보수를 넣으면 D=1일 때 오히려 리셋되어 반대로 동작한다', '정답이다. D=1일 때 S=1,R=0(세트), D=0일 때 S=0,R=1(리셋)이 되어 정확히 D 플립플롭처럼 동작한다', 'S와 R에 같은 값 D를 넣으면 D=1일 때 S=R=1(금지 입력)이 되어 정의되지 않은 상태에 빠진다', 'S와 R에 같은 값 D′을 넣으면 D=0일 때 S=R=1(금지 입력)이 되어 정의되지 않은 상태에 빠진다']}
/>

<Quiz
  questionNumber={8}
  question="D 플립플롭을 T 플립플롭처럼 동작시키기 위한 변환식으로 옳은 것은?"
  options={['D=T·Q(t)', 'D=T+Q(t)', 'D=T⊕Q(t)', "D=T′"]}
  correctAnswer={3}
  explanation="D 플립플롭은 Q(t+1)=D이므로, 목표인 T 플립플롭의 특성식 Q(t+1)=T⊕Q(t)를 그대로 D 자리에 대입하면 D=T⊕Q(t)를 얻는다."
  optionExplanations={['AND 형태로는 T=1,Q(t)=0일 때 D=0이 되어 반전(토글)이 일어나야 하는 상황과 맞지 않는다', 'OR 형태로는 T=0,Q(t)=1일 때 D=1이 되어 상태를 유지해야 하는 상황과 맞지 않는다', '정답이다. T 플립플롭의 특성식을 D 자리에 그대로 대입한 결과다', 'D를 T의 보수로만 정하면 현재 상태 Q(t)를 전혀 반영하지 못해 토글 동작을 만들 수 없다']}
/>

<Quiz
  questionNumber={9}
  question="회로의 전파 지연이 8나노초, 셋업 타임이 3나노초일 때, 안정적으로 동작하기 위한 최소 클록 주기로 옳은 것은?"
  options={['5나노초', '8나노초', '11나노초', '24나노초']}
  correctAnswer={3}
  explanation="최소 클록 주기는 전파 지연과 셋업 타임의 합이다. 8나노초와 3나노초를 더하면 11나노초이므로, 클록 주기는 최소 11나노초 이상이어야 한다."
  optionExplanations={['8과 3을 뺀 값으로, 최소 클록 주기 계산과 관련이 없다', '전파 지연만 고려한 값이며 셋업 타임을 더하지 않았다', '정답이다. 전파 지연 8나노초와 셋업 타임 3나노초를 더한 11나노초가 최소 클록 주기다', '두 값을 곱하는 등 잘못된 계산으로 나온 값이다']}
/>

<Quiz
  questionNumber={10}
  question="레이스 컨디션과 해저드를 비교한 설명으로 옳은 것은?"
  options={['레이스 컨디션은 주로 래치·펄스 트리거 구조에서, 해저드는 주로 조합논리회로의 경로별 지연 차이에서 발생한다', '레이스 컨디션과 해저드는 항상 동시에만 발생하는 하나의 현상이다', '해저드는 순차회로에서만 발생하고 조합논리회로에는 나타나지 않는다', '레이스 컨디션은 에지 트리거 플립플롭에서 가장 흔히 발생한다']}
  correctAnswer={1}
  explanation="레이스 컨디션은 인에이블 구간이 길거나 펄스 폭이 넓은 래치·펄스 트리거 구조에서 값이 의도치 않게 여러 번 바뀌는 문제이고, 해저드는 조합논리회로 내부 경로별 전파 지연 차이로 순간적인 글리치가 발생하는 현상이다."
  optionExplanations={['정답이다. 두 현상이 주로 발생하는 위치가 다르다는 것이 핵심 구분 기준이다', '두 현상은 원인과 발생 위치가 다른 별개의 문제이며 항상 함께 발생하지 않는다', '해저드는 오히려 조합논리회로에서 주로 발생하는 현상이다', '레이스 컨디션은 에지 트리거보다 인에이블 구간이 긴 래치·펄스 트리거 구조에서 더 흔히 발생한다']}
/>

<Quiz
  questionNumber={11}
  question="Mealy 기계와 Moore 기계의 상태 개수에 대한 설명으로 옳은 것은?"
  options={['Moore 기계가 Mealy 기계보다 항상 상태 개수가 적다', '일반적으로 Mealy 기계가 Moore 기계보다 같은 동작을 더 적은 상태로 구현할 수 있다', '두 모델은 상태 개수가 항상 정확히 같다', '상태 개수는 항상 입력 변수의 개수와 정확히 같다']}
  correctAnswer={2}
  explanation="Mealy 기계는 출력을 정할 때 현재 상태뿐 아니라 입력까지 함께 활용할 수 있어, 같은 동작을 Moore 기계보다 더 적은 상태로 구현하는 경우가 일반적이다."
  optionExplanations={['실제로는 반대로 Mealy 기계가 더 적은 상태로 구현되는 경우가 일반적이다', '정답이다. 입력이라는 추가 정보를 쓸 수 있는 Mealy 기계가 상태를 더 세분화하지 않아도 된다', '두 모델은 설계하는 동작에 따라 필요한 상태 개수가 다를 수 있다', '상태 개수는 회로가 기억해야 하는 조건의 개수에 따라 정해지며 입력 변수 개수와 직접 일치하지 않는다']}
/>

<Quiz
  questionNumber={12}
  question="상태가 6개인 순차회로를 설계할 때 필요한 최소 플립플롭 개수로 옳은 것은?"
  options={['2개', '3개', '4개', '6개']}
  correctAnswer={2}
  explanation="플립플롭 n개로 표현 가능한 상태는 2의 n제곱개다. 2의 2제곱은 4로 6개를 담기에 부족하고, 2의 3제곱은 8로 6개를 담고도 남으므로 최소 3개가 필요하다."
  optionExplanations={['2개의 플립플롭으로는 2의 2제곱인 4가지 상태만 표현 가능해 6개를 담기에 부족하다', '정답이다. 2의 3제곱인 8가지 상태로 6개의 상태를 모두 담을 수 있는 최소 개수다', '4개의 플립플롭은 2의 4제곱인 16가지 상태를 표현하므로 필요 이상으로 많다', '상태 개수와 플립플롭 개수를 1대1로 대응시키는 것은 이진 코드화의 이점을 활용하지 못하는 방식이다']}
/>

<Quiz
  questionNumber={13}
  question="순차회로 설계 절차의 올바른 순서로 옳은 것은?"
  options={['상태도 작성 → 상태표·상태 할당 → 여기표로 입력 논리 도출 → 카르노맵 간소화', '카르노맵 간소화 → 여기표 적용 → 상태표 작성 → 상태도 작성', '상태표 작성 → 상태도 작성 → 카르노맵 간소화 → 여기표 적용', '여기표로 입력 논리 도출 → 카르노맵 간소화 → 상태도 작성 → 상태표 작성']}
  correctAnswer={1}
  explanation="순차회로 설계는 요구사항을 상태도로 표현하고, 상태표를 만들며 상태를 할당한 뒤, 여기표로 플립플롭 입력 논리를 도출하고, 마지막으로 카르노맵으로 간소화하는 순서를 따른다."
  optionExplanations={['정답이다. 상태도에서 시작해 상태표, 여기표, 카르노맵 순서로 진행하는 것이 표준 절차다', '순서가 완전히 거꾸로 배치되어 있다', '상태도 작성이 상태표 작성보다 먼저 이루어져야 하므로 순서가 맞지 않는다', '여기표를 적용하려면 먼저 상태표(상태 할당 포함)가 정해져 있어야 하므로 이 순서는 맞지 않다']}
/>

<Quiz
  questionNumber={14}
  question="0부터 6까지 7개 상태를 순환하는 mod-7 카운터를 설계할 때 필요한 최소 비트 수와 비정상(잉여) 상태 개수로 옳은 것은?"
  options={['2비트, 1개', '3비트, 1개', '3비트, 2개', '4비트, 9개']}
  correctAnswer={2}
  explanation="2의 2제곱(4)은 7개 상태를 담기에 부족하고, 2의 3제곱(8)은 7개를 담고도 남는다. 따라서 최소 3비트가 필요하며, 남는 상태는 8-7=1개다."
  optionExplanations={['2비트로는 최대 4개 상태만 표현 가능해 7개를 모두 담을 수 없다', '정답이다. 3비트(8가지 상태)로 7개를 정상 경로로, 1개를 비정상 상태로 쓸 수 있다', '3비트는 맞지만 비정상 상태는 8-7=1개이지 2개가 아니다', '4비트는 필요 이상으로 많으며 비정상 상태 개수도 9개가 아니라 16-7=9개가 아닌 잘못된 계산이다']}
/>

<Quiz
  questionNumber={15}
  question="0부터 11까지 12개 상태를 순환하는 mod-12 카운터를 설계할 때 필요한 최소 비트 수와 비정상(잉여) 상태 개수로 옳은 것은?"
  options={['3비트, 4개', '4비트, 4개', '4비트, 6개', '5비트, 20개']}
  correctAnswer={2}
  explanation="2의 3제곱(8)은 12개 상태를 담기에 부족하고, 2의 4제곱(16)은 12개를 담고도 남는다. 따라서 최소 4비트가 필요하며, 남는 상태는 16-12=4개다."
  optionExplanations={['3비트로는 최대 8개 상태만 표현 가능해 12개를 모두 담을 수 없다', '정답이다. 4비트(16가지 상태)로 12개를 정상 경로로, 4개를 비정상 상태로 쓸 수 있다', '4비트는 맞지만 비정상 상태는 16-12=4개이지 6개가 아니다', '5비트는 필요 이상으로 많으며 비정상 상태 개수도 32-12=20이 아니라 잘못된 계산이다']}
/>

<Quiz
  questionNumber={16}
  question="비동기 카운터와 동기 카운터에 대한 설명 중 옳지 않은 것은?"
  options={['비동기 카운터는 뒤 비트가 앞 비트의 출력을 클록으로 사용한다', '동기 카운터는 모든 플립플롭이 같은 클록을 동시에 공유한다', '비동기 카운터는 비트 수가 늘어나도 전파 지연이 누적되지 않는다', '동기 카운터는 여기표 기반 절차로 임의의 순서(모드 5 등)를 설계할 수 있다']}
  correctAnswer={3}
  explanation="옳지 않은 설명을 고르는 문제다. 비동기(리플) 카운터는 클록 신호가 비트마다 순서대로 전달되므로, 비트 수가 늘어날수록 전파 지연이 오히려 누적된다."
  optionExplanations={['비동기 카운터의 실제 구조에 대한 옳은 설명이다', '동기 카운터의 정의에 대한 옳은 설명이다', '정답이다(옳지 않은 설명). 실제로는 비트 수가 늘수록 전파 지연이 누적된다', '동기 카운터의 장점에 대한 옳은 설명이다']}
/>

<Quiz
  questionNumber={17}
  question="동기 카운터를 JK 플립플롭으로 설계할 때, 현재 상태 Q(t)=1에서 다음 상태 Q(t+1)=0으로 가기 위해 필요한 J, K 값으로 옳은 것은?"
  options={['J=0, K=0', 'J=1, K=X', 'J=X, K=1', 'J=0, K=1']}
  correctAnswer={3}
  explanation="JK 여기표에서 Q(t)=1, Q(t+1)=0인 행은 J가 무관(X), K=1이다. K=1이면 Q(t)=1을 0으로 리셋하며, 이때 J는 무엇이든 상관없다."
  optionExplanations={['J=0,K=0은 상태 유지 조건이며 1에서 0으로의 전이를 만들지 못한다', '이는 Q(t)=0에서 1로 바뀔 때의 여기표 값이다', '정답이다. K=1이면 1에서 0으로 리셋되며, 이때 J는 무관하다', 'K=1이 필요조건이지 J=0으로 고정되는 것은 아니다(J는 무관)']}
/>

<Quiz
  questionNumber={18}
  question="5비트 링 카운터가 순환하는 전체 상태의 개수로 옳은 것은?"
  options={['4개', '5개', '10개', '32개']}
  correctAnswer={2}
  explanation="n비트 링 카운터는 정확히 n개의 상태를 순환한다. 5비트이므로 5개의 상태를 순환하는 모드 5 카운터로 동작한다."
  optionExplanations={['4개는 5비트가 아니라 4비트 링 카운터가 순환하는 상태 개수다', '정답이다. n비트 링 카운터는 n개의 상태를 순환하므로 5비트는 5개다', '10개는 같은 비트 수의 존슨 카운터(2n)가 순환하는 상태 개수다', '32개는 5비트로 표현 가능한 전체 조합의 개수이며 링 카운터가 실제로 순환하는 상태 개수와는 다르다']}
/>

<Quiz
  questionNumber={19}
  question="5비트 존슨 카운터가 순환하는 전체 상태의 개수로 옳은 것은?"
  options={['5개', '10개', '25개', '32개']}
  correctAnswer={2}
  explanation="n비트 존슨 카운터는 2n개의 상태를 순환한다. 5비트이므로 2×5=10개의 상태를 순환한다."
  optionExplanations={['5개는 같은 비트 수의 링 카운터가 순환하는 상태 개수다', '정답이다. 5비트 존슨 카운터는 2×5=10개 상태를 순환한다', '25개는 5의 제곱으로, 존슨 카운터의 상태 개수 공식과 무관한 값이다', '32개는 5비트로 표현 가능한 전체 조합의 개수이며 존슨 카운터가 실제로 순환하는 상태 개수의 세 배가 넘는다']}
/>

<Quiz
  questionNumber={20}
  question="병렬입력 직렬출력 시프트 레지스터를 가리키는 용어와 그 설명으로 옳은 것은?"
  options={['SIPO - 한 비트씩 입력받아 한 번에 병렬로 출력한다', 'PISO - 한 번에 병렬로 입력받아 한 비트씩 순서대로 직렬로 출력한다', 'SISO - 입출력이 모두 한 비트씩 순서대로 이루어진다', 'PIPO - 입출력이 모두 한 번에 병렬로 이루어진다']}
  correctAnswer={2}
  explanation="입력이 병렬(Parallel-In), 출력이 직렬(Serial-Out)인 형식은 PISO다. 병렬 데이터를 한 번에 로드한 뒤, 클록마다 한 비트씩 순서대로 꺼내 직렬 통신선으로 내보낼 때 쓰인다."
  optionExplanations={['이는 SIPO에 대한 설명이며, 문제에서 묻는 병렬입력 직렬출력과는 입출력 방향이 반대다', '정답이다. 병렬로 입력받아 직렬로 출력하는 형식이 PISO의 정의다', '이는 SISO에 대한 설명이며 입출력이 모두 직렬인 형식이다', '이는 PIPO에 대한 설명이며 입출력이 모두 병렬인 형식이다']}
/>

<Quiz
  questionNumber={21}
  question="유니버설 시프트 레지스터에서 모드 선택 신호가 S1S0=01일 때의 동작으로 옳은 것은?"
  options={['상태 유지', '오른쪽 시프트', '왼쪽 시프트', '병렬 로드']}
  correctAnswer={2}
  explanation="유니버설 시프트 레지스터의 모드 선택 신호는 00=유지, 01=오른쪽 시프트, 10=왼쪽 시프트, 11=병렬 로드로 정의된다. S1S0=01은 오른쪽 시프트에 해당한다."
  optionExplanations={['상태 유지는 S1S0=00일 때다', '정답이다. S1S0=01은 오른쪽 시프트에 해당한다', '왼쪽 시프트는 S1S0=10일 때다', '병렬 로드는 S1S0=11일 때다']}
/>

<Quiz
  questionNumber={22}
  question="4비트 SISO 시프트 레지스터(초기값 0000)에 직렬로 0, 1, 1, 0(가장 먼저 들어오는 비트가 0)을 순서대로 입력했다. 4번째 클록 직후 Q3Q2Q1Q0 값으로 옳은 것은?"
  options={['0011', '0110', '1100', '0111']}
  correctAnswer={2}
  explanation="클록마다 Q0에 새 입력이 들어오고 나머지는 한 칸씩 밀린다. 1클록 후 0000, 2클록 후 0001, 3클록 후 0011, 4클록 후 0110이 된다."
  optionExplanations={['이는 3클록 후의 상태이며 아직 마지막 입력(0)이 반영되지 않았다', '정답이다. 0, 1, 1, 0을 순서대로 넣으면 4클록 후 0110이 된다', '입력 순서를 반대로 적용했을 때 나올 수 있는 값에 가깝다', '입력값 중 하나를 잘못 반영했을 때 나오는 값이다']}
/>

<Quiz
  questionNumber={23}
  question="카운터 설계에서 비정상 상태들이 서로 순환하며 정상 경로로 돌아오지 못하는 잠금(lockout) 현상을 방지하는 표준적인 방법은?"
  options={['비정상 상태를 모두 정상 상태로 취급해 무시한다', '비정상 상태의 다음 상태를 모두 정상 순환 경로로 되돌리는 자기 보정(self-correcting) 설계를 한다', '비정상 상태에 도달하면 클록 신호를 정지시킨다', '비정상 상태가 하나라도 있으면 설계 자체를 처음부터 다시 한다']}
  correctAnswer={2}
  explanation="모든 비정상 상태의 다음 상태를 정상 순환 경로(주로 시작 상태)로 향하도록 설계하면, 회로가 잡음이나 초기값 오류로 비정상 상태에 들어가더라도 다음 클록에 곧바로 정상 경로로 복귀한다. 이를 자기 보정 설계라 한다."
  optionExplanations={['무시하면 오히려 비정상 상태들끼리 우연히 순환하는 잠금 상태를 놓칠 수 있다', '정답이다. 자기 보정 설계가 잠금 현상을 방지하는 표준적인 방법이다', '클록을 정지시키는 것은 일반적인 카운터 설계 방법이 아니며 회로 전체 동작을 멈추게 한다', '비정상 상태는 상태 개수가 2의 거듭제곱과 정확히 일치하지 않을 때 흔히 생기며, 있다고 해서 설계를 처음부터 다시 할 필요는 없다']}
/>

<Quiz
  questionNumber={24}
  question="플립플롭이 셋업·홀드 타임을 위반했을 때 나타날 수 있는 현상으로 옳은 것은?"
  options={['출력이 즉시 정확한 값으로 안정된다', '출력이 0도 1도 아닌 애매한 상태에 머물다가 예측할 수 없는 시점에 값이 정해지는 준안정 상태가 발생할 수 있다', '오버플로가 즉시 발생한다', '레지스터의 내용이 자동으로 초기화된다']}
  correctAnswer={2}
  explanation="셋업·홀드 타임을 위반하면 플립플롭 내부의 되먹임 회로가 불안정한 전압에 걸려, 출력이 한동안 0도 1도 아닌 애매한 값을 유지하다가 예측할 수 없는 시점에 정착하는 준안정(metastability) 상태가 발생할 수 있다."
  optionExplanations={['셋업·홀드 타임을 위반하면 오히려 출력이 불안정해질 수 있으므로 즉시 안정된다는 설명은 틀렸다', '정답이다. 준안정 상태가 셋업·홀드 타임 위반의 대표적인 결과다', '오버플로는 연산 결과가 표현 범위를 벗어나는 현상으로, 타이밍 위반과는 다른 개념이다', '타이밍 위반이 레지스터 내용을 자동으로 초기화시키지는 않는다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 - 과목별 평가영역](https://bdes.nile.or.kr/nile/study/nStudy2_1.do)
- [국가평생교육진흥원 독학학위제 - 출제방향/수준](https://bdes.nile.or.kr/nile/study/nStudy1_1.do)
