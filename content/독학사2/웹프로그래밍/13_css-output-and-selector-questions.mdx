---
title: '13. CSS 개념 확인 문제'
description: '선택자 적용 결과, 우선순위 비교, 박스 모델 계산, display·position 판별을 코드와 함께 재구성한 유형 문제 20문항'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 문제집은 국가평생교육진흥원 독학학위제 2단계 웹 프로그래밍 출제기준(CSS 선택자·박스 모델·레이아웃·우선순위 영역)의 평가 범위와 문항 유형을 분석해 새로 구성한 **재구성 문항**입니다. 실제 기출 문항을 그대로 옮긴 것이 아닙니다. 회차별 정확한 문항 수·시험 시간은 시행처 공고를 확인해야 하며, 여기서는 20문항을 표준 분량으로 구성했습니다.
</Callout>

## 출제 범위와 문항 배분

| 영역 | 문항 수 | 문항 번호 | 관련 편 |
| --- | --- | --- | --- |
| 선택자 적용 결과 | 6 | 1–6 | 06 |
| 우선순위 비교 | 5 | 7–11 | 06 |
| 박스 모델 계산 | 5 | 12–16 | 07 |
| display·position 판별 | 4 | 17–20 | 07 |

계산형 문항은 값을 대입하기 전에 먼저 어떤 공식이 적용되는지 말로 정리한 뒤 숫자를 넣어보세요. 우선순위 문항은 반드시 각 선택자의 점수(개수)를 자리별로 세어 비교해야 실수를 줄일 수 있습니다.

## 선택자 적용 결과 (1–6)

### 문제 1

```html
<p class="notice">안내</p>
<p>일반 문단</p>
```

```css
.notice {
  color: red;
}
```

<Quiz
  questionNumber={1}
  question="위 코드를 적용했을 때 빨간색으로 표시되는 문단은 몇 개인가?"
  options={['1개', '0개', '2개', '문단이 아니라 문서 전체가 빨간색이 된다.']}
  correctAnswer={1}
  explanation="점 기호로 시작하는 선택자는 클래스 선택자로, 지정한 클래스 속성 값을 가진 요소에만 적용된다. notice 클래스를 가진 첫 번째 문단에만 빨간색이 적용되고, 클래스가 없는 두 번째 문단에는 적용되지 않는다."
  optionExplanations={['클래스 선택자는 해당 클래스를 가진 요소에만 적용되므로 1개가 정답이다.', '클래스가 일치하는 요소가 실제로 존재하므로 0개는 틀렸다.', '클래스가 없는 두 번째 문단에는 스타일이 적용되지 않는다.', '클래스 선택자는 지정한 클래스를 가진 요소에만 적용되며 문서 전체에 적용되지 않는다.']}
/>

### 문제 2

```html
<div id="header">머리말</div>
<div id="footer">꼬리말</div>
```

```css
#header {
  font-weight: bold;
}
```

<Quiz
  questionNumber={2}
  question="위 코드에서 굵게 표시되는 요소는 무엇인가?"
  options={['id 속성 값이 header인 div 하나만 굵게 표시된다.', '두 div 모두 굵게 표시된다.', '아무 요소도 굵게 표시되지 않는다.', 'id 속성 값이 footer인 div만 굵게 표시된다.']}
  correctAnswer={1}
  explanation="샵 기호로 시작하는 선택자는 아이디 선택자로, 문서 안에서 유일해야 하는 id 값을 정확히 지정한 요소 하나에만 적용된다. header라는 id를 가진 div에만 규칙이 적용된다."
  optionExplanations={['아이디 선택자는 해당 id를 가진 요소 하나에만 적용되므로 정답이다.', 'footer id를 가진 div는 이 선택자와 일치하지 않는다.', 'id가 정확히 일치하는 요소가 존재하므로 적용되는 요소가 없다는 설명은 틀렸다.', '이 선택자는 header id를 지정했으므로 footer id를 가진 요소에는 적용되지 않는다.']}
/>

### 문제 3

```html
<ul>
  <li>사과</li>
  <li>바나나</li>
</ul>
```

```css
ul li {
  list-style: none;
}
```

<Quiz
  questionNumber={3}
  question="위 코드에서 사용한 선택자의 의미로 옳은 것은?"
  options={['ul 요소 안에 후손으로 포함된 모든 li 요소를 선택한다(직계 자식이 아니어도 된다).', 'ul 요소의 바로 다음 형제인 li 요소만 선택한다.', 'ul 요소와 li 요소 둘 중 하나라도 있으면 모두 선택한다.', 'li 요소 안에 있는 ul 요소만 선택한다.']}
  correctAnswer={1}
  explanation="공백으로 구분된 두 선택자는 후손 결합자(descendant combinator)로, 앞의 요소 안에 어떤 깊이로든 포함된 뒤의 요소를 모두 선택한다. ul 바로 아래 자식이든, 더 깊은 곳에 중첩된 li든 상관없이 모두 선택 대상이 된다."
  optionExplanations={['후손 결합자의 정확한 의미이므로 정답이다.', '형제 결합자는 물결표(~)나 더하기(+) 기호를 쓰며 공백과는 다른 결합자다.', '쉼표로 구분해야 여러 선택자 중 하나라도 해당하면 선택하는 방식이 되며, 공백은 포함 관계를 의미한다.', '선택자에 적힌 순서(ul 다음 li)는 바깥쪽에서 안쪽 방향을 가리키므로 반대로 해석하면 안 된다.']}
/>

### 문제 4

```html
<div>
  <p>첫 문단</p>
  <p>둘째 문단</p>
</div>
```

```css
div > p {
  margin: 0;
}
```

<Quiz
  questionNumber={4}
  question="위 코드에서 사용한 자식 결합자(부등호 기호)의 의미로 옳은 것은?"
  options={['div 바로 아래에 직접 속한 자식 p 요소만 선택하고, 더 깊이 중첩된 p는 선택하지 않는다.', '후손 결합자와 완전히 동일한 의미로, 깊이와 상관없이 모든 하위 p를 선택한다.', 'div보다 먼저 나오는 형제 p 요소를 선택한다.', 'p 요소 중에서 div를 자식으로 가지는 것만 선택한다.']}
  correctAnswer={1}
  explanation="부등호 기호로 표현하는 자식 결합자는 바로 한 단계 아래의 직계 자식만 선택한다. 만약 div 안에 다른 태그가 있고 그 태그 안에 p가 있다면, 그 p는 직계 자식이 아니므로 이 선택자로는 선택되지 않는다."
  optionExplanations={['직계 자식만 선택한다는 설명이 자식 결합자의 정확한 정의이므로 정답이다.', '후손 결합자(공백)와 자식 결합자(부등호 기호)는 선택 범위가 다르므로 동일하지 않다.', '이 선택자는 순서상 형제를 선택하는 결합자가 아니라 포함 관계를 나타내는 결합자다.', '선택자에 적힌 순서는 바깥(div)에서 안쪽(p) 방향이며 반대로 해석하면 안 된다.']}
/>

### 문제 5

```html
<input type="text" />
<input type="checkbox" />
```

```css
input[type='checkbox'] {
  width: 20px;
}
```

<Quiz
  questionNumber={5}
  question="위 코드에서 속성 선택자가 너비를 적용하는 대상으로 옳은 것은?"
  options={['type 속성 값이 정확히 checkbox인 input 요소', '모든 input 요소', 'type 속성이 있기만 하면 값과 상관없이 모든 input 요소', 'checkbox라는 이름의 클래스를 가진 요소']}
  correctAnswer={1}
  explanation="대괄호 안에 속성 이름과 값을 지정하는 속성 선택자는 해당 속성이 정확히 그 값을 가지고 있는 요소만 선택한다. 여기서는 type 속성 값이 checkbox인 input 요소에만 규칙이 적용되고, type이 text인 input에는 적용되지 않는다."
  optionExplanations={['속성 값이 정확히 checkbox인 요소만 선택한다는 설명이 정확하므로 정답이다.', '속성 값을 지정했으므로 모든 input이 아니라 조건에 맞는 input만 선택된다.', '값까지 지정했으므로 속성 존재 여부만으로 선택되지 않는다.', '대괄호 표기는 클래스 선택자가 아니라 속성 선택자이므로 클래스와는 다른 문법이다.']}
/>

### 문제 6

```html
<h2>제목</h2>
<h2>다른 제목</h2>
```

```css
h2::first-line {
  font-weight: bold;
}
```

<Quiz
  questionNumber={6}
  question="위 코드에서 콜론 두 개로 시작하는 부분의 역할로 옳은 것은?"
  options={['요소 자체가 아니라 요소 안 콘텐츠의 특정 부분(여기서는 첫 줄)을 선택하는 가상 요소다.', 'h2 요소가 두 번째로 등장했을 때만 적용되는 가상 클래스다.', '마우스를 올렸을 때만 적용되는 가상 클래스다.', 'CSS 문법 오류이므로 아무 효과도 없다.']}
  correctAnswer={1}
  explanation="콜론 두 개로 표기하는 것은 가상 요소(pseudo-element)로, 실제 문서에는 없는 부분을 마치 있는 것처럼 선택할 수 있게 해준다. first-line은 요소 콘텐츠의 첫 줄만 골라 스타일을 적용하는 가상 요소다. 콜론 한 개짜리 가상 클래스와는 표기와 역할이 다르다."
  optionExplanations={['콘텐츠의 특정 부분을 선택하는 가상 요소라는 설명이 정확하므로 정답이다.', 'nth-of-type 같은 가상 클래스가 순서 조건을 다루는 것과는 다른 문법이다.', '마우스 오버는 hover라는 가상 클래스가 담당하는 조건이다.', 'first-line은 표준 CSS 가상 요소이므로 문법 오류가 아니다.']}
/>

## 우선순위 비교 (7–11)

### 문제 7

```html
<p class="text" style="color: blue;">문단</p>
```

```css
.text {
  color: red;
}
```

<Quiz
  questionNumber={7}
  question="위 코드에서 실제로 적용되는 글자 색은 무엇인가?"
  options={['파란색', '빨간색', '검은색', '두 규칙이 충돌해 색이 지정되지 않은 기본값으로 남는다.']}
  correctAnswer={1}
  explanation="태그 안에 직접 style 속성으로 적은 인라인 스타일은 외부·내부 스타일시트의 어떤 선택자보다도 우선순위가 높다(!important를 쓴 규칙 제외). 따라서 클래스 선택자로 지정한 빨간색보다 인라인으로 지정한 파란색이 최종 적용된다."
  optionExplanations={['인라인 스타일이 클래스 선택자보다 우선순위가 높으므로 파란색이 정답이다.', '클래스 선택자로 지정한 빨간색은 인라인 스타일에 밀려 적용되지 않는다.', '색을 지정하는 두 규칙 모두 유효한 값을 가지고 있으므로 기본값으로 돌아가지 않는다.', '충돌 시 우선순위가 더 높은 규칙 하나가 명확하게 승리하며 색이 비워지지 않는다.']}
/>

### 문제 8

```css
p {
  color: green;
}
.text {
  color: red;
}
#main {
  color: blue;
}
```

<Quiz
  questionNumber={8}
  question="id가 main이고 클래스가 text인 p 요소에 위 세 규칙이 모두 걸릴 때 최종 색상은?"
  options={['파란색', '초록색', '빨간색', '세 색이 섞인 색']}
  correctAnswer={1}
  explanation="우선순위는 아이디 선택자, 클래스 선택자, 태그 선택자 순으로 높다. 아이디 선택자 한 개가 클래스 선택자 한 개보다, 클래스 선택자 한 개가 태그 선택자 한 개보다 점수가 높으므로, id 선택자로 지정한 파란색이 최종적으로 적용된다."
  optionExplanations={['아이디 선택자가 세 선택자 중 가장 높은 우선순위를 가지므로 파란색이 정답이다.', '태그 선택자는 세 선택자 중 우선순위가 가장 낮다.', '클래스 선택자는 아이디 선택자보다 우선순위가 낮다.', 'CSS는 색을 섞어 적용하지 않고 우선순위가 가장 높은 규칙 하나만 적용한다.']}
/>

### 문제 9

```css
div p {
  color: purple;
}
p.highlight {
  color: orange;
}
```

<Quiz
  questionNumber={9}
  question="div 안에 있고 highlight 클래스를 가진 p 요소에 위 두 규칙이 모두 걸릴 때 최종 색상은?"
  options={['주황색', '보라색', '두 색이 번갈아 깜빡인다.', '오류가 발생해 아무 색도 적용되지 않는다.']}
  correctAnswer={1}
  explanation="첫 번째 규칙은 태그 선택자 두 개(div, p)로 점수가 낮고, 두 번째 규칙은 태그 선택자 한 개와 클래스 선택자 한 개(p.highlight)로 점수가 더 높다. 클래스 선택자가 포함된 두 번째 규칙의 점수가 더 높으므로 주황색이 최종 적용된다."
  optionExplanations={['클래스 선택자가 포함된 규칙의 점수가 더 높으므로 주황색이 정답이다.', '태그 선택자만 두 개 쓰인 규칙은 클래스 선택자가 포함된 규칙보다 점수가 낮다.', 'CSS 우선순위 비교는 정적으로 한 번 계산되며 시간에 따라 번갈아 적용되지 않는다.', '두 규칙 모두 문법이 올바르므로 오류 없이 우선순위 비교가 이루어진다.']}
/>

### 문제 10

```css
p {
  color: black;
}
p {
  color: gray;
}
```

<Quiz
  questionNumber={10}
  question="완전히 동일한 선택자를 가진 두 규칙이 이 순서로 작성되어 있을 때 최종 색상은?"
  options={['회색', '검은색', '두 규칙이 충돌해 기본값으로 돌아간다.', '먼저 등장한 규칙이 항상 이긴다.']}
  correctAnswer={1}
  explanation="선택자의 우선순위 점수가 완전히 같을 때는 소스 코드에서 나중에 등장한 규칙이 이긴다는 원칙이 적용된다. 여기서는 두 규칙의 선택자가 똑같이 p 하나이므로 점수가 같고, 나중에 나온 회색 규칙이 최종 적용된다."
  optionExplanations={['점수가 같을 때는 나중에 나온 규칙이 이긴다는 원칙에 따라 회색이 정답이다.', '검은색 규칙은 나중에 나온 회색 규칙에 의해 덮어써진다.', '점수가 같을 때의 규칙은 명확히 정의되어 있으므로 기본값으로 돌아가지 않는다.', '점수가 같을 때는 먼저가 아니라 나중에 나온 규칙이 이긴다.']}
/>

### 문제 11

```css
p {
  color: navy !important;
}
#title {
  color: red;
}
```

<Quiz
  questionNumber={11}
  question="id가 title인 p 요소에 위 두 규칙이 모두 걸릴 때 최종 색상은?"
  options={['남색(navy)', '빨간색', '두 값이 함께 적용되어 보라색처럼 보인다.', '아이디 선택자가 항상 이기므로 빨간색이다.']}
  correctAnswer={1}
  explanation="!important가 붙은 선언은 일반적인 우선순위 계산 규칙을 무시하고 다른 어떤 일반 선언보다도 우선 적용된다. 따라서 점수가 더 높은 아이디 선택자의 선언이라도 !important가 없다면, !important가 붙은 태그 선택자의 선언에 밀린다."
  optionExplanations={['!important가 일반 우선순위 규칙을 넘어서므로 남색이 정답이다.', '아이디 선택자의 점수가 더 높아도 !important가 없으므로 이기지 못한다.', 'CSS는 색상 값을 혼합하지 않고 하나의 최종 값만 적용한다.', '!important가 있는 경우는 이 일반 원칙의 예외이므로 아이디 선택자가 항상 이긴다고 단정할 수 없다.']}
/>

## 박스 모델 계산 (12–16)

### 문제 12

```css
.box {
  width: 200px;
  padding: 10px;
  border: 5px solid black;
  box-sizing: content-box;
}
```

<Quiz
  questionNumber={12}
  question="위 코드가 적용된 요소가 화면에서 차지하는 실제 가로 폭은?"
  options={['230px', '200px', '210px', '220px']}
  correctAnswer={1}
  explanation="box-sizing이 content-box(기본값)일 때 width는 내용 영역(content)만의 너비를 의미한다. 실제 차지하는 폭은 내용 영역 200px에 좌우 padding(10px씩 2번, 20px)과 좌우 border(5px씩 2번, 10px)를 모두 더해야 한다. 200 + 20 + 10 = 230px이다."
  optionExplanations={['content 200에 좌우 padding 20, 좌우 border 10을 더한 230px이 정답이다.', '200px은 content 영역만의 너비이며 padding·border를 더하지 않은 값이다.', '210px은 border만 더하고 padding을 빠뜨렸을 때 나올 수 있는 값이다.', '220px은 padding만 더하고 border를 빠뜨렸을 때 나올 수 있는 값이다.']}
/>

### 문제 13

```css
.box {
  width: 200px;
  padding: 10px;
  border: 5px solid black;
  box-sizing: border-box;
}
```

<Quiz
  questionNumber={13}
  question="문제 12와 같은 padding·border 값에서 box-sizing만 border-box로 바꾸면 실제 가로 폭은?"
  options={['200px', '230px', '170px', '190px']}
  correctAnswer={1}
  explanation="box-sizing이 border-box일 때 width는 padding과 border를 포함한 전체 폭을 의미한다. 즉 지정한 200px 안에 padding과 border가 이미 포함되어 있으므로, 브라우저는 content 영역의 폭을 200에서 padding·border의 합만큼 줄여서 계산할 뿐, 요소가 실제로 차지하는 폭은 그대로 200px이다."
  optionExplanations={['border-box에서는 지정한 width 값 자체가 이미 padding·border를 포함한 최종 폭이므로 200px이 정답이다.', '230px은 content-box일 때(문제 12)의 결과이며 border-box에서는 해당하지 않는다.', '170px, 190px 모두 border-box의 정의(지정한 값이 곧 전체 폭)와 맞지 않는 계산이다.', '위와 같은 이유로 190px도 정답이 아니다.']}
/>

### 문제 14

```css
.box {
  width: 100px;
  margin: 20px;
}
```

<Quiz
  questionNumber={14}
  question="위 요소와 그 옆에 나란히 있는 다른 요소 사이의 관계에서, margin이 하는 역할로 가장 적절한 것은?"
  options={['요소의 테두리 바깥쪽에 다른 요소와의 간격을 만들어주며, 이 영역은 배경색이 칠해지지 않는 투명한 여백이다.', '요소 안쪽 내용과 테두리 사이의 간격을 만든다.', '요소의 테두리 두께 자체를 결정한다.', '요소의 실제 콘텐츠 너비를 100px에서 늘려준다.']}
  correctAnswer={1}
  explanation="margin은 박스 모델에서 테두리(border) 바깥쪽에 위치하는 여백으로, 다른 요소와의 간격을 만드는 역할을 한다. 이 여백에는 배경색이 적용되지 않아 투명하게 보인다. 안쪽 내용과 테두리 사이의 간격은 padding이 담당한다."
  optionExplanations={['테두리 바깥의 투명한 간격이라는 설명이 margin의 정확한 정의이므로 정답이다.', '안쪽 내용과 테두리 사이의 간격은 margin이 아니라 padding의 역할이다.', '테두리 두께는 border-width 같은 속성이 결정하며 margin과는 무관하다.', 'margin은 content 영역의 너비 자체를 변경하지 않는다.']}
/>

### 문제 15

```css
.box {
  width: 100px;
  height: 50px;
  margin: 10px auto;
}
```

<Quiz
  questionNumber={15}
  question="블록 요소에 위와 같이 좌우 margin을 auto로 지정하면 일반적으로 나타나는 효과는?"
  options={['요소가 부모 요소를 기준으로 가로 방향 가운데 정렬된다.', '요소의 너비가 부모 요소의 너비만큼 자동으로 늘어난다.', '요소가 세로 방향으로 가운데 정렬된다.', '위아래 margin이 자동으로 0이 된다.']}
  correctAnswer={1}
  explanation="너비가 고정된 블록 요소에서 좌우 margin을 auto로 지정하면, 브라우저는 부모의 남는 가로 공간을 좌우 margin에 균등하게 배분해 요소를 가로 방향으로 가운데 정렬한다. 이는 세로 정렬이나 너비 자동 확장과는 다른 효과다."
  optionExplanations={['좌우 auto margin이 가로 가운데 정렬을 만든다는 설명이 정확하므로 정답이다.', 'width를 100px로 고정했으므로 너비가 자동으로 늘어나지 않는다.', '세로 방향 정렬은 좌우 margin auto만으로는 이루어지지 않는다.', '위아래 margin 값은 10px로 지정한 값 그대로 유지되며 자동으로 0이 되지 않는다.']}
/>

### 문제 16

```css
.a {
  margin-bottom: 30px;
}
.b {
  margin-top: 20px;
}
```

<Quiz
  questionNumber={16}
  question="클래스 a를 가진 블록 요소 바로 아래에 클래스 b를 가진 블록 요소가 이어질 때, 두 요소 사이의 실제 간격으로 가장 적절한 것은?"
  options={['두 margin 값 중 더 큰 값인 30px만 적용된다(margin collapsing).', '두 값을 더한 50px가 적용된다.', '두 값의 평균인 25px가 적용된다.', '나중에 선언된 20px만 적용된다.']}
  correctAnswer={1}
  explanation="세로 방향으로 인접한 두 블록 요소의 margin은 서로 겹쳐서(margin collapsing) 더 큰 값 하나만 실제 간격으로 적용되는 것이 기본 동작이다. 여기서는 30px과 20px 중 더 큰 30px이 두 요소 사이의 실제 간격이 된다."
  optionExplanations={['margin collapsing에 따라 더 큰 값 하나만 적용된다는 설명이 정확하므로 정답이다.', 'margin은 합산되지 않고 더 큰 값 하나로 겹쳐지므로 50px은 틀렸다.', '평균을 내는 방식은 margin collapsing의 동작이 아니다.', '선언 순서가 아니라 값의 크기가 기준이므로 나중에 선언된 값이 무조건 이기지 않는다.']}
/>

## display·position 판별 (17–20)

### 문제 17

```css
span {
  display: block;
}
```

<Quiz
  questionNumber={17}
  question="원래 인라인 요소인 span에 display를 block으로 바꾸면 나타나는 변화로 가장 적절한 것은?"
  options={['한 줄 전체를 차지하며 앞뒤로 줄바꿈이 생기고, width·height 지정이 의미를 가지게 된다.', '요소가 화면에서 완전히 사라진다.', '요소 안의 글자 색이 자동으로 바뀐다.', '요소가 부모 요소 밖으로 항상 튀어나간다.']}
  correctAnswer={1}
  explanation="display를 block으로 바꾸면 그 요소는 블록 레벨 요소처럼 동작해 가로 폭 전체를 차지하려 하고 앞뒤에서 줄이 바뀐다. 또한 인라인 요소일 때는 무시되던 width·height 지정이 이제 실제로 적용된다."
  optionExplanations={['줄바꿈과 width·height 적용이라는 블록 레벨의 특징을 정확히 설명하므로 정답이다.', 'display: block은 요소를 화면에 보이게 하는 값이지 숨기는 값이 아니다.', 'display 값 자체는 글자 색과 직접 관련이 없다.', 'display: block 자체가 요소를 부모 밖으로 튀어나가게 만드는 속성은 아니다.']}
/>

### 문제 18

```css
.hidden {
  display: none;
}
```

<Quiz
  questionNumber={18}
  question="display를 none으로 지정한 요소에 대한 설명으로 옳은 것은?"
  options={['화면에 전혀 그려지지 않고 다른 요소가 그 자리를 차지하도록 공간도 사라진다.', '눈에는 보이지 않지만 원래 차지하던 공간은 그대로 남아 있다.', '투명해질 뿐 마우스 클릭 등 상호작용은 여전히 가능하다.', '요소의 글자 크기만 0으로 줄어든다.']}
  correctAnswer={1}
  explanation="display: none은 요소를 렌더링 트리에서 아예 제거하는 것과 같은 효과를 낸다. 화면에 그려지지 않을 뿐 아니라 차지하던 공간 자체가 사라져 다른 요소들이 그 빈자리를 채우며 배치된다. 이는 공간은 남기고 안 보이게만 하는 visibility: hidden과 다른 동작이다."
  optionExplanations={['화면 표시와 공간 모두 사라진다는 설명이 display: none의 정확한 동작이므로 정답이다.', '공간까지 사라진다는 점에서 visibility: hidden과는 다르며, 공간이 남는다는 설명은 옳지 않다.', 'display: none인 요소는 상호작용도 불가능하다.', '글자 크기가 줄어드는 것이 아니라 요소 자체가 렌더링되지 않는 것이다.']}
/>

### 문제 19

```css
.box {
  position: relative;
  top: 10px;
  left: 20px;
}
```

<Quiz
  questionNumber={19}
  question="position을 relative로 지정하고 top·left 값을 준 요소의 이동 기준으로 옳은 것은?"
  options={['그 요소가 원래(static일 때) 있었을 자기 자신의 위치를 기준으로 이동한다.', '가장 가까운 조상 중 position이 지정된 요소를 기준으로 이동한다.', '브라우저 화면 전체를 기준으로 이동한다.', '문서의 맨 위와 맨 왼쪽을 기준으로 항상 고정된다.']}
  correctAnswer={1}
  explanation="position: relative는 요소가 원래 차지했을 위치(static일 때의 자리)를 기준점으로 삼아 top·left 등의 값만큼 상대적으로 이동시킨다. 원래 자리는 여전히 빈 공간으로 남아 다른 요소가 그 자리를 채우지 않는다는 점이 특징이다. 조상 기준 이동은 absolute의 동작이다."
  optionExplanations={['원래 자기 자신의 위치를 기준으로 이동한다는 설명이 relative의 정확한 정의이므로 정답이다.', '조상 요소를 기준으로 이동하는 것은 absolute에서 나타나는 동작이다.', '화면 전체를 기준으로 고정되는 것은 fixed의 동작에 가깝다.', 'relative는 지정한 값만큼만 이동하며 맨 위·맨 왼쪽에 고정되는 것이 아니다.']}
/>

### 문제 20

```css
.box {
  position: absolute;
  top: 0;
  right: 0;
}
```

<Quiz
  questionNumber={20}
  question="이 요소를 감싸는 조상 요소들 중 어느 것도 position 값을 static이 아닌 값으로 지정하지 않았을 때, 이 요소의 위치 기준이 되는 것은?"
  options={['문서 전체를 감싸는 최상위 영역(뷰포트에 가까운 초기 컨테이닝 블록)을 기준으로 오른쪽 위 모서리에 배치된다.', '바로 위 부모 요소를 무조건 기준으로 삼는다.', 'position: absolute는 조상의 position 설정과 무관하게 항상 자기 자신의 원래 자리를 기준으로 삼는다.', '위치가 정해지지 않아 화면에 표시되지 않는다.']}
  correctAnswer={1}
  explanation="position: absolute인 요소는 조상 중 position이 static이 아닌(relative, absolute, fixed 등) 가장 가까운 요소를 기준으로 배치된다. 만약 그런 조상이 하나도 없다면, 문서의 최상위 컨테이닝 블록을 기준으로 삼아 top·right 등의 값만큼 배치된다. 이 때문에 시험에서는 부모에 position: relative를 걸어주는 습관을 강조한다."
  optionExplanations={['조상 중 기준이 될 요소가 없을 때 최상위 컨테이닝 블록이 기준이 된다는 설명이 정확하므로 정답이다.', '바로 위 부모라도 position이 static이면 기준이 되지 못하고 더 상위를 찾아 올라간다.', 'absolute는 조상의 position 값에 따라 기준이 달라지는 것이 핵심 동작이므로 이 설명은 틀렸다.', 'top·right 값이 지정되어 있으므로 위치가 정해지지 않아 표시되지 않는다는 설명은 틀렸다.']}
/>

## 참고 자료

- [W3C CSS Specifications](https://www.w3.org/Style/CSS/specs.en.html)
- [MDN Web Docs - CSS](https://developer.mozilla.org/en-US/docs/Web/CSS)
- [국가평생교육진흥원 독학학위제 시험안내](https://bdes.nile.or.kr/nile/exam/nExam2_1.do)
