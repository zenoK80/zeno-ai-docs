---
title: '15. 독학사 2단계 웹 프로그래밍 예상기출문제 1'
description: '인터넷 기초부터 HTML·CSS·JavaScript·XML·Ajax·웹 표준·접근성까지 전 범위를 골고루 다루는 22문항 종합형 모의고사'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 모의고사는 국가평생교육진흥원 독학학위제 웹 프로그래밍 과목의 공식 출제기준(평가영역)을 근거로 새로 구성한 **재구성 문항**입니다. 실제 기출 문항을 그대로 옮긴 것이 아니며, 회차별 실제 문항 수·시험 시간은 매 공고마다 다를 수 있으므로 응시 전 반드시 **공고 확인이 필요**합니다. 전체 22문항을 시간 제한 없이 먼저 풀어본 뒤, 문제 아래 해설로 정답 근거와 오답 이유를 하나씩 확인하세요.
</Callout>

## 문항 구성

| 영역 | 문항 수 | 문항 번호 |
| --- | --- | --- |
| 인터넷과 웹의 기본 구조 | 3 | 1–3 |
| HTML | 4 | 4–7 |
| CSS | 4 | 8–11 |
| JavaScript | 5 | 12–16 |
| XML과 Ajax | 3 | 17–19 |
| 웹 표준과 접근성 | 3 | 20–22 |

### 문제 1

다음과 같은 URL이 있다.

```text
https://www.example.com:8080/products/list?category=shoes&page=2
```

이 URL을 구성하는 각 부분(스킴, 호스트, 포트, 경로, 쿼리스트링)을 정확히 구분할 수 있어야 한다. 콜론(:) 뒤에 오는 숫자는 포트 번호이고, 물음표(?) 뒤에 오는 문자열은 서버에 추가로 전달할 조건을 담은 쿼리스트링이다. https의 기본 포트는 443이지만, URL에 포트가 명시되어 있으면 그 값을 우선 사용한다.

<Quiz
  questionNumber={1}
  question="위 URL에서 포트 번호와 쿼리스트링을 올바르게 짝지은 것은?"
  options={['포트 8080, 쿼리스트링 category=shoes&page=2', '포트 443, 쿼리스트링 products/list', '포트 8080, 쿼리스트링 products/list', '포트 443, 쿼리스트링 category=shoes&page=2']}
  correctAnswer={1}
  explanation="콜론 뒤에 명시된 8080이 포트 번호이며, 물음표 뒤의 category=shoes&page=2가 쿼리스트링이다. products/list는 경로(path)에 해당한다. https의 기본 포트 443은 URL에 포트가 따로 명시되지 않았을 때만 사용되므로 이 URL에서는 적용되지 않는다."
  optionExplanations={['정답 — 포트는 8080, 쿼리스트링은 category=shoes&page=2다.', '포트를 기본값 443으로 착각했고 경로를 쿼리스트링으로 착각했다.', '포트는 맞았지만 경로와 쿼리스트링을 혼동했다.', '포트를 기본값으로 착각했다.']}
/>

### 문제 2

HTTP는 클라이언트가 서버에 무언가를 요청(request)하고 서버가 그 결과를 응답(response)으로 돌려주는 프로토콜이다. 요청에는 목적에 따라 여러 메서드가 있는데, 그중 GET과 POST의 차이는 시험에서 자주 다루는 지점이다.

<Quiz
  questionNumber={2}
  question="HTTP GET 방식과 POST 방식에 대한 설명으로 옳은 것은?"
  options={['GET은 요청 데이터를 URL의 쿼리스트링에 실어 보내므로 브라우저 히스토리와 즐겨찾기에 그 값이 남을 수 있다', 'POST는 요청 데이터 길이에 제한이 없고 전송 내용이 항상 암호화되어 안전하다', 'GET은 서버의 자원 상태를 변경하는 용도로 표준에서 권장하는 메서드다', 'POST 요청의 응답은 항상 브라우저 캐시에 자동으로 저장되어 재사용된다']}
  correctAnswer={1}
  explanation="GET 요청은 데이터를 URL 뒤에 쿼리스트링 형태로 붙여 보내기 때문에 주소창·히스토리·서버 로그에 그대로 남는다. POST는 길이 제한이 실무상 완화되어 있지만 그 자체가 암호화를 보장하지는 않는다(암호화는 HTTPS가 담당). GET은 서버 상태를 바꾸지 않는 안전한 조회용으로 권장되며, 자원 상태 변경에는 POST·PUT·DELETE가 쓰인다. 캐시 저장 여부는 메서드 자체가 아니라 응답 헤더 설정에 달려 있다."
  optionExplanations={['정답 — GET의 값은 URL에 그대로 노출되어 남는다.', '암호화는 POST라는 메서드 자체가 아니라 HTTPS 계층이 담당한다.', 'GET은 상태를 바꾸지 않는 조회용으로 권장되는 메서드다.', '캐시 저장은 메서드가 아니라 응답 헤더에 따라 달라진다.']}
/>

### 문제 3

브라우저 주소창에 도메인 이름을 입력하고 엔터를 누르면, 그 이름을 실제 서버의 IP 주소로 바꾸는 과정과 서버에 연결해 문서를 받아오는 과정이 순서대로 일어난다.

<Quiz
  questionNumber={3}
  question="브라우저 주소창에 도메인 이름을 입력했을 때 이루어지는 과정의 순서로 옳은 것은?"
  options={['DNS 서버에 질의해 IP 주소를 알아낸다, 그다음 해당 IP로 서버에 연결해 HTTP 요청을 보낸다, 그다음 서버가 응답을 반환하면 브라우저가 화면을 그린다', '서버가 먼저 브라우저에 접속을 시도한 뒤, 그다음 브라우저가 DNS 질의를 수행한다', '브라우저가 곧바로 로컬에서 HTML 파일을 생성한 뒤, 그다음 서버에 결과만 보고한다', 'IP 주소를 먼저 화면에 표시한 뒤, 그다음 도메인 이름을 나중에 알아낸다']}
  correctAnswer={1}
  explanation="도메인 이름은 사람이 기억하기 쉬운 별칭일 뿐 실제 통신은 IP 주소로 이루어진다. 그래서 브라우저는 먼저 DNS(Domain Name System, 도메인 이름 시스템)에 질의해 도메인에 대응하는 IP 주소를 알아낸 뒤, 그 IP로 서버에 연결해 HTTP 요청을 보낸다. 서버가 응답을 돌려주면 브라우저가 그 내용을 해석해 화면에 렌더링한다."
  optionExplanations={['정답 — DNS 조회가 항상 통신보다 먼저 일어난다.', '클라이언트-서버 모델에서 연결은 클라이언트인 브라우저가 먼저 시도한다.', 'HTML은 서버가 제공하며 브라우저가 임의로 생성하지 않는다.', 'IP 주소는 사용자에게 직접 보이지 않고 내부적으로만 쓰인다.']}
/>

### 문제 4

HTML5는 문서의 각 영역이 어떤 역할을 하는지 드러내는 시맨틱(semantic, 의미론적) 태그를 제공한다. header, nav, section, article, aside, footer가 대표적이다.

<Quiz
  questionNumber={4}
  question="다음 중 시맨틱 태그의 용도 설명으로 옳지 않은 것은?"
  options={['nav 요소는 사이트의 주요 내비게이션 링크 묶음을 감싸는 데 사용한다', 'article 요소는 블로그 게시글처럼 그 자체로 독립적으로 배포·재사용될 수 있는 콘텐츠에 사용한다', 'aside 요소는 본문과 직접적인 관련은 없지만 부가적으로 참고할 수 있는 정보에 사용한다', 'section 요소는 한 문서에서 단 한 번만 사용할 수 있고 항상 문서의 최상위 위치에만 있어야 한다']}
  correctAnswer={4}
  explanation="section은 문서를 주제별로 묶는 범용 구획 태그로, 한 문서 안에서 여러 번 사용할 수 있고 article이나 다른 section 안에 중첩될 수도 있다. 최상위 위치로 제한되지 않으므로 이 설명이 옳지 않다. 나머지 셋은 각 태그의 표준적인 용도를 정확히 설명하고 있다."
  optionExplanations={['nav의 정확한 용도 설명이라 옳다.', 'article의 정확한 용도 설명이라 옳다.', 'aside의 정확한 용도 설명이라 옳다.', '정답(틀린 설명) — section은 여러 번 쓸 수 있고 중첩도 가능하다.']}
/>

### 문제 5

폼(form)은 사용자 입력을 받아 서버로 전달하는 HTML의 핵심 기능이다. input 요소의 type 속성 값에 따라 입력 방식과 브라우저의 기본 검증 동작이 달라진다.

<Quiz
  questionNumber={5}
  question="HTML 폼 요소에 대한 설명으로 옳지 않은 것은?"
  options={['input 요소의 type 속성 값을 email로 지정하면 브라우저가 기본적인 이메일 형식 검증을 수행할 수 있다', 'label 요소의 for 속성 값과 input 요소의 id 속성 값을 일치시키면 레이블 텍스트를 클릭해도 입력란에 포커스가 이동한다', 'input 요소의 type 속성 값이 number일 때 min과 max 속성을 함께 쓰면 입력 가능한 값의 범위를 제한할 수 있다', 'checkbox 타입은 여러 옵션 중 하나만 선택되도록 강제하고, radio 타입은 여러 개를 동시에 선택할 수 있다']}
  correctAnswer={4}
  explanation="checkbox와 radio의 역할이 반대로 서술되었다. checkbox는 같은 이름으로 묶여 있어도 여러 개를 동시에 선택할 수 있는 다중 선택용이고, radio는 같은 name 값을 공유하는 그룹 안에서 오직 하나만 선택되도록 강제하는 단일 선택용이다."
  optionExplanations={['email 타입의 기본 검증 동작에 대한 옳은 설명이다.', 'label과 input을 for·id로 연결하는 표준 동작에 대한 옳은 설명이다.', 'number 타입에서 min·max로 범위를 제한하는 옳은 설명이다.', '정답(틀린 설명) — checkbox는 다중 선택, radio는 단일 선택이 정확한 설명이다.']}
/>

### 문제 6

다음은 직원 명단의 일부를 나타낸 표다.

```html
<table>
  <thead>
    <tr><th>이름</th><th>부서</th><th>직급</th></tr>
  </thead>
  <tbody>
    <tr><td rowspan="2">홍길동</td><td>영업팀</td><td>사원</td></tr>
    <tr><td>기획팀</td><td>대리</td></tr>
  </tbody>
</table>
```

rowspan 속성은 세로 방향으로 셀을 병합할 때, colspan 속성은 가로 방향으로 셀을 병합할 때 쓴다. 속성 값은 병합해서 차지할 행(또는 열)의 개수를 뜻한다.

<Quiz
  questionNumber={6}
  question="위 표에서 rowspan 속성이 적용된 이름 셀에 대한 설명으로 옳은 것은?"
  options={['홍길동 셀은 세로로 2개 행에 걸쳐 병합되므로 두 번째 tr에는 이름 열의 td를 다시 쓰지 않는다', 'rowspan은 가로 방향으로 셀을 병합하는 속성이다', '두 번째 tr에도 이름 열의 td를 반드시 다시 작성해야 표가 깨지지 않는다', 'rowspan 값 2는 병합할 열의 개수가 2개라는 뜻이다']}
  correctAnswer={1}
  explanation="rowspan은 세로(행) 방향 병합 속성이다. 값이 2이므로 홍길동 셀은 첫 번째 행과 두 번째 행에 걸쳐 하나로 표시되고, 이미 그 칸을 차지했기 때문에 두 번째 tr에서는 이름 열의 td를 다시 작성하지 않는다. 다시 작성하면 오히려 열이 하나 더 생겨 표가 어긋난다."
  optionExplanations={['정답 — 병합된 셀은 다음 행에서 다시 작성하지 않는다.', 'rowspan은 세로(행) 방향, colspan이 가로(열) 방향 병합 속성이다.', '다시 작성하면 표의 열 개수가 어긋나 오히려 깨진다.', 'rowspan 값은 병합할 행의 개수를 의미한다.']}
/>

### 문제 7

HTML 문서는 doctype 선언으로 시작하고, head와 body로 구성된 계층 구조를 가진다.

<Quiz
  questionNumber={7}
  question="HTML 문서 구조에 대한 설명으로 옳지 않은 것은?"
  options={['문서 맨 앞의 doctype 선언은 브라우저가 표준 모드로 렌더링하도록 지시하는 역할을 한다', '주석은 화면에 표시되지 않지만 문서 소스를 열람하면 볼 수 있다', '부등호 기호를 문서 내용 안에 그대로 쓰려면 앤드(&) 기호로 시작하는 문자 참조(엔티티)로 표현해야 한다', 'head 요소 안의 내용은 모두 사용자 화면의 본문에 그대로 렌더링되는 콘텐츠다']}
  correctAnswer={4}
  explanation="head 요소는 제목·문자셋·외부 스타일시트 연결 같은 문서 메타데이터를 담는 영역이며, title 요소의 내용은 브라우저 탭에 표시될 뿐 body처럼 화면 본문에 그려지지 않는다. 따라서 head 안의 내용이 모두 본문에 렌더링된다는 설명은 옳지 않다."
  optionExplanations={['doctype 선언의 정확한 역할에 대한 옳은 설명이다.', '주석의 정확한 동작에 대한 옳은 설명이다.', '엔티티(문자 참조) 사용에 대한 옳은 설명이다.', '정답(틀린 설명) — head의 내용은 본문에 그대로 렌더링되지 않는다.']}
/>

### 문제 8

다음 CSS 규칙이 하나의 스타일시트에 함께 있고, 대상 문단은 id 값이 main인 요소 안, class 값이 box인 요소 안에 있으며 자신도 class 값 active를 가지고 있다고 하자.

```css
#main .box p {
  color: blue;
}
.box p.active {
  color: red;
}
```

CSS 우선순위(specificity, 명시도)는 아이디 선택자·클래스 선택자·태그 선택자 개수를 비교해서 정해진다. 아이디 선택자 하나는 클래스 선택자를 아무리 많이 조합해도 이기는 가장 강한 단위다.

<Quiz
  questionNumber={8}
  question="위 두 규칙이 함께 적용될 때 문단의 글자색으로 옳은 것은?"
  options={['파란색 — 아이디 선택자를 포함한 규칙이 클래스 개수와 무관하게 우선한다', '빨간색 — 클래스 선택자 개수가 더 많은 규칙이 우선한다', '두 규칙이 충돌해 브라우저 기본색으로 표시된다', '나중에 선언된 규칙이 항상 이기므로 빨간색이 적용된다']}
  correctAnswer={1}
  explanation="첫 번째 규칙 #main .box p는 아이디 1개, 클래스 1개, 태그 1개를 포함하고, 두 번째 규칙 .box p.active는 아이디 0개, 클래스 2개, 태그 1개를 포함한다. 우선순위는 아이디 개수부터 비교하므로, 클래스 개수와 관계없이 아이디를 포함한 첫 번째 규칙이 이긴다. 따라서 파란색이 적용된다."
  optionExplanations={['정답 — 아이디 선택자 포함 여부가 클래스 개수보다 먼저 비교된다.', '아이디를 포함한 규칙이 클래스 개수와 무관하게 우선한다.', 'CSS는 우선순위 규칙에 따라 하나의 값으로 결정되며 충돌로 기본색이 되지 않는다.', '작성 순서는 우선순위가 같을 때만 결정 기준이 되며, 여기서는 아이디 포함 여부가 먼저 갈린다.']}
/>

### 문제 9

다음과 같은 CSS가 적용된 div 요소가 있다.

```css
div {
  width: 200px;
  padding: 20px;
  border: 5px solid black;
  box-sizing: content-box;
}
```

box-sizing 속성의 기본값인 content-box에서는 width에 지정한 값이 content(내용) 영역만을 가리키고, padding과 border는 그 바깥에 추가로 더해진다.

<Quiz
  questionNumber={9}
  question="box-sizing이 content-box(기본값)일 때, width 200px·padding 20px·border 5px인 요소의 실제 렌더링 너비는?"
  options={['200px', '220px', '240px', '250px']}
  correctAnswer={4}
  explanation="content-box에서 요소의 전체 너비는 content 너비에 좌우 padding과 좌우 border를 모두 더한 값이다. 계산식은 200 더하기 (20 곱하기 2) 더하기 (5 곱하기 2)이며, 200 더하기 40 더하기 10은 250이다. 즉 실제 렌더링 너비는 250px이다."
  optionExplanations={['padding과 border를 전혀 더하지 않은 값이라 틀렸다.', 'padding만 더하고 border를 빠뜨린 값이라 틀렸다.', 'border를 한쪽만 더한 값이라 틀렸다.', '정답 — 200 더하기 좌우 padding 40 더하기 좌우 border 10은 250이다.']}
/>

### 문제 10

position 속성은 요소를 문서 흐름의 어디에 어떻게 배치할지를 결정한다.

<Quiz
  questionNumber={10}
  question="position 속성 값에 대한 설명으로 옳지 않은 것은?"
  options={['absolute로 지정된 요소는 가장 가까운 조상 중 position 값이 static이 아닌 요소를 기준으로 배치된다', 'fixed로 지정된 요소는 스크롤을 해도 뷰포트를 기준으로 같은 위치에 고정된다', 'relative로 지정된 요소는 자기 자신의 원래 위치를 기준으로 이동하며 원래 차지하던 공간은 그대로 남는다', 'static은 요소를 문서 흐름에서 제외시켜 다른 요소와 자유롭게 겹칠 수 있게 만든다']}
  correctAnswer={4}
  explanation="static은 position 속성의 기본값으로, 요소를 일반적인 문서 흐름 안에 그대로 배치한다. 문서 흐름에서 제외되어 겹침이 가능해지는 것은 absolute나 fixed의 특징이며, static 요소는 오히려 흐름을 그대로 따른다."
  optionExplanations={['absolute의 배치 기준에 대한 옳은 설명이다.', 'fixed의 고정 동작에 대한 옳은 설명이다.', 'relative가 원래 공간을 남긴다는 옳은 설명이다.', '정답(틀린 설명) — static은 흐름에서 제외되지 않고 일반 흐름을 그대로 따른다.']}
/>

### 문제 11

display 속성은 요소가 화면에서 차지하는 형태(줄바꿈 여부, 너비·높이 적용 여부)를 결정한다.

<Quiz
  questionNumber={11}
  question="display 속성 값에 대한 설명으로 옳은 것은?"
  options={['display none으로 지정된 요소는 화면에 보이지 않지만 레이아웃 공간은 그대로 차지한다', 'inline 요소는 width와 height 속성을 지정해도 기본적으로 적용되지 않는다', 'inline-block 요소는 줄바꿈 없이 옆으로 배치되면서도 width와 height는 절대 적용할 수 없다', 'block 요소는 항상 옆에 다른 요소를 나란히 배치할 수 있다']}
  correctAnswer={2}
  explanation="inline 요소는 텍스트처럼 줄 안에서 흐르는 요소라서 width·height를 지정해도 기본적으로 반영되지 않는다. display none은 공간까지 완전히 사라지게 만들며(공간을 남기는 것은 visibility hidden), inline-block은 옆으로 배치되면서도 width·height가 정상 적용되고, block 요소는 기본적으로 한 줄을 통째로 차지해 다른 요소를 옆에 둘 수 없다."
  optionExplanations={['display none은 공간까지 없애며, 공간만 남기고 숨기는 것은 visibility hidden이다.', '정답 — inline 요소는 width·height가 기본적으로 적용되지 않는다.', 'inline-block은 width·height가 정상적으로 적용되는 것이 핵심 특징이다.', 'block 요소는 기본적으로 한 줄 전체를 차지해 옆에 다른 요소를 둘 수 없다.']}
/>

### 문제 12

다음 코드를 살펴보자.

```js
for (var i = 0; i < 3; i++) {
  setTimeout(function () {
    console.log(i);
  }, 0);
}
```

var로 선언한 변수는 함수 스코프(또는 전역 스코프)를 가지며 블록 스코프를 만들지 않는다. setTimeout에 넘긴 콜백 함수는 대기 시간이 0이어도 반복문이 전부 끝난 뒤에야 실행된다. 반복문이 끝난 시점의 i 값을 한 줄씩 따라가 보면 다음과 같다.

| 시점 | i의 값 | 비고 |
| --- | --- | --- |
| 반복문 진입 전 | 0 | var i가 함수(전역) 스코프에 하나만 존재 |
| 1회차 종료 후 | 1 | 콜백 3개가 모두 같은 i를 참조 |
| 2회차 종료 후 | 2 | - |
| 3회차 조건 검사 실패 | 3 | 반복문 종료, i는 3으로 고정 |
| 콜백 3개 실행 시점 | 3 | 모두 같은 i를 참조하므로 3, 3, 3 출력 |

<Quiz
  questionNumber={12}
  question="위 코드를 실행하면 콘솔에 출력되는 값의 순서로 옳은 것은?"
  options={['0, 1, 2', '3, 3, 3', '0, 0, 0', '1, 2, 3']}
  correctAnswer={2}
  explanation="var로 선언된 i는 블록 스코프가 아닌 함수(또는 전역) 스코프를 가지므로, 반복문 3번이 만든 콜백 함수 3개가 전부 하나의 i를 공유해서 참조한다. setTimeout의 콜백은 반복문이 완전히 끝난 뒤에 실행되고, 그 시점의 i는 이미 3이 되어 있으므로 세 콜백 모두 3을 출력한다."
  optionExplanations={['각 회차의 i 값을 그대로 출력한다고 착각한 경우다. var는 블록마다 별도의 값을 갖지 않는다.', '정답 — 콜백 실행 시점에는 반복문이 끝나 i가 3이 되어 있고, 세 콜백이 그 값을 공유해서 출력한다.', 'i가 초기화되지 않는다고 착각한 경우다.', 'i 값이 1씩 증가한 상태로 각각 남는다고 착각한 경우로, let을 썼을 때의 결과와 혼동한 것이다.']}
/>

### 문제 13

다음 코드를 살펴보자.

```js
console.log(0 == "0");
console.log(0 === "0");
console.log(null == undefined);
console.log(null === undefined);
```

동등 비교 연산자는 두 값의 타입이 다르면 한쪽을 다른 쪽 타입으로 변환한 뒤 비교하고, 일치 비교 연산자는 타입 변환 없이 타입과 값이 모두 같아야 참이 된다. null과 undefined는 서로 다른 타입이지만 동등 비교에서만 예외적으로 같다고 취급되는 특별한 규칙이 있다.

<Quiz
  questionNumber={13}
  question="위 네 줄의 출력 결과를 순서대로 나열한 것은?"
  options={['true, false, true, false', 'false, true, false, true', 'true, true, false, false', 'false, false, true, true']}
  correctAnswer={1}
  explanation="0과 문자열 0은 타입이 다르므로 동등 비교는 문자열을 숫자로 변환해 비교해 참이 되지만, 일치 비교는 타입이 달라 거짓이 된다. null과 undefined도 타입은 다르지만 동등 비교에서만 서로 같다고 정의되어 있어 참이 되고, 일치 비교는 타입이 다르므로 거짓이 된다. 따라서 순서대로 참, 거짓, 참, 거짓이다."
  optionExplanations={['정답 — 동등 비교는 타입 변환 후 비교해 참이 나오고, 일치 비교는 타입이 다르면 항상 거짓이다.', '동등 비교와 일치 비교의 결과를 서로 뒤바꿔 이해한 경우다.', 'null과 undefined의 동등 비교 결과를 반대로 이해한 경우다.', '0과 문자열 0의 동등 비교 결과를 반대로 이해한 경우다.']}
/>

### 문제 14

다음 코드를 살펴보자.

```js
const numbers = [1, 2, 3];
const result = numbers.map(function (n) {
  return n * 2;
});
console.log(result);
console.log(numbers);
```

map 메서드는 배열의 각 요소에 함수를 적용한 결과를 담은 새로운 배열을 반환하며, 원본 배열 자체는 변경하지 않는다.

<Quiz
  questionNumber={14}
  question="위 코드 실행 후 result와 numbers를 순서대로 콘솔에 출력한 값으로 옳은 것은?"
  options={['[2, 4, 6] 그리고 [1, 2, 3]', '[2, 4, 6] 그리고 [2, 4, 6]', '[1, 2, 3] 그리고 [1, 2, 3]', 'undefined 그리고 [1, 2, 3]']}
  correctAnswer={1}
  explanation="map은 콜백 함수가 반환한 값들을 모아 새 배열을 만들어 돌려주므로 result는 각 요소를 2배 한 [2, 4, 6]이 된다. 원본 배열 numbers는 map이 직접 수정하지 않으므로 처음 그대로인 [1, 2, 3]으로 남는다."
  optionExplanations={['정답 — map은 새 배열을 반환하고 원본은 그대로 유지된다.', 'map이 원본 배열까지 바꾼다고 착각한 경우다. forEach로 원본을 직접 수정하는 경우와 혼동한 것이다.', 'map이 아무 변화도 만들지 않는다고 착각한 경우다.', 'map이 값을 반환하지 않는다고 착각한 경우로, forEach와 map의 반환값 차이를 혼동한 것이다.']}
/>

### 문제 15

다음 코드를 살펴보자.

```js
console.log(typeof greet);
console.log(typeof sayHi);

function greet() {
  return "hello";
}

var sayHi = function () {
  return "hi";
};
```

함수 선언문(function greet)은 실행 전 단계에서 함수 전체가 미리 메모리에 올라가는 호이스팅이 일어나 어디서든 바로 호출할 수 있다. 반면 var로 선언한 변수는 선언만 끌어올려지고 값 대입은 원래 코드 위치에서 이루어지므로, 대입 이전 시점에는 undefined 상태다.

<Quiz
  questionNumber={15}
  question="위 코드를 실행했을 때 두 줄의 출력 결과로 옳은 것은?"
  options={['function 그리고 undefined', 'undefined 그리고 function', 'function 그리고 function', 'undefined 그리고 undefined']}
  correctAnswer={1}
  explanation="function greet 같은 함수 선언문은 호이스팅될 때 함수 본체까지 통째로 끌어올려지므로, 코드 위쪽에서 typeof greet를 호출해도 이미 function 타입이다. 반면 var sayHi는 선언만 끌어올려지고 함수 표현식 대입은 원래 위치에서 실행되므로, 대입 이전인 위쪽에서는 아직 undefined다."
  optionExplanations={['정답 — 함수 선언문은 통째로 호이스팅되어 function, 함수 표현식이 대입되기 전 var는 undefined다.', '두 경우를 서로 반대로 이해한 경우다.', 'var로 선언한 함수 표현식도 함수 선언문처럼 통째로 호이스팅된다고 착각한 경우다.', 'greet가 함수 선언문으로 완전히 호이스팅된다는 사실을 놓친 경우다.']}
/>

### 문제 16

다음 코드를 살펴보자.

```html
<button id="btn">클릭</button>
<script>
  const button = document.getElementById("btn");
  button.addEventListener("click", function () {
    console.log("클릭됨");
  });
</script>
```

getElementById는 문서 안에서 지정한 id 값을 가진 요소 하나만 찾아 반환하며, addEventListener는 특정 이벤트가 발생했을 때 실행할 함수를 등록한다.

<Quiz
  questionNumber={16}
  question="위 코드에 대한 설명으로 옳은 것은?"
  options={['addEventListener의 두 번째 인자로 전달한 함수는 버튼을 클릭하는 순간 호출된다', 'getElementById는 조건에 맞는 요소를 문서 전체에서 모두 찾아 배열로 반환한다', 'addEventListener를 쓰면 한 요소에는 이벤트 리스너를 하나만 등록할 수 있다', 'click 이벤트는 마우스 포인터를 버튼 위에 올리기만 해도 발생한다']}
  correctAnswer={1}
  explanation="addEventListener는 지정한 이벤트 종류(여기서는 click)가 실제로 발생하는 시점에 등록해 둔 콜백 함수를 실행한다. getElementById는 문서에서 유일해야 하는 id 값을 기준으로 요소 하나만 반환하며, addEventListener는 같은 요소에 여러 개의 리스너를 등록할 수 있다. 마우스를 올리기만 할 때 발생하는 이벤트는 click이 아니라 mouseover 계열 이벤트다."
  optionExplanations={['정답 — 등록된 콜백은 클릭이 실제로 발생하는 순간 호출된다.', 'getElementById는 단일 요소를 반환하며, 여러 요소를 찾을 때는 querySelectorAll 등을 사용한다.', 'addEventListener는 같은 이벤트에 대해서도 여러 리스너를 누적해서 등록할 수 있다.', '마우스를 올리는 동작은 mouseover·mouseenter 계열 이벤트이며 click과는 다르다.']}
/>

### 문제 17

XML(Extensible Markup Language, 확장 가능한 마크업 언어)은 HTML처럼 태그 구조를 쓰지만, 태그의 종류와 의미를 미리 정해 두지 않고 문서를 작성하는 사람이 필요에 따라 자유롭게 만들어 쓴다는 점이 가장 큰 차이다.

<Quiz
  questionNumber={17}
  question="XML과 HTML의 차이에 대한 설명으로 옳은 것은?"
  options={['XML은 태그를 사용자가 자유롭게 정의할 수 있지만 HTML은 미리 정해진 태그 집합만 사용한다', 'HTML 문서는 반드시 태그를 닫아야 하지만 XML은 태그를 닫지 않아도 문제가 없다', 'XML은 화면에 표시할 목적이 아니라 오직 스타일을 지정하기 위한 언어다', 'HTML과 XML 모두 태그 이름의 대소문자를 구분하지 않는다']}
  correctAnswer={1}
  explanation="XML은 데이터의 구조를 표현하기 위해 태그 이름을 자유롭게 정의해서 쓰는 범용 마크업 언어이고, HTML은 웹 문서를 위해 미리 정해진 태그만 사용하는 언어다. 태그를 반드시 닫아야 하는 엄격한 규칙은 오히려 XML 쪽이 더 강하며, XML은 스타일이 아니라 데이터 표현이 목적이다. 또한 XML은 대소문자를 엄격히 구분하지만 HTML 태그 이름은 대소문자를 구분하지 않는다."
  optionExplanations={['정답 — 태그를 자유롭게 정의할 수 있다는 점이 XML의 핵심 특징이다.', '태그를 반드시 닫아야 하는 엄격한 규칙은 XML 쪽이며, 설명이 반대로 되어 있다.', 'XML은 데이터 표현이 목적이며 스타일 지정이 목적인 언어가 아니다.', 'XML은 대소문자를 구분하므로 이 설명은 옳지 않다.']}
/>

### 문제 18

다음은 도서 정보를 표현한 XML 문서다.

```xml
<도서>
  <제목>독학사 웹프로그래밍</제목>
  <가격>15000</가격>
</도서>
```

XML 문서가 잘 정형화되었다(well-formed)고 인정받으려면 몇 가지 구조 규칙을 지켜야 한다.

<Quiz
  questionNumber={18}
  question="잘 정형화된(well-formed) XML 문서의 조건으로 옳지 않은 것은?"
  options={['모든 여는 태그는 반드시 대응하는 닫는 태그가 있어야 한다', '문서 전체에는 하나의 루트 요소만 있어야 한다', '태그 이름은 대문자와 소문자를 구분하지 않아도 상관없다', '요소는 서로 교차하지 않고 올바르게 중첩되어야 한다']}
  correctAnswer={3}
  explanation="XML은 태그 이름의 대소문자를 엄격히 구분하는 언어다. 예를 들어 여는 태그를 도서로 열고 닫는 태그를 다른 대소문자로 닫으면 정형화되지 않은 문서로 취급된다. 나머지 셋(닫는 태그 존재, 단일 루트 요소, 올바른 중첩)은 모두 잘 정형화된 XML 문서가 지켜야 하는 실제 조건이다."
  optionExplanations={['잘 정형화된 XML의 실제 조건에 대한 옳은 설명이다.', '단일 루트 요소 규칙에 대한 옳은 설명이다.', '정답(틀린 설명) — XML은 대소문자를 엄격히 구분한다.', '올바른 중첩 규칙에 대한 옳은 설명이다.']}
/>

### 문제 19

Ajax(Asynchronous JavaScript and XML, 비동기 자바스크립트와 XML)는 이름에 XML이 들어 있지만, 실제로는 페이지 전체를 새로고침하지 않고도 자바스크립트로 서버와 데이터를 주고받는 기법 전반을 가리키는 말로 자리 잡았다.

<Quiz
  questionNumber={19}
  question="Ajax에 대한 설명으로 옳은 것은?"
  options={['페이지 전체를 다시 불러오지 않고도 서버와 데이터를 주고받아 화면 일부만 갱신할 수 있게 하는 기법이다', 'Ajax를 사용하면 서버와 반드시 XML 형식으로만 데이터를 주고받아야 한다', 'Ajax는 브라우저와 무관하게 서버에서만 단독으로 실행되는 기술이다', 'Ajax 요청은 항상 동기적으로 처리되어 응답이 올 때까지 화면 전체가 멈춘다']}
  correctAnswer={1}
  explanation="Ajax의 핵심은 페이지 전체를 새로고침하지 않고도 브라우저가 백그라운드에서 서버와 통신해 필요한 부분만 갱신할 수 있다는 점이다. 이름에 XML이 들어 있지만 현재는 JSON 등 다른 형식도 널리 쓰이며, Ajax는 브라우저(클라이언트) 쪽 자바스크립트가 주도하는 기술이고, 요청은 기본적으로 비동기로 처리되어 응답을 기다리는 동안에도 다른 화면 조작이 가능하다."
  optionExplanations={['정답 — 화면 일부만 비동기로 갱신할 수 있다는 점이 Ajax의 핵심이다.', 'XML은 초기 관례일 뿐이며 현재는 JSON 등 다양한 형식이 쓰인다.', 'Ajax는 브라우저 쪽 자바스크립트가 서버와 통신하는 기술이다.', 'Ajax라는 이름의 A는 비동기(asynchronous)를 뜻하며 화면이 멈추지 않는 것이 핵심이다.']}
/>

### 문제 20

웹 표준(web standard)은 특정 회사나 브라우저의 독자적인 방식이 아니라, W3C(World Wide Web Consortium, 월드와이드웹 컨소시엄) 같은 표준화 기구가 정한 공통 규격을 뜻한다.

<Quiz
  questionNumber={20}
  question="웹 표준을 준수하는 것의 의미로 가장 옳은 것은?"
  options={['특정 브라우저의 기능에 맞춰 최적화해 그 브라우저에서만 완벽하게 보이도록 만드는 것이다', 'W3C 등 표준화 기구가 정한 규격에 맞게 문서를 작성해 여러 브라우저와 기기에서 동일하게 동작하도록 하는 것이다', '자바스크립트를 전혀 사용하지 않고 페이지를 만드는 것이다', '모든 사이트의 디자인을 통일해 같은 모양으로 보이게 만드는 것이다']}
  correctAnswer={2}
  explanation="웹 표준의 목적은 특정 브라우저에 종속되지 않고, 공통 규격에 맞춰 작성한 문서가 어떤 브라우저·기기에서도 예측 가능하게 동작하도록 하는 것이다. 특정 브라우저 전용 최적화는 오히려 웹 표준의 취지에 반한다. 자바스크립트 사용 여부나 디자인 통일은 웹 표준 준수와 직접적인 관계가 없다."
  optionExplanations={['특정 브라우저 전용 최적화는 웹 표준이 지양하는 방식이다.', '정답 — 공통 규격에 맞춘 문서가 여러 환경에서 동일하게 동작하는 것이 웹 표준의 목적이다.', '웹 표준은 특정 기술의 사용 금지가 아니라 규격 준수를 의미한다.', '디자인 통일은 웹 표준과 무관한 개념이다.']}
/>

### 문제 21

웹 접근성(web accessibility)의 대표적인 국제 원칙은 인식의 용이성(perceivable), 운용의 용이성(operable), 이해의 용이성(understandable), 견고성(robust) 네 가지로 요약된다.

<Quiz
  questionNumber={21}
  question="이미지에 대체 텍스트를 제공하는 것과 가장 관련이 깊은 웹 접근성 원칙은?"
  options={['인식의 용이성(perceivable) — 정보를 감각적으로 인식할 수 있도록 대체 수단을 제공해야 한다는 원칙', '운용의 용이성(operable) — 키보드만으로도 모든 기능을 조작할 수 있어야 한다는 원칙', '이해의 용이성(understandable) — 콘텐츠와 조작 방법을 예측 가능하게 만들어야 한다는 원칙', '견고성(robust) — 다양한 보조 기술과 호환되어야 한다는 원칙']}
  correctAnswer={1}
  explanation="이미지의 대체 텍스트는 시각 정보를 인식하기 어려운 사용자에게 같은 정보를 다른 감각(스크린 리더의 음성)으로 전달하기 위한 수단이므로, 정보를 인식할 수 있게 만든다는 인식의 용이성 원칙에 직접 해당한다. 키보드 조작은 운용의 용이성, 예측 가능한 흐름은 이해의 용이성, 보조 기술과의 호환은 견고성과 관련된 별개의 원칙이다."
  optionExplanations={['정답 — 대체 텍스트는 정보를 다른 감각으로도 인식 가능하게 만드는 수단이다.', '키보드 조작 가능성과 관련된 원칙으로 이 문제와는 다른 항목이다.', '콘텐츠의 예측 가능성과 관련된 원칙으로 이 문제와는 다른 항목이다.', '보조 기술과의 기술적 호환성과 관련된 원칙으로 이 문제와는 다른 항목이다.']}
/>

### 문제 22

다음 img 요소를 살펴보자.

```html
<img src="chart.png" alt="2024년 분기별 매출 증가 추이 막대그래프">
```

alt 속성은 이미지를 대체하는 텍스트를 지정하는 속성으로, 웹 접근성과 검색 엔진 최적화 모두에 영향을 준다.

<Quiz
  questionNumber={22}
  question="위 img 요소의 alt 속성에 대한 설명으로 옳은 것은?"
  options={['이미지를 표시할 수 없거나 스크린 리더를 사용하는 사용자에게 이미지의 내용을 대신 전달하는 역할을 한다', 'alt 속성은 이미지가 클릭되었을 때 실행할 자바스크립트 코드를 지정하는 속성이다', 'alt 속성 값은 검색 엔진과 무관하며 접근성에도 아무 영향을 주지 않는다', 'alt 속성은 이미지의 가로세로 비율을 지정하는 데 사용하는 속성이다']}
  correctAnswer={1}
  explanation="alt 속성은 이미지 파일을 불러오지 못했을 때나 스크린 리더가 화면을 읽어 줄 때, 이미지가 전달하려는 내용을 텍스트로 대신 전달하는 역할을 한다. 클릭 이벤트 처리는 자바스크립트의 몫이고, 이미지 비율 지정은 width·height나 CSS의 역할이며, alt는 검색 엔진 최적화와 접근성 모두에 실제로 영향을 준다."
  optionExplanations={['정답 — 이미지 내용을 텍스트로 대신 전달하는 것이 alt의 핵심 역할이다.', '클릭 시 실행할 코드는 자바스크립트 이벤트 처리로 지정하며 alt와는 무관하다.', 'alt는 검색 엔진 최적화와 접근성 모두에 실제로 영향을 준다.', '가로세로 비율은 width·height 속성이나 CSS로 지정한다.']}
/>

## 참고 자료

- [WHATWG HTML Standard](https://html.spec.whatwg.org/)
- [W3C CSS Specifications](https://www.w3.org/Style/CSS/specs.en.html)
- [MDN Web Docs - JavaScript](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
- [MDN Web Docs - AJAX](https://developer.mozilla.org/en-US/docs/Glossary/AJAX)
- [W3C WAI - Introduction to Web Accessibility](https://www.w3.org/WAI/fundamentals/accessibility-intro/)
- [국가평생교육진흥원 독학학위제 시험안내](https://bdes.nile.or.kr/nile/exam/nExam2_1.do)
