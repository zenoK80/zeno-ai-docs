---
title: '21. 재사용·컴포넌트·과목 종합 기출 1'
description: '소프트웨어 재사용과 컴포넌트 개념, 그리고 01편부터 20편까지 전 범위를 잇는 연결형 예상문제 22선'
---

<Callout type="warning">
  이 편의 문항은 실제 독학학위제 3단계 소프트웨어공학 기출문제를 그대로 옮긴 것이 아닙니다. 국가평생교육진흥원이 공시하는 출제기준(평가영역)과 SWEBOK 계열 표준 지식체계를 근거로 새로 구성한 예상문제이므로, 실제 시험의 문항 수·배점·표현은 최신 시행 공고를 통해 반드시 다시 확인하세요.
</Callout>

## 이 편의 구성

이 편은 크게 두 부분으로 나뉩니다. 앞부분은 05편부터 20편까지 이어진 본론에서 다루지 않았던 재사용(reuse)과 컴포넌트(component) 개념을 새로 정리해 묻고, 뒷부분은 01편부터 20편까지 배운 생명주기 모델, 요구공학, 구조적·객체지향 분석, UML, 설계 원리, 구현, 테스트, 유지보수, 형상관리, 품질보증, 프로젝트관리, 위험관리를 골고루 섞어 연결형으로 점검합니다. 한 문제 안에서 여러 편의 개념이 동시에 등장하는 독학사 3단계 특유의 혼합형 문항에 익숙해지는 것이 이 편의 목표입니다.

## 문제

<Quiz
  questionNumber={1}
  question="소프트웨어 재사용(software reuse)에 대한 설명으로 가장 적절한 것은?"
  options={['이미 개발된 소프트웨어 자산을 새로운 시스템 개발에 다시 활용하는 것이다', '항상 소스 코드 수준의 복사와 붙여넣기만을 의미한다', '재사용은 요구공학 단계에서만 적용할 수 있는 기법이다', '재사용을 적용하면 개발 기간이 항상 늘어난다']}
  correctAnswer={1}
  explanation="소프트웨어 재사용은 코드, 설계, 아키텍처, 테스트 케이스, 문서 등 이미 검증된 소프트웨어 자산을 새 시스템 개발에 다시 활용해 개발 생산성과 품질을 높이는 활동입니다. 코드 수준에 한정되지 않고 요구공학부터 유지보수까지 생명주기 전 단계에서 적용할 수 있으며, 잘 적용하면 개발 기간과 비용을 줄이는 효과가 있습니다."
  optionExplanations={['정답입니다. 재사용은 코드뿐 아니라 설계, 아키텍처, 테스트 자산까지 포함하는 개념입니다', '재사용은 설계 패턴, 아키텍처, 컴포넌트, 테스트 케이스 등 다양한 산출물 수준에서 이루어집니다', '재사용은 요구공학, 설계, 구현, 테스트 등 생명주기 전반에 적용됩니다', '재사용의 목적은 오히려 개발 기간과 비용을 줄이는 것입니다']}
/>

<Quiz
  questionNumber={2}
  question="소프트웨어 재사용의 기대 효과로 옳지 않은 것은?"
  options={['검증된 자산을 사용해 결함 발생 가능성을 낮춘다', '개발 생산성을 높이고 개발 기간을 단축한다', '표준화된 자산을 공유해 유지보수 일관성을 높인다', '재사용 자산의 도메인 적합성을 별도로 검토하지 않아도 항상 안전하다']}
  correctAnswer={4}
  explanation="재사용의 기대 효과는 결함 감소, 생산성 향상, 개발 기간 단축, 유지보수 일관성 확보 등입니다. 다만 재사용 자산이 새로운 시스템의 도메인이나 요구사항에 맞는지는 반드시 별도로 검증해야 하며, 검토 없이 그대로 가져다 쓰면 숨은 결함이나 비적합성 문제가 발생할 수 있습니다. 따라서 항상 안전하다는 설명은 옳지 않습니다."
  optionExplanations={['옳은 설명입니다. 검증된 자산 재사용은 결함 가능성을 낮춥니다', '옳은 설명입니다. 재사용의 대표적 효과입니다', '옳은 설명입니다. 표준 자산 공유는 유지보수 일관성을 높입니다', '정답입니다. 재사용 자산도 새 시스템의 요구사항·도메인에 맞는지 검토해야 하며 항상 안전한 것은 아닙니다']}
/>

<Quiz
  questionNumber={3}
  question="소프트웨어 재사용을 도입할 때 발생할 수 있는 위험으로 가장 거리가 먼 것은?"
  options={['재사용 자산에 대한 과도한 의존으로 특정 결함이 여러 시스템에 전파될 수 있다', '재사용 자산의 인터페이스가 새 시스템 요구와 맞지 않아 추가 수정 비용이 발생할 수 있다', '재사용 자산 관리와 카탈로그화에 별도의 조직적 투자가 필요하다', '재사용을 적용하면 신규 개발자의 학습 부담이 완전히 사라진다']}
  correctAnswer={4}
  explanation="재사용의 위험으로는 공통 결함의 전파, 인터페이스 불일치로 인한 수정 비용, 재사용 자산 저장소 구축과 관리에 드는 조직적 투자 등이 꼽힙니다. 반면 재사용 자산을 이해하고 올바르게 적용하려면 개발자가 해당 자산의 설계 의도와 사용 제약을 학습해야 하므로, 학습 부담이 완전히 사라진다는 설명은 옳지 않습니다."
  optionExplanations={['실제 위험입니다. 공통 자산의 결함은 여러 시스템에 동시에 영향을 줍니다', '실제 위험입니다. 인터페이스 불일치는 대표적인 재사용 비용 요인입니다', '실제 위험(비용)입니다. 재사용 자산 저장소 구축과 관리에는 조직적 투자가 필요합니다', '정답입니다. 재사용 자산도 이해하고 적용하는 학습 비용이 존재하므로 학습 부담이 완전히 사라지지는 않습니다']}
/>

<Quiz
  questionNumber={4}
  question="재사용 대상이 되는 소프트웨어 자산의 유형을 짝지은 것으로 옳지 않은 것은?"
  options={['코드 재사용 - 함수, 라이브러리, 모듈 단위의 소스 코드', '설계 재사용 - 검증된 설계 패턴과 아키텍처 스타일', '테스트 재사용 - 테스트 케이스와 테스트 데이터', '요구 재사용 - 실행 파일 형태로 배포된 컴파일 결과물']}
  correctAnswer={4}
  explanation="재사용 대상은 코드(함수·라이브러리·모듈), 설계(설계 패턴·아키텍처 스타일), 테스트 자산(테스트 케이스·데이터), 도메인 요구사항 명세(유사 도메인의 요구사항 템플릿) 등 다양합니다. 그런데 실행 파일 형태의 컴파일 결과물은 요구 재사용이 아니라 배포 가능한 컴포넌트나 실행 모듈의 재사용에 해당하므로, 이 짝짓기는 옳지 않습니다."
  optionExplanations={['옳은 짝짓기입니다', '옳은 짝짓기입니다', '옳은 짝짓기입니다', '정답입니다. 요구 재사용은 유사 도메인의 요구사항 명세·템플릿을 재사용하는 것이며, 실행 파일 배포는 컴포넌트 재사용에 가깝습니다']}
/>

<Quiz
  questionNumber={5}
  question="소프트웨어 공학에서 정의하는 컴포넌트(component)의 특징으로 옳지 않은 것은?"
  options={['명확히 정의된 인터페이스를 통해서만 다른 컴포넌트와 상호작용한다', '독립적으로 배치하고 교체할 수 있는 단위이다', '내부 구현을 캡슐화해 인터페이스 뒤로 숨긴다', '반드시 하나의 소스 파일로만 구현되어야 한다']}
  correctAnswer={4}
  explanation="컴포넌트는 정의된 인터페이스로만 상호작용하고, 독립적으로 배치·교체 가능하며, 내부 구현을 캡슐화하는 소프트웨어 단위입니다. 구현이 몇 개의 소스 파일이나 모듈로 이루어지는지는 컴포넌트의 정의 요건이 아니므로, 반드시 하나의 소스 파일이어야 한다는 설명은 옳지 않습니다."
  optionExplanations={['옳은 특징입니다', '옳은 특징입니다', '옳은 특징입니다', '정답입니다. 컴포넌트는 내부적으로 여러 파일·모듈로 구현될 수 있으며, 소스 파일 수는 정의 요건이 아닙니다']}
/>

<Quiz
  questionNumber={6}
  question="컴포넌트 기반 개발(CBD, Component-Based Development)의 일반적인 절차를 순서대로 나열한 것은?"
  options={['요구 정의 → 컴포넌트 식별 및 평가 → 컴포넌트 적응·통합 → 시스템 테스트', '시스템 테스트 → 요구 정의 → 컴포넌트 통합 → 컴포넌트 식별', '컴포넌트 통합 → 요구 정의 → 시스템 테스트 → 컴포넌트 식별', '컴포넌트 식별 → 시스템 테스트 → 요구 정의 → 컴포넌트 통합']}
  correctAnswer={1}
  explanation="컴포넌트 기반 개발은 먼저 시스템 요구사항을 정의하고, 요구를 만족할 수 있는 기존 컴포넌트를 탐색·식별해 평가한 뒤, 선택한 컴포넌트를 시스템에 맞게 적응(수정)시키고 통합하며, 마지막으로 통합된 시스템을 테스트하는 순서로 진행됩니다. 이 순서를 벗어나면 요구에 맞지 않는 컴포넌트를 조립하게 되어 재작업 비용이 커집니다."
  optionExplanations={['정답입니다. 요구 정의가 가장 먼저이고 테스트가 가장 마지막입니다', '요구 정의보다 테스트가 먼저 나올 수 없습니다', '요구 정의가 통합보다 먼저 이루어져야 합니다', '요구 정의 없이 컴포넌트부터 식별할 수 없습니다']}
/>

<Quiz
  questionNumber={7}
  question="컴포넌트의 재사용성을 높이는 설계 특성으로 가장 적절한 것은?"
  options={['컴포넌트 내부 구현 세부사항을 인터페이스에 그대로 노출한다', '다른 컴포넌트와의 결합도를 낮추고 응집도를 높인다', '컴포넌트마다 서로 다른 방식의 임시 인터페이스를 사용한다', '컴포넌트 간 의존 관계를 가능한 한 많이 만든다']}
  correctAnswer={2}
  explanation="재사용성이 높은 컴포넌트는 다른 컴포넌트와의 결합도(coupling)는 낮추고 컴포넌트 내부 요소들의 응집도(cohesion)는 높여, 독립적으로 이해하고 교체할 수 있도록 설계됩니다. 내부 구현을 인터페이스에 그대로 노출하거나 임시방편의 비표준 인터페이스를 쓰거나 의존 관계를 늘리면 오히려 재사용성과 유지보수성이 떨어집니다."
  optionExplanations={['내부 구현 노출은 캡슐화 원칙에 어긋나 재사용성을 낮춥니다', '정답입니다. 낮은 결합도와 높은 응집도는 재사용성을 높이는 핵심 설계 특성입니다', '표준화되지 않은 임시 인터페이스는 재사용을 어렵게 만듭니다', '의존 관계가 많아지면 독립적 교체가 어려워져 재사용성이 낮아집니다']}
/>

<Quiz
  questionNumber={8}
  question="폭포수 모델과 나선형 모델을 비교한 설명으로 옳은 것은?"
  options={['폭포수 모델은 각 단계를 순차적으로 진행하며 이전 단계로 되돌아가지 않는 것을 전제로 한다', '나선형 모델은 위험 분석 활동 없이 반복만 수행하는 모델이다', '폭포수 모델은 반복적 위험 분석을 통해 점진적으로 완성도를 높이는 모델이다', '나선형 모델은 요구사항이 명확하고 변경이 없는 소규모 프로젝트에만 적합하다']}
  correctAnswer={1}
  explanation="폭포수 모델은 요구분석, 설계, 구현, 테스트, 유지보수 등 각 단계를 순차적으로 진행하고 원칙적으로 이전 단계로 되돌아가지 않는 것을 전제로 하는 모델입니다. 반면 나선형 모델은 위험 분석을 반복 사이클마다 수행하면서 점진적으로 시스템을 완성해 가는 모델로, 요구사항이 불확실하거나 대규모·고위험 프로젝트에 적합합니다."
  optionExplanations={['정답입니다. 폭포수 모델의 핵심 전제는 순차 진행과 역행 최소화입니다', '나선형 모델의 핵심 활동이 바로 각 반복의 위험 분석입니다', '반복적 위험 분석은 나선형 모델의 특징이지 폭포수 모델의 특징이 아닙니다', '나선형 모델은 오히려 요구사항이 불확실한 대규모·고위험 프로젝트에 적합합니다']}
/>

<Quiz
  questionNumber={9}
  question="요구사항 도출(requirements elicitation) 기법에 대한 설명으로 옳지 않은 것은?"
  options={['인터뷰는 이해관계자와 직접 대화하며 요구를 수집하는 기법이다', '브레인스토밍은 다양한 아이디어를 자유롭게 제시하고 이후 정리하는 기법이다', '프로토타이핑은 시제품을 통해 사용자 반응을 확인하며 요구를 구체화하는 기법이다', '요구사항 도출은 명세서 작성이 끝난 이후에 시작하는 마지막 활동이다']}
  correctAnswer={4}
  explanation="요구사항 도출은 이해관계자로부터 요구를 수집하는 활동으로, 인터뷰, 브레인스토밍, 프로토타이핑, 워크숍, 관찰 등의 기법을 사용합니다. 이는 요구공학의 초기 활동이며, 도출된 요구는 이후 분석과 명세(SRS 작성) 단계로 이어집니다. 따라서 명세서 작성이 끝난 뒤 시작하는 마지막 활동이라는 설명은 순서가 뒤바뀐 것으로 옳지 않습니다."
  optionExplanations={['옳은 설명입니다', '옳은 설명입니다', '옳은 설명입니다', '정답입니다. 도출은 명세 작성 이전의 초기 활동이며 마지막 활동이 아닙니다']}
/>

<Quiz
  questionNumber={10}
  question="좋은 소프트웨어 요구사항 명세서(SRS)가 갖추어야 할 품질 속성으로 옳지 않은 것은?"
  options={['명확성 - 하나의 요구는 하나의 해석만 가능해야 한다', '완전성 - 필요한 모든 요구가 누락 없이 기술되어야 한다', '검증 가능성 - 요구 충족 여부를 확인할 방법이 있어야 한다', '모호성 - 다양한 해석의 여지를 남겨 유연성을 확보해야 한다']}
  correctAnswer={4}
  explanation="좋은 SRS는 명확성(unambiguous), 완전성(complete), 검증 가능성(verifiable), 일관성(consistent), 수정 가능성 등을 갖추어야 합니다. 모호성은 오히려 SRS 품질을 해치는 요소로, 요구가 여러 방식으로 해석되면 설계자와 개발자마다 다르게 구현해 결함의 원인이 됩니다. 따라서 모호성을 유연성 확보의 장점으로 설명한 것은 옳지 않습니다."
  optionExplanations={['옳은 품질 속성입니다', '옳은 품질 속성입니다', '옳은 품질 속성입니다', '정답입니다. 모호성은 SRS 품질을 저해하는 요소이지 갖추어야 할 속성이 아닙니다']}
/>

<Quiz
  questionNumber={11}
  question="구조적 분석에서 사용하는 자료흐름도(DFD)에 대한 설명으로 옳지 않은 것은?"
  options={['프로세스, 자료흐름, 자료저장소, 외부개체(단말)로 구성된다', '시스템 내부의 자료 흐름과 처리 과정을 시각적으로 표현한다', '레벨을 나누어 상위 수준에서 하위 수준으로 점진적으로 상세화할 수 있다', '클래스 간의 상속 관계와 다중성을 표현하는 데 사용하는 표기법이다']}
  correctAnswer={4}
  explanation="DFD는 프로세스, 자료흐름, 자료저장소, 외부개체(단말)라는 네 가지 구성요소로 시스템 내부의 자료 처리와 흐름을 표현하며, 레벨 0(맥락도)부터 하위 레벨로 점진적으로 상세화합니다. 클래스 간 상속 관계와 다중성을 표현하는 것은 객체지향 분석의 클래스 다이어그램의 역할이므로, DFD를 그렇게 설명하는 것은 옳지 않습니다."
  optionExplanations={['옳은 설명입니다', '옳은 설명입니다', '옳은 설명입니다', '정답입니다. 상속·다중성 표현은 UML 클래스 다이어그램의 역할입니다']}
/>

<Quiz
  questionNumber={12}
  question="객체지향의 특성 중 상위 클래스의 속성과 연산을 하위 클래스가 물려받는 것을 가리키는 용어는?"
  options={['캡슐화(encapsulation)', '상속(inheritance)', '다형성(polymorphism)', '추상화(abstraction)']}
  correctAnswer={2}
  explanation="상속은 상위 클래스(부모 클래스)가 가진 속성과 연산을 하위 클래스(자식 클래스)가 물려받아 재사용하고 확장하는 객체지향 특성입니다. 캡슐화는 데이터와 연산을 하나로 묶고 내부를 숨기는 것, 다형성은 같은 메시지에 대해 객체마다 다르게 반응하는 것, 추상화는 공통 특성을 뽑아 일반화하는 것을 가리킵니다."
  optionExplanations={['캡슐화는 데이터와 연산을 하나로 묶고 은닉하는 특성으로 상속과 다릅니다', '정답입니다. 상위 클래스의 속성·연산을 물려받는 특성이 상속입니다', '다형성은 동일 메시지에 대해 객체별로 다른 동작을 수행하는 특성입니다', '추상화는 공통 특성을 뽑아 일반화하는 특성입니다']}
/>

<Quiz
  questionNumber={13}
  question="UML 다이어그램과 주된 표현 대상을 짝지은 것으로 옳지 않은 것은?"
  options={['유스케이스 다이어그램 - 시스템과 액터 간의 기능적 상호작용', '시퀀스 다이어그램 - 객체 간 메시지 교환의 시간적 순서', '상태 다이어그램 - 객체가 생명주기 동안 갖는 상태와 전이', '클래스 다이어그램 - 프로세스와 자료저장소 간의 자료 흐름']}
  correctAnswer={4}
  explanation="유스케이스 다이어그램은 액터와 시스템 기능의 상호작용을, 시퀀스 다이어그램은 객체 간 메시지의 시간적 순서를, 상태 다이어그램은 객체의 상태 전이를 표현합니다. 클래스 다이어그램은 클래스와 클래스 간의 관계(연관, 집합, 상속 등)를 표현하는 다이어그램이며, 프로세스와 자료저장소 간의 자료 흐름은 UML이 아니라 구조적 분석의 DFD가 표현하는 대상이므로 이 짝짓기는 옳지 않습니다."
  optionExplanations={['옳은 짝짓기입니다', '옳은 짝짓기입니다', '옳은 짝짓기입니다', '정답입니다. 프로세스와 자료저장소 간 흐름은 DFD의 표현 대상이며 클래스 다이어그램의 역할이 아닙니다']}
/>

<Quiz
  questionNumber={14}
  question="결합도(coupling)가 낮은 것부터 높은 순서로 옳게 나열한 것은?"
  options={['자료 결합도 - 스탬프 결합도 - 제어 결합도 - 내용 결합도', '내용 결합도 - 제어 결합도 - 스탬프 결합도 - 자료 결합도', '제어 결합도 - 자료 결합도 - 내용 결합도 - 스탬프 결합도', '스탬프 결합도 - 내용 결합도 - 자료 결합도 - 제어 결합도']}
  correctAnswer={1}
  explanation="결합도는 일반적으로 자료 결합도가 가장 낮고, 스탬프 결합도, 제어 결합도, 외부 결합도, 공통 결합도를 거쳐 내용 결합도가 가장 높다고 봅니다. 자료 결합도는 필요한 데이터 항목만 매개변수로 주고받는 가장 바람직한 형태이고, 내용 결합도는 한 모듈이 다른 모듈의 내부를 직접 참조·수정하는 가장 바람직하지 않은 형태입니다."
  optionExplanations={['정답입니다. 자료 결합도가 가장 낮고 내용 결합도가 가장 높습니다', '순서가 정반대로 나열되어 있습니다', '자료 결합도가 제어 결합도보다 낮아야 하는데 순서가 뒤섞여 있습니다', '내용 결합도가 가장 높아야 하는데 두 번째에 위치해 순서가 틀렸습니다']}
/>

<Quiz
  questionNumber={15}
  question="코드 리뷰와 정적 분석 도구의 차이를 설명한 것으로 옳지 않은 것은?"
  options={['코드 리뷰는 사람이 직접 코드를 읽고 논리적 오류나 설계 의도 위반을 찾는 활동이다', '정적 분석 도구는 프로그램을 실행하지 않고 코드 구조를 분석해 잠재적 결함을 찾는다', '정적 분석 도구는 코딩 표준 위반, 널 참조 가능성 같은 패턴화된 문제를 자동으로 찾는 데 유리하다', '코드 리뷰와 정적 분석은 서로 대체 관계이므로 두 가지를 함께 적용할 필요가 없다']}
  correctAnswer={4}
  explanation="코드 리뷰는 사람이 코드를 읽고 논리적 오류나 설계 의도 위반, 가독성 문제를 찾는 활동이고, 정적 분석 도구는 실행 없이 코드 구조를 분석해 코딩 표준 위반이나 널 참조 가능성 같은 패턴화된 문제를 자동으로 찾아줍니다. 두 방법은 찾아내는 결함의 성격이 다르므로 서로 보완적으로 함께 사용하는 것이 권장되며, 대체 관계로 보아 둘 중 하나만 쓰면 된다는 설명은 옳지 않습니다."
  optionExplanations={['옳은 설명입니다', '옳은 설명입니다', '옳은 설명입니다', '정답입니다. 코드 리뷰와 정적 분석은 보완적으로 함께 적용하는 것이 바람직합니다']}
/>

<Quiz
  questionNumber={16}
  question="입력 범위가 1 이상 100 이하인 정수를 받는 프로그램에서, 경계값 분석(boundary value analysis)에 따라 선정할 시험 값으로 가장 적절하지 않은 것은?"
  options={['0, 1, 2', '99, 100, 101', '50', '1, 100']}
  correctAnswer={3}
  explanation="경계값 분석은 유효 범위의 경계와 그 바로 안쪽·바깥쪽 값을 집중적으로 시험하는 기법입니다. 범위가 1 이상 100 이하이므로 하한 경계 주변인 0, 1, 2와 상한 경계 주변인 99, 100, 101이 대표적인 경계값 시험 케이스입니다. 50은 범위의 중간값으로 동등분할(equivalence partitioning)에서 대표값을 뽑을 때는 의미가 있지만, 경계값 분석이 노리는 경계 지점과는 거리가 멀어 경계값 분석의 대표 사례로 가장 적절하지 않습니다."
  optionExplanations={['하한 경계 0, 1, 2는 적절한 경계값 시험 케이스입니다', '상한 경계 99, 100, 101은 적절한 경계값 시험 케이스입니다', '정답입니다. 50은 범위의 중간값으로 경계값이 아니라 동등분할의 대표값에 해당합니다', '유효 범위의 양쪽 경계값이므로 적절한 경계값 시험 케이스입니다']}
/>

<Quiz
  questionNumber={17}
  question="일반적인 테스트 수준을 진행 순서대로 나열한 것은?"
  options={['단위 테스트 → 통합 테스트 → 시스템 테스트 → 인수 테스트', '인수 테스트 → 시스템 테스트 → 통합 테스트 → 단위 테스트', '통합 테스트 → 단위 테스트 → 인수 테스트 → 시스템 테스트', '시스템 테스트 → 통합 테스트 → 단위 테스트 → 인수 테스트']}
  correctAnswer={1}
  explanation="테스트는 일반적으로 가장 작은 단위인 모듈을 검사하는 단위 테스트에서 시작해, 모듈 간 연동을 검사하는 통합 테스트, 전체 시스템의 요구 충족 여부를 검사하는 시스템 테스트를 거쳐, 실제 사용자·고객 관점에서 인수 여부를 판단하는 인수 테스트로 마무리됩니다. 이 순서를 거꾸로 하면 하위 단위의 결함이 상위 테스트에서 뒤늦게 발견되어 수정 비용이 커집니다."
  optionExplanations={['정답입니다. 단위에서 인수까지 점진적으로 범위를 넓혀가는 순서입니다', '순서가 완전히 반대로 나열되어 있습니다', '통합이 단위보다 먼저 나올 수 없습니다', '시스템 테스트가 통합·단위 테스트보다 먼저일 수 없습니다']}
/>

<Quiz
  questionNumber={18}
  question="운영 중인 시스템에서 발견된 결함을 수정하는 유지보수 활동을 가리키는 용어는?"
  options={['적응 유지보수(adaptive maintenance)', '완전화 유지보수(perfective maintenance)', '교정 유지보수(corrective maintenance)', '예방 유지보수(preventive maintenance)']}
  correctAnswer={3}
  explanation="교정 유지보수는 운영 중 발견된 결함(잠재된 오류가 실제 실패로 드러난 경우)을 수정하는 활동입니다. 적응 유지보수는 운영 환경(하드웨어·운영체제 등) 변화에 맞춰 시스템을 수정하는 것, 완전화 유지보수는 성능 개선이나 기능 추가 요청을 반영하는 것, 예방 유지보수는 향후 결함 발생을 막기 위해 미리 코드를 개선하는 것을 뜻합니다."
  optionExplanations={['적응 유지보수는 환경 변화에 대응하는 활동으로 결함 수정과는 다릅니다', '완전화 유지보수는 기능 개선·추가에 관한 활동입니다', '정답입니다. 발견된 결함을 수정하는 활동이 교정 유지보수입니다', '예방 유지보수는 미래 결함을 예방하기 위한 사전 개선 활동입니다']}
/>

<Quiz
  questionNumber={19}
  question="형상관리(SCM)에서 베이스라인(baseline)에 대한 설명으로 가장 적절한 것은?"
  options={['공식 검토를 거쳐 승인되어 이후 변경 시 정식 절차를 거쳐야 하는 기준 산출물이다', '개발자 개인이 임시로 저장한 작업 중 파일을 의미한다', '테스트가 끝나지 않은 산출물은 모두 베이스라인으로 지정할 수 없다는 규칙이다', '베이스라인은 한 번 설정되면 프로젝트 종료까지 절대 변경할 수 없다']}
  correctAnswer={1}
  explanation="베이스라인은 공식 검토와 승인을 거쳐 형상항목으로 확정된 산출물로, 이후 이를 변경하려면 변경 통제 절차(변경 요청, 영향 분석, 승인)를 거쳐야 합니다. 개발자의 임시 저장 파일은 베이스라인이 아니며, 테스트 완료 여부가 베이스라인 지정의 절대 조건은 아닙니다. 또한 베이스라인은 변경이 금지된 것이 아니라 정식 절차를 통해 통제된 변경이 가능합니다."
  optionExplanations={['정답입니다. 베이스라인은 승인된 기준 산출물이며 변경 시 정식 통제 절차를 거칩니다', '개인의 임시 작업 파일은 형상항목으로 승인된 것이 아니므로 베이스라인이 아닙니다', '베이스라인 지정 조건은 승인 여부이지 테스트 완료 여부만으로 결정되지 않습니다', '베이스라인은 통제된 절차를 통해 변경될 수 있으며 절대 불변은 아닙니다']}
/>

<Quiz
  questionNumber={20}
  question="ISO/IEC 25010에서 정의하는 소프트웨어 품질 특성에 해당하지 않는 것은?"
  options={['기능 적합성(functional suitability)', '신뢰성(reliability)', '사용성(usability)', '수익성(profitability)']}
  correctAnswer={4}
  explanation="ISO/IEC 25010은 기능 적합성, 성능 효율성, 호환성, 사용성, 신뢰성, 보안성, 유지보수성, 이식성의 여덟 가지 품질 특성을 정의합니다. 수익성은 소프트웨어 제품의 품질 특성이 아니라 경영·재무적 개념으로, ISO/IEC 25010의 품질 특성 목록에 포함되지 않습니다."
  optionExplanations={['ISO/IEC 25010의 품질 특성입니다', 'ISO/IEC 25010의 품질 특성입니다', 'ISO/IEC 25010의 품질 특성입니다', '정답입니다. 수익성은 재무적 개념으로 ISO/IEC 25010의 품질 특성에 포함되지 않습니다']}
/>

<Quiz
  questionNumber={21}
  question="어떤 작업의 낙관적 소요시간이 2일, 최빈 소요시간이 5일, 비관적 소요시간이 8일일 때, PERT 기법의 기대 소요시간으로 옳은 것은?"
  options={['4일', '5일', '6일', '15일']}
  correctAnswer={2}
  explanation="PERT의 기대 소요시간 공식은 te = (O + 4M + P) / 6입니다. 여기서 O는 낙관적 시간, M은 최빈 시간, P는 비관적 시간입니다. 문제의 값을 대입하면 te = (2 + 4×5 + 8) / 6 = (2 + 20 + 8) / 6 = 30 / 6 = 5입니다. 따라서 기대 소요시간은 5일입니다."
  optionExplanations={['M에 4배가 아닌 3배 가중치를 적용해 (2+15+8)/6 ≈ 4.17을 반올림한 경우 나오는 오답입니다', '정답입니다. te = (2 + 4×5 + 8) / 6 = 30 / 6 = 5일입니다', '분모를 6이 아닌 5로 잘못 나누어 30 / 5 = 6이 된 경우입니다', '가중치 없이 O+M+P를 단순히 더한 값(2+5+8=15)을 그대로 답으로 착각한 경우입니다']}
/>

<Quiz
  questionNumber={22}
  question="다음 중 소프트웨어공학의 활동과 그 목적을 짝지은 것으로 옳지 않은 것은?"
  options={['요구공학 - 이해관계자의 요구를 도출하고 명세화한다', '형상관리 - 산출물의 변경 이력을 통제하고 일관성을 유지한다', '품질보증 - 개발 프로세스와 산출물이 표준·기준을 만족하는지 확인한다', '위험관리 - 프로젝트 산출물의 소스 코드를 재사용 가능한 컴포넌트로 변환한다']}
  correctAnswer={4}
  explanation="요구공학은 요구 도출과 명세화, 형상관리는 변경 통제와 일관성 유지, 품질보증은 프로세스·산출물의 표준 준수 확인을 목적으로 합니다. 위험관리는 프로젝트에 영향을 줄 수 있는 위험을 식별·분석하고 대응 계획을 세우는 활동이며, 소스 코드를 재사용 가능한 컴포넌트로 변환하는 것은 재공학(reengineering)이나 컴포넌트화 활동에 해당하므로 이 짝짓기는 옳지 않습니다."
  optionExplanations={['옳은 짝짓기입니다', '옳은 짝짓기입니다', '옳은 짝짓기입니다', '정답입니다. 코드를 재사용 가능한 컴포넌트로 변환하는 것은 재공학·컴포넌트화 활동이며 위험관리의 목적이 아닙니다']}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제 합격결정](https://bdes.nile.or.kr/nile/exam/nExam5_1.do)
- [ACM/IEEE SWEBOK v4 안내](https://www.computer.org/education/bodies-of-knowledge/software-engineering)
