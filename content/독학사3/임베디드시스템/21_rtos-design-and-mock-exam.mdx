---
title: '21. 독학사 3단계 임베디드시스템 예상기출문제'
description: '개요·아키텍처·메모리·주변장치·통신·소프트웨어·RTOS·설계를 아우르는 25문항 종합 예상기출과 계산형 전 과정 해설, 시간 배분 전략'
---

<Callout type="info">
  **이번 문서의 목표:** 이 파일의 25문항을 시간 배분 전략에 따라 풀고 해설로 채점하면, 독학사 3단계 임베디드시스템 시험의 전 범위(개요·아키텍처·메모리·주변장치·통신·소프트웨어·RTOS·설계)를 실전 감각으로 최종 점검할 수 있다.
</Callout>

<Callout type="warning">
  **출제기준 기반 재구성 문제임을 고지합니다.** 이 편의 문제는 국가평생교육진흥원 독학학위제 공식 출제기준(평가영역)과 01~18편에서 다룬 개념을 근거로 새로 구성한 예상 문제이며, 특정 회차의 실제 기출문제를 그대로 옮기거나 복원한 것이 아닙니다. 실제 시험의 문항 수·배점·시험 시간·난이도 분포는 매 공고마다 달라질 수 있으므로, 응시 전 반드시 국가평생교육진흥원 공식 시험안내에서 최신 공고를 확인하시기 바랍니다.
</Callout>

## 시험 시간 배분 전략

실전 시험에서 가장 흔한 실패 패턴은 계산형 문항 한두 개에 시간을 다 쓰고 뒤쪽의 쉬운 개념 문항을 놓치는 것이다. 25문항을 풀기 전에 먼저 문항 유형을 눈으로 훑어 **개념형**(정의·비교·옳고 그름 판단)과 **계산형**(수치를 직접 대입해 답을 구하는 문항)을 구분하는 습관을 들여야 한다. 개념형은 이미 아는 지식을 꺼내는 문제라 오래 붙잡고 있어도 정답률이 크게 오르지 않지만, 계산형은 공식만 정확히 세우면 시간을 들인 만큼 정답률이 올라간다. 그래서 **개념형은 짧게, 계산형은 여유 있게**가 기본 원칙이다.

> **쉽게 말하면:** 아는 문제는 빨리 넘기고, 계산 문제에 아낀 시간을 몰아준다.

이 문서의 25문항 중 수치를 직접 계산해야 하는 문항은 7번(인터럽트 지연), 12번(PWM 주파수), 13번(ADC 분해능), 15번(UART 보드레이트 오차), 21번(RM 스케줄 가능성), 24번(우선순위 역전 블로킹 시간)으로 총 6개다. 나머지 19개는 정의·비교·판단 중심의 개념형 문항이다.

| 문항 유형 | 문항 수 | 문항당 목표 시간 | 구간 합계 |
|---|---|---|---|
| 개념형(정의·비교·옳고 그름) | 19개 | 40초 | 약 12분 40초 |
| 계산형(수치 대입·공식 적용) | 6개 | 100초 | 약 10분 |
| 마킹 확인·재검토 | - | - | 5분 |
| **합계** | 25개 | - | **약 28분** |

이 표의 시간은 이 문서로 연습할 때 기준으로 삼을 **연습용 배분**이다. 실제 시험의 과목당 문항 수와 시험 시간은 공고마다 다르므로(공고 확인 필요), 실전에서는 "전체 시험 시간 ÷ 전체 문항 수"로 문항당 평균 시간을 먼저 계산한 뒤, 그 평균의 약 0.7배를 개념형에, 약 1.7배를 계산형에 배분하는 방식으로 이 표의 비율을 그대로 옮겨 쓰면 된다.

풀이 순서는 다음 세 단계로 권장한다.

<Steps>

### 1차 통과 — 아는 문제부터 확정

처음부터 순서대로 풀되, 10초 안에 답이 떠오르지 않는 문항은 표시만 해 두고 바로 다음으로 넘어간다. 이 단계의 목표는 시간을 버는 것이 아니라 **확실한 점수를 먼저 확보**하는 것이다.

### 2차 통과 — 계산형·표시 문항 집중 공략

1차에서 표시해 둔 문항과 계산형 문항(7, 12, 13, 15, 21, 24번)에 집중한다. 계산형은 반드시 "공식 → 대입 → 정리 → 답"의 순서를 손으로 직접 적으면서 푼다. 암산으로 끝내려다 부호나 자리수를 틀리는 것이 계산형 문항의 가장 흔한 실수다.

### 3차 통과 — 마킹 확인

마지막 5분은 새 문제를 풀지 않고, 답안지에 표기한 번호가 실제로 고른 보기 번호와 일치하는지 문항 순서대로 하나씩 손가락으로 짚어 가며 확인한다. "옳지 않은 것을 고르시오" 유형에서 반대로 "옳은 것"을 골라 표기하는 실수가 이 단계에서 가장 많이 잡힌다.

</Steps>

---

## 개요 (1~3번)

<Quiz
  questionNumber={1}
  question="다음 중 임베디드 시스템의 일반적 특징으로 옳지 않은 것은?"
  options={['정해진 특정 기능을 수행하도록 설계된 전용 컴퓨팅 시스템이다', '범용 데스크톱 운영체제와 동일한 자원 관리 정책을 사용해 확장성을 최우선으로 한다', '전력, 메모리, 비용 등 자원 제약을 고려해 설계된다', '하드웨어와 소프트웨어가 밀접하게 결합되어 동작한다']}
  correctAnswer={2}
  explanation="임베디드 시스템은 특정 기능 수행을 위해 하드웨어와 소프트웨어를 함께 설계하는 전용 시스템이며, 전력·메모리·비용 같은 자원 제약 안에서 동작해야 한다는 점이 범용 컴퓨터와 가장 크게 다르다. 확장성과 범용 자원 관리를 최우선으로 삼는 것은 데스크톱·서버용 범용 운영체제의 설계 철학이지 임베디드 시스템의 일반적 특징이 아니므로 2번이 옳지 않은 설명이다."
  optionExplanations={['옳은 설명이다. 임베디드 시스템은 하나 또는 소수의 정해진 기능만 수행하도록 만들어지는 전용 시스템이다', '옳지 않은 설명이므로 정답이다. 임베디드 시스템은 확장성보다 제한된 자원 안에서의 효율과 신뢰성을 우선한다', '옳은 설명이다. 전력, 메모리, 비용은 임베디드 시스템 설계를 좌우하는 핵심 제약이다', '옳은 설명이다. 임베디드 시스템은 특정 하드웨어를 전제로 소프트웨어를 만들기 때문에 두 요소가 밀접하게 결합된다']}
/>

<Quiz
  questionNumber={2}
  question="다음 중 hard real-time 시스템의 예로 가장 적절한 것은?"
  options={['온라인 동영상 스트리밍 앱', '자동차 에어백 제어 시스템', '웹 브라우저의 이미지 로딩', '음악 재생 애플리케이션의 볼륨 조절']}
  correctAnswer={2}
  explanation="hard real-time 시스템은 정해진 데드라인을 지키지 못하면 시스템 오동작이나 인명·재산 피해 같은 치명적 결과로 이어지는 시스템이다. 자동차 에어백은 충돌 감지 후 정해진 짧은 시간 안에 반드시 전개되어야 하며 그 시간을 넘기면 안전에 직결된 실패가 되므로 hard real-time의 대표적인 예다. 나머지 세 보기는 응답이 조금 늦어져도 품질이 저하될 뿐 치명적 실패로 이어지지 않는 soft real-time 또는 비실시간 성격의 응용이다."
  optionExplanations={['오답이다. 동영상 스트리밍은 버퍼링이 늘어나는 정도의 품질 저하만 발생하는 soft real-time에 가깝다', '정답이다. 데드라인을 놓치면 치명적 사고로 이어지는 hard real-time의 전형적 사례다', '오답이다. 이미지 로딩 지연은 사용자 경험 저하일 뿐 치명적 실패가 아니다', '오답이다. 볼륨 조절 반응이 약간 늦어도 시스템 안전에 영향이 없다']}
/>

<Quiz
  questionNumber={3}
  question="다음 중 임베디드 시스템 설계 제약에 대한 설명으로 옳은 것은?"
  options={['배터리로 동작하는 기기는 전력 소모보다 처리 속도 극대화가 항상 우선이다', '메모리 제약이 있는 시스템에서는 코드 크기와 자료구조 크기를 함께 고려해야 한다', '비용 제약은 대량 생산 제품에는 적용되지 않는다', '실시간 제약은 반응 속도가 아니라 평균 처리량만을 의미한다']}
  correctAnswer={2}
  explanation="임베디드 시스템은 대개 ROM/Flash와 SRAM의 용량이 고정되어 있으므로, 프로그램 코드의 크기(코드 메모리)와 실행 중 사용하는 변수·버퍼 등 자료구조의 크기(데이터 메모리)를 함께 계획해야 한다. 배터리 기기는 오히려 전력 소모를 최소화하는 것이 처리 속도보다 우선일 때가 많고, 비용 제약은 대량 생산 제품일수록 부품 단가 절감의 효과가 커지므로 더 중요해지며, 실시간 제약은 평균 처리량이 아니라 데드라인 안의 응답 여부를 의미하므로 나머지 보기는 모두 옳지 않다."
  optionExplanations={['오답이다. 배터리 기기는 처리 속도보다 저전력 설계가 우선인 경우가 많다', '정답이다. 코드 크기와 데이터 크기를 함께 고려해야 제한된 메모리 안에 프로그램이 들어간다', '오답이다. 비용 제약은 대량 생산 제품에서 부품 단가 절감 효과가 커지므로 오히려 더 중요하게 작용한다', '오답이다. 실시간 제약의 핵심은 데드라인 준수이지 평균 처리량이 아니다']}
/>

## 아키텍처 (4~7번)

<Quiz
  questionNumber={4}
  question="MCU(Micro Controller Unit)와 MPU(Micro Processor Unit)의 차이에 대한 설명으로 옳지 않은 것은?"
  options={['MCU는 CPU 코어와 메모리, 주변장치 컨트롤러를 하나의 칩에 통합한다', 'MPU는 일반적으로 외부에 별도의 메모리와 주변장치를 연결해 사용한다', 'MCU는 MPU에 비해 일반적으로 더 높은 범용 연산 성능과 대용량 운영체제 구동에 적합하다', 'MCU는 저전력, 저비용의 단순 제어 응용에 널리 사용된다']}
  correctAnswer={3}
  explanation="MCU는 CPU, 메모리, 주변장치 컨트롤러를 한 칩에 통합해 저전력·저비용의 단순 제어 응용에 적합하도록 만든 칩이고, MPU는 코어 연산 성능에 집중하고 메모리·주변장치를 외부 칩으로 확장해 리눅스 같은 범용 운영체제와 대용량 애플리케이션을 구동하는 데 적합하다. 따라서 범용 연산 성능과 대용량 운영체제 구동에 더 적합한 쪽은 MCU가 아니라 MPU이므로 3번 설명이 옳지 않다."
  optionExplanations={['옳은 설명이다. MCU는 CPU, 메모리, 주변장치를 하나의 칩에 통합한 원칩 솔루션이다', '옳은 설명이다. MPU는 외부에 메모리와 주변장치 칩을 별도로 연결해 시스템을 구성한다', '옳지 않은 설명이므로 정답이다. 범용 연산 성능과 대용량 OS 구동에 유리한 쪽은 MCU가 아니라 MPU다', '옳은 설명이다. MCU는 저전력·저비용의 단순 제어용으로 널리 쓰인다']}
/>

<Quiz
  questionNumber={5}
  question="RISC와 CISC 아키텍처를 비교한 설명으로 옳은 것은?"
  options={['RISC는 명령어 종류가 많고 실행 사이클이 명령어마다 크게 다르다', 'CISC는 고정 길이 명령어와 단일 사이클 실행을 지향한다', 'RISC는 명령어 집합을 단순화하고 대부분의 명령어를 짧고 균일한 사이클 내에 처리하도록 설계한다', 'ARM Cortex-M은 CISC 계열 아키텍처를 채택한 대표적인 MCU 코어다']}
  correctAnswer={3}
  explanation="RISC(Reduced Instruction Set Computer, 축소 명령어 집합 컴퓨터)는 명령어 종류를 줄이고 각 명령어의 실행 사이클을 짧고 균일하게 만들어 파이프라인 효율을 높이는 설계 철학이다. 반대로 CISC(Complex Instruction Set Computer, 복합 명령어 집합 컴퓨터)는 하나의 명령어가 복잡한 동작을 수행하도록 만들어 명령어 길이와 실행 사이클이 명령어마다 크게 달라진다. ARM Cortex-M 계열은 RISC 계열 아키텍처를 채택한 대표적인 MCU 코어이므로 1, 2, 4번은 설명이 서로 뒤바뀌어 있어 옳지 않다."
  optionExplanations={['오답이다. 명령어 종류가 많고 사이클이 명령어마다 다른 쪽은 RISC가 아니라 CISC의 특징이다', '오답이다. 고정 길이 명령어와 단일 사이클 실행을 지향하는 쪽은 CISC가 아니라 RISC다', '정답이다. RISC는 단순한 명령어 집합으로 짧고 균일한 실행 사이클을 지향한다', '오답이다. ARM Cortex-M은 CISC가 아니라 RISC 계열 아키텍처를 채택하고 있다']}
/>

<Quiz
  questionNumber={6}
  question="Cortex-M 프로세서가 인터럽트를 수락했을 때 일어나는 동작 순서로 옳은 것은?"
  options={['레지스터 스태킹 → 벡터 테이블에서 핸들러 주소 fetch → 핸들러 실행 → 언스태킹', '핸들러 실행 → 벡터 테이블 fetch → 레지스터 스태킹 → 언스태킹', '언스태킹 → 핸들러 실행 → 레지스터 스태킹 → 벡터 fetch', '벡터 fetch와 레지스터 스태킹은 Cortex-M에서 지원하지 않는 개념이다']}
  correctAnswer={1}
  explanation="Cortex-M은 인터럽트가 수락되면 먼저 현재 실행 상태를 복원할 수 있도록 주요 레지스터를 스택에 자동으로 밀어 넣는 스태킹을 수행하고, 그와 병행해 벡터 테이블에서 해당 예외 번호에 대응하는 핸들러 주소를 읽어 온 뒤, 핸들러를 실행하고, 핸들러가 끝나면 스택에 저장해 둔 레지스터 값을 복원하는 언스태킹으로 원래 실행 흐름에 복귀한다. 그러므로 스태킹과 벡터 fetch가 먼저 일어나고 핸들러 실행, 언스태킹이 뒤따르는 1번이 옳은 순서다."
  optionExplanations={['정답이다. 스태킹과 벡터 fetch가 먼저 일어난 뒤 핸들러가 실행되고 언스태킹으로 복귀한다', '오답이다. 핸들러 주소를 모르는 상태에서 핸들러를 먼저 실행할 수는 없다', '오답이다. 언스태킹은 핸들러 실행이 끝난 뒤에 일어나는 복귀 동작이다', '오답이다. 벡터 fetch와 레지스터 스태킹은 Cortex-M 예외 처리의 핵심 하드웨어 동작이다']}
/>

<Quiz
  questionNumber={7}
  question="시스템 클록이 50MHz이고 Cortex-M 코어의 고정 인터럽트 지연이 12사이클이라고 할 때, 클록 지연만으로 계산한 최소 인터럽트 응답 시간에 가장 가까운 값은?"
  options={['24ns', '120ns', '240ns', '480ns']}
  correctAnswer={3}
  explanation="공식은 지연 시간 = 지연 사이클 수 × 한 클록 주기이다. 클록 주기부터 구하면, 주기 = 1 ÷ 클록 주파수 = 1 ÷ 50,000,000Hz = 20ns다. 여기에 고정 지연 사이클 12를 곱하면 지연 시간 = 12 × 20ns = 240ns가 된다. 이는 파이프라인 재적재나 우선순위 판정 등 다른 지연 요인을 제외한, 클록 자체가 만드는 최소 지연이며 실제 응답 시간은 이보다 더 걸릴 수 있다."
  optionExplanations={['오답이다. 24ns는 12사이클이 아니라 약 1.2사이클에 해당하는 시간으로 계산이 틀렸다', '오답이다. 120ns는 6사이클에 해당하는 값이라 사이클 수를 절반으로 잘못 계산한 경우다', '정답이다. 12사이클 × 20ns(50MHz의 1주기) = 240ns다', '오답이다. 480ns는 24사이클에 해당하는 값으로 사이클 수를 두 배로 잘못 계산한 경우다']}
/>

## 메모리 (8~10번)

<Quiz
  questionNumber={8}
  question="하버드 구조와 폰노이만 구조를 비교한 설명으로 옳지 않은 것은?"
  options={['하버드 구조는 명령어 메모리와 데이터 메모리의 버스가 분리되어 있다', '폰노이만 구조는 명령어와 데이터가 같은 메모리 공간과 버스를 공유한다', '하버드 구조는 명령어 페치와 데이터 접근을 동시에 수행할 수 있어 구조적으로 유리하다', '대부분의 Cortex-M 기반 MCU는 순수 폰노이만 구조만 채택하여 명령어와 데이터 버스를 항상 완전히 공유한다']}
  correctAnswer={4}
  explanation="하버드 구조(Harvard architecture)는 명령어 버스와 데이터 버스를 분리해 두 접근을 동시에 진행할 수 있고, 폰노이만 구조(von Neumann architecture)는 명령어와 데이터가 같은 메모리와 버스를 공유해 구조는 단순하지만 동시 접근이 제한된다. Cortex-M 기반 MCU는 성능을 높이기 위해 명령어 버스와 데이터 버스를 분리한 수정 하버드 구조(modified Harvard architecture)를 널리 채택하고 있으며, 순수 폰노이만 구조만 사용한다는 설명은 사실과 다르므로 4번이 옳지 않은 설명이다."
  optionExplanations={['옳은 설명이다. 하버드 구조는 명령어 버스와 데이터 버스가 물리적으로 분리되어 있다', '옳은 설명이다. 폰노이만 구조는 명령어와 데이터가 같은 메모리 공간과 버스를 사용한다', '옳은 설명이다. 버스가 분리되어 있으면 명령어 페치와 데이터 접근을 같은 사이클에 겹쳐 수행할 수 있다', '옳지 않은 설명이므로 정답이다. 대부분의 Cortex-M MCU는 순수 폰노이만이 아니라 수정 하버드 구조를 채택한다']}
/>

<Quiz
  questionNumber={9}
  question="메모리 맵드 I/O(memory-mapped I/O)에 대한 설명으로 옳은 것은?"
  options={['주변장치 레지스터를 CPU의 일반 메모리 주소 공간에 배치해 메모리 접근 명령으로 제어한다', '입출력 전용 명령어 집합이 반드시 별도로 필요하다', '메모리 맵드 I/O를 사용하는 레지스터는 컴파일러 최적화의 영향을 절대 받지 않는다', 'ARM Cortex-M은 메모리 맵드 I/O를 지원하지 않는다']}
  correctAnswer={1}
  explanation="메모리 맵드 I/O는 주변장치의 제어·상태 레지스터를 CPU가 접근하는 메모리 주소 공간의 일부에 배치해, 별도의 입출력 전용 명령어 없이 일반적인 메모리 읽기·쓰기 명령만으로 주변장치를 제어하는 방식이다. Cortex-M을 포함한 ARM 계열은 이 방식을 표준으로 채택하고 있으며, 오히려 메모리 맵드 I/O 레지스터는 값이 하드웨어에 의해 예고 없이 바뀔 수 있어 컴파일러가 값을 캐시해 최적화하면 안 되므로 반드시 volatile로 선언해야 한다. 따라서 3번과 4번은 사실과 반대다."
  optionExplanations={['정답이다. 메모리 맵드 I/O는 주변장치 레지스터를 메모리 주소 공간에 배치해 메모리 접근 명령으로 다룬다', '오답이다. 메모리 맵드 I/O는 오히려 별도의 입출력 전용 명령어가 필요 없다는 것이 특징이다', '오답이다. 메모리 맵드 I/O 레지스터는 컴파일러 최적화의 영향을 받을 수 있어 volatile 선언이 필요하다', '오답이다. ARM Cortex-M은 메모리 맵드 I/O 방식을 표준으로 사용한다']}
/>

<Quiz
  questionNumber={10}
  question="Cortex-M의 표준 메모리 맵에서 주소가 낮은 영역부터 순서대로 나열한 것으로 옳은 것은?"
  options={['Code 영역 → SRAM 영역 → Peripheral 영역', 'Peripheral 영역 → SRAM 영역 → Code 영역', 'SRAM 영역 → Peripheral 영역 → Code 영역', '세 영역의 순서는 정해져 있지 않고 항상 무작위로 배치된다']}
  correctAnswer={1}
  explanation="Cortex-M의 일반적인 메모리 맵은 주소 0번지에 가까운 낮은 주소부터 Code 영역(플래시에 저장된 프로그램이 위치, 대략 0x00000000부터), 그다음 SRAM 영역(대략 0x20000000부터), 그다음 Peripheral 영역(대략 0x40000000부터)의 순서로 정의되어 있다. 이 배치는 ARM이 정의한 표준 메모리 맵으로, 실제 칩 제조사가 세부 주소를 조정하더라도 이 큰 순서 자체는 유지되는 경우가 대부분이므로 4번처럼 무작위라는 설명은 옳지 않다."
  optionExplanations={['정답이다. Code, SRAM, Peripheral 순서로 주소가 낮은 쪽부터 배치되는 것이 Cortex-M의 표준 메모리 맵이다', '오답이다. Peripheral 영역이 가장 먼저 온다는 설명은 표준 메모리 맵과 순서가 반대다', '오답이다. SRAM이 Code보다 먼저 온다는 설명은 표준 메모리 맵과 순서가 반대다', '오답이다. Cortex-M 표준 메모리 맵은 정해진 영역 순서를 가지고 있어 무작위가 아니다']}
/>

## 주변장치 (11~14번)

<Quiz
  questionNumber={11}
  question="GPIO 입력을 폴링 방식과 인터럽트 방식으로 처리할 때의 비교로 옳지 않은 것은?"
  options={['폴링 방식은 CPU가 주기적으로 상태를 검사하므로 이벤트 발생 즉시 반응하지 못할 수 있다', '인터럽트 방식은 이벤트 발생 시 CPU가 다른 작업을 수행하다가 즉시 전환할 수 있다', '폴링 방식은 항상 인터럽트 방식보다 CPU 사용률이 낮다', '인터럽트 방식은 우선순위와 벡터 테이블 관리가 필요하다']}
  correctAnswer={3}
  explanation="폴링 방식은 CPU가 주기적으로 반복문을 돌며 상태 레지스터를 직접 검사하기 때문에, 이벤트가 없는 동안에도 CPU가 검사 루프를 계속 실행해야 해 오히려 CPU 사용률이 높아지는 경우가 많다. 반면 인터럽트 방식은 이벤트가 없을 때 CPU가 다른 작업을 하거나 저전력 대기 상태에 들어갈 수 있어 평균 CPU 사용률이 더 낮은 경우가 일반적이다. 따라서 폴링이 항상 CPU 사용률이 낮다는 3번 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. 폴링은 검사 주기 사이에 발생한 이벤트에 즉시 반응하지 못한다', '옳은 설명이다. 인터럽트는 이벤트 발생 시 현재 작업을 중단하고 즉시 처리로 전환할 수 있다', '옳지 않은 설명이므로 정답이다. 폴링은 반복 검사 때문에 오히려 CPU 사용률이 더 높아지는 경우가 많다', '옳은 설명이다. 인터럽트 방식은 여러 인터럽트의 우선순위와 벡터 테이블 설정 관리가 필요하다']}
/>

<Quiz
  questionNumber={12}
  question="타이머 클록이 1MHz이고 자동 재적재 레지스터(ARR) 값이 999로 설정되어 있을 때 PWM 주파수는 얼마인가? (카운터는 0부터 시작하며 ARR+1번 카운트마다 한 주기가 완성된다고 가정)"
  options={['100Hz', '1kHz', '10kHz', '1MHz']}
  correctAnswer={2}
  explanation="공식은 PWM 주파수 = 타이머 클록 ÷ (ARR + 1)이다. 여기서 ARR(Auto-Reload Register, 자동 재적재 레지스터)은 카운터가 몇까지 세고 0으로 되돌아갈지를 정하는 값이다. 대입하면 주파수 = 1,000,000Hz ÷ (999 + 1) = 1,000,000Hz ÷ 1,000 = 1,000Hz가 된다. 즉 카운터가 0부터 999까지 총 1,000번 세는 데 걸리는 시간이 한 주기이므로 그 역수가 곧 PWM 주파수이며, 정리하면 1kHz다."
  optionExplanations={['오답이다. 100Hz가 되려면 ARR이 9999여야 한다', '정답이다. 1,000,000Hz ÷ 1,000 = 1,000Hz = 1kHz다', '오답이다. 10kHz가 되려면 ARR이 99여야 한다', '오답이다. 1MHz는 타이머 클록 자체의 값이지 분주 후의 PWM 주파수가 아니다']}
/>

<Quiz
  questionNumber={13}
  question="기준전압(Vref) 5V, 10비트 ADC를 사용할 때 1LSB(최소 분해 전압)에 가장 가까운 값은?"
  options={['약 0.49mV', '약 4.89mV', '약 9.77mV', '약 48.9mV']}
  correctAnswer={2}
  explanation="공식은 1LSB = Vref ÷ (2의 비트수 제곱 − 1)이다. 10비트이므로 2의 10제곱은 1024이고, 여기서 1을 뺀 1023이 ADC가 표현할 수 있는 최대 정수 코드 값이다. 대입하면 1LSB = 5V ÷ 1023 ≈ 0.004888V다. 이를 밀리볼트 단위로 정리하면 약 4.89mV가 되며, 이는 입력 전압이 이 값만큼 변할 때마다 ADC 출력 코드가 1씩 바뀐다는 뜻이다."
  optionExplanations={['오답이다. 0.49mV는 소수점 자리를 한 칸 잘못 옮긴 값이다', '정답이다. 5V ÷ 1023 ≈ 4.89mV다', '오답이다. 9.77mV는 9비트 ADC(2의 9제곱 −1 = 511)를 기준으로 계산했을 때 나오는 값이다', '오답이다. 48.9mV는 소수점 자리를 한 칸 잘못 옮긴 값이다']}
/>

<Quiz
  questionNumber={14}
  question="워치독 타이머(WDT)와 DMA(Direct Memory Access)에 대한 설명으로 옳은 것은?"
  options={['WDT는 소프트웨어가 정해진 시간 안에 리프레시하지 않으면 시스템을 리셋시켜 오동작을 감지하는 감시 장치다', 'DMA는 CPU가 모든 데이터 바이트를 직접 레지스터로 옮겨야만 동작한다', 'WDT는 데이터 전송 속도를 높이기 위한 주변장치이다', 'DMA를 사용하면 CPU 개입 없이는 어떤 데이터 전송도 시작할 수 없어 오히려 전체 오버헤드가 항상 늘어난다']}
  correctAnswer={1}
  explanation="WDT(Watchdog Timer, 워치독 타이머)는 소프트웨어가 정상적으로 동작하고 있다면 정해진 주기 안에 타이머를 리프레시(kick)하도록 설계해 두고, 만약 소프트웨어가 무한 루프나 정지 상태에 빠져 리프레시를 하지 못하면 타이머가 만료되어 시스템을 강제로 리셋시키는 감시 장치다. DMA(Direct Memory Access, 직접 메모리 접근)는 CPU의 개입 없이 메모리와 주변장치 사이에서 데이터를 직접 옮기는 하드웨어 기능으로, CPU는 전송 시작 설정만 해 두고 다른 작업을 계속할 수 있어 오히려 CPU 부담을 줄여 준다. 따라서 2, 3, 4번은 WDT와 DMA의 정의와 어긋난다."
  optionExplanations={['정답이다. WDT는 리프레시가 끊기면 시스템을 리셋시켜 오동작을 감지하는 안전장치다', '오답이다. DMA의 핵심은 CPU 개입 없이 데이터를 직접 옮기는 것이지, CPU가 매 바이트를 옮기는 것이 아니다', '오답이다. WDT는 전송 속도와 무관한 오동작 감시용 타이머다', '오답이다. DMA는 전송 시작 이후에는 CPU 개입 없이 동작해 오히려 CPU 오버헤드를 줄여 준다']}
/>

## 통신 (15~17번)

<Quiz
  questionNumber={15}
  question="시스템 클록 8MHz, 오버샘플링 16배 방식의 UART에서 목표 보드레이트 9600bps를 만들기 위해 분주값(BRR)을 정수로 반올림해 52로 설정했다. 이때 실제 보드레이트와 오차율로 가장 가까운 것은?"
  options={['실제 약 9615bps, 오차 약 0.16퍼센트', '실제 약 9600bps, 오차 0퍼센트', '실제 약 8800bps, 오차 약 8.3퍼센트', '실제 약 10000bps, 오차 약 4.2퍼센트']}
  correctAnswer={1}
  explanation="공식은 이상적인 분주값 = 시스템 클록 ÷ (오버샘플링 배수 × 목표 보드레이트)이다. 대입하면 이상적인 분주값 = 8,000,000 ÷ (16 × 9600) = 8,000,000 ÷ 153,600 ≈ 52.083이다. 레지스터에는 정수만 넣을 수 있으므로 반올림한 52를 사용한다. 이 값으로 실제 보드레이트를 역산하면 실제 보드레이트 = 시스템 클록 ÷ (오버샘플링 배수 × 분주값) = 8,000,000 ÷ (16 × 52) = 8,000,000 ÷ 832 ≈ 9615.4bps다. 오차율 = (실제값 − 목표값) ÷ 목표값 × 100 = (9615.4 − 9600) ÷ 9600 × 100 ≈ 0.16퍼센트로, UART가 일반적으로 허용하는 오차 범위(대략 2~3퍼센트 이내) 안에 들어와 통신에 문제가 없다."
  optionExplanations={['정답이다. 52를 대입해 역산하면 약 9615.4bps가 나오고 목표 9600bps 대비 오차는 약 0.16퍼센트다', '오답이다. 분주값이 정수로 반올림된 이상 오차가 정확히 0퍼센트가 되기는 어렵다', '오답이다. 8800bps는 분주값을 잘못 대입했을 때 나올 수 있는 값으로 계산 과정이 틀렸다', '오답이다. 10000bps와 4.2퍼센트 오차는 분주값을 반대로(작게) 대입했을 때 나오는 값이다']}
/>

<Quiz
  questionNumber={16}
  question="SPI와 I2C를 비교한 설명으로 옳지 않은 것은?"
  options={['SPI는 별도의 슬레이브 선택 신호선을 사용해 다중 슬레이브를 구분한다', 'I2C는 두 개의 신호선(SDA, SCL)만으로 다중 마스터와 다중 슬레이브 통신이 가능하다', 'SPI는 일반적으로 I2C보다 배선 수가 항상 더 적어 하드웨어가 단순하다', 'I2C는 각 슬레이브가 고유한 주소를 가지고 있어 소프트웨어로 슬레이브를 선택한다']}
  correctAnswer={3}
  explanation="SPI(Serial Peripheral Interface)는 클록선, 마스터에서 슬레이브로의 데이터선, 슬레이브에서 마스터로의 데이터선에 더해 슬레이브 하나마다 별도의 슬레이브 선택선을 추가로 배선해야 하므로, 슬레이브 수가 늘어날수록 배선 수도 함께 늘어난다. 반면 I2C(Inter-Integrated Circuit)는 슬레이브 수와 관계없이 SDA(데이터선)와 SCL(클록선) 단 두 개의 신호선만으로 여러 슬레이브, 심지어 여러 마스터까지 연결할 수 있다. 따라서 SPI의 배선 수가 I2C보다 항상 더 적다는 3번 설명은 사실과 반대다."
  optionExplanations={['옳은 설명이다. SPI는 슬레이브마다 별도의 선택 신호선을 두어 통신 대상을 구분한다', '옳은 설명이다. I2C는 SDA, SCL 두 선만으로 다중 마스터와 다중 슬레이브 구성을 지원한다', '옳지 않은 설명이므로 정답이다. 슬레이브 수가 늘어나면 SPI의 배선 수가 I2C보다 오히려 더 많아진다', '옳은 설명이다. I2C 슬레이브는 고유 주소를 가지고 있어 소프트웨어가 주소로 슬레이브를 선택한다']}
/>

<Quiz
  questionNumber={17}
  question="CAN(Controller Area Network)에 대한 설명으로 옳은 것은?"
  options={['CAN은 마스터-슬레이브 구조로만 동작하며 항상 하나의 마스터가 버스를 독점한다', 'CAN은 메시지 식별자(ID)를 이용한 비트 단위 중재로 우선순위가 높은 메시지가 버스를 선점하도록 한다', 'CAN은 오류 검출 기능이 없어 산업용이나 차량용으로는 부적합하다', 'CAN 통신 속도는 항상 UART보다 느리며 짧은 거리에서만 사용된다']}
  correctAnswer={2}
  explanation="CAN은 특정 마스터가 고정되어 있지 않은 멀티마스터 방식의 버스로, 여러 노드가 동시에 전송을 시도할 때 메시지 식별자(ID)의 비트 값을 비교하는 비트 단위 중재(bitwise arbitration) 과정을 거쳐, 우선순위가 더 높은(식별자 값이 더 낮은) 메시지가 버스 사용권을 얻고 나머지는 자동으로 전송을 양보한다. CAN은 CRC 검사와 다양한 오류 프레임을 통한 강력한 오류 검출 기능을 갖추고 있어 차량과 산업 현장의 잡음 환경에서도 널리 쓰이며, 통신 거리와 속도는 설정에 따라 UART보다 길거나 빠를 수도 있으므로 1, 3, 4번은 CAN의 실제 특성과 어긋난다."
  optionExplanations={['오답이다. CAN은 고정된 마스터가 없는 멀티마스터 방식의 버스다', '정답이다. CAN은 메시지 식별자의 비트 단위 중재로 우선순위가 높은 메시지가 버스를 선점한다', '오답이다. CAN은 CRC와 오류 프레임을 이용한 강력한 오류 검출 기능을 갖추고 있어 산업용·차량용으로 널리 쓰인다', '오답이다. CAN의 속도와 사용 거리는 설정과 응용에 따라 달라지며 항상 UART보다 느리다고 단정할 수 없다']}
/>

## 소프트웨어 (18~20번)

<Quiz
  questionNumber={18}
  question="임베디드 크로스 개발 환경에서 소스 코드가 실행 가능한 이미지가 되어 타깃 보드에서 동작하기까지의 일반적인 순서로 옳은 것은?"
  options={['크로스 컴파일 → 링킹(스타트업 코드 포함) → 이미지 생성과 플래싱 → 부트로더 실행 → 애플리케이션 점프', '부트로더 실행 → 크로스 컴파일 → 애플리케이션 점프 → 링킹', '이미지 플래싱 → 애플리케이션 점프 → 크로스 컴파일 → 링킹', '링킹 → 애플리케이션 점프 → 크로스 컴파일 → 부트로더 실행']}
  correctAnswer={1}
  explanation="개발 PC에서 타깃 MCU용 명령어로 소스를 크로스 컴파일한 뒤, 스타트업 코드와 라이브러리를 포함해 링킹으로 하나의 실행 이미지를 만들고, 이 이미지를 타깃 보드의 플래시 메모리에 기록(플래싱)한다. 전원이 켜지면 부트로더가 먼저 실행되어 필요한 초기화를 마친 뒤, 마지막으로 애플리케이션 코드로 실행 흐름을 넘긴다(점프). 이 순서를 벗어나면, 예를 들어 링킹 전에 실행부터 하려 하는 것처럼 애초에 실행 가능한 이미지 자체가 없는 상태가 되므로 나머지 보기는 순서가 성립하지 않는다."
  optionExplanations={['정답이다. 컴파일과 링킹으로 이미지를 만들고 플래싱한 뒤, 부트로더를 거쳐 애플리케이션으로 넘어가는 순서다', '오답이다. 크로스 컴파일이 끝나기 전에는 실행할 이미지 자체가 존재하지 않는다', '오답이다. 컴파일과 링킹 없이 플래싱할 이미지를 만들 수 없다', '오답이다. 컴파일과 링킹이 끝나기 전에는 부트로더가 넘겨줄 애플리케이션이 존재하지 않는다']}
/>

<Quiz
  questionNumber={19}
  question="다음 중 volatile 키워드가 반드시 필요한 상황으로 가장 적절한 것은?"
  options={['함수 내부에서만 사용되고 다른 곳에서 값이 바뀌지 않는 지역 변수', '컴파일러가 최적화를 위해 값을 레지스터에 캐시해도 무방한 상수', '인터럽트 서비스 루틴과 메인 루프가 함께 접근하는 하드웨어 상태 레지스터나 플래그 변수', '프로그램 시작 시 한 번만 초기화되고 이후 절대 읽지 않는 변수']}
  correctAnswer={3}
  explanation="volatile은 컴파일러에게 이 변수의 값이 컴파일러가 알 수 없는 방식(하드웨어나 다른 실행 흐름)으로 바뀔 수 있으니, 매번 실제 메모리에서 값을 다시 읽고 다시 쓰라고 지시하는 키워드다. 인터럽트 서비스 루틴이 세팅하고 메인 루프가 읽는 플래그 변수나, 하드웨어가 값을 바꾸는 상태 레지스터가 대표적인 경우다. volatile 없이 이런 변수를 다루면, 컴파일러가 값이 바뀌지 않는다고 착각해 레지스터에 캐시해 둔 옛날 값을 계속 사용하는 최적화를 적용할 수 있어, 메인 루프가 인터럽트가 바꾼 값을 영영 보지 못하는 버그가 생긴다."
  optionExplanations={['오답이다. 다른 곳에서 바뀌지 않는 지역 변수는 컴파일러가 자유롭게 최적화해도 안전하다', '오답이다. 값이 바뀌지 않는 상수는 volatile이 필요 없는 대표적인 경우다', '정답이다. ISR과 메인 루프가 함께 접근하는 변수는 volatile로 선언해야 최신 값을 보장받는다', '오답이다. 한 번만 쓰고 다시 읽지 않는 변수는 volatile이 필요한 상황이 아니다']}
/>

<Quiz
  questionNumber={20}
  question="인터럽트 서비스 루틴(ISR) 작성 원칙으로 옳지 않은 것은?"
  options={['ISR은 가능한 짧고 빠르게 실행되도록 작성한다', 'ISR 내부에서 시간이 오래 걸리는 연산이나 블로킹 함수 호출은 피한다', 'ISR과 공유하는 변수는 volatile로 선언하고 접근 구간을 임계구역으로 보호한다', 'ISR 안에서는 동적 메모리 할당과 복잡한 문자열 처리를 자유롭게 사용해도 된다']}
  correctAnswer={4}
  explanation="ISR은 다른 인터럽트나 태스크의 응답을 지연시키지 않도록 최대한 짧고 빠르게 실행되어야 하며, 시간이 오래 걸리는 연산이나 블로킹 함수 호출은 피하고 필요하면 플래그만 세운 뒤 실제 처리는 메인 루프나 별도 태스크로 넘기는 것이 원칙이다. 동적 메모리 할당은 실행 시간이 일정하지 않고 메모리 단편화나 힙 손상을 유발할 위험이 있어, 특히 ISR처럼 시간이 결정적이어야 하는 곳에서는 사용을 피해야 하므로 4번처럼 자유롭게 사용해도 된다는 설명은 옳지 않다."
  optionExplanations={['옳은 설명이다. ISR은 짧고 빠르게 끝내는 것이 기본 원칙이다', '옳은 설명이다. 오래 걸리는 연산이나 블로킹 호출은 다른 인터럽트 응답을 지연시킨다', '옳은 설명이다. 공유 변수는 volatile 선언과 임계구역 보호가 함께 필요하다', '옳지 않은 설명이므로 정답이다. 동적 메모리 할당과 복잡한 문자열 처리는 실행 시간이 불확실해 ISR에서 피해야 한다']}
/>

## RTOS (21~23번)

<Quiz
  questionNumber={21}
  question="주기(T)와 실행시간(C)이 각각 T1=20ms, C1=3ms, T2=50ms, C2=10ms, T3=100ms, C3=20ms인 세 태스크를 RM(Rate Monotonic) 스케줄링한다. Liu와 Layland의 충분조건(이용률 상한 검사)으로 스케줄 가능성을 판단하면?"
  options={['전체 이용률이 약 0.55이고 3태스크 상한 약 0.78보다 작으므로 스케줄 가능하다고 판단할 수 있다', '전체 이용률이 약 1.2로 상한을 초과해 스케줄 불가능하다', '이용률 계산과 무관하게 RM은 항상 스케줄 가능하다', '태스크 수가 3개이므로 이 조건으로는 판단할 수 없다']}
  correctAnswer={1}
  explanation="공식은 전체 이용률 U = 각 태스크의 (실행시간 ÷ 주기)를 모두 더한 값이고, 판단 기준은 U가 태스크 개수 n에 대한 상한 U_lub(n) = n × (2의 1/n제곱 − 1) 이하이면 스케줄 가능하다고 판단하는 것이다. 먼저 각 태스크의 이용률을 구하면 U1 = 3 ÷ 20 = 0.15, U2 = 10 ÷ 50 = 0.2, U3 = 20 ÷ 100 = 0.2이고, 이를 모두 더하면 전체 이용률 U = 0.15 + 0.2 + 0.2 = 0.55다. 다음으로 태스크 수 n=3에 대한 상한을 구하면 U_lub(3) = 3 × (2의 1/3제곱 − 1) ≈ 3 × (1.2599 − 1) = 3 × 0.2599 ≈ 0.7798이다. 0.55는 0.7798보다 작으므로, 이 충분조건에 의해 세 태스크는 RM 스케줄링으로 모든 데드라인을 지킬 수 있다고 판단한다. 다만 이 조건은 충분조건일 뿐 필요조건은 아니어서, 이 상한을 넘더라도 실제로는 스케줄 가능한 경우가 있을 수 있다는 점은 함정으로 자주 출제된다."
  optionExplanations={['정답이다. 이용률 0.55가 3태스크 상한 약 0.78보다 작아 스케줄 가능하다고 판단할 수 있다', '오답이다. 전체 이용률을 다시 더하면 0.15+0.2+0.2=0.55이지 1.2가 아니다', '오답이다. RM은 이용률이 상한을 크게 넘으면 스케줄 불가능해질 수 있어 항상 가능한 것은 아니다', '오답이다. Liu와 Layland의 이용률 상한 공식은 태스크 개수 n을 그대로 대입해 계산할 수 있다']}
/>

<Quiz
  questionNumber={22}
  question="EDF(Earliest Deadline First) 스케줄링에 대한 설명으로 옳은 것은?"
  options={['EDF는 정적 우선순위 스케줄링이며 우선순위는 실행 중 절대 바뀌지 않는다', '단일 프로세서에서 EDF는 전체 이용률이 1 이하이면 스케줄 가능하다는 필요충분조건을 가진다', 'EDF는 데드라인이 가장 늦게 남은 태스크에 가장 높은 우선순위를 부여한다', 'EDF는 RM보다 항상 이용률 상한이 낮아 더 보수적인 스케줄링 기법이다']}
  correctAnswer={2}
  explanation="EDF는 그 순간에 데드라인이 가장 임박한(가장 가까운) 태스크에 가장 높은 우선순위를 동적으로 부여하는 동적 우선순위 스케줄링 기법이다. 단일 프로세서 환경에서 EDF는 전체 이용률(각 태스크의 실행시간 ÷ 주기의 합)이 1 이하이기만 하면 반드시 모든 데드라인을 지킬 수 있고, 반대로 1을 넘으면 반드시 어떤 태스크는 데드라인을 놓친다는 필요충분조건(정확한 이론적 경계)을 가지고 있어, 이용률 상한이 100퍼센트에 가까운 RM보다 자원을 더 여유 있게 쓸 수 있는 기법이다. 따라서 1, 3, 4번은 EDF의 정의와 반대된다."
  optionExplanations={['오답이다. EDF는 정적이 아니라 데드라인 임박도에 따라 우선순위가 실시간으로 바뀌는 동적 우선순위 기법이다', '정답이다. 단일 프로세서 EDF는 이용률 1 이하가 스케줄 가능성의 필요충분조건이다', '오답이다. EDF는 데드라인이 가장 늦게 남은 태스크가 아니라 가장 임박한 태스크에 높은 우선순위를 준다', '오답이다. EDF의 이용률 상한(1)은 RM의 이용률 상한(n(2^(1/n)-1), 항상 1 이하)보다 오히려 더 높거나 같다']}
/>

<Quiz
  questionNumber={23}
  question="세마포어(semaphore)와 뮤텍스(mutex)를 비교한 설명으로 옳지 않은 것은?"
  options={['뮤텍스는 소유권 개념이 있어 잠근 태스크만 해제할 수 있는 것이 일반적이다', '세마포어는 카운트 값을 가져 여러 개의 자원을 관리하는 데 사용할 수 있다', '이진 세마포어와 뮤텍스는 모든 구현에서 완전히 동일하며 아무 차이가 없다', '뮤텍스는 상호 배제를 위한 임계구역 보호에 주로 사용된다']}
  correctAnswer={3}
  explanation="뮤텍스는 잠근 태스크만 해제할 수 있는 소유권 개념을 가지고 있고, 이 소유권 덕분에 우선순위 상속 같은 기법을 적용해 우선순위 역전 문제를 완화할 수 있다. 반면 세마포어는 소유권 개념이 없어 어떤 태스크든 값을 증가시켜 해제할 수 있고, 카운트 값을 0과 1이 아닌 여러 값으로 두면 여러 개의 동일 자원(예를 들어 버퍼 슬롯 여러 개)을 관리하는 데도 쓸 수 있다. 이진 세마포어와 뮤텍스는 값이 0과 1만 가진다는 점에서 겉모습은 비슷하지만, 소유권과 우선순위 상속 지원 여부라는 중요한 차이가 있어 완전히 동일하다고 볼 수 없으므로 3번이 옳지 않은 설명이다."
  optionExplanations={['옳은 설명이다. 뮤텍스는 잠근 태스크만 해제할 수 있는 소유권 개념을 가진다', '옳은 설명이다. 세마포어는 카운트 값으로 여러 개의 자원을 함께 관리할 수 있다', '옳지 않은 설명이므로 정답이다. 이진 세마포어와 뮤텍스는 소유권과 우선순위 상속 지원 여부에서 차이가 있다', '옳은 설명이다. 뮤텍스는 상호 배제를 통한 임계구역 보호에 주로 사용된다']}
/>

## 설계 (24~25번)

<Quiz
  questionNumber={24}
  question="우선순위가 낮은 태스크 L이 뮤텍스를 잡고 4ms 동안 임계구역을 실행 중이다. 그 사이 우선순위가 가장 높은 태스크 H가 같은 뮤텍스를 요청하며 대기하고, 중간 우선순위 태스크 M이 준비 상태가 되어 L을 선점할 수 있는 상황이다. 우선순위 상속(priority inheritance) 프로토콜을 적용하면 H의 최대 블로킹 시간은 어떻게 되는가?"
  options={['M이 L을 계속 선점할 수 있어 H의 블로킹 시간이 무한정 늘어난다', 'L의 우선순위가 H 수준으로 일시 상승해 M이 L을 선점하지 못하므로 블로킹 시간은 L의 임계구역 실행 시간인 4ms 이내로 제한된다', '우선순위 상속을 적용해도 M의 선점을 막을 방법이 없어 효과가 없다', '우선순위 상속은 H의 우선순위를 낮추는 방식으로 문제를 해결한다']}
  correctAnswer={2}
  explanation="우선순위 상속이 없다면, H가 뮤텍스를 기다리는 동안 중간 우선순위 M이 준비 상태가 될 때마다 L을 계속 선점해 L의 임계구역 실행이 끝없이 늦어지고, 그 결과 H의 대기 시간도 예측할 수 없이 길어지는 무한정 우선순위 역전이 발생한다. 우선순위 상속 프로토콜은 L이 H가 필요로 하는 뮤텍스를 쥐고 있는 동안 L의 우선순위를 일시적으로 H와 같은 수준까지 끌어올려, M이 더 이상 L보다 우선순위가 높지 않게 만들어 L을 선점하지 못하게 한다. 그 결과 H가 기다려야 하는 최대 시간은 M의 개입 없이 L이 임계구역을 실행하는 데 걸리는 시간, 즉 이 문제에서는 4ms로 제한된다."
  optionExplanations={['오답이다. 우선순위 상속을 적용하면 M의 선점이 차단되어 블로킹 시간이 무한정 늘어나지 않는다', '정답이다. L의 우선순위가 일시적으로 H 수준까지 올라가 M의 선점을 막아 블로킹 시간이 4ms로 제한된다', '오답이다. 우선순위 상속은 정확히 M의 선점을 막기 위해 고안된 기법이다', '오답이다. 우선순위 상속은 H가 아니라 뮤텍스를 쥔 L의 우선순위를 일시적으로 올리는 방식이다']}
/>

<Quiz
  questionNumber={25}
  question="JTAG와 SWD, 그리고 임베디드 저전력 설계 기법에 대한 설명으로 옳지 않은 것은?"
  options={['JTAG는 여러 개의 신호선(TCK, TMS, TDI, TDO 등)을 사용하는 표준 디버그와 바운더리 스캔 인터페이스다', 'SWD는 두 개의 신호선만으로 디버깅과 프로그래밍이 가능해 핀 수가 제한된 소형 MCU에 유리하다', '클록 게이팅, 저전력 모드(슬립, 딥슬립) 진입, 사용하지 않는 주변장치의 클록 차단은 대표적인 저전력 설계 기법이다', 'JTAG와 SWD는 디버깅 용도로만 사용되며 정상 동작 시의 저전력 설계와는 아무 관계가 없다']}
  correctAnswer={4}
  explanation="JTAG(Joint Test Action Group)는 TCK, TMS, TDI, TDO 등 여러 신호선을 사용해 디버깅뿐 아니라 바운더리 스캔을 통한 보드 검사까지 지원하는 표준 인터페이스이고, SWD(Serial Wire Debug)는 클록선과 데이터선 단 두 개만으로 같은 디버깅·프로그래밍 기능을 제공해 핀 수가 부족한 소형 MCU에서 널리 쓰인다. 저전력 설계에서는 사용하지 않는 클록 도메인을 꺼 두는 클록 게이팅, 슬립·딥슬립 같은 저전력 모드 진입, 미사용 주변장치의 클록 차단이 대표적인 기법이며, 개발 단계에서 디버그 인터페이스가 계속 연결되어 있으면 저전력 모드 진입이 제한되거나 디버그 회로 자체가 추가 전력을 소모할 수 있어 JTAG와 SWD도 저전력 설계와 무관하지 않으므로 4번이 옳지 않은 설명이다."
  optionExplanations={['옳은 설명이다. JTAG는 여러 신호선을 사용하는 표준 디버그와 바운더리 스캔 인터페이스다', '옳은 설명이다. SWD는 두 개의 신호선만으로 디버깅과 프로그래밍을 지원해 소형 MCU에 유리하다', '옳은 설명이다. 클록 게이팅, 저전력 모드 진입, 미사용 주변장치 클록 차단은 대표적인 저전력 설계 기법이다', '옳지 않은 설명이므로 정답이다. 디버그 인터페이스가 연결된 상태는 저전력 모드 진입을 제한하거나 추가 전력을 소모시킬 수 있어 저전력 설계와 무관하지 않다']}
/>

## 핵심 정리

- 이 25문항은 국가평생교육진흥원 공식 출제기준과 01~18편 개념을 근거로 재구성한 예상 문제이며, 실제 기출을 그대로 옮긴 것이 아니다.
- 개념형 19문항은 문항당 40초, 계산형 6문항(7, 12, 13, 15, 21, 24번)은 문항당 100초를 기준으로 시간을 배분하고, 마지막 5분은 마킹 확인에 쓴다.
- 계산형 문항은 항상 "공식 → 대입 → 정리 → 답"의 순서로 손으로 직접 풀어야 실수를 줄일 수 있다.
- "옳지 않은 것을 고르시오" 유형에서는 정답 보기가 실제로 틀린 진술인지, 나머지 세 보기가 정말 옳은 진술인지 각각 따로 확인해야 한다.
- RTOS 스케줄링(RM 이용률 상한, EDF 이용률 1 조건)과 우선순위 역전·상속은 이 과목에서 계산과 개념이 함께 얽히는 대표적인 고난도 영역이므로 21, 22, 24번 해설을 반복해 읽어 둔다.

## 정답 위치 문항별 대조

작성 후 모든 문항의 정답을 다시 계산·재확인하고, correctAnswer로 표기한 번호가 실제로 옳은(또는 "옳지 않은 것을 고르시오" 유형에서는 틀린) 보기의 위치와 일치하는지 아래 표로 하나씩 대조했다.

| 문항 | 영역 | 유형 | correctAnswer | 정답 보기 요지 | 대조 결과 |
|---|---|---|---|---|---|
| 1 | 개요 | 옳지 않은 것 | 2 | 확장성 최우선은 임베디드가 아니라 범용 OS 특징 | 일치 |
| 2 | 개요 | 옳은 것 | 2 | 에어백은 데드라인 미준수 시 치명적인 hard real-time | 일치 |
| 3 | 개요 | 옳은 것 | 2 | 코드 크기와 데이터 크기를 함께 고려해야 함 | 일치 |
| 4 | 아키텍처 | 옳지 않은 것 | 3 | 범용 연산·대용량 OS는 MCU가 아니라 MPU가 유리 | 일치 |
| 5 | 아키텍처 | 옳은 것 | 3 | RISC는 단순한 명령어로 짧고 균일한 사이클 지향 | 일치 |
| 6 | 아키텍처 | 옳은 순서 | 1 | 스태킹·벡터 fetch → 핸들러 실행 → 언스태킹 | 일치 |
| 7 | 아키텍처 | 계산 | 3 | 12사이클 × 20ns = 240ns | 일치 |
| 8 | 메모리 | 옳지 않은 것 | 4 | Cortex-M 대부분은 순수 폰노이만이 아니라 수정 하버드 | 일치 |
| 9 | 메모리 | 옳은 것 | 1 | 레지스터를 메모리 주소 공간에 배치해 메모리 명령으로 제어 | 일치 |
| 10 | 메모리 | 옳은 순서 | 1 | Code → SRAM → Peripheral | 일치 |
| 11 | 주변장치 | 옳지 않은 것 | 3 | 폴링이 항상 CPU 사용률이 낮은 것은 아님 | 일치 |
| 12 | 주변장치 | 계산 | 2 | 1,000,000 ÷ 1,000 = 1kHz | 일치 |
| 13 | 주변장치 | 계산 | 2 | 5V ÷ 1023 ≈ 4.89mV | 일치 |
| 14 | 주변장치 | 옳은 것 | 1 | WDT는 리프레시 미수신 시 시스템 리셋 | 일치 |
| 15 | 통신 | 계산 | 1 | 실제 약 9615bps, 오차 약 0.16퍼센트 | 일치 |
| 16 | 통신 | 옳지 않은 것 | 3 | 슬레이브가 늘수록 SPI 배선이 I2C보다 많아짐 | 일치 |
| 17 | 통신 | 옳은 것 | 2 | 식별자 비트 단위 중재로 우선순위 높은 메시지가 선점 | 일치 |
| 18 | 소프트웨어 | 옳은 순서 | 1 | 컴파일 → 링킹 → 플래싱 → 부트로더 → 애플리케이션 점프 | 일치 |
| 19 | 소프트웨어 | 옳은 것 | 3 | ISR·메인 루프 공유 변수는 volatile 필요 | 일치 |
| 20 | 소프트웨어 | 옳지 않은 것 | 4 | ISR에서 동적 메모리 할당은 피해야 함 | 일치 |
| 21 | RTOS | 계산 | 1 | 이용률 0.55 〈 3태스크 상한 약 0.78 | 일치 |
| 22 | RTOS | 옳은 것 | 2 | 단일 프로세서 EDF는 이용률 1 이하가 필요충분조건 | 일치 |
| 23 | RTOS | 옳지 않은 것 | 3 | 이진 세마포어와 뮤텍스는 소유권 등에서 차이 있음 | 일치 |
| 24 | 설계 | 계산 | 2 | 우선순위 상속으로 블로킹 시간이 4ms 이내로 제한 | 일치 |
| 25 | 설계 | 옳지 않은 것 | 4 | 디버그 인터페이스도 저전력 설계와 무관하지 않음 | 일치 |

## 참고 자료

- [FreeRTOS documentation](https://www.freertos.org/Documentation/00-Overview)
- [FreeRTOS Reference Manual](https://www.freertos.org/media/2018/FreeRTOS_Reference_Manual_V10.0.0.pdf)
- [Cortex-M4 Technical Reference Manual](https://documentation-service.arm.com/static/5f19da2a20b7cf4bc524d99a)
- [국가평생교육진흥원 독학학위제 - 시험안내](https://bdes.nile.or.kr/nile/exam/nExam2_1.do)
