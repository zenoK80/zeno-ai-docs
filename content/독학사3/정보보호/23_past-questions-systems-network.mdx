---
title: '23. 기출·예상문제: 시스템·네트워크·관리체계·위험분석'
description: '애플리케이션·DB 보안, 네트워크 보안 장비·프로토콜, 정보보호 관리체계와 위험분석 영역을 국가평생교육진흥원 출제기준(평가영역)에 근거해 재구성한 예상 문항 20개로 점검한다'
---

<Callout type="warning">
  **이 편은 실제 기출 문제가 아닙니다.** 정보보호학 3단계 관련 `reference/` 기출 원문은 확보되어 있지 않습니다. 아래 20개 문항은 국가평생교육진흥원 독학학위제 정보보호학 3단계 **출제기준(평가영역)** 의 "시스템 및 네트워크 보안", "정보보호 관리 및 관련 법제" 영역을 근거로 **새로 구성한 예상 문항**입니다. 문장을 암기하기보다 개념과 구조 자체를 이해하는 데 써야 합니다.

  회차별 실제 출제 문항 수·배점·시간은 매 공고마다 다를 수 있으므로, 응시 전 국가평생교육진흥원 공고 원문을 반드시 확인하십시오.
</Callout>

## 이 편이 다루는 범위와 접근법

이 편은 09편(애플리케이션·웹·DB 보안 기본 구조), 12편(데이터베이스 접근통제와 권한관리), 13편(정보보호 관리체계와 정책·조직), 17편(네트워크 보안 기본 개념과 장비), 19편(위험분석·평가·대응)을 하나로 묶어 점검합니다. 이 영역은 응시자가 다루는 대상이 애플리케이션·DB·네트워크·관리체계로 넓게 퍼져 있어, 문제를 풀 때 **지금 묻는 것이 어느 층(layer)의 이야기인지**를 먼저 확인하는 습관이 중요합니다.

1. **구조·계층형** — OSI 7계층, ISMS 관리과정 5단계처럼 여러 단계·계층에 개념을 배치하는 문제입니다.
2. **장비·기능 비교형** — 방화벽·IDS·IPS·VPN처럼 역할이 비슷해 보이는 보안 장비의 **탐지냐 차단이냐, 어느 계층에서 동작하느냐**를 구분합니다.
3. **계산·응용형** — 위험분석 계산(다른 숫자로 ALE 재계산), SQL Injection 등 취약점의 발생 원리를 코드 수준에서 판단하는 문제입니다.

이미 21·22편에서 풀이 습관을 익혔다면, 이 편은 같은 습관을 시스템·네트워크·관리체계 영역에 적용하는 연습입니다.

## 마무리 복습

### 유형 1 — 애플리케이션·웹·DB 보안 기본 구조

**이 유형은 이렇게 접근한다:** 쿠키·세션·SQL Injection 문제는 "이 데이터가 어디에 저장되는가", "이 입력값이 왜 코드처럼 실행되는가"를 그림으로 그려 보면 쉽게 풀립니다.

<Quiz
  questionNumber={1}
  question="웹 애플리케이션의 쿠키(cookie)와 세션(session)에 대한 설명으로 옳지 않은 것은?"
  options={['① 쿠키는 클라이언트(브라우저) 측에 저장되는 작은 데이터 조각으로, 서버가 Set-Cookie 응답 헤더로 내려보낸다', '② 세션은 서버 측에 사용자별 상태 정보를 저장하고, 클라이언트는 그 세션을 식별하는 세션 ID만 쿠키 등으로 보관한다', '③ 세션 방식은 민감한 상태 정보 자체를 서버에 두므로 쿠키에 민감정보를 직접 저장하는 방식보다 상대적으로 안전하다', '④ 쿠키에는 세션 ID 외의 정보를 담을 수 없으며 쿠키의 유일한 용도는 세션 유지다']}
  correctAnswer={4}
  explanation="쿠키가 클라이언트 측에 저장된다는 것, 세션이 서버 측에 상태를 두고 클라이언트는 세션 ID만 보관한다는 것, 세션 방식이 쿠키에 민감정보를 직접 담는 것보다 안전하다는 것은 모두 옳은 설명이다. 문제는 4번이다. 쿠키(cookie)는 세션 ID 외에도 사용자 언어 설정, 장바구니 정보, 자동 로그인 토큰, 광고 추적용 식별자 등 **다양한 목적의 데이터를 담을 수 있는 범용 저장 수단**이다. 세션 유지가 대표적인 용도 중 하나일 뿐, 유일한 용도라고 한정하는 것은 쿠키의 실제 활용 범위와 다르다."
  optionExplanations={['쿠키가 클라이언트 측에 저장되고 Set-Cookie 헤더로 전달된다는 것을 정확히 설명한 옳은 문장이다.', '세션의 서버 측 저장 구조와 세션 ID 전달 방식을 정확히 설명한 옳은 문장이다.', '민감정보를 서버에 두는 세션 방식이 쿠키 직접 저장보다 안전하다는 것을 정확히 설명한 옳은 문장이다.', '쿠키는 세션 ID뿐 아니라 설정값, 자동 로그인 토큰 등 다양한 데이터를 담는 범용 저장 수단이므로 용도를 세션 유지로 한정하는 것은 옳지 않다.']}
/>

<Quiz
  questionNumber={2}
  question="다음 SQL 조회 코드에서 사용자 입력값 userId를 문자열 그대로 이어 붙여 쿼리를 만든다. SELECT * FROM members WHERE id = 'userId' AND pw = 'inputPw'; 공격자가 userId 입력란에 ' OR '1'='1 을 입력했을 때 발생하는 문제로 가장 적절한 것은?"
  options={['① 입력값에 특수문자가 포함되어 있어 쿼리 자체가 문법 오류로 실행되지 않는다', '② 조건절이 항상 참이 되도록 쿼리 구조가 바뀌어, 비밀번호 없이도 조건을 통과해 부적절한 데이터 조회나 인증 우회가 발생할 수 있다', '③ 데이터베이스 서버의 물리적인 저장 공간이 부족해지는 문제가 발생한다', '④ 이 입력은 문자열 데이터로만 처리되므로 쿼리 구조에는 어떤 영향도 주지 않는다']}
  correctAnswer={2}
  explanation="SQL Injection(SQL 삽입 공격)은 사용자 입력값을 검증·이스케이프 없이 SQL 쿼리 문자열에 그대로 이어 붙일 때 발생한다. 예시 쿼리에 작은따옴표로 문자열을 끊고 OR '1'='1'라는 항상 참인 조건을 삽입하면, 원래 의도한 id 비교 조건과 무관하게 WHERE 절 전체가 참이 되어 모든 레코드가 조회 조건을 통과하게 된다. 이는 인증 우회, 정보 노출, 심하면 데이터 변조·삭제로까지 이어질 수 있는 대표적인 웹 애플리케이션 취약점(OWASP Top 10에도 포함)이다. 1번은 특수문자가 오히려 SQL 문법을 조작하는 데 쓰인다는 점을 놓쳤고, 3번은 이 공격과 무관한 결과이며, 4번은 SQL Injection의 핵심 원리(입력값이 쿼리 구조 자체를 바꾼다)를 부정하는 오답이다."
  optionExplanations={['특수문자가 있다고 무조건 문법 오류가 나는 것이 아니라, 오히려 그 특수문자가 쿼리 구조를 의도적으로 바꾸는 데 악용된다.', "OR '1'='1'이 삽입되면 WHERE 절이 항상 참이 되어 인증·조회 조건을 우회하게 되므로 이 설명이 SQL Injection의 실제 위험을 정확히 짚었다.", 'SQL Injection은 저장 공간 문제가 아니라 쿼리의 논리 구조를 조작하는 공격이므로 이 설명은 관련이 없다.', '입력값이 문자열 리터럴을 끊고 쿼리 구조 자체를 바꾸는 것이 SQL Injection의 핵심 원리이므로 영향이 없다는 설명은 틀렸다.']}
/>

<Quiz
  questionNumber={3}
  question="입력 검증(input validation)에 대한 설명으로 옳지 않은 것은?"
  options={['① 화이트리스트(허용목록) 방식은 허용된 형식·값만 통과시키고 나머지는 모두 거부하는 방식이다', '② 블랙리스트(차단목록) 방식은 알려진 위험 패턴만 나열해 차단하므로, 목록에 없는 새로운 공격 패턴에는 취약할 수 있다', '③ 클라이언트(브라우저) 측 자바스크립트 검증만으로도 서버 측 검증 없이 충분한 보안을 확보할 수 있다', '④ SQL Injection 방어에는 입력값을 문자열에 직접 이어 붙이는 대신 준비된 문(Prepared Statement)이나 파라미터 바인딩을 사용하는 것이 권장된다']}
  correctAnswer={3}
  explanation="화이트리스트·블랙리스트 방식의 정의와 준비된 문(Prepared Statement)을 통한 SQL Injection 방어 방법은 모두 옳은 설명이다. 문제는 3번이다. 클라이언트 측 자바스크립트 검증은 사용자 경험을 개선하는 데는 유용하지만, 공격자는 브라우저의 개발자 도구나 별도의 HTTP 요청 도구를 이용해 클라이언트 측 검증을 손쉽게 우회하고 서버에 임의의 값을 직접 전송할 수 있다. 따라서 **서버 측 검증이 반드시 함께 이루어져야** 하며, 클라이언트 측 검증만으로 충분하다는 서술은 웹 보안의 기본 원칙과 어긋난다."
  optionExplanations={['화이트리스트 방식의 정의(허용된 것만 통과)를 정확히 설명한 옳은 문장이다.', '블랙리스트 방식의 한계(신규 패턴에 취약)를 정확히 설명한 옳은 문장이다.', '클라이언트 측 검증은 공격자가 쉽게 우회할 수 있어 서버 측 검증이 반드시 필요하다. 클라이언트 검증만으로 충분하다는 것은 옳지 않다.', 'Prepared Statement·파라미터 바인딩이 SQL Injection의 표준 방어 기법이라는 것을 정확히 설명한 옳은 문장이다.']}
/>

### 유형 2 — 데이터베이스 접근통제와 권한관리

**이 유형은 이렇게 접근한다:** DB 보안 문항은 "누가 원본 데이터를 볼 수 있는가"와 "누가 가공된 데이터만 볼 수 있는가"를 구분하는 데서 출발합니다. 뷰·마스킹·암호화는 각각 다른 층에서 원본 노출을 막습니다.

<Quiz
  questionNumber={4}
  question="데이터베이스의 뷰(View), 마스킹(Masking), 암호화(Encryption)를 이용한 개인정보 보호 기법에 대한 설명으로 옳지 않은 것은?"
  options={['① 뷰는 특정 컬럼이나 행만 선별해 보여주는 가상의 테이블로, 사용자가 원본 테이블 전체 구조에 직접 접근하지 못하게 제한할 수 있다', '② 마스킹은 주민등록번호의 뒷자리를 별표로 가리는 것처럼 원본 값의 일부를 가려서 보여주되 원본 데이터 자체는 데이터베이스에 그대로 남아 있다', '③ 암호화는 저장된 원본 값 자체를 암호문으로 바꾸어 두는 방식으로, 올바른 키가 없으면 원본을 복원할 수 없다', '④ 마스킹된 데이터는 이미 암호화와 동일한 수준의 보안을 제공하므로 마스킹만 적용하면 별도로 암호화할 필요가 없다']}
  correctAnswer={4}
  explanation="뷰·마스킹·암호화의 정의는 1~3번 모두 옳다. 문제는 4번이다. 마스킹(masking)은 **화면에 표시되는 값만 가리는 조치**이며, 데이터베이스에 저장된 원본 값 자체는 변경되지 않고 그대로 남아 있다. 반면 암호화(encryption)는 저장된 값 자체를 수학적으로 변환해 키 없이는 복원이 불가능하게 만든다. 즉 마스킹은 표시 계층의 보호이고 암호화는 저장 계층의 보호로, 보호하는 지점과 강도가 다르다. 마스킹만으로는 DB 서버에 직접 접근하거나 백업 파일이 유출되는 경우 원본이 그대로 노출되므로, 마스킹이 암호화와 동일한 수준의 보안을 제공한다고 볼 수 없다."
  optionExplanations={['뷰가 원본 테이블 구조를 감추고 선별된 데이터만 노출하는 방식이라는 것을 정확히 설명한 옳은 문장이다.', '마스킹이 화면 표시값만 가리고 원본은 그대로 저장되어 있다는 것을 정확히 설명한 옳은 문장이다.', '암호화가 저장된 원본 값 자체를 변환해 키 없이 복원 불가능하게 만든다는 것을 정확히 설명한 옳은 문장이다.', '마스킹은 표시 계층 보호에 그치고 원본이 DB에 그대로 남아 있어 저장 계층 보호인 암호화와 보안 수준이 다르므로, 마스킹만으로 암호화를 대체할 수 있다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={5}
  question="데이터베이스 감사(audit)와 로깅에 대한 설명으로 옳지 않은 것은?"
  options={['① 감사 로그는 누가, 언제, 어떤 데이터에, 어떤 작업을 했는지를 기록해 사후 추적성(accountability)을 확보하는 수단이다', '② 감사 로그가 변조되면 추적성 자체가 무력화되므로 로그의 무결성 보호도 함께 고려해야 한다', '③ DBA(데이터베이스 관리자)와 같이 강력한 권한을 가진 계정의 활동은 오히려 더 철저히 감사 대상에 포함해야 한다', '④ 감사 로그를 남기는 것은 성능에 어떠한 영향도 주지 않으므로 모든 테이블의 모든 작업을 예외 없이 상세하게 기록하는 것이 항상 최선이다']}
  correctAnswer={4}
  explanation="감사 로그의 목적(추적성 확보), 로그 무결성 보호의 필요성, 강력한 권한을 가진 계정일수록 더 철저히 감사해야 한다는 것은 모두 옳은 설명이다. 문제는 4번이다. 감사 로깅은 모든 접근·작업을 기록하기 위해 추가 연산과 저장 공간을 소모하므로 **실제로 데이터베이스 성능에 영향을 준다.** 따라서 실무에서는 위험도가 높은 테이블·작업(개인정보 테이블, 관리자 권한 작업 등)을 우선순위로 선별해 감사 범위를 설계하는 것이 일반적이며, 무조건 모든 것을 상세히 기록하는 것이 항상 최선이라고 단정할 수 없다. 성능과 추적성 확보 사이의 균형을 고려한 설계가 필요하다."
  optionExplanations={['감사 로그의 목적(누가·언제·무엇을 했는지 기록해 추적성 확보)을 정확히 설명한 옳은 문장이다.', '로그가 변조되면 추적성이 무력화되므로 무결성 보호가 필요하다는 것을 정확히 설명한 옳은 문장이다.', '권한이 강할수록 오남용 위험이 크므로 더 철저히 감사해야 한다는 것을 정확히 설명한 옳은 문장이다.', '감사 로깅은 실제로 성능 부담을 유발하므로 무조건 모든 것을 상세히 기록하는 것이 항상 최선은 아니며, 위험도 기반으로 범위를 설계하는 것이 일반적이다.']}
/>

### 유형 3 — 정보보호 관리체계(ISMS)와 조직

**이 유형은 이렇게 접근한다:** ISMS 관리과정은 계획(Plan) → 실행(Do) → 점검(Check) → 개선(Act)이라는 PDCA 순환 구조를 따릅니다. 이 틀에 맞춰 단계를 배치하면 순서 문제가 쉽게 풀립니다.

<Quiz
  questionNumber={6}
  question="정보보호 관리체계(ISMS)의 관리과정 단계를 PDCA(Plan-Do-Check-Act) 순환 관점에서 순서대로 바르게 나열한 것은? ㉠ 정보보호 정책 수립과 위험관리 계획 등 관리체계 기반을 마련한다 ㉢ 수립된 대책과 통제를 실제 조직에 적용하고 운영한다 ㉢ 관리체계 운영 현황을 점검하고 내부 감사를 수행한다 ㉣ 점검 결과 발견된 문제를 보완하고 지속적으로 개선한다"
  options={['① ㉠ - ㉢ - ㉢ - ㉣', '② ㉢ - ㉠ - ㉣ - ㉢', '③ ㉠ - ㉣ - ㉢ - ㉢', '④ ㉣ - ㉠ - ㉢ - ㉢']}
  correctAnswer={1}
  explanation="ISMS 관리과정은 품질관리에서 널리 쓰이는 PDCA 순환 구조를 따른다. 계획(Plan) 단계에서 정책 수립·위험관리 계획 등 기반을 마련하고, 실행(Do) 단계에서 그 계획에 따라 대책·통제를 실제로 적용·운영하며, 점검(Check) 단계에서 운영 현황을 점검하고 내부 감사를 수행하고, 개선(Act) 단계에서 발견된 문제를 보완해 다음 순환에 반영한다. 지문의 ㉠이 계획, 두 번째 항목(운영·적용)이 실행, 세 번째 항목(점검·감사)이 점검, ㉣이 개선에 해당하므로 순서는 계획-실행-점검-개선이다. 이 순환은 한 번으로 끝나지 않고 개선 이후 다시 계획 단계로 돌아가 반복된다는 점도 함께 기억해야 한다."
  optionExplanations={['정책·계획 수립(P) - 적용·운영(D) - 점검·감사(C) - 보완·개선(A) 순서로 PDCA 순환의 실제 흐름과 일치한다.', '적용·운영이 계획보다 먼저 온다는 것은 계획 없이 실행부터 한다는 뜻이므로 순서가 맞지 않는다.', '개선(Act)이 점검(Check)보다 먼저 온다는 것은 점검 결과 없이 개선한다는 뜻이므로 순서가 맞지 않는다.', '개선(Act)이 계획(Plan)보다 먼저 온다는 것은 순환의 시작점 자체가 잘못되어 있어 순서가 맞지 않는다.']}
/>

<Quiz
  questionNumber={7}
  question="정보보호 조직의 역할인 CISO(Chief Information Security Officer)와 CPO(Chief Privacy Officer)에 대한 설명으로 옳지 않은 것은?"
  options={['① CISO는 조직의 정보보호 정책 수립, 보안 위험관리, 정보보호 전략 총괄 등을 책임지는 최고 정보보호 책임자다', '② CPO는 개인정보의 처리 방침 수립, 개인정보 보호 관련 고충 처리, 개인정보 침해 방지 대책 마련 등을 책임지는 개인정보 보호 책임자다', '③ CISO와 CPO의 역할은 조직에 따라 겸직될 수도 있고 분리될 수도 있으며, 이는 조직의 규모와 법령 요건에 따라 달라진다', '④ CISO와 CPO는 법률상 반드시 동일한 인물이 겸임해야 하며 별도로 지정하는 것은 허용되지 않는다']}
  correctAnswer={4}
  explanation="CISO(최고정보보호책임자)와 CPO(개인정보 보호책임자)의 역할 정의는 1, 2번이 옳다. 두 역할이 조직의 규모·법령 요건에 따라 겸직되거나 분리될 수 있다는 3번도 실제 제도와 일치한다. 문제는 4번이다. 법령은 일정 규모 이상의 조직에서 CISO와 CPO를 **각각 지정하도록 요구**하거나, 특정 조건 아래에서만 겸직을 허용하는 등 조직의 특성에 따라 다르게 규율하며, 반드시 동일 인물이 겸임해야 한다고 일률적으로 강제하지 않는다. 오히려 두 역할이 서로 견제·균형을 이루도록 분리해 지정하는 것이 정보보호 거버넌스의 일반적인 지향점이다."
  optionExplanations={['CISO의 역할(정보보호 정책·위험관리 총괄)을 정확히 설명한 옳은 문장이다.', 'CPO의 역할(개인정보 처리방침·고충처리 등)을 정확히 설명한 옳은 문장이다.', 'CISO·CPO의 겸직·분리 여부가 조직 규모와 법령 요건에 따라 달라진다는 것을 정확히 설명한 옳은 문장이다.', '법령은 조직 특성에 따라 CISO·CPO를 각각 지정하도록 요구하기도 하며 반드시 동일인이 겸임해야 한다고 일률적으로 강제하지 않으므로 이 설명은 옳지 않다.']}
/>

### 유형 4 — 네트워크 보안 장비와 프로토콜

**이 유형은 이렇게 접근한다:** 방화벽·IDS·IPS는 "탐지만 하는가, 차단까지 하는가"와 "어느 계층의 정보를 보는가"로 구분됩니다. VPN·DMZ는 네트워크 구성도를 머릿속에 그리면서 위치를 짚는 것이 중요합니다.

<Quiz
  questionNumber={8}
  question="방화벽(firewall), IDS(Intrusion Detection System), IPS(Intrusion Prevention System)에 대한 설명으로 옳지 않은 것은?"
  options={['① 패킷 필터링 방화벽은 출발지·목적지 IP, 포트 번호 등을 기준으로 패킷의 통과 여부를 결정한다', '② IDS는 트래픽에서 침입·공격 징후를 탐지해 관리자에게 경보를 알리는 데 주된 목적이 있으며 트래픽을 직접 차단하지는 않는 것이 일반적인 구조다', '③ IPS는 침입 징후를 탐지하는 것에서 나아가 트래픽 경로에 직접 위치해 의심되는 트래픽을 실시간으로 차단할 수 있다', '④ 애플리케이션 게이트웨이(Application Gateway) 방화벽은 IP·포트 정보만 확인하며 애플리케이션 계층의 데이터 내용은 전혀 검사하지 않는다']}
  correctAnswer={4}
  explanation="패킷 필터링 방화벽의 동작 방식, IDS가 탐지·경보에 집중하고 직접 차단하지 않는다는 구조, IPS가 탐지에 더해 실시간 차단까지 수행한다는 것은 모두 옳은 설명이다. 문제는 4번이다. 애플리케이션 게이트웨이(응용 계층 방화벽, 프록시 방화벽)는 오히려 그 이름이 뜻하는 대로 **OSI 응용 계층(application layer)까지 들여다보며 HTTP 요청 내용, 명령어 등 애플리케이션 수준의 데이터까지 검사**할 수 있다는 점이 특징이다. 이는 IP·포트 같은 하위 계층 정보만 보는 패킷 필터링 방화벽과 대비되는 지점이며, IP·포트만 확인하고 애플리케이션 데이터를 전혀 보지 않는다는 서술은 애플리케이션 게이트웨이의 정의와 정반대다."
  optionExplanations={['패킷 필터링 방화벽이 IP·포트 기준으로 필터링한다는 것을 정확히 설명한 옳은 문장이다.', 'IDS가 탐지·경보 중심이며 직접 차단하지 않는다는 일반적 구조를 정확히 설명한 옳은 문장이다.', 'IPS가 탐지에 더해 실시간 차단까지 한다는 것을 정확히 설명한 옳은 문장이다.', '애플리케이션 게이트웨이는 오히려 애플리케이션 계층 데이터까지 검사하는 것이 핵심 특징이므로, IP·포트만 본다는 서술은 패킷 필터링 방화벽과 애플리케이션 게이트웨이를 뒤바꾼 오답이다.']}
/>

<Quiz
  questionNumber={9}
  question="VPN(Virtual Private Network)과 DMZ(Demilitarized Zone)에 대한 설명으로 옳지 않은 것은?"
  options={['① VPN은 공중망(인터넷) 위에 암호화된 터널을 만들어 마치 사설망처럼 안전하게 통신할 수 있게 하는 기술이다', '② DMZ는 내부망과 외부망(인터넷) 사이에 위치해, 외부에서 접근이 필요한 웹서버·메일서버 등을 배치하는 완충 구역이다', '③ DMZ에 있는 서버가 침해되더라도 방화벽 정책을 통해 내부망으로의 직접 접근을 제한함으로써 피해 확산을 줄이는 것이 설계 목적이다', '④ DMZ를 구성하면 내부망의 서버를 외부에 노출하지 않고 안전하게 운영할 수 있으므로 별도의 방화벽 정책 없이도 내부망이 완전히 안전해진다']}
  correctAnswer={4}
  explanation="VPN의 정의, DMZ의 위치와 목적, 방화벽 정책을 통한 피해 확산 최소화 설계는 모두 옳은 설명이다. 문제는 4번이다. DMZ(비무장지대)는 외부 노출이 필요한 서버를 내부망과 분리해 배치함으로써 위험을 줄여 주는 구조적 장치일 뿐, 그 자체로 내부망을 **완전히 안전하게 만들어 주는 것은 아니다.** DMZ와 내부망 사이에도 별도의 방화벽 정책과 접근통제가 반드시 함께 적용되어야 하며, DMZ 서버가 침해된 뒤 내부망으로 이동(lateral movement)하는 것을 막으려면 지속적인 정책 관리와 모니터링이 필요하다. 방화벽 정책 없이도 완전히 안전해진다는 서술은 DMZ 구조가 만능 방어책이 아니라는 점을 놓친 오답이다."
  optionExplanations={['VPN의 암호화 터널을 통한 사설망 구현 방식을 정확히 설명한 옳은 문장이다.', 'DMZ의 위치와 외부 노출 서버 배치 목적을 정확히 설명한 옳은 문장이다.', 'DMZ가 침해 확산을 줄이기 위한 방화벽 정책과 함께 설계된다는 것을 정확히 설명한 옳은 문장이다.', 'DMZ는 위험을 줄이는 구조일 뿐 내부망을 완전히 안전하게 만드는 만능 장치가 아니며, 별도의 방화벽 정책·모니터링이 계속 필요하므로 이 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={10}
  question="다음 설명에 해당하는 네트워크 계층 공격 기법으로 가장 적절한 것은? '공격자가 위조된 ARP 응답 메시지를 네트워크에 뿌려, 특정 IP 주소에 대응하는 MAC 주소를 자신의 MAC 주소로 속임으로써 다른 호스트로 향하는 트래픽을 가로챈다.'"
  options={['① SQL Injection', '② ARP 스푸핑(ARP Spoofing)', '③ 세션 하이재킹', '④ DNS 캐시 포이즈닝']}
  correctAnswer={2}
  explanation="ARP(Address Resolution Protocol, 주소 결정 프로토콜)는 같은 네트워크 대역 안에서 IP 주소에 대응하는 MAC 주소를 찾는 프로토콜이다. ARP 스푸핑(ARP spoofing)은 공격자가 위조된 ARP 응답을 브로드캐스트하거나 특정 대상에게 보내, 실제로는 자신의 MAC 주소인데도 다른 호스트의 IP 주소에 대응하는 MAC 주소인 것처럼 속이는 공격이다. 이렇게 되면 피해자의 트래픽이 공격자를 거쳐 가게 되어(중간자 공격, Man-in-the-Middle) 도청·변조가 가능해진다. 1번 SQL Injection은 애플리케이션 계층의 DB 조작 공격이고, 3번 세션 하이재킹은 이미 성립된 세션을 탈취하는 공격이며, 4번 DNS 캐시 포이즈닝은 도메인 이름을 잘못된 IP로 연결시키는 공격으로, 셋 다 IP-MAC 주소 매핑을 속이는 이 설명의 메커니즘과는 다르다."
  optionExplanations={['SQL Injection은 데이터베이스 쿼리를 조작하는 애플리케이션 계층 공격으로, ARP 기반의 MAC 주소 위조와는 다른 메커니즘이다.', 'IP-MAC 주소 매핑을 위조된 ARP 응답으로 속여 트래픽을 가로채는 설명과 정확히 일치하는 공격이다.', '세션 하이재킹은 이미 인증된 세션의 식별자를 탈취하는 공격으로, ARP 테이블 자체를 속이는 이 설명과는 메커니즘이 다르다.', 'DNS 캐시 포이즈닝은 도메인 이름 해석 결과를 조작하는 공격으로, IP-MAC 매핑을 다루는 ARP 스푸핑과는 계층과 대상이 다르다.']}
/>

<Quiz
  questionNumber={11}
  question="OSI 7계층과 대응하는 보안 위협·기술을 짝지은 것으로 옳지 않은 것은?"
  options={['① 물리 계층(1계층) — 도청, 배선 절단 등 물리적 접근을 통한 위협', '② 네트워크 계층(3계층) — IP 스푸핑, 라우팅 정보 조작을 이용한 위협', '③ 전송 계층(4계층) — TCP SYN 플러딩과 같이 연결 설정 과정을 악용한 위협', '④ 응용 계층(7계층) — ARP 스푸핑과 같이 MAC 주소를 위조하는 위협']}
  correctAnswer={4}
  explanation="물리·네트워크·전송 계층에 대응하는 위협 예시는 1~3번이 모두 정확하다. 문제는 4번이다. ARP는 IP 주소를 MAC 주소로 대응시키는 프로토콜로 **데이터 링크 계층(2계층)** 에서 동작한다. 따라서 ARP 스푸핑은 응용 계층(7계층)이 아니라 데이터 링크 계층의 위협으로 분류해야 한다. 응용 계층에 대응하는 위협의 예로는 SQL Injection, XSS, 악성 이메일 첨부파일 실행처럼 사용자와 애플리케이션이 직접 상호작용하는 지점에서 발생하는 것들을 들 수 있다."
  optionExplanations={['물리 계층은 케이블·장비 등 물리적 매체를 다루므로 도청·배선 절단 같은 물리적 위협과 정확히 대응한다.', '네트워크 계층은 IP 주소·라우팅을 다루므로 IP 스푸핑·라우팅 조작과 정확히 대응한다.', '전송 계층은 TCP·UDP 연결을 다루므로 SYN 플러딩 같은 연결 설정 악용 위협과 정확히 대응한다.', 'ARP는 데이터 링크 계층(2계층)에서 동작하는 프로토콜이므로 ARP 스푸핑을 응용 계층 위협으로 분류한 것은 계층을 잘못 짝지은 오답이다.']}
/>

### 유형 5 — 위험분석 재계산과 대응

**이 유형은 이렇게 접근한다:** 이 유형은 22편에서 익힌 ALE 계산 공식을 시스템·네트워크 시나리오에 다시 적용하는 연습입니다. 숫자가 달라져도 SLE = AV × EF, ALE = SLE × ARO라는 틀은 동일합니다.

<Quiz
  questionNumber={12}
  question="한 기업의 웹서버 자산가치(AV)는 5,000만 원이다. DDoS 공격이 발생하면 서비스 중단으로 자산가치의 20퍼센트에 해당하는 손실(EF)이 발생하며, 이 공격은 연평균 2회(ARO) 발생하는 것으로 파악되었다. 이 위험의 연간 손실 예상액(ALE)은 얼마인가?"
  options={['① 1,000만 원', '② 2,000만 원', '③ 500만 원', '④ 5,000만 원']}
  correctAnswer={2}
  explanation="SLE = AV × EF = 5,000만 원 × 0.2 = 1,000만 원이다. 이는 DDoS 공격이 한 번 발생했을 때 예상되는 손실이다. ALE = SLE × ARO = 1,000만 원 × 2 = 2,000만 원이다. 이 위험은 연평균 2,000만 원의 손실을 낼 것으로 예상되므로, 이 금액을 기준으로 방어 대책(예: DDoS 방어 서비스 도입 비용)에 얼마를 투자할지 판단할 수 있다. 이 문제의 함정은 SLE(1,000만 원)에서 계산을 멈추고 ARO(연 2회)를 곱하는 것을 빠뜨리는 것이다. ARO가 1보다 크면 연간 손실은 1회 손실보다 커진다는 점에 유의해야 한다."
  optionExplanations={['SLE=1,000만 원까지만 계산하고 ARO(2회)를 곱하지 않은 값이다. 이는 1회 발생 손실이지 연간 손실이 아니다.', 'SLE(1,000만 원)에 ARO(2회)를 곱한 2,000만 원이 정확한 ALE 값이다.', 'AV×EF×ARO 계산에서 EF나 ARO를 잘못된 값으로 대입했을 때 나올 수 있는 오답으로 공식을 정확히 적용한 결과와 다르다.', 'AV(5,000만 원) 전액을 그대로 답으로 고른 오답으로, EF와 ARO를 전혀 반영하지 않은 값이다.']}
/>

<Quiz
  questionNumber={13}
  question="12번 문제에서 연간 1,500만 원짜리 DDoS 방어 서비스를 도입하면 ARO가 연 2회에서 연 0.5회로 낮아진다고 한다. 이 투자에 대한 경제성 판단으로 가장 적절한 것은? (EF는 20퍼센트로 동일하다고 가정한다)"
  options={['① 대책 적용 후 ALE는 500만 원이 되고 ALE 감소분(1,500만 원)이 투자 비용(1,500만 원)과 같으므로 손익분기점 수준의 투자다', '② 대책 적용 후 ALE는 250만 원이 되고 ALE 감소분(1,750만 원)이 투자 비용보다 커서 투자 가치가 충분하다', '③ ARO가 낮아져도 EF가 그대로이므로 ALE는 변하지 않는다', '④ 이 투자는 ARO만 변화시킬 뿐 ALE 계산과는 무관한 별개의 지표다']}
  correctAnswer={1}
  explanation="대책 적용 후 SLE는 변하지 않고 5,000만 원 × 0.2 = 1,000만 원이며, ALE = SLE × ARO = 1,000만 원 × 0.5 = 500만 원이 된다. 기존 ALE(2,000만 원)에서 대책 적용 후 ALE(500만 원)를 빼면 ALE 감소분은 1,500만 원이다. 이 감소분(1,500만 원)이 연간 투자 비용(1,500만 원)과 정확히 같으므로, 이 투자는 손해를 보지는 않지만 이익도 나지 않는 손익분기점 수준의 투자로 판단할 수 있다. 실무에서는 이런 경우 다른 부가 효과(평판 손실 방지, 법적 의무 준수 등)까지 함께 고려해 투자 여부를 최종 판단한다. 2번은 ALE를 잘못 계산한 값이고, 3·4번은 ARO 변화가 ALE 계산에 직접 반영된다는 공식 자체를 부정하는 오답이다."
  optionExplanations={['대책 적용 후 ALE=500만 원, 감소분=2,000만 원-500만 원=1,500만 원이 투자 비용 1,500만 원과 같아 손익분기점 수준이라는 판단이 정확한 계산에 근거한다.', 'ALE=1,000만 원×0.5=500만 원이지 250만 원이 아니다. EF나 SLE 계산을 다시 잘못 대입한 오답이다.', 'ARO가 2회에서 0.5회로 낮아지면 ALE=SLE×ARO 공식에 따라 ALE도 비례해 낮아지므로 변하지 않는다는 설명은 틀렸다.', 'ARO는 ALE 계산 공식의 핵심 변수이므로 ARO 변화는 곧바로 ALE 값에 반영된다. 무관하다는 설명은 공식 자체를 부정하는 오답이다.']}
/>

### 유형 6 — 안전한 개발·운영

**이 유형은 이렇게 접근한다:** 안전한 개발·운영 문항은 "보안을 어느 시점에 넣는가"가 핵심입니다. 보안 요구사항은 개발 초기에, 취약점 점검은 배포 전후에, 패치 관리는 운영 중 지속적으로 이루어져야 한다는 시점 감각을 기준으로 판단합니다.

<Quiz
  questionNumber={14}
  question="안전한 소프트웨어 개발과 운영에 대한 설명으로 옳지 않은 것은?"
  options={['① 보안 요구사항은 개발 완료 후 출시 직전 단계에서 한 번만 점검하는 것이 가장 효율적이다', '② 시큐어 코딩(secure coding)은 개발 단계에서 알려진 취약점 패턴을 피하도록 코드를 작성하는 활동이다', '③ 변경관리(change management)는 시스템 변경 사항을 계획·검토·승인·기록하는 절차로, 무단 변경으로 인한 장애나 보안 사고를 예방한다', '④ 형상관리(configuration management)는 소프트웨어·설정 파일 등의 버전과 변경 이력을 체계적으로 관리해 문제 발생 시 이전 상태로 되돌릴 수 있게 한다']}
  correctAnswer={1}
  explanation="시큐어 코딩, 변경관리, 형상관리에 대한 2~4번 설명은 모두 옳다. 문제는 1번이다. 보안 요구사항은 소프트웨어 개발생명주기(SDLC)의 **기획·설계 단계부터 반영**되어야 하며, 개발이 끝난 뒤 출시 직전에 한 번만 점검하는 방식은 이미 만들어진 구조적 결함을 발견해도 수정 비용과 시간이 매우 커지는 문제를 낳는다. 이를 「왼쪽으로 이동한다」(shift left)라고 부르며, 보안을 개발 초기 단계로 앞당길수록 결함을 더 저렴하고 빠르게 고칠 수 있다는 것이 안전한 개발의 핵심 원칙이다."
  optionExplanations={['보안 요구사항은 기획·설계 단계부터 반영해야 하며, 출시 직전 한 번의 점검만으로는 구조적 결함을 뒤늦게 발견해 수정 비용이 커지므로 이 설명은 옳지 않다.', '시큐어 코딩의 정의(개발 단계에서 취약점 패턴 회피)를 정확히 설명한 옳은 문장이다.', '변경관리의 목적(계획된 절차를 통한 무단 변경 방지)을 정확히 설명한 옳은 문장이다.', '형상관리의 목적(버전·이력 관리를 통한 롤백 가능성 확보)을 정확히 설명한 옳은 문장이다.']}
/>

<Quiz
  questionNumber={15}
  question="패치 관리(patch management)와 취약점 관리에 대한 설명으로 옳지 않은 것은?"
  options={['① 패치 관리는 운영체제·소프트웨어의 알려진 취약점을 보완하는 업데이트를 체계적으로 적용하는 활동이다', '② 패치를 운영 환경에 바로 적용하기 전에 별도의 테스트 환경에서 호환성과 부작용을 검증하는 것이 권장된다', '③ 취약점 스캐너로 발견된 모든 취약점은 발견 즉시 심각도와 무관하게 동일한 우선순위로 처리해야 한다', '④ 지원이 종료(End of Life)된 소프트웨어는 더 이상 보안 패치가 제공되지 않으므로 별도의 보완 대책이나 교체 계획이 필요하다']}
  correctAnswer={3}
  explanation="패치 관리의 목적, 테스트 환경에서의 사전 검증 권장, 지원 종료 소프트웨어의 위험성은 모두 옳은 설명이다. 문제는 3번이다. 실무에서는 발견된 취약점을 **심각도(critical, high, medium, low 등)와 악용 가능성, 해당 자산의 중요도에 따라 우선순위를 매겨** 처리한다. 예를 들어 원격 코드 실행이 가능한 치명적 취약점은 즉시 긴급 패치를 적용하지만, 영향이 제한적인 낮은 심각도의 취약점은 정기 패치 주기에 맞춰 처리하는 식이다. 모든 취약점을 심각도와 무관하게 동일한 우선순위로 처리한다는 것은 한정된 자원으로 위험을 효율적으로 관리해야 하는 실무 원칙과 맞지 않는다."
  optionExplanations={['패치 관리의 목적(알려진 취약점 보완)을 정확히 설명한 옳은 문장이다.', '운영 적용 전 테스트 환경 검증의 필요성을 정확히 설명한 옳은 문장이다.', '취약점은 심각도·악용 가능성·자산 중요도에 따라 우선순위를 매겨 처리해야 하며, 모두 동일한 우선순위로 처리한다는 것은 위험 기반 관리 원칙과 맞지 않는다.', '지원 종료 소프트웨어에 대한 보완·교체 필요성을 정확히 설명한 옳은 문장이다.']}
/>

### 유형 7 — 종합·응용

**이 유형은 이렇게 접근한다:** 마지막 유형은 앞의 여러 영역이 한 시나리오에 함께 등장하는 문제입니다. 시나리오에서 자산·위협·통제를 각각 표시하며 읽으면 정리가 쉬워집니다.

<Quiz
  questionNumber={16}
  question="한 회사가 내부망에 있던 그룹웨어 서버를 외부에서도 접속 가능하도록 개방하려 한다. 보안팀이 제시한 방안으로 가장 적절한 것은?"
  options={['① 내부망 방화벽 규칙을 전면 해제해 외부의 모든 IP가 그룹웨어 서버로 직접 접속할 수 있게 한다', '② 그룹웨어 서버를 DMZ에 배치하고, 내부망과의 통신은 최소한으로 제한하며, 외부 접속자는 VPN이나 강화된 인증을 거치도록 구성한다', '③ 그룹웨어 서버의 데이터베이스 계정에 관리자 권한을 부여해 접속 문제를 원천적으로 방지한다', '④ 외부 접속을 허용하는 대신 서버의 감사 로그 기능을 모두 비활성화해 응답 속도를 높인다']}
  correctAnswer={2}
  explanation="외부 노출이 필요한 서버는 DMZ에 배치해 내부망과 분리하고, DMZ와 내부망 사이의 통신은 반드시 필요한 범위로 최소화하며, 외부 접속자에게는 VPN이나 강화된 인증(다중요소인증 등)을 요구하는 것이 이런 상황에서 표준적으로 권장되는 설계다. 이는 09·12·14·17편에서 다룬 DMZ 구조, 최소권한 원칙, 인증 강화를 하나의 시나리오에 통합 적용한 것이다. 1번은 방화벽 규칙을 전면 해제해 오히려 공격 표면을 극대화하는 조치이고, 3번은 서비스 계정에 불필요하게 강력한 권한을 부여해 최소권한 원칙을 정면으로 위반하며, 4번은 감사 로그를 비활성화해 추적성을 스스로 포기하는 조치로 모두 부적절하다."
  optionExplanations={['방화벽 규칙을 전면 해제하면 공격자가 아무 제한 없이 서버에 접근할 수 있게 되어 오히려 공격 표면을 극대화한다.', 'DMZ 배치, 내부망과의 통신 최소화, VPN·강화된 인증 요구는 외부 노출 서버를 안전하게 운영하는 표준적인 설계이므로 가장 적절하다.', '접속 문제 해결을 이유로 DB 계정에 관리자 권한을 부여하는 것은 최소권한 원칙을 정면으로 위반하는 조치다.', '감사 로그를 비활성화하면 침해 발생 시 원인 분석과 책임 추적이 불가능해지므로 보안을 오히려 약화시키는 조치다.']}
/>

<Quiz
  questionNumber={17}
  question="위험분석 과정에서 자산, 위협, 취약점의 관계에 대한 설명으로 옳지 않은 것은?"
  options={['① 자산(asset)은 조직이 보호해야 할 정보나 정보처리 설비 등 가치 있는 대상이다', '② 위협(threat)은 자산에 손실을 끼칠 수 있는 잠재적 원인이나 사건이다', '③ 취약점(vulnerability)은 위협이 자산에 손실을 끼치는 것을 가능하게 하는 약점이나 결함이다', '④ 자산가치가 0에 가까운 정보라면 아무리 심각한 취약점과 강력한 위협이 함께 존재해도 그로 인한 위험은 통상적으로 무시할 수 있는 수준으로 평가된다']}
  correctAnswer={4}
  explanation="자산·위협·취약점의 정의는 1~3번이 모두 정확하다. 위험(risk)은 흔히 자산가치, 위협의 발생 가능성, 취약점의 심각성이 함께 결합해 산정되는 값으로 이해된다. 문제는 4번이다. 세 요소 중 어느 하나의 값이 실질적으로 0에 가까우면 그 조합으로 인한 위험이 낮게 평가된다는 것 자체는 맞지만, 4번은 이를 지나치게 단정적으로 서술해 함정을 만든다. 실무에서 자산가치를 평가할 때는 그 정보 자체의 금전적 가치뿐 아니라 유출 시의 평판 손상, 법적 책임, 다른 자산과의 연계 가치까지 함께 고려하므로, 표면적인 금전 가치가 낮아 보이는 자산도 실제 위험 평가에서는 무시할 수 없는 수준으로 나올 수 있다. 따라서 자산가치가 낮다는 이유만으로 위험을 통상적으로 무시할 수 있는 수준이라고 일반화하는 것은 위험분석의 실제 관행과 맞지 않는다."
  optionExplanations={['자산의 정의(보호해야 할 가치 있는 대상)를 정확히 설명한 옳은 문장이다.', '위협의 정의(손실을 끼칠 수 있는 잠재적 원인)를 정확히 설명한 옳은 문장이다.', '취약점의 정의(위협이 손실로 이어지게 하는 약점)를 정확히 설명한 옳은 문장이다.', '자산가치는 금전적 가치뿐 아니라 평판·법적 책임·연계 가치까지 포함해 평가되므로, 표면적 가치가 낮다는 이유만으로 위험을 통상적으로 무시할 수 있다고 단정하는 것은 옳지 않다.']}
/>

<Quiz
  questionNumber={18}
  question="다음 중 클라우드 환경 도입 시 새롭게 고려해야 할 접근통제·보안 이슈로 가장 거리가 먼 것은?"
  options={['① 클라우드 서비스 제공자와 이용자 간 보안 책임을 어디까지 나눌지 정하는 책임공유모델(Shared Responsibility Model)', '② 클라우드 관리 콘솔 계정에 대한 강력한 인증과 최소권한 부여', '③ 저장 데이터와 전송 데이터에 대한 암호화 적용 여부', '④ 클라우드로 전환하면 물리적 서버가 사라지므로 모든 접근통제와 인증 절차를 생략해도 무방하다']}
  correctAnswer={4}
  explanation="책임공유모델, 관리 콘솔 계정 보안, 데이터 암호화는 모두 클라우드 환경에서 실제로 중요하게 다뤄지는 보안 이슈다. 문제는 4번이다. 클라우드 환경에서는 물리적 서버를 직접 관리하지 않게 될 뿐, 논리적인 접근통제·인증·권한관리의 필요성은 전혀 줄어들지 않는다. 오히려 클라우드는 인터넷을 통해 어디서든 관리 콘솔에 접근할 수 있는 구조이므로, 계정 탈취·설정 오류로 인한 데이터 노출 위험이 온프레미스 환경보다 더 부각되는 경우가 많다. 물리적 서버가 없어졌다는 이유로 접근통제·인증을 생략해도 된다는 서술은 클라우드 보안의 기본 원칙과 정반대다."
  optionExplanations={['책임공유모델은 클라우드 도입 시 반드시 명확히 해야 하는 핵심 보안 이슈이므로 새롭게 고려할 사항에 해당한다.', '관리 콘솔 계정의 인증·권한 관리는 클라우드 환경에서 특히 중요하게 다뤄지는 이슈이므로 새롭게 고려할 사항에 해당한다.', '저장·전송 데이터 암호화는 클라우드 환경에서도 핵심적인 보안 이슈이므로 새롭게 고려할 사항에 해당한다.', '물리적 서버가 사라진다고 해서 논리적 접근통제·인증의 필요성이 사라지는 것은 아니며 오히려 원격 접근 특성상 더 중요해지므로, 생략해도 무방하다는 설명은 클라우드 보안 원칙과 정반대다.']}
/>

<Quiz
  questionNumber={19}
  question="정보보호 관리체계에서 위험평가 결과에 따른 위험수용기준(risk acceptance criteria)에 대한 설명으로 가장 적절한 것은?"
  options={['① 위험수용기준은 조직이 감내할 수 있는 위험의 허용 수준을 사전에 정한 기준으로, 이 기준을 초과하는 위험에는 별도의 대책을 적용해야 한다', '② 위험수용기준은 모든 조직에서 동일하게 법으로 고정된 값이며 조직의 특성과 무관하다', '③ 위험수용기준을 낮게 설정할수록 더 많은 위험을 별도 대책 없이 수용하게 된다', '④ 위험수용기준은 위험분석을 실시하기 전에는 정할 필요가 없으며 분석이 끝난 뒤에 임의로 정해도 무방하다']}
  correctAnswer={1}
  explanation="위험수용기준(risk acceptance criteria)은 조직이 사전에 '이 정도 수준의 위험까지는 별도 대책 없이 감내하겠다'고 정해 두는 기준선이다. 위험평가 결과 이 기준을 넘는 위험이 발견되면 위험 감소·회피·전가 같은 대응 전략을 적용해 기준 이하로 낮춰야 한다. 이 기준은 조직의 업종, 규모, 감당 가능한 재정 여력, 법적 요구사항에 따라 조직마다 다르게 설정되므로 모든 조직에 동일하게 고정된 법적 값이라는 2번은 틀렸다. 3번은 기준을 낮게 설정할수록(더 엄격하게 잡을수록) 오히려 더 많은 위험이 기준을 초과해 대책이 필요해지므로, 수용하는 위험이 늘어난다는 설명은 반대다. 4번은 위험수용기준을 위험분석 이전에 미리 정해 두어야 평가 결과를 판단할 근거가 생긴다는 점에서 순서가 틀렸다."
  optionExplanations={['위험수용기준의 정의와, 이를 초과하는 위험에 대책을 적용해야 한다는 원칙을 정확히 설명한 옳은 문장이다.', '위험수용기준은 조직의 업종·규모·재정 여력에 따라 각기 다르게 설정되는 조직별 기준이며 법으로 고정된 동일 값이 아니다.', '기준을 낮게(엄격하게) 설정할수록 더 많은 위험이 기준을 초과해 대책이 필요해지므로, 수용 위험이 늘어난다는 설명은 반대로 서술되었다.', '위험수용기준은 위험분석 결과를 판단할 근거가 되어야 하므로 분석 이전에 미리 정해 두는 것이 원칙이며, 분석 후 임의로 정해도 무방하다는 설명은 순서가 틀렸다.']}
/>

<Quiz
  questionNumber={20}
  question="다음 중 정보보호 관리체계에서 '보호대책'에 해당하는 예로 가장 거리가 먼 것은?"
  options={['① 접근권한을 최소권한 원칙에 따라 부여하고 정기적으로 검토하는 절차', '② 중요 데이터베이스에 대한 접근을 암호화 통신과 강화된 인증으로 제한하는 기술적 조치', '③ 화재·정전에 대비한 서버실의 소화 설비와 무정전 전원장치(UPS) 설치', '④ 이미 발생한 침해사고의 피해자에게 손해배상금을 지급하는 것']}
  correctAnswer={4}
  explanation="정보보호 관리체계(ISMS-P 등)에서 보호대책은 크게 관리적 보호대책(정책·절차·교육 등), 기술적 보호대책(접근통제·암호화·인증 등), 물리적 보호대책(출입통제·소화설비·전원설비 등)으로 나뉜다. 1번은 관리적·기술적 보호대책, 2번은 기술적 보호대책, 3번은 물리적 보호대책의 대표적인 예로 모두 해당한다. 문제는 4번이다. 손해배상금 지급은 사고가 이미 발생한 뒤 그 피해를 금전으로 보상하는 사후적·재정적 조치이며, 위험을 사전에 줄이거나 예방하는 보호대책의 범주가 아니라 위험 대응 전략 중 위험 전가(예: 보험)나 사고 이후의 법적·재정적 처리에 가깝다. 따라서 보호대책의 예로 보기 가장 거리가 멀다."
  optionExplanations={['최소권한 부여와 정기 검토는 관리적·기술적 보호대책의 대표적인 예이므로 보호대책에 해당한다.', '암호화 통신과 강화된 인증은 기술적 보호대책의 대표적인 예이므로 보호대책에 해당한다.', '소화 설비와 UPS는 물리적 보호대책의 대표적인 예이므로 보호대책에 해당한다.', '손해배상금 지급은 사고 발생 이후의 금전적 보상 조치로, 사전 예방을 목적으로 하는 보호대책의 범주와는 성격이 다르므로 거리가 가장 멀다.']}
/>

## 참고 자료

- 국가평생교육진흥원 독학학위제: [https://bdes.nile.or.kr](https://bdes.nile.or.kr)
- OWASP Top 10: [https://owasp.org](https://owasp.org)
- 정보보호 및 개인정보보호 관리체계(ISMS-P) 인증기준 안내서 (KISA): [https://www.kisa.or.kr](https://www.kisa.or.kr)
