---
title: '25. 개념·정의형 예상문제 2 (이름·형·스코프·자료형)'
description: '이름·바인딩·스코프·수명, 자료형·형 시스템·포인터/참조·형 변환 영역을 국가평생교육진흥원 출제기준에 맞춰 재구성한 개념·정의·비교 24문항'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 편은 국가평생교육진흥원 독학학위제 컴퓨터과학 전공 3단계 「프로그래밍언어론」의 **최신 출제기준(평가영역)** 중 이름·바인딩·스코프·수명, 자료형·형 시스템·포인터/참조·형 변환 영역을 근거로 새로 구성한 **개념·정의형 예상문제 24문항**입니다. 실제 특정 회차의 기출 문항을 그대로 옮기거나 암기해 재현한 것이 아니며, 09~11편에서 다룬 개념을 옳은 것/옳지 않은 것 판별, 정의 매칭, 비교 판단 형태로 재구성했습니다. 실제 응시 시 문항 수·배점·시험 시간은 그해 시행 공고를 반드시 확인하시기 바랍니다.
</Callout>

## 이 편의 영역 구성

| 문항 번호 | 출제기준 영역 | 관련 편 | 유형 |
| --- | --- | --- | --- |
| 1–3 | 이름·바인딩 종류(형·저장·매개변수 바인딩)와 초기화 | 09편 | 개념·정의 |
| 4–5 | 정적/동적 바인딩과 바인딩 시기 | 09편 | 개념·비교 |
| 6–9 | 정적(렉시컬) 스코프 vs 동적 스코프 | 09편 | 개념·코드 판단 |
| 10–11 | 수명(lifetime)·저장 클래스, 스코프와 수명의 구분 | 09편 | 개념·비교 |
| 12–14 | 원시형·구조형·사용자 정의형, 강형·약형 | 10편 | 개념·비교 |
| 15–16 | 정적/동적 형 시스템, 형 검사·형 추론 | 10편 | 개념·비교 |
| 17–18 | 강제 변환(coercion)·명시적 캐스팅(casting)과 함정 | 10편 | 개념·함정 |
| 19–20 | 배열·레코드 등 복합 자료형 설계 | 11편 | 개념·비교 |
| 21–23 | 포인터·참조, 댕글링 포인터·메모리 누수·가비지 컬렉션 | 11편 | 개념·비교 |
| 24 | 스코프·바인딩·형 시스템 종합 | 09–11편 | 종합·정오 판단 |

## 문제 1

<Quiz
  questionNumber={1}
  question="이름(name)에 관한 여러 바인딩 중 형 바인딩(type binding)과 저장 바인딩(storage binding)의 관계를 가장 정확히 설명한 것은?"
  options={['형 바인딩은 변수가 어떤 자료형을 갖는지를 이름에 연결하는 것이고, 저장 바인딩은 그 변수가 실행 중 어느 메모리 위치를 차지할지를 연결하는 것으로, 두 바인딩은 서로 독립적으로 다른 시점에 이루어질 수 있다.', '형 바인딩과 저장 바인딩은 항상 완전히 동시에 이루어지며 절대 분리될 수 없다.', '저장 바인딩이 이루어져야만 비로소 형 바인딩이 가능해지므로 저장 바인딩이 항상 형 바인딩보다 논리적으로 먼저다.', '형 바인딩은 실행 중에는 절대 일어날 수 없고 반드시 컴파일 이전에 사람이 직접 지정해야 한다.']}
  correctAnswer={1}
  explanation="형 바인딩은 이름과 자료형 사이의 연결이고, 저장 바인딩은 이름과 실제 메모리 위치(주소) 사이의 연결입니다. 예를 들어 정적 타입 언어에서는 컴파일 시점에 형 바인딩이 끝나지만, 저장 바인딩은 함수가 호출될 때(스택 활성화 레코드가 만들어질 때)까지 늦춰지는 경우가 흔합니다. 이렇게 두 바인딩은 서로 다른 시점에 독립적으로 일어날 수 있습니다."
  optionExplanations={['형 바인딩과 저장 바인딩이 서로 다른 성격의 연결이며 시점이 분리될 수 있다는 점을 정확히 설명했으므로 정답입니다.', '동적 지역 변수처럼 형 바인딩은 컴파일 시점에, 저장 바인딩은 호출 시점에 이루어지는 사례가 많아 항상 동시라는 설명은 틀렸습니다.', '오히려 형 바인딩이 먼저 정해지고 그 형에 맞는 크기의 저장 공간이 나중에 할당되는 경우(정적 타입 언어)가 더 일반적이므로 순서를 반대로 말해 틀렸습니다.', '동적 타입 언어에서는 실행 중 값이 대입될 때 형 바인딩이 일어나므로, 컴파일 이전에만 가능하다는 설명은 틀렸습니다.']}
/>

## 문제 2

<Quiz
  questionNumber={2}
  question="매개변수 바인딩(parameter binding)에 대한 설명으로 가장 적절한 것은?"
  options={['하위프로그램이 호출될 때 형식 매개변수(formal parameter)라는 이름이 실제 인자(actual argument)가 가리키는 값이나 위치에 연결되는 것을 말한다.', '매개변수 바인딩은 하위프로그램을 정의하는 시점에 딱 한 번만 일어나고 이후 호출과는 무관하다.', '매개변수 바인딩은 전역 변수에만 적용되는 개념이다.', '매개변수 바인딩이 이루어지면 그 하위프로그램은 다시는 다른 인자로 호출될 수 없다.']}
  correctAnswer={1}
  explanation="매개변수 바인딩은 하위프로그램이 호출될 때마다 새로 이루어집니다. 형식 매개변수는 하위프로그램 정의에 등장하는 이름일 뿐이고, 실제로 어떤 값·변수·주소에 연결되는지는 호출 시점마다 실제 인자에 따라 달라집니다."
  optionExplanations={['호출 시점마다 형식 매개변수가 실제 인자에 연결된다는 점을 정확히 설명했으므로 정답입니다.', '매개변수 바인딩은 정의 시점이 아니라 호출 시점마다 반복적으로 일어나므로 틀렸습니다.', '매개변수는 지역적인 형식 매개변수 이름에 관한 개념이며 전역 변수와는 직접 관련이 없어 틀렸습니다.', '같은 하위프로그램을 서로 다른 인자로 여러 번 호출할 수 있고 그때마다 바인딩이 새로 이루어지므로 틀렸습니다.']}
/>

## 문제 3

<Quiz
  questionNumber={3}
  question="변수의 초기화(initialization)와 바인딩의 관계를 설명한 것으로 옳지 않은 것은?"
  options={['초기화는 변수에 처음 값을 대입하는 것으로, 값 바인딩(value binding)의 한 형태로 볼 수 있다.', '변수 선언과 동시에 초기값을 지정하면 저장 바인딩과 값 바인딩이 사실상 같은 시점에 이루어지는 경우가 많다.', '초기화되지 않은 지역 변수는 언어에 따라 이전에 그 메모리 위치에 남아 있던 쓰레기 값(garbage value)을 가질 수 있다.', '모든 언어는 선언된 변수를 항상 자동으로 0이나 특정 기본값으로 초기화하도록 표준화되어 있어 초기화 누락은 어떤 언어에서도 절대 문제가 되지 않는다.']}
  correctAnswer={4}
  explanation="C 언어의 지역 변수처럼 명시적으로 초기화하지 않으면 이전에 그 메모리 영역에 있던 임의의 값이 그대로 남아 있는 언어가 많습니다. 모든 언어가 항상 자동으로 기본값을 넣어준다는 설명은 옳지 않으며, 이는 프로그래밍언어론에서 반복적으로 지적되는 함정입니다."
  optionExplanations={['초기화가 이름에 값을 연결하는 행위라는 점에서 값 바인딩의 한 형태로 보는 것은 옳은 설명입니다.', '선언과 동시에 초기화하면 저장 공간 확보와 값 대입이 거의 같은 시점에 일어난다는 것은 옳은 설명입니다.', 'C처럼 초기화하지 않은 지역 변수가 쓰레기 값을 가질 수 있다는 것은 실제로 옳은 설명입니다.', '언어마다 자동 초기화 여부가 다르고 초기화되지 않은 변수 사용은 실제 버그의 흔한 원인이므로, 절대 문제가 되지 않는다는 이 설명이 옳지 않아 정답입니다.']}
/>

## 문제 4

<Quiz
  questionNumber={4}
  question="정적 바인딩(static binding)과 동적 바인딩(dynamic binding)을 구분하는 기준으로 가장 적절한 것은?"
  options={['바인딩이 프로그램 실행 전(컴파일 또는 링크 시점)에 확정되어 실행 중 바뀌지 않으면 정적 바인딩이고, 실행 중에 비로소 확정되거나 실행 중 바뀔 수 있으면 동적 바인딩이다.', '정적 바인딩은 전역 변수에만 적용되고 동적 바인딩은 지역 변수에만 적용된다.', '정적 바인딩은 항상 동적 바인딩보다 실행 속도가 느리다.', '정적 바인딩과 동적 바인딩은 자료형에는 적용되지 않고 오직 함수 이름에만 적용되는 개념이다.']}
  correctAnswer={1}
  explanation="정적/동적 바인딩을 나누는 기준은 바인딩이 확정되는 시점입니다. 실행 전에 이미 정해져 실행 중 변하지 않으면 정적 바인딩, 실행 중에야 정해지거나 실행 중 바뀔 수 있으면 동적 바인딩입니다. 이 구분은 변수의 형·스코프뿐 아니라 객체지향의 메서드 호출(동적 바인딩) 등 여러 영역에 폭넓게 적용됩니다."
  optionExplanations={['바인딩이 확정되는 시점을 기준으로 나눈다는 정의를 정확히 설명했으므로 정답입니다.', '정적/동적 바인딩 구분은 변수의 유효범위(전역·지역)가 아니라 바인딩 확정 시점에 관한 것이므로 틀렸습니다.', '일반적으로는 정적 바인딩이 실행 중 재확인 과정이 없어 더 빠른 경향이 있지만, 이는 결과적 경향이지 정의 자체의 기준이 아니므로 정의를 묻는 문항의 답으로는 부적절합니다.', '정적/동적 바인딩은 형 바인딩·스코프뿐 아니라 OOP의 메서드 바인딩 등 이름과 관련된 다양한 개념에 폭넓게 적용되므로 틀렸습니다.']}
/>

## 문제 5

<Quiz
  questionNumber={5}
  question="바인딩 시기(binding time)가 이르다(early binding)는 것과 늦다(late binding)는 것의 일반적인 트레이드오프를 설명한 것으로 가장 적절한 것은?"
  options={['바인딩을 일찍 확정할수록 컴파일 시점에 더 많은 오류를 미리 검출하고 실행 속도도 유리해지는 경향이 있는 반면, 바인딩을 늦출수록 실행 중 상황에 맞춰 유연하게 대응할 수 있는 대신 오류 발견이 늦어지고 실행 오버헤드가 늘어나는 경향이 있다.', '바인딩 시기는 프로그램의 정확성과는 아무런 관련이 없고 오직 프로그램의 줄 수에만 영향을 준다.', '바인딩을 늦출수록 항상 컴파일 오류를 더 많이 잡아낼 수 있다.', '바인딩 시기가 이르든 늦든 실행 속도에는 어떠한 차이도 발생하지 않는다.']}
  correctAnswer={1}
  explanation="이른 바인딩(정적 타입 언어의 컴파일 시 형 확정 등)은 실행 전에 많은 오류를 발견할 수 있어 신뢰성과 성능에 유리하지만 유연성이 떨어집니다. 늦은 바인딩(동적 타입 언어의 실행 중 형 결정 등)은 유연하지만 오류가 실행 중에야 드러나고, 매번 확인하는 비용 때문에 성능 오버헤드가 발생하는 경향이 있습니다. 이는 독학사에서 반복적으로 다루는 설계 트레이드오프입니다."
  optionExplanations={['이른/늦은 바인딩의 실제 장단점(오류 검출 시점, 성능, 유연성)을 정확히 대비했으므로 정답입니다.', '바인딩 시기는 오류 검출 시점·유연성·성능 등 프로그램의 여러 특성에 직접 영향을 주므로 틀렸습니다.', '오히려 바인딩을 늦추면 컴파일 시점에는 확정되지 않은 정보가 많아져 컴파일 시 검출 가능한 오류가 줄어드는 경향이 있으므로 틀렸습니다.', '이른 바인딩은 실행 중 재확인 과정이 줄어 일반적으로 더 빠른 경향이 있으므로 차이가 없다는 설명은 틀렸습니다.']}
/>

## 문제 6

정적(렉시컬) 스코프를 따르는 언어에서 다음과 같은 구조의 의사코드가 있다고 하자.

```text
전역 변수 x = 10

procedure A는:
    지역 변수 x = 20
    call B()

procedure B는:
    x를 출력한다

A()를 호출한다
```

`B`는 `A` 안이 아니라 전역 범위에 정의되어 있다.

<Quiz
  questionNumber={6}
  question="정적 스코프 규칙에 따를 때 이 프로그램이 출력하는 값은?"
  options={['10', '20', '실행할 때마다 값이 달라진다', '변수를 찾지 못해 항상 오류가 발생한다']}
  correctAnswer={1}
  explanation="정적 스코프는 변수가 코드 상에서 어디에 놓여 있는지(정의된 위치)를 기준으로 유효 범위를 정합니다. B는 A 내부가 아니라 전역 범위에서 정의되었으므로, B 안에서 x를 참조하면 B를 둘러싸는 전역 범위의 x(값 10)를 찾습니다. A 안의 지역 변수 x=20은 B의 코드상 위치와는 무관하므로 영향을 주지 않습니다."
  optionExplanations={['B가 코드상 전역 범위에 있으므로 정적 스코프 규칙에 따라 전역 x인 10을 참조한다는 것이 정답입니다.', 'A의 지역 변수 20은 A를 호출한 것과 상관없이 B의 코드상 위치가 전역이므로 영향을 주지 않아 오답입니다.', '정적 스코프는 코드 구조로 완전히 결정되므로 호출 순서와 무관하게 항상 같은 값을 출력해 오답입니다.', 'B의 전역 범위에는 x가 이미 선언되어 있으므로 변수를 찾지 못하는 오류는 발생하지 않아 오답입니다.']}
/>

## 문제 7

문제 6과 동일한 코드 구조이지만, 이번에는 이 언어가 동적 스코프(dynamic scope)를 따른다고 가정한다.

<Quiz
  questionNumber={7}
  question="동적 스코프 규칙에 따를 때 이 프로그램이 출력하는 값은?"
  options={['20', '10', '두 값이 동시에 출력된다', '반드시 문법 오류가 발생한다']}
  correctAnswer={1}
  explanation="동적 스코프는 변수가 코드상 어디에 있는지가 아니라, 실행 중 호출이 이루어진 순서(누가 누구를 호출했는가)를 기준으로 유효 범위를 정합니다. A가 B를 호출했으므로, B가 실행되는 시점에는 호출 체인상 가장 가까운 x인 A의 지역 변수 x=20이 사용됩니다."
  optionExplanations={['동적 스코프에서는 호출 체인상 가장 최근에 선언된 x, 즉 A의 지역 변수 20이 사용된다는 것이 정답입니다.', '10은 정적 스코프를 적용했을 때의 결과이며, 이 문제는 동적 스코프를 가정하므로 오답입니다.', '동적 스코프도 어느 한 x만을 참조하도록 규칙이 정해져 있으므로 두 값이 동시에 출력되지 않아 오답입니다.', '동적 스코프 규칙 자체는 문법적으로 유효한 참조이므로 문법 오류가 발생하지 않아 오답입니다.']}
/>

## 문제 8

<Quiz
  questionNumber={8}
  question="문제 6·7의 두 결과 차이가 보여주는 정적 스코프와 동적 스코프의 근본적 차이로 가장 적절한 것은?"
  options={['정적 스코프는 코드의 텍스트 구조(어디에 선언되었는가)로 참조 대상이 정해지는 반면, 동적 스코프는 실행 시점의 호출 순서(누가 호출했는가)로 참조 대상이 정해진다.', '정적 스코프는 컴파일이 되지 않는 언어에서만 쓰이고 동적 스코프는 컴파일 언어에서만 쓰인다.', '정적 스코프와 동적 스코프는 결과에 차이를 만들지 않는 표기법 차이일 뿐이다.', '동적 스코프는 전역 변수를 아예 허용하지 않는 규칙이다.']}
  correctAnswer={1}
  explanation="정적 스코프의 핵심은 프로그램 텍스트(소스 코드) 상의 중첩 구조만 보고 참조 대상을 결정할 수 있다는 점이고, 동적 스코프의 핵심은 실행 중 호출 스택의 상태(누가 누구를 호출했는가)에 따라 참조 대상이 달라질 수 있다는 점입니다. 이 때문에 정적 스코프는 코드만 보고 의미를 분석하기 쉬운 반면, 동적 스코프는 실행 흐름을 추적해야 의미를 알 수 있습니다."
  optionExplanations={['코드 구조 기반 vs 호출 순서 기반이라는 두 스코프 규칙의 근본 차이를 정확히 설명했으므로 정답입니다.', '스코프 규칙은 언어의 컴파일 여부와 직접적인 필연 관계가 없으므로 틀렸습니다.', '문제 6·7에서 본 것처럼 같은 코드 구조라도 스코프 규칙에 따라 실제 출력 값이 달라지므로 결과에 차이가 없다는 설명은 틀렸습니다.', '동적 스코프에서도 전역 변수는 존재할 수 있으며, 다만 참조 대상을 찾는 방식이 다를 뿐이므로 틀렸습니다.']}
/>

## 문제 9

<Quiz
  questionNumber={9}
  question="정적 스코프 언어에서 비지역 변수를 찾을 때 흔히 쓰이는 '가장 가깝게 둘러싼 범위 규칙(most closely nested scope rule)'을 가장 정확히 설명한 것은?"
  options={['현재 위치를 기준으로 코드상 가장 가깝게 둘러싸고 있는 범위부터 바깥쪽 범위로 차례로 검색하여, 그 이름을 선언한 범위를 처음 찾는 즉시 그것을 사용한다.', '항상 가장 바깥쪽 전역 범위의 선언을 최우선으로 사용한다.', '이름이 여러 범위에서 선언되어 있으면 그중 알파벳 순서가 가장 빠른 선언을 사용한다.', '중첩된 범위가 있으면 반드시 컴파일 오류로 처리해야 한다는 규칙이다.']}
  correctAnswer={1}
  explanation="정적 스코프에서 어떤 이름을 참조하면, 그 이름이 등장한 위치를 기준으로 가장 안쪽(가장 가깝게 둘러싼) 범위부터 검사하고, 거기에 선언이 없으면 한 단계씩 바깥 범위로 나가며 검사합니다. 선언을 찾는 즉시 검색을 멈추고 그 선언을 사용하는 것이 most closely nested rule입니다."
  optionExplanations={['안쪽에서 바깥쪽으로 차례로 검색해 가장 먼저 찾은 선언을 쓴다는 규칙을 정확히 설명했으므로 정답입니다.', '가장 가까운 범위의 선언이 있으면 그것을 우선 사용하므로, 전역 선언을 항상 최우선으로 쓴다는 설명은 규칙을 거꾸로 말해 틀렸습니다.', '스코프 규칙은 이름이 선언된 위치의 중첩 구조에 따라 정해지는 것이지 알파벳 순서와는 무관하므로 틀렸습니다.', '변수 중첩 선언(섀도잉) 자체는 대부분의 언어에서 허용되는 정상적인 상황이며 컴파일 오류가 아니므로 틀렸습니다.']}
/>

## 문제 10

<Quiz
  questionNumber={10}
  question="변수의 저장 클래스(storage class)에 따른 수명(lifetime) 비교로 옳지 않은 것은?"
  options={['정적(static) 변수는 하위프로그램이 반환된 뒤에도 값이 사라지지 않고 프로그램이 끝날 때까지 유지된다.', '스택 동적(stack-dynamic) 변수는 그 변수가 선언된 하위프로그램이 호출될 때 생성되고, 그 하위프로그램이 반환하면 소멸한다.', '힙 동적(heap-dynamic) 변수는 프로그래머가 명시적으로 요청(예: 동적 할당)하거나 프로그램 구조상 필요할 때 생성되며, 그 수명은 코드상 어느 범위에 선언되었는지가 아니라 할당·해제 시점으로 결정된다.', '변수의 수명(lifetime)과 스코프(scope)는 완전히 같은 개념이므로, 스코프를 벗어나는 순간이 곧 그 변수가 소멸하는 시점과 항상 정확히 일치한다.']}
  correctAnswer={4}
  explanation="수명은 변수가 실제로 메모리에 존재하는 시간(생성부터 소멸까지)을 말하고, 스코프는 그 변수의 이름을 코드에서 참조할 수 있는 텍스트상의 범위를 말합니다. 두 개념은 종종 겹치지만 항상 같지는 않습니다. 예를 들어 정적 지역 변수는 하위프로그램을 벗어나면 스코프(이름을 쓸 수 있는 범위)는 끝나지만, 메모리에는 프로그램이 끝날 때까지 값이 남아 있어 수명은 스코프보다 훨씬 깁니다."
  optionExplanations={['정적 변수의 수명이 하위프로그램 반환 이후에도 유지된다는 것은 옳은 설명입니다.', '스택 동적 변수(일반적인 지역 변수)가 호출 시 생성되고 반환 시 소멸한다는 것은 옳은 설명입니다.', '힙 동적 변수의 수명이 코드상 스코프가 아니라 할당·해제 시점에 의해 결정된다는 것은 옳은 설명입니다.', '정적 지역 변수처럼 스코프가 끝나도 수명이 계속되는 사례가 있으므로 두 개념이 항상 일치한다는 설명이 옳지 않아 정답입니다.']}
/>

## 문제 11

<Quiz
  questionNumber={11}
  question="스코프(scope)와 수명(lifetime)의 차이를 설명한 것으로 가장 적절한 것은?"
  options={['스코프는 이름을 참조할 수 있는 코드상의 텍스트 범위를 뜻하고, 수명은 그 이름에 바인딩된 저장 공간이 실제로 메모리에 존재하는 시간 구간을 뜻한다.', '스코프는 실행 시간에 관한 개념이고 수명은 코드의 텍스트 구조에 관한 개념이다.', '스코프와 수명은 완전히 같은 뜻을 가진 동의어로, 프로그래밍언어론에서는 두 용어를 구분할 필요가 없다.', '스코프는 오직 전역 변수에만, 수명은 오직 지역 변수에만 적용되는 서로 배타적인 개념이다.']}
  correctAnswer={1}
  explanation="스코프는 '어디에서 그 이름을 쓸 수 있는가'라는 텍스트(코드) 상의 개념이고, 수명은 '그 변수가 실제로 언제부터 언제까지 메모리에 존재하는가'라는 실행 시간상의 개념입니다. 두 개념은 정의가 다르기 때문에 문제 10의 정적 지역 변수처럼 스코프와 수명이 어긋나는 사례가 생깁니다."
  optionExplanations={['스코프는 텍스트상 참조 가능 범위, 수명은 실행 중 존재 구간이라는 정의를 정확히 대비했으므로 정답입니다.', '실제로는 반대로 스코프가 텍스트 구조에 관한 개념이고 수명이 실행 시간에 관한 개념이므로 설명이 뒤바뀌어 틀렸습니다.', '문제 10에서 보였듯 두 개념이 항상 일치하지는 않는 별개의 개념이므로 동의어라는 설명은 틀렸습니다.', '스코프와 수명은 전역 변수·지역 변수 모두에 적용되는 개념이므로 서로 배타적이라는 설명은 틀렸습니다.']}
/>

## 문제 12

<Quiz
  questionNumber={12}
  question="원시형(primitive type)·구조형(structured type)·사용자 정의형(user-defined type)을 구분한 설명으로 옳지 않은 것은?"
  options={['원시형은 언어가 기본으로 제공하는 정수·실수·문자·불리언 등으로, 더 이상 다른 형을 조합해 정의된 것이 아니다.', '구조형은 배열·레코드처럼 여러 개의 값(원소)을 하나의 이름으로 묶어 다루는 형이다.', '사용자 정의형은 프로그래머가 기존 형들을 조합하거나 별도의 정의를 통해 만든 새로운 형으로, 열거형이나 구조체 기반의 새 이름 붙이기 등이 해당한다.', '원시형·구조형·사용자 정의형은 서로 배타적이어서, 구조형 안에는 절대로 원시형 원소가 들어갈 수 없다.']}
  correctAnswer={4}
  explanation="구조형은 오히려 원시형을 원소로 담는 경우가 대부분입니다. 정수 배열은 원시형인 정수를 원소로 갖는 구조형이고, 레코드도 여러 원시형·구조형 필드를 담을 수 있습니다. 세 분류가 서로 완전히 배타적이어서 구조형 안에 원시형이 들어갈 수 없다는 설명은 옳지 않습니다."
  optionExplanations={['원시형이 더 이상 분해되지 않는 기본 형이라는 정의는 옳은 설명입니다.', '구조형이 여러 값을 하나로 묶는 형이라는 정의는 옳은 설명입니다.', '사용자 정의형이 기존 형을 조합해 새로 만든 형이라는 정의는 옳은 설명입니다.', '정수 배열처럼 구조형이 원시형을 원소로 담는 것이 일반적이므로, 절대 담을 수 없다는 설명이 옳지 않아 정답입니다.']}
/>

## 문제 13

<Quiz
  questionNumber={13}
  question="강한 형 검사(strong typing)와 약한 형 검사(weak typing)의 차이를 설명한 것으로 가장 적절한 것은?"
  options={['강형 언어는 모든 형 오류(잘못된 연산에 형이 맞지 않는 값을 쓰는 것)를 컴파일 시점이든 실행 시점이든 반드시 검출해 형이 맞지 않는 연산이 조용히 넘어가지 않도록 하는 경향이 강한 반면, 약형 언어는 서로 다른 형 사이의 암묵적 변환·연산을 비교적 관대하게 허용하는 경향이 있다.', '강형 언어는 변수를 선언할 때 반드시 형을 명시해야 하는 언어를 뜻하고, 약형 언어는 형 선언 자체가 아예 불가능한 언어를 뜻한다.', '강형 언어와 약형 언어는 실행 속도로만 구분되는 개념이다.', '강형 언어는 항상 동적 타입 언어이고 약형 언어는 항상 정적 타입 언어이다.']}
  correctAnswer={1}
  explanation="강형/약형은 형이 맞지 않는 연산을 언어가 얼마나 엄격하게 걸러내는가에 대한 개념입니다. 강형 언어는 형 오류를 확실하게 검출해 프로그램이 형을 무시한 채 계속 실행되지 않도록 하는 경향이 강하고, 약형 언어는 서로 다른 형끼리도 암묵적으로 변환해 연산을 허용하는 경우가 많습니다. 이는 정적/동적 타입 여부(형 검사 시점)와는 별개의 축입니다."
  optionExplanations={['형 오류 검출의 엄격성이라는 강형·약형 구분의 핵심 기준을 정확히 설명했으므로 정답입니다.', '강형/약형 구분은 형 선언 문법의 유무가 아니라 형 오류에 대한 엄격성에 관한 것이므로 틀렸습니다.', '강형/약형은 형 검사의 엄격성에 관한 개념이지 실행 속도 자체를 정의하는 개념이 아니므로 틀렸습니다.', '강형이면서 동적 타입인 언어, 약형이면서 정적 타입에 가까운 언어처럼 두 축은 서로 독립적이므로 항상 짝지어진다는 설명은 틀렸습니다.']}
/>

## 문제 14

<Quiz
  questionNumber={14}
  question="정적 형 시스템(static type system)과 동적 형 시스템(dynamic type system)을 비교한 것으로 옳지 않은 것은?"
  options={['정적 형 시스템은 변수의 형이 컴파일 시점에 결정되고 고정되어, 실행 전에 많은 형 오류를 찾아낼 수 있는 경향이 있다.', '동적 형 시스템은 변수 자체보다 실행 중 값에 형이 결부되는 경향이 있어, 같은 변수 이름이 실행 흐름에 따라 서로 다른 형의 값을 가리킬 수 있다.', '동적 형 시스템을 쓰는 언어는 컴파일이라는 절차 자체가 존재할 수 없다.', '정적 형 시스템은 일반적으로 형 오류로 인한 실행 중 예기치 못한 동작의 위험을 줄여 주지만, 형을 명시해야 하므로 코드 작성이 상대적으로 번거로울 수 있다.']}
  correctAnswer={3}
  explanation="동적 형 시스템을 쓰는 언어도 소스 코드를 바이트코드 등으로 컴파일한 뒤 실행하는 경우가 많습니다(예: 일부 스크립트 언어의 바이트코드 컴파일). 동적 형 시스템은 형 검사가 실행 중에 이루어진다는 뜻이지, 컴파일 절차 자체가 없다는 뜻이 아닙니다."
  optionExplanations={['정적 형 시스템이 컴파일 시점에 형 오류를 미리 찾아낸다는 것은 옳은 설명입니다.', '동적 형 시스템에서 같은 변수 이름이 실행 중 다른 형의 값을 가리킬 수 있다는 것은 옳은 설명입니다.', '동적 형 시스템도 내부적으로 컴파일 단계를 거칠 수 있으므로, 컴파일 자체가 존재할 수 없다는 설명이 옳지 않아 정답입니다.', '정적 형 시스템이 안정성을 높여 주는 대신 형 명시의 번거로움이 있다는 것은 옳은 트레이드오프 설명입니다.']}
/>

## 문제 15

<Quiz
  questionNumber={15}
  question="형 검사(type checking)와 형 추론(type inference)의 관계를 설명한 것으로 가장 적절한 것은?"
  options={['형 검사는 어떤 연산에 사용된 값들의 형이 그 연산에 맞는지 확인하는 과정이고, 형 추론은 프로그래머가 형을 일일이 명시하지 않아도 컴파일러가 문맥으로부터 그 값의 형을 스스로 알아내는 과정이다.', '형 검사와 형 추론은 완전히 같은 절차를 가리키는 다른 이름일 뿐이다.', '형 추론이 가능한 언어는 형 검사를 절대로 수행하지 않는다.', '형 검사는 실행 중에만 가능하고 형 추론은 실행 전에만 가능하다.']}
  correctAnswer={1}
  explanation="형 검사는 연산에 쓰인 값들의 형이 그 연산의 요구 조건에 맞는지 확인하는 절차이고, 형 추론은 프로그래머가 형을 명시하지 않아도 초기값이나 사용 문맥을 분석해 컴파일러가 형을 스스로 결정해 주는 절차입니다. 형 추론으로 형이 결정된 뒤에도 형 검사는 그대로 수행됩니다."
  optionExplanations={['형 검사(적합성 확인)와 형 추론(형 결정)의 역할 차이를 정확히 설명했으므로 정답입니다.', '형 추론은 형을 알아내는 절차이고 형 검사는 그렇게 알아낸(또는 명시된) 형이 맞는지 확인하는 절차로 서로 다른 개념이므로 틀렸습니다.', '형 추론으로 형이 정해진 뒤에도 그 형이 연산에 맞는지 확인하는 형 검사는 여전히 이루어지므로 틀렸습니다.', '형 검사·형 추론 모두 정적으로(컴파일 시점) 이루어질 수도, 동적으로(실행 시점) 이루어질 수도 있으므로 시점을 고정해 말한 설명은 틀렸습니다.']}
/>

## 문제 16

<Quiz
  questionNumber={16}
  question="강제 형 변환인 코어션(coercion)과 프로그래머가 명시적으로 지시하는 캐스팅(casting)의 차이로 가장 적절한 것은?"
  options={['코어션은 언어가 연산 문맥에 맞춰 암묵적으로 자동으로 형을 바꾸는 것이고, 캐스팅은 프로그래머가 코드에 형 변환을 명시적으로 적어 넣는 것이다.', '코어션과 캐스팅은 완전히 같은 절차이며 순서만 다르게 부르는 이름이다.', '코어션은 항상 정보 손실 없이 이루어지고, 캐스팅은 항상 정보 손실이 발생한다.', '캐스팅은 원시형에는 적용할 수 없고 오직 사용자 정의형에만 적용할 수 있다.']}
  correctAnswer={1}
  explanation="코어션은 예를 들어 정수와 실수를 더할 때 언어가 자동으로 정수를 실수로 바꿔 주는 것처럼 프로그래머의 명시적 지시 없이 문맥에 따라 자동으로 일어나는 변환입니다. 캐스팅은 프로그래머가 코드에 명시적으로 형 변환 연산을 적어 컴파일러에게 변환을 지시하는 것입니다."
  optionExplanations={['암묵적 자동 변환(코어션)과 명시적 지시 변환(캐스팅)이라는 구분을 정확히 설명했으므로 정답입니다.', '코어션은 암묵적, 캐스팅은 명시적이라는 서로 다른 성격을 가지므로 완전히 같은 절차라는 설명은 틀렸습니다.', '실수를 정수로 좁혀 캐스팅하면 소수 부분이 잘려 정보 손실이 생기지만, 반대로 정수를 넓혀 실수로 코어션해도 아주 큰 정수에서는 정밀도 손실이 있을 수 있어 항상 손실이 없다·있다고 단정할 수 없으므로 틀렸습니다.', '캐스팅은 정수를 실수로 바꾸는 것처럼 원시형 사이에서도 흔히 쓰이므로 사용자 정의형에만 적용된다는 설명은 틀렸습니다.']}
/>

## 문제 17

<Quiz
  questionNumber={17}
  question="형 변환과 관련해 시험에서 자주 지적되는 함정으로 가장 적절한 것은?"
  options={['큰 범위의 형(예: 배정도 실수)을 더 좁은 범위의 형(예: 정수)으로 변환하는 좁히기 변환(narrowing conversion)은 소수 부분이 잘리거나 값이 표현 범위를 벗어나 정보 손실이 발생할 수 있다.', '좁은 형에서 넓은 형으로 가는 넓히기 변환(widening conversion)은 어떤 언어에서도 절대로 일어나지 않는다.', '형 변환은 산술 연산에만 관련이 있고 논리·비교 연산과는 전혀 관계가 없다.', '형 변환이 일어나면 반드시 프로그램이 즉시 종료된다.']}
  correctAnswer={1}
  explanation="실수를 정수로 변환하면 소수 부분이 버려지고, 큰 정수형을 작은 정수형으로 변환하면 표현 범위를 넘는 값에서 오버플로가 발생할 수 있습니다. 이러한 좁히기 변환에서의 정보 손실은 실제 코드 오류의 흔한 원인이며, 프로그래밍언어론 시험에서도 자주 다루어지는 함정입니다."
  optionExplanations={['좁히기 변환에서 정보 손실이 발생할 수 있다는 실제 함정을 정확히 설명했으므로 정답입니다.', '정수를 실수로 바꾸는 것과 같은 넓히기 변환은 여러 언어에서 흔히 자동으로 일어나므로 절대 일어나지 않는다는 설명은 틀렸습니다.', '문자와 정수 코드 값 사이의 변환처럼 비교·논리 맥락에서도 형 변환이 관련될 수 있으므로 틀렸습니다.', '형 변환이 일어난다고 해서 프로그램이 즉시 종료되는 것은 아니며, 대부분은 변환된 값으로 계속 실행되므로 틀렸습니다.']}
/>

## 문제 18

<Quiz
  questionNumber={18}
  question="다음 중 형 시스템·형 변환에 관한 설명으로 옳지 않은 것은?"
  options={['정적 형 시스템에서는 컴파일러가 변수의 형을 이용해 잘못된 연산을 실행 전에 걸러낼 수 있다.', '동적 형 시스템에서는 같은 코드 위치라도 실행할 때마다 그 변수가 가리키는 값의 형이 달라질 수 있다.', '좁히기 변환은 정보 손실 위험이 있으므로 일부 언어는 이를 명시적으로 요구하거나 경고를 낸다.', '형 변환 규칙은 모든 프로그래밍언어에서 완전히 동일하게 정의되어 있어 언어 간 차이가 전혀 없다.']}
  correctAnswer={4}
  explanation="형 변환 규칙(어떤 변환을 암묵적으로 허용할지, 어떤 변환은 반드시 명시하게 할지)은 언어마다 상당히 다릅니다. 예를 들어 어떤 언어는 정수와 실수의 자동 덧셈을 허용하지만, 형 검사가 매우 엄격한 언어는 이를 금지하고 명시적 변환을 요구합니다. 언어 간 차이가 전혀 없다는 설명은 옳지 않습니다."
  optionExplanations={['정적 형 시스템이 실행 전 오류 검출에 도움을 준다는 것은 옳은 설명입니다.', '동적 형 시스템에서 값의 형이 실행마다 달라질 수 있다는 것은 옳은 설명입니다.', '좁히기 변환의 위험 때문에 명시적 표기를 요구하는 언어가 있다는 것은 옳은 설명입니다.', '형 변환 규칙은 언어 설계에 따라 크게 달라지므로 완전히 동일하다는 설명이 옳지 않아 정답입니다.']}
/>

## 문제 19

<Quiz
  questionNumber={19}
  question="배열(array)과 레코드(record, 구조체)를 비교한 설명으로 가장 적절한 것은?"
  options={['배열은 같은 형의 원소를 인덱스로 접근하는 동질적(homogeneous) 구조인 반면, 레코드는 서로 다른 형의 필드를 이름으로 접근하는 이질적(heterogeneous) 구조이다.', '배열과 레코드는 항상 같은 형의 원소만 담을 수 있다는 점에서 완전히 동일한 구조이다.', '레코드는 인덱스로만 필드에 접근할 수 있고 이름으로는 접근할 수 없다.', '배열은 필드 이름을 가질 수 있지만 레코드는 이름을 가질 수 없다.']}
  correctAnswer={1}
  explanation="배열은 정수 인덱스로 접근하는, 모두 같은 형인 원소들의 모음입니다. 레코드는 이름이 붙은 필드들로 구성되며 각 필드가 서로 다른 형을 가질 수 있는 이질적 구조입니다. 이 동질성/이질성과 접근 방식(인덱스 vs 이름)의 차이가 두 자료형을 구분하는 핵심입니다."
  optionExplanations={['동질적·인덱스 접근(배열)과 이질적·이름 접근(레코드)이라는 핵심 차이를 정확히 설명했으므로 정답입니다.', '레코드는 서로 다른 형의 필드를 담을 수 있는 것이 핵심 특징이므로 완전히 동일한 구조라는 설명은 틀렸습니다.', '레코드는 필드 이름(예: 이름.나이)으로 접근하는 것이 일반적이므로 틀렸습니다.', '이름으로 필드를 구분하는 것은 레코드의 특징이며 배열은 보통 정수 인덱스로 접근하므로 설명이 뒤바뀌어 틀렸습니다.']}
/>

## 문제 20

<Quiz
  questionNumber={20}
  question="집합(set) 자료형을 언어에서 지원할 때의 특징으로 가장 적절한 것은?"
  options={['원소의 중복을 허용하지 않고 순서를 따지지 않는 값들의 모음을 표현하며, 합집합·교집합·차집합 같은 연산을 언어 차원에서 직접 제공하는 경우가 있다.', '집합은 항상 원소의 순서를 엄격하게 유지해야 하는 자료형이다.', '집합은 배열과 완전히 동일한 내부 구현과 연산만 가능한 자료형이다.', '집합 자료형은 오직 정수만 원소로 가질 수 있다.']}
  correctAnswer={1}
  explanation="집합 자료형은 수학의 집합 개념을 반영해, 중복 없는 원소들의 순서 없는 모음을 표현합니다. 일부 언어(예: Pascal)는 집합을 언어 차원의 자료형으로 지원하며 합집합·교집합·차집합·포함 여부 판정 등의 연산을 기본으로 제공합니다."
  optionExplanations={['중복 불허·순서 무관이라는 집합의 수학적 정의와 언어의 집합 연산 지원을 정확히 설명했으므로 정답입니다.', '집합은 원소 간 순서를 따지지 않는 것이 정의상 특징이므로 순서를 엄격히 유지해야 한다는 설명은 틀렸습니다.', '집합은 중복을 허용하지 않고 순서가 없다는 점에서 배열과 다른 별도의 의미론을 가지므로 완전히 동일하다는 설명은 틀렸습니다.', '집합의 원소형은 언어 설계에 따라 문자·열거형 등 다양할 수 있으므로 오직 정수만 가능하다는 설명은 틀렸습니다.']}
/>

## 문제 21

<Quiz
  questionNumber={21}
  question="포인터(pointer)와 참조(reference)의 차이를 설명한 것으로 가장 적절한 것은?"
  options={['포인터는 메모리 주소 값 자체를 담는 변수로 산술 연산이나 재대입이 비교적 자유로운 경우가 많은 반면, 참조는 어떤 변수의 별명(alias)처럼 동작하도록 설계되어 일반적으로 재대입이나 주소 연산이 제한되는 경우가 많다.', '포인터와 참조는 이름만 다를 뿐 모든 언어에서 완전히 동일한 기능과 제약을 가진다.', '참조는 항상 널(null) 값을 가질 수 있지만 포인터는 절대로 널 값을 가질 수 없다.', '포인터는 오직 함수의 반환값에만 쓰일 수 있고 변수 선언에는 쓰일 수 없다.']}
  correctAnswer={1}
  explanation="포인터는 대체로 메모리 주소를 값으로 갖는 독립적인 변수로, 언어에 따라 주소 산술이나 다른 주소로의 재대입이 허용됩니다. 참조는 한 번 특정 변수에 묶이면 그 변수의 또 다른 이름(별명)처럼 동작하도록 설계되어, 다른 대상을 다시 가리키게 하거나 주소 연산을 하는 것을 제한하는 언어가 많습니다."
  optionExplanations={['주소 값을 다루는 자유도와 별명으로서의 제약이라는 실제 설계 차이를 정확히 설명했으므로 정답입니다.', '언어마다 포인터·참조의 구체적 규칙(널 허용 여부, 산술 허용 여부 등)이 다르므로 완전히 동일하다는 설명은 틀렸습니다.', '포인터도 널 값을 가질 수 있도록 설계된 언어가 많고, 참조가 널을 허용하지 않도록 설계된 언어도 있으므로 이 설명은 실제와 다르게 단정해 틀렸습니다.', '포인터는 변수 선언에서도 흔히 사용되는 자료형이므로 반환값에만 쓰인다는 설명은 틀렸습니다.']}
/>

## 문제 22

<Quiz
  questionNumber={22}
  question="댕글링 포인터(dangling pointer)가 발생하는 상황을 가장 정확히 설명한 것은?"
  options={['포인터가 가리키던 메모리 영역이 해제(free)되었거나 그 변수의 수명이 끝났는데도, 포인터 변수 자체는 여전히 그 이전 주소 값을 들고 있어 유효하지 않은 메모리를 가리키게 되는 상황이다.', '포인터가 처음부터 아무 주소도 대입받지 않은 상태를 가리키는 용어이다.', '두 개의 포인터가 서로 다른 변수를 각각 정상적으로 가리키고 있는 평범한 상황을 가리키는 용어이다.', '댕글링 포인터는 정적 타입 언어에서는 이론적으로 절대 발생할 수 없는 현상이다.']}
  correctAnswer={1}
  explanation="댕글링 포인터는 가리키던 대상이 해제되거나 수명이 끝난 뒤에도 포인터에 남아 있는 옛 주소 값을 그대로 사용하려 할 때 생기는 문제입니다. 이미 무효화된 메모리에 접근하므로 예측할 수 없는 동작이나 오류로 이어질 수 있습니다."
  optionExplanations={['해제·수명 종료 이후에도 옛 주소를 들고 있는 상황이라는 정의를 정확히 설명했으므로 정답입니다.', '아무 주소도 대입받지 않은 상태는 초기화되지 않은 포인터를 가리키는 것으로, 댕글링 포인터와는 다른 개념이라 틀렸습니다.', '각각 정상적으로 유효한 대상을 가리키는 상황은 문제가 없는 정상 사례이므로 댕글링 포인터의 정의와 무관해 틀렸습니다.', '동적 메모리 할당·해제를 지원하는 정적 타입 언어(예: C)에서도 댕글링 포인터는 실제로 흔히 발생하는 문제이므로 틀렸습니다.']}
/>

## 문제 23

<Quiz
  questionNumber={23}
  question="메모리 누수(memory leak)와 가비지 컬렉션(garbage collection)의 관계를 설명한 것으로 가장 적절한 것은?"
  options={['메모리 누수는 더 이상 필요 없어진 메모리를 프로그램이 해제하지 못해 사용 가능한 메모리가 점점 줄어드는 문제이며, 가비지 컬렉션은 더 이상 참조되지 않는 메모리를 자동으로 찾아 회수함으로써 이 문제를 줄이려는 기법이다.', '가비지 컬렉션을 사용하는 언어에서는 메모리 누수와 비슷한 문제가 절대로 일어날 수 없다.', '메모리 누수는 오직 포인터가 없는 언어에서만 발생하는 문제이다.', '가비지 컬렉션은 프로그램 실행 속도를 항상 아무런 대가 없이 빠르게 만들어 주는 기법이다.']}
  correctAnswer={1}
  explanation="메모리 누수는 더 이상 쓰지 않는 메모리를 계속 붙들고 있어 사용 가능한 메모리가 줄어드는 현상이며, 명시적으로 메모리를 해제해야 하는 언어에서 해제를 빠뜨릴 때 자주 발생합니다. 가비지 컬렉션은 더 이상 어디에서도 참조되지 않는 객체를 런타임이 자동으로 탐지해 회수함으로써 이런 누수를 줄이려는 기법입니다."
  optionExplanations={['메모리 누수의 정의와 가비지 컬렉션이 이를 완화하는 방식을 정확히 설명했으므로 정답입니다.', '가비지 컬렉션을 쓰는 언어에서도 의도치 않게 객체에 대한 참조를 계속 남겨 두면 논리적인 메모리 누수와 비슷한 문제가 생길 수 있으므로 절대 발생하지 않는다는 설명은 틀렸습니다.', '메모리 누수는 포인터가 있는 언어(수동 해제가 필요한 언어)에서 특히 흔히 논의되는 문제이므로 포인터가 없는 언어에서만 발생한다는 설명은 틀렸습니다.', '가비지 컬렉션은 회수 작업 자체에 실행 시간과 자원이 들어 일시적인 성능 오버헤드(대가)가 있을 수 있으므로 아무 대가가 없다는 설명은 틀렸습니다.']}
/>

## 문제 24

<Quiz
  questionNumber={24}
  question="이름·바인딩·스코프·수명·형 시스템을 종합적으로 비교한 설명 중 옳지 않은 것은?"
  options={['정적 스코프에서 비지역 변수를 찾을 때는 실행 중 호출 순서가 아니라 코드상 중첩 구조를 기준으로 삼는다.', '정적 지역 변수(static local variable)는 스코프는 하위프로그램 내부로 제한되지만 수명은 프로그램 전체 실행 기간과 같을 수 있다.', '강형 언어일수록 반드시 동적 형 시스템을 채택해야 하고, 약형 언어일수록 반드시 정적 형 시스템을 채택해야 한다.', '댕글링 포인터와 메모리 누수는 모두 동적으로 할당된 메모리의 수명 관리를 잘못했을 때 나타나는 문제라는 공통점이 있다.']}
  correctAnswer={3}
  explanation="강형/약형(형 오류 검출의 엄격성)과 정적/동적 형 시스템(형이 결정되는 시점)은 서로 독립적인 축입니다. 강형이면서 동적 타입인 언어도 있고, 강형이면서 정적 타입인 언어도 있으므로 강형이면 반드시 동적이어야 한다는 식의 필연적 연결은 성립하지 않습니다."
  optionExplanations={['정적 스코프가 코드상 중첩 구조를 기준으로 한다는 것은 문제 6~9에서 확인한 옳은 설명입니다.', '정적 지역 변수의 스코프와 수명이 다를 수 있다는 것은 문제 10~11에서 확인한 옳은 설명입니다.', '강형·약형과 정적·동적 형 시스템은 서로 독립적인 별개의 축이므로, 하나가 다른 하나를 필연적으로 결정한다는 이 설명이 옳지 않아 정답입니다.', '댕글링 포인터와 메모리 누수가 모두 동적 메모리의 수명 관리 실패라는 공통점을 갖는다는 것은 문제 22~23에서 확인한 옳은 설명입니다.']}
/>

## 참고 자료

- 국가평생교육진흥원 독학학위제 — https://bdes.nile.or.kr
- cppreference.com, Pointers and references — https://en.cppreference.com/w/cpp/language/pointer
