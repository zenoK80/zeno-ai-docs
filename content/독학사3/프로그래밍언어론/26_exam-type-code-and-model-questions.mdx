---
title: '26. 코드·모델 추론형 예상문제 (제어·하위프로그램·활성화 레코드)'
description: '제어 구조·매개변수 전달·활성화 레코드·정적/동적 체인·언어 구현 방식을 국가평생교육진흥원 출제기준에 맞춰 재구성한 코드·모델 추론형 24문항, 코드 결과 대조와 스택 그림 해설 포함'
---

<Callout type="warning">
  **재구성 출제 고지:** 이 편은 국가평생교육진흥원 독학학위제 컴퓨터과학 전공 3단계 「프로그래밍언어론」의 **최신 출제기준(평가영역)** 중 제어 구조, 하위프로그램·매개변수 전달, 하위프로그램 구현(활성화 레코드), 언어 구현 방식 영역을 근거로 새로 구성한 **코드·모델 추론형 예상문제 24문항**입니다. 실제 특정 회차의 기출 문항을 그대로 옮기거나 암기해 재현한 것이 아니며, 13~15·21편에서 다룬 개념을 의사코드 실행 추적과 스택 그림 해설로 재구성했습니다. 실제 응시 시 문항 수·배점·시험 시간은 그해 시행 공고를 반드시 확인하시기 바랍니다. 아래 의사코드는 특정 프로그래밍언어의 실제 문법이 아니라 매개변수 전달·스코프 규칙을 비교하기 위한 교육용 표기입니다.
</Callout>

## 이 편의 영역 구성

| 문항 번호 | 출제기준 영역 | 관련 편 | 유형 |
| --- | --- | --- | --- |
| 1–4 | 제어 구조 설계(선택·반복·다중 루프·case) | 13편 | 코드 추적 |
| 5–10 | 매개변수 전달 방식(값·참조·값-결과·이름) | 14편 | 코드 결과 대조 |
| 11–17 | 활성화 레코드 구조·정적 체인·동적 체인·재귀 구현 | 15편 | 모델·스택 그림 |
| 18–22 | 언어 구현 방식(컴파일러·인터프리터·링크) | 21편 | 개념·비교 |
| 23–24 | 매개변수 전달과 활성화 레코드 종합 | 14–15편 | 종합·코드 추적 |

## 문제 1

```text
i = 1
while i <= 3 do:
    j = 1
    while j <= 2 do:
        만약 j == 2이면 break(안쪽 루프만 탈출)
        (i, j)를 출력한다
        j = j + 1
    i = i + 1
```

<Quiz
  questionNumber={1}
  question="위 의사코드에서 break가 안쪽 while 루프만 탈출하는 구조적 제어문일 때, 총 몇 번 (i, j) 쌍이 출력되는가?"
  options={['3번', '6번', '2번', '1번']}
  correctAnswer={1}
  explanation="바깥 루프는 i가 1, 2, 3일 때 각각 한 번씩 총 3번 실행됩니다. 매번 안쪽 루프에서 j=1일 때 조건(j가 2)이 거짓이므로 먼저 (i, 1)을 출력하고 j를 2로 늘린 뒤, 다음 반복에서 j가 2가 되어 출력 전에 break로 안쪽 루프만 빠져나갑니다. 따라서 바깥 루프 한 바퀴당 정확히 1번씩 출력되어 총 3번 출력됩니다."
  optionExplanations={['바깥 루프 3바퀴 각각에서 안쪽 루프가 1번씩만 출력을 내고 break로 빠져나가므로 총 3번이 정답입니다.', '안쪽 루프가 매번 2번씩 출력한다고 착각하면 나오는 오답으로, break가 두 번째 반복에서 출력 전에 실행됨을 놓친 것입니다.', 'break가 바깥 루프까지 함께 탈출한다고 착각해 전체가 1바퀴만 도는 것으로 오해한 값입니다.', '바깥 루프가 아예 실행되지 않는다고 착각한 값으로, i=1 조건이 처음부터 참임을 놓친 것입니다.']}
/>

## 문제 2

<Quiz
  questionNumber={2}
  question="문제 1의 break가 만약 안쪽 루프뿐 아니라 바깥 루프까지 함께 탈출하는 언어였다면(다중 루프 탈출), 몇 번 출력되었겠는가?"
  options={['1번', '3번', '0번', '6번']}
  correctAnswer={1}
  explanation="다중 루프를 한 번에 탈출하는 break라면, i=1일 때 안쪽 루프에서 (1,1)을 한 번 출력한 뒤 j=2에서 break가 걸리는 순간 바깥 루프까지 완전히 종료됩니다. 따라서 프로그램 전체에서 출력은 단 1번뿐입니다."
  optionExplanations={['바깥 루프까지 한 번에 탈출하므로 전체 실행에서 출력이 딱 1번만 일어난다는 것이 정답입니다.', '이 값은 안쪽 루프만 탈출하는(문제 1의) 경우의 결과이므로, 다중 루프 탈출 상황에는 맞지 않는 오답입니다.', '적어도 첫 번째 바깥 루프 반복에서 break 전에 한 번은 출력이 일어나므로 0번이라는 답은 틀렸습니다.', '이 값은 break가 아무 루프도 탈출하지 못하는 것으로 착각했을 때 나오는 값으로 오답입니다.']}
/>

## 문제 3

```text
n = 3
switch n:
    case 1: A를 출력한다
    case 2: B를 출력한다
    case 3: C를 출력한다
    case 4: D를 출력한다
    default: E를 출력한다
(각 case 끝에 break가 없다고 가정 — fall-through 언어)
```

<Quiz
  questionNumber={3}
  question="case마다 break가 없어 다음 case로 그대로 흘러 내려가는(fall-through) 언어에서 위 코드를 실행하면 출력되는 것은?"
  options={['C, D, E', 'C', 'A, B, C, D, E', 'E']}
  correctAnswer={1}
  explanation="n이 3이므로 실행은 case 3의 문장(C를 출력)부터 시작합니다. break가 없으므로 그 아래에 있는 case 4(D를 출력), default(E를 출력)까지 순서대로 계속 흘러 내려가 모두 실행됩니다. 위쪽의 case 1·2는 건너뛰므로 실행되지 않습니다."
  optionExplanations={['일치하는 case부터 시작해 break 없이 그 아래 모든 case를 순서대로 실행한다는 fall-through 규칙을 정확히 반영했으므로 정답입니다.', 'break가 있다고 가정했을 때(정상적으로 case 3만 실행)의 결과이므로, break가 없다는 조건을 놓친 오답입니다.', 'switch는 일치하는 case부터 실행을 시작하므로 그 이전의 case 1·2까지 모두 실행된다고 본 것은 fall-through의 시작 지점을 잘못 이해한 오답입니다.', 'default만 실행된다고 본 것은 case 3이 일치했다는 사실 자체를 놓친 오답입니다.']}
/>

## 문제 4

<Quiz
  questionNumber={4}
  question="switch 문의 fall-through 특성이 시험에서 자주 지적되는 함정인 이유로 가장 적절한 것은?"
  options={['프로그래머가 각 case 끝에 break를 넣는 것을 잊으면, 의도치 않게 다음 case의 코드까지 실행되어 예상과 다른 결과가 나올 수 있기 때문이다.', 'fall-through가 있는 언어는 switch 문 자체를 아예 사용할 수 없기 때문이다.', 'fall-through는 오직 default 절에서만 발생하고 일반 case에서는 절대 발생하지 않기 때문이다.', 'fall-through가 있으면 조건에 맞는 case를 찾는 순서가 무작위로 바뀌기 때문이다.']}
  correctAnswer={1}
  explanation="fall-through는 break를 빠뜨리면 실행이 다음 case로 자연스럽게 이어져 프로그래머가 의도하지 않은 문장까지 실행되게 만드는 대표적인 함정입니다. 이 때문에 다중 조건에서 신중하게 break 배치를 검토해야 하며, 독학사 시험에서도 fall-through로 인한 출력 결과를 묻는 문제가 자주 출제됩니다."
  optionExplanations={['break 누락 시 의도하지 않은 다음 case까지 실행된다는 실제 함정을 정확히 설명했으므로 정답입니다.', 'fall-through가 있는 언어도 switch 문을 정상적으로 지원하며 다만 break로 명시적으로 흐름을 끊어야 할 뿐이므로 틀렸습니다.', 'fall-through는 case든 default든 break가 없으면 동일하게 다음 문장으로 흘러 내려가므로 default에만 국한된다는 설명은 틀렸습니다.', 'case를 찾는 순서(어느 case가 조건과 일치하는지 판정하는 것)와 fall-through(일치한 이후 흐름이 아래로 이어지는 것)는 서로 다른 단계이므로 순서가 무작위가 된다는 설명은 틀렸습니다.']}
/>

## 문제 5

다음 하위프로그램 `swap`이 두 값을 서로 바꾸려는 목적으로 작성되었다고 하자.

```text
procedure swap(a, b):
    temp = a
    a = b
    b = temp

x = 1
y = 2
call swap(x, y)   // 값 전달(pass by value) 방식
x와 y를 출력한다
```

<Quiz
  questionNumber={5}
  question="매개변수 전달 방식이 값 전달(pass by value)일 때, swap 호출 이후 x와 y의 값은?"
  options={['x=1, y=2 (바뀌지 않는다)', 'x=2, y=1 (서로 바뀐다)', 'x=2, y=2', 'x=1, y=1']}
  correctAnswer={1}
  explanation="값 전달에서는 호출 시점에 실제 인자의 값을 형식 매개변수(a, b)에 복사합니다. swap 내부에서 a와 b를 아무리 바꾸어도 그것은 복사본에 대한 조작일 뿐, 호출자의 변수 x, y와는 저장 공간이 분리되어 있으므로 x, y는 전혀 영향을 받지 않습니다."
  optionExplanations={['값 전달은 복사본에 대한 조작이라 호출자의 변수는 그대로 유지된다는 것이 정답입니다.', '이 결과는 참조 전달이나 값-결과 전달처럼 호출자의 변수까지 실제로 바뀌는 방식에서 나오는 결과이므로 값 전달의 결과로는 틀렸습니다.', 'swap 내부에서 a와 b를 맞바꾸는 로직 자체가 잘못 실행되었다고 가정한 것으로, 이 로직 자체는 정상적으로 a와 b를 바꾸므로 틀렸습니다.', '두 변수가 같은 값으로 합쳐진다고 볼 근거가 없는 임의의 오답입니다.']}
/>

## 문제 6

문제 5와 동일한 `swap` 코드를 이번에는 참조 전달(pass by reference)로 호출한다고 가정한다.

<Quiz
  questionNumber={6}
  question="매개변수 전달 방식이 참조 전달일 때, swap 호출 이후 x와 y의 값은?"
  options={['x=2, y=1 (서로 바뀐다)', 'x=1, y=2 (바뀌지 않는다)', 'x=1, y=1', 'x=2, y=2']}
  correctAnswer={1}
  explanation="참조 전달에서는 형식 매개변수 a, b가 실제 인자 x, y와 같은 저장 공간을 공유하는 별명(alias)처럼 동작합니다. swap 내부에서 a와 b의 값을 바꾸는 것이 곧바로 x와 y 자체의 값을 바꾸는 것과 같으므로, 호출이 끝나면 x=2, y=1로 실제로 서로 바뀝니다."
  optionExplanations={['참조 전달에서는 형식 매개변수가 실제 인자와 저장 공간을 공유하므로 값이 실제로 교환된다는 것이 정답입니다.', '이 결과는 값 전달일 때의 결과이며, 참조 전달에서는 호출자의 변수까지 함께 바뀌므로 이 문제의 답으로는 틀렸습니다.', 'swap 로직 자체는 정상적으로 두 값을 교환하는 로직이므로 두 값이 같아진다는 결과는 나올 수 없어 틀렸습니다.', '두 변수가 우연히 같은 값으로 수렴한다고 볼 근거가 없는 임의의 오답입니다.']}
/>

## 문제 7

<Quiz
  questionNumber={7}
  question="문제 5와 문제 6의 결과 차이가 보여주는 값 전달과 참조 전달의 근본적 차이로 가장 적절한 것은?"
  options={['값 전달은 실제 인자의 값을 복사해 형식 매개변수라는 별도의 저장 공간에 넣지만, 참조 전달은 형식 매개변수가 실제 인자의 저장 공간 자체를 가리켜 하위프로그램 안에서의 변경이 호출자에게도 그대로 반영된다.', '값 전달과 참조 전달은 실행 결과에 어떠한 차이도 만들지 않는, 이름만 다른 동일한 방식이다.', '값 전달은 숫자에만 쓸 수 있고 참조 전달은 문자열에만 쓸 수 있다.', '참조 전달은 항상 값 전달보다 느리게 실행되도록 언어 표준에 정해져 있다.']}
  correctAnswer={1}
  explanation="값 전달은 호출자의 값을 복사해 형식 매개변수라는 독립된 저장 공간에 담기 때문에 하위프로그램 안의 변경이 호출자에게 영향을 주지 않습니다. 반면 참조 전달은 형식 매개변수가 실제 인자의 저장 공간 자체를 가리키므로(별명), 하위프로그램 안에서의 변경이 곧 호출자 변수의 변경이 됩니다. 문제 5·6의 swap 결과 차이는 바로 이 저장 공간 공유 여부에서 비롯됩니다."
  optionExplanations={['복사(독립 저장 공간) vs 공유(별명)라는 근본적 차이를 정확히 설명했으므로 정답입니다.', '문제 5와 6에서 실제로 x, y 값이 다르게 나타났으므로 두 방식이 결과 차이를 만들지 않는다는 설명은 틀렸습니다.', '전달 방식은 자료형(숫자·문자열 등)이 아니라 언어의 매개변수 전달 규칙에 따라 정해지는 것이므로 틀렸습니다.', '전달 방식의 속도는 구현과 자료 크기에 따라 달라질 수 있는 구현 세부 사항이지, 참조 전달이 항상 더 느리다고 표준화되어 있지 않으므로 틀렸습니다.']}
/>

## 문제 8

다음은 값-결과 전달(pass by value-result, copy-restore)로 호출되는 하위프로그램이다. 형식 매개변수 a는 실제 인자 x와 연결되고, 또 다른 실제 인자로 배열 원소 `arr[1]`도 함께 넘겨받으며, 이 하위프로그램 안에서 배열 `arr` 자체도 전역으로 접근 가능하다고 하자.

```text
전역 배열 arr = [10, 20]

procedure p(a):
    a = a + 1
    arr[1] = 99   // 전역 배열을 직접 변경

// 값-결과 전달로 arr[1]을 인자로 넘겨 호출
call p(arr[1])
arr[1]을 출력한다
```

<Quiz
  questionNumber={8}
  question="값-결과 전달 방식에서 arr[1]을 넘겼을 때, 호출이 끝난 뒤 arr[1]의 값은?"
  options={['11', '99', '10', '12']}
  correctAnswer={1}
  explanation="값-결과 전달은 호출 시 arr[1]의 값(10)을 형식 매개변수 a에 복사해 시작하고, 하위프로그램이 끝날 때 a의 최종값을 다시 arr[1]에 복사해 넣습니다. 하위프로그램 안에서 arr[1] = 99로 전역 배열을 직접 바꾸는 것은 a와는 별개의 저장 공간에 대한 변경이므로 즉시 arr[1]에 반영되지 않습니다. 이후 a = a + 1로 a는 11이 되고, 하위프로그램이 반환되는 순간 이 a의 최종값 11이 arr[1]에 복사되어 arr[1]에 있던 99를 덮어씁니다."
  optionExplanations={['진입 시 복사, 종료 시 결과를 다시 복사한다는 값-결과 전달의 정의를 정확히 반영해 최종적으로 11이 남는다는 것이 정답입니다.', '하위프로그램 안에서 전역 배열을 직접 바꾼 99는 종료 시 a의 최종값으로 덮어써지므로 최종 결과로 남지 않아 틀렸습니다.', '이 값은 처음 복사된 값(10)이 전혀 바뀌지 않았다고 본 것으로, a = a + 1 연산과 종료 시 재복사를 반영하지 않은 오답입니다.', 'a = a + 1을 두 번 적용한 것처럼 계산한 값으로, 실제로는 한 번만 증가시키므로 틀렸습니다.']}
/>

## 문제 9

<Quiz
  questionNumber={9}
  question="문제 8의 상황을 참조 전달로 바꾸어 arr[1]을 참조로 넘겼다면 결과가 어떻게 달라지는가?"
  options={['하위프로그램 안에서 arr[1] = 99로 전역 배열을 직접 바꾸는 순간 그 변경이 즉시 arr[1]에 반영되므로, 이어지는 a = a + 1도 이미 99가 된 저장 공간에 적용되어 최종적으로 arr[1]은 100이 된다.', '참조 전달이든 값-결과 전달이든 항상 완전히 같은 최종 결과가 나온다.', '참조 전달에서는 arr[1] = 99라는 문장이 아예 실행되지 않는다.', '참조 전달에서는 a와 arr[1]이 서로 다른 저장 공간을 갖게 되어 값-결과 전달보다 오히려 더 독립적으로 동작한다.']}
  correctAnswer={1}
  explanation="참조 전달에서는 a가 arr[1]의 저장 공간 자체를 가리키는 별명이므로, arr[1] = 99라는 전역 배열에 대한 직접 대입이 그 즉시 a가 가리키는 같은 공간에 반영됩니다. 따라서 이후 a = a + 1은 이미 99로 바뀐 값 위에서 계산되어 100이 되고, 이 값이 곧 arr[1]의 값이 됩니다. 값-결과 전달과 달리 진입·종료 시점의 복사가 아니라 매 순간 같은 저장 공간을 공유한다는 점이 이 차이를 만듭니다."
  optionExplanations={['같은 저장 공간을 매 순간 공유하는 참조 전달의 성질을 반영해 최종값 100을 정확히 계산했으므로 정답입니다.', '문제 8(최종 11)과 이 문제(최종 100)의 결과가 다르므로 항상 같은 결과가 나온다는 설명은 틀렸습니다.', 'arr[1] = 99는 정상적으로 실행되는 대입문이므로 실행되지 않는다는 설명은 근거가 없어 틀렸습니다.', '참조 전달은 오히려 값-결과 전달보다 저장 공간을 더 강하게 공유하는(진입·종료 복사조차 없는) 방식이므로 더 독립적이라는 설명은 실제와 반대라 틀렸습니다.']}
/>

## 문제 10

<Quiz
  questionNumber={10}
  question="이름에 의한 호출(call by name)에 대한 설명으로 가장 적절한 것은?"
  options={['형식 매개변수를 실제 인자로 그대로 치환한 식(텍스트) 자체를 넘겨, 하위프로그램 안에서 그 매개변수가 쓰일 때마다 그 식을 매번 다시 계산하는 방식이다.', '이름에 의한 호출은 값 전달과 완전히 동일한 시점에 딱 한 번만 계산해 넘기는 방식이다.', '이름에 의한 호출은 반드시 배열에만 적용할 수 있는 전달 방식이다.', '이름에 의한 호출에서는 실제 인자에 부작용이 있는 식이 와도 항상 값 전달과 똑같은 결과가 보장된다.']}
  correctAnswer={1}
  explanation="이름에 의한 호출은 실제 인자를 값으로 미리 계산해 넘기는 것이 아니라, 그 인자 식 자체를 형식 매개변수 자리에 텍스트로 대입(치환)하는 것처럼 동작합니다. 그 결과 하위프로그램 안에서 그 매개변수가 참조될 때마다 원래의 식이 매번 다시 평가됩니다. 이 때문에 인자 식에 부작용이 있거나 반복 평가 비용이 크면 값 전달과 다른 결과나 성능 특성이 나타날 수 있습니다(이른바 Jensen's device로 알려진 사례)."
  optionExplanations={['텍스트 치환과 매번 재평가라는 이름에 의한 호출의 핵심 동작을 정확히 설명했으므로 정답입니다.', '값 전달은 호출 시점에 한 번만 계산해 복사하지만 이름에 의한 호출은 매번 다시 계산하므로 동일하다는 설명은 틀렸습니다.', '이름에 의한 호출은 배열뿐 아니라 일반 식·변수에도 적용될 수 있는 전달 방식이므로 틀렸습니다.', '인자 식이 매번 재평가되므로 그 식에 부작용이 있거나 값이 바뀌는 변수가 섞여 있으면 값 전달과 다른 결과가 나올 수 있어, 항상 결과가 보장된다는 설명은 틀렸습니다.']}
/>

## 문제 11

<Quiz
  questionNumber={11}
  question="하위프로그램이 호출될 때 실행 스택에 새로 쌓이는 활성화 레코드(activation record)에 일반적으로 포함되는 항목으로 가장 거리가 먼 것은?"
  options={['형식 매개변수 값(또는 참조), 지역 변수, 반환 주소, 동적 링크', '프로그램 전체에서 딱 하나만 존재하는 전역 심볼 테이블 전체의 복사본', '임시값을 담아 두는 임시 저장 공간', '정적 체인을 구성하기 위한 정적 링크(access link)']}
  correctAnswer={2}
  explanation="활성화 레코드는 형식 매개변수, 지역 변수, 반환 주소, 이전 활성화 레코드를 가리키는 동적 링크(제어 링크), 필요하다면 정적 링크(접근 링크), 계산 중간값을 담는 임시 공간 등으로 구성됩니다. 전역 심볼 테이블 전체를 매 호출마다 복사해 담는 것은 활성화 레코드의 목적(그 호출에 국한된 정보 저장)과 맞지 않으며 일반적으로 포함되지 않습니다."
  optionExplanations={['이 항목들은 활성화 레코드를 구성하는 일반적이고 표준적인 요소이므로 거리가 먼 항목이 아닙니다.', '전역 심볼 테이블 전체를 호출마다 복사해 넣는 것은 활성화 레코드의 목적과 맞지 않는 비현실적인 구성이므로 이것이 거리가 먼 항목이며 정답입니다.', '계산 중간값을 위한 임시 공간은 활성화 레코드의 일반적 구성 요소이므로 거리가 먼 항목이 아닙니다.', '중첩 하위프로그램에서 비지역 변수 접근을 위한 정적 링크도 활성화 레코드의 일반적 구성 요소이므로 거리가 먼 항목이 아닙니다.']}
/>

## 문제 12

<Quiz
  questionNumber={12}
  question="활성화 레코드의 동적 링크(dynamic link, control link)가 하는 역할로 가장 적절한 것은?"
  options={['현재 하위프로그램을 호출한 쪽의 활성화 레코드를 가리켜, 현재 하위프로그램이 반환될 때 실행을 어디로 되돌려야 할지(호출자의 활성화 레코드)를 알 수 있게 해 준다.', '동적 링크는 프로그램에서 가장 처음 실행되는 main 하위프로그램의 활성화 레코드만 가리킨다.', '동적 링크는 변수의 자료형 정보를 저장하는 용도로만 쓰인다.', '동적 링크는 정적 스코프 언어에서는 아예 존재하지 않는 요소이다.']}
  correctAnswer={1}
  explanation="동적 링크는 실행 스택에서 현재 활성화 레코드 바로 아래에 있는, 즉 현재 하위프로그램을 호출한 쪽의 활성화 레코드를 가리키는 포인터입니다. 이 링크 덕분에 현재 하위프로그램이 끝나 스택에서 제거될 때, 실행 제어를 어느 활성화 레코드(호출자)로 되돌려야 하는지 추적할 수 있습니다."
  optionExplanations={['호출자의 활성화 레코드를 가리켜 반환 시 제어 흐름을 되돌리는 역할을 정확히 설명했으므로 정답입니다.', '동적 링크는 모든 호출마다 그 호출자를 가리키도록 매번 새로 설정되는 것이지 항상 main만 가리키지 않으므로 틀렸습니다.', '동적 링크는 호출 관계(제어 흐름)를 나타내는 포인터이지 자료형 정보를 담는 용도가 아니므로 틀렸습니다.', '동적 링크는 정적 스코프·동적 스코프 여부와 무관하게 어떤 하위프로그램 호출 구조에서도 반환 지점을 추적하기 위해 필요한 요소이므로 틀렸습니다.']}
/>

## 문제 13

정적 스코프 언어에서 다음과 같이 하위프로그램이 중첩되어 정의되어 있다고 하자.

```text
procedure Outer는:
    지역 변수 x = 5

    procedure Inner는:
        x를 출력한다   // Outer의 지역 변수 x를 참조

    call Inner()

call Outer()
```

<Quiz
  questionNumber={13}
  question="Inner의 활성화 레코드가 자신을 둘러싼 Outer의 지역 변수 x를 찾아갈 때 사용하는 것은?"
  options={['정적 링크(static link, access link)를 따라, 코드상 자신을 둘러싼 범위의 활성화 레코드로 이동해 x를 찾는다.', '동적 링크(dynamic link)를 따라, 자신을 호출한 쪽의 활성화 레코드로 이동해 x를 찾는다.', '반환 주소를 따라가서 x를 찾는다.', 'x는 어떤 링크로도 찾을 수 없으므로 반드시 컴파일 오류가 발생한다.']}
  correctAnswer={1}
  explanation="정적 스코프에서 비지역 변수 접근은 코드상의 중첩 구조를 따라야 하므로, 이를 실행 스택에서 구현하기 위해 정적 링크(access link, 정적 체인)를 사용합니다. Inner의 활성화 레코드에 있는 정적 링크는 코드상 자신을 둘러싼 Outer의 활성화 레코드를 가리키도록 설정되어, 이를 따라가면 Outer의 지역 변수 x를 찾을 수 있습니다."
  optionExplanations={['정적 스코프의 비지역 변수 접근이 정적 링크(정적 체인)를 통해 구현된다는 것을 정확히 설명했으므로 정답입니다.', '동적 링크는 호출자를 가리켜 반환 지점을 찾는 데 쓰이는 것이지, 정적 스코프의 비지역 변수 탐색에는 쓰이지 않으므로 틀렸습니다.', '반환 주소는 하위프로그램이 끝난 뒤 실행을 재개할 코드 위치를 가리키는 것이지 변수 탐색과는 무관하므로 틀렸습니다.', 'Inner가 Outer 내부에 정의되어 있으므로 정적 스코프 규칙상 x는 정상적으로 찾을 수 있는 변수이므로 틀렸습니다.']}
/>

## 문제 14

<Quiz
  questionNumber={14}
  question="정적 체인(static chain)과 동적 체인(dynamic chain)의 차이를 설명한 것으로 가장 적절한 것은?"
  options={['정적 체인은 정적 링크들을 따라 이어지며 코드상 둘러싼 범위 구조를 반영하는 반면, 동적 체인은 동적 링크들을 따라 이어지며 실제 호출 순서(누가 누구를 호출했는가)를 반영한다.', '정적 체인과 동적 체인은 항상 정확히 같은 경로를 가리키는 동일한 구조이다.', '정적 체인은 재귀 호출에서는 절대로 만들어질 수 없다.', '동적 체인은 컴파일 시점에 이미 완전히 고정되어 실행 중에는 전혀 바뀌지 않는다.']}
  correctAnswer={1}
  explanation="정적 체인은 정적 링크를 연결한 것으로 코드상 렉시컬 중첩 구조(누가 누구를 감싸고 있는가)를 따라가며, 동적 체인은 동적 링크를 연결한 것으로 실제 실행 중 호출 순서(누가 누구를 불렀는가)를 따라갑니다. 이 두 체인은 재귀나 서로 다른 위치에서의 호출이 섞이면 서로 다른 경로를 가리키게 되는 경우가 흔합니다."
  optionExplanations={['코드 구조 기반(정적 체인)과 호출 순서 기반(동적 체인)이라는 근본적 차이를 정확히 설명했으므로 정답입니다.', '재귀나 교차 호출이 있으면 두 체인이 서로 다른 경로를 가리키는 경우가 흔하므로 항상 같다는 설명은 틀렸습니다.', '재귀 호출에서도 각 활성화마다 자신을 둘러싼 범위를 가리키는 정적 링크가 정상적으로 만들어지므로 틀렸습니다.', '동적 체인은 실행 중 호출이 일어날 때마다 그 시점의 호출자를 가리키도록 새로 설정되므로 실행 중 계속 바뀌는 구조이며 틀렸습니다.']}
/>

## 문제 15

다음과 같이 재귀적으로 정의된 팩토리얼 하위프로그램을 3을 인자로 호출한다.

```text
function factorial(n):
    만약 n <= 1이면 1을 반환한다
    아니면 n * factorial(n - 1)을 반환한다

factorial(3)을 호출한다
```

<Quiz
  questionNumber={15}
  question="factorial(3) 호출 과정에서 실행 스택에 동시에 쌓이는 활성화 레코드의 최대 개수는? (factorial(3) 자신의 최초 호출을 포함해서 센다)"
  options={['4개 (factorial(3), factorial(2), factorial(1), 그리고 이를 호출한 최초 호출 지점 포함)', '1개', '3개뿐, factorial(1)의 활성화 레코드는 생기지 않는다', '무한히 계속 쌓여 스택이 끝없이 늘어난다']}
  correctAnswer={1}
  explanation="factorial(3)은 factorial(2)를, factorial(2)는 factorial(1)을 호출하며 각 호출마다 새로운 활성화 레코드가 스택에 쌓입니다. factorial(1)은 n이 1 이하이므로 더 이상 재귀 호출을 하지 않고 값을 반환하기 시작합니다. 이 시점에 최초 호출 지점(呼출자)의 활성화 레코드까지 포함하면 최초 호출자, factorial(3), factorial(2), factorial(1)의 활성화 레코드가 동시에 스택에 존재해 총 4개가 됩니다."
  optionExplanations={['재귀가 가장 깊이 들어간 시점에 호출자까지 포함한 활성화 레코드 개수를 정확히 센 것이므로 정답입니다.', '재귀 호출마다 각각 새로운 활성화 레코드가 필요하므로 1개만 존재한다는 것은 재귀 구현의 기본 원리와 맞지 않아 틀렸습니다.', 'factorial(1) 호출도 정상적인 하위프로그램 호출이므로 활성화 레코드가 생성되며, 다만 그 안에서 더 이상의 재귀 호출만 하지 않을 뿐이므로 틀렸습니다.', '기저 조건(n이 1 이하)이 있어 재귀가 유한한 횟수에서 멈추도록 설계되어 있으므로 무한히 쌓인다는 설명은 틀렸습니다.']}
/>

## 문제 16

<Quiz
  questionNumber={16}
  question="재귀 함수가 기저 조건(base case) 없이 잘못 작성되어 계속 자기 자신을 호출할 때 일반적으로 발생하는 문제로 가장 적절한 것은?"
  options={['활성화 레코드가 실행 스택에 계속 쌓이기만 하고 반환되지 않아, 결국 스택에 할당된 메모리 공간을 모두 소진해 스택 오버플로(stack overflow) 오류로 이어질 수 있다.', '활성화 레코드가 아예 생성되지 않아 프로그램이 아무 동작도 하지 않는다.', '재귀 호출은 활성화 레코드를 전혀 사용하지 않으므로 이런 상황에서는 아무 문제도 생기지 않는다.', '기저 조건이 없어도 컴파일러가 자동으로 무한 재귀를 감지해 항상 안전하게 실행을 멈춰 준다.']}
  correctAnswer={1}
  explanation="재귀 호출마다 새로운 활성화 레코드가 스택에 추가되는데, 멈추는 기저 조건이 없으면 이 호출이 끝없이 이어져 활성화 레코드가 계속 쌓이기만 합니다. 스택에 할당된 메모리는 유한하므로 결국 이 공간을 모두 소진해 스택 오버플로 오류가 발생하는 것이 전형적인 결과입니다."
  optionExplanations={['활성화 레코드가 무한히 쌓여 유한한 스택 공간을 소진한다는 실제 결과를 정확히 설명했으므로 정답입니다.', '재귀 호출도 일반 하위프로그램 호출과 마찬가지로 매번 활성화 레코드를 생성하므로 아예 생성되지 않는다는 설명은 틀렸습니다.', '재귀 호출도 다른 하위프로그램 호출과 동일하게 활성화 레코드 메커니즘을 사용하므로 사용하지 않는다는 설명은 틀렸습니다.', '대부분의 언어와 컴파일러는 무한 재귀 여부를 실행 전에 일반적으로 판정할 수 없어(정지 문제와 관련) 자동으로 항상 안전하게 막아 주지 못하므로 틀렸습니다.']}
/>

## 문제 17

<Quiz
  questionNumber={17}
  question="하위프로그램이 정상적으로 반환될 때 실행 스택에서 일어나는 일로 가장 적절한 것은?"
  options={['해당 하위프로그램의 활성화 레코드가 스택에서 제거(pop)되고, 그 레코드에 저장되어 있던 반환 주소를 이용해 실행 제어가 호출자의 다음 문장으로 되돌아간다.', '해당 하위프로그램의 활성화 레코드가 스택에서 제거되지 않고 프로그램이 끝날 때까지 계속 남아 있는다.', '반환이 일어나면 호출자의 활성화 레코드까지 함께 제거되어 호출자도 곧바로 종료된다.', '반환 값은 항상 전역 변수를 통해서만 전달될 수 있고 활성화 레코드와는 무관하다.']}
  correctAnswer={1}
  explanation="하위프로그램이 반환되면 그 하위프로그램의 활성화 레코드는 자신의 역할을 마치고 스택에서 제거(pop)됩니다. 이때 레코드에 저장해 두었던 반환 주소를 이용해, 실행 제어가 원래 이 하위프로그램을 호출했던 위치(호출자)의 다음 문장으로 돌아갑니다."
  optionExplanations={['활성화 레코드 제거와 반환 주소를 이용한 제어 흐름 복귀라는 표준적인 반환 절차를 정확히 설명했으므로 정답입니다.', '스택 동적 변수의 특성상 하위프로그램이 반환되면 그 활성화 레코드는 더 이상 필요 없어 제거되는 것이 일반적이므로 틀렸습니다.', '호출자의 활성화 레코드는 여전히 스택에 남아 자신의 실행을 이어가야 하므로, 하위프로그램 하나의 반환으로 호출자까지 사라진다는 설명은 틀렸습니다.', '반환 값은 활성화 레코드 안의 지정된 위치나 레지스터 등을 통해 전달되는 구현도 흔하므로 전역 변수를 통해서만 가능하다는 설명은 틀렸습니다.']}
/>

## 문제 18

<Quiz
  questionNumber={18}
  question="컴파일러(compiler)와 인터프리터(interpreter) 구현 방식의 차이를 설명한 것으로 가장 적절한 것은?"
  options={['컴파일러는 소스 코드 전체를 실행 전에 목적 코드(기계어 등)로 변환해 두고 나중에 그 결과물을 실행하는 반면, 인터프리터는 소스 코드(또는 중간 표현)를 한 문장씩 읽어 그때그때 바로 해석하며 실행한다.', '컴파일러는 오직 기계어만 만들어 낼 수 있고 인터프리터는 오직 사람이 읽는 문서만 만들어 낼 수 있다.', '컴파일러 방식의 언어는 오류를 절대 낼 수 없고, 인터프리터 방식의 언어는 항상 오류를 낸다.', '컴파일러와 인터프리터는 이름만 다를 뿐 완전히 동일한 실행 절차를 가리킨다.']}
  correctAnswer={1}
  explanation="컴파일러는 실행 전에 소스 코드 전체를 분석해 목적 코드를 미리 만들어 두고, 실행 시점에는 그 목적 코드를 그대로 실행합니다. 인터프리터는 별도의 목적 코드 생성 없이 소스나 중간 표현을 한 문장(또는 한 단위)씩 그때그때 해석하면서 즉시 실행합니다. 이 차이는 오류 검출 시점, 실행 속도, 이식성 등 여러 특성에 영향을 줍니다."
  optionExplanations={['사전 변환 후 실행(컴파일러)과 즉시 해석 실행(인터프리터)이라는 핵심 차이를 정확히 설명했으므로 정답입니다.', '컴파일러도 다른 중간 표현(바이트코드 등)을 만들어 낼 수 있고, 인터프리터도 기계어 수준까지 즉시 번역해 실행하는 구현이 있으므로 이렇게 단정하는 것은 틀렸습니다.', '컴파일러 방식의 언어도 논리적 오류나 실행 중 오류가 발생할 수 있고, 인터프리터 방식의 언어도 오류 없이 정상 실행될 수 있으므로 틀렸습니다.', '두 방식은 코드를 처리하는 시점과 절차가 근본적으로 다르므로 동일한 절차라는 설명은 틀렸습니다.']}
/>

## 문제 19

<Quiz
  questionNumber={19}
  question="자바(Java)처럼 소스 코드를 바이트코드로 컴파일한 뒤, 이를 가상 머신이 해석 또는 즉시 컴파일(JIT)하여 실행하는 하이브리드 구현 방식의 특징으로 가장 적절한 것은?"
  options={['컴파일 단계에서 특정 하드웨어에 종속되지 않는 중간 표현(바이트코드)을 만들어 이식성을 확보하고, 실행 시점에는 가상 머신이 이를 해석하거나 필요한 부분을 다시 기계어로 즉시 컴파일해 성능을 보완한다.', '하이브리드 구현은 소스 코드를 아예 처리하지 않고 무작위로 실행 결과를 만들어 낸다.', '하이브리드 구현 방식은 오직 정적 타입 언어에서만 존재할 수 있다.', '하이브리드 구현에서는 가상 머신이라는 개념 자체가 필요하지 않다.']}
  correctAnswer={1}
  explanation="자바와 같은 언어는 소스를 특정 하드웨어에 의존하지 않는 바이트코드로 먼저 컴파일해 이식성을 확보하고, 실행 시점에는 가상 머신(JVM 등)이 이 바이트코드를 해석하거나, 자주 실행되는 부분은 즉시 컴파일(JIT, Just-In-Time compilation)로 기계어로 바꾸어 실행 속도를 높입니다. 이는 순수 컴파일 방식과 순수 인터프리터 방식의 장점을 함께 취하려는 하이브리드 구현입니다."
  optionExplanations={['중간 표현을 통한 이식성 확보와 가상 머신의 해석·JIT 컴파일 조합이라는 하이브리드 구현의 핵심을 정확히 설명했으므로 정답입니다.', '하이브리드 구현도 소스 코드를 정해진 규칙에 따라 바이트코드로 변환하는 결정적인 절차를 따르므로 무작위 실행이라는 설명은 틀렸습니다.', '동적 타입 언어 중에도 바이트코드로 컴파일한 뒤 가상 머신에서 실행하는 하이브리드 구현 사례가 있으므로 정적 타입 언어에만 국한된다는 설명은 틀렸습니다.', '하이브리드 구현은 바이트코드를 해석·실행할 가상 머신이 핵심 구성 요소이므로 가상 머신이 필요 없다는 설명은 틀렸습니다.']}
/>

## 문제 20

<Quiz
  questionNumber={20}
  question="활성화 레코드의 정적 링크(access link)가 만드는 정적 체인과, 실행 파일을 만들 때 라이브러리 코드를 연결하는 정적 링크(static linking)라는 용어를 비교한 설명으로 가장 적절한 것은?"
  options={['두 용어는 모두 링크(연결)라는 단어를 쓰지만, 전자는 실행 스택에서 비지역 변수 접근 경로를 만드는 개념이고 후자는 실행 파일을 만들 때 라이브러리 코드를 어느 시점에 결합할지에 관한 개념으로 서로 다른 층위의 개념이다.', '두 용어는 완전히 동일한 개념을 가리키는 하나의 용어이다.', '활성화 레코드의 정적 링크는 실행 파일의 라이브러리 연결 방식까지 함께 결정한다.', '정적 링크(access link)라는 개념은 정적 링킹(static linking)이 존재하는 언어에서만 만들어질 수 있다.']}
  correctAnswer={1}
  explanation="이름이 비슷해 혼동하기 쉽지만, 활성화 레코드의 정적 링크(access link)는 실행 중 스코프 체인을 따라 비지역 변수를 찾기 위한 하위프로그램 구현 층위의 개념이고, 정적 링킹(static linking)은 프로그램을 빌드할 때 라이브러리 코드를 실행 파일 안에 미리 결합해 넣을지(정적) 아니면 실행 중에 연결할지(동적 링킹)를 정하는 빌드·배포 층위의 개념입니다. 서로 관련은 없는 별개의 개념입니다."
  optionExplanations={['두 개념이 이름만 비슷할 뿐 서로 다른 층위(실행 중 스코프 접근 vs 빌드 시 라이브러리 결합)의 개념이라는 것을 정확히 설명했으므로 정답입니다.', '두 개념은 적용되는 영역과 목적이 전혀 다르므로 동일한 개념이라는 설명은 틀렸습니다.', '활성화 레코드의 정적 링크는 런타임 스택 구조에 관한 것으로 빌드 시 라이브러리 결합 방식과는 직접적인 관련이 없으므로 틀렸습니다.', '정적 스코프를 지원하는 언어라면 정적 링킹 지원 여부와 무관하게 access link 개념이 구현에 사용될 수 있으므로 틀렸습니다.']}
/>

## 문제 21

<Quiz
  questionNumber={21}
  question="언어 설계와 구현 방식의 관계에 대한 설명으로 가장 적절한 것은?"
  options={['일반적으로 매우 동적인 형 시스템이나 유연한 실행 중 코드 변경을 지원하는 언어는 실행 전 모든 것을 고정해야 하는 순수 컴파일 방식보다 인터프리터나 하이브리드 구현으로 실행되는 경향이 있다.', '언어의 설계(정적/동적 타입 등)와 그 언어의 구현 방식은 서로 어떠한 관련도 없이 완전히 독립적으로 결정된다.', '모든 프로그래밍언어는 반드시 순수 컴파일 방식으로만 구현할 수 있다.', '동적 타입 언어는 절대로 컴파일이라는 단계를 거칠 수 없다.']}
  correctAnswer={1}
  explanation="형이 실행 중에 결정되거나 코드 구조가 실행 중 바뀔 수 있는 매우 동적인 언어는, 실행 전에 모든 것을 고정해 최적화하는 순수 컴파일 방식보다는 실행 중 유연하게 대응할 수 있는 인터프리터나 하이브리드(바이트코드+가상머신) 방식으로 구현되는 경향이 있습니다. 이는 언어 설계상의 특성이 구현 방식 선택에 실제로 영향을 준다는 것을 보여 줍니다."
  optionExplanations={['동적인 언어 특성과 인터프리터·하이브리드 구현 경향 사이의 실제 관계를 정확히 설명했으므로 정답입니다.', '문제의 정답 설명처럼 언어의 동적 특성이 구현 방식 선택에 실제로 영향을 주므로 완전히 독립적이라는 설명은 틀렸습니다.', '동적 타입 언어나 스크립트 언어처럼 인터프리터나 하이브리드 방식으로 구현되는 언어도 많으므로 반드시 순수 컴파일 방식만 가능하다는 설명은 틀렸습니다.', '동적 타입 언어도 바이트코드 등으로 컴파일하는 하이브리드 구현이 흔하므로 컴파일 단계를 절대 거칠 수 없다는 설명은 틀렸습니다.']}
/>

## 문제 22

<Quiz
  questionNumber={22}
  question="런타임 라이브러리(runtime library)의 일반적인 역할로 가장 적절한 것은?"
  options={['프로그램 실행 중 필요한 메모리 관리(할당·해제), 입출력 처리, 일부 언어 기능의 실제 동작 등을 제공해, 실행 파일이 매번 이런 기능을 처음부터 직접 구현하지 않아도 되게 해 준다.', '런타임 라이브러리는 소스 코드를 사람이 읽기 쉬운 형태로 예쁘게 정렬해 주는 도구일 뿐이다.', '런타임 라이브러리는 컴파일이 끝난 뒤에는 프로그램 실행과 전혀 관계가 없어진다.', '런타임 라이브러리는 오직 그래픽 화면 출력에만 관련된 기능을 제공한다.']}
  correctAnswer={1}
  explanation="런타임 라이브러리는 프로그램이 실제로 실행되는 동안 필요한 공통 기능들(메모리 할당·해제, 입출력, 예외 처리 지원, 가비지 컬렉션 등 언어 런타임 기능)을 제공하는 코드 모음입니다. 실행 파일은 이 라이브러리를 링크하거나 호출해 이런 기능을 재사용함으로써 직접 모든 것을 구현할 필요가 없습니다."
  optionExplanations={['실행 중 필요한 메모리 관리·입출력 등 공통 기능을 제공한다는 런타임 라이브러리의 실제 역할을 정확히 설명했으므로 정답입니다.', '소스 코드 정렬은 편집기나 포매터의 역할이지 런타임 라이브러리의 역할이 아니므로 틀렸습니다.', '런타임 라이브러리는 오히려 프로그램이 실행되는 동안(런타임에) 계속 호출되며 관여하므로 컴파일 이후 관계가 없어진다는 설명은 틀렸습니다.', '런타임 라이브러리는 메모리 관리·입출력 등 폭넓은 기능을 포함하므로 그래픽 출력에만 국한된다는 설명은 틀렸습니다.']}
/>

## 문제 23

다음 하위프로그램은 참조 전달로 배열 원소 하나를 넘겨받아 값을 두 배로 만든다.

```text
procedure doubleIt(a):   // 참조 전달
    a = a * 2

전역 배열 nums = [3, 7]
call doubleIt(nums[2])
nums를 출력한다
```

<Quiz
  questionNumber={23}
  question="doubleIt이 참조 전달로 nums[2]를 넘겨받아 실행된 뒤, 전역 배열 nums의 상태는? (배열 인덱스는 1부터 시작한다)"
  options={['[3, 14]', '[6, 7]', '[3, 7]', '[6, 14]']}
  correctAnswer={1}
  explanation="참조 전달에서는 형식 매개변수 a가 nums[2](값 7)가 저장된 공간 자체를 가리키는 별명처럼 동작합니다. doubleIt 안에서 a = a * 2를 실행하면 그 저장 공간의 값이 7에서 14로 바뀌고, 이는 곧 nums[2]가 14로 바뀌는 것과 같습니다. nums[1]은 이 호출과 무관하므로 3 그대로 남습니다."
  optionExplanations={['참조 전달로 인해 nums[2]만 두 배(7에서 14)로 바뀌고 nums[1]은 그대로라는 결과를 정확히 계산했으므로 정답입니다.', '이 값은 nums[1]이 바뀌었다고 잘못 계산한 것으로, 실제로는 인자로 넘긴 nums[2]가 바뀌어야 하므로 틀렸습니다.', '이 값은 값 전달이었다면 나왔을 결과(호출자의 배열이 전혀 바뀌지 않음)이므로, 참조 전달을 가정한 이 문제의 답으로는 틀렸습니다.', '두 원소가 모두 바뀐다고 계산한 것으로, doubleIt은 넘겨받은 하나의 원소에만 접근하므로 틀렸습니다.']}
/>

## 문제 24

<Quiz
  questionNumber={24}
  question="제어 구조·매개변수 전달·활성화 레코드를 종합했을 때 옳지 않은 설명은?"
  options={['switch 문에서 break를 생략하면 fall-through로 인해 여러 case의 코드가 연쇄적으로 실행될 수 있다.', '값 전달에서는 하위프로그램 내부의 형식 매개변수 변경이 호출자의 실제 인자에 반영되지 않는다.', '중첩된 하위프로그램에서 비지역 변수를 찾을 때 정적 스코프 언어는 정적 링크(정적 체인)를, 활성화 레코드의 반환 지점 추적은 동적 링크(동적 체인)를 사용한다.', '재귀 호출은 매번 같은 활성화 레코드 하나만 재사용하므로 호출 횟수가 아무리 많아져도 스택 사용량은 전혀 늘어나지 않는다.']}
  correctAnswer={4}
  explanation="재귀 호출은 매번 새로운 활성화 레코드를 스택에 추가로 쌓습니다(문제 15 참고). 같은 활성화 레코드를 재사용하는 것이 아니라 호출 깊이만큼 서로 다른 활성화 레코드가 동시에 스택에 존재하므로, 재귀가 깊어질수록 스택 사용량은 늘어나며 이것이 스택 오버플로의 원인이 됩니다."
  optionExplanations={['fall-through의 동작 방식은 문제 3~4에서 확인한 옳은 설명입니다.', '값 전달의 성질은 문제 5·7에서 확인한 옳은 설명입니다.', '비지역 변수 탐색에는 정적 링크(정적 체인), 반환 지점 추적에는 동적 링크(동적 체인)를 사용한다는 것은 문제 12~14에서 확인한 옳은 설명입니다.', '재귀 호출마다 새로운 활성화 레코드가 추가로 쌓여 스택 사용량이 늘어나는 것이 사실이므로, 스택 사용량이 전혀 늘어나지 않는다는 이 설명이 옳지 않아 정답입니다.']}
/>

## 참고 자료

- 국가평생교육진흥원 독학학위제 — https://bdes.nile.or.kr
- cppreference.com, Function declaration and parameter passing — https://en.cppreference.com/w/cpp/language/function
