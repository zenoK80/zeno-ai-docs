---
title: '24. 통합 모의고사 2회분 및 출제 포인트 정리'
description: '두 번째 통합 모의고사 22문항과 파이프라인·캐시·스케줄링·교착상태·페이징·RAID·통합형 문항의 반복 출제 포인트, 시험 직전 체크리스트를 정리한다.'
---

<Callout type="warning">
  이 모의고사는 국가평생교육진흥원 출제기준을 근거로 **새로 구성한 연습문제**이며 실제 기출문제를 그대로 옮긴 것이 아니다. 실제 시험의 문항 수·시험 시간·배점은 회차 공고마다 다를 수 있으므로 반드시 최신 응시요강을 확인해야 한다(공고 확인 필요).
</Callout>

## 모의고사 2회분

<Quiz
  questionNumber={1}
  question="어떤 프로그램의 명령어 구성이 적재/저장(load/store) CPI 4인 명령어 25퍼센트, ALU 연산 CPI 1인 명령어 50퍼센트, 분기(branch) CPI 2인 명령어 25퍼센트일 때 평균 CPI는?"
  options={['1.50', '1.75', '2.00', '2.50']}
  correctAnswer={3}
  explanation="평균 CPI는 각 명령어 비율과 CPI를 곱해 더한 값이다. 0.25 곱하기 4는 1.0, 0.5 곱하기 1은 0.5, 0.25 곱하기 2는 0.5이며 모두 더하면 1.0 더하기 0.5 더하기 0.5는 2.0이다."
  optionExplanations={['일부 항을 빠뜨렸을 때 나올 수 있는 오답이다', '가중치 곱셈 중 일부를 잘못 계산했을 때 나오는 오답이다', '정답이다. 세 항을 모두 더한 가중평균이다', '가중치를 반영하지 않고 단순히 큰 값 쪽으로 치우친 오답이다']}
/>

<Quiz
  questionNumber={2}
  question="파이프라인에서 데이터 해저드를 처리하는 기법인 포워딩(forwarding)에 대한 설명으로 옳은 것은?"
  options={['포워딩은 분기 명령어의 결과를 미리 예측하는 기법이다', '포워딩은 아직 레지스터에 쓰이지 않은 연산 결과를 다음 단계로 직접 전달해 정지(stall) 없이 사용할 수 있게 한다', '포워딩을 쓰면 데이터 해저드가 아니라 구조적 해저드가 완전히 사라진다', '포워딩은 항상 소프트웨어(컴파일러)만으로 구현되는 기법이다']}
  correctAnswer={2}
  explanation="포워딩(또는 바이패싱)은 어떤 명령어의 연산 결과가 레지스터 파일에 실제로 기록되기 전이라도, 그 결과를 필요로 하는 다음 명령어의 실행 단계로 직접 전달해 파이프라인을 멈추지 않고 데이터 해저드를 해결하는 하드웨어 기법이다."
  optionExplanations={['분기 결과를 미리 예측하는 것은 분기 예측이며 포워딩과는 다른 기법이다', '정답이다. 결과를 직접 전달해 정지 없이 데이터 해저드를 해결하는 기법이다', '포워딩은 데이터 해저드를 줄이는 기법이며 구조적 해저드와는 별개의 문제다', '포워딩은 보통 하드웨어 회로(포워딩 유닛)로 구현되며 소프트웨어만으로 구현되는 기법이 아니다']}
/>

<Quiz
  questionNumber={3}
  question="32비트 주소 체계에서 캐시 크기 256킬로바이트, 블록 크기 128바이트인 직접 매핑 캐시의 태그 비트 수는?"
  options={['12비트', '13비트', '14비트', '15비트']}
  correctAnswer={3}
  explanation="블록 수는 256킬로바이트를 128바이트로 나눈 2048개다. 오프셋 비트는 128바이트를 표현하는 7비트, 인덱스 비트는 2048개를 표현하는 11비트다. 태그 비트는 32에서 11과 7을 뺀 14비트다."
  optionExplanations={['비트 계산 중 하나를 빠뜨렸을 때 나오는 오답이다', '인덱스나 오프셋 계산에서 1비트 오차가 생겼을 때 나오는 오답이다', '정답이다. 32에서 인덱스 11비트, 오프셋 7비트를 뺀 14비트다', '태그 비트를 과다하게 계산한 오답이다']}
/>

<Quiz
  questionNumber={4}
  question="어떤 캐시에 총 1,000번의 메모리 접근이 있었고 그중 150번이 미스(miss)였다면 이 캐시의 적중률(hit rate)은?"
  options={['70퍼센트', '80퍼센트', '85퍼센트', '90퍼센트']}
  correctAnswer={3}
  explanation="적중 횟수는 전체 접근에서 미스를 뺀 1,000 빼기 150은 850회다. 적중률은 850을 1,000으로 나눈 0.85, 즉 85퍼센트다."
  optionExplanations={['미스율과 적중률을 혼동했을 때 나올 수 있는 오답이다', '계산 과정에서 오차가 생겼을 때 나오는 근사 오답이다', '정답이다. 850 나누기 1,000은 85퍼센트다', '미스율(15퍼센트)을 반대로 적중률에 가깝게 착각한 오답이다']}
/>

<Quiz
  questionNumber={5}
  question="현재 헤드 위치 100에서 요청 큐 55, 180, 30, 120을 SSTF(Shortest Seek Time First)로 처리할 때 총 헤드 이동 거리는?"
  options={['180', '205', '230', '255']}
  correctAnswer={3}
  explanation="100에서 가장 가까운 120으로 이동(20). 120에서 남은 55, 180, 30 중 가장 가까운 180으로 이동(60). 180에서 남은 55, 30 중 가까운 55로 이동(125). 55에서 30으로 이동(25). 모두 더하면 20 더하기 60 더하기 125 더하기 25는 230이다."
  optionExplanations={['일부 구간을 빠뜨렸을 때 나올 수 있는 오답이다', '중간 계산에서 오차가 생겼을 때 나오는 오답이다', '정답이다. 네 구간 이동 거리의 합이다', '요청 순서를 그대로(FCFS로) 처리했을 때 나올 수 있는 더 큰 값이다']}
/>

<Quiz
  questionNumber={6}
  question="1테라바이트 디스크 4개로 RAID 1(미러링)을 두 쌍으로 구성했을 때 사용 가능한 저장 용량과 장애 허용 특성으로 옳은 것은?"
  options={['4테라바이트 사용 가능, 장애 허용 불가', '2테라바이트 사용 가능, 각 쌍에서 디스크 1개까지 장애를 허용한다', '3테라바이트 사용 가능, 디스크 2개까지 항상 장애를 허용한다', '2테라바이트 사용 가능, 어떤 디스크가 몇 개 고장 나도 항상 복구된다']}
  correctAnswer={2}
  explanation="RAID 1은 디스크를 쌍으로 묶어 서로 완전히 같은 데이터를 저장(미러링)하므로 전체 용량의 절반인 2테라바이트만 사용할 수 있다. 각 쌍 안에서는 디스크 1개가 고장 나도 나머지 하나로 데이터를 유지할 수 있지만, 같은 쌍의 두 디스크가 동시에 고장 나면 그 쌍의 데이터는 복구할 수 없다."
  optionExplanations={['RAID 0(스트라이핑)과 혼동한 오답이며 RAID 1은 미러링으로 용량의 절반만 사용한다', '정답이다. 절반 용량을 사용하고 각 쌍에서 디스크 1개까지 장애를 허용한다', '2쌍 구성에서 항상 디스크 2개 장애를 허용한다고 단정할 수 없다. 같은 쌍의 두 디스크가 함께 고장 나면 복구되지 않는다', '같은 쌍의 두 디스크가 동시에 고장 나면 그 데이터는 복구할 수 없으므로 항상 복구된다는 설명은 옳지 않다']}
/>

<Quiz
  questionNumber={7}
  question="프로세스와 스레드를 비교한 설명으로 옳지 않은 것은?"
  options={['같은 프로세스에 속한 스레드들은 일반적으로 코드 영역과 데이터 영역을 공유한다', '스레드는 프로세스보다 생성과 컨텍스트 스위칭 비용이 일반적으로 더 적게 든다', '프로세스 간 메모리 공간은 기본적으로 서로 격리되어 있어 직접 접근할 수 없다', '한 프로세스 안의 여러 스레드는 각자 완전히 독립된 별도의 주소 공간을 갖는다']}
  correctAnswer={4}
  explanation="같은 프로세스에 속한 스레드들은 일반적으로 같은 주소 공간(코드, 데이터, 힙 등)을 공유하며, 스택과 레지스터 같은 실행 문맥만 각자 독립적으로 가진다. 완전히 독립된 별도의 주소 공간을 갖는다는 설명은 프로세스 간의 관계에 가깝다."
  optionExplanations={['스레드의 정의로 옳은 설명이다', '스레드가 프로세스보다 가벼운 이유로 옳은 설명이다', '프로세스 간 메모리 격리 특성으로 옳은 설명이다', '옳지 않은 설명이다(정답). 같은 프로세스의 스레드들은 주소 공간 대부분을 공유한다']}
/>

<Quiz
  questionNumber={8}
  question="도착시간과 실행시간이 P1(0,3), P2(1,6), P3(2,4), P4(3,5)인 프로세스 집합을 FCFS로 스케줄링할 때 평균 대기시간은?"
  options={['3.75', '4.75', '5.75', '9.25']}
  correctAnswer={2}
  explanation="도착 순서대로 P1은 0부터 3까지, P2는 3부터 9까지, P3은 9부터 13까지, P4는 13부터 18까지 실행된다. 대기시간은 P1=0, P2=2, P3=7, P4=10이며 평균은 (0+2+7+10) 나누기 4는 4.75다."
  optionExplanations={['대기시간 일부를 적게 계산했을 때 나올 수 있는 오답이다', '정답이다. 네 프로세스 대기시간 합 19를 4로 나눈 값이다', '반환시간과 대기시간을 혼동했을 때 나오는 오답이다', '평균 반환시간(9.25)과 평균 대기시간을 혼동한 오답이다']}
/>

<Quiz
  questionNumber={9}
  question="선점형 우선순위 스케줄링에서 P1(도착0, 실행10, 우선순위3), P2(도착1, 실행1, 우선순위1), P3(도착2, 실행2, 우선순위2)이 있을 때(숫자가 작을수록 우선순위가 높음) 평균 대기시간은?"
  options={['0', '1', '2', '3']}
  correctAnswer={2}
  explanation="t=0부터 P1이 실행되다가 t=1에 우선순위가 더 높은 P2가 도착해 선점하고 1부터 2까지 실행되어 완료된다. t=2에 P3이 도착해 P1(우선순위3)보다 높은 우선순위이므로 다시 선점해 2부터 4까지 실행되어 완료된다. 이후 P1이 남은 시간 9를 4부터 13까지 실행해 완료된다. 대기시간은 P1=13-10=3, P2=0, P3=0이며 평균은 3을 3으로 나눈 1이다."
  optionExplanations={['P1이 선점당한 시간만큼 대기시간이 생기므로 0은 옳지 않다', '정답이다. P1만 대기시간 3을 가지며 평균은 1이다', '평균을 실제보다 크게 계산한 오답이다', '평균을 실제보다 더 크게 계산한 오답이다']}
/>

<Quiz
  questionNumber={10}
  question="초기값이 2인 카운팅 세마포어 S에 대해 wait(S), wait(S), wait(S), signal(S) 순서로 호출이 일어났다. 이 시점까지 자원을 확보한 프로세스는 모두 몇 개이며, 대기 큐에 남아 있는 프로세스는 몇 개인가?"
  options={['확보 2개, 대기 1개', '확보 2개, 대기 0개', '확보 3개, 대기 0개', '확보 2개, 대기 2개']}
  correctAnswer={3}
  explanation="초기값 2에서 wait을 세 번 호출하면 값은 2, 1, 0, -1 순서로 내려간다. 앞의 두 번은 자원을 바로 확보하지만 세 번째 wait 호출은 값이 음수가 되어 그 프로세스가 대기 큐에 들어간다. 이후 signal이 호출되면 대기 큐의 그 프로세스가 깨어나 자원을 확보하므로, 최종적으로 자원을 확보한 프로세스는 처음 두 개에 signal로 깨어난 한 개를 더한 3개이고 대기 큐는 0개가 된다."
  optionExplanations={['세 번째 wait으로 대기하던 프로세스가 signal 이후 실제로 자원을 확보했다는 사실을 반영하지 않은 오답이다', 'signal 호출 이전까지의 상태만 반영하고 signal 이후 대기 프로세스가 자원을 확보한 것을 반영하지 않은 오답이다', '정답이다. 세 번의 wait과 한 번의 signal을 거치며 총 세 프로세스가 순차적으로 자원을 확보하고 대기 큐는 비게 된다', '대기 큐에 2개가 남아 있다는 것은 wait 세 번, signal 한 번이라는 호출 횟수와 맞지 않는 오답이다']}
/>

<Quiz
  questionNumber={11}
  question="자원 2종류(A, B)에 대해 Available=(1, 0)이고, P0의 Allocation(1, 0)·Max(4, 2), P1의 Allocation(1, 1)·Max(2, 2)인 상태를 뱅커스 알고리즘으로 판정하면?"
  options={['P0, P1 순서로 안전 순서가 성립한다', 'P1, P0 순서로 안전 순서가 성립한다', '어느 프로세스도 당장 필요량을 만족시킬 수 없어 불안전 상태다', '가용 자원이 0인 자원 종류가 있으므로 이미 교착상태가 발생한 것이 확정된다']}
  correctAnswer={3}
  explanation="각 프로세스의 필요량은 Max에서 Allocation을 뺀 값으로 P0은 (3, 2), P1은 (1, 1)이다. 가용 자원 (1, 0)으로는 P0의 필요량(3, 2)도, P1의 필요량(1, 1)도 만족시킬 수 없다(P1은 B자원이 0이라 1만큼 부족). 따라서 어느 프로세스도 진행할 수 없어 불안전 상태다."
  optionExplanations={['P0의 필요량(3,2)이 가용 자원(1,0)보다 크므로 지금 당장 실행을 시작할 수 없다', 'P1의 필요량(1,1) 중 B자원 1이 가용 자원 B(0)보다 커서 지금 당장 실행할 수 없다', '정답이다. 두 프로세스 모두 당장 필요량을 만족할 수 없는 불안전 상태다', '불안전 상태는 교착상태가 발생할 위험이 있다는 뜻일 뿐, 이미 교착상태가 확정되었다는 뜻은 아니다. 이후 다른 자원이 반환되면 안전해질 수도 있다']}
/>

<Quiz
  questionNumber={12}
  question="모니터(monitor)에 대한 설명으로 옳은 것은?"
  options={['모니터는 프로그래머가 세마포어의 wait과 signal 호출 순서를 직접 관리해야 하는 저수준 기법이다', '모니터는 공유 자원과 그 자원에 접근하는 절차를 하나의 프로그래밍 언어 구성 요소로 묶어 상호 배제를 자동으로 보장한다', '모니터 안에서는 조건 변수(condition variable)를 사용할 수 없다', '모니터는 하드웨어로만 구현할 수 있고 소프트웨어로는 구현할 수 없다']}
  correctAnswer={2}
  explanation="모니터는 공유 데이터와 그 데이터를 다루는 프로시저(절차)를 하나의 모듈로 묶어, 한 번에 하나의 프로세스만 모니터 내부에서 실행되도록 언어 차원에서 상호 배제를 자동으로 보장하는 동기화 도구다. 대기가 필요한 상황은 조건 변수로 처리한다."
  optionExplanations={['세마포어처럼 프로그래머가 직접 wait/signal 순서를 관리해야 하는 것은 모니터의 장점(자동화)과 반대되는 설명이다', '정답이다. 모니터는 상호 배제를 언어 차원에서 자동으로 보장하는 고수준 동기화 도구다', '모니터는 조건 변수를 이용해 특정 조건이 만족될 때까지 대기하는 기능을 제공한다', '모니터는 일반적으로 프로그래밍 언어나 운영체제 수준의 소프트웨어 구성 요소로 구현된다']}
/>

<Quiz
  questionNumber={13}
  question="가상 주소가 32비트이고 페이지 크기가 4킬로바이트일 때, 페이지 오프셋(offset)을 표현하는 데 필요한 비트 수와 페이지 번호를 표현하는 데 필요한 비트 수는?"
  options={['오프셋 10비트, 페이지 번호 22비트', '오프셋 12비트, 페이지 번호 20비트', '오프셋 14비트, 페이지 번호 18비트', '오프셋 16비트, 페이지 번호 16비트']}
  correctAnswer={2}
  explanation="4킬로바이트는 2의 12제곱 바이트이므로 오프셋을 표현하는 데 12비트가 필요하다. 전체 주소 32비트에서 오프셋 12비트를 빼면 페이지 번호는 20비트가 된다."
  optionExplanations={['페이지 크기를 2의 10제곱으로 잘못 계산했을 때 나오는 오답이다', '정답이다. 4킬로바이트는 2의 12제곱이므로 오프셋 12비트, 나머지 20비트가 페이지 번호다', '페이지 크기를 실제보다 크게 잘못 계산했을 때 나오는 오답이다', '오프셋과 페이지 번호 비트 수를 절반씩 임의로 나눈 오답이다']}
/>

<Quiz
  questionNumber={14}
  question="참조열 4, 1, 2, 3, 1, 4, 5, 1, 2, 4에 프레임 3개로 LRU 페이지 교체 알고리즘을 적용했을 때 총 페이지 부재 횟수는?"
  options={['6회', '7회', '8회', '9회']}
  correctAnswer={3}
  explanation="본문 풀이 순서대로 4, 1, 2, 3까지 부재 4회 후 3이 4를 내쫓고, 이어 4가 다시 들어오며 2를 내쫓고(부재), 5가 들어오며 3을 내쫓고(부재), 2가 다시 들어오며 4를 내쫓고(부재), 마지막 4가 5를 내쫓는(부재) 과정을 거쳐 총 8회 부재, 2회(1, 1) 적중이 된다."
  optionExplanations={['부재를 실제보다 적게 센 오답이다', '부재 하나를 놓친 오답이다', '정답이다. 순서대로 추적하면 총 8회 부재다', '적중을 부재로 잘못 센 오답이다']}
/>

<Quiz
  questionNumber={15}
  question="세그먼테이션(segmentation)과 페이징(paging)을 비교한 설명으로 옳은 것은?"
  options={['세그먼테이션은 항상 고정 크기 단위로 메모리를 나누어 내부 단편화가 발생하지 않는다', '페이징은 프로그램의 논리적 구성(코드, 데이터, 스택 등) 단위를 그대로 반영하는 가변 크기 분할이다', '세그먼테이션은 가변 크기 단위로 나누어 프로그램의 논리적 구조를 반영하기 쉽지만 외부 단편화가 발생할 수 있다', '페이징과 세그먼테이션은 동시에 함께 쓸 수 없는 서로 배타적인 기법이다']}
  correctAnswer={3}
  explanation="세그먼테이션은 코드, 데이터, 스택처럼 프로그램의 논리적 단위를 가변 크기로 나누어 관리하므로 프로그램 구조를 자연스럽게 반영할 수 있지만, 세그먼트 크기가 제각각이라 외부 단편화가 발생할 수 있다. 반면 페이징은 고정 크기 프레임 단위로 나누어 외부 단편화는 없지만 내부 단편화가 생길 수 있다."
  optionExplanations={['세그먼테이션은 가변 크기 단위라 내부 단편화보다는 외부 단편화 문제가 더 자주 거론된다', '이 설명은 페이징이 아니라 세그먼테이션의 특징이다', '정답이다. 세그먼테이션의 특징과 한계로 옳은 설명이다', '두 기법을 결합한 세그먼테이션-페이징 혼합 방식도 실제로 쓰인다']}
/>

<Quiz
  questionNumber={16}
  question="연속 할당, 연결 할당, 인덱스 할당(파일 블록 할당 방식)을 비교한 설명으로 옳지 않은 것은?"
  options={['연속 할당은 순차 접근 성능이 좋지만 파일 크기가 늘어나면 재배치가 필요할 수 있다', '연결 할당은 각 블록이 다음 블록의 위치를 가리켜 파일이 흩어져 있어도 저장할 수 있지만 임의 접근(직접 접근)이 비효율적이다', '인덱스 할당은 인덱스 블록에 모든 데이터 블록의 위치를 모아두어 임의 접근에 유리하다', '연결 할당은 인덱스 할당보다 항상 임의 접근 속도가 빠르다']}
  correctAnswer={4}
  explanation="연결 할당은 원하는 블록에 접근하려면 첫 블록부터 포인터를 따라 순서대로 이동해야 하므로 임의 접근이 비효율적이다. 인덱스 할당은 인덱스 블록만 참조하면 원하는 블록 위치를 바로 알 수 있어 임의 접근에서 연결 할당보다 일반적으로 더 유리하다."
  optionExplanations={['연속 할당의 특징으로 옳은 설명이다', '연결 할당의 특징으로 옳은 설명이다', '인덱스 할당의 특징으로 옳은 설명이다', '옳지 않은 설명이다(정답). 임의 접근은 오히려 인덱스 할당이 연결 할당보다 유리하다']}
/>

<Quiz
  questionNumber={17}
  question="블록 크기 4킬로바이트, 블록 포인터 크기 4바이트인 inode 구조에서 직접 블록 12개와 1단계 간접 블록 1개만 사용한다고 할 때, 표현할 수 있는 최대 파일 크기에 가장 가까운 값은?"
  options={['약 48킬로바이트', '약 1메가바이트', '약 4메가바이트', '약 16메가바이트']}
  correctAnswer={3}
  explanation="한 블록에 담을 수 있는 포인터 개수는 블록 크기 4킬로바이트를 포인터 크기 4바이트로 나눈 1,024개다. 직접 블록 12개와 1단계 간접 블록이 가리키는 1,024개 블록을 더하면 총 1,036개 블록이며, 여기에 블록 크기 4킬로바이트를 곱하면 약 4,144킬로바이트, 즉 약 4메가바이트에 가깝다."
  optionExplanations={['직접 블록 12개만 고려하고 간접 블록을 반영하지 않았을 때 나올 수 있는 값에 가깝다', '간접 블록이 가리키는 포인터 수를 실제보다 적게 계산했을 때 나올 수 있는 오답이다', '정답이다. 직접 블록과 1단계 간접 블록을 모두 합한 값이다', '2단계 간접 블록까지 포함했을 때 나올 수 있는 훨씬 큰 값과 혼동한 오답이다']}
/>

<Quiz
  questionNumber={18}
  question="Flynn의 분류(Flynn's taxonomy)에서 하나의 명령어로 여러 데이터 요소에 동시에 같은 연산을 적용하는 구조는?"
  options={['SISD(Single Instruction Single Data)', 'SIMD(Single Instruction Multiple Data)', 'MISD(Multiple Instruction Single Data)', 'MIMD(Multiple Instruction Multiple Data)']}
  correctAnswer={2}
  explanation="SIMD는 하나의 명령어를 여러 데이터 요소에 동시에 적용하는 구조로, 벡터 연산이나 그래픽 처리 장치(GPU)의 병렬 연산에서 대표적으로 쓰인다. SISD는 일반적인 단일 프로세서 구조, MIMD는 여러 프로세서가 각자 다른 명령어로 다른 데이터를 처리하는 구조다."
  optionExplanations={['SISD는 하나의 명령어로 하나의 데이터만 처리하는 전통적인 단일 프로세서 구조다', '정답이다. 하나의 명령어로 여러 데이터를 동시에 처리하는 구조다', 'MISD는 실제 사례가 매우 드문 이론적 분류로, 여러 명령어가 하나의 데이터를 처리하는 구조다', 'MIMD는 여러 프로세서가 각자 다른 명령어로 다른 데이터를 독립적으로 처리하는 구조다']}
/>

<Quiz
  questionNumber={19}
  question="여러 서버 노드에 요청을 분산시키는 부하 분산(load balancing)에 대한 설명으로 옳지 않은 것은?"
  options={['부하 분산은 여러 노드에 요청을 나누어 처리량과 가용성을 함께 높이는 데 목적이 있다', '한 노드가 장애로 응답하지 못하면 부하 분산기가 다른 노드로 요청을 돌릴 수 있다', '부하 분산을 적용하면 개별 노드의 성능과 관계없이 시스템 전체 성능이 무한히 향상된다', '부하 분산 정책에는 라운드 로빈, 최소 연결 수 기반 분배 등 다양한 방식이 있다']}
  correctAnswer={3}
  explanation="부하 분산은 여러 노드에 요청을 나누어 처리량과 가용성을 높이는 데 효과적이지만, 개별 노드의 처리 능력이나 공유 자원(예: 데이터베이스)의 한계 때문에 시스템 전체 성능이 무한히 향상되지는 않는다."
  optionExplanations={['부하 분산의 목적으로 옳은 설명이다', '부하 분산기의 장애 대응 기능으로 옳은 설명이다', '옳지 않은 설명이다(정답). 성능 향상에는 공유 자원 등에 의한 한계가 있다', '실제로 쓰이는 대표적인 부하 분산 정책들로 옳은 설명이다']}
/>

<Quiz
  questionNumber={20}
  question="우선순위 역전(priority inversion) 현상에 대한 설명으로 옳은 것은?"
  options={['우선순위가 높은 프로세스가 항상 우선순위가 낮은 프로세스보다 먼저 실행되는 정상적인 상황을 말한다', '우선순위가 낮은 프로세스가 공유 자원을 점유한 상태에서, 우선순위가 높은 프로세스가 그 자원을 기다리며 오히려 더 오래 대기하게 되는 현상이다', '우선순위 역전은 우선순위 기반 스케줄링을 쓰지 않는 시스템에서만 발생한다', '우선순위 역전은 항상 시스템 설계와 무관하게 발생하며 예방할 수 없는 현상이다']}
  correctAnswer={2}
  explanation="우선순위 역전은 우선순위가 낮은 프로세스가 공유 자원(락)을 잡고 있는 동안, 우선순위가 높은 프로세스가 그 자원을 기다려야 해서 결과적으로 낮은 우선순위 프로세스 때문에 높은 우선순위 프로세스의 실행이 지연되는 현상이다. 우선순위 상속(priority inheritance) 같은 기법으로 완화할 수 있다."
  optionExplanations={['이는 정상적인 우선순위 기반 스케줄링 상황이며 역전 현상이 아니다', '정답이다. 낮은 우선순위 프로세스 때문에 높은 우선순위 프로세스가 지연되는 현상이다', '우선순위 역전은 오히려 우선순위 기반 스케줄링을 쓰는 시스템에서 공유 자원이 있을 때 나타나는 현상이다', '우선순위 상속과 같은 기법으로 우선순위 역전의 영향을 완화할 수 있다']}
/>

<Quiz
  questionNumber={21}
  question="독학사 4단계 통합컴퓨터시스템의 서술형 문항에서, 여러 영역이 섞인 시나리오를 답안으로 구성할 때 권장되는 순서로 가장 적절한 것은?"
  options={['결과 → 대안 → 원인 순서로 서술한다', '대안만 나열하고 원인과 결과는 생략한다', '원인 → 결과 → 대안 순서로 서술한다', '문제에 등장하는 모든 용어의 정의만 나열하고 끝맺는다']}
  correctAnswer={3}
  explanation="통합형 서술 문항은 왜 그 현상이 발생하는지(원인), 그 시나리오에서 어떤 영향을 주는지(결과), 어떻게 완화하거나 해결할 수 있는지(대안)의 3단 구성으로 답안을 쓰면 채점 기준에 맞춰 논리적으로 답할 수 있다."
  optionExplanations={['순서가 뒤바뀌어 논리적 흐름이 어색해진다', '원인과 결과 설명 없이 대안만 제시하면 근거가 부족한 답안이 된다', '정답이다. 원인, 결과, 대안 순서가 서술형 채점 기준과 잘 맞는다', '정의만 나열하면 시나리오에 대한 적용력과 분석력을 보여줄 수 없다']}
/>

<Quiz
  questionNumber={22}
  question="교착상태와 스래싱을 비교한 설명으로 옳은 것은?"
  options={['교착상태와 스래싱은 완전히 같은 현상을 가리키는 서로 다른 이름일 뿐이다', '교착상태는 자원을 서로 기다리며 영원히 멈추는 현상이고, 스래싱은 페이지 부재가 과도해 CPU 이용률이 급격히 떨어지는 현상으로 원인이 다르다', '스래싱은 항상 교착상태를 유발하고, 교착상태는 항상 스래싱을 유발한다', '두 현상 모두 CPU 스케줄링 알고리즘을 바꾸는 것만으로 완전히 해결된다']}
  correctAnswer={2}
  explanation="교착상태는 프로세스들이 서로 필요한 자원을 점유한 채 상대방의 자원을 기다리며 영원히 진행하지 못하는 현상이고, 스래싱은 다중 프로그래밍 정도가 지나쳐 프로세스마다 필요한 프레임을 확보하지 못해 페이지 부재가 과도하게 발생하고 CPU 이용률이 급격히 떨어지는 현상이다. 발생 원인과 메커니즘이 서로 다르다."
  optionExplanations={['두 현상은 원인과 메커니즘이 서로 다른 별개의 문제다', '정답이다. 두 현상의 원인과 메커니즘 차이를 정확히 설명한 내용이다', '한 현상이 다른 현상을 항상 유발한다는 일반적인 인과관계는 성립하지 않는다', '교착상태는 자원 관리 기법으로, 스래싱은 다중 프로그래밍 정도 조절이나 작업 집합 관리로 완화하며 스케줄링 알고리즘 변경만으로 해결되지 않는다']}
/>

## 출제 포인트 정리와 시험 직전 체크리스트

22편부터 24편까지 반복해서 등장한 계산 유형은 실제 시험에서도 반복 출제되는 대표 영역이다. 시험 직전에는 아래 항목을 순서대로 스스로 점검한다.

- **파이프라인 계산**: 명령어 수와 단계 수만 주어지면 이상적인 속도 향상 공식(k / (1 + (k-1)/n))을 바로 쓸 수 있는가. 해저드가 있으면 그만큼 사이클이 추가된다는 것을 반영했는가.
- **캐시 계산**: 태그·인덱스·오프셋 비트를 블록 크기와 캐시 크기로부터 순서대로(오프셋 → 인덱스 → 태그) 구할 수 있는가. 다단계 캐시의 평균 접근시간(AMAT) 공식을 헷갈리지 않는가.
- **CPU 스케줄링**: FCFS·SJF·SRTF·우선순위·라운드 로빈 각각의 선점 여부를 구분하고, 간트 차트를 그린 뒤 완료시각 → 반환시간 → 대기시간 순서로 계산하는 습관이 붙었는가.
- **교착상태**: 자원 인스턴스 수를 먼저 확인하고, 뱅커스 알고리즘의 필요량(Max-Allocation)과 가용 자원을 비교해 안전 순서 존재 여부를 판정할 수 있는가.
- **페이징**: FIFO·LRU·OPT 각각의 교체 규칙을 참조열에 정확히 적용하고, 벨레이디의 변칙처럼 "일반적인 상식과 다른 결과"가 나올 수 있다는 것을 알고 있는가.
- **RAID**: 레벨별(0, 1, 5) 사용 가능 용량 공식과 장애 허용 개수를 구분해서 기억하고 있는가.
- **통합형 문항**: 여러 영역이 섞인 시나리오에서 어느 영역의 계산부터 먼저 확정할지 순서를 정하고, 서술형은 원인 → 결과 → 대안 순서로 구성하는가.

이 체크리스트에서 스스로 "아니오"라고 답한 항목이 있다면, 해당 항목이 등장했던 편(07~19편, 22편)으로 돌아가 계산 과정을 한 번 더 손으로 따라가 보는 것이 가장 효과적인 마무리 복습이다.

## 참고 자료

- 최신 독학사 통합컴퓨터시스템 출제기준(국가평생교육진흥원 자료실·출제기준 PDF)
- Patterson & Hennessy, Computer Organization and Design(Revised 4th Ed.) — 파이프라인·캐시·RAID 정량 모델
- Silberschatz et al., Operating System Concepts(9판 강의노트·챕터 요약) — 스케줄링·동기화·가상메모리·파일시스템
- 국가평생교육진흥원 독학학위제: https://bdes.nile.or.kr
