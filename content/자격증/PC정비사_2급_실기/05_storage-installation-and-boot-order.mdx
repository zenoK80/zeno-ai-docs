---
title: '05. 저장장치 연결과 부팅 순서 설정'
description: 'SATA·M.2·NVMe 저장장치를 올바르게 연결하고 부팅 우선순위를 지정하며, 실기 단골인 디스크 관리자 파티션 분할을 크기·문자·파일 시스템·볼륨명까지 정확히 수행한다'
---

<Callout type="info">
  **이번 문서의 목표:** 이 파일을 다 읽으면 저장장치를 케이블부터 인식 확인까지 절차대로 연결할 수 있고, BIOS에서 부팅 순서를 지정할 수 있으며, 디스크 관리에서 비할당 공간을 지정된 크기·드라이브 문자·파일 시스템·볼륨 레이블로 분할할 수 있습니다.
</Callout>

## 저장장치는 "연결"과 "인식"이 다른 문제다

저장장치 작업에는 단계가 세 개 있습니다. 이 셋을 구분하지 못하면 진단이 엉킵니다.

```mermaid
graph LR
  A[① 물리 연결 — 케이블·슬롯] --> B[② 펌웨어 인식 — BIOS가 장치를 봄]
  B --> C[③ OS 사용 준비 — 파티션·포맷·드라이브 문자]
```

- ①이 안 되면 BIOS에도 안 보입니다.
- ②까지는 되는데 ③이 안 되면, **내 컴퓨터에는 안 보이지만 디스크 관리에는 보입니다.**
- ③까지 끝나야 드라이브 문자가 붙고 파일을 저장할 수 있습니다.

실기에서 "새 하드디스크를 연결했는데 내 컴퓨터에 보이지 않습니다"라는 문제의 정답은 대부분 ③입니다. 부품 교체가 아니라 **디스크 관리에서 초기화하고 볼륨을 만드는 것**이 답입니다.

> **쉽게 말하면:** 저장장치는 꽂았다고 쓸 수 있는 게 아니라, 선반을 만들고 이름표를 붙여야 쓸 수 있습니다.

## 1. 물리 연결 — 인터페이스별 절차

### SATA 저장장치 (HDD·SATA SSD)

케이블이 **두 개** 필요합니다.

| 케이블 | 출처 | 꽂는 곳 | 특징 |
|--------|------|---------|------|
| SATA 데이터 | 메인보드 SATA 포트 | 드라이브 데이터 단자 | 좁은 L자, 7핀 |
| SATA 전원 | 파워 서플라이 | 드라이브 전원 단자 | 넓은 L자, 15핀 |

<Steps>

### 드라이브를 베이에 고정한다

케이스의 저장장치 베이에 넣고 나사 네 개 또는 전용 트레이로 고정합니다. **나사 조임 누락**은 진동·소음의 원인이며 실기 마감 점검 항목입니다.

### 데이터 케이블을 연결한다

메인보드의 `SATA0` 또는 `SATA1` 포트에 꽂습니다. L자 방향이 있어 한 방향으로만 들어갑니다. 부팅용 드라이브는 가장 낮은 번호 포트에 연결하는 것이 관리상 편합니다.

### 전원 케이블을 연결한다

파워에서 나온 15핀 SATA 전원 커넥터를 드라이브에 꽂습니다.

### 케이블을 정리한다

팬 근처를 지나지 않게 묶습니다. 12편에서 라우팅 원칙을 다룹니다.

</Steps>

### M.2 NVMe SSD

케이블이 **하나도** 필요 없습니다. 슬롯이 데이터와 전원을 함께 공급합니다.

<Steps>

### 지지 나사 위치를 맞춘다

모듈 길이(`2280`, `2242` 등)에 맞는 위치에 지지 기둥(standoff)을 옮겨 끼웁니다. 위치가 틀리면 모듈이 뜨거나 나사가 안 맞습니다.

### 비스듬히 꽂는다

M.2 모듈은 슬롯에 약 30도 각도로 비스듬히 삽입한 뒤 아래로 눕힙니다. 수평으로 밀어 넣으려 하면 들어가지 않습니다.

### 나사로 고정한다

눕힌 상태에서 고정 나사를 조입니다. 세게 조이면 기판이 휘므로 가볍게 밀착되는 정도까지만 조입니다.

</Steps>

<Callout type="warning">
  **M.2 슬롯의 함정:** 많은 메인보드에서 M.2 슬롯을 사용하면 특정 SATA 포트가 자동으로 비활성화됩니다. 대역폭(레인)을 공유하기 때문입니다. "SSD를 추가했더니 기존 HDD가 사라졌다"는 증상의 정답이 바로 이것이며, 해결책은 **다른 SATA 포트로 옮기는 것**입니다. 메인보드 매뉴얼의 공유 표를 확인해야 합니다.
</Callout>

## 2. BIOS에서 인식 확인과 부팅 순서 설정

### 인식 확인

BIOS/UEFI에 들어가 다음 위치에서 장치 목록을 봅니다.

| 인터페이스 | 확인 위치 | 표시 형태 |
|------------|-----------|-----------|
| 구형 BIOS | Standard CMOS Features | IDE/SATA Channel별 모델명 |
| UEFI | 메인 화면 또는 Boot 메뉴 | 저장장치 아이콘·모델명 목록 |

여기에 이름이 뜨지 않으면 ① 물리 연결 문제입니다. 케이블 양쪽, 전원, 포트를 순서대로 바꿔 봅니다.

### 부팅 순서 지정

**부팅 순서**(Boot Order, Boot Sequence)는 BIOS가 운영체제를 찾을 때 어떤 장치를 먼저 뒤질지 정한 목록입니다.

<Steps>

### Boot 메뉴로 이동

구형 BIOS는 `Advanced BIOS Features` 안의 `First/Second/Third Boot Device`, UEFI는 `Boot` 탭의 `Boot Option Priorities` 입니다.

### 1순위 장치를 지정

`Boot Option #1` 에서 원하는 장치를 선택합니다. USB 부팅 미디어로 설치하려면 USB 장치를, 평소 사용에는 OS가 설치된 SSD를 1순위로 둡니다.

### UEFI 항목과 레거시 항목을 구분

같은 USB가 목록에 두 번 나올 수 있습니다. 앞에 `UEFI:` 가 붙은 항목과 안 붙은 항목입니다. **UEFI 모드로 설치하려면 `UEFI:` 가 붙은 쪽**을 선택해야 합니다. 이 선택이 나중에 GPT/MBR 파티션 방식을 결정합니다. 07편에서 이어집니다.

### F10으로 저장

저장하지 않으면 순서가 원래대로 돌아갑니다.

</Steps>

부팅 순서를 영구히 바꾸지 않고 한 번만 다른 장치로 부팅하려면 부팅 중 `F11` 또는 `F12` 로 열리는 **부팅 메뉴**(Boot Menu)를 씁니다. 설치 작업에서는 이 방법이 더 안전합니다 — 설정을 되돌릴 필요가 없기 때문입니다.

## 3. 디스크 관리 — 실기 파티션 문항의 무대

### 파티션과 볼륨이라는 단어부터

**파티션**(partition, 칸막이)은 하나의 물리 디스크를 논리적으로 나눈 구획입니다. 한 채의 집을 벽으로 나눠 여러 방으로 쓰는 것과 같습니다.

**볼륨**(volume)은 파일 시스템으로 포맷되어 실제로 쓸 수 있게 된 저장 공간입니다. 방에 바닥을 깔고 이름표를 붙여 사용 가능해진 상태입니다.

**비할당 공간**(Unallocated)은 아직 어떤 파티션에도 속하지 않은 빈 영역입니다. 검은색 막대로 표시되며, 여기에 볼륨을 만들어야 사용할 수 있습니다.

### 디스크 관리 열기

실행 창(`Win` + `R`)에 `diskmgmt.msc` 를 입력합니다. 또는 시작 버튼을 우클릭해 디스크 관리를 선택합니다.

### 문항 예시로 절차 익히기

실기 문항은 이런 형태로 나옵니다.

> 디스크 0의 750GB 비할당 공간을 두 개의 파티션으로 분할하시오.
> 파티션 1 — 크기 500GB, 파일 시스템 NTFS, 드라이브 문자 D, 볼륨 레이블 IT 기술부
> 파티션 2 — 크기 250GB, 파일 시스템 exFAT, 드라이브 문자 E, 볼륨 레이블 기술영업부

<Steps>

### 대상 확인

디스크 0 행에서 검은색 막대의 비할당 공간을 확인합니다. **디스크 번호를 반드시 확인**하세요. 디스크 1의 공간을 건드리면 0점입니다.

### 새 단순 볼륨 마법사 시작

비할당 공간 위에서 마우스 우클릭 → **새 단순 볼륨**을 선택합니다. 마법사가 시작되면 다음을 누릅니다.

### 볼륨 크기 입력

단순 볼륨 크기 입력란에 `500000` 을 입력합니다. **단위가 MB**이므로 500GB는 500000MB입니다. 정확히 512000이 아니라 500000으로 입력하라는 문제 의도이므로, 문제에 GB로 적혀 있으면 1000을 곱한 값을 넣는 것이 채점 기준에 부합합니다.

### 드라이브 문자 지정

다음 드라이브 문자 할당에서 목록을 열어 `D` 를 선택합니다. 기본으로 제안된 문자가 다를 수 있으므로 반드시 바꿉니다.

### 파일 시스템과 볼륨 레이블 지정

포맷 화면에서 파일 시스템을 `NTFS` 로 선택하고, 볼륨 레이블 입력란에 `IT 기술부` 를 입력합니다. 할당 단위 크기는 문제 지시가 없으면 기본값을 둡니다. **빠른 포맷 실행** 체크는 켜 두는 것이 시간상 유리합니다.

### 마침

요약 화면에서 네 값(크기·문자·파일 시스템·레이블)을 다시 읽고 마침을 누릅니다.

### 두 번째 파티션 반복

남은 비할당 공간에서 다시 우클릭 → 새 단순 볼륨. 크기는 남은 전부(약 `250000`MB), 드라이브 문자 `E`, 파일 시스템 `exFAT`, 볼륨 레이블 `기술영업부` 로 지정하고 마칩니다.

</Steps>

<Callout type="warning">
  **채점 항목으로 쪼개 보기:** 파티션 하나당 ① 크기 ② 드라이브 문자 ③ 파일 시스템 ④ 볼륨 레이블 네 개가 채점점입니다. 두 파티션이면 여덟 개입니다. 볼륨 레이블의 **띄어쓰기까지 문제 그대로** 입력해야 하며, 완료 후 디스크 관리 화면에서 막대에 표시된 레이블과 문자를 눈으로 확인하는 것이 마지막 절차입니다.
</Callout>

### 자주 하는 실수

- **단위 착각** — 입력란은 MB입니다. `500` 을 넣으면 500MB짜리 볼륨이 생깁니다.
- **드라이브 문자 미변경** — 마법사가 제안한 문자를 그대로 두면 오답입니다.
- **파일 시스템 미변경** — 기본값이 NTFS이므로 exFAT 지정 문항에서 특히 주의합니다.
- **볼륨 레이블 오타·공백 누락** — `IT 기술부` 와 `IT기술부` 는 다릅니다.
- **디스크 번호 혼동** — 디스크 0과 디스크 1을 반드시 구분합니다.

### 파일 시스템 선택의 근거

문제가 왜 굳이 exFAT을 지정하는지도 알아 두면 서술형에 대응할 수 있습니다.

| 파일 시스템 | 단일 파일 최대 | 특징 | 주 용도 |
|-------------|----------------|------|---------|
| FAT32 | 4GB | 호환성 최고, 대용량 제약 | 구형 USB, 일부 장비 |
| exFAT | 사실상 제한 없음 | FAT의 확장판, 권한·저널링 없음 | 대용량 USB·외장 SSD, 이종 OS 교환 |
| NTFS | 사실상 제한 없음 | 권한·압축·저널링 지원 | Windows 시스템 드라이브 |

**저널링**(journaling)은 파일을 바꾸기 전에 "무엇을 바꿀 것인지"를 먼저 기록해 두는 기능입니다. 작업 도중 전원이 꺼져도 기록을 보고 복구할 수 있어 손상에 강합니다. NTFS는 이 기능이 있고 exFAT은 없습니다. 대신 exFAT은 구조가 단순해 여러 운영체제에서 잘 읽힙니다.

**exFAT**은 Extended File Allocation Table의 줄임말로, "확장된 파일 할당 표"라는 뜻입니다. FAT32의 4GB 파일 크기 한계를 없애면서도 가벼움을 유지한 규격입니다.

### 볼륨 축소·확장·삭제

문항이 "기존 볼륨을 줄여 새 파티션을 만드시오"로 나올 수도 있습니다.

| 작업 | 우클릭 메뉴 | 주의 |
|------|-------------|------|
| 축소 | 볼륨 축소 | 축소 가능 크기는 데이터 배치에 따라 제한됨 |
| 확장 | 볼륨 확장 | **바로 오른쪽에 인접한** 비할당 공간만 흡수 가능 |
| 삭제 | 볼륨 삭제 | 데이터가 사라짐, 되돌릴 수 없음 |
| 문자 변경 | 드라이브 문자 및 경로 변경 | 사용 중인 프로그램 경로가 깨질 수 있음 |

볼륨 확장이 회색으로 비활성인 경우, 원인은 대부분 **비할당 공간이 그 볼륨의 오른쪽에 인접해 있지 않아서**입니다. 이 원리를 모르면 "확장이 안 됩니다"라는 문제 앞에서 헤맵니다.

## 4. 새 디스크를 처음 붙였을 때 — 초기화 절차

완전히 새 디스크는 디스크 관리에서 **초기화되지 않음**으로 표시됩니다.

<Steps>

### 디스크 초기화

디스크 이름 부분(왼쪽 회색 영역)을 우클릭 → 디스크 초기화를 선택합니다.

### 파티션 스타일 선택

`MBR` 과 `GPT` 중 선택합니다. 2.2TB를 넘는 디스크이거나 UEFI 부팅용이면 **GPT**를 고릅니다. 구형 시스템 호환이 필요하면 MBR입니다. 자세한 비교는 07편에서 다룹니다.

### 새 단순 볼륨 생성

위의 파티션 절차를 그대로 수행합니다.

</Steps>

## 5. 저장장치 진단 표

| 증상 | 어디까지 보이는가 | 원인 | 조치 |
|------|-------------------|------|------|
| BIOS에도 안 보임 | 없음 | 케이블·전원·포트 불량 | 케이블 교체, 포트 변경 |
| BIOS에 보이는데 탐색기에 없음 | BIOS만 | 볼륨 미생성 또는 문자 미할당 | 디스크 관리에서 볼륨 생성 |
| 디스크 관리에 오프라인 표시 | 디스크 관리 | 디스크가 오프라인 상태 | 우클릭 → 온라인 |
| M.2 추가 후 기존 SATA가 사라짐 | 일부 | 레인 공유로 SATA 포트 비활성 | 다른 SATA 포트로 이동 |
| 부팅 시 운영체제 없음 메시지 | – | 부팅 순서가 데이터 디스크로 지정 | 부팅 순서에서 OS 디스크를 1순위로 |
| 딸깍거리는 소리와 인식 실패 | 간헐 | HDD 헤드 물리 손상 | 즉시 전원 차단, 데이터 백업 우선 |

마지막 항목은 특히 중요합니다. HDD에서 규칙적인 딸깍 소리가 나면 **계속 켜 두는 것이 데이터를 더 망칩니다.** 실기 서술형에서 "가장 먼저 취할 조치"를 묻는다면 "전원을 끄고 데이터 백업을 우선한다"가 정답입니다.

## 핵심 정리

- 저장장치는 물리 연결 → BIOS 인식 → 볼륨 생성 세 단계이며, 탐색기에 안 보이는 문제의 대부분은 세 번째 단계 누락입니다.
- M.2 슬롯 사용 시 특정 SATA 포트가 비활성화될 수 있으므로, 기존 드라이브가 사라지면 포트를 옮깁니다.
- 부팅 순서에서 `UEFI:` 접두어가 붙은 항목과 아닌 항목은 서로 다른 부팅 모드이며, 이 선택이 파티션 방식을 결정합니다.
- 새 단순 볼륨 마법사는 **크기(MB) → 드라이브 문자 → 파일 시스템 → 볼륨 레이블** 순이며 네 값 모두가 채점 항목입니다.
- 볼륨 확장은 오른쪽에 인접한 비할당 공간만 흡수할 수 있습니다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="디스크 관리에서 500GB 크기의 파티션을 만들 때 단순 볼륨 크기 입력란에 넣어야 할 값은?"
  options={['500', '5000', '500000', '50']}
  correctAnswer={3}
  explanation="입력 단위는 MB이므로 500GB는 500000MB로 입력합니다. 500을 입력하면 500MB짜리 볼륨이 만들어집니다."
  optionExplanations={['500MB에 해당해 크기가 크게 부족합니다', '5000MB로 여전히 부족합니다', 'MB 단위로 환산한 올바른 값입니다', '50MB로 전혀 다른 크기입니다']}
/>

<Quiz
  questionNumber={2}
  question="새로 연결한 하드디스크가 BIOS에는 보이지만 탐색기에는 나타나지 않을 때의 올바른 조치는?"
  options={['메인보드를 교체한다', '디스크 관리에서 초기화하고 새 단순 볼륨을 생성해 드라이브 문자를 할당한다', 'SATA 케이블을 두 개 연결한다', 'BIOS를 초기화한다']}
  correctAnswer={2}
  explanation="BIOS 인식은 물리 연결이 정상이라는 뜻입니다. 파티션과 포맷을 거쳐 드라이브 문자가 할당되어야 탐색기에 나타납니다."
  optionExplanations={['하드웨어 인식이 정상이므로 교체 사유가 없습니다', '볼륨 생성과 문자 할당이 누락된 단계입니다', '데이터 케이블을 중복 연결할 수는 없습니다', 'BIOS 초기화는 인식 문제와 무관합니다']}
/>

<Quiz
  questionNumber={3}
  question="볼륨 확장 메뉴가 회색으로 비활성화되어 있는 가장 흔한 원인은?"
  options={['디스크가 GPT라서', '확장하려는 볼륨의 오른쪽에 인접한 비할당 공간이 없어서', '파일 시스템이 NTFS라서', '드라이브 문자가 D라서']}
  correctAnswer={2}
  explanation="볼륨 확장은 해당 볼륨 바로 오른쪽에 인접한 비할당 공간만 흡수할 수 있습니다. 떨어져 있거나 왼쪽에 있으면 확장할 수 없습니다."
  optionExplanations={['GPT는 오히려 확장에 유리한 방식입니다', '인접성 조건이 확장 가능 여부를 결정합니다', 'NTFS는 확장을 지원하는 파일 시스템입니다', '드라이브 문자는 확장 조건과 무관합니다']}
/>

<Quiz
  questionNumber={4}
  question="M.2 NVMe SSD를 장착한 뒤 기존에 쓰던 SATA 하드디스크가 인식되지 않는다면 가장 유력한 원인은?"
  options={['NVMe가 SATA보다 빨라서', 'M.2 슬롯과 특정 SATA 포트가 대역폭을 공유해 해당 포트가 비활성화되었기 때문', '전원 용량이 부족해서', 'NTFS가 손상되어서']}
  correctAnswer={2}
  explanation="많은 메인보드에서 M.2 슬롯 사용 시 특정 SATA 포트가 자동으로 비활성화됩니다. 하드디스크를 다른 SATA 포트로 옮기면 해결됩니다."
  optionExplanations={['속도 차이는 인식 여부와 무관합니다', '레인 공유로 인한 포트 비활성화가 원인입니다', '전원 부족이면 다른 증상이 함께 나타납니다', '파일 시스템 손상이면 디스크 관리에는 표시됩니다']}
/>

<Quiz
  questionNumber={5}
  question="대용량 파일을 다루면서 여러 운영체제 간 교환이 필요한 외장 저장장치에 가장 적합한 파일 시스템은?"
  options={['FAT32', 'exFAT', 'NTFS', 'APFS']}
  correctAnswer={2}
  explanation="exFAT은 FAT32의 4GB 단일 파일 제한을 없앤 규격으로 구조가 단순해 여러 운영체제에서 잘 읽히며 대용량 이동식 매체에 적합합니다."
  optionExplanations={['FAT32는 단일 파일 4GB 제한이 있습니다', '대용량과 이종 OS 호환을 동시에 만족합니다', 'NTFS는 Windows 시스템용으로 타 OS 쓰기 지원이 제한적입니다', 'APFS는 macOS 전용 파일 시스템입니다']}
/>

<Quiz
  questionNumber={6}
  question="USB 부팅 미디어를 UEFI 모드로 부팅하려 할 때 부팅 메뉴에서 선택해야 하는 항목은?"
  options={['장치 이름만 표시된 항목', 'UEFI 접두어가 붙은 장치 항목', '항목이 두 개면 아래쪽 항목', '아무거나 상관없다']}
  correctAnswer={2}
  explanation="같은 장치가 UEFI 항목과 레거시 항목으로 각각 표시될 수 있으며, UEFI 접두어가 붙은 항목을 선택해야 UEFI 모드로 설치가 진행되고 GPT 파티션을 사용할 수 있습니다."
  optionExplanations={['접두어가 없는 항목은 레거시 모드입니다', 'UEFI 접두어가 부팅 모드를 결정합니다', '목록의 위치는 기준이 되지 않습니다', '부팅 모드가 파티션 방식을 결정하므로 상관있습니다']}
/>

<Quiz
  questionNumber={7}
  question="하드디스크에서 규칙적인 딸깍 소리가 나면서 인식이 되지 않을 때 가장 먼저 취해야 할 조치는?"
  options={['계속 켜 둔 채로 오랫동안 검사 도구를 돌린다', '즉시 전원을 차단하고 데이터 백업과 복구를 우선 검토한다', '포맷을 진행한다', '파티션을 다시 만든다']}
  correctAnswer={2}
  explanation="규칙적인 딸깍 소리는 헤드의 물리적 손상을 시사합니다. 계속 동작시키면 디스크 표면이 추가로 손상되므로 전원을 차단하고 데이터 보존을 우선해야 합니다."
  optionExplanations={['동작을 지속하면 손상이 확대됩니다', '전원 차단과 데이터 보존이 최우선입니다', '포맷은 데이터를 잃고 손상도 막지 못합니다', '파티션 작업 역시 물리 손상을 악화시킵니다']}
/>

<Quiz
  questionNumber={8}
  question="M.2 SSD를 장착할 때의 올바른 절차는?"
  options={['수평으로 밀어 넣고 나사를 조인다', '모듈 길이에 맞게 지지 기둥을 옮긴 뒤 비스듬히 삽입하고 눕혀서 나사로 고정한다', '전원 케이블과 데이터 케이블을 각각 연결한다', '슬롯 걸쇠를 양쪽으로 젖히고 수직으로 누른다']}
  correctAnswer={2}
  explanation="M.2 모듈은 약 30도 각도로 삽입한 뒤 눕혀서 고정 나사를 조입니다. 케이블은 필요 없고 슬롯이 데이터와 전원을 함께 공급합니다."
  optionExplanations={['수평으로는 삽입되지 않습니다', '지지 기둥 위치 조정과 비스듬한 삽입이 정석입니다', 'M.2는 별도 케이블이 필요 없습니다', '그것은 DIMM 메모리 장착 방식입니다']}
/>

## 참고 자료

- [Microsoft Learn - Windows Setup: Installing using the MBR or GPT partition style](https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/windows-setup-installing-using-the-mbr-or-gpt-partition-style?view=windows-11)
- [Intel - How to Configure the System in UEFI Mode before Installing Windows](https://www.intel.com/content/www/us/en/support/articles/000025535/memory-and-storage/intel-optane-memory.html)
- [UEFI Forum - Windows Boot Environment](https://uefi.org/sites/default/files/resources/UEFI-Plugfest-WindowsBootEnvironment.pdf)
