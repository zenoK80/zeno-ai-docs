---
title: '15. 그 밖의 에디터와 비교'
description: 'emacs·nano·pico·gedit의 특징과 사용 상황, vi와의 조작 방식 차이를 정리한다'
---

<Callout type="info">
  **이번 문서의 목표:** 이 파일을 다 읽으면 emacs·nano·pico·gedit 네 편집기의 특징과 서로 다른 조작 방식을 설명할 수 있고, 어떤 상황에 어떤 에디터가 어울리는지 판단할 수 있으며, 이 편집기들을 개발자·계열과 짝지어 묻는 기출 유형에 대비할 수 있다.
</Callout>

## 왜 vi 말고도 여러 에디터를 알아야 하는가

15편에서 배운 vi는 리눅스 어디에나 있다는 장점이 있지만, 명령 모드·입력 모드를 오가야 하는 조작 방식이 초보자에게는 낯설고 진입 장벽이 높다. 그래서 실제 현장에서는 상황에 따라 다른 편집기를 선택해서 쓴다. 서버에 처음 접속한 신입 관리자가 급하게 설정 파일 한 줄만 고쳐야 한다면 화면 하단에 사용법이 그대로 적혀 있는 에디터가 훨씬 빠르고, 프로그래머가 며칠씩 붙잡고 코드를 짤 때는 확장 기능이 풍부한 에디터가 유리하며, 데스크톱 환경에서 마우스로 편하게 작업하고 싶다면 굳이 콘솔 에디터를 고집할 이유가 없다.

> **쉽게 말하면:** vi가 "어디서나 쓸 수 있는 기본 도구"라면, emacs·nano·pico·gedit은 상황에 맞춰 고를 수 있는 "특화 도구"들이다.

리눅스마스터 시험은 이 네 에디터의 특징과 개발자·계열을 짝지어 묻거나, "어느 것이 X 윈도 환경에서만 동작하는가" 같은 분류 문제를 즐겨 낸다. 아래에서 하나씩 살펴본다.

## emacs — 메타키 조합형의 강력한 편집기

> **쉽게 말하면:** emacs는 단축키 조합으로 온갖 기능을 처리하는, 배우기는 어렵지만 매우 강력한 편집기다.

emacs는 리처드 스톨만(Richard Stallman)이 GNU 프로젝트의 일환으로 만든 편집기다. GNU(GNU's Not Unix)는 스톨만이 시작한 자유 소프트웨어 운동으로, 누구나 소스 코드를 보고 고치고 재배포할 수 있는 소프트웨어를 만드는 것을 목표로 한다. emacs는 이 GNU 프로젝트를 대표하는 소프트웨어 중 하나이며, 그래서 정식 명칭도 GNU Emacs다.

emacs의 조작 방식은 vi와 근본적으로 다르다. vi가 "지금 명령 모드인가 입력 모드인가"로 키의 의미를 바꾸는 방식이라면, emacs는 항상 글자를 바로 입력할 수 있는 상태를 기본으로 두고, `Ctrl` 키나 `Esc`(emacs에서는 이를 메타키라고 부른다)와 다른 키를 조합해서 각종 기능을 실행한다. 예를 들어 `Ctrl+x`를 누른 다음 `Ctrl+s`를 누르면 저장, `Ctrl+x` 다음 `Ctrl+c`를 누르면 종료다. vi처럼 모드를 오갈 필요는 없지만, 대신 외워야 할 키 조합의 가짓수가 훨씬 많다.

emacs는 단순한 텍스트 편집기를 넘어 이메일 확인, 일정 관리, 계산기 실행까지 내부 기능으로 제공할 만큼 확장성이 크다는 점에서 "편집기라기보다 하나의 운영 환경에 가깝다"는 평가를 받는다. 콘솔에서도 동작하지만 X 윈도 환경에서 그래픽 버전(XEmacs 등)으로도 실행할 수 있다.

**자주 틀리는 점**: emacs의 개발자를 vi의 개발자(빌 조이)와 혼동하는 문제가 자주 나온다. **emacs는 리처드 스톨만**, **vi는 빌 조이**, **vim은 브람 몰레나르**라고 인물과 소프트웨어를 정확히 짝지어야 한다.

## nano와 pico — 화면 하단 단축키 안내형

> **쉽게 말하면:** nano와 pico는 화면 아래쪽에 사용법이 그대로 적혀 있어서 외우지 않아도 바로 쓸 수 있는 편집기다.

pico는 원래 유닉스용 이메일 프로그램인 Pine에 딸린 텍스트 작성 도구로 만들어졌다. 화면 하단에 `^X`(Ctrl+X는 종료), `^O`(Ctrl+O는 저장) 같은 안내 문구를 항상 띄워 두어서, vi처럼 모드나 명령을 외우지 않아도 화면만 보고 바로 따라 할 수 있다는 것이 가장 큰 특징이다. 여기서 `^` 기호는 `Ctrl` 키를 누른 채 뒤의 글자를 함께 누르라는 표시다.

nano는 GNU 프로젝트가 pico와 호환되도록(즉 같은 단축키·같은 화면 구성으로) 새로 만든 편집기다. pico가 자유 소프트웨어 라이선스 문제로 일부 배포판에 기본 포함되기 어려웠던 시절이 있었는데, 이를 대체할 목적으로 nano가 만들어졌다. 기본 조작은 pico와 거의 동일하지만, 구문 강조(문법에 따라 색을 다르게 표시하는 기능)와 정규표현식 검색처럼 pico에는 없던 기능이 추가되어 있다는 점에서 "nano는 pico의 상위 호환"이라고 정리할 수 있다.

두 편집기 모두 콘솔 환경에서 동작하며, X 윈도가 없는 환경에서도 문제없이 쓸 수 있다. 서버 설정 파일을 급하게 수정해야 하는데 vi 조작이 아직 손에 익지 않은 초보자에게는 nano가 가장 접근하기 쉬운 선택이다.

**자주 틀리는 점**: "pico와 nano는 완전히 다른 사용법을 가진 편집기"라는 식의 오답이 나오는데, 실제로는 **단축키와 화면 구성이 거의 동일**하다. 다른 점은 라이선스와 몇 가지 부가 기능(구문 강조, 정규식 검색)뿐이다.

## gedit — X 윈도 전용 GUI 편집기

> **쉽게 말하면:** gedit은 윈도우의 메모장처럼 마우스로 클릭하고 타이핑만 하면 되는, X 윈도 환경 전용 그래픽 편집기다.

gedit은 리눅스 데스크톱 환경 중 하나인 GNOME(그놈)의 기본 텍스트 편집기다. GNOME은 X 윈도 시스템(그래픽 화면을 그려 주는 리눅스의 창 시스템, 22편에서 자세히 다룬다) 위에서 동작하는 데스크톱 환경으로, 아이콘·창·메뉴 같은 익숙한 그래픽 인터페이스를 제공한다. gedit은 이 GNOME 위에서 마우스로 파일을 열고, 클릭으로 커서를 옮기고, 메뉴에서 저장 버튼을 누르는 방식으로 동작한다. 지금까지 살펴본 vi·emacs·nano·pico가 모두 콘솔(그래픽 화면 없이 문자만 나오는 환경)에서도 동작하는 것과 달리, **gedit은 X 윈도가 실행되고 있어야만 쓸 수 있는 GUI 전용 편집기**다.

KDE(케이디이) 데스크톱 환경에는 gedit과 비슷한 위치의 kate·kwrite 같은 GUI 편집기가 있고, emacs와 vim도 각각 XEmacs·gVim이라는 그래픽 버전을 가지고 있다. 하지만 이들은 "그래픽으로도 쓸 수 있는" 편집기일 뿐 콘솔에서도 여전히 동작하는 반면, gedit은 처음부터 GNOME 환경 전용으로 설계되었다는 차이가 있다.

**자주 틀리는 점**: "X 윈도 환경에서만 실행되는 편집기"를 고르는 문제에서 vi·nano·pico·emacs는 모두 콘솔에서도 동작하므로 오답이고, **gedit만 X 윈도 없이는 실행할 수 없는 정답**이 된다. 콘솔 동작 여부가 이 유형 문제를 가르는 기준이라는 점을 기억해 둔다.

## 네 편집기 한눈에 비교

| 편집기 | 개발 주체·배경 | 조작 방식 | 실행 환경 |
|--------|----------------|-----------|-----------|
| emacs | 리처드 스톨만, GNU 프로젝트 | 메타키(Ctrl·Esc) 조합형, 항상 입력 가능 | 콘솔 + 그래픽(XEmacs) |
| nano | GNU 프로젝트(pico 호환으로 개발) | 화면 하단 단축키 안내형 | 콘솔 |
| pico | Pine 이메일 프로그램의 편집 도구 | 화면 하단 단축키 안내형(nano와 거의 동일) | 콘솔 |
| gedit | GNOME 데스크톱 환경 | 마우스·메뉴 기반 GUI | X 윈도 전용 |

vi와 비교하면 조작 방식의 차이가 더 뚜렷해진다. vi는 명령 모드와 입력 모드를 오가며 키 하나하나에 의미를 부여하는 **모드 기반**이고, emacs는 모드 구분 없이 키 조합으로 기능을 실행하는 **메타키 기반**이며, nano·pico는 외울 필요 없이 화면에 적힌 대로 따라 하는 **단축키 안내형**이고, gedit은 아예 키보드 명령 자체를 최소화한 **마우스 기반**이다. 같은 "텍스트를 저장하고 종료한다"는 동작 하나도 vi는 `:wq`, emacs는 `Ctrl+x` 다음 `Ctrl+s`로 저장 후 `Ctrl+x` 다음 `Ctrl+c`로 종료, nano는 `Ctrl+o`로 저장 후 `Ctrl+x`로 종료, gedit은 메뉴나 도구 모음의 저장 아이콘과 닫기 버튼 클릭으로 전혀 다르게 처리한다.

## 어떤 상황에 어떤 에디터를 쓰는가

- **원격 서버에서 설정 파일 한두 줄만 급하게 고칠 때**: vi(어느 배포판에나 있다는 확실성) 또는 nano(사용법을 외우지 않아도 되는 편의성).
- **콘솔 환경에서 대규모 설정 파일이나 스크립트를 오래 붙잡고 편집할 때**: emacs(확장 기능과 다중 버퍼 편집).
- **그래픽 데스크톱에서 마우스로 편하게 메모하듯 문서를 작성할 때**: gedit.
- **초보자에게 처음 리눅스 편집을 가르칠 때**: pico·nano(화면에 사용법이 그대로 보이므로 학습 부담이 적다).

## 핵심 정리

- **emacs**는 리처드 스톨만이 만든 GNU 프로젝트의 편집기로, 메타키(Ctrl·Esc) 조합으로 기능을 실행하며 콘솔·그래픽 모두 지원한다.
- **pico**는 Pine 이메일 프로그램의 편집 도구에서 시작되었고, **nano**는 이를 호환 대체하며 구문 강조 등을 더한 편집기다. 둘 다 화면 하단 단축키 안내가 특징이며 조작 방식이 거의 동일하다.
- **gedit**은 GNOME 데스크톱의 GUI 전용 편집기로, 네 편집기 중 유일하게 X 윈도 없이는 실행할 수 없다.
- 인물·계열 매칭(빌 조이-vi, 브람 몰레나르-vim, 리처드 스톨만-emacs)과 콘솔·GUI 구분이 시험의 핵심 함정이다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="다음 중 리처드 스톨만이 GNU 프로젝트의 일환으로 개발한 편집기로 알맞은 것은?"
  options={['vi', 'emacs', 'gedit', 'pico']}
  correctAnswer={2}
  explanation="emacs는 리처드 스톨만이 GNU 프로젝트의 하나로 개발한 편집기다. vi는 빌 조이, gedit은 GNOME 진영, pico는 Pine 이메일 프로그램에서 시작되었다."
  optionExplanations={['vi는 빌 조이가 BSD 유닉스 시절에 개발한 편집기다', 'emacs가 리처드 스톨만이 GNU 프로젝트로 개발한 편집기로 옳다', 'gedit은 GNOME 데스크톱 환경의 GUI 편집기로 스톨만의 작품이 아니다', 'pico는 Pine 이메일 프로그램에 딸린 편집 도구에서 시작되었다']}
/>

<Quiz
  questionNumber={2}
  question="다음 중 화면 하단에 단축키 안내를 표시하며 pico와 조작 방식이 거의 동일한 편집기로 알맞은 것은?"
  options={['emacs', 'gedit', 'nano', 'vi']}
  correctAnswer={3}
  explanation="nano는 GNU 프로젝트가 pico를 호환 대체하기 위해 만든 편집기로, 화면 하단 단축키 안내와 조작 방식이 pico와 거의 동일하다."
  optionExplanations={['emacs는 메타키 조합형으로 pico와 조작 방식이 전혀 다르다', 'gedit은 마우스 기반 GUI 편집기로 화면 하단 단축키 안내 방식과 다르다', 'nano가 pico를 호환 대체한 편집기로 옳다', 'vi는 명령 모드와 입력 모드를 오가는 방식으로 pico와 조작 방식이 다르다']}
/>

<Quiz
  questionNumber={3}
  question="다음 편집기 중 X 윈도 환경이 실행되고 있어야만 사용할 수 있는 것으로 알맞은 것은?"
  options={['nano', 'pico', 'gedit', 'vi']}
  correctAnswer={3}
  explanation="gedit은 GNOME 데스크톱 환경의 GUI 전용 편집기로, X 윈도가 실행되지 않으면 사용할 수 없다. 나머지는 모두 콘솔에서도 동작한다."
  optionExplanations={['nano는 콘솔 환경에서 동작하는 텍스트 기반 편집기다', 'pico는 콘솔 환경에서 동작하는 텍스트 기반 편집기다', 'gedit이 X 윈도 없이는 실행할 수 없는 GUI 전용 편집기로 옳다', 'vi는 콘솔에서도 동작하는 모드 기반 편집기다']}
/>

<Quiz
  questionNumber={4}
  question="emacs의 조작 방식을 vi와 비교해 설명한 것으로 가장 알맞은 것은?"
  options={['emacs는 vi와 마찬가지로 명령 모드와 입력 모드를 구분한다', 'emacs는 항상 입력이 가능한 상태에서 메타키 조합으로 기능을 실행한다', 'emacs는 마우스 클릭으로만 기능을 실행하는 GUI 전용 편집기다', 'emacs는 화면 하단 단축키 안내만으로 조작하는 편집기다']}
  correctAnswer={2}
  explanation="emacs는 vi처럼 모드를 구분하지 않고 항상 글자를 입력할 수 있는 상태를 기본으로 하며, Ctrl이나 메타키와 다른 키를 조합해 기능을 실행한다."
  optionExplanations={['모드를 구분하는 것은 vi의 방식이며 emacs는 항상 입력 가능한 상태를 기본으로 한다', 'emacs가 메타키 조합으로 기능을 실행하는 방식으로 옳다', 'emacs는 콘솔에서도 키보드로 조작하는 편집기이며 마우스 전용이 아니다', '화면 하단 단축키 안내는 nano와 pico의 특징이다']}
/>

<Quiz
  questionNumber={5}
  question="nano가 pico를 대체하기 위해 만들어진 배경으로 가장 알맞은 것은?"
  options={['pico가 X 윈도 전용이라 콘솔에서 쓸 수 없었기 때문에', 'pico의 라이선스 문제로 일부 배포판에 기본 포함하기 어려웠기 때문에', 'pico가 저장·종료 기능을 지원하지 않았기 때문에', 'pico가 emacs와 조작 방식이 동일해 중복이었기 때문에']}
  correctAnswer={2}
  explanation="pico는 자유 소프트웨어 라이선스 문제로 일부 배포판에 기본 포함하기 어려웠고, 이를 대체할 목적으로 GNU 진영에서 호환 편집기인 nano를 만들었다."
  optionExplanations={['pico는 콘솔 환경에서 동작하는 편집기이며 X 윈도 전용이 아니다', '라이선스 문제로 인한 대체가 nano 개발의 배경으로 옳다', 'pico도 Ctrl+O 저장, Ctrl+X 종료 같은 기본 기능을 지원했다', 'pico는 화면 하단 단축키 안내형으로 메타키 조합형인 emacs와 조작 방식이 다르다']}
/>

<Quiz
  questionNumber={6}
  question="다음 중 vi와 emacs의 조작 방식 차이를 가장 정확하게 설명한 것은?"
  options={['vi는 마우스 기반이고 emacs는 키보드 기반이다', 'vi는 모드에 따라 키의 의미가 달라지고 emacs는 항상 같은 방식으로 키 조합을 해석한다', 'vi는 GUI 전용이고 emacs는 콘솔 전용이다', 'vi와 emacs는 사실상 동일한 조작 방식을 쓴다']}
  correctAnswer={2}
  explanation="vi는 명령 모드·입력 모드에 따라 같은 키라도 의미가 달라지는 모드 기반 편집기이고, emacs는 모드 구분 없이 항상 같은 방식(메타키 조합)으로 키를 해석한다."
  optionExplanations={['vi와 emacs 모두 콘솔에서 키보드로 조작하는 편집기다', '모드에 따라 키 해석이 달라지는 것이 vi, 항상 동일하게 해석하는 것이 emacs의 특징으로 옳다', '두 편집기 모두 콘솔에서 동작하며 vi가 GUI 전용인 것은 아니다', '두 편집기의 조작 방식은 모드 기반과 메타키 기반으로 서로 다르다']}
/>

## 참고 자료

- [The Linux Documentation Project](https://tldp.org)
- [GNU 라이선스 목록](https://www.gnu.org/licenses/license-list.html)
