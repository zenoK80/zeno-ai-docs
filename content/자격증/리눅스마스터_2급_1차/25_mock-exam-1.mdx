---
title: '25. 리눅스마스터 2급 1차 예상 모의고사 1회'
description: '3과목 비율에 맞춘 표준 난이도의 예상 모의고사 50문항, 60분 풀이 기준'
---

<Callout type="warning">
  이 모의고사는 실제 기출문제를 그대로 옮긴 것이 아니다. `reference/`에 확보된 2급 기출·복원·모의고사 자료를 분석해 (1) 어떤 개념이 반복 출제되는지의 빈도, (2) 정의 고르기·옳지 않은 것 고르기·명령어 및 옵션 식별·실행 결과 예측·계산형 등 문항 형식의 비율, (3) 오답 선택지를 만드는 방식(용어 혼동, 반대 개념, 자릿수 착오 등)을 뽑아낸 뒤 숫자와 상황을 바꿔 **새로 출제**했다. 실제 시험은 온라인시험으로 50문항이 출제되며 60분 안에 풀어야 하므로, 이 모의고사도 **60분 타이머를 맞추고** 실전처럼 풀어 본 뒤 채점하기를 권한다.
</Callout>

## 과목별 문항 배분표

리눅스마스터 2급 1차 검정은 리눅스 일반·리눅스 운영 및 관리·리눅스 활용 3과목으로 구성된다. 이 시리즈의 목차(02–25편)를 기준으로 세부 항목 수에 비례해 문항을 배분하면 다음과 같다. 2과목(리눅스 운영 및 관리)은 세부 항목이 파일 시스템·셸·프로세스·에디터·소프트웨어 설치·장치 설정 6개로 가장 많아 문항도 가장 많이 배정했다.

| 과목 | 다루는 세부 항목 | 이 회차 문항 수 | 문항 번호 |
|------|-------------------|------------------|-----------|
| 1과목 리눅스 일반 | 리눅스의 이해, 설치, 기본 명령어 | 14문항 | 1–14 |
| 2과목 리눅스 운영 및 관리 | 파일 시스템 명령어, 셸, 프로세스 관리, 에디터, 소프트웨어 설치, 장치 설정 | 27문항 | 15–41 |
| 3과목 리눅스 활용 | X 윈도, 인터넷 활용, 응용분야 | 9문항 | 42–50 |

실제 회차별 과목별 정확한 문항 배분(예: 각 과목 최소 출제 수, 과락 기준 존재 여부)은 KAIT의 회차 공고에서 최종 확인해야 한다. 이 표는 세부 항목 수 비례로 추정한 학습용 배분이다.

## 모의고사 1회 (50문항 · 60분)

<Quiz
  questionNumber={1}
  question="다음 중 GPL(GNU General Public License) 라이선스의 특징으로 가장 알맞은 것은?"
  options={['소스코드를 공개하지 않고 재배포해도 된다', '파생 저작물도 동일한 라이선스로 소스코드를 공개해야 한다', '상업적 이용을 원천적으로 금지한다', '라이브러리로 링크만 하면 소스코드 공개 의무가 없다']}
  correctAnswer={2}
  explanation="GPL은 카피레프트(copyleft) 조항을 두어, GPL 코드를 가져다 만든 파생 프로그램도 GPL로 소스코드를 공개해야 한다. 이는 소스 공개 의무가 아예 없는 BSD·MIT 같은 permissive 라이선스와 가장 크게 구분되는 지점이다."
  optionExplanations={['BSD나 MIT 같은 permissive 라이선스의 특징을 GPL로 착각한 오답이다', 'GPL 카피레프트의 핵심을 정확히 짚은 정답이다', 'GPL도 상업적 이용 자체는 허용한다. 다만 소스코드 공개 조건이 따라붙을 뿐이므로 상업적 이용 금지로 이해하면 오답이다', '라이브러리 동적 링크 시 소스 공개 의무를 면제해 주는 것은 LGPL의 특징이다. GPL과 LGPL을 혼동한 오답이다']}
/>

<Quiz
  questionNumber={2}
  question="다음 중 BSD 라이선스와 GPL 라이선스의 차이를 설명한 것으로 옳지 않은 것은?"
  options={['BSD는 파생물을 소스코드 공개 없이 독점 소프트웨어로 재배포할 수 있다', 'GPL은 파생물도 GPL로 공개해야 하는 카피레프트 조항을 둔다', 'BSD와 GPL 모두 원저작자 표시 의무가 전혀 없다', 'GPL로 배포된 코드를 상업 제품에 포함하면 그 제품도 소스코드를 공개해야 한다']}
  correctAnswer={3}
  explanation="BSD 라이선스는 소스 공개 의무는 없지만 원저작자 저작권 고지 문구는 유지해야 한다는 조건을 둔다. 따라서 두 라이선스 모두 저작자 표시 의무가 전혀 없다는 설명은 사실과 다르며, 이것이 이 문제에서 찾아야 할 틀린 설명이다."
  optionExplanations={['BSD의 permissive한 성격을 정확히 설명한 옳은 설명이다', 'GPL의 카피레프트 조항을 정확히 설명한 옳은 설명이다', 'BSD도 저작권 고지 유지 의무가 있으므로 표시 의무가 전혀 없다는 설명은 틀렸다. 이것이 정답이다', 'GPL 카피레프트가 상업 제품에도 그대로 적용된다는 옳은 설명이다']}
/>

<Quiz
  questionNumber={3}
  question="다음 중 rpm 패키지와 yum 명령을 기본으로 사용하는 배포판 계열로 가장 알맞은 것은?"
  options={['Debian, Ubuntu', 'CentOS, Rocky Linux', 'Fedora만 해당하고 CentOS는 해당하지 않는다', 'Ubuntu, Linux Mint']}
  correctAnswer={2}
  explanation="CentOS와 Rocky Linux는 레드햇(Red Hat) 계열 배포판으로, rpm 패키지 형식과 yum(또는 최신 배포판의 dnf) 명령을 기본 패키지 관리 도구로 사용한다."
  optionExplanations={['Debian과 Ubuntu는 데비안 계열로 deb 패키지와 apt 명령을 사용한다. 계열을 반대로 짝지은 오답이다', 'CentOS와 Rocky Linux 모두 레드햇 계열로 rpm·yum을 사용한다는 옳은 짝짓기이다', 'CentOS도 레드햇 계열에 속해 rpm·yum(또는 dnf)을 사용하므로 해당하지 않는다는 설명은 사실과 다르다', 'Ubuntu와 Linux Mint는 데비안 계열이다. 계열을 반대로 짝지은 오답이다']}
/>

<Quiz
  questionNumber={4}
  question="다음 중 데비안 계열 배포판에서 기본적으로 사용하는 패키지 관리 명령의 조합으로 알맞은 것은?"
  options={['rpm, yum', 'dpkg, apt', 'rpm, dnf', 'pacman, yum']}
  correctAnswer={2}
  explanation="데비안 계열(Debian, Ubuntu, Linux Mint 등)은 deb 패키지를 다루는 저수준 도구 dpkg와, 의존성까지 자동으로 해결해 주는 상위 도구 apt를 기본 패키지 관리 명령으로 사용한다."
  optionExplanations={['rpm과 yum은 레드햇 계열의 패키지 관리 명령이다. 계열을 혼동한 오답이다', 'dpkg와 apt의 조합을 정확히 짝지은 정답이다', 'rpm과 dnf 역시 레드햇 계열의 명령 조합이다. 계열을 혼동한 오답이다', 'pacman은 Arch Linux 계열의 패키지 관리 명령이며 yum과 조합될 수 없다']}
/>

<Quiz
  questionNumber={5}
  question="다음 중 스왑(swap) 영역에 대한 설명으로 알맞은 것은?"
  options={['물리 메모리가 부족할 때 디스크 일부를 가상 메모리처럼 사용하는 영역이다', '루트 파일시스템을 저장하는 필수 파티션으로 마운트 포인트가 반드시 필요하다', '부트로더가 설치되는 영역이다', '사용자 홈 디렉터리 전용 파티션이다']}
  correctAnswer={1}
  explanation="스왑 영역은 물리 메모리(RAM)가 부족해질 때 디스크의 일부를 가상 메모리처럼 빌려 쓰게 해 주는 공간이다. 일반 파일시스템처럼 디렉터리 트리 안에 마운트하지 않고 swapon 명령으로 활성화한다."
  optionExplanations={['스왑의 정의를 정확히 설명한 정답이다', '루트(/) 파티션에 대한 설명을 스왑과 혼동한 오답이다. 스왑은 마운트 포인트 없이 활성화된다', 'GRUB 같은 부트로더가 설치되는 영역과 혼동한 오답이다', '/home 파티션에 대한 설명과 혼동한 오답이다']}
/>

<Quiz
  questionNumber={6}
  question="다음 중 GRUB(부트로더)의 역할로 가장 알맞은 것은?"
  options={['파일시스템을 포맷한다', '전원 인가 후 하드웨어 자체 점검(POST)을 수행한다', '운영체제 커널을 메모리에 적재하고 실행을 넘긴다', '사용자 로그인 인증을 처리한다']}
  correctAnswer={3}
  explanation="GRUB은 대표적인 부트로더로, 전원이 켜진 뒤 BIOS/UEFI의 자체 점검이 끝나면 실행되어 지정된 커널 이미지를 메모리에 적재하고 제어권을 커널에 넘기는 역할을 한다."
  optionExplanations={['파일시스템 포맷은 mkfs 계열 명령의 역할이다. GRUB의 역할과 혼동한 오답이다', 'POST(전원 인가 후 자체 점검)는 BIOS/UEFI의 역할이다. 부팅 단계를 한 단계 앞으로 착각한 오답이다', '커널 적재와 실행 이관을 정확히 설명한 정답이다', '로그인 인증은 login 프로세스나 PAM의 역할이다. 부팅 후반부 단계와 혼동한 오답이다']}
/>

<Quiz
  questionNumber={7}
  question="다음 중 systemd 기반 리눅스에서 런레벨 5(그래픽 다중 사용자 모드)에 대응하는 타깃(target)으로 알맞은 것은?"
  options={['rescue.target', 'multi-user.target', 'graphical.target', 'reboot.target']}
  correctAnswer={3}
  explanation="런레벨과 systemd 타깃은 서로 대응 관계가 있다. 그래픽 다중 사용자 모드인 런레벨 5는 graphical.target에 대응한다."
  optionExplanations={['rescue.target은 단일 사용자 모드인 런레벨 1에 대응한다. 런레벨 번호를 착각한 오답이다', 'multi-user.target은 텍스트 기반 다중 사용자 모드인 런레벨 3에 대응한다. 그래픽 모드와 혼동한 오답이다', '런레벨 5와 graphical.target의 대응을 정확히 짚은 정답이다', 'reboot.target은 재부팅을 의미하는 런레벨 6에 대응한다. 그래픽 모드와 혼동한 오답이다']}
/>

<Quiz
  questionNumber={8}
  question="다음 중 리눅스의 부팅 순서를 올바르게 나열한 것은?"
  options={['커널 적재 → BIOS/UEFI POST → 부트로더 실행 → 로그인', 'BIOS/UEFI POST → 부트로더 실행 → 커널 적재 → init/systemd 실행 → 로그인', '부트로더 실행 → BIOS/UEFI POST → 로그인 → 커널 적재', 'init/systemd 실행 → BIOS/UEFI POST → 커널 적재 → 로그인']}
  correctAnswer={2}
  explanation="전원이 켜지면 BIOS/UEFI가 하드웨어를 점검(POST)하고, 부트로더(GRUB)가 실행되어 커널을 메모리에 적재하며, 커널이 init 또는 systemd라는 최초 프로세스를 띄운 뒤 로그인 화면에 이른다."
  optionExplanations={['커널 적재를 POST보다 앞에 둔 순서 오류이다', '실제 부팅 단계를 순서대로 정확히 나열한 정답이다', '부트로더 실행을 POST보다 앞에 두고 로그인을 커널 적재보다 앞에 둔 이중 순서 오류이다', 'init/systemd 실행을 POST보다 앞에 둔 순서 오류이다']}
/>

<Quiz
  questionNumber={9}
  question="다음 중 현재 로그인 셸에서 사용 중인 셸의 종류를 확인하는 방법으로 가장 알맞은 것은?"
  options={['echo $SHELL 명령으로 확인한다', 'ls -l /bin 명령으로 확인한다', 'pwd 명령으로 확인한다', 'mount 명령으로 확인한다']}
  correctAnswer={1}
  explanation="SHELL 환경변수에는 현재 로그인 셸의 경로가 저장되어 있으므로, echo $SHELL 명령으로 어떤 셸을 쓰고 있는지 바로 확인할 수 있다."
  optionExplanations={['SHELL 환경변수를 출력하는 정확한 방법이다', 'ls -l /bin은 /bin 디렉터리의 파일 목록을 보여줄 뿐 현재 사용 중인 셸을 알려주지 않는다', 'pwd는 현재 작업 디렉터리 경로를 보여줄 뿐 셸 종류와 무관하다', 'mount는 마운트된 파일시스템 목록을 보여줄 뿐 셸 종류와 무관하다']}
/>

<Quiz
  questionNumber={10}
  question="셸에 [ihd@www ~]$ 라는 프롬프트가 표시되어 있다. 이 프롬프트에 담긴 정보에 대한 설명으로 옳지 않은 것은?"
  options={['ihd는 현재 로그인한 사용자명이다', 'www는 호스트명(컴퓨터 이름)이다', '물결표는 현재 작업 디렉터리가 홈 디렉터리임을 뜻한다', '문자열 끝의 달러 기호는 현재 root 권한으로 로그인했음을 뜻한다']}
  correctAnswer={4}
  explanation="프롬프트 끝 기호는 계정 종류를 알려준다. 일반 사용자는 달러 기호($)로, root 사용자는 샵 기호(#)로 끝난다. 따라서 달러 기호가 root 권한을 뜻한다는 설명은 틀렸다."
  optionExplanations={['프롬프트의 사용자명 부분을 정확히 설명한 옳은 설명이다', '프롬프트의 호스트명 부분을 정확히 설명한 옳은 설명이다', '물결표가 홈 디렉터리를 나타낸다는 옳은 설명이다', '달러 기호는 일반 사용자, 샵 기호가 root를 뜻하므로 이 설명은 반대로 되어 있다. 이것이 정답이다']}
/>

<Quiz
  questionNumber={11}
  question="다음 중 절대경로에 대한 설명으로 알맞은 것은?"
  options={['현재 작업 디렉터리를 기준으로 목적지를 표시한 경로이다', '루트 디렉터리에서 시작해 목적지까지 전체 경로를 표시한 것이다', '홈 디렉터리만을 기준으로 표시한 경로이다', '심볼릭 링크로만 구성된 경로이다']}
  correctAnswer={2}
  explanation="절대경로는 파일시스템의 최상위인 루트 디렉터리(슬래시 하나)에서 시작해 목적지까지의 전체 경로를 그대로 적은 것이다. 어디서 명령을 실행하든 가리키는 대상이 같다."
  optionExplanations={['현재 작업 디렉터리를 기준으로 하는 것은 상대경로에 대한 설명이다', '루트에서 시작하는 절대경로의 정의를 정확히 설명한 정답이다', '홈 디렉터리 기준 표기는 물결표를 쓰는 축약 표기일 뿐 절대경로의 정의는 아니다', '절대경로와 심볼릭 링크는 서로 다른 개념이다']}
/>

<Quiz
  questionNumber={12}
  question="다음 중 디렉터리 work와 그 안의 모든 하위 파일·디렉터리를 통째로 복사하는 명령으로 알맞은 것은?"
  options={['cp work backup', 'cp -r work backup', 'mv work backup', 'ln work backup']}
  correctAnswer={2}
  explanation="cp 명령은 기본적으로 디렉터리를 복사하지 못한다. 디렉터리와 그 하위 내용을 통째로 복사하려면 재귀 옵션인 -r(또는 -R)을 붙여야 한다."
  optionExplanations={['-r 옵션이 없으면 디렉터리 복사 시 오류가 발생한다', '재귀 옵션 -r을 정확히 사용한 정답이다', 'mv는 파일을 옮기는 명령으로, 원본이 사라지므로 복사가 아니다', 'ln은 링크를 만드는 명령으로 디렉터리 복사와는 다른 기능이다']}
/>

<Quiz
  questionNumber={13}
  question="다음 중 파일 내용 중 특정 문자열이 포함된 줄만 대소문자 구분 없이 찾는 명령으로 알맞은 것은?"
  options={['grep -i pattern file.txt', 'grep -v pattern file.txt', 'find -name pattern file.txt', 'sort pattern file.txt']}
  correctAnswer={1}
  explanation="grep의 -i 옵션은 대소문자를 구분하지 않고(ignore case) 검색하게 해 준다. 문자열 pattern이 포함된 줄을 대소문자 구분 없이 찾는 명령은 grep -i이다."
  optionExplanations={['대소문자 무시 검색 옵션 -i를 정확히 사용한 정답이다', '-v는 지정한 패턴이 없는 줄만 골라내는 반대(제외) 검색 옵션이다', 'find는 파일 이름을 찾는 명령이지 파일 내용을 검색하는 명령이 아니다', 'sort는 줄 단위 정렬 명령으로 문자열 검색과는 무관하다']}
/>

<Quiz
  questionNumber={14}
  question="다음 중 파일의 마지막 20줄만 출력하는 명령으로 알맞은 것은?"
  options={['head -n 20 file.txt', 'tail -n 20 file.txt', 'cat -n 20 file.txt', 'more -n 20 file.txt']}
  correctAnswer={2}
  explanation="tail 명령은 파일의 뒷부분을 출력하며, -n 옵션으로 출력할 줄 수를 지정한다. tail -n 20은 마지막 20줄을 출력한다."
  optionExplanations={['head는 파일의 앞부분을 출력하는 명령으로 방향이 반대다', '뒤에서부터 지정한 줄 수를 출력하는 tail -n을 정확히 사용한 정답이다', 'cat의 -n 옵션은 줄 번호를 붙여 전체를 출력하는 옵션이지 줄 수를 제한하는 옵션이 아니다', 'more는 화면 단위로 페이지를 넘기며 보는 명령으로 -n 20이 마지막 줄 수 제한을 의미하지 않는다']}
/>

<Quiz
  questionNumber={15}
  question="다음 중 시스템 설정 파일이 저장되는 디렉터리로 가장 알맞은 것은?"
  options={['/etc', '/var', '/usr', '/proc']}
  correctAnswer={1}
  explanation="FHS(파일시스템 계층 표준) 규정에 따르면 /etc는 시스템 전반의 설정 파일이 모이는 디렉터리이다."
  optionExplanations={['시스템 설정 파일 저장 위치를 정확히 짚은 정답이다', '/var는 로그·메일 스풀 등 크기가 변하는 가변 데이터가 저장되는 디렉터리이다', '/usr는 프로그램 실행 파일과 라이브러리 등 공유 자원이 저장되는 디렉터리이다', '/proc는 실행 중인 프로세스 정보를 제공하는 가상 파일시스템이다']}
/>

<Quiz
  questionNumber={16}
  question="다음 중 현재 실행 중인 프로세스 정보를 파일처럼 제공하는 가상 파일시스템 디렉터리로 알맞은 것은?"
  options={['/dev', '/proc', '/tmp', '/opt']}
  correctAnswer={2}
  explanation="/proc는 커널과 실행 중인 프로세스의 정보를 마치 파일처럼 읽을 수 있게 제공하는 가상 파일시스템이다. 실제 디스크에 저장되는 파일이 아니라 커널이 메모리 상태를 실시간으로 보여주는 창구다."
  optionExplanations={['/dev는 하드디스크·터미널 등 장치를 나타내는 장치 파일이 저장되는 디렉터리이다', '프로세스 정보를 제공하는 가상 파일시스템을 정확히 짚은 정답이다', '/tmp는 임시 파일이 저장되는 디렉터리이다', '/opt는 추가로 설치하는 소프트웨어 패키지가 놓이는 디렉터리이다']}
/>

<Quiz
  questionNumber={17}
  question="다음 중 /etc/fstab에 기록되지 않은 파일시스템을 임시로 마운트할 때 사용하는 명령으로 알맞은 것은?"
  options={['mount', 'df', 'du', 'fsck']}
  correctAnswer={1}
  explanation="mount 명령은 장치나 파일시스템을 지정한 디렉터리(마운트 포인트)에 연결한다. /etc/fstab에 등록되지 않은 장치도 장치 경로와 마운트 포인트를 직접 지정해 임시로 마운트할 수 있다."
  optionExplanations={['마운트를 수행하는 명령을 정확히 짚은 정답이다', 'df는 이미 마운트된 파일시스템의 사용량을 확인하는 명령이다', 'du는 디렉터리별 사용 용량을 합산해 보여주는 명령이다', 'fsck는 파일시스템 오류를 검사하고 복구하는 명령이다']}
/>

<Quiz
  questionNumber={18}
  question="다음 중 파일시스템의 사용 가능 용량과 사용 중 용량을 파일시스템 단위로 요약해 보여주는 명령으로 알맞은 것은?"
  options={['du -h', 'df -h', 'mount -a', 'fdisk -l']}
  correctAnswer={2}
  explanation="df 명령은 마운트된 파일시스템 각각의 전체 용량, 사용량, 남은 용량을 요약해 보여준다. -h 옵션을 붙이면 사람이 읽기 쉬운 단위(K, M, G)로 표시된다."
  optionExplanations={['du -h는 디렉터리별 사용 용량을 합산해 보여줄 뿐 파일시스템 전체 요약은 아니다', '파일시스템 단위 용량 요약 명령을 정확히 짚은 정답이다', 'mount -a는 /etc/fstab에 등록된 항목을 모두 마운트하는 명령이지 용량 확인 명령이 아니다', 'fdisk -l은 디스크의 파티션 목록을 보여주는 명령이지 사용량 확인 명령이 아니다']}
/>

<Quiz
  questionNumber={19}
  question="현재 umask 값이 0022일 때, touch 명령으로 새로 만든 일반 파일의 권한을 8진수로 구하면?"
  options={['644', '755', '622', '600']}
  correctAnswer={1}
  explanation="일반 파일이 가질 수 있는 기본 최대 권한은 666(rw-rw-rw-)이다. umask 값은 이 최대 권한에서 자릿수별로 빼서 실제 생성 권한을 정하는 역할을 한다. umask 022는 그룹과 기타 사용자 자리에서 쓰기 권한(2)을 각각 빼겠다는 뜻이므로, 666에서 022를 자릿수별로 빼면 644가 된다."
  optionExplanations={['666에서 umask 022를 자릿수별로 뺀 결과를 정확히 계산한 정답이다', '디렉터리의 기본 최대 권한 777에서 022를 뺀 값(755)과 혼동한 오답이다. 이 문제는 디렉터리가 아니라 일반 파일을 묻고 있다', '그룹 자리(둘째 자리)만 뺀 값으로, 기타 사용자 자리도 함께 빼야 한다는 점을 놓친 오답이다', '자릿수를 과도하게 뺀 계산 오류로 나온 값이다']}
/>

<Quiz
  questionNumber={20}
  question="디렉터리 project의 권한을 심볼릭 표기로 나타냈더니 drwxr-sr-x였다. 이 디렉터리에 설정된 특수 권한과 8진수 표기로 알맞은 것은?"
  options={['Set-UID, 4755', 'Set-GID, 2755', 'Sticky-Bit, 1755', 'Set-GID, 2775']}
  correctAnswer={2}
  explanation="심볼릭 표기 drwxr-sr-x를 자리별로 보면 소유자는 rwx, 그룹은 r-s, 기타는 r-x이다. 그룹 실행 자리에 s가 나타난 것은 그룹 실행 권한(x)이 있는 상태에서 Set-GID가 함께 설정되었다는 뜻이다. 기본 권한 부분만 8진수로 바꾸면 rwx(7) r-x(5) r-x(5)이므로 755이고, 여기에 Set-GID를 뜻하는 2000자리를 더하면 2755가 된다."
  optionExplanations={['Set-UID는 소유자 실행 자리(첫 번째 x 자리)에 s가 나타난다. 이 문제는 그룹 자리에 s가 있으므로 Set-UID가 아니다', '그룹 실행 자리의 s를 Set-GID로 정확히 읽고 8진수 2755까지 바르게 계산한 정답이다', 'Sticky-Bit은 기타 사용자 실행 자리에 t(또는 T)로 표시된다. 그룹 자리의 s와는 다른 위치이므로 오답이다', '기본 권한 부분을 rwxrwxr-x(775)로 잘못 읽은 오답이다. 그룹 쓰기 자리에는 w가 없으므로 775가 아니라 755다']}
/>

<Quiz
  questionNumber={21}
  question="다음 중 /usr/bin/passwd처럼 실행 파일에 Set-UID가 설정되어 있을 때 나타나는 동작으로 알맞은 것은?"
  options={['실행한 사용자 권한이 아니라 파일 소유자 권한으로 실행된다', '그룹 구성원만 실행할 수 있게 제한된다', '디렉터리 안의 파일을 소유자만 삭제할 수 있게 된다', '파일을 실행할 때마다 새 그룹이 자동 생성된다']}
  correctAnswer={1}
  explanation="Set-UID가 설정된 실행 파일은 누가 실행하든 실행 중에는 그 파일의 소유자 권한으로 동작한다. passwd 명령은 소유자가 root이므로, 일반 사용자가 실행해도 실행 중에는 root 권한으로 /etc/shadow 같은 파일을 수정할 수 있다."
  optionExplanations={['Set-UID의 정의를 정확히 설명한 정답이다', '그룹 구성원 제한은 파일 자체의 그룹 권한 설정과 관련된 이야기이지 Set-UID의 동작이 아니다', '소유자만 삭제 가능하게 만드는 것은 Sticky-Bit의 동작이다', 'Set-UID는 그룹 생성과 아무 관련이 없다']}
/>

<Quiz
  questionNumber={22}
  question="다음 /etc/passwd 항목에서 두 번째 필드 x가 의미하는 바로 알맞은 것은?"
  options={['실제 암호 해시가 /etc/shadow에 저장되어 있음을 나타내는 표시이다', '사용자의 UID가 x임을 뜻한다', '로그인이 차단된 계정임을 뜻한다', '홈 디렉터리 경로가 암호화되어 있음을 뜻한다']}
  correctAnswer={1}
  explanation="/etc/passwd의 두 번째 필드는 원래 암호 해시가 들어가던 자리였지만, 보안을 위해 지금은 x라는 표시만 남기고 실제 암호 해시는 root만 읽을 수 있는 /etc/shadow 파일에 따로 저장한다."
  optionExplanations={['/etc/passwd 두 번째 필드의 실제 의미를 정확히 설명한 정답이다', 'UID는 세 번째 필드에 숫자로 들어간다. 필드 순서를 혼동한 오답이다', 'x 표시만으로는 로그인 차단 여부를 알 수 없다. 로그인 차단은 일곱 번째 필드(로그인 셸)가 /sbin/nologin 등으로 설정된 경우에 해당한다', '홈 디렉터리 경로는 여섯 번째 필드에 평문 경로로 그대로 기록된다']}
/>

<Quiz
  questionNumber={23}
  question="다음 중 su와 sudo의 차이를 설명한 것으로 알맞은 것은?"
  options={['su는 대상 계정의 암호를, sudo는 자기 자신의 암호를 입력해 권한을 얻는다', 'su와 sudo는 완전히 동일한 방식으로 동작한다', 'sudo는 /etc/sudoers 설정과 무관하게 누구나 사용할 수 있다', 'su는 명령 단위로만 권한을 부여하고 계정 전환은 불가능하다']}
  correctAnswer={1}
  explanation="su는 전환하려는 대상 계정(예: root)의 암호를 요구해 그 계정으로 전환한다. 반면 sudo는 자기 자신의 암호를 입력해 /etc/sudoers에 등록된 범위 안에서 다른 사용자 권한으로 특정 명령을 실행한다."
  optionExplanations={['su와 sudo의 인증 대상 차이를 정확히 설명한 정답이다', '두 명령은 인증 방식과 권한 부여 범위가 서로 다르다', 'sudo는 /etc/sudoers에 등록된 사용자만 사용할 수 있도록 제한된다', 'su는 오히려 계정 자체를 전환하는 기능이다. 명령 단위 권한 부여는 sudo에 가까운 설명이므로 역할이 뒤바뀐 오답이다']}
/>

<Quiz
  questionNumber={24}
  question="다음 중 프로그램과 프로세스의 관계를 설명한 것으로 알맞은 것은?"
  options={['프로그램은 실행 중인 상태이고 프로세스는 디스크에 저장된 파일이다', '프로그램은 디스크에 저장된 실행 파일이고 프로세스는 그 프로그램이 메모리에 적재되어 실행되는 상태이다', '프로그램과 프로세스는 완전히 같은 개념으로 구분되지 않는다', '하나의 프로그램은 동시에 하나의 프로세스로만 실행될 수 있다']}
  correctAnswer={2}
  explanation="프로그램은 디스크에 정적으로 저장된 실행 파일이며, 그 프로그램이 실행되어 메모리에 적재되고 CPU 시간을 할당받는 동적인 상태가 프로세스이다."
  optionExplanations={['프로그램과 프로세스의 정의를 서로 반대로 설명한 오답이다', '정적인 파일과 동적인 실행 상태의 구분을 정확히 설명한 정답이다', '두 개념은 명확히 구분되는 별개의 개념이다', '같은 프로그램도 여러 터미널에서 동시에 실행하면 서로 다른 PID를 가진 여러 프로세스로 존재할 수 있다']}
/>

<Quiz
  questionNumber={25}
  question="다음 중 부모 프로세스가 종료되기 전에 자식 프로세스가 먼저 종료되었지만 부모가 자식의 종료 상태를 회수하지 않아 발생하는 상태로 알맞은 것은?"
  options={['데몬(daemon)', '좀비(zombie) 프로세스', '고아(orphan) 프로세스', '백그라운드 프로세스']}
  correctAnswer={2}
  explanation="자식 프로세스가 먼저 종료되면 커널은 종료 상태 정보를 남겨두고, 부모 프로세스가 이를 회수(wait)할 때까지 그 프로세스는 자원은 반환했지만 프로세스 테이블 항목만 남은 좀비 상태로 남는다."
  optionExplanations={['데몬은 지속적으로 실행되는 서비스 프로세스를 가리키는 말로, 좀비 상태와는 다른 개념이다', '종료 상태가 회수되지 않아 남는 프로세스를 정확히 설명한 정답이다', '고아 프로세스는 반대로 부모가 자식보다 먼저 종료되어 자식이 부모 없이 남은 경우를 가리킨다', '백그라운드 프로세스는 단순히 앤퍼샌드(&)로 실행되어 터미널을 점유하지 않는 프로세스를 가리킬 뿐 좀비 상태와는 무관하다']}
/>

<Quiz
  questionNumber={26}
  question="다음 중 PID 3050 프로세스에 시그널 중 프로세스가 무시하거나 가로챌 수 없는 강제 종료 시그널을 보내는 명령으로 알맞은 것은?"
  options={['kill -1 3050', 'kill -9 3050', 'kill -15 3050', 'kill -2 3050']}
  correctAnswer={2}
  explanation="시그널 번호 9번인 SIGKILL은 커널이 프로세스를 즉시 강제 종료시키는 시그널로, 프로세스가 이를 가로채거나 무시할 수 없다. kill -9는 이 SIGKILL을 보내는 명령이다."
  optionExplanations={['1번은 SIGHUP으로, 설정을 다시 읽어들이거나 접속이 끊겼음을 알리는 시그널이며 프로세스가 가로챌 수 있다', 'SIGKILL(9번)로 강제 종료하는 명령을 정확히 짚은 정답이다', '15번은 SIGTERM으로 kill의 기본값이지만, 프로세스가 이를 받아 정리 작업 후 종료하거나 무시할 수 있다', '2번은 SIGINT로 Ctrl+C에 해당하며 역시 프로세스가 가로챌 수 있는 시그널이다']}
/>

<Quiz
  questionNumber={27}
  question="우선순위(NI) 값이 0인 프로세스 PID 2200의 NI 값을 -5로 낮춰(더 높은 우선순위로) 변경하려고 한다. root 권한으로 실행할 알맞은 명령은?"
  options={['nice -5 2200', 'renice -5 -p 2200', 'renice 5 -p 2200', 'nice -n -5 2200']}
  correctAnswer={2}
  explanation="nice는 새로 시작하는 프로세스에 우선순위를 부여하는 명령이고, 이미 실행 중인 프로세스의 우선순위를 바꾸려면 renice를 써야 한다. renice -5 -p 2200은 PID 2200 프로세스의 NI 값을 -5로 설정한다."
  optionExplanations={['nice는 이미 실행 중인 프로세스가 아니라 새로 시작할 프로세스에 우선순위를 부여하는 명령이므로 PID를 대상으로 쓸 수 없다', '이미 실행 중인 프로세스의 우선순위를 -p 옵션으로 지정해 변경하는 renice 명령을 정확히 사용한 정답이다', '부호를 반대로 입력해 NI 값을 오히려 5로 높이는(우선순위를 낮추는) 명령이 되어 버린 오답이다', 'nice는 새 프로세스 실행에 쓰는 명령이라 이미 실행 중인 PID 2200에는 적용할 수 없다']}
/>

<Quiz
  questionNumber={28}
  question="다음 중 ps 명령 결과에서 STAT 열의 값이 Z로 표시된 프로세스에 대한 설명으로 알맞은 것은?"
  options={['정상적으로 실행 중인 프로세스이다', '일시 정지된 프로세스이다', '종료되었지만 부모 프로세스가 회수하지 않아 남아 있는 좀비 프로세스이다', '백그라운드에서 대기 중인 프로세스이다']}
  correctAnswer={3}
  explanation="ps 명령의 STAT 열에서 Z는 좀비(zombie, defunct) 상태를 나타내며, 프로세스 자체는 실행을 마쳤지만 부모가 종료 상태를 회수하지 않아 프로세스 테이블에 항목만 남아 있는 상태다."
  optionExplanations={['정상 실행 중인 프로세스는 보통 R(running) 또는 S(sleeping)로 표시된다', '일시 정지 상태는 T(stopped)로 표시된다', '좀비 상태를 나타내는 Z를 정확히 설명한 정답이다', '단순 백그라운드 대기는 별도의 STAT 코드가 아니라 R이나 S로 표시되며 &로 실행되었는지는 STAT 열만으로 알 수 없다']}
/>

<Quiz
  questionNumber={29}
  question="다음 중 vi 편집기에서 입력 모드로 전환하는 방법에 대한 설명으로 옳지 않은 것은?"
  options={['i는 커서 위치 앞에서 입력을 시작한다', 'a는 커서 위치 뒤에서 입력을 시작한다', 'o는 현재 줄 아래에 새 줄을 만들어 입력을 시작한다', 'Esc 키를 누르면 명령 모드에서 입력 모드로 전환된다']}
  correctAnswer={4}
  explanation="Esc 키는 입력 모드에서 명령 모드로 되돌아갈 때 쓰는 키다. 명령 모드에서 입력 모드로 들어가려면 i, a, o 같은 입력 전환 명령을 눌러야 하므로, Esc가 입력 모드로 전환시켜 준다는 설명은 순서가 반대다."
  optionExplanations={['i의 동작을 정확히 설명한 옳은 설명이다', 'a의 동작을 정확히 설명한 옳은 설명이다', 'o의 동작을 정확히 설명한 옳은 설명이다', 'Esc는 입력 모드에서 명령 모드로 돌아가는 키다. 방향을 반대로 설명한 이것이 정답이다']}
/>

<Quiz
  questionNumber={30}
  question="다음 중 vi 편집기의 명령 모드에서 현재 커서가 있는 줄을 삭제하는 명령으로 알맞은 것은?"
  options={['yy', 'dd', 'p', 'u']}
  correctAnswer={2}
  explanation="dd는 현재 커서가 있는 줄 전체를 삭제하면서 동시에 버퍼에 담아두는 명령이다."
  optionExplanations={['yy는 현재 줄을 삭제하지 않고 복사(yank)만 하는 명령이다', '현재 줄을 삭제하는 dd를 정확히 짚은 정답이다', 'p는 버퍼에 담긴 내용을 붙여넣는 명령으로 삭제 기능이 아니다', 'u는 방금 한 작업을 취소(undo)하는 명령으로 삭제 기능이 아니다']}
/>

<Quiz
  questionNumber={31}
  question="다음 중 nano 편집기를 저장 여부를 묻는 절차를 거쳐 종료하는 키 조합으로 알맞은 것은?"
  options={['Ctrl+O', 'Ctrl+X', 'Ctrl+K', 'Ctrl+G']}
  correctAnswer={2}
  explanation="nano에서 Ctrl+X는 종료 명령이며, 저장하지 않은 변경 내용이 있으면 저장할지 물어보는 절차로 이어진다."
  optionExplanations={['Ctrl+O는 현재까지 편집한 내용을 파일에 기록(Write Out)하는 저장 명령이지 종료 명령이 아니다', '종료(Exit) 명령 Ctrl+X를 정확히 짚은 정답이다', 'Ctrl+K는 현재 줄을 잘라내는(cut) 편집 명령이다', 'Ctrl+G는 도움말(Get Help) 화면을 여는 명령이다']}
/>

<Quiz
  questionNumber={32}
  question="다음 중 소스코드를 컴파일해 설치하는 3단계 순서로 알맞은 것은?"
  options={['make → configure → make install', 'configure → make → make install', 'make install → configure → make', 'configure → make install → make']}
  correctAnswer={2}
  explanation="소스 설치는 먼저 configure로 시스템 환경을 점검하고 Makefile을 생성한 뒤, make로 Makefile에 따라 컴파일하고, 마지막으로 make install로 결과물을 시스템 경로에 설치하는 순서로 진행된다."
  optionExplanations={['Makefile이 아직 생성되지 않은 상태에서 make를 먼저 실행하는 순서 오류이다', '설치 3단계를 순서대로 정확히 나열한 정답이다', '설치가 컴파일보다 먼저 오는 순서 오류이다', '설치가 컴파일보다 먼저 오는 순서 오류이다']}
/>

<Quiz
  questionNumber={33}
  question="다음 중 데비안 계열에서 의존성 문제를 자동으로 해결하며 패키지를 설치하는 명령으로 알맞은 것은?"
  options={['dpkg -i package.deb', 'apt install package', 'rpm -ivh package.rpm', 'yum install package']}
  correctAnswer={2}
  explanation="apt는 데비안 계열의 상위 패키지 관리 도구로, 저장소에서 패키지를 찾아 의존성까지 자동으로 계산해 설치해 준다."
  optionExplanations={['dpkg는 deb 파일을 직접 설치하는 저수준 도구로 의존성을 스스로 해결하지 못한다', '의존성을 자동으로 해결하는 데비안 계열 명령 apt를 정확히 짚은 정답이다', 'rpm은 레드햇 계열의 저수준 설치 명령으로 계열이 다르다', 'yum도 레드햇 계열의 상위 패키지 관리 명령으로 계열이 다르다']}
/>

<Quiz
  questionNumber={34}
  question="다음 중 리눅스에서 하드디스크 장치 파일이 위치하는 디렉터리로 알맞은 것은?"
  options={['/etc', '/dev', '/proc', '/var']}
  correctAnswer={2}
  explanation="/dev 디렉터리에는 하드디스크, 터미널, USB 저장장치 등 각종 하드웨어 장치를 나타내는 장치 파일이 들어 있다."
  optionExplanations={['/etc는 설정 파일이 저장되는 디렉터리이다', '장치 파일이 저장되는 디렉터리를 정확히 짚은 정답이다', '/proc는 프로세스 정보를 제공하는 가상 파일시스템이다', '/var는 로그 등 가변 데이터가 저장되는 디렉터리이다']}
/>

<Quiz
  questionNumber={35}
  question="다음 중 실행 파일을 찾을 디렉터리 목록을 저장하는 환경변수로 알맞은 것은?"
  options={['HOME', 'PATH', 'SHELL', 'PS1']}
  correctAnswer={2}
  explanation="PATH 환경변수에는 셸이 명령을 실행할 때 실행 파일을 찾아볼 디렉터리 목록이 콜론(:)으로 구분되어 저장되어 있다."
  optionExplanations={['HOME은 현재 사용자의 홈 디렉터리 경로를 저장하는 변수이다', '실행 파일 탐색 경로를 저장하는 PATH를 정확히 짚은 정답이다', 'SHELL은 현재 로그인 셸의 경로를 저장하는 변수이다', 'PS1은 프롬프트의 표시 형식을 정의하는 변수이다']}
/>

<Quiz
  questionNumber={36}
  question="다음 중 로그인 셸을 시작할 때 개별 사용자에게 적용되는 환경설정 파일로 알맞은 것은?"
  options={['/etc/profile', '/etc/bashrc', '~/.bash_profile', '/etc/passwd']}
  correctAnswer={3}
  explanation="~/.bash_profile은 각 사용자의 홈 디렉터리에 있는 개인 설정 파일로, 로그인 셸이 시작될 때 그 사용자에게만 적용되는 환경변수와 시작 프로그램을 설정한다."
  optionExplanations={['/etc/profile은 시스템의 모든 사용자에게 공통으로 적용되는 전역 설정 파일이다', '/etc/bashrc 역시 시스템 전체에 적용되는 공통 설정 파일이다', '개별 사용자 전용 로그인 설정 파일을 정확히 짚은 정답이다', '/etc/passwd는 계정 정보가 담긴 파일로 환경설정 파일이 아니다']}
/>

<Quiz
  questionNumber={37}
  question="다음 중 명령 실행 결과 중 표준에러만 파일 error.log에 저장하고 화면에는 표준출력을 그대로 보이게 하는 방법으로 알맞은 것은?"
  options={['command > error.log', 'command 2> error.log', 'command < error.log', 'command 2>&1 error.log']}
  correctAnswer={2}
  explanation="파일 디스크립터 번호 2번은 표준에러를 가리킨다. 숫자 2 뒤에 재지정 기호를 붙인 표기는 표준에러만 파일로 재지정하고, 표준출력(파일 디스크립터 1번)은 그대로 화면에 남긴다."
  optionExplanations={['부등호만 쓴 재지정은 표준출력을 저장하는 것이지 표준에러를 저장하는 것이 아니다', '표준에러 전용 재지정 기호를 정확히 사용한 정답이다', '부등호 왼쪽으로 향하는 재지정은 표준입력을 파일에서 읽어오는 용도로, 이 문제의 요구와 무관하다', '표준에러를 표준출력이 가리키는 곳으로 합치는 표기법 뒤에 파일명을 그대로 붙이면 의도한 대로 동작하지 않는다']}
/>

<Quiz
  questionNumber={38}
  question="다음 중 ls 명령의 결과에서 파일명에 lin이 포함된 줄만 걸러 개수를 세는 명령 조합으로 알맞은 것은?"
  options={['ls | grep lin | wc -l', 'ls > grep lin > wc -l', 'ls grep lin wc -l', 'ls -l lin | wc']}
  correctAnswer={1}
  explanation="파이프(|)는 앞 명령의 표준출력을 뒤 명령의 표준입력으로 그대로 넘겨준다. ls 결과를 grep lin으로 걸러내고, 그 결과의 줄 수를 wc -l로 세는 조합이 올바르다."
  optionExplanations={['파이프를 정확히 사용해 세 명령을 연결한 정답이다', '부등호(재지정)는 파일로 저장하는 기호이지 다음 명령으로 결과를 넘기는 기호가 아니므로 이렇게 쓰면 의도대로 동작하지 않는다', '명령 사이에 아무 기호 없이 나열하면 grep과 wc가 ls의 인자로 취급되어 의도한 필터링이 이루어지지 않는다', 'ls -l lin은 lin이라는 이름의 파일 하나를 상세히 보여줄 뿐 문자열 포함 검색이 아니다']}
/>

<Quiz
  questionNumber={39}
  question="매주 월요일부터 금요일까지 오후 11시 30분에 백업 스크립트를 실행하는 crontab 항목을 구하면?"
  options={['30 23 * * 1-5', '23 30 * * 1-5', '30 11 * * 1-5', '30 23 1-5 * *']}
  correctAnswer={1}
  explanation="crontab의 다섯 필드는 분, 시, 일, 월, 요일 순서다. 30분에 실행해야 하므로 첫 필드는 30, 오후 11시는 24시간제로 23시이므로 둘째 필드는 23, 일과 월은 매일·매월이므로 각각 별표(*), 요일은 월요일(1)부터 금요일(5)까지이므로 1-5가 된다. 따라서 30 23 * * 1-5가 정답이다."
  optionExplanations={['분과 시, 요일 범위를 순서대로 정확히 계산한 정답이다', '분 필드와 시 필드의 값을 서로 바꿔 써서 23분 30시라는 존재하지 않는 시각이 되어 버린 오답이다', '오후 11시를 24시간제로 바꾸지 않고 오전 11시로 착각한 오답이다', '요일 필드에 들어가야 할 1-5를 일(day-of-month) 필드 자리에 잘못 넣은 오답이다']}
/>

<Quiz
  questionNumber={40}
  question="다음 crontab 항목이 실행되는 시점을 해석한 것으로 알맞은 것은?"
  options={['4분마다 한 번씩 실행된다', '매 4시간마다(0,4,8,12,16,20시) 정각에 실행된다', '매월 4일 0시에 실행된다', '4일마다 정각에 실행된다']}
  correctAnswer={2}
  explanation="crontab 항목은 분 시 일 월 요일 순서이며, 0 */4 * * *는 분 필드가 0(정각), 시 필드가 */4(4시간마다)이고 나머지 필드는 모두 별표이므로 매일 0,4,8,12,16,20시 정각에 실행된다."
  optionExplanations={['분 필드에 있는 0을 시 필드의 간격(4시간)과 혼동한 오답이다', '분 필드 0(정각)과 시 필드 */4(4시간 간격)를 정확히 해석한 정답이다', '시 필드의 간격 표기를 일(day-of-month) 필드로 착각한 오답이다', '시간 간격을 요일 간격으로 착각한 오답이다']}
/>

<Quiz
  questionNumber={41}
  question="다음 중 반복 작업이 아니라 지정한 시각에 한 번만 실행되는 예약 작업을 등록할 때 사용하는 명령으로 알맞은 것은?"
  options={['crontab -e', 'at', 'cron', 'systemctl']}
  correctAnswer={2}
  explanation="at 명령은 지정한 시각에 딱 한 번만 실행되는 일회성 작업을 예약한다. 반복 작업을 등록하는 crontab -e와 구분된다."
  optionExplanations={['crontab -e는 반복 실행되는 예약 작업을 등록하는 명령이다', '일회성 예약 작업 명령 at을 정확히 짚은 정답이다', 'cron은 반복 작업을 관리하는 데몬 이름이지 일회성 예약 명령이 아니다', 'systemctl은 서비스와 유닛을 관리하는 명령으로 일회성 예약 작업과는 무관하다']}
/>

<Quiz
  questionNumber={42}
  question="X 윈도 시스템에서 화면·키보드·마우스 같은 하드웨어를 직접 제어하는 쪽과 그 위에서 실행되는 응용 프로그램 쪽의 역할을 짝지은 것으로 알맞은 것은?"
  options={['하드웨어 제어는 X 클라이언트, 응용 프로그램 실행은 X 서버가 담당한다', '하드웨어 제어는 X 서버, 응용 프로그램 실행은 X 클라이언트가 담당한다', 'X 서버와 X 클라이언트는 항상 같은 컴퓨터에서만 동작해야 한다', 'X 서버는 응용 프로그램의 논리적 실행을, X 클라이언트는 입출력 장치 제어를 담당한다']}
  correctAnswer={2}
  explanation="X 윈도는 이름과 달리 화면·키보드·마우스 같은 로컬 하드웨어를 직접 제어하는 쪽이 X 서버이고, 그 위에서 실행되는 워드프로세서·브라우저 같은 응용 프로그램이 X 클라이언트다. 클라이언트는 네트워크를 통해 원격 컴퓨터에서 실행되고 결과만 서버로 전송될 수도 있다."
  optionExplanations={['서버와 클라이언트의 역할을 반대로 설명한 오답이다', 'X 윈도의 서버·클라이언트 역할을 정확히 설명한 정답이다', 'X 윈도는 네트워크 투명성을 지원해 서버와 클라이언트가 서로 다른 컴퓨터에서 동작할 수 있다', '서버와 클라이언트의 역할을 반대로 설명한 오답이다']}
/>

<Quiz
  questionNumber={43}
  question="다음 중 그래픽 로그인 화면을 제공해 사용자 인증 후 X 세션을 시작해 주는 프로그램으로 알맞은 것은?"
  options={['윈도 매니저', '디스플레이 매니저', 'X 클라이언트', '터미널 에뮬레이터']}
  correctAnswer={2}
  explanation="디스플레이 매니저(gdm, lightdm 등)는 부팅 후 그래픽 로그인 화면을 띄우고, 사용자 인증이 끝나면 X 세션과 데스크톱 환경을 시작해 주는 프로그램이다."
  optionExplanations={['윈도 매니저는 로그인 이후 창의 배치와 꾸밈을 담당할 뿐 로그인 화면 자체와는 무관하다', '그래픽 로그인과 세션 시작을 담당하는 디스플레이 매니저를 정확히 짚은 정답이다', 'X 클라이언트는 X 서버 위에서 실행되는 응용 프로그램 자체를 가리킨다', '터미널 에뮬레이터는 셸 명령창을 제공하는 프로그램으로 로그인 화면과 무관하다']}
/>

<Quiz
  questionNumber={44}
  question="환경변수 DISPLAY 값이 다음과 같을 때에 대한 해석으로 알맞은 것은? (192.168.0.20:0.0)"
  options={['로컬 컴퓨터의 0번 화면에서만 실행하라는 뜻이다', 'IP 192.168.0.20인 컴퓨터의 X 서버 0번 디스플레이, 0번 화면에 출력하라는 뜻이다', 'IP 192.168.0.20으로 원격 접속해 로그인하라는 뜻이다', '해당 IP는 DNS 서버 주소를 가리킨다']}
  correctAnswer={2}
  explanation="DISPLAY 환경변수는 호스트:디스플레이번호.화면번호 형식으로 출력 대상을 지정한다. 192.168.0.20:0.0은 해당 IP를 가진 컴퓨터의 X 서버가 관리하는 0번 디스플레이, 0번 화면으로 그림을 출력하라는 뜻이다."
  optionExplanations={['IP가 명시되어 있으므로 로컬 컴퓨터가 아니라 지정된 원격 컴퓨터의 화면으로 출력된다', 'DISPLAY 값의 형식과 의미를 정확히 해석한 정답이다', 'DISPLAY는 로그인 접속 대상이 아니라 그림을 출력할 X 서버의 위치를 지정하는 변수다', 'DISPLAY 값은 X 서버 위치를 가리킬 뿐 DNS 서버와는 무관하다']}
/>

<Quiz
  questionNumber={45}
  question="IP 주소 192.168.10.130, 서브넷 마스크 255.255.255.192로 설정된 호스트가 속한 네트워크 대역을 구하면?"
  options={['192.168.10.0', '192.168.10.64', '192.168.10.128', '192.168.10.192']}
  correctAnswer={3}
  explanation="서브넷 마스크 255.255.255.192는 마지막 옥텟에서 2비트만 호스트 비트로 남기므로(256을 4등분) 네트워크 크기는 64개씩이다. 마지막 옥텟을 64 단위로 나누면 0–63, 64–127, 128–191, 192–255 네 블록이 되는데, 130은 128–191 블록에 속하므로 네트워크 주소는 192.168.10.128이다."
  optionExplanations={['서브넷 크기를 128 단위로 잘못 계산해 130을 첫 블록으로 넣은 오답이다', '130을 64–127 블록으로 잘못 배정한 오답이다. 130은 128보다 크므로 이 블록에 속하지 않는다', '128–191 블록에 130이 속함을 정확히 계산한 정답이다', '130을 192–255 블록으로 과도하게 올려 배정한 오답이다']}
/>

<Quiz
  questionNumber={46}
  question="다음 중 사설(private) IP 주소 대역으로 알맞은 것은?"
  options={['8.8.8.8', '172.16.0.5', '200.100.50.10', '1.1.1.1']}
  correctAnswer={2}
  explanation="172.16.0.0부터 172.31.255.255까지는 대표적인 사설 IP 대역(172.16.0.0/12) 중 하나이며, 172.16.0.5는 이 범위에 속한다."
  optionExplanations={['8.8.8.8은 공인 DNS 서버로 잘 알려진 공인 IP 주소이다', '172.16.0.0/12 사설 대역에 속하는 값을 정확히 짚은 정답이다', '임의로 만든 공인 IP 형태의 주소이다', '1.1.1.1 역시 잘 알려진 공인 DNS 서버 주소이다']}
/>

<Quiz
  questionNumber={47}
  question="다음 중 상대 호스트까지 패킷이 거쳐 가는 경로(홉)를 순서대로 보여주는 명령으로 알맞은 것은?"
  options={['ping', 'traceroute', 'netstat', 'nslookup']}
  correctAnswer={2}
  explanation="traceroute는 목적지까지 패킷이 거쳐 가는 각 라우터(홉)의 주소와 응답 시간을 순서대로 보여주어 경로를 추적하는 명령이다."
  optionExplanations={['ping은 목적지까지 도달 가능한지와 왕복 시간만 확인할 뿐 경로는 보여주지 않는다', '경로 추적 명령 traceroute를 정확히 짚은 정답이다', 'netstat은 현재 컴퓨터의 소켓 연결 상태를 보여주는 명령으로 경로 추적과는 다르다', 'nslookup은 도메인 이름을 IP 주소로 변환해 조회하는 명령이다']}
/>

<Quiz
  questionNumber={48}
  question="다음 중 SSH 서비스가 기본으로 사용하는 포트 번호로 알맞은 것은?"
  options={['21', '22', '23', '25']}
  correctAnswer={2}
  explanation="SSH(Secure Shell)는 기본적으로 22번 포트를 사용해 암호화된 원격 접속을 제공한다."
  optionExplanations={['21번은 FTP가 사용하는 제어 포트이다', 'SSH의 기본 포트 22번을 정확히 짚은 정답이다', '23번은 암호화되지 않은 원격 접속 프로토콜인 TELNET이 사용하는 포트이다', '25번은 메일 전송 프로토콜 SMTP가 사용하는 포트이다']}
/>

<Quiz
  questionNumber={49}
  question="다음 설명에 해당하는 서비스로 알맞은 것은? (윈도우와 리눅스 사이에서 파일과 프린터를 공유할 때 사용하는 서비스이다)"
  options={['NFS', 'SAMBA', 'FTP', 'DNS']}
  correctAnswer={2}
  explanation="SAMBA는 SMB/CIFS 프로토콜을 구현해 윈도우 시스템과 리눅스 시스템 사이에서 파일·프린터를 공유할 수 있게 해 주는 서비스다."
  optionExplanations={['NFS는 주로 유닉스·리눅스 계열 시스템 사이의 파일 공유에 쓰이는 서비스다', '윈도우와 리눅스 간 파일·프린터 공유 서비스를 정확히 짚은 정답이다', 'FTP는 단순 파일 전송을 위한 프로토콜로 프린터 공유 기능은 없다', 'DNS는 도메인 이름을 IP 주소로 바꿔주는 서비스로 파일 공유와는 무관하다']}
/>

<Quiz
  questionNumber={50}
  question="다음 중 다수의 저가형 컴퓨터를 네트워크로 묶어 고성능 연산 능력을 얻는 클러스터 구성 방식으로 알맞은 것은?"
  options={['고가용성(HA) 클러스터', '부하분산 클러스터', '베어울프(Beowulf) 클러스터', '임베디드 시스템']}
  correctAnswer={3}
  explanation="베어울프 클러스터는 여러 대의 일반 PC급 컴퓨터를 네트워크로 연결해 병렬 계산 능력을 얻는 방식으로, 과학 계산용 슈퍼컴퓨터를 저비용으로 구축할 때 널리 쓰인다."
  optionExplanations={['고가용성 클러스터는 한 서버에 장애가 생겨도 서비스가 끊기지 않도록 이중화하는 목적의 구성이다', '부하분산 클러스터는 여러 서버에 요청을 나누어 처리하는 목적의 구성이다', '저가형 컴퓨터를 묶어 연산 능력을 얻는 방식을 정확히 짚은 정답이다', '임베디드 시스템은 클러스터 구성 방식이 아니라 특정 기능에 특화된 내장형 컴퓨터 시스템을 가리킨다']}
/>

## 채점 후 복습 동선표

50문항 중 정답 개수를 세어 60점(30문항) 이상이면 합격선이다. 틀린 문항이 몰린 영역을 아래에서 찾아 해당 편으로 돌아가 복습한다.

| 틀린 문항 번호대 | 다루는 영역 | 돌아갈 편 |
|-------------------|-------------|-----------|
| 1–4 | 라이선스, 배포판과 패키지 형식 | 02, 03편 |
| 5–8 | 설치·파티션·부팅·런레벨 | 04, 05편 |
| 9–14 | 로그인·셸 기본, 기본 명령어 | 06, 07, 08편 |
| 15–18 | 파일시스템 구조, 마운트 명령어 | 09, 10편 |
| 19–21 | 권한 계산, 특수 권한 | 11편 |
| 22–23 | 사용자·그룹, su와 sudo | 12편 |
| 24–28 | 프로세스 개념, 명령어와 시그널 | 13, 14편 |
| 29–31 | vi 및 그 외 에디터 | 15, 16편 |
| 32–34 | 소프트웨어 설치, 장치 설정 | 17, 18편 |
| 35–38 | 셸 환경변수, 재지정·파이프 | 19, 20편 |
| 39–41 | cron과 at | 21편 |
| 42–44 | X 윈도 | 22편 |
| 45–49 | 네트워크 기초·명령어·서비스 | 23, 24편 |
| 50 | 응용분야 | 25편 |

## 참고 자료

- [KAIT 리눅스마스터 자격안내](https://www.ihd.or.kr/introducesubject1.do)
- [GNU Coreutils 매뉴얼](https://www.gnu.org/software/coreutils/manual/coreutils.html)
- [GNU Bash 매뉴얼](https://www.gnu.org/software/bash/manual/bash.html)
- [Filesystem Hierarchy Standard (FHS)](https://refspecs.linuxfoundation.org/fhs.shtml)
- [Linux man-pages 프로젝트](https://www.kernel.org/doc/man-pages/)
- [X.Org Foundation](https://www.x.org)
