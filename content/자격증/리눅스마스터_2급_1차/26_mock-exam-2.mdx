---
title: '26. 리눅스마스터 2급 1차 예상 모의고사 2회'
description: '기본 명령어와 옵션 식별 문항을 강화한 예상 모의고사 50문항, 60분 풀이 기준'
---

<Callout type="warning">
  이 모의고사는 실제 기출문제를 그대로 옮긴 것이 아니다. `reference/`에 확보된 2급 기출·복원·모의고사 자료를 분석해 (1) 어떤 개념이 반복 출제되는지의 빈도, (2) 정의 고르기·옳지 않은 것 고르기·명령어 및 옵션 식별·실행 결과 예측·계산형 등 문항 형식의 비율, (3) 오답 선택지를 만드는 방식(용어 혼동, 반대 개념, 자릿수 착오, 존재하지 않는 옵션 끼워 넣기 등)을 뽑아낸 뒤 숫자와 상황을 바꿔 **새로 출제**했다. 이번 2회차는 기본 명령어의 정확한 옵션을 식별하는 문항 비중을 특히 높였다. 실제 시험은 온라인시험으로 50문항·60분이므로, **60분 타이머를 맞추고** 실전처럼 풀어 본 뒤 채점하기를 권한다.
</Callout>

## 과목별 문항 배분표

이 회차는 1과목(리눅스 일반)의 기본 명령어 단원과 2과목(리눅스 운영 및 관리)의 명령어 중심 단원(권한, 프로세스 명령, 소프트웨어 설치, 재지정·파이프)에서 옵션 식별형 문항을 1회차보다 더 많이 배치했다. 세부 항목 수가 가장 많은 2과목은 여전히 가장 많은 문항을 배정받는다.

| 과목 | 다루는 세부 항목 | 이 회차 문항 수 | 문항 번호 |
|------|-------------------|------------------|-----------|
| 1과목 리눅스 일반 | 리눅스의 이해, 설치, 기본 명령어(옵션 식별 강화) | 16문항 | 1–16 |
| 2과목 리눅스 운영 및 관리 | 파일 시스템 명령어, 셸, 프로세스 관리, 에디터, 소프트웨어 설치, 장치 설정(명령어 옵션 강화) | 25문항 | 17–41 |
| 3과목 리눅스 활용 | X 윈도, 인터넷 활용, 응용분야 | 9문항 | 42–50 |

실제 회차별 과목별 정확한 문항 배분(예: 각 과목 최소 출제 수, 과락 기준 존재 여부)은 KAIT의 회차 공고에서 최종 확인해야 한다. 이 표는 세부 항목 수 비례로 추정한 학습용 배분이다.

## 모의고사 2회 (50문항 · 60분)

<Quiz
  questionNumber={1}
  question="다음 중 LGPL 라이선스에 대한 설명으로 알맞은 것은?"
  options={['GPL과 동일하게 파생 프로그램 전체의 소스코드 공개를 강제한다', '라이브러리를 동적으로 링크해 사용하는 프로그램에는 소스코드 공개 의무를 면제해 주는 완화된 카피레프트 라이선스이다', '소스코드 공개 의무가 전혀 없는 완전한 permissive 라이선스이다', '상업적 이용을 금지하는 라이선스이다']}
  correctAnswer={2}
  explanation="LGPL(Lesser GPL)은 GPL의 카피레프트를 완화한 라이선스로, 이 라이브러리를 동적으로 링크해서 쓰는 응용 프로그램까지 GPL로 공개하도록 강제하지 않는다."
  optionExplanations={['파생 프로그램 전체의 소스코드 공개를 강제하는 것은 GPL이다. LGPL은 이를 완화한 라이선스이므로 GPL과 혼동한 오답이다', 'LGPL의 핵심 특징인 링크 예외 조항을 정확히 설명한 정답이다', '소스코드 공개 의무가 전혀 없는 것은 BSD·MIT 같은 permissive 라이선스이다. LGPL은 여전히 카피레프트 요소를 일부 갖고 있으므로 오답이다', 'LGPL도 GPL처럼 상업적 이용 자체를 금지하지 않는다']}
/>

<Quiz
  questionNumber={2}
  question="다음 중 MIT 라이선스의 특징으로 알맞은 것은?"
  options={['카피레프트 조항이 있어 파생물도 같은 라이선스로 배포해야 한다', '저작권 고지와 라이선스 문구만 유지하면 소스코드 공개 없이 자유롭게 수정·재배포할 수 있다', '소스코드를 절대 공개할 수 없다', '리눅스 커널의 기본 라이선스이다']}
  correctAnswer={2}
  explanation="MIT 라이선스는 대표적인 permissive 라이선스로, 원저작자 저작권 고지와 라이선스 문구만 유지하면 소스코드 공개 의무 없이 자유롭게 수정하고 독점 소프트웨어로도 재배포할 수 있다."
  optionExplanations={['카피레프트 조항은 GPL의 특징이다. MIT는 카피레프트가 없는 permissive 라이선스이므로 오답이다', 'MIT 라이선스의 permissive한 성격을 정확히 설명한 정답이다', 'MIT는 소스코드 공개 자체를 금지하지 않는다. 공개하든 안 하든 자유롭다는 점이 오히려 특징이므로 오답이다', '리눅스 커널은 GPLv2를 기본 라이선스로 채택하고 있다. MIT와 혼동한 오답이다']}
/>

<Quiz
  questionNumber={3}
  question="다음 중 Fedora에 대한 설명으로 알맞은 것은?"
  options={['데비안 계열의 실험적 배포판이다', '레드햇 계열로 최신 기술을 빠르게 반영하는 커뮤니티 배포판이다', '상용 유료 배포판으로 소스코드가 비공개다', '임베디드 전용 배포판이다']}
  correctAnswer={2}
  explanation="Fedora는 레드햇(Red Hat)이 후원하는 커뮤니티 배포판으로, 신기술을 빠르게 채택해 검증하는 성격을 가지며 이후 안정화된 기술이 RHEL 계열로 흘러 들어간다."
  optionExplanations={['Fedora는 데비안 계열이 아니라 레드햇 계열이다', 'Fedora의 성격을 정확히 설명한 정답이다', 'Fedora는 무료로 배포되며 소스코드도 공개되는 오픈소스 배포판이다', 'Fedora는 서버·데스크톱 범용 배포판으로 임베디드 전용이 아니다']}
/>

<Quiz
  questionNumber={4}
  question="다음 중 서버용으로 안정성과 장기 지원을 중시할 때 상대적으로 더 적합한 선택으로 알맞은 것은?"
  options={['최신 기능 위주의 롤링 릴리스 배포판', '장기 지원(LTS) 버전을 제공하는 배포판', '데스크톱 전용으로만 설계된 배포판', '임베디드 전용 배포판']}
  correctAnswer={2}
  explanation="서버 운영은 장기간 변경 없이 안정적으로 돌아가는 것이 중요하므로, 오랜 기간 보안 패치와 지원을 제공하는 LTS(Long Term Support) 배포판이 상대적으로 더 적합하다."
  optionExplanations={['롤링 릴리스는 최신 기능을 빠르게 반영하는 대신 안정성이 떨어질 수 있어 서버 운영 목적과는 상대적으로 덜 맞는다', '장기 지원 배포판의 장점을 정확히 짚은 정답이다', '데스크톱 전용 설계는 서버 안정성 요구와 직접적인 관련이 없다', '임베디드 전용 배포판은 서버 용도로 설계된 것이 아니다']}
/>

<Quiz
  questionNumber={5}
  question="다음 중 파티션과 마운트 포인트의 관계를 설명한 것으로 알맞은 것은?"
  options={['파티션은 마운트 포인트가 없어도 파일을 저장하고 접근할 수 있다', '마운트 포인트는 파티션(파일시스템)을 디렉터리 트리의 특정 위치에 연결하는 통로이다', '하나의 파티션은 여러 마운트 포인트에 동시에 연결되어야 한다', '마운트 포인트는 반드시 /mnt 디렉터리여야 한다']}
  correctAnswer={2}
  explanation="마운트 포인트는 특정 파티션(파일시스템)을 리눅스의 단일 디렉터리 트리 안의 한 지점에 연결해, 그 디렉터리를 통해 해당 파티션에 접근할 수 있게 해 주는 통로다."
  optionExplanations={['파티션은 마운트해야 디렉터리 트리를 통해 접근할 수 있다. 마운트 없이 접근 가능하다는 설명은 사실과 다르다', '마운트 포인트의 역할을 정확히 설명한 정답이다', '일반적으로 하나의 파티션은 한 시점에 하나의 마운트 포인트에만 연결된다', '마운트 포인트는 /mnt뿐 아니라 원하는 임의의 디렉터리로 지정할 수 있다']}
/>

<Quiz
  questionNumber={6}
  question="다음 중 UEFI 방식 부팅에서 부트로더 관련 파일이 저장되는 위치로 알맞은 것은?"
  options={['/boot/efi', '/etc/efi', '/var/efi', '/home/efi']}
  correctAnswer={1}
  explanation="UEFI 방식으로 부팅하는 시스템에서는 EFI 시스템 파티션이 보통 /boot/efi에 마운트되어 부트로더 관련 파일을 저장한다."
  optionExplanations={['UEFI 부트로더 파일 위치를 정확히 짚은 정답이다', '/etc는 설정 파일 저장 위치로 부트로더 파일과는 무관하다', '/var는 가변 데이터 저장 위치로 부트로더 파일과는 무관하다', '/home은 사용자 홈 디렉터리로 부트로더 파일과는 무관하다']}
/>

<Quiz
  questionNumber={7}
  question="다음 중 시스템을 즉시 재부팅하는 명령으로 알맞은 것은?"
  options={['shutdown -h now', 'reboot', 'halt', 'init 0']}
  correctAnswer={2}
  explanation="reboot 명령은 시스템을 종료했다가 즉시 다시 켜는(재부팅) 동작을 수행한다."
  optionExplanations={['shutdown -h now는 시스템을 즉시 정지(halt)시키는 명령으로 재부팅이 아니다', '재부팅 명령을 정확히 짚은 정답이다', 'halt는 시스템을 정지시키는 명령으로 재부팅이 아니다', 'init 0은 런레벨 0, 즉 시스템 정지 상태로 전환하는 명령으로 재부팅이 아니다']}
/>

<Quiz
  questionNumber={8}
  question="다음 중 로그인 셸의 종류를 확인할 수 있는 파일로 알맞은 것은?"
  options={['/etc/shells', '/etc/hosts', '/etc/fstab', '/etc/group']}
  correctAnswer={1}
  explanation="/etc/shells에는 시스템에서 로그인 셸로 사용할 수 있도록 허용된 셸 경로 목록이 나열되어 있다."
  optionExplanations={['시스템에 등록된 로그인 셸 목록 파일을 정확히 짚은 정답이다', '/etc/hosts는 호스트 이름과 IP 주소를 매핑하는 파일이다', '/etc/fstab은 파일시스템 자동 마운트 정보를 담은 파일이다', '/etc/group은 그룹 정보를 담은 파일이다']}
/>

<Quiz
  questionNumber={9}
  question="다음 중 이미 존재하는 디렉터리 안에 여러 단계의 하위 디렉터리를 한 번에 생성할 때 사용하는 mkdir 옵션으로 알맞은 것은?"
  options={['mkdir -p a/b/c', 'mkdir -r a/b/c', 'mkdir -R a/b/c', 'mkdir -m a/b/c']}
  correctAnswer={1}
  explanation="mkdir의 -p 옵션은 중간 경로에 있는 디렉터리가 없어도 필요한 상위 디렉터리까지 한꺼번에 만들어 준다."
  optionExplanations={['중간 경로를 포함해 한 번에 생성하는 -p 옵션을 정확히 사용한 정답이다', '-r은 rm이나 cp에서 재귀 옵션으로 쓰이는 표기이며 mkdir의 옵션이 아니다', '-R 역시 다른 명령의 재귀 옵션 표기와 혼동한 오답으로, mkdir에서 중간 경로 생성 옵션이 아니다', '-m은 생성할 디렉터리의 권한 모드를 지정하는 옵션일 뿐 중간 경로를 자동으로 만들어 주지 않는다']}
/>

<Quiz
  questionNumber={10}
  question="다음 중 rm 명령으로 디렉터리와 그 내용을 확인 절차 없이 강제로 모두 삭제하는 옵션 조합으로 알맞은 것은?"
  options={['rm -i', 'rm -rf', 'rm -l', 'rm -v']}
  correctAnswer={2}
  explanation="rm -rf는 -r(재귀적으로 하위 내용까지 삭제)과 -f(확인 절차 없이 강제 삭제)를 함께 적용해, 디렉터리와 그 안의 모든 내용을 묻지 않고 삭제한다."
  optionExplanations={['-i는 삭제 전에 매번 확인을 묻는 옵션으로, 오히려 문제의 요구와 반대되는 동작이다', '재귀 삭제(-r)와 강제 삭제(-f)를 함께 사용한 정답이다', '-l은 rm의 표준 옵션이 아니며 링크 개수를 세는 기능도 rm에는 없다', '-v는 삭제 과정을 자세히 화면에 출력해 줄 뿐 확인 절차를 생략하거나 디렉터리를 재귀적으로 지우지는 못한다']}
/>

<Quiz
  questionNumber={11}
  question="다음 중 ls 명령에서 파일 크기를 사람이 읽기 쉬운 단위(K, M, G)로 함께 보여주는 옵션으로 알맞은 것은?"
  options={['ls -a', 'ls -l', 'ls -lh', 'ls -R']}
  correctAnswer={3}
  explanation="ls -lh는 상세 정보를 보여주는 -l 옵션에 사람이 읽기 쉬운 단위로 크기를 표시하는 -h 옵션을 더한 조합이다."
  optionExplanations={['-a는 숨김 파일까지 표시하는 옵션으로 크기 단위 표시와는 무관하다', '-l은 상세 정보를 보여주지만 크기를 바이트 단위 그대로 표시한다', '-l과 -h를 함께 사용해 읽기 쉬운 단위로 표시하는 정답이다', '-R은 하위 디렉터리까지 재귀적으로 나열하는 옵션으로 단위 표시와는 무관하다']}
/>

<Quiz
  questionNumber={12}
  question="다음 중 심볼릭 링크에 대한 설명으로 옳지 않은 것은?"
  options={['원본 파일과 다른 inode 번호를 가진다', '원본 파일이 삭제되면 깨진 링크(dangling link)가 된다', '서로 다른 파일시스템(파티션) 사이에도 만들 수 있다', '원본 파일과 동일한 inode 번호를 공유해 하드 링크와 동일하게 동작한다']}
  correctAnswer={4}
  explanation="심볼릭 링크는 원본과는 별개의 새 inode를 가지며, 그 안에 원본의 경로 문자열만 담아 두는 방식으로 동작한다. 원본과 동일한 inode를 공유하는 것은 하드 링크의 특징이므로, 이 설명은 두 링크 방식을 뒤바꾼 틀린 설명이다."
  optionExplanations={['심볼릭 링크가 자신만의 별도 inode를 가진다는 옳은 설명이다', '원본이 사라지면 경로만 남아 깨진 링크가 된다는 옳은 설명이다', '심볼릭 링크는 경로 문자열만 담으므로 파일시스템 경계를 넘어서도 만들 수 있다는 옳은 설명이다', 'inode를 공유하는 것은 하드 링크의 특징이며 심볼릭 링크는 별도 inode를 가진다. 두 방식을 뒤바꾼 이 설명이 정답이다']}
/>

<Quiz
  questionNumber={13}
  question="다음 중 find 명령으로 현재 디렉터리 이하에서 확장자가 log인 파일만 찾는 명령으로 알맞은 것은?"
  options={['find . -name *.log', 'find . -grep *.log', 'grep . -name *.log', 'find . -type f log']}
  correctAnswer={1}
  explanation="find는 -name 옵션 뒤에 파일명 패턴을 지정해 이름 조건으로 검색한다. 현재 디렉터리(.) 이하에서 이름이 .log로 끝나는 파일을 찾으려면 find . -name *.log 형태로 쓴다."
  optionExplanations={['이름 패턴 검색 옵션 -name을 정확히 사용한 정답이다', 'find에는 -grep이라는 옵션이 없다. grep 명령과 혼동한 오답이다', 'grep은 파일 내용을 검색하는 명령이지 파일 이름을 검색하는 명령이 아니며, -name 옵션도 갖고 있지 않다', '-type f 뒤에 패턴만 바로 붙이는 문법은 올바르지 않다. 이름 조건에는 -name 옵션이 필요하다']}
/>

<Quiz
  questionNumber={14}
  question="다음 중 두 파일의 내용 차이를 줄 단위로 비교해 보여주는 명령으로 알맞은 것은?"
  options={['diff a.txt b.txt', 'sort a.txt b.txt', 'wc a.txt b.txt', 'cat a.txt b.txt']}
  correctAnswer={1}
  explanation="diff 명령은 두 파일을 줄 단위로 비교해 서로 다른 부분을 표시해 준다."
  optionExplanations={['두 파일의 차이를 비교하는 diff를 정확히 짚은 정답이다', 'sort는 파일 내용을 정렬해 출력하는 명령으로 두 파일을 비교하지 않는다', 'wc는 줄·단어·문자 수를 세는 명령으로 내용 비교 기능이 없다', 'cat은 파일 내용을 그대로 이어 붙여 출력할 뿐 차이를 비교해 주지 않는다']}
/>

<Quiz
  questionNumber={15}
  question="다음 중 파일 내용을 화면 크기 단위로 나눠 보여주며 스페이스바로 다음 페이지, b 키로 이전 페이지 이동이 가능한 명령으로 알맞은 것은?"
  options={['cat', 'head', 'less', 'tail']}
  correctAnswer={3}
  explanation="less는 파일 내용을 화면 단위로 나눠 보여주면서 스페이스바로 다음 페이지, b 키로 이전 페이지로 이동하는 등 자유로운 페이지 탐색을 지원하는 명령이다."
  optionExplanations={['cat은 파일 전체를 한 번에 출력할 뿐 페이지 단위 이동 기능이 없다', 'head는 파일의 앞부분 일부만 출력할 뿐 페이지 이동 기능이 없다', '자유로운 페이지 이동을 지원하는 less를 정확히 짚은 정답이다', 'tail은 파일의 뒷부분 일부만 출력할 뿐 페이지 이동 기능이 없다']}
/>

<Quiz
  questionNumber={16}
  question="다음 중 명령어 wc의 옵션에 대한 설명으로 옳지 않은 것은?"
  options={['wc -l은 줄 수를 센다', 'wc -w는 단어 수를 센다', 'wc -c는 바이트 수를 센다', 'wc -a는 파일의 모든 숨김 정보를 센다']}
  correctAnswer={4}
  explanation="wc에는 -a라는 옵션이 존재하지 않는다. wc가 지원하는 대표 옵션은 줄 수(-l), 단어 수(-w), 바이트 수(-c) 정도이며, 존재하지 않는 옵션에 대한 이 설명이 틀린 설명이다."
  optionExplanations={['-l 옵션이 줄 수를 센다는 옳은 설명이다', '-w 옵션이 단어 수를 센다는 옳은 설명이다', '-c 옵션이 바이트 수를 센다는 옳은 설명이다', 'wc에 존재하지 않는 -a 옵션을 마치 있는 것처럼 설명한 것이 정답이다']}
/>

<Quiz
  questionNumber={17}
  question="다음 중 /var 디렉터리에 대한 설명으로 알맞은 것은?"
  options={['커널 소스와 헤더 파일이 저장된다', '로그, 메일 스풀 등 시스템 운영 중 크기가 변하는 가변 데이터가 저장된다', '사용자 홈 디렉터리가 저장된다', '부트로더 파일이 저장된다']}
  correctAnswer={2}
  explanation="/var는 variable의 줄임말로, 로그 파일이나 메일 스풀처럼 시스템이 운영되는 동안 계속 크기가 변하는 데이터가 저장되는 디렉터리다."
  optionExplanations={['커널 소스와 헤더는 보통 /usr/src 계열에 저장된다', '가변 데이터 저장 디렉터리로서 /var의 역할을 정확히 짚은 정답이다', '사용자 홈 디렉터리는 /home에 저장된다', '부트로더 파일은 /boot에 저장된다']}
/>

<Quiz
  questionNumber={18}
  question="다음 중 마운트되어 있는 파일시스템을 안전하게 분리(해제)하는 명령으로 알맞은 것은?"
  options={['mount', 'umount', 'df', 'du']}
  correctAnswer={2}
  explanation="umount 명령은 마운트되어 있는 파일시스템을 디렉터리 트리에서 안전하게 분리한다."
  optionExplanations={['mount는 파일시스템을 연결(장착)하는 명령으로 분리와는 반대 기능이다', '마운트 해제를 담당하는 umount를 정확히 짚은 정답이다', 'df는 사용량을 확인하는 명령으로 마운트 해제 기능이 없다', 'du는 디렉터리 용량을 합산하는 명령으로 마운트 해제 기능이 없다']}
/>

<Quiz
  questionNumber={19}
  question="다음 중 파일시스템 오류를 검사하고 복구하는 명령으로 알맞은 것은?"
  options={['fsck', 'mkfs', 'mount', 'blkid']}
  correctAnswer={1}
  explanation="fsck(file system check)는 파일시스템의 오류를 검사하고 가능하면 복구해 주는 명령이다."
  optionExplanations={['파일시스템 검사·복구 명령 fsck를 정확히 짚은 정답이다', 'mkfs는 새 파일시스템을 생성(포맷)하는 명령으로 검사·복구 기능이 아니다', 'mount는 파일시스템을 연결하는 명령으로 검사·복구 기능이 없다', 'blkid는 장치의 UUID와 라벨 등을 확인하는 명령으로 오류 복구 기능이 없다']}
/>

<Quiz
  questionNumber={20}
  question="umask 값이 0077인 사용자가 mkdir로 새 디렉터리를 만들면 생성되는 권한을 8진수로 구하면?"
  options={['700', '755', '070', '777']}
  correctAnswer={1}
  explanation="디렉터리가 가질 수 있는 기본 최대 권한은 777(rwxrwxrwx)이다. umask 077은 그룹과 기타 사용자 자리에서 각각 7(rwx 전체)을 빼겠다는 뜻이므로, 777에서 077을 자릿수별로 빼면 700이 된다."
  optionExplanations={['777에서 077을 자릿수별로 정확히 뺀 정답이다', '흔히 보는 umask 022일 때의 결과값(755)과 혼동한 오답이다. 이 문제의 umask는 077이다', '자릿수 위치를 뒤바꿔 계산한 오답이다', 'umask를 전혀 적용하지 않은 기본 최대 권한 값을 그대로 고른 오답이다']}
/>

<Quiz
  questionNumber={21}
  question="다음 명령을 순서대로 실행했을 때(chmod 644 file.txt, chmod o-r file.txt, chmod u+x file.txt) file.txt의 최종 권한을 심볼릭 표기로 구하면?"
  options={['rwxr-----', 'rw-r-----', 'rwxr--r--', 'rw-r--r--']}
  correctAnswer={1}
  explanation="chmod 644 file.txt로 rw-r--r--이 된다. 이어서 chmod o-r file.txt로 기타 사용자의 읽기 권한을 없애면 rw-r-----이 된다. 마지막으로 chmod u+x file.txt로 소유자에게 실행 권한을 더하면 rwxr-----이 된다."
  optionExplanations={['세 명령을 순서대로 모두 반영해 정확히 계산한 정답이다', '마지막 단계인 u+x(소유자 실행 권한 추가)를 반영하지 않은 오답이다', '두 번째 단계인 o-r(기타 사용자 읽기 권한 제거)을 반영하지 않은 오답이다', '어떤 chmod도 반영하지 않은 최초 644 상태 그대로를 고른 오답이다']}
/>

<Quiz
  questionNumber={22}
  question="다음 중 디렉터리에 실행(x) 권한이 없을 때 나타나는 현상으로 알맞은 것은?"
  options={['디렉터리 목록을 볼 수는 있지만 그 안으로 들어가거나 파일에 접근할 수 없다', '디렉터리를 삭제할 수 없게 될 뿐 다른 기능은 정상이다', '아무 영향이 없다', '디렉터리 안의 파일을 읽기만 가능하고 쓰기는 무관하다']}
  correctAnswer={1}
  explanation="디렉터리의 실행(x) 권한은 그 디렉터리 안으로 들어가거나(cd) 그 안의 파일에 실제로 접근하기 위해 반드시 필요하다. x 권한이 없으면 ls로 목록을 볼 수는 있어도 그 안의 파일을 열거나 그 디렉터리로 들어갈 수 없다."
  optionExplanations={['디렉터리 실행 권한의 의미를 정확히 설명한 정답이다', '삭제 가능 여부는 상위 디렉터리의 쓰기 권한과 관련이 있으며, 이 디렉터리 자체의 실행 권한과는 다른 이야기다', '실행 권한이 없으면 실질적인 접근이 크게 제한되므로 영향이 없다는 설명은 사실과 다르다', '실행 권한 부재는 읽기·쓰기 여부와 무관하게 접근 자체를 막는다는 점에서 이 설명은 범위를 좁게 잘못 짚었다']}
/>

<Quiz
  questionNumber={23}
  question="다음 중 사용자 계정을 삭제하면서 홈 디렉터리까지 함께 제거하는 명령으로 알맞은 것은?"
  options={['userdel user1', 'userdel -r user1', 'usermod -d user1', 'groupdel user1']}
  correctAnswer={2}
  explanation="userdel -r은 계정을 삭제하면서 그 사용자의 홈 디렉터리와 메일 스풀까지 함께 제거한다."
  optionExplanations={['userdel만 사용하면 계정 정보만 삭제되고 홈 디렉터리는 그대로 남는다', '홈 디렉터리까지 함께 제거하는 -r 옵션을 정확히 사용한 정답이다', 'usermod -d는 홈 디렉터리 경로를 변경하는 옵션이지 삭제 명령이 아니다', 'groupdel은 그룹을 삭제하는 명령으로 사용자 계정 삭제와는 다른 명령이다']}
/>

<Quiz
  questionNumber={24}
  question="다음 중 백그라운드에서 실행되며 사용자 요청을 기다리는 지속 실행 프로세스로 알맞은 것은?"
  options={['좀비 프로세스', '데몬(daemon)', '고아 프로세스', '포그라운드 프로세스']}
  correctAnswer={2}
  explanation="데몬은 사용자와 직접 상호작용하지 않고 백그라운드에서 계속 실행되며 특정 서비스 요청을 기다렸다가 처리하는 프로세스를 가리킨다."
  optionExplanations={['좀비 프로세스는 이미 종료되었지만 회수되지 않은 상태로, 지속 실행 서비스와는 다른 개념이다', '지속 실행되는 백그라운드 서비스 프로세스를 정확히 짚은 정답이다', '고아 프로세스는 부모가 먼저 종료되어 남은 프로세스를 가리키는 말로 데몬의 정의와는 다르다', '포그라운드 프로세스는 터미널을 점유하며 실행되는 프로세스로 데몬과는 반대 성격이다']}
/>

<Quiz
  questionNumber={25}
  question="다음 중 & 없이 실행 중이던 포그라운드 명령을 정지(stopped) 상태로 만들고 다시 프롬프트로 돌아오는 키 조합으로 알맞은 것은?"
  options={['Ctrl+C', 'Ctrl+Z', 'Ctrl+D', 'Ctrl+L']}
  correctAnswer={2}
  explanation="Ctrl+Z는 실행 중인 포그라운드 프로세스를 완전히 종료시키지 않고 정지(stopped) 상태로 만들어 셸 프롬프트로 돌아오게 한다. 이후 bg나 fg로 다시 실행을 이어갈 수 있다."
  optionExplanations={['Ctrl+C는 프로세스를 정지가 아니라 완전히 종료시키는 인터럽트 신호를 보낸다', '포그라운드 프로세스를 정지 상태로 만드는 Ctrl+Z를 정확히 짚은 정답이다', 'Ctrl+D는 표준입력의 끝(EOF)을 알리는 신호로 정지와는 다른 동작이다', 'Ctrl+L은 터미널 화면을 지우는 단축키로 프로세스 상태와는 무관하다']}
/>

<Quiz
  questionNumber={26}
  question="다음 jobs 명령 결과에서 정지된 2번 작업을 백그라운드에서 계속 실행시키는 명령으로 알맞은 것은?"
  options={['fg %2', 'bg %2', 'kill %2', 'jobs %2']}
  correctAnswer={2}
  explanation="bg %2는 정지된 2번 작업을 백그라운드에서 계속 실행되도록 재개시킨다."
  optionExplanations={['fg %2는 2번 작업을 포그라운드로 불러와 화면을 점유하게 만들 뿐 백그라운드 실행이 아니다', '정지된 작업을 백그라운드에서 재개하는 bg를 정확히 짚은 정답이다', 'kill %2는 2번 작업을 종료시키는 명령으로 재개와는 반대다', 'jobs %2는 표준적인 사용법이 아니며 단순히 작업 목록을 다시 보여줄 뿐 재개 기능이 없다']}
/>

<Quiz
  questionNumber={27}
  question="다음 중 프로세스 이름으로 여러 프로세스를 한꺼번에 종료할 때 사용하는 명령으로 알맞은 것은?"
  options={['kill httpd', 'killall httpd', 'ps httpd', 'top httpd']}
  correctAnswer={2}
  explanation="killall은 프로세스 이름을 인자로 받아, 그 이름으로 실행 중인 모든 프로세스에 시그널을 보내 종료시킨다."
  optionExplanations={['kill은 PID를 인자로 받는 명령이라 프로세스 이름을 직접 줄 수 없다', '프로세스 이름으로 한꺼번에 종료하는 killall을 정확히 짚은 정답이다', 'ps는 프로세스 상태를 조회하는 명령으로 종료 기능이 없다', 'top은 실시간 프로세스 상태를 보여주는 명령으로 종료 기능이 없다']}
/>

<Quiz
  questionNumber={28}
  question="다음 중 vi에서 문서 전체 범위에서 old라는 문자열을 new로 모두 치환하는 명령으로 알맞은 것은?"
  options={['치환 명령 :%s/old/new/g', '치환 명령 :s/old/new/g', '검색 명령 /old/new/g', '전역 명령 :g/old/new']}
  correctAnswer={1}
  explanation="vi의 치환 명령은 :범위s/찾을문자열/바꿀문자열/옵션 형태다. 앞의 범위를 %로 지정하면 문서 전체를 뜻하고, 끝의 g 옵션은 한 줄에 여러 번 나타나도 전부 치환하라는 뜻이다."
  optionExplanations={['범위 지정자 %(문서 전체)를 포함해 치환 명령을 정확히 사용한 정답이다', '범위 지정 없이 :s만 쓰면 현재 커서가 있는 줄에서만 치환이 이루어져 문서 전체를 치환하지 못한다', '슬래시로 시작하는 형태는 검색 명령의 문법이며, 치환에는 콜론(:)으로 시작하는 s 명령을 써야 한다', ':g는 전역 명령의 접두어이지만 이 형태만으로는 치환 동작을 완성하지 못하는 잘못된 문법이다']}
/>

<Quiz
  questionNumber={29}
  question="다음 중 vi에서 저장하지 않고 강제로 종료하는 명령으로 알맞은 것은?"
  options={[':wq', ':q!', ':x', ':w']}
  correctAnswer={2}
  explanation=":q!는 변경 사항을 저장하지 않고 강제로 편집기를 종료하는 명령이다. 느낌표는 경고를 무시하고 강제로 실행하라는 뜻이다."
  optionExplanations={[':wq는 변경 사항을 저장한 뒤 종료하는 명령으로 저장하지 않는다는 조건과 맞지 않는다', '저장하지 않고 강제 종료하는 :q!를 정확히 짚은 정답이다', ':x도 변경 사항이 있으면 저장한 뒤 종료하는 명령이다', ':w는 저장만 할 뿐 종료하지 않는 명령이다']}
/>

<Quiz
  questionNumber={30}
  question="다음 중 GNOME 데스크톱 환경에서 사용되는 그래픽 텍스트 편집기로 알맞은 것은?"
  options={['vi', 'emacs', 'gedit', 'nano']}
  correctAnswer={3}
  explanation="gedit는 GNOME 데스크톱 환경에서 널리 쓰이는 그래픽 기반 텍스트 편집기다."
  optionExplanations={['vi는 터미널(CLI) 환경에서 실행되는 편집기다', 'emacs는 CLI·GUI 모두 지원하지만 GNOME의 기본 그래픽 편집기는 아니다', 'GNOME의 대표적인 그래픽 편집기를 정확히 짚은 정답이다', 'nano도 터미널(CLI) 환경에서 실행되는 편집기다']}
/>

<Quiz
  questionNumber={31}
  question="다음 중 rpm 명령으로 이미 설치된 모든 패키지 목록을 조회하는 옵션으로 알맞은 것은?"
  options={['rpm -qa', 'rpm -ivh', 'rpm -e', 'rpm -qf']}
  correctAnswer={1}
  explanation="rpm -qa는 현재 시스템에 설치된 모든 패키지 목록을 조회(query all)하는 옵션이다."
  optionExplanations={['설치된 전체 패키지 목록 조회 옵션을 정확히 짚은 정답이다', '-ivh는 패키지를 설치(install)하면서 진행 상황을 표시하는 옵션이지 조회 옵션이 아니다', '-e는 패키지를 제거(erase)하는 옵션이다', '-qf는 특정 파일이 어느 패키지에 속하는지 조회하는 옵션으로 전체 목록 조회와는 다르다']}
/>

<Quiz
  questionNumber={32}
  question="다음 중 yum 명령으로 설치된 nmap 패키지를 삭제하는 명령으로 알맞은 것은?"
  options={['yum -e nmap', 'yum delete nmap', 'yum remove nmap', 'yum uninstall nmap']}
  correctAnswer={3}
  explanation="yum에서 패키지를 삭제하는 서브커맨드는 remove이다. yum remove nmap은 nmap 패키지를 시스템에서 제거한다."
  optionExplanations={['-e는 rpm 명령의 제거 옵션이며 yum의 문법이 아니다', 'delete는 yum이 지원하는 서브커맨드 이름이 아니다', 'yum의 표준 삭제 서브커맨드 remove를 정확히 짚은 정답이다', 'uninstall 역시 yum이 지원하는 서브커맨드 이름이 아니다']}
/>

<Quiz
  questionNumber={33}
  question="다음 중 tar로 묶이고 gzip으로 압축된 백업파일 backup.tar.gz의 압축을 풀어 실제로 파일을 추출하는 옵션 조합으로 알맞은 것은?"
  options={['tar -xzvf backup.tar.gz', 'tar -cvf backup.tar.gz', 'tar -tvf backup.tar.gz', 'tar -qzvf backup.tar.gz']}
  correctAnswer={1}
  explanation="x는 추출(extract), z는 gzip 압축 해제, v는 진행 과정 출력, f는 뒤에 파일명이 온다는 뜻이다. 이 네 옵션을 조합한 tar -xzvf backup.tar.gz가 실제로 압축을 풀고 파일을 꺼내는 명령이다."
  optionExplanations={['추출·압축해제·파일명 지정을 모두 포함한 정답이다', 'c는 새 아카이브를 생성(create)하는 옵션으로 추출과는 반대 동작이다', 't는 아카이브 안의 파일 목록만 보여줄 뿐 실제로 추출하지는 않는다', 'q는 tar의 표준 옵션이 아니며, 존재하지 않는 옵션을 끼워 넣은 오답이다']}
/>

<Quiz
  questionNumber={34}
  question="다음 중 USB 저장장치를 리눅스에 연결했을 때 자동으로 장치명을 할당해 관리해 주는 시스템으로 알맞은 것은?"
  options={['udev', 'cron', 'systemd-journald', 'NetworkManager']}
  correctAnswer={1}
  explanation="udev는 커널에서 하드웨어 인식 이벤트를 전달받아 장치 파일을 동적으로 생성하고 관리하는 시스템으로, USB 저장장치를 꽂으면 자동으로 /dev/sdb1 같은 장치명을 할당해 준다."
  optionExplanations={['장치 자동 인식과 이름 할당을 담당하는 udev를 정확히 짚은 정답이다', 'cron은 예약 작업을 실행하는 데몬으로 장치 인식과는 무관하다', 'systemd-journald는 시스템 로그를 수집·관리하는 데몬으로 장치 인식과는 무관하다', 'NetworkManager는 네트워크 연결을 관리하는 데몬으로 저장장치 인식과는 무관하다']}
/>

<Quiz
  questionNumber={35}
  question="다음 중 현재 로그인한 사용자명이 저장된 환경변수로 알맞은 것은?"
  options={['USER', 'PATH', 'SHELL', 'PS1']}
  correctAnswer={1}
  explanation="USER 환경변수에는 현재 로그인한 사용자의 계정명이 저장된다."
  optionExplanations={['현재 사용자명을 저장하는 USER를 정확히 짚은 정답이다', 'PATH는 실행 파일을 찾을 디렉터리 목록을 저장하는 변수다', 'SHELL은 현재 로그인 셸의 경로를 저장하는 변수다', 'PS1은 프롬프트 표시 형식을 정의하는 변수다']}
/>

<Quiz
  questionNumber={36}
  question="다음 중 alias와 export의 차이를 설명한 것으로 알맞은 것은?"
  options={['alias는 명령어의 별명을 만들고, export는 변수를 자식 프로세스에서도 사용할 수 있게 내보낸다', 'alias와 export는 완전히 같은 기능을 한다', 'export는 명령어 별명을 만드는 기능이다', 'alias로 만든 별명은 자동으로 모든 사용자에게 적용된다']}
  correctAnswer={1}
  explanation="alias는 자주 쓰는 명령을 짧은 별명으로 등록하는 기능이고, export는 셸 변수를 환경변수로 승격시켜 그 셸에서 실행되는 자식 프로세스에서도 값을 물려받아 쓸 수 있게 해 준다."
  optionExplanations={['두 명령의 역할 차이를 정확히 설명한 정답이다', '두 명령은 서로 다른 목적을 가진 별개의 기능이다', '명령어 별명을 만드는 기능은 alias이며, 두 명령의 역할을 반대로 설명한 오답이다', 'alias는 기본적으로 그 별명을 설정한 셸 세션에만 적용되며, 모든 사용자에게 자동으로 적용되지 않는다']}
/>

<Quiz
  questionNumber={37}
  question="다음 중 표준출력과 표준에러를 모두 result.log 파일 하나에 저장하는 방법으로 알맞은 것은?"
  options={['command > result.log', 'command 2> result.log', 'command > result.log 2>&1', 'command < result.log']}
  correctAnswer={3}
  explanation="먼저 표준출력(1번)을 result.log로 재지정한 뒤, 표준에러(2번)를 표준출력이 가리키는 곳으로 합치는 표기를 뒤에 붙여야 같은 파일로 합쳐져 두 출력이 모두 한 파일에 기록된다."
  optionExplanations={['표준출력만 파일에 저장될 뿐 표준에러는 화면에 그대로 출력된다', '표준에러만 파일에 저장될 뿐 표준출력은 화면에 그대로 출력된다', '표준출력을 먼저 파일로 돌린 뒤 표준에러를 그 표준출력 자리로 합치는 순서를 정확히 사용한 정답이다', '부등호 왼쪽 방향의 재지정은 표준입력을 파일에서 읽어오는 용도로, 이 문제의 요구와 무관하다']}
/>

<Quiz
  questionNumber={38}
  question="다음 셸 스크립트를 sh script.sh args1 args2로 실행했을 때 $1이 가리키는 값으로 알맞은 것은?"
  options={['script.sh', 'args1', 'args2', 'sh']}
  correctAnswer={2}
  explanation="셸 스크립트에서 $0은 스크립트 자신의 이름, $1은 첫 번째 인자, $2는 두 번째 인자를 가리킨다. sh script.sh args1 args2로 실행했으므로 $1은 첫 번째 인자인 args1이다."
  optionExplanations={['script.sh는 $0이 가리키는 값이다. 번호를 혼동한 오답이다', '첫 번째 위치 매개변수 $1이 가리키는 값을 정확히 짚은 정답이다', 'args2는 $2가 가리키는 값이다. 번호를 혼동한 오답이다', 'sh는 스크립트를 실행하는 데 사용한 인터프리터 이름일 뿐 위치 매개변수가 아니다']}
/>

<Quiz
  questionNumber={39}
  question="다음 중 파일에 실행 권한이 없어 ./script.sh 명령이 Permission denied 오류를 낼 때 해결 방법으로 알맞은 것은?"
  options={['chmod +x script.sh', 'chown +x script.sh', 'mv +x script.sh', 'cp +x script.sh']}
  correctAnswer={1}
  explanation="셸 스크립트를 직접 실행하려면 실행(x) 권한이 있어야 한다. chmod +x script.sh 명령으로 실행 권한을 추가하면 오류가 해결된다."
  optionExplanations={['실행 권한을 부여하는 chmod +x를 정확히 짚은 정답이다', 'chown은 소유자를 변경하는 명령으로 실행 권한 부여와는 관련이 없다', 'mv는 파일을 옮기거나 이름을 바꾸는 명령으로 +x라는 옵션 자체가 존재하지 않는다', 'cp는 파일을 복사하는 명령으로 +x라는 옵션 자체가 존재하지 않는다']}
/>

<Quiz
  questionNumber={40}
  question="다음 crontab 항목이 실행되는 시점을 해석하면?"
  options={['매월 1일 오전 2시 15분', '매주 1요일(월요일) 오전 2시 15분', '매일 2시 1분', '1월 매일 2시 15분']}
  correctAnswer={1}
  explanation="crontab의 다섯 필드는 분 시 일 월 요일 순서다. 15 2 1 * *는 분이 15, 시가 2, 일(day-of-month)이 1, 월과 요일은 매월·매요일이므로, 매월 1일 오전 2시 15분에 실행된다는 뜻이다."
  optionExplanations={['일(day-of-month) 필드 1을 매월 1일로 정확히 해석하고 분·시까지 맞춘 정답이다', '세 번째 필드는 요일이 아니라 일(day-of-month)이다. 필드를 혼동한 오답이다', '분 필드 15와 일 필드 1을 뒤섞어 해석한 오답이다', '일 필드의 1을 월(month) 필드로 착각한 오답이다']}
/>

<Quiz
  questionNumber={41}
  question="다음 중 지정한 한 번의 시각에 실행되도록 작업을 등록한 뒤, 등록된 예약 목록을 확인하는 명령으로 알맞은 것은?"
  options={['crontab -l', 'atq', 'jobs', 'ps -ef']}
  correctAnswer={2}
  explanation="atq는 at 명령으로 등록해 둔 일회성 예약 작업 목록을 확인하는 명령이다."
  optionExplanations={['crontab -l은 반복 예약 작업(cron) 목록을 확인하는 명령으로 at의 일회성 예약과는 다른 시스템이다', 'at으로 등록한 일회성 예약 목록 확인 명령을 정확히 짚은 정답이다', 'jobs는 현재 셸 세션의 포그라운드·백그라운드 작업 목록을 보여줄 뿐 at 예약과는 무관하다', 'ps -ef는 전체 프로세스 목록을 보여줄 뿐 예약 작업 목록이 아니다']}
/>

<Quiz
  questionNumber={42}
  question="다음 중 윈도 매니저(Window Manager)의 역할로 알맞은 것은?"
  options={['디스플레이 하드웨어를 직접 제어하고 그리기 명령을 처리한다', '창의 테두리, 이동, 크기 조절 등 창 배치와 꾸밈을 담당한다', '사용자 로그인 인증 화면을 제공한다', '네트워크를 통해 원격 클라이언트를 인증한다']}
  correctAnswer={2}
  explanation="윈도 매니저는 X 서버 위에서 창의 테두리를 그리고, 창을 옮기거나 크기를 조절하는 등 창 배치와 꾸밈을 담당하는 프로그램이다."
  optionExplanations={['디스플레이 하드웨어 제어는 X 서버의 역할이다', '창 배치와 꾸밈이라는 윈도 매니저의 역할을 정확히 설명한 정답이다', '로그인 인증 화면 제공은 디스플레이 매니저의 역할이다', '원격 클라이언트 인증과 관련된 근거 없는 오답이다']}
/>

<Quiz
  questionNumber={43}
  question="다음 중 KDE 데스크톱 환경이 기반으로 하는 GUI 라이브러리로 알맞은 것은?"
  options={['GTK+', 'Qt', 'FLTK', 'Motif']}
  correctAnswer={2}
  explanation="KDE는 Qt 라이브러리를 기반으로 만들어진 데스크톱 환경이다."
  optionExplanations={['GTK+는 GNOME 계열 데스크톱 환경이 기반으로 하는 라이브러리다', 'KDE가 기반으로 하는 라이브러리 Qt를 정확히 짚은 정답이다', 'FLTK는 옛 유닉스 계열의 경량 GUI 툴킷으로 KDE의 기반이 아니다', 'Motif는 옛 유닉스 계열의 전통적인 GUI 툴킷으로 KDE의 기반이 아니다']}
/>

<Quiz
  questionNumber={44}
  question="IP 주소 10.20.30.40, 서브넷 마스크 255.255.0.0으로 설정된 호스트가 속한 네트워크 주소를 구하면?"
  options={['10.20.0.0', '10.0.0.0', '10.20.30.0', '10.20.30.40']}
  correctAnswer={1}
  explanation="서브넷 마스크 255.255.0.0은 앞 두 옥텟(255.255)만 네트워크 부분이고 뒤 두 옥텟은 호스트 부분이라는 뜻이다. 따라서 호스트 부분인 뒤 두 옥텟을 0으로 바꾼 10.20.0.0이 네트워크 주소가 된다."
  optionExplanations={['앞 두 옥텟만 네트워크로 남기고 나머지를 0으로 바꾼 정확한 계산의 정답이다', '마스크를 255.0.0.0(첫 옥텟만 네트워크)으로 착각한 오답이다', '마스크를 255.255.255.0(세 옥텟이 네트워크)으로 착각한 오답이다', '호스트 주소 자체를 네트워크 주소로 잘못 고른 오답이다']}
/>

<Quiz
  questionNumber={45}
  question="다음 중 게이트웨이(gateway)의 역할로 알맞은 것은?"
  options={['도메인 이름을 IP 주소로 변환한다', '다른 네트워크 대역으로 나가는 패킷을 중계하는 출입구 역할을 한다', '같은 네트워크 안에서 IP 주소를 자동으로 할당한다', '물리적으로 케이블 신호를 증폭한다']}
  correctAnswer={2}
  explanation="게이트웨이는 자신이 속한 네트워크에서 다른 네트워크(주로 인터넷)로 나가는 패킷을 중계해 주는 출입구 역할을 하는 장치(주로 라우터)의 주소다."
  optionExplanations={['도메인 이름을 IP로 바꾸는 것은 DNS의 역할이다', '다른 네트워크로 나가는 출입구 역할을 정확히 설명한 정답이다', '같은 네트워크 안에서 IP 주소를 자동으로 할당하는 것은 DHCP의 역할이다', '케이블 신호를 증폭하는 것은 리피터의 역할이다']}
/>

<Quiz
  questionNumber={46}
  question="다음 중 도메인 이름에 대응하는 IP 주소를 조회하는 명령으로 알맞은 것은?"
  options={['ping', 'nslookup', 'netstat', 'ifconfig']}
  correctAnswer={2}
  explanation="nslookup은 지정한 도메인 이름에 대응하는 IP 주소를 DNS 서버에 물어 확인하는 명령이다."
  optionExplanations={['ping은 목적지까지 도달 가능한지 확인하는 명령으로 이름 해석 자체가 목적이 아니다', '도메인 이름을 IP로 조회하는 nslookup을 정확히 짚은 정답이다', 'netstat은 소켓 연결 상태를 확인하는 명령으로 이름 해석 기능이 없다', 'ifconfig는 네트워크 인터페이스 설정을 확인·변경하는 명령으로 이름 해석 기능이 없다']}
/>

<Quiz
  questionNumber={47}
  question="다음 중 HTTP 서비스가 기본으로 사용하는 포트 번호로 알맞은 것은?"
  options={['21', '25', '80', '443']}
  correctAnswer={3}
  explanation="HTTP는 기본적으로 80번 포트를 사용한다. 암호화된 버전인 HTTPS는 443번 포트를 사용한다."
  optionExplanations={['21번은 FTP가 사용하는 포트다', '25번은 SMTP가 사용하는 포트다', 'HTTP의 기본 포트 80번을 정확히 짚은 정답이다', '443번은 HTTP가 아니라 암호화된 HTTPS가 사용하는 포트다']}
/>

<Quiz
  questionNumber={48}
  question="다음 중 현재 시스템에서 열려 있는 포트와 연결 상태를 확인하는 명령으로 알맞은 것은?"
  options={['netstat -an', 'ping -c 4', 'traceroute', 'dig']}
  correctAnswer={1}
  explanation="netstat -an은 현재 시스템의 소켓(포트) 상태와 연결 정보를 숫자 형태로 모두 보여준다."
  optionExplanations={['포트와 연결 상태를 확인하는 netstat -an을 정확히 짚은 정답이다', 'ping은 목적지까지 도달 가능한지만 확인할 뿐 자신의 포트 상태는 보여주지 않는다', 'traceroute는 목적지까지의 경로를 추적할 뿐 포트 상태와는 무관하다', 'dig는 DNS 조회 명령으로 포트 상태 확인과는 무관하다']}
/>

<Quiz
  questionNumber={49}
  question="다음 중 안드로이드 운영체제와 리눅스의 관계를 설명한 것으로 알맞은 것은?"
  options={['안드로이드는 리눅스 커널과 무관한 독자 커널을 사용한다', '안드로이드는 리눅스 커널을 기반으로 모바일 환경에 맞게 수정한 운영체제이다', '안드로이드는 윈도 커널 위에서 동작한다', '안드로이드는 서버 전용 운영체제이다']}
  correctAnswer={2}
  explanation="안드로이드는 리눅스 커널을 기반으로 모바일 기기에 맞게 수정을 가한 운영체제로, 리눅스가 모바일 분야로 확장된 대표적인 응용분야다."
  optionExplanations={['안드로이드는 리눅스 커널을 기반으로 만들어졌다는 점이 널리 알려진 사실과 다른 오답이다', '안드로이드와 리눅스 커널의 관계를 정확히 설명한 정답이다', '안드로이드는 윈도 계열 커널과는 무관하다', '안드로이드는 모바일 기기를 위한 운영체제이지 서버 전용이 아니다']}
/>

<Quiz
  questionNumber={50}
  question="다음 중 여러 개의 리눅스 컨테이너를 자동으로 배포·확장·관리해 주는 오케스트레이션 도구로 알맞은 것은?"
  options={['Docker', 'Kubernetes', 'Vim', 'GRUB']}
  correctAnswer={2}
  explanation="Kubernetes는 여러 대의 서버에 걸쳐 다수의 컨테이너를 자동으로 배포·확장·관리해 주는 컨테이너 오케스트레이션 도구다."
  optionExplanations={['Docker는 컨테이너를 만들고 실행하는 도구로, 여러 서버에 걸친 자동 관리(오케스트레이션)는 Kubernetes의 역할이다', '컨테이너 오케스트레이션 도구 Kubernetes를 정확히 짚은 정답이다', 'Vim은 텍스트 편집기로 컨테이너 관리와는 무관하다', 'GRUB은 부트로더로 컨테이너 관리와는 무관하다']}
/>

## 채점 후 복습 동선표

50문항 중 정답 개수를 세어 60점(30문항) 이상이면 합격선이다. 특히 이 회차는 명령어와 옵션 식별 문항이 많으므로, 틀린 문항이 몰린 영역의 옵션표를 다시 정리해 보는 것이 효과적이다.

| 틀린 문항 번호대 | 다루는 영역 | 돌아갈 편 |
|-------------------|-------------|-----------|
| 1–4 | 라이선스, 배포판과 패키지 형식 | 02, 03편 |
| 5–8 | 설치·파티션·부팅·런레벨·로그인 셸 | 04, 05, 06편 |
| 9–16 | 기본 명령어 옵션(파일·디렉터리, 텍스트·검색) | 07, 08편 |
| 17–19 | 파일시스템 구조, 마운트 명령어 | 09, 10편 |
| 20–22 | 권한 계산, 디렉터리 실행 권한 | 11편 |
| 23 | 사용자·그룹 관리 명령어 | 12편 |
| 24–27 | 프로세스 개념, 명령어와 시그널 | 13, 14편 |
| 28–30 | vi 및 그 외 에디터 | 15, 16편 |
| 31–34 | 소프트웨어 설치 옵션, 장치 설정 | 17, 18편 |
| 35–39 | 셸 환경변수, 재지정·파이프·스크립트 | 19, 20편 |
| 40–41 | cron과 at | 21편 |
| 42–43 | X 윈도 | 22편 |
| 44–48 | 네트워크 기초·명령어·서비스 | 23, 24편 |
| 49–50 | 응용분야 | 25편 |

## 참고 자료

- [KAIT 리눅스마스터 자격안내](https://www.ihd.or.kr/introducesubject1.do)
- [GNU Coreutils 매뉴얼](https://www.gnu.org/software/coreutils/manual/coreutils.html)
- [GNU Bash 매뉴얼](https://www.gnu.org/software/bash/manual/bash.html)
- [Filesystem Hierarchy Standard (FHS)](https://refspecs.linuxfoundation.org/fhs.shtml)
- [Linux man-pages 프로젝트](https://www.kernel.org/doc/man-pages/)
- [X.Org Foundation](https://www.x.org)
