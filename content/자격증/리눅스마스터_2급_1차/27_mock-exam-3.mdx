---
title: '27. 리눅스마스터 2급 1차 예상 모의고사 3회'
description: '권한 계산, 파일시스템 명령, 프로세스 계산형을 강화한 재구성 모의고사 50문항'
---

<Callout type="warning">
  **이 모의고사는 실제 기출 문제를 그대로 옮긴 것이 아니다.** reference 자료에서 뽑아낸 **출제 개념의 빈도·문항 형식 비율·오답 구성 방식**만 반영해, 숫자와 상황을 모두 새로 바꿔 재구성했다. 실제 리눅스마스터 2급 1차는 온라인시험으로 **50문항을 60분** 안에 풀어야 하므로, 이 모의고사도 실제 시험처럼 **60분** 타이머를 맞춰 놓고 풀어 보길 권장한다. 이번 3회는 특히 **권한 계산(기호 ↔ 8진수 변환, umask), 파일시스템 명령(df·du·fstab), 프로세스 계산(nice·renice·cron)** 문항의 비중을 높였다.
</Callout>

## 과목별 문항 배분표

아래 배분은 KAIT 공식 3과목 구조를 참고해 이 모의고사 안에서 임의로 나눈 것이다. **실제 시험의 회차별 정확한 과목별 문항 수는 공고에 따라 달라질 수 있으므로, 응시 전 반드시 해당 회차의 검정 안내를 확인해야 한다.**

| 과목 | 문항 번호 | 문항 수 | 이번 회차 강조점 |
|------|-----------|---------|-------------------|
| 1과목 리눅스 일반 | 1–11 | 11문항 | 배포판·라이선스·설치·부팅·기본 명령어 개념 |
| 2과목 리눅스 운영 및 관리 | 12–41 | 30문항 | 파일시스템 명령, 권한·umask 계산, 사용자 관리, 프로세스·시그널·nice·cron 계산 |
| 3과목 리눅스 활용 | 42–50 | 9문항 | X 윈도, 네트워크, 응용분야 개념 |
| 합계 | 1–50 | 50문항 | 권장 풀이 시간 60분 |

## 모의고사 3회 (50문항)

<Quiz
  questionNumber={1}
  question="다음 중 리눅스 커널과 배포판의 관계에 대한 설명으로 옳지 않은 것은?"
  options={['커널은 하드웨어를 제어하고 프로세스, 메모리, 파일시스템을 관리하는 운영체제의 핵심이다', '배포판은 커널에 셸, 라이브러리, 패키지 관리자, 응용 프로그램을 묶어 설치 가능한 형태로 만든 것이다', '서로 다른 배포판이라도 같은 계열의 커널 버전을 쓰면 반드시 동일한 패키지 관리자를 사용한다', '하나의 커널 소스 위에 데비안 계열과 레드햇 계열처럼 서로 다른 배포판이 만들어질 수 있다']}
  correctAnswer={3}
  explanation="패키지 관리자는 커널 버전이 아니라 배포판이 독자적으로 선택하는 도구다. 커널 버전이 같아도 데비안 계열은 apt·dpkg를, 레드햇 계열은 dnf(yum)·rpm을 쓸 수 있으므로 3번 설명이 틀렸다."
  optionExplanations={['커널의 정의를 정확히 설명한 옳은 문장이다', '배포판의 정의를 정확히 설명한 옳은 문장이다', '커널 버전과 패키지 관리자를 하나로 묶어 생각하는 착각에서 나온 오답이다', '실제로 우분투와 데비안, 페도라와 CentOS처럼 커널을 공유하면서도 배포판이 갈라진 사례가 많다']}
/>

<Quiz
  questionNumber={2}
  question="다음 중 오픈소스 라이선스에 대한 설명으로 옳지 않은 것은?"
  options={['GPL은 파생 저작물도 동일한 라이선스로 공개하도록 요구하는 카피레프트 라이선스다', 'BSD 라이선스는 소스 코드 공개 의무 없이 재배포 시 저작권 표시만 유지하면 된다', 'LGPL은 라이브러리를 링크해 사용하는 프로그램까지도 반드시 GPL로 공개하도록 강제한다', 'MIT 라이선스는 상업적 이용에 거의 제한을 두지 않는 관대한 라이선스다']}
  correctAnswer={3}
  explanation="LGPL(Lesser GPL)은 라이브러리 자체의 수정본은 카피레프트를 적용하지만, 그 라이브러리를 단순히 링크해서 쓰는 응용 프로그램까지 GPL로 강제 공개하지는 않는다. 이 완화된 조건이 GPL과 LGPL의 핵심 차이이므로 3번이 틀렸다."
  optionExplanations={['카피레프트 개념을 정확히 설명한 옳은 문장이다', 'BSD 라이선스의 관대한 조건을 정확히 설명한 옳은 문장이다', 'GPL과 LGPL의 적용 범위 차이를 반대로 이해한 오답이다', 'MIT 라이선스의 특징을 정확히 설명한 옳은 문장이다']}
/>

<Quiz
  questionNumber={3}
  question="다음 중 배포판 계열과 사용하는 패키지 형식의 연결로 옳지 않은 것은?"
  options={['레드햇 계열 - rpm', '데비안 계열 - deb', '수세(SUSE) 계열 - rpm', '우분투 - rpm']}
  correctAnswer={4}
  explanation="우분투는 데비안 계열 배포판이므로 deb 형식과 apt·dpkg 도구를 사용한다. rpm은 레드햇 계열과 수세 계열이 쓰는 패키지 형식이므로 4번 연결이 틀렸다."
  optionExplanations={['레드햇 계열은 rpm 형식을 쓴다는 옳은 설명이다', '데비안 계열은 deb 형식을 쓴다는 옳은 설명이다', '수세 계열도 rpm 형식을 쓴다는 옳은 설명이다', '우분투를 데비안 계열이 아닌 독립된 rpm 계열로 착각한 오답이다']}
/>

<Quiz
  questionNumber={4}
  question="다음 중 리눅스 설치 시 파티션 설계에 대한 설명으로 옳지 않은 것은?"
  options={['마운트 포인트는 파티션이 파일시스템 트리에 연결되는 위치다', '스왑 파티션은 별도의 파일시스템 형식 없이 스왑 전용 영역으로 사용된다', '부트로더(GRUB)는 반드시 스왑 파티션 내부에 설치해야 한다', '루트(/) 파티션은 모든 설치에서 반드시 존재해야 하는 파티션이다']}
  correctAnswer={3}
  explanation="GRUB 같은 부트로더는 보통 MBR이나 별도의 부트 파티션(/boot 등)에 설치되며, 스왑 파티션은 데이터를 저장하는 파일시스템이 아니라 메모리 대체 공간이라 부트로더를 담을 수 없다. 따라서 3번이 틀린 설명이다."
  optionExplanations={['마운트 포인트의 정의를 정확히 설명한 옳은 문장이다', '스왑 영역의 특성을 정확히 설명한 옳은 문장이다', '스왑 파티션의 역할과 부트로더 설치 위치를 혼동한 오답이다', '루트 파티션이 필수라는 설명은 옳다']}
/>

<Quiz
  questionNumber={5}
  question="다음 중 전원을 켠 뒤 로그인 화면이 나타나기까지의 부팅 단계 순서로 알맞은 것은?"
  options={['부트로더 → BIOS/UEFI → 커널 적재 → init/systemd 시작', 'BIOS/UEFI → 부트로더 → 커널 적재 → init/systemd 시작', '커널 적재 → BIOS/UEFI → 부트로더 → init/systemd 시작', 'BIOS/UEFI → 커널 적재 → 부트로더 → init/systemd 시작']}
  correctAnswer={2}
  explanation="전원이 켜지면 먼저 BIOS/UEFI가 하드웨어를 자체 점검하고 부팅 장치를 찾아 부트로더(GRUB)를 실행한다. 부트로더가 커널을 메모리에 적재하면 커널이 init(또는 systemd) 프로세스를 시작해 로그인 화면까지 이어진다."
  optionExplanations={['BIOS/UEFI가 가장 먼저 실행된다는 사실을 빠뜨린 오답이다', '실제 부팅 순서를 그대로 나열한 옳은 답이다', '커널이 스스로 적재되는 것이 아니라 부트로더가 불러온다는 점을 빠뜨린 오답이다', '부트로더 없이 커널이 먼저 올라온다고 잘못 배열한 오답이다']}
/>

<Quiz
  questionNumber={6}
  question="다음 중 전통적 런레벨과 systemd 타깃의 대응으로 옳지 않은 것은?"
  options={['런레벨 3 - multi-user.target', '런레벨 5 - graphical.target', '런레벨 0 - poweroff.target', '런레벨 6 - rescue.target']}
  correctAnswer={4}
  explanation="런레벨 6은 재부팅을 뜻하므로 systemd에서는 reboot.target에 대응한다. rescue.target은 단일 사용자 복구 모드인 런레벨 1에 대응하므로 4번 연결이 틀렸다."
  optionExplanations={['런레벨 3(다중 사용자, 텍스트)은 multi-user.target이 맞다', '런레벨 5(그래픽 로그인)는 graphical.target이 맞다', '런레벨 0(시스템 종료)은 poweroff.target이 맞다', '재부팅을 뜻하는 런레벨 6을 복구 모드인 rescue.target으로 착각한 오답이다']}
/>

<Quiz
  questionNumber={7}
  question="다음 중 셸에 대한 설명으로 옳지 않은 것은?"
  options={['bash는 GNU 프로젝트가 만든 본 셸(Bourne shell) 계열 셸이다', 'csh는 C 언어 문법과 비슷한 문법을 가진 셸이다', '시스템에 등록된 모든 사용자의 로그인 셸 정보는 오직 /etc/shells 파일에만 기록되어 있다', 'chsh 명령으로 자신의 로그인 셸을 변경할 수 있다']}
  correctAnswer={3}
  explanation="사용자별 로그인 셸은 /etc/passwd 파일의 마지막 필드에 기록된다. /etc/shells는 시스템에서 사용 가능한 셸의 종류를 나열한 목록 파일일 뿐, 사용자별 지정 정보를 담고 있지 않으므로 3번이 틀렸다."
  optionExplanations={['bash가 본 셸 계열의 GNU 구현이라는 설명은 옳다', 'csh의 문법적 특징을 정확히 설명한 옳은 문장이다', '/etc/shells(사용 가능 셸 목록)와 /etc/passwd(사용자별 로그인 셸)의 역할을 혼동한 오답이다', 'chsh로 로그인 셸을 바꿀 수 있다는 설명은 옳다']}
/>

<Quiz
  questionNumber={8}
  question="셸에 [posein@www ~]$ 라는 프롬프트가 표시되어 있다. 이 프롬프트에 대한 해석으로 옳지 않은 것은?"
  options={['posein은 현재 로그인한 사용자명이다', 'www는 호스트명이다', '물결표(~)는 현재 작업 디렉터리가 홈 디렉터리임을 뜻한다', '프롬프트 끝의 달러 기호는 root 사용자로 로그인했음을 뜻한다']}
  correctAnswer={4}
  explanation="프롬프트 끝의 기호는 사용자 권한을 알려 준다. 일반 사용자는 달러 기호로, root 사용자는 샤프(우물 정) 기호로 표시되므로 4번 설명이 틀렸다."
  optionExplanations={['posein이 사용자명 자리라는 해석은 옳다', 'www가 호스트명 자리라는 해석은 옳다', '물결표가 홈 디렉터리를 뜻한다는 해석은 옳다', '일반 사용자 기호와 root 사용자 기호를 반대로 안 오답이다']}
/>

<Quiz
  questionNumber={9}
  question="다음 중 숨김 파일을 포함해 각 파일의 상세 정보를 함께 보여 주는 ls 명령으로 가장 알맞은 것은?"
  options={['ls -al', 'ls -R', 'ls -d', 'ls -h']}
  correctAnswer={1}
  explanation="-a는 점(.)으로 시작하는 숨김 파일까지 표시하고, -l은 권한·소유자·크기·수정 시각 같은 상세 정보를 목록 형태로 보여 준다. 두 옵션을 함께 쓴 ls -al이 조건을 모두 만족한다."
  optionExplanations={['숨김 파일 표시(-a)와 상세 정보(-l)를 모두 만족하는 옳은 답이다', '-R은 하위 디렉터리까지 재귀적으로 나열할 뿐 숨김 파일이나 상세 정보 옵션이 아니다', '-d는 디렉터리 자체 정보만 보여줄 뿐 숨김 파일을 모두 나열하지 않는다', '-h는 파일 크기를 사람이 읽기 쉬운 단위로 바꿀 뿐 숨김 파일이나 상세 정보를 보장하지 않는다']}
/>

<Quiz
  questionNumber={10}
  question="디렉터리 old를 하위 파일까지 통째로 new라는 다른 디렉터리 안으로 복사하려고 한다. 알맞은 명령은?"
  options={['cp -r old new', 'cp old new', 'mv -r old new', 'rm -r old new']}
  correctAnswer={1}
  explanation="디렉터리를 하위 내용까지 복사하려면 재귀 옵션인 -r(또는 -R)이 반드시 필요하다. cp -r old new는 old 디렉터리 전체를 new라는 이름으로 복사한다."
  optionExplanations={['재귀 옵션 -r로 디렉터리 전체를 복사하는 옳은 명령이다', '옵션 없이 cp만 쓰면 디렉터리는 복사할 수 없다는 오류가 발생한다', 'mv는 이동 명령이며 재귀 옵션 없이도 디렉터리 전체를 그대로 옮기므로 -r을 붙이는 것 자체가 잘못된 조합이다', 'rm -r은 삭제 명령이라 복사 목적과 정반대다']}
/>

<Quiz
  questionNumber={11}
  question="다음 중 하드 링크와 심볼릭 링크의 차이에 대한 설명으로 옳지 않은 것은?"
  options={['하드 링크는 원본과 동일한 inode 번호를 공유한다', '심볼릭 링크는 원본의 경로를 저장하는 별도의 작은 파일이다', '하드 링크는 원본 파일을 삭제해도 데이터가 남아 있는 링크로 계속 접근할 수 있다', '심볼릭 링크는 디렉터리를 가리킬 수 없고 오직 일반 파일만 가리킬 수 있다']}
  correctAnswer={4}
  explanation="심볼릭 링크는 디렉터리도 가리킬 수 있다. 오히려 하드 링크가 디렉터리에 대해서는 자유롭게 만들 수 없도록 제한되어 있으므로 4번 설명이 틀렸다."
  optionExplanations={['하드 링크가 inode를 공유한다는 설명은 옳다', '심볼릭 링크가 경로를 담은 별도 파일이라는 설명은 옳다', '하드 링크가 원본 삭제 후에도 데이터를 유지한다는 설명은 옳다', '하드 링크와 심볼릭 링크 중 어느 쪽이 디렉터리 제약을 받는지 반대로 안 오답이다']}
/>

<Quiz
  questionNumber={12}
  question="다음 중 디렉터리와 그 용도의 연결로 옳지 않은 것은?"
  options={['/etc - 시스템 설정 파일', '/var - 로그, 메일 큐 등 자주 변하는 가변 데이터', '/usr - 사용자 개인 홈 디렉터리 전용 공간', '/home - 일반 사용자의 홈 디렉터리']}
  correctAnswer={3}
  explanation="FHS(Filesystem Hierarchy Standard) 기준으로 /usr는 프로그램, 라이브러리, 문서 등 배포판이 설치하는 공유 자원을 담는 공간이며, 사용자 개인 홈 디렉터리는 /home이 맡는다. 따라서 3번 연결이 틀렸다."
  optionExplanations={['/etc가 설정 파일 저장소라는 설명은 옳다', '/var가 가변 데이터를 담는다는 설명은 옳다', '/usr와 /home의 용도를 서로 바꿔 이해한 오답이다', '/home이 사용자 홈 디렉터리라는 설명은 옳다']}
/>

<Quiz
  questionNumber={13}
  question="다음 중 /proc 디렉터리에 대한 설명으로 옳지 않은 것은?"
  options={['실제 디스크에 저장되지 않는 가상 파일시스템이다', '실행 중인 프로세스와 커널 정보를 파일 형태로 보여준다', '/proc/cpuinfo, /proc/meminfo 같은 파일로 하드웨어 정보를 확인할 수 있다', '/proc 아래의 파일들은 모두 일반 파일처럼 디스크 용량을 차지한다']}
  correctAnswer={4}
  explanation="/proc는 커널이 메모리에서 실시간으로 만들어 내는 가상 파일시스템이다. 그 안의 파일들은 디스크에 실제로 저장되는 것이 아니라 조회 시점에 커널이 생성해 보여 주는 정보이므로 디스크 용량을 차지하지 않는다. 따라서 4번이 틀렸다."
  optionExplanations={['/proc가 가상 파일시스템이라는 설명은 옳다', '프로세스·커널 정보를 파일 형태로 노출한다는 설명은 옳다', 'cpuinfo·meminfo로 하드웨어 정보를 볼 수 있다는 설명은 옳다', '가상 파일시스템의 특성을 일반 파일과 혼동한 오답이다']}
/>

<Quiz
  questionNumber={14}
  question="다음 중 ext4와 xfs 파일시스템의 특징 비교로 옳지 않은 것은?"
  options={['ext4는 저널링 파일시스템으로 갑작스러운 전원 차단 후 복구가 비교적 빠르다', 'xfs는 대용량 데이터와 높은 처리량이 필요한 환경에 적합하다', 'xfs는 이미 생성된 파일시스템의 크기를 늘리는 것뿐 아니라 줄이는 것도 자유롭게 지원한다', 'ext4는 resize2fs 명령으로 마운트를 해제한 상태에서 축소할 수 있다']}
  correctAnswer={3}
  explanation="xfs는 온라인으로 크기를 늘리는(grow) 것은 지원하지만, 생성된 파일시스템을 줄이는(shrink) 기능은 공식적으로 지원하지 않는다. 축소가 필요하면 백업 후 다시 만들어야 하므로 3번 설명이 틀렸다."
  optionExplanations={['ext4의 저널링 특성을 정확히 설명한 옳은 문장이다', 'xfs가 대용량·고처리량에 강점이 있다는 설명은 옳다', 'xfs가 축소를 지원하지 않는다는 사실을 반대로 안 오답이다', 'ext4를 언마운트 후 resize2fs로 축소할 수 있다는 설명은 옳다']}
/>

<Quiz
  questionNumber={15}
  question="다음 /etc/fstab 항목에서 마지막 두 필드가 의미하는 바로 알맞은 것은?"
  options={['0은 마운트 옵션 개수, 2는 파일시스템 버전이다', '0은 dump 백업을 사용하지 않음, 2는 부팅 시 fsck 검사 순서다', '0은 읽기 전용 여부, 2는 마운트 재시도 횟수다', '0과 2는 모두 마운트 우선순위를 나타낸다']}
  correctAnswer={2}
  explanation="/etc/fstab의 여섯 번째 필드는 dump 백업 대상 여부(0이면 사용 안 함), 일곱 번째 필드는 부팅 시 fsck 검사 순서를 뜻한다. 보통 루트 파티션은 1, 그 외 파티션은 2를 준다."
  optionExplanations={['dump·fsck 필드를 옵션 개수·버전으로 잘못 해석한 오답이다', 'fstab의 여섯·일곱 번째 필드 의미를 정확히 설명한 옳은 답이다', 'dump·fsck 필드를 읽기 전용·재시도 횟수로 잘못 해석한 오답이다', '두 필드가 서로 다른 역할을 한다는 점을 무시한 오답이다']}
/>

```text
/dev/sdb1  /data  ext4  defaults,noatime  0  2
```

<Quiz
  questionNumber={16}
  question="다음 df -h 결과에서 Use% 열에 들어갈 값으로 가장 알맞은 것은?"
  options={['70%', '75%', '80%', '90%']}
  correctAnswer={3}
  explanation="Use% 는 전체 크기 대비 사용량의 비율이다. 이 표에서 Size는 40G, Used는 32G이므로 32 나누기 40에 100을 곱하면 80%가 된다."
  optionExplanations={['32를 40으로 나눈 값을 70%로 잘못 어림한 오답이다', '32를 40으로 나눈 값을 75%로 잘못 계산한 오답이다', '32÷40×100=80으로 정확히 계산한 옳은 답이다', 'Avail(8G)을 기준으로 90%를 계산해 분모를 혼동한 오답이다']}
/>

```text
Filesystem   Size  Used  Avail  Use%  Mounted on
/dev/sdb1     40G   32G     8G    ?   /data
```

<Quiz
  questionNumber={17}
  question="/var 파티션을 df -h로 확인하면 92% 사용 중인데, du -sh /var/* 로 하위 디렉터리 용량을 모두 더하면 55%에 불과하다. 이런 불일치가 발생하는 가장 흔한 원인으로 알맞은 것은?"
  options={['df는 파일시스템 전체 블록 기준, du는 디렉터리 용량 기준이라 원래 값이 같을 수 없다', '이미 삭제됐지만 어떤 프로세스가 계속 열어 둔(open) 파일이 df에는 반영되지만 du 합산에는 잡히지 않는다', 'du 명령은 파일 압축률을 반영해 항상 실제보다 작게 표시한다', 'df는 파티션의 예약 블록만 표시하고 실제 사용량은 표시하지 않는다']}
  correctAnswer={2}
  explanation="어떤 프로세스가 파일을 열어 둔 상태에서 그 파일이 삭제되면, 디렉터리 목록에서는 사라져 du가 세지 못하지만 실제 디스크 블록은 그 프로세스가 파일을 닫을 때까지 계속 점유된다. df는 실제 디스크 블록 점유량을 보므로 이 차이만큼 du보다 큰 값을 보고한다."
  optionExplanations={['단순 단위 차이로 뭉뚱그린 설명일 뿐, 이렇게 큰 불일치의 실제 원인을 설명하지 못하는 오답이다', '삭제되었지만 열려 있는 파일의 점유 현상을 정확히 짚은 옳은 답이다', 'du는 압축률과 무관하게 실제 블록 사용량을 계산하므로 근거 없는 오답이다', 'df가 예약 블록만 표시한다는 것은 사실이 아닌 오답이다']}
/>

<Quiz
  questionNumber={18}
  question="다음 중 fsck 명령에 대한 설명으로 옳지 않은 것은?"
  options={['파일시스템의 무결성을 검사하고 오류를 복구한다', '일반적으로 마운트되지 않은 상태에서 실행하는 것이 안전하다', '정상적으로 마운트되어 사용 중인 루트 파일시스템도 실행 중인 상태에서 즉시 데이터 손상 없이 전체 검사와 복구를 마칠 수 있다', '비정상 종료 후 부팅 시 자동으로 실행되도록 설정할 수 있다']}
  correctAnswer={3}
  explanation="사용 중인 파일시스템, 특히 마운트된 루트 파일시스템에 fsck를 그대로 실행하면 검사 도중 파일 내용이 바뀌어 오히려 파일시스템이 손상될 위험이 있다. 그래서 루트 파일시스템 점검은 보통 부팅 초기의 언마운트된 상태나 복구 모드에서 이뤄지므로 3번이 틀렸다."
  optionExplanations={['fsck의 기본 기능을 정확히 설명한 옳은 문장이다', '언마운트 상태에서 실행하는 것이 안전하다는 설명은 옳다', '마운트된 상태에서도 안전하게 즉시 복구된다고 한 것은 위험성을 무시한 오답이다', '부팅 시 자동 fsck 설정이 가능하다는 설명은 옳다']}
/>

<Quiz
  questionNumber={19}
  question="심볼릭 권한 표기가 rwxr-x--- 인 파일의 권한을 8진수로 변환한 값으로 알맞은 것은?"
  options={['750', '754', '705', '570']}
  correctAnswer={1}
  explanation="소유자 rwx는 4+2+1=7, 그룹 r-x는 4+0+1=5, 기타 ---는 0이다. 세 자리를 이어 붙이면 750이 된다."
  optionExplanations={['소유자 7, 그룹 5, 기타 0을 정확히 더해 750을 얻은 옳은 답이다', '기타 자리에 없는 실행 권한(1)을 잘못 더해 754로 계산한 오답이다', '그룹의 읽기 권한을 빼고 실행 권한만 남겨 705로 잘못 계산한 오답이다', '소유자와 기타 자리의 숫자를 서로 뒤바꿔 계산한 오답이다']}
/>

```text
-rwxr-x---
```

<Quiz
  questionNumber={20}
  question="8진수 764를 심볼릭 권한 표기로 변환한 것으로 알맞은 것은?"
  options={['-rwxrw-r--', '-rwxr-xr--', '-rw-rwxr--', '-rwxrw--wx']}
  correctAnswer={1}
  explanation="7은 rwx(4+2+1), 6은 rw-(4+2+0), 4는 r--(4+0+0)이다. 소유자·그룹·기타 순서대로 이어 쓰면 -rwxrw-r--가 된다."
  optionExplanations={['7·6·4를 각각 rwx·rw-·r--로 정확히 변환한 옳은 답이다', '그룹 자리의 6(rw-)을 5(r-x)로 잘못 바꿔 쓴 오답이다', '소유자와 그룹의 권한 비트를 서로 뒤섞어 쓴 오답이다', '기타 자리의 4(r--)를 3(-wx)으로 잘못 바꿔 쓴 오답이다']}
/>

<Quiz
  questionNumber={21}
  question="다음과 같이 umask 값이 설정된 상태에서 touch 명령으로 새 파일을 생성하면 부여되는 권한(8진수)으로 알맞은 것은?"
  options={['664', '662', '644', '640']}
  correctAnswer={1}
  explanation="파일의 기본 권한은 666(rw-rw-rw-)이다. umask 0113의 세 자리(소유자 1, 그룹 1, 기타 3)는 각 대상에서 빼야 할 비트를 뜻한다. 소유자 1(--x)은 애초에 파일에 없는 실행 비트라 rw-(6)에 영향이 없다. 그룹도 마찬가지로 1(--x)이 실행 비트만 가리키므로 rw-(6) 그대로 남는다. 기타는 3(-wx) 중 쓰기 비트가 걸려 rw-에서 쓰기를 뺀 r--(4)가 된다. 따라서 최종 권한은 소유자 6, 그룹 6, 기타 4 즉 664다."
  optionExplanations={['umask의 실행 비트 성분이 파일에는 애초에 적용될 게 없다는 점을 반영해 소유자·그룹을 6으로, 기타만 4로 정확히 계산한 옳은 답이다', '그룹 자리에서 실행 비트뿐 아니라 쓰기 비트까지 빠진다고 잘못 계산한 오답이다', '666에서 umask 세 자리 숫자를 단순히 그대로 빼는(666-113 방식) 산술 뺄셈으로 잘못 계산한 오답이다', '기타 자리 권한을 아예 0으로 잘못 계산한 오답이다']}
/>

```text
[ihd@www ~]$ umask
0113
```

<Quiz
  questionNumber={22}
  question="다음과 같이 umask 값이 설정된 상태에서 mkdir 명령으로 새 디렉터리를 생성하면 부여되는 권한(8진수)으로 알맞은 것은?"
  options={['750', '700', '705', '755']}
  correctAnswer={3}
  explanation="디렉터리의 기본 권한은 777(rwxrwxrwx)이다. umask 0072의 세 자리는 소유자 0, 그룹 7, 기타 2다. 소유자는 뺄 비트가 없어 rwx(7) 그대로다. 그룹은 7(rwx) 전체가 빠져 ---(0)이 된다. 기타는 2(-w-) 중 쓰기 비트만 빠져 rwx에서 쓰기를 뺀 r-x(5)가 된다. 따라서 최종 권한은 소유자 7, 그룹 0, 기타 5 즉 705다."
  optionExplanations={['그룹 자리를 0이 아니라 5로 잘못 계산한 오답이다', '기타 자리에서 실행 비트까지 함께 빠졌다고 잘못 계산한 오답이다', '소유자 7, 그룹 0, 기타 5를 정확히 계산한 옳은 답이다', '그룹과 기타 자리를 서로 바꿔 계산한 오답이다']}
/>

```text
[ihd@www ~]$ umask
0072
```

<Quiz
  questionNumber={23}
  question="다음 명령을 차례로 실행한 뒤 파일 a와 디렉터리 b를 만들었을 때, 각각의 권한(8진수) 조합으로 옳은 것은?"
  options={['a는 600, b는 700', 'a는 700, b는 600', 'a는 600, b는 600', 'a는 666, b는 777']}
  correctAnswer={1}
  explanation="umask 077의 세 자리는 소유자 0, 그룹 7, 기타 7이다. 파일 기본 666에서 소유자는 그대로 rw-(6), 그룹과 기타는 rw-에서 전부 빠져 ---(0)이 되어 파일은 600이 된다. 디렉터리 기본 777에서는 소유자가 rwx(7) 그대로, 그룹과 기타는 rwx 전부가 빠져 ---(0)이 되어 디렉터리는 700이 된다."
  optionExplanations={['파일 600, 디렉터리 700을 각각 올바르게 계산한 옳은 답이다', '파일과 디렉터리의 결과를 서로 바꿔 쓴 오답이다', '디렉터리에서 소유자 실행 비트를 빠뜨려 700이 아니라 600으로 잘못 계산한 오답이다', 'umask를 아예 적용하지 않은 기본값을 그대로 쓴 오답이다']}
/>

```text
[ihd@www ~]$ umask 077
[ihd@www ~]$ touch a
[ihd@www ~]$ mkdir b
```

<Quiz
  questionNumber={24}
  question="다음 8진수 특수 권한 4750을 심볼릭 표기로 변환한 것으로 알맞은 것은? (일반 파일 기준)"
  options={['-rwsr-x---', '-rwSr-x---', '-rwxr-s---', '-rwsr-S---']}
  correctAnswer={1}
  explanation="맨 앞자리 4는 SetUID를 뜻한다. 나머지 750은 소유자 rwx, 그룹 r-x, 기타 ---다. SetUID는 소유자의 실행 비트 자리에 표시되는데, 소유자에게 이미 실행 권한(x)이 있으므로 소문자 s로 표시되어 rws가 된다. 따라서 전체 표기는 -rwsr-x---다."
  optionExplanations={['소유자 실행 비트가 있을 때는 소문자 s로 표시된다는 점을 정확히 반영한 옳은 답이다', '소유자에게 실행 권한이 없을 때만 대문자 S로 표시되는데, 이 경우엔 실행 권한이 있으므로 틀린 오답이다', 'SetUID를 소유자가 아닌 그룹 자리에 잘못 표시한 오답이다', '그룹 자리에도 특수 권한이 있는 것처럼 잘못 표시한 오답이다']}
/>

<Quiz
  questionNumber={25}
  question="다음 8진수 특수 권한 2770을 심볼릭 표기로 변환한 것으로 알맞은 것은? (디렉터리 기준)"
  options={['drwxrws---', 'drwsrwx---', 'drwxrwS---', 'drwxrwxrwt']}
  correctAnswer={1}
  explanation="맨 앞자리 2는 SetGID를 뜻한다. 나머지 770은 소유자 rwx, 그룹 rwx, 기타 ---다. SetGID는 그룹의 실행 비트 자리에 표시되는데, 그룹에 이미 실행 권한(x)이 있으므로 소문자 s로 표시되어 rws가 된다. 대상이 디렉터리이므로 맨 앞에 d를 붙여 drwxrws---가 된다."
  optionExplanations={['그룹 실행 비트가 있을 때 소문자 s로 표시된다는 점을 정확히 반영한 옳은 답이다', 'SetGID를 그룹이 아닌 소유자 자리에 잘못 표시한 오답이다', '그룹에 실행 권한이 없을 때만 쓰는 대문자 S를 잘못 적용한 오답이다', 'SetGID를 스티키 비트(t)로 착각한 오답이다']}
/>

<Quiz
  questionNumber={26}
  question="공유 디렉터리 /team에 Sticky-Bit가 올바르게 설정되어 있을 때 나타나는 동작으로 알맞은 것은?"
  options={['/team 안의 모든 파일을 누구나 삭제할 수 있게 된다', '/team 안에 새로 생성되는 파일의 그룹이 /team의 그룹으로 자동 지정된다', '/team 안의 파일은 그 파일의 소유자와 root만 삭제할 수 있게 된다', '/team 디렉터리를 실행할 때 디렉터리 소유자의 권한으로 동작한다']}
  correctAnswer={3}
  explanation="스티키 비트(Sticky-Bit)는 여러 사용자가 쓰기 권한을 공유하는 디렉터리에서, 그 안의 각 파일을 소유자 본인과 root만 삭제·이름 변경할 수 있도록 제한하는 용도로 쓰인다."
  optionExplanations={['스티키 비트는 오히려 임의 삭제를 막는 기능이라 반대로 설명한 오답이다', '그룹 자동 상속은 스티키 비트가 아니라 SetGID의 동작이다', '스티키 비트의 핵심 동작을 정확히 설명한 옳은 답이다', '소유자 권한으로 실행되는 동작은 SetUID의 특징이며 디렉터리 실행과는 관계가 없다']}
/>

<Quiz
  questionNumber={27}
  question="관리자가 백업 도구 /usr/local/bin/backup_tool에 root 소유의 SetUID를 설정했다. 일반 사용자 kim이 이 파일을 실행했을 때 나타나는 동작으로 알맞은 것은?"
  options={['kim의 권한 그대로 실행되며 SetUID는 아무 영향을 주지 않는다', '실행되는 동안 파일 소유자인 root의 권한으로 동작한다', '실행이 즉시 거부되고 오류 메시지만 출력된다', 'kim의 UID가 영구적으로 root의 UID로 바뀐다']}
  correctAnswer={2}
  explanation="SetUID가 설정된 실행 파일은 그 파일을 누가 실행하든 실행되는 동안만 파일 소유자의 권한(effective UID)으로 동작한다. 여기서는 소유자가 root이므로 kim이 실행해도 그 프로세스는 root 권한으로 작업을 수행한다."
  optionExplanations={['SetUID의 핵심 효과를 무시한 오답이다', 'SetUID의 정의를 정확히 설명한 옳은 답이다', 'SetUID는 실행을 거부하는 기능이 아니므로 근거 없는 오답이다', 'SetUID는 프로세스 실행 중에만 유효한 일시적 권한 상승이지 계정 자체의 UID를 영구히 바꾸지 않는다']}
/>

<Quiz
  questionNumber={28}
  question="디렉터리 web과 그 하위 모든 파일·디렉터리의 소유자를 svc, 그룹을 svc로 한꺼번에 변경하는 명령으로 가장 알맞은 것은?"
  options={['chown -R svc:svc web', 'chown svc:svc -a web', 'chmod -R svc:svc web', 'chgrp -R svc.svc web']}
  correctAnswer={1}
  explanation="소유자와 그룹을 동시에 바꾸는 명령은 chown이며, 형식은 chown 소유자:그룹 대상이다. 하위 파일·디렉터리까지 한 번에 적용하려면 재귀 옵션 -R을 붙인다."
  optionExplanations={['chown -R로 소유자·그룹을 재귀적으로 바꾸는 옳은 명령이다', '-a라는 옵션은 chown에 존재하지 않는 잘못된 옵션이다', 'chmod는 권한(rwx)을 바꾸는 명령이지 소유자·그룹을 바꾸는 명령이 아니다', 'chgrp은 그룹만 바꿀 수 있어 소유자:그룹 형식의 인자를 받지 못한다']}
/>

<Quiz
  questionNumber={29}
  question="다음 /etc/passwd 항목의 필드 해석 중 옳지 않은 것은?"
  options={['두 번째 필드 x는 비밀번호가 /etc/shadow에 저장되어 있음을 뜻한다', '세 번째 필드 1050은 UID, 네 번째 필드 1050은 GID다', '다섯 번째 필드 Dev One은 사용자에 대한 설명(comment)이다', '마지막 필드 /bin/bash는 사용자의 홈 디렉터리 경로다']}
  correctAnswer={4}
  explanation="/etc/passwd의 마지막(일곱 번째) 필드는 로그인 셸이다. 홈 디렉터리는 그 앞의 여섯 번째 필드인 /home/dev1이 담당하므로 4번 설명이 틀렸다."
  optionExplanations={['x가 그림자 비밀번호를 뜻한다는 설명은 옳다', 'UID·GID 필드 해석은 옳다', '설명(comment) 필드 해석은 옳다', '홈 디렉터리 필드와 로그인 셸 필드를 서로 바꿔 이해한 오답이다']}
/>

```text
dev1:x:1050:1050:Dev One:/home/dev1:/bin/bash
```

<Quiz
  questionNumber={30}
  question="다음 중 /etc/shadow 파일의 필드에 대한 설명으로 옳지 않은 것은?"
  options={['두 번째 필드는 암호화된 비밀번호(해시)다', '세 번째 필드는 1970년 1월 1일 이후 마지막으로 비밀번호를 변경한 일수다', '다섯 번째 필드는 비밀번호 최소 사용 기간이다', 'shadow 파일은 root만 읽을 수 있도록 권한이 제한되어 있다']}
  correctAnswer={3}
  explanation="/etc/shadow의 필드 순서는 사용자명, 암호화된 비밀번호, 마지막 변경일, 최소 사용 기간, 최대 사용 기간, 경고 일수, 비활성 유예 일수, 계정 만료일이다. 다섯 번째 필드는 최소가 아니라 최대 사용 기간이므로 3번이 틀렸다."
  optionExplanations={['암호화된 비밀번호 필드 설명은 옳다', '마지막 변경일 필드 설명은 옳다', '네 번째(최소 사용 기간)와 다섯 번째(최대 사용 기간) 필드를 혼동한 오답이다', 'shadow 파일의 접근 제한 설명은 옳다']}
/>

<Quiz
  questionNumber={31}
  question="새 사용자 dev2를 추가하면서 홈 디렉터리를 자동 생성하고, 로그인을 금지하는 셸(/sbin/nologin)로 지정하려고 한다. 알맞은 명령은?"
  options={['useradd -m -s /sbin/nologin dev2', 'useradd -d /sbin/nologin dev2', 'usermod -m -s /sbin/nologin dev2', 'adduser -M -s /sbin/nologin dev2']}
  correctAnswer={1}
  explanation="useradd에서 -m은 홈 디렉터리를 자동 생성하는 옵션, -s는 로그인 셸을 지정하는 옵션이다. 두 옵션을 함께 쓰면 홈 디렉터리를 만들면서 로그인이 금지된 셸을 지정할 수 있다."
  optionExplanations={['-m과 -s를 정확히 조합한 옳은 명령이다', '-d는 홈 디렉터리 경로를 지정하는 옵션인데 여기에 셸 경로를 잘못 넣은 오답이다', 'usermod는 이미 존재하는 계정을 수정하는 명령이라 신규 계정 생성에는 쓸 수 없다', '-M은 홈 디렉터리를 만들지 않는 옵션이라 요구 조건과 반대다']}
/>

<Quiz
  questionNumber={32}
  question="다음 중 su와 sudo 명령의 차이에 대한 설명으로 옳지 않은 것은?"
  options={['su는 대상 계정의 비밀번호를 입력해 셸 자체를 그 계정으로 전환한다', 'sudo는 실행하는 사용자 자신의 비밀번호로 특정 명령을 관리자 권한으로 실행한다', 'sudo 사용 권한은 /etc/sudoers 파일로 세밀하게 제어할 수 있다', 'su와 sudo 모두 기본적으로 실행 기록을 전혀 남기지 않는다']}
  correctAnswer={4}
  explanation="sudo는 기본 설정에서 실행한 명령과 시각을 로그(보통 /var/log/secure나 auth.log 등)에 남긴다. 이는 감사 추적을 위한 sudo의 중요한 특징이므로, 로그를 전혀 남기지 않는다는 4번 설명이 틀렸다."
  optionExplanations={['su의 동작 방식을 정확히 설명한 옳은 문장이다', 'sudo의 동작 방식을 정확히 설명한 옳은 문장이다', '/etc/sudoers로 권한을 세밀하게 제어한다는 설명은 옳다', 'sudo가 기본적으로 로그를 남긴다는 사실을 반대로 안 오답이다']}
/>

<Quiz
  questionNumber={33}
  question="다음 중 프로세스의 PID와 PPID에 대한 설명으로 옳지 않은 것은?"
  options={['PID는 프로세스가 생성될 때 커널이 부여하는 고유 식별 번호다', 'PPID는 그 프로세스를 생성(fork)한 부모 프로세스의 PID다', '한 시스템에서 같은 PID를 가진 프로세스가 동시에 두 개 이상 존재할 수 있다', '부모 프로세스가 먼저 종료되면 자식 프로세스는 보통 init(또는 systemd)가 새로운 부모가 되어 거둔다']}
  correctAnswer={3}
  explanation="PID는 특정 시점에 실행 중인 프로세스를 유일하게 식별하기 위한 번호이므로, 같은 순간에 동일한 PID를 가진 프로세스가 동시에 두 개 존재할 수 없다. 따라서 3번이 틀렸다."
  optionExplanations={['PID의 정의를 정확히 설명한 옳은 문장이다', 'PPID의 정의를 정확히 설명한 옳은 문장이다', 'PID의 유일성을 부정한 오답이다', '고아 프로세스가 init에 입양된다는 설명은 옳다']}
/>

<Quiz
  questionNumber={34}
  question="다음 중 데몬(daemon)과 좀비(zombie) 프로세스에 대한 설명으로 옳지 않은 것은?"
  options={['데몬은 특정 서비스 요청을 처리하기 위해 백그라운드에서 계속 실행되는 프로세스다', '좀비 프로세스는 이미 실행을 마쳤지만 부모가 종료 상태를 회수하지 않아 프로세스 테이블에 남아 있는 상태다', '좀비 프로세스는 CPU와 메모리를 실행 중인 프로세스와 동일한 수준으로 계속 소비한다', '데몬 이름은 관례적으로 끝에 d가 붙는 경우가 많다']}
  correctAnswer={3}
  explanation="좀비 프로세스는 이미 실행이 끝난 상태라 실제 작업을 수행하지 않으며, 종료 상태 값을 담은 최소한의 프로세스 테이블 항목만 남긴다. CPU나 메모리를 실행 중인 프로세스처럼 소비하지 않으므로 3번이 틀렸다."
  optionExplanations={['데몬의 정의를 정확히 설명한 옳은 문장이다', '좀비 프로세스의 발생 원인을 정확히 설명한 옳은 문장이다', '좀비가 실제로는 거의 자원을 소비하지 않는다는 점을 반대로 안 오답이다', 'sshd, httpd처럼 d로 끝나는 데몬 이름 관례를 설명한 옳은 문장이다']}
/>

<Quiz
  questionNumber={35}
  question="다음 중 ps 명령의 STAT(상태) 열 표기에 대한 설명으로 옳지 않은 것은?"
  options={['R은 실행 중이거나 실행 대기 상태다', 'S는 신호로 깨울 수 있는 대기(sleep) 상태다', 'Z는 좀비 상태를 뜻한다', 'D는 사용자가 [Ctrl]+[Z]로 일시 정지시킨 상태를 뜻한다']}
  correctAnswer={4}
  explanation="D는 디스크 입출력처럼 인터럽트가 불가능한 대기 상태를 뜻한다. 사용자가 [Ctrl]+[Z]로 일시 정지시킨 상태는 T(정지, stopped)로 표시되므로 4번 설명이 틀렸다."
  optionExplanations={['R 상태의 정의는 옳다', 'S 상태의 정의는 옳다', 'Z 상태의 정의는 옳다', 'D 상태와 T 상태의 의미를 서로 바꿔 이해한 오답이다']}
/>

<Quiz
  questionNumber={36}
  question="다음 jobs 결과에서 두 번째로 등록된 작업(vim b.txt)을 포그라운드로 불러오는 명령으로 알맞은 것은?"
  options={['fg %2', 'bg %2', 'fg 2', 'jobs %2']}
  correctAnswer={1}
  explanation="작업 번호는 퍼센트 기호와 함께 %번호 형태로 지정한다. fg %2는 잡 번호 2번인 vim b.txt를 포그라운드로 불러온다."
  optionExplanations={['잡 번호 지정 형식을 정확히 쓴 옳은 답이다', 'bg는 포그라운드가 아니라 백그라운드로 재개하는 명령이라 요구 조건과 다르다', '퍼센트 기호 없이 숫자만 쓰면 작업 번호가 아니라 다른 인자로 해석되어 의도대로 동작하지 않는다', 'jobs는 작업 목록을 보여주는 명령일 뿐 특정 작업을 불러오는 명령이 아니다']}
/>

```text
[ihd@www ~]$ jobs
[1]-  Stopped     vim a.txt
[2]+  Stopped     vim b.txt
```

<Quiz
  questionNumber={37}
  question="다음 중 시그널 번호와 이름의 연결로 옳지 않은 것은?"
  options={['1 - SIGHUP', '2 - SIGINT', '9 - SIGKILL', '15 - SIGSTOP']}
  correctAnswer={4}
  explanation="15번 시그널은 정상 종료를 요청하는 SIGTERM이다. SIGSTOP은 19번 시그널이며 프로세스를 강제로 일시 정지시킨다. 따라서 4번 연결이 틀렸다."
  optionExplanations={['1번 SIGHUP 연결은 옳다', '2번 SIGINT 연결은 옳다', '9번 SIGKILL 연결은 옳다', '15번(SIGTERM)과 19번(SIGSTOP)의 번호를 혼동한 오답이다']}
/>

<Quiz
  questionNumber={38}
  question="새로 실행할 백업 스크립트 backup.sh를 nice 값 12로 실행하려고 한다. 알맞은 명령은?"
  options={['renice -n 12 backup.sh', 'nice -n 12 backup.sh', 'nice -n -12 backup.sh', 'nice 12 backup.sh']}
  correctAnswer={2}
  explanation="새 프로세스를 특정 nice 값으로 시작할 때는 nice -n 값 명령 형식을 쓴다. nice -n 12 backup.sh는 nice 값 12로 백업 스크립트를 실행한다."
  optionExplanations={['renice는 이미 실행 중인 프로세스의 nice 값만 바꿀 수 있어 새 프로세스 시작에는 쓸 수 없다', '-n 옵션으로 nice 값을 정확히 지정한 옳은 답이다', '-n -12는 값을 12가 아닌 음수 -12로 지정해 오히려 우선순위를 높이는 반대 결과를 낸다', '-n 옵션 없이 숫자만 쓰면 GNU nice에서 그 숫자를 nice 값으로 인식하지 못해 의도대로 동작하지 않는다']}
/>

<Quiz
  questionNumber={39}
  question="PID 3301인 프로세스 A는 nice 값이 -8이고, PID 3302인 프로세스 B는 nice 값이 15다. 이 상태에서 우선순위 관계와, 프로세스 A의 nice 값을 -8에서 -3으로 조정하려 할 때의 결과로 가장 알맞은 것은?"
  options={['A가 B보다 낮은 우선순위이며, nice 값 조정은 일반 사용자에게 모두 거부된다', 'A가 B보다 높은 우선순위이며, -8에서 -3으로 값을 올리는 것은 값을 증가시키는 조정이라 일반 사용자에게도 허용된다', 'A와 B의 우선순위는 nice 값과 무관하게 항상 동일하다', 'A가 B보다 높은 우선순위이며, 일반 사용자는 -8보다 더 낮은 값(예: -15)으로도 자유롭게 낮출 수 있다']}
  correctAnswer={2}
  explanation="nice 값은 낮을수록(음수에 가까울수록) 우선순위가 높다. -8인 A가 15인 B보다 우선순위가 높다. nice 값을 -8에서 -3으로 바꾸는 것은 수치가 커지는 방향, 즉 우선순위를 낮추는 방향의 조정이며, 일반 사용자도 자신의 프로세스의 nice 값을 높이는(우선순위를 낮추는) 조정은 언제나 허용된다."
  optionExplanations={['nice 값이 낮을수록 우선순위가 높다는 규칙을 반대로 안 오답이다', '우선순위 관계와 nice 값 증가 조정이 허용된다는 규칙을 정확히 짚은 옳은 답이다', 'nice 값이 실제 스케줄링 우선순위에 영향을 준다는 사실을 부정한 오답이다', '더 낮은(더 음수인) 값으로 내리는 것은 우선순위를 더 높이는 조정이라 일반 사용자에게는 거부된다는 점을 놓친 오답이다']}
/>

<Quiz
  questionNumber={40}
  question="다음 crontab 항목이 실행되는 시점으로 알맞은 것은?"
  options={['매월 2일 밤 10시 15분에 실행된다', '매주 화요일 밤 10시 15분에 실행된다', '매주 화요일 오전 10시 15분에 실행된다', '15시간마다 22분 간격으로 실행된다']}
  correctAnswer={2}
  explanation="crontab의 다섯 필드는 순서대로 분, 시, 일, 월, 요일이다. 이 항목은 분 15, 시 22, 일 *, 월 *, 요일 2다. 요일은 0이 일요일부터 시작하므로 2는 화요일이고, 시 22는 24시간제로 오후 10시를 뜻한다. 따라서 매주 화요일 밤 10시 15분에 실행된다."
  optionExplanations={['다섯 번째 필드(요일)를 세 번째 필드(일)로 착각한 오답이다', '분·시·요일 필드를 정확히 해석한 옳은 답이다', '24시간제 22시를 오전으로 잘못 해석한 오답이다', '분·시 필드를 실행 주기로 잘못 해석한 오답이다']}
/>

```text
15 22 * * 2 /usr/local/bin/report.sh
```

<Quiz
  questionNumber={41}
  question="다음 crontab 항목이 실행되는 시점으로 알맞은 것은?"
  options={['평일 오전 8시부터 오후 6시까지 10분마다 실행된다', '매일 8시부터 18시까지 10시간마다 실행된다', '주말을 포함해 매일 10분마다 실행된다', '평일 오전 8시 10분에 한 번만 실행된다']}
  correctAnswer={1}
  explanation="분 필드의 */10은 0분부터 10분 간격으로, 즉 매시 0·10·20·30·40·50분에 실행됨을 뜻한다. 시 필드 8-18은 8시부터 18시까지의 범위, 요일 필드 1-5는 월요일부터 금요일까지를 뜻한다. 따라서 평일 오전 8시부터 오후 6시까지 10분마다 실행된다."
  optionExplanations={['분·시·요일 필드의 스텝(*/10)과 범위(8-18, 1-5)를 정확히 해석한 옳은 답이다', '분 필드의 스텝 표기(*/10, 10분마다)를 시간 단위(10시간마다)로 잘못 해석한 오답이다', '요일 필드의 범위(1-5, 평일)를 무시하고 매일로 잘못 해석한 오답이다', '스텝 표기(*/10)를 특정 시각 하나로 잘못 해석한 오답이다']}
/>

```text
*/10 8-18 * * 1-5 /usr/local/bin/check.sh
```

<Quiz
  questionNumber={42}
  question="다음 중 X 윈도 시스템의 클라이언트-서버 구조에 대한 설명으로 옳지 않은 것은?"
  options={['화면 출력과 입력장치를 제어하는 쪽이 X 서버다', '사용자가 실행하는 응용 프로그램은 X 클라이언트다', '원격 호스트에서 실행한 응용 프로그램을 로컬 화면에 띄우면 그 응용 프로그램이 X 서버가 된다', '서버와 클라이언트는 네트워크를 통해 X 프로토콜로 통신할 수 있다']}
  correctAnswer={3}
  explanation="X 윈도에서는 응용 프로그램을 어디서 실행하든 그 응용 프로그램 자체가 X 클라이언트이고, 화면과 입력장치를 가진 쪽이 X 서버다. 원격 호스트의 GIMP를 실행해도 GIMP는 여전히 클라이언트이며, 그림을 그려 주는 로컬 디스플레이가 서버 역할을 한다. 따라서 3번이 틀렸다."
  optionExplanations={['X 서버의 정의를 정확히 설명한 옳은 문장이다', 'X 클라이언트의 정의를 정확히 설명한 옳은 문장이다', '원격 실행이라는 상황에 현혹되어 서버·클라이언트 역할을 뒤바꾼 오답이다', 'X 프로토콜을 통한 네트워크 통신 설명은 옳다']}
/>

<Quiz
  questionNumber={43}
  question="다음 중 디스플레이 매니저와 윈도 매니저의 역할 구분으로 옳지 않은 것은?"
  options={['디스플레이 매니저(GDM, LightDM 등)는 그래픽 로그인 화면을 제공하고 세션을 시작한다', '윈도 매니저(Metacity, Mutter 등)는 창의 테두리, 이동, 크기 조절을 담당한다', '디스플레이 매니저와 윈도 매니저는 완전히 동일한 프로그램으로 서로 대체해 쓸 수 있다', '데스크톱 환경은 패널, 파일 관리자 등과 함께 윈도 매니저를 통합해 제공하는 경우가 많다']}
  correctAnswer={3}
  explanation="디스플레이 매니저는 로그인 화면과 세션 시작을 담당하고, 윈도 매니저는 로그인 이후 화면 안의 창을 관리하는 서로 다른 역할의 프로그램이다. 하나가 없다고 다른 하나로 대체할 수 없으므로 3번이 틀렸다."
  optionExplanations={['디스플레이 매니저의 역할 설명은 옳다', '윈도 매니저의 역할 설명은 옳다', '서로 다른 계층의 프로그램을 동일하다고 본 오답이다', '데스크톱 환경이 윈도 매니저를 포함해 통합 제공한다는 설명은 옳다']}
/>

<Quiz
  questionNumber={44}
  question="원격 호스트 PC-B(IP 192.168.10.20)에서 실행한 프로그램 화면을, 디스플레이 번호 1을 쓰는 로컬 호스트 PC-A(IP 192.168.10.5)에 출력하려고 한다. PC-B에서 실행해야 하는 명령으로 알맞은 것은?"
  options={['export DISPLAY=192.168.10.5:1.0', 'export DISPLAY=192.168.10.20:1.0', 'export DISPLAY=192.168.10.5:0.1', 'xhost DISPLAY=192.168.10.5:1']}
  correctAnswer={1}
  explanation="DISPLAY 환경변수는 화면을 그려 줄 X 서버, 즉 결과를 보고 싶은 로컬 호스트를 가리켜야 한다. 형식은 호스트:디스플레이번호.스크린번호이므로 PC-A의 주소인 192.168.10.5에 디스플레이 번호 1, 스크린 번호 0을 붙여 192.168.10.5:1.0으로 지정해야 한다."
  optionExplanations={['출력을 보고 싶은 로컬 호스트(PC-A) 주소와 디스플레이 번호를 정확히 지정한 옳은 답이다', 'DISPLAY에 응용 프로그램이 실행되는 원격 호스트(PC-B) 자신의 주소를 잘못 넣은 오답이다', '디스플레이 번호와 스크린 번호의 순서를 서로 바꿔 쓴 오답이다', 'DISPLAY는 export로 지정하는 환경변수이지 xhost 명령의 인자 형식이 아니어서 문법 자체가 잘못됐다']}
/>

<Quiz
  questionNumber={45}
  question="다음 중 사설(private) IP 주소 대역으로 옳지 않은 것은?"
  options={['10.0.0.0부터 10.255.255.255까지', '172.16.0.0부터 172.31.255.255까지', '192.168.0.0부터 192.168.255.255까지', '172.32.0.0부터 172.63.255.255까지']}
  correctAnswer={4}
  explanation="사설 IP 대역은 A클래스 10.0.0.0/8, B클래스 172.16.0.0/12(172.16.0.0–172.31.255.255), C클래스 192.168.0.0/16으로 정해져 있다. 172.32.0.0 이상은 이 사설 대역을 벗어난 공인 IP 대역이므로 4번이 사설 IP 대역이 아니다."
  optionExplanations={['10.0.0.0/8 대역은 사설 IP가 맞다', '172.16.0.0/12(172.16–172.31)는 사설 IP가 맞다', '192.168.0.0/16 대역은 사설 IP가 맞다', 'B클래스 사설 대역이 172.31까지만이라는 경계를 넘겨 172.32 이후도 사설이라 착각한 오답이다']}
/>

<Quiz
  questionNumber={46}
  question="C클래스 네트워크에서 서브넷 마스크를 255.255.255.240으로 설정했을 때, 하나의 서브넷에서 호스트에 할당 가능한 IP 주소 개수로 알맞은 것은?"
  options={['16개', '14개', '30개', '32개']}
  correctAnswer={2}
  explanation="255.255.255.240은 이진수로 11110000이라 호스트 비트가 4자리 남는다. 2의 4제곱은 16이며, 이 16개 주소 중 네트워크 주소 1개와 브로드캐스트 주소 1개를 제외하면 실제 호스트에 할당 가능한 주소는 16에서 2를 뺀 14개다."
  optionExplanations={['네트워크 주소와 브로드캐스트 주소를 빼지 않은 전체 개수를 그대로 답한 오답이다', '2의 4제곱에서 예약된 2개 주소를 정확히 뺀 옳은 답이다', '호스트 비트를 5자리로 잘못 계산해 32-2를 절반으로 어림한 오답이다', '호스트 비트를 5자리로 잘못 계산한 오답이다']}
/>

<Quiz
  questionNumber={47}
  question="다음 중 목적지까지의 경로상 각 홉(hop)의 응답 시간을 확인하는 명령으로 알맞은 것은?"
  options={['ping', 'traceroute', 'netstat', 'nslookup']}
  correctAnswer={2}
  explanation="traceroute는 목적지까지 패킷이 거쳐 가는 각 라우터(홉)를 순서대로 보여 주고, 각 구간의 왕복 시간을 함께 표시해 경로상 지연 구간을 파악하는 데 쓰인다."
  optionExplanations={['ping은 목적지까지의 전체 왕복 시간과 도달 여부만 확인할 뿐 중간 경로를 보여주지 않는다', '각 홉의 경로와 응답 시간을 보여주는 정확한 답이다', 'netstat은 현재 연결·포트 상태를 보여주는 명령이라 경로 추적과는 관계가 없다', 'nslookup은 도메인 이름을 IP로 변환하는 명령이라 경로 추적과는 관계가 없다']}
/>

<Quiz
  questionNumber={48}
  question="다음 중 프로토콜과 포트 번호의 조합으로 옳지 않은 것은?"
  options={['FTP 데이터 - 20', 'TELNET - 23', 'DNS - 53', 'HTTPS - 143']}
  correctAnswer={4}
  explanation="HTTPS의 잘 알려진 포트 번호는 443이다. 143번 포트는 IMAP이 사용하는 포트이므로 4번 조합이 틀렸다."
  optionExplanations={['FTP 데이터 포트 20번 연결은 옳다', 'TELNET 포트 23번 연결은 옳다', 'DNS 포트 53번 연결은 옳다', 'HTTPS(443번)와 IMAP(143번)의 포트 번호를 혼동한 오답이다']}
/>

<Quiz
  questionNumber={49}
  question="다음 중 컨테이너 기반 가상화의 특징으로 옳지 않은 것은?"
  options={['호스트 커널을 공유하므로 게스트 운영체제 전체를 구동할 필요가 없다', '하이퍼바이저 기반 가상화보다 일반적으로 기동 속도가 빠르다', '각 컨테이너가 서로 다른 독립된 커널을 실행한다', '대표적인 구현 도구로 도커(Docker)가 있다']}
  correctAnswer={3}
  explanation="컨테이너 기반 가상화의 핵심 특징은 여러 컨테이너가 호스트의 커널 하나를 함께 공유한다는 점이다. 컨테이너마다 별도의 커널을 실행하는 것은 하이퍼바이저 기반 가상화(가상 머신)의 특징이므로 3번이 틀렸다."
  optionExplanations={['커널 공유로 게스트 OS 전체 구동이 불필요하다는 설명은 옳다', '기동 속도가 빠르다는 설명은 옳다', '커널 공유라는 컨테이너의 핵심 특징을 반대로 설명한 오답이다', '도커가 대표적인 컨테이너 도구라는 설명은 옳다']}
/>

<Quiz
  questionNumber={50}
  question="다음 중 슈퍼컴퓨터의 성능을 측정해 TOP500 순위를 매길 때 기준으로 쓰이는 벤치마크로 알맞은 것은?"
  options={['LINPACK', 'SPEC', 'Geekbench', 'PassMark']}
  correctAnswer={1}
  explanation="TOP500 프로젝트는 대규모 선형 연립방정식을 얼마나 빨리 푸는지를 측정하는 LINPACK 벤치마크 결과를 기준으로 전 세계 슈퍼컴퓨터의 순위를 매긴다."
  optionExplanations={['TOP500 순위 산정에 실제로 쓰이는 벤치마크로 옳은 답이다', 'SPEC은 서버·워크스테이션의 일반 성능 평가에 주로 쓰이는 벤치마크로 TOP500 기준이 아니다', 'Geekbench는 모바일·개인용 기기의 성능 측정에 주로 쓰이는 벤치마크로 TOP500 기준이 아니다', 'PassMark는 개인용 PC 부품 성능 비교에 주로 쓰이는 벤치마크로 TOP500 기준이 아니다']}
/>

## 채점 후 복습 가이드

이 시리즈는 **60점 이상(50문항 중 30문항 이상 정답)** 이면 합격이다. 채점 후 틀린 문항의 주제를 아래 표에서 찾아 해당 편으로 돌아가 복습하자.

| 틀린 영역 | 관련 문항 번호 | 돌아갈 편 |
|-----------|----------------|-----------|
| 커널·배포판·라이선스 | 1–3 | 02, 03 |
| 설치·부팅·런레벨 | 4–6 | 04, 05 |
| 셸 기본·프롬프트·기본 명령어 | 7–11 | 06, 07 |
| 파일시스템 구조·마운트·df·du·fsck | 12–18 | 09, 10 |
| 권한·umask·특수 권한 | 19–28 | 11 |
| 사용자·그룹 관리 | 29–31 | 12 |
| su·sudo | 32 | 12 |
| 프로세스 개념 | 33–34 | 13 |
| 프로세스 명령·시그널·nice | 35–39 | 14 |
| cron | 40–41 | 21 |
| X 윈도 | 42–44 | 22 |
| 네트워크 기초·명령어 | 45–48 | 23, 24 |
| 응용분야 | 49–50 | 25 |

틀린 문항이 특정 영역에 몰려 있다면 그 편을 다시 읽고, 그 편의 Quiz를 한 번 더 풀어 본 뒤 이 모의고사를 다시 채점해 보는 방식으로 반복하는 것이 가장 효율적이다.

## 참고 자료

- [KAIT 리눅스마스터 자격안내](https://www.ihd.or.kr/introducesubject1.do)
- [Linux man-pages 프로젝트](https://www.kernel.org/doc/man-pages/)
- [GNU Coreutils 매뉴얼](https://www.gnu.org/software/coreutils/manual/coreutils.html)
- [Filesystem Hierarchy Standard (FHS)](https://refspecs.linuxfoundation.org/fhs.shtml)
