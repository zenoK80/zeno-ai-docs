---
title: '26. 빅데이터분석기사 필기 예상 모의고사 1회'
description: '4과목 80문항을 실제 시험과 같은 배분으로 재구성한 첫 번째 모의고사, 채점 후 과목별 복습 동선까지 정리'
---

<Callout type="warning">
  **이번 회차는 기출 그대로가 아니라 재구성 출제입니다.** 01~27편에서 다룬 개념과 최근 회차 기출·모의고사 자료의 출제 유형·빈도·오답 구성 방식을 분석해 숫자와 상황을 새로 만든 문제입니다. 문항을 외워 두어도 실제 시험에는 그대로 나오지 않습니다.
  권장 풀이 시간은 **120분**이며, 합격 기준은 **과목별 100점 만점에 40점 미만이면 과락**(20문항 중 8문항 미만 정답)이고 **4과목 평균 60점 이상**(80문항 중 48문항 이상 정답)입니다. 실제 응시 회차의 세부 출제기준·배점·합격선은 한국데이터산업진흥원 공고를 반드시 확인하십시오.
</Callout>

## 문항 배분표

| 과목 | 문항 범위 | 문항 수 | 주요 내용 |
|---|---|---|---|
| 1과목 빅데이터 분석 기획 | 1–20 | 20 | 빅데이터 이해, 분석 기획, 수집·저장 계획, 개인정보·거버넌스 |
| 2과목 빅데이터 탐색 | 21–40 | 20 | 전처리, 탐색적 데이터 분석, 기술통계·확률분포·추론 |
| 3과목 빅데이터 모델링 | 41–60 | 20 | 회귀·분류, 의사결정나무, 군집, 신경망·시계열 기초 |
| 4과목 빅데이터 결과 해석 | 61–80 | 20 | 평가지표, 검증·과적합, 초매개변수, 시각화·활용 |
| **합계** | – | **80** | – |

이 회차는 **표준 난이도**로 구성했습니다. 문항 형식은 정의 고르기, 옳지 않은 것 고르기, 보기(ㄱㄴㄷㄹ) 조합형, 표를 활용한 계산형을 실제 기출 비율에 맞춰 섞었습니다.

## 1과목: 빅데이터 분석 기획 (1~20)

<Quiz
  questionNumber={1}
  question="한 종합병원의 진료 정보 시스템에는 검사 결과가 담긴 정형 테이블, 자유 서술로 작성된 퇴원 요약 문서, 검사 장비가 표준 규격으로 전송하는 반정형 메시지가 함께 저장된다. 이처럼 구조화 정도가 서로 다른 자료가 한 저장소에 섞여 있는 성질을 가리키는 빅데이터 특성은 무엇인가"
  options={['규모(Volume)', '다양성(Variety)', '속도(Velocity)', '정확성(Veracity)']}
  correctAnswer={2}
  explanation="구조화 정도가 서로 다른 정형·반정형·비정형 자료가 함께 존재한다는 성질은 다양성이 가리키는 내용이다. 규모는 총량, 속도는 생성과 처리 간격, 정확성은 값의 신뢰 수준을 다루므로 이 사례와는 초점이 다르다."
  optionExplanations={['규모는 자료의 총량이 크다는 뜻이며 이 사례는 형식이 섞여 있다는 점을 묻고 있어 총량과는 무관하다.', '정답이다. 표 형태, 자유 서술 문서, 표준 메시지처럼 구조화 정도가 다른 자료가 함께 있다는 점이 다양성의 정의와 정확히 맞는다.', '속도는 자료가 만들어지고 소비되는 시간 간격이 짧다는 뜻이며 이 사례에는 시간 제약이 언급되지 않았다.', '정확성은 자료 값을 얼마나 신뢰할 수 있는지를 다루며 자료 형식이 섞여 있는 상황과는 다른 축이다.']}
/>

<Quiz
  questionNumber={2}
  question="한 유통 기업이 고객 구매 이력을 분석해 재구매율을 8퍼센트포인트 끌어올리고 그만큼 매출을 늘렸다. 이 성과가 직접 대응하는 빅데이터 특성 확장 요소는 무엇인가"
  options={['가치(Value)', '규모(Volume)', '다양성(Variety)', '속도(Velocity)']}
  correctAnswer={1}
  explanation="분석 결과가 실제 의사결정과 수익 창출로 이어지는 것은 3V 이후 확장된 가치 관점이다. 규모·다양성·속도는 자료 자체의 성질을 가리킬 뿐 분석으로 얻은 효용을 뜻하지 않는다."
  optionExplanations={['정답이다. 분석 결과가 매출 증대와 같은 실제 효용으로 이어졌다는 점이 가치가 가리키는 내용과 정확히 일치한다.', '규모는 자료의 양이 많다는 뜻이며 이 사례는 양이 아니라 분석의 성과를 묻고 있다.', '다양성은 자료 형식이 여러 가지라는 뜻이며 이 사례에는 형식 차이가 등장하지 않는다.', '속도는 자료가 발생하고 처리되는 빠르기를 뜻하며 재구매율 개선이라는 성과와는 관련이 없다.']}
/>

<Quiz
  questionNumber={3}
  question="다음 중 빅데이터 활용 확대에 따른 위기 요인으로 보기 어려운 것은 무엇인가"
  options={['서로 다른 곳에 흩어져 있던 행동 기록이 결합되어 개인의 생활 패턴이 드러나는 문제', '분석 알고리즘이 내놓은 예측 점수만으로 아직 벌어지지 않은 일에 불이익을 주는 문제', '치우친 표본으로 학습한 모형이 특정 집단에 불리한 결과를 반복해서 내놓는 문제', '여러 기기와 서비스가 연결되면서 분석에 활용할 수 있는 자료의 폭이 넓어지는 변화']}
  correctAnswer={4}
  explanation="통상 거론되는 빅데이터 위기 요인은 사생활 침해, 책임 원칙의 훼손, 데이터 오용(편향에 따른 차별) 세 갈래다. 연결이 늘어 활용 가능한 자료가 넓어지는 것은 위험이 아니라 빅데이터가 만들어 내는 기대 효과이므로 위기 요인으로 보기 어렵다."
  optionExplanations={['개인 정보가 재조합되어 사생활이 드러나는 것은 대표적인 위기 요인인 사생활 침해에 해당하므로 이 문제에서 찾는 답이 아니다.', '예측만으로 불이익을 주는 것은 책임 원칙의 훼손에 해당하는 대표 사례이므로 이 문제에서 찾는 답이 아니다.', '편향된 표본이 차별적 결과를 낳는 것은 데이터 오용에 해당하는 위기 요인이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 연결 확대로 활용 가능한 자료가 늘어나는 것은 위험이 아니라 빅데이터가 주는 기회이며, 위기 요인 세 갈래 어디에도 속하지 않는다.']}
/>

<Quiz
  questionNumber={4}
  question="한 제조 기업이 분석 준비도와 분석 성숙도 두 축으로 자사 수준을 진단했다. 두 축의 조합과 유형 명칭을 짝지은 설명 중 옳지 않은 것은 무엇인가"
  options={['도입형은 준비도는 높지만 성숙도는 낮아 기법 활용을 앞으로 채워야 하는 상태다', '준비형은 두 축이 모두 낮아 기반 마련과 기법 적용을 함께 시작해야 하는 상태다', '확산형은 두 축이 모두 높아 전사 차원으로 분석을 넓혀 갈 수 있는 상태다', '정착형은 준비도와 성숙도가 모두 높아 이미 전사 표준화가 끝난 상태다']}
  correctAnswer={4}
  explanation="정착형은 준비도가 낮은데도 제한된 범위에서 분석을 이미 수행하고 있어 성숙도만 높은 상태다. 두 축이 모두 높다고 서술한 4번은 확산형의 정의를 정착형에 잘못 붙인 것이다."
  optionExplanations={['도입형은 준비도가 높고 성숙도가 낮은 조합이 맞으므로 옳은 설명이다.', '준비형은 두 축이 모두 낮은 상태를 가리키므로 옳은 설명이다.', '확산형은 두 축이 모두 높은 상태를 가리키므로 옳은 설명이다.', '정답이다. 정착형은 준비도는 낮지만 성숙도만 높은 상태이며, 두 축이 모두 높다는 서술은 확산형과 정착형을 혼동한 것이다.']}
/>

<Quiz
  questionNumber={5}
  question="분석 업무를 담당하는 조직 구조 유형에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['기능구조는 전담 조직 없이 각 현업 부서가 자기 범위에서 분석을 처리해 전사 관점의 과제를 다루기 어렵다', '분산구조는 분석 인력을 현업 부서에 배치해 업무 맥락을 아는 사람이 결과를 빠르게 실무에 반영할 수 있다', '집중구조로 전담 조직을 세우면 현업 부서가 수행하던 분석과 업무가 겹칠 여지가 완전히 사라진다', '집중구조는 여러 부서에서 올라온 과제의 우선순위를 한곳에서 조정할 수 있다']}
  correctAnswer={3}
  explanation="전담 조직을 세워도 현업 부서가 자체적으로 수행하던 분석과 역할이 겹칠 수 있으며, 오히려 업무 중복은 집중구조의 대표적 약점으로 지적된다. 구조를 바꾼다고 중복 가능성이 원천적으로 사라지지는 않는다."
  optionExplanations={['기능구조는 부서별 분산 운영 탓에 전사 과제 조율이 어렵다는 것이 알려진 약점이므로 옳은 설명이다.', '분산구조는 업무 맥락을 아는 인력이 직접 분석해 실무 반영이 빠르다는 것이 알려진 장점이므로 옳은 설명이다.', '정답이다. 집중구조를 세워도 현업의 자체 분석과 겹치는 문제는 여전히 관리해야 할 과제로 남으며 완전히 사라진다고 볼 수 없다.', '집중구조는 우선순위를 한곳에서 조정할 수 있다는 것이 알려진 장점이므로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={6}
  question="데이터 과학자에게 요구되는 역량을 하드 스킬과 소프트 스킬로 나눌 때, 소프트 스킬에 해당하는 것은 무엇인가"
  options={['분산 처리 프레임워크를 설정하고 운영하는 능력', '통계적 추정량과 평가지표를 정확히 계산하는 능력', '서로 다른 부서의 관점을 통합해 분석 결과를 설득력 있게 전달하는 능력', '머신러닝 알고리즘을 코드로 구현하는 능력']}
  correctAnswer={3}
  explanation="관점을 통합하고 설득력 있게 전달하는 능력은 대인관계·의사소통에 속하는 소프트 스킬이다. 나머지 세 능력은 도구·통계·구현 기술을 다루는 하드 스킬에 해당한다."
  optionExplanations={['분산 처리 프레임워크 설정은 기술 도구를 다루는 하드 스킬이다.', '통계적 추정량 계산은 분석 기법을 다루는 하드 스킬이다.', '정답이다. 관점 통합과 설득적 전달은 사람과 사람 사이의 협업·소통 역량이므로 소프트 스킬에 해당한다.', '알고리즘 구현은 프로그래밍 능력을 다루는 하드 스킬이다.']}
/>

<Quiz
  questionNumber={7}
  question="CRISP-DM 방법론의 표준 단계를 올바른 순서로 나열한 것은 무엇인가"
  options={['데이터 이해 → 업무 이해 → 모델링 → 평가 → 배포', '업무 이해 → 데이터 이해 → 데이터 준비 → 모델링 → 평가 → 배포', '데이터 준비 → 업무 이해 → 모델링 → 배포 → 평가', '모델링 → 데이터 이해 → 업무 이해 → 평가 → 배포']}
  correctAnswer={2}
  explanation="CRISP-DM은 업무 이해로 시작해 데이터 이해, 데이터 준비, 모델링, 평가를 거쳐 배포로 끝나는 여섯 단계로 구성된다. 업무 목적을 먼저 정하지 않으면 이후 단계의 기준을 세울 수 없다."
  optionExplanations={['업무 이해 없이 데이터 이해부터 시작하면 무엇을 분석할지 기준이 없는 상태로 출발하게 되어 순서가 틀렸다.', '정답이다. 업무 이해에서 시작해 데이터 이해, 준비, 모델링, 평가, 배포로 이어지는 순서가 CRISP-DM의 표준 흐름이다.', '데이터 준비가 업무 이해보다 먼저 오면 목적 없이 자료부터 다듬게 되어 순서가 틀렸다.', '모델링을 가장 먼저 두면 업무 목적과 데이터 이해 없이 모형부터 만드는 셈이 되어 순서가 틀렸다.']}
/>

<Quiz
  questionNumber={8}
  question="한 콜센터가 특별한 목표 없이 상담 통화 기록을 모아 탐색하던 중, 특정 문의 유형 뒤에 해지가 몰린다는 패턴을 발견하고 이를 바탕으로 분석 과제를 정의했다. 이 과제 발굴 방식은 무엇인가"
  options={['하향식 접근', '상향식 접근', '기법 우선 접근', '무작위 탐색']}
  correctAnswer={2}
  explanation="사업 문제를 먼저 정하지 않고 데이터를 탐색하는 과정에서 새로운 패턴과 과제를 발견했으므로 상향식 접근에 해당한다. 하향식은 사업 문제를 먼저 정의한 뒤 필요한 데이터를 거꾸로 찾아가는 방식이다."
  optionExplanations={['하향식은 사업 문제와 목표를 먼저 정의한 뒤 데이터를 찾는 방식이며 이 사례는 반대로 데이터 탐색이 먼저였다.', '정답이다. 목표 없이 데이터를 탐색하다 패턴을 발견해 과제를 정의하는 흐름은 상향식 접근의 정의에 정확히 맞는다.', '기법 우선 접근이라는 표준 용어는 이 분류 체계에 속하지 않는 임의의 명칭이다.', '무작위 탐색은 이 문제에서 다루는 과제 발굴 방법론의 정식 명칭이 아니다.']}
/>

<Quiz
  questionNumber={9}
  question="한 물류 회사가 최신 경로 최적화 알고리즘을 이미 채택했지만, 어떤 배송 구역에 적용하고 어떤 사업 목표를 달성할지는 아직 정하지 못했다. 이 상태에 해당하는 분석 과제 유형은 무엇인가"
  options={['최적화형', '솔루션형', '통찰형', '발견형']}
  correctAnswer={3}
  explanation="분석 방법은 이미 알고 있지만 적용 대상과 목표는 아직 모르는 상태는 통찰형 과제에 해당한다. 방법과 목표를 모두 아는 경우는 최적화형, 목표만 알고 방법을 모르는 경우는 솔루션형, 방법과 목표를 모두 모르는 경우는 발견형이다."
  optionExplanations={['최적화형은 목표와 방법을 모두 알고 있는 상태를 가리키며 이 사례는 목표가 정해지지 않았다.', '솔루션형은 목표는 알지만 방법을 모르는 상태를 가리키며 이 사례는 방법이 먼저 정해진 반대 상황이다.', '정답이다. 방법은 확정했지만 대상과 목표를 아직 모르는 상태는 통찰형 과제의 정의와 정확히 맞는다.', '발견형은 방법과 목표를 모두 모르는 상태를 가리키며 이 사례는 방법을 이미 알고 있다.']}
/>

<Quiz
  questionNumber={10}
  question="다음 중 데이터 3법에 속하는 법률로 옳지 않은 것은 무엇인가"
  options={['개인정보 보호법', '정보통신망 이용촉진 및 정보보호 등에 관한 법률', '신용정보의 이용 및 보호에 관한 법률', '공공데이터의 제공 및 이용 활성화에 관한 법률']}
  correctAnswer={4}
  explanation="데이터 3법은 개인정보 보호법, 정보통신망법, 신용정보법 세 가지를 가리킨다. 공공데이터법은 공공기관 데이터의 개방·활용을 다루는 별도 법률로 데이터 3법 개정 묶음에 포함되지 않는다."
  optionExplanations={['개인정보 보호법은 데이터 3법의 핵심 법률이므로 옳은 항목이다.', '정보통신망법은 데이터 3법에 포함되는 법률이므로 옳은 항목이다.', '신용정보법은 데이터 3법에 포함되는 법률이므로 옳은 항목이다.', '정답이다. 공공데이터법은 공공기관 데이터 개방을 다루는 별개의 법률로 데이터 3법 개정 묶음에 속하지 않는다.']}
/>

<Quiz
  questionNumber={11}
  question="설문 응답자의 이름을 임의 코드로 바꾼 파일이 있다. 이 파일만으로는 개인을 알아보기 어렵지만, 별도로 보관 중인 이름-코드 대응표와 결합하면 다시 식별할 수 있다. 이 정보의 성격으로 옳은 것은 무엇인가"
  options={['익명정보', '가명정보', '공개정보', '통계정보']}
  correctAnswer={2}
  explanation="추가 정보인 대응표와 결합하면 개인을 다시 식별할 수 있도록 처리된 정보는 가명정보다. 대응표까지 완전히 폐기되어 어떤 수단으로도 재식별할 수 없는 상태가 되어야 익명정보로 분류된다."
  optionExplanations={['익명정보는 어떤 추가 정보와 결합해도 재식별이 불가능한 상태를 가리키며 이 사례는 대응표로 재식별이 가능하므로 해당하지 않는다.', '정답이다. 대응표라는 추가 정보와 결합해 재식별할 수 있는 상태이므로 가명정보의 정의와 정확히 맞는다.', '공개정보는 이미 공개적으로 열람 가능한 정보를 뜻하며 이 사례와는 무관하다.', '통계정보는 개별 값이 아니라 집계된 수치를 가리키는 개념으로 이 사례에 해당하지 않는다.']}
/>

<Quiz
  questionNumber={12}
  question="비식별 처리 기법과 적용 사례를 짝지은 것 중 옳지 않은 것은 무엇인가"
  options={['라운딩 - 나이 34세를 30세로 반올림해 표시한다', '범주화 - 진료 건수를 0에서 5건, 6에서 10건처럼 구간으로 묶어 표시한다', '데이터 마스킹 - 휴대전화 번호 뒤 네 자리를 별표로 가려 표시한다', '총계처리 - 카드 번호 뒤 네 자리를 임의의 다른 숫자로 치환한다']}
  correctAnswer={4}
  explanation="총계처리는 개별 값 대신 집단 합계나 평균 같은 통계값으로 바꾸어 제공하는 기법이다. 카드 번호 뒤 네 자리를 다른 숫자로 치환하는 것은 개별 식별자를 가리는 마스킹에 해당하므로 총계처리로 서술한 4번이 옳지 않다."
  optionExplanations={['라운딩은 값을 일정 단위로 반올림해 정밀도를 낮추는 기법이며 사례와 정확히 일치한다.', '범주화는 연속된 값을 구간으로 묶는 기법이며 사례와 정확히 일치한다.', '데이터 마스킹은 식별 가능한 값 일부를 다른 문자로 가리는 기법이며 사례와 정확히 일치한다.', '정답이다. 개별 식별자의 일부를 다른 값으로 바꾸는 것은 마스킹이며, 총계처리는 개별 값이 아니라 집단 단위의 합계·평균으로 바꾸는 기법이므로 이 사례와 이름이 맞지 않는다.']}
/>

<Quiz
  questionNumber={13}
  question="데이터 품질 요소와 그 설명을 짝지은 것 중 옳지 않은 것은 무엇인가"
  options={['완전성 - 반드시 있어야 할 값이 빠짐없이 채워져 있는 정도', '정확성 - 기록된 값이 실제 대상의 상태를 바르게 반영하는 정도', '일관성 - 서로 다른 시스템에 있는 같은 항목의 값이 모순되지 않는 정도', '적시성 - 분석자가 다루기 편한 열만 남기고 나머지를 지운 정도']}
  correctAnswer={4}
  explanation="적시성은 필요한 시점에 데이터를 사용할 수 있는 정도를 뜻한다. 분석자가 편한 열만 남기는 행위는 어떤 품질 차원의 정의와도 맞지 않으며, 특히 적시성과는 아무 관련이 없다."
  optionExplanations={['완전성은 필수 값의 누락 여부를 다루는 차원이므로 옳은 설명이다.', '정확성은 값이 실제 상태를 바르게 반영하는지를 다루는 차원이므로 옳은 설명이다.', '일관성은 시스템 간 값의 모순 여부를 다루는 차원이므로 옳은 설명이다.', '정답이다. 적시성은 필요한 시점에 데이터를 쓸 수 있는지를 뜻하며, 열을 임의로 골라 남기는 행위와는 관련이 없다.']}
/>

<Quiz
  questionNumber={14}
  question="다음 중 비정형 데이터로 보기 어려운 것은 무엇인가"
  options={['고객 상담 통화의 녹음 파일', '작업 현장을 촬영한 이미지 파일', '관계형 테이블에 저장된 일별 매출 금액 열', '자유 형식으로 작성된 민원 보고서 문서']}
  correctAnswer={3}
  explanation="관계형 테이블의 숫자 열은 미리 정의된 스키마와 자료형을 갖는 정형 데이터다. 녹음 파일, 이미지, 자유 형식 문서는 모두 고정된 행과 열 구조로 표현하기 어려운 비정형 데이터에 속한다."
  optionExplanations={['녹음 파일은 고정된 구조가 없는 비정형 데이터이므로 이 문제에서 찾는 답이 아니다.', '이미지 파일은 고정된 구조가 없는 비정형 데이터이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 관계형 테이블의 매출 금액 열은 열과 자료형이 사전에 정의된 정형 데이터이므로 비정형과는 반대다.', '자유 형식 문서는 고정된 구조가 없는 비정형 데이터이므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={15}
  question="분산 파일 시스템의 특징에 대한 설명으로 가장 적절한 것은 무엇인가"
  options={['한 대의 고성능 서버에 저장 장치를 집중시켜 단일 장애점을 없앤다', '큰 파일을 일정 크기 블록으로 나누어 여러 노드에 분산 저장하고 각 노드가 병렬로 읽어 처리량을 높인다', '여러 사용자가 같은 행을 동시에 수정할 때 잠금으로 트랜잭션 일관성을 보장하는 것이 주된 목적이다', '저장한 파일을 임의 위치에서 잦게 부분 수정하는 작업에 최적화되어 있다']}
  correctAnswer={2}
  explanation="분산 파일 시스템은 파일을 블록 단위로 쪼개 여러 노드에 흩어 저장하고, 각 노드가 자기 블록을 동시에 읽어 처리량을 끌어올리는 저장 계층이다. 단일 서버 집중, 트랜잭션 동시성 제어, 잦은 임의 수정은 다른 계층이 담당하는 역할이다."
  optionExplanations={['단일 서버에 저장을 집중시키는 것은 오히려 단일 장애점을 만드는 방식이며 분산 파일 시스템의 취지와 반대다.', '정답이다. 블록 단위 분산 저장과 노드별 병렬 읽기로 처리량을 높이는 것이 분산 파일 시스템의 핵심 특징이다.', '행 단위 잠금과 트랜잭션 일관성은 관계형 데이터베이스가 담당하는 역할이며 분산 파일 시스템의 주된 목적이 아니다.', '분산 파일 시스템은 대용량 순차 처리에 강점이 있으며 잦은 임의 부분 수정에는 최적화되어 있지 않다.']}
/>

<Quiz
  questionNumber={16}
  question="레코드마다 저장할 속성 구성이 달라질 수 있는 JSON 형태의 상품 카탈로그를 저장하기에 가장 적합한 NoSQL 유형은 무엇인가"
  options={['문서 지향 데이터베이스', '컬럼 패밀리 데이터베이스', '그래프 데이터베이스', '키-값 데이터베이스']}
  correctAnswer={1}
  explanation="레코드마다 속성 구성이 달라질 수 있는 JSON 문서를 그대로 저장하고 조회하는 데는 문서 지향 데이터베이스가 적합하다. 컬럼 패밀리는 대량의 열 지향 집계, 그래프는 관계 탐색, 키-값은 단순 조회에 강점이 있다."
  optionExplanations={['정답이다. 문서 지향 데이터베이스는 레코드마다 다른 필드 구성을 가진 JSON 문서를 그대로 저장하도록 설계되었다.', '컬럼 패밀리 데이터베이스는 대량의 데이터를 열 단위로 저장해 집계에 강하지만 가변 구조의 문서 저장에는 적합하지 않다.', '그래프 데이터베이스는 노드와 관계 탐색에 특화되어 있어 상품 카탈로그 저장이라는 목적과 맞지 않는다.', '키-값 데이터베이스는 단일 키로 값을 빠르게 조회하는 데 강점이 있지만 문서 내부 구조를 다루는 데는 적합하지 않다.']}
/>

<Quiz
  questionNumber={17}
  question="한 보험사가 사내 관계형 데이터베이스의 계약 원장 테이블을 분산 저장 환경으로 옮기고, 배치 처리가 끝나면 다시 관계형 데이터베이스로 되돌리려 한다. 이처럼 관계형 저장소와 분산 저장 환경 사이에서 정형 자료를 양방향으로 옮기는 데 쓰이는 수집 기술은 무엇인가"
  options={['Flume', 'Sqoop', 'FTP', 'Open API']}
  correctAnswer={2}
  explanation="Sqoop은 관계형 데이터베이스와 분산 저장 환경 사이에서 정형 자료를 양방향으로 옮기도록 만들어진 수집 도구다. Flume은 로그 스트림 수집, FTP는 파일 전송, Open API는 외부 서비스가 공개한 인터페이스를 통한 수집에 쓰인다."
  optionExplanations={['Flume은 로그처럼 계속 흘러 들어오는 스트림 자료를 수집하는 도구이며 정형 테이블 이관 목적과는 다르다.', '정답이다. Sqoop은 관계형 데이터베이스와 분산 저장 환경 사이에서 정형 자료를 양방향으로 이관하도록 설계된 도구다.', 'FTP는 파일을 서버 간 전송하는 범용 프로토콜이며 정형 테이블의 구조를 이해하고 옮기는 전용 도구가 아니다.', 'Open API는 외부 서비스가 공개한 인터페이스를 통해 자료를 받아오는 방식이며 사내 시스템 간 이관과는 성격이 다르다.']}
/>

<Quiz
  questionNumber={18}
  question="데이터 웨어하우스의 전통적 특성에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['주제 지향적으로 고객, 상품처럼 업무 주제를 중심으로 자료를 구성한다', '여러 시스템에서 온 자료의 코드와 단위를 통합된 기준으로 맞춘다', '한 번 적재된 자료는 임의로 갱신되지 않는 비휘발성을 갖는다', '실시간으로 끊임없이 값이 바뀌는 휘발성 저장을 전통적 특성으로 삼는다']}
  correctAnswer={4}
  explanation="데이터 웨어하우스의 전통적 네 특성은 주제 지향성, 통합성, 비휘발성, 시계열성(시간에 따른 변화 이력 보존)이다. 실시간으로 값이 계속 바뀌는 휘발성 저장은 오히려 이 특성과 반대되므로 옳지 않다."
  optionExplanations={['주제 지향성은 데이터 웨어하우스의 전통적 특성이므로 옳은 설명이다.', '통합성은 데이터 웨어하우스의 전통적 특성이므로 옳은 설명이다.', '비휘발성은 데이터 웨어하우스의 전통적 특성이므로 옳은 설명이다.', '정답이다. 데이터 웨어하우스는 비휘발성을 특징으로 하며, 실시간으로 값이 계속 바뀌는 휘발성 저장은 이 특성과 정반대다.']}
/>

<Quiz
  questionNumber={19}
  question="여러 업무 시스템에서 자료를 뽑아내고 코드 체계와 단위를 목표 형식에 맞게 바꾼 뒤 목적지 저장소에 적재하는 일련의 처리 과정을 가리키는 명칭은 무엇인가"
  options={['데이터 마트', 'ETL', '데이터 레이크', 'OLAP']}
  correctAnswer={2}
  explanation="추출(Extract), 변환(Transform), 적재(Load)로 이어지는 처리 과정을 가리키는 명칭이 ETL이다. 데이터 마트와 데이터 레이크는 그 처리 결과가 쌓이는 저장소이고, OLAP은 다차원 분석을 지원하는 조회 방식이므로 처리 절차와 층위가 다르다."
  optionExplanations={['데이터 마트는 특정 부서나 주제에 맞춰 축소된 데이터 웨어하우스로, 처리 과정이 아니라 저장소를 가리키는 명칭이다.', '정답이다. 추출·변환·적재로 이어지는 처리 과정을 가리키는 명칭이 ETL이다.', '데이터 레이크는 원본 형태를 유지한 채 저장하는 저장소를 가리키는 명칭이지 처리 절차가 아니다.', 'OLAP은 다차원 관점에서 자료를 조회·분석하는 방식을 가리키며 추출·변환·적재 절차와는 다른 개념이다.']}
/>

<Quiz
  questionNumber={20}
  question="분석 의뢰를 접수한 팀이 산출물을 차례로 만들려 한다. ㄱ. 세부 일정과 투입 자원이 적힌 수행 계획서 ㄴ. 사업 범위와 배경이 적힌 문제 정의서 ㄷ. 과제 우선순위와 적용 시점을 담은 마스터플랜 ㄹ. 계획에 따라 나눈 작업 단위별 담당과 산출물 목록(WBS) 이 넷을 합리적인 순서로 인계한다면 어떤 순서가 맞는가"
  options={['ㄴ → ㄷ → ㄱ → ㄹ', 'ㄷ → ㄴ → ㄹ → ㄱ', 'ㄱ → ㄹ → ㄴ → ㄷ', 'ㄹ → ㄱ → ㄷ → ㄴ']}
  correctAnswer={1}
  explanation="사업 범위와 문제를 먼저 정의하고, 그 문제를 어떤 과제로 언제 추진할지 정하는 마스터플랜을 세운 뒤, 개별 과제의 세부 일정과 자원을 담은 수행 계획서를 작성하고, 마지막으로 계획을 작업 단위로 쪼갠 WBS를 만드는 순서가 합리적이다."
  optionExplanations={['정답이다. 문제 정의(ㄴ)로 범위를 확정한 뒤 마스터플랜(ㄷ)으로 추진 시점을 정하고, 수행 계획서(ㄱ)로 세부 자원을 배정한 뒤 WBS(ㄹ)로 작업을 쪼개는 순서가 합리적이다.', '마스터플랜을 문제 정의보다 먼저 두면 사업 범위도 모른 채 과제 우선순위부터 정하는 셈이 되어 순서가 어긋난다.', '수행 계획서를 가장 먼저 두면 사업 범위와 우선순위를 정하지 않은 채 세부 일정부터 짜는 셈이 되어 순서가 어긋난다.', 'WBS를 가장 먼저 두면 상위 계획이 없는 상태에서 작업 단위부터 나누는 셈이 되어 순서가 어긋난다.']}
/>

## 2과목: 빅데이터 탐색 (21~40)

<Quiz
  questionNumber={21}
  question="한 렌터카 업체가 예약 이력 자료의 전처리를 정제, 통합, 축소, 변환 네 단계로 나누어 설계했다. 각 단계에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['정제 단계에서는 반납 시각이 비어 있거나 대여 일수가 음수로 기록된 값을 찾아 보정하거나 제외한다', '통합 단계에서는 예약 시스템과 정비 이력 시스템에 나뉜 기록을 공통 키로 결합해 하나의 분석용 자료로 만든다', '축소 단계에서는 주행 거리에 섞인 잡음을 걸러 내고 측정 단위를 킬로미터로 통일해 척도를 맞춘다', '변환 단계에서는 대여 금액에 로그를 취하거나 대여 일수를 구간으로 나누어 분석에 쓰기 좋은 형태로 바꾼다']}
  correctAnswer={3}
  explanation="축소는 분석에 필요한 정보를 유지하면서 관측치 수나 변수 개수 같은 자료의 규모를 줄이는 단계다. 잡음 제거와 척도 통일은 정제와 변환에 속하는 작업이므로 이를 축소의 목적으로 서술한 3번이 옳지 않다."
  optionExplanations={['결측 및 오류값 보정은 정제 단계의 정의와 정확히 일치하므로 옳은 설명이다.', '서로 다른 시스템의 기록을 공통 키로 합치는 것은 통합 단계의 정의와 정확히 일치하므로 옳은 설명이다.', '정답이다. 잡음 제거와 단위 통일은 정제·변환의 목적이며, 축소는 관측치나 변수의 규모를 줄이는 단계이므로 이 설명은 단계를 잘못 짝지었다.', '로그 변환과 구간화는 변환 단계의 정의와 정확히 일치하므로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={22}
  question="건강검진 자료의 결측값 처리 기법에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['완전 분석법은 결측이 하나라도 있는 관측치를 통째로 제외하고 모든 항목이 채워진 사례만 분석에 쓴다', '다중 대치법은 결측 자리를 여러 벌로 서로 다르게 채운 뒤 각각 분석해 그 결과를 결합하는 기법이다', '회귀 대치법은 결측이 없는 다른 항목들로 회귀식을 세운 뒤 그 예측값을 결측 자리에 채워 넣는다', '단순 확률 대치법은 평균이나 중앙값 하나만을 결측 자리에 채워 넣어 계산이 가장 단순한 기법이다']}
  correctAnswer={4}
  explanation="단순 확률 대치법은 대치값에 확률적 오차를 함께 더해 채워 넣어, 평균이나 중앙값으로만 채울 때 분산이 지나치게 작아지는 문제를 줄이는 기법이다. 오차 없이 대푯값 하나만 채운다고 서술한 4번은 단순 대치법을 잘못 설명한 것이다."
  optionExplanations={['완전 분석법은 결측이 있는 사례를 통째로 제외하는 방식이 맞으므로 옳은 설명이다.', '다중 대치법은 여러 벌의 값을 채워 결과를 결합하는 방식이 맞으므로 옳은 설명이다.', '회귀 대치법은 다른 변수로 예측한 값을 채우는 방식이 맞으므로 옳은 설명이다.', '정답이다. 단순 확률 대치법은 대푯값에 확률적 오차를 더해 채우는 기법이며, 오차 없이 대푯값만 채우는 방식과는 다르다.']}
/>

<Quiz
  questionNumber={23}
  question="설비 압력 측정치 1,500건 가운데 양 끝 극단에 놓인 30건을 이상값으로 판정해 제거할지 검토하고 있다. 이상값 제거에 대한 설명으로 옳은 것은 무엇인가"
  options={['이상값을 제거하면 남은 자료의 산포는 줄어들지만, 특정 방향의 관측치만 사라져 추정치가 한쪽으로 치우칠 위험은 오히려 커진다', '이상값을 제거하면 산포는 커지는 대신 추정치가 치우칠 위험은 완전히 사라진다', '이상값은 비중이 작으므로 발생 원인을 확인하지 않고 그대로 두어도 요약통계에는 영향이 없다', '이상값 자리에 평균을 채워 넣는 방식은 산포를 왜곡하지 않으므로 항상 안전하다']}
  correctAnswer={1}
  explanation="극단값을 덜어 내면 분산과 범위 같은 산포는 작아지지만, 특정 방향의 관측치만 빠지므로 평균 같은 추정치는 반대쪽으로 치우칠 수 있다. 산포 감소와 편향 증가 가능성을 함께 짚은 1번이 옳다."
  optionExplanations={['정답이다. 극단값 제거는 산포를 줄이지만 특정 방향의 값만 빠지면 추정치가 편향될 위험이 커진다는 점을 정확히 짚었다.', '이상값 제거는 산포를 키우는 것이 아니라 줄이는 방향으로 작용하며 편향 위험이 사라진다고 단정할 수도 없다.', '이상값의 발생 원인을 확인하지 않고 방치하면 평균이나 분산 같은 요약통계가 왜곡될 수 있으므로 영향이 없다는 서술은 틀렸다.', '평균으로 대체하는 방식은 결측값 대치와 마찬가지로 자료의 산포를 실제보다 작게 왜곡할 수 있으므로 항상 안전하다고 볼 수 없다.']}
/>

<Quiz
  questionNumber={24}
  question="다음 중 연속형 변수로 보기 어려운 것은 무엇인가"
  options={['실험 참가자의 체온을 섭씨 0.1도 단위까지 측정한 값', '배송 차량이 하루 동안 주행한 거리를 킬로미터로 기록한 값', '고객이 이용한 결제 수단을 1부터 4까지의 정수 코드로 저장한 값', '제품 표면의 두께를 마이크로미터 단위로 측정한 값']}
  correctAnswer={3}
  explanation="결제 수단은 이름표에 번호를 붙인 것일 뿐 값 사이의 크기 비교나 사칙연산이 의미를 갖지 않으므로 숫자로 저장되어 있어도 명목형 범주 변수다."
  optionExplanations={['체온은 소수점까지 측정되는 연속형 변수이므로 이 문제에서 찾는 답이 아니다.', '주행 거리는 연속적으로 측정되는 연속형 변수이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 결제 수단 코드는 숫자로 저장되어 있어도 범주를 구분하는 이름표일 뿐이어서 명목형 변수에 해당한다.', '두께는 연속적으로 측정되는 연속형 변수이므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={25}
  question="다음 중 파생변수 생성의 사례로 보기 어려운 것은 무엇인가"
  options={['가입일과 첫 구매일의 차이로 첫 구매까지 걸린 일수를 계산한다', '수량과 단가를 곱해 주문 금액을 계산한다', '위도와 경도 정보를 이용해 배송 권역 범주를 만든다', '원본 나이 열의 값을 계산 없이 그대로 나이_사본 열에 복사한다']}
  correctAnswer={4}
  explanation="파생변수 생성은 기존 변수를 조합하거나 계산해 새로운 정보를 담은 변수를 만드는 작업이다. 값을 그대로 복사하는 것은 이름만 늘어날 뿐 새로운 정보를 만들지 않으므로 파생변수 생성으로 볼 수 없다."
  optionExplanations={['두 날짜의 차이를 계산해 새 의미의 변수를 만든 것이므로 파생변수 생성의 예에 해당한다.', '두 값을 곱해 새로운 의미의 변수를 만든 것이므로 파생변수 생성의 예에 해당한다.', '두 변수를 조합해 새로운 범주를 만든 것이므로 파생변수 생성의 예에 해당한다.', '정답이다. 계산 없이 값을 그대로 복사하는 것은 새로운 정보를 만들지 않으므로 파생변수 생성이 아니다.']}
/>

**문항 26 자료**

| 설비 소음 측정값(dB) | 5 | 10 | 15 | 25 |
|---|---|---|---|---|

<Quiz
  questionNumber={26}
  question="위 표의 네 값에 최소-최대 정규화 공식을 각각 적용할 때, 변환된 네 값의 합은 얼마인가"
  options={['1.75', '1.50', '2.00', '1.25']}
  correctAnswer={1}
  explanation="최솟값은 5, 최댓값은 25이므로 분모는 20이다. 변환값은 각각 (5-5)/20=0, (10-5)/20=0.25, (15-5)/20=0.5, (25-5)/20=1이며 이 네 값의 합은 0+0.25+0.5+1=1.75다."
  optionExplanations={['정답이다. 최솟값 5, 최댓값 25로 정규화하면 네 값은 0, 0.25, 0.5, 1이 되고 그 합은 1.75다.', '분모를 20이 아니라 다른 값으로 잘못 나누거나 한 값의 계산을 빠뜨리면 나올 수 있는 오답이다.', '변환값을 모두 반올림해 더하거나 분모를 실제보다 작게 잡았을 때 나올 수 있는 오답이다.', '최댓값 25를 그대로 분자에 넣는 등 공식의 분자와 분모를 혼동했을 때 나올 수 있는 오답이다.']}
/>

**문항 27 자료**

| 구분 | 개수 |
|---|---|
| 연속형 설명변수 | 3개 |
| 범주형 설명변수 A(범주 5개) | 1개 |
| 범주형 설명변수 B(범주 4개) | 1개 |

<Quiz
  questionNumber={27}
  question="월 매출을 예측하는 회귀모형에 위 표의 설명변수를 모두 넣으려 한다. 범주형 변수는 각각 기준 범주 하나를 제외하고 나머지를 지시변수로 바꾼다. 절편을 포함할 때 이 모형이 추정하는 모수는 모두 몇 개인가"
  options={['9개', '10개', '11개', '12개']}
  correctAnswer={3}
  explanation="지시변수는 범주 5개인 A에서 4개, 범주 4개인 B에서 3개가 만들어진다. 여기에 연속형 3개와 절편 1개를 더하면 3+4+3+1=11개다."
  optionExplanations={['범주형 변수의 지시변수 개수를 범주 수 그대로(5개, 4개) 넣거나 절편을 빠뜨리면 나올 수 있는 오답이다.', '지시변수 개수 중 하나를 하나 적게 세거나 절편을 빠뜨리면 나올 수 있는 값이다.', '정답이다. 지시변수 4개(A)와 3개(B), 연속형 3개, 절편 1개를 모두 더하면 11개다.', '두 범주형 변수 모두 기준 범주를 빼지 않고 범주 수 그대로(5개, 4개) 셌을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={28}
  question="신용카드 거래 80만 건 가운데 사기 거래가 0.3퍼센트인 자료로 사기 탐지 분류 모형을 학습하려 한다. 클래스 불균형에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['다수 범주 쪽으로 치우쳐 학습되면 사기 거래를 거의 찾아내지 못해도 전체 정확도는 매우 높게 나타날 수 있다', '소수 범주의 표본을 늘리거나 다수 범주의 표본을 줄여 두 범주의 관측치 수를 조정해 대응할 수 있다', '소수 범주를 잘못 분류했을 때 더 큰 비용을 물리도록 가중치를 조정하는 방식으로도 대응할 수 있다', '불균형의 정도는 범주별 구성 비율과는 무관하며 전체 표본 크기가 얼마나 큰지에 따라 정해진다']}
  correctAnswer={4}
  explanation="클래스 불균형은 범주별 구성 비율이 한쪽으로 치우친 상태를 가리키는 개념이다. 전체 표본 크기가 아무리 커도 두 범주의 비율 차이가 곧 불균형의 정도를 결정한다."
  optionExplanations={['소수 범주를 거의 맞히지 못해도 정확도가 높게 나오는 함정은 클래스 불균형의 대표적 문제이므로 옳은 설명이다.', '표본 수 조정(오버샘플링·언더샘플링)은 클래스 불균형에 대한 표준 대응법이므로 옳은 설명이다.', '비용 민감 학습은 클래스 불균형에 대한 표준 대응법이므로 옳은 설명이다.', '정답이다. 클래스 불균형은 범주별 구성 비율의 문제이며 전체 표본 크기와는 별개의 개념이므로 이 서술이 틀렸다.']}
/>

<Quiz
  questionNumber={29}
  question="가구별 월 소비지출 자료의 산포와 분포 형태를 여러 통계량으로 살펴보려 한다. 각 통계량에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['사분위범위는 제3사분위수에서 제1사분위수를 뺀 값으로, 가운데 절반의 관측치가 퍼진 폭을 나타낸다', '왜도는 분포가 좌우 어느 쪽으로 치우쳤는지를 나타내며 0보다 크면 오른쪽으로 긴 꼬리를 갖는 모양이다', '첨도는 분포의 봉우리가 얼마나 뾰족한지를 재는 값으로 꼬리의 두께와 함께 해석한다', '변동계수는 평균을 표준편차로 나눈 값으로 단위가 다른 자료의 산포를 비교할 때 쓴다']}
  correctAnswer={4}
  explanation="변동계수는 표준편차를 평균으로 나눈 값이다. 분자와 분모를 뒤바꿔 평균을 표준편차로 나눈다고 서술한 4번이 정의를 잘못 진술했다."
  optionExplanations={['사분위범위의 정의는 제3사분위수에서 제1사분위수를 뺀 값이 맞으므로 옳은 설명이다.', '왜도가 0보다 크면 오른쪽으로 긴 꼬리를 갖는다는 것은 옳은 설명이다.', '첨도는 봉우리의 뾰족함과 꼬리 두께를 함께 나타내는 지표라는 것은 옳은 설명이다.', '정답이다. 변동계수는 표준편차를 평균으로 나눈 값인데, 이 설명은 분자와 분모를 뒤바꾸어 평균을 표준편차로 나눈다고 잘못 서술했다.']}
/>

**문항 30 자료**

| 관측 순서 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 무인 매장 하루 방문객 수(명) | 6 | 9 | 10 | 12 | 13 |

<Quiz
  questionNumber={30}
  question="위 표의 다섯 값을 표본으로 볼 때 표본평균과 표본분산을 바르게 구한 것은 무엇인가"
  options={['표본평균 10, 표본분산 6.5', '표본평균 10, 표본분산 7.5', '표본평균 9.5, 표본분산 8', '표본평균 10, 표본분산 30']}
  correctAnswer={2}
  explanation="합이 50이므로 표본평균은 50/5=10이다. 각 편차는 -4, -1, 0, 2, 3이고 제곱합은 16+1+0+4+9=30이다. 표본분산은 이 값을 n-1인 4로 나눈 30/4=7.5다."
  optionExplanations={['편차 제곱합을 30이 아니라 26으로 잘못 계산했을 때 나올 수 있는 오답이다.', '정답이다. 평균 10, 편차 제곱합 30을 자유도 4로 나누면 표본분산은 7.5다.', '평균을 잘못 계산하고 분산도 다른 값으로 나눈 경우에 나올 수 있는 오답이다.', '편차 제곱합 30을 n-1로 나누지 않고 그대로 분산으로 제시한 오답이다.']}
/>

<Quiz
  questionNumber={31}
  question="어떤 확률변수 X가 성공 확률 0.4인 독립 시행을 12번 반복할 때의 성공 횟수를 나타낸다. X가 따르는 분포로 옳은 것은 무엇인가"
  options={['정규분포', '포아송분포', '이항분포', '초기하분포']}
  correctAnswer={3}
  explanation="고정된 횟수의 독립적인 베르누이 시행에서 성공 횟수를 나타내는 확률변수는 이항분포를 따른다. 포아송분포는 단위 시간당 사건 발생 횟수, 초기하분포는 비복원추출 상황에서 쓰인다."
  optionExplanations={['정규분포는 연속형 확률변수를 나타내는 분포이며 이산적인 성공 횟수를 직접 나타내지 않는다.', '포아송분포는 단위 구간에서 드물게 발생하는 사건의 횟수를 나타내는 분포이며 고정 횟수 시행과는 다른 상황이다.', '정답이다. 고정된 횟수의 독립 시행에서 성공 횟수를 나타내는 확률변수는 이항분포를 따른다.', '초기하분포는 유한 모집단에서 복원하지 않고 추출할 때 쓰이며 이 사례처럼 각 시행이 독립인 경우와는 다르다.']}
/>

<Quiz
  questionNumber={32}
  question="정규분포에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['평균을 중심으로 좌우가 대칭이며 평균과 중앙값, 최빈값이 모두 같은 값을 갖는다', '정규분포의 왜도는 0이고 첨도는 3이다', '확률밀도함수 곡선과 가로축 사이의 전체 넓이는 1이다', '분산을 그대로 둔 채 평균만 바꾸면 곡선의 폭과 모양이 함께 넓어진다']}
  correctAnswer={4}
  explanation="분산을 고정한 채 평균만 바꾸면 곡선의 모양과 폭은 그대로 유지된 채 좌우 위치만 옮겨진다. 폭이 함께 넓어진다고 서술한 4번이 옳지 않다."
  optionExplanations={['정규분포가 좌우 대칭이고 평균, 중앙값, 최빈값이 일치한다는 것은 옳은 설명이다.', '정규분포의 왜도가 0이고 첨도가 3이라는 것은 옳은 설명이다.', '확률밀도함수 곡선 아래 전체 넓이가 1이라는 것은 옳은 설명이다.', '정답이다. 평균만 바꾸면 곡선은 모양과 폭이 그대로인 채 위치만 옮겨지며, 폭이 넓어진다는 서술은 분산을 바꾸는 경우와 혼동한 것이다.']}
/>

<Quiz
  questionNumber={33}
  question="전국 대학생의 하루 학습 시간을 알아보기 위해 표본 500명을 뽑아 표본평균을 구했다. 표본평균의 표집분포에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['표본평균의 표집분포란 같은 크기의 표본을 되풀이해 뽑았을 때 그 표본평균들이 이루는 분포를 말한다', '모집단의 분포가 정규분포가 아니더라도 표본 크기가 충분히 크면 표본평균의 분포는 정규분포에 가까워진다', '표본평균의 기대값은 모집단 평균과 같다', '추론통계는 이미 관측을 마친 모집단 전체 자료로부터 아직 뽑지 않은 표본의 값을 알아내는 절차다']}
  correctAnswer={4}
  explanation="추론의 방향은 표본에서 모집단으로 향한다. 모집단 전체를 이미 관측했다면 추론이 필요 없으므로, 방향을 뒤집어 서술한 4번이 옳지 않다."
  optionExplanations={['표집분포의 정의는 옳게 서술되어 있으므로 옳은 설명이다.', '중심극한정리에 대한 설명이 옳게 서술되어 있으므로 옳은 설명이다.', '표본평균이 모평균의 불편추정량이라는 것은 옳은 설명이다.', '정답이다. 추론통계는 표본에서 모집단을 알아내는 절차이며, 모집단을 이미 관측했다는 전제 자체가 방향을 뒤집은 것이다.']}
/>

<Quiz
  questionNumber={34}
  question="다음 중 확률표본을 얻는 방법으로 보기 어려운 것은 무엇인가"
  options={['단순임의추출', '층화추출', '편의추출', '계통추출']}
  correctAnswer={3}
  explanation="편의추출은 조사자가 접근하기 쉬운 대상을 임의로 선택하는 비확률표본추출법이다. 단순임의추출, 층화추출, 계통추출은 모두 각 대상이 뽑힐 확률을 사전에 정할 수 있는 확률표본추출법이다."
  optionExplanations={['단순임의추출은 대표적인 확률표본추출법이므로 이 문제에서 찾는 답이 아니다.', '층화추출은 모집단을 층으로 나눈 뒤 각 층에서 임의로 뽑는 확률표본추출법이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 편의추출은 뽑힐 확률을 정할 수 없는 비확률표본추출법으로, 나머지 세 방법과 성격이 다르다.', '계통추출은 일정 간격으로 표본을 뽑는 확률표본추출법이므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={35}
  question="한 온라인 강의 사이트의 월별 신규 가입자 수를 1월부터 12월까지 이어 그렸다. 1월 500명에서 시작해 7월 2,800명까지 매달 늘었고, 7월을 정점으로 방향이 바뀌어 12월 700명까지 매달 줄었다. 이 그림에 대한 해석으로 가장 적절한 것은 무엇인가"
  options={['7월을 전환점으로 앞뒤 구간을 나누어 보아야 관계가 드러나며 12개월 전체를 하나의 직선으로 요약하면 이 관계를 놓치게 된다', '7월 이후 구간에서도 월이 지날수록 가입자 수가 늘어나므로 두 구간의 증감 방향은 서로 같다', '12개월 전체를 한 덩어리로 보면 월과 가입자 수 사이에 강한 양의 선형관계가 있다고 결론지을 수 있다', '값이 500명에서 2,800명까지 넓게 흩어져 있으므로 이 자료에는 이상값이 다수 섞여 있다고 보아야 한다']}
  correctAnswer={1}
  explanation="7월 앞뒤로 기울기의 부호가 뒤바뀌므로 구간을 나누어 보아야 관계가 드러난다. 전체를 하나의 직선으로 적합하면 상승분과 하락분이 서로 상쇄되어 관계가 없는 것처럼 보인다."
  optionExplanations={['정답이다. 전환점 앞뒤로 증감 방향이 반대이므로 구간을 나누지 않으면 전체 추세를 하나의 직선으로 잘못 요약하게 된다.', '7월 이후에는 가입자 수가 줄어드는 방향이므로 7월 이전의 증가 방향과 반대다.', '증감 방향이 중간에 뒤바뀌는 자료를 하나의 직선으로 요약하면 실제로는 상쇄되어 관계가 약하게 나타난다.', '값이 넓게 퍼진 것은 계절에 따른 정상적인 변동 패턴이며 이상값이 많다는 근거가 되지 않는다.']}
/>

<Quiz
  questionNumber={36}
  question="탐색적 데이터 분석의 성격과 범위에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['그림과 요약값을 함께 써서 분포 모양, 치우침, 동떨어진 값을 먼저 살피는 활동이다', '변수 개수를 줄여 자료가 놓인 구조를 간추려 보는 기법도 탐색 단계에서 활용할 수 있다', '자료가 따를 분포와 모형 구조를 미리 확정해 두고 그 가정에 들어맞는 값만 확인하는 절차다', '같은 자료를 여러 각도로 되풀이해 보면서 새로운 물음과 가설을 얻어 가는 반복적 성격을 지닌다']}
  correctAnswer={3}
  explanation="탐색적 분석은 분포나 모형 구조를 미리 정해 놓고 확인하는 절차가 아니라, 가정을 최소화한 채 자료가 스스로 드러내는 특징을 살피는 활동이다."
  optionExplanations={['그림과 요약값으로 분포 특징을 살피는 것은 탐색적 분석의 정의와 일치하므로 옳은 설명이다.', '차원 축소 기법을 탐색 단계에서 활용할 수 있다는 것은 옳은 설명이다.', '정답이다. 탐색적 분석은 구조를 미리 확정하지 않고 살펴보는 활동이므로 구조를 확정해 둔다는 서술이 옳지 않다.', '탐색 과정이 반복적이고 새로운 가설을 만들어 낸다는 것은 옳은 설명이다.']}
/>

<Quiz
  questionNumber={37}
  question="광고 노출량과 매출액 자료를 놓고 어떤 분석을 할지 정하려 한다. 인과관계 분석에 대한 설명으로 옳은 것은 무엇인가"
  options={['두 변수가 함께 커지거나 작아지는 정도를 -1과 1 사이의 수치 하나로 요약하는 것이 목적이다', '원인으로 보는 설명변수의 변화가 결과로 보는 반응변수를 얼마나 움직이는지, 두 변수의 역할을 미리 정해 두고 따진다', '자료에서 다른 관측치와 크게 동떨어진 값을 찾아내고 그 발생 원인을 규명하는 것이 핵심 목적이다', '두 변수를 각각 순위로 바꾼 뒤 순위끼리 얼마나 일치하는지를 재는 분석이다']}
  correctAnswer={2}
  explanation="인과관계 분석은 어느 변수가 원인이고 어느 변수가 결과인지 방향을 미리 정해 두고 영향의 크기를 따지는 분석이다."
  optionExplanations={['두 변수의 동반 변화를 하나의 수치로 요약하는 것은 상관분석의 목적이며 인과관계 분석과는 다르다.', '정답이다. 원인과 결과의 역할을 미리 정해 두고 영향의 크기를 따지는 것이 인과관계 분석의 정의다.', '동떨어진 값을 찾아 원인을 규명하는 것은 이상치 탐지의 목적이며 인과관계 분석과는 다르다.', '값을 순위로 바꾸어 일치도를 재는 것은 스피어만 상관분석의 방법이며 인과관계 분석과는 다르다.']}
/>

<Quiz
  questionNumber={38}
  question="변수 x를 z=(x-평균)/표준편차 공식으로 바꾸었다. 이 처리의 명칭과 효과로 옳은 것은 무엇인가"
  options={['최소-최대 정규화이며 모든 값을 0과 1 사이로 바꾼다', '표준화이며 평균을 0, 표준편차를 1로 맞춘다', '원-핫 인코딩이며 범주형 변수를 다차원 벡터로 바꾼다', '로그 변환이며 오른쪽으로 치우친 분포를 완만하게 만든다']}
  correctAnswer={2}
  explanation="평균을 빼고 표준편차로 나누는 처리는 표준화(Z-점수 표준화)이며, 그 결과로 변환된 값의 평균은 0, 표준편차는 1이 된다."
  optionExplanations={['최소-최대 정규화는 최솟값과 최댓값을 이용해 값을 0과 1 사이로 바꾸는 다른 공식이므로 이 사례와 다르다.', '정답이다. 평균을 빼고 표준편차로 나누는 처리는 표준화이며 결과 값의 평균은 0, 표준편차는 1이 된다.', '원-핫 인코딩은 범주형 변수를 이진 벡터로 바꾸는 처리이며 이 공식과는 다르다.', '로그 변환은 값에 로그 함수를 적용하는 처리이며 평균과 표준편차를 이용하는 이 공식과는 다르다.']}
/>

<Quiz
  questionNumber={39}
  question="온라인 서점의 주문 자료로 이탈 예측 모형을 만들기 위해 여러 전처리 작업을 검토했다. 데이터 정제 작업에 해당하지 않는 것은 무엇인가"
  options={['결제 금액이 비어 있는 행을 찾아 대치하거나 분석 대상에서 제외한다', '회원 등급이 VIP, vip, V다1처럼 제각각 적힌 값을 하나의 표기로 통일한다', '나이 항목에 들어 있는 음수나 200처럼 정의 범위를 벗어난 값을 오류로 판정해 처리한다', '한쪽으로 치우친 구매 금액에 로그를 취해 분포를 완만하게 만든 새 척도로 바꾼다']}
  correctAnswer={4}
  explanation="정제는 결측, 오류, 표기 불일치처럼 자료에 남은 결함을 바로잡는 작업이다. 로그를 취해 분포 모양을 바꾸는 일은 결함 교정이 아니라 변수 변환이므로 4번은 정제에 해당하지 않는다."
  optionExplanations={['결측값 처리는 정제 작업에 해당하므로 이 문제에서 찾는 답이 아니다.', '표기 불일치 통일은 정제 작업에 해당하므로 이 문제에서 찾는 답이 아니다.', '정의 범위를 벗어난 오류값 처리는 정제 작업에 해당하므로 이 문제에서 찾는 답이 아니다.', '정답이다. 분포 모양을 바꾸는 로그 변환은 결함 교정이 아니라 변수 변환에 해당하므로 정제 작업으로 볼 수 없다.']}
/>

<Quiz
  questionNumber={40}
  question="쇼핑몰 회원의 지역 정보를 층으로 나누고, 각 층에서 정해진 비율만큼 회원을 임의로 뽑아 설문을 진행했다. 이 표본추출법은 무엇인가"
  options={['단순임의추출', '층화추출', '군집추출', '편의추출']}
  correctAnswer={2}
  explanation="모집단을 특성이 비슷한 층으로 나눈 뒤 각 층에서 정해진 비율만큼 임의로 뽑는 방법은 층화추출이다."
  optionExplanations={['단순임의추출은 층을 나누지 않고 전체에서 동일한 확률로 뽑는 방법이며 이 사례와는 다르다.', '정답이다. 지역이라는 층으로 나눈 뒤 각 층에서 비율에 맞게 뽑는 방식은 층화추출의 정의와 정확히 맞는다.', '군집추출은 집단 전체를 통째로 선택해 그 안의 모든 구성원을 조사하는 방법이며 이 사례와는 다르다.', '편의추출은 뽑힐 확률을 정하지 않고 접근하기 쉬운 대상을 뽑는 방법이며 층별 비율을 지킨 이 사례와는 다르다.']}
/>

## 3과목: 빅데이터 모델링 (41~60)

<Quiz
  questionNumber={41}
  question="제조 공정 자료를 다루는 분석팀이 학습 방식에 따라 과제를 분류하고 있다. 다음 중 지도학습 과제로 보기 어려운 것은 무엇인가"
  options={['불량 여부 표지가 붙은 자료로 새 제품의 불량 여부를 예측하는 과제', '판매 이력을 이용해 다음 달 매출액을 예측하는 과제', '구매 이력만으로 비슷한 구매 패턴을 가진 고객을 무리 짓는 과제', '스팸 여부 표지가 붙은 메일로 새 메일의 스팸 여부를 분류하는 과제']}
  correctAnswer={3}
  explanation="정답 표지 없이 자료 자체의 구조를 찾아 무리를 짓는 군집화는 비지도학습 과제에 해당한다. 나머지 셋은 모두 정답 표지가 있는 자료로 학습하는 지도학습 과제다."
  optionExplanations={['불량 여부라는 표지가 있는 지도학습(분류) 과제이므로 이 문제에서 찾는 답이 아니다.', '매출액이라는 연속형 정답이 있는 지도학습(회귀) 과제이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 정답 표지 없이 구매 패턴만으로 무리를 짓는 것은 비지도학습인 군집화에 해당한다.', '스팸 여부라는 표지가 있는 지도학습(분류) 과제이므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={42}
  question="전력 사용량을 기온과 가동 설비 수로 설명하는 선형회귀모형을 세우고 가정 충족 여부를 점검하려 한다. 선형회귀모형의 기본 가정에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['오차항의 분산은 설명변수 값이 달라져도 일정해야 하며 잔차 산점도가 깔때기 모양이면 이 가정이 깨진 것으로 본다', '선형성 가정은 반응변수가 설명변수들의 일차 결합과 오차항의 합으로 표현된다는 뜻이다', '서로 다른 관측치의 오차항은 독립이어야 하며 시간 순서 자료에서는 더빈-왓슨 통계량으로 자기상관을 점검한다', '오차항이 정규분포를 따른다는 가정은 회귀계수의 부호를 양수로 만드는 데 필요한 조건이다']}
  correctAnswer={4}
  explanation="오차항의 정규성 가정은 검정통계량의 분포를 정확히 알기 위한 조건이며, 회귀계수의 부호와는 무관하다. 계수의 부호는 자료가 보여 주는 실제 관계 방향에 따라 정해진다."
  optionExplanations={['등분산성 가정과 그 진단 방법에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '선형성 가정에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '독립성 가정과 더빈-왓슨 통계량에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 정규성 가정은 검정통계량의 분포를 보장하기 위한 것이며 계수의 부호를 정하는 조건이 아니다.']}
/>

**문항 43 자료**

| 변수 | 계수 | 표준오차 | t | 유의확률 |
|---|---|---|---|---|
| 상수항 | 15.20 | 2.80 | 5.43 | 0.000 |
| 매장면적(평) | 0.92 | 0.18 | 5.11 | 0.000 |
| 경쟁점포수(개) | -0.75 | 0.22 | -3.41 | 0.001 |
| 주차면수(면) | 0.05 | 0.06 | 0.83 | 0.408 |

<Quiz
  questionNumber={43}
  question="위 표는 카페 지점 70곳의 월 매출(백만원)을 세 변수로 설명한 다중회귀 결과다. 유의수준 0.05에서 다음 진술 중 옳은 것만을 모두 고른 것은 다음과 같다. ㄱ. 경쟁점포수가 많을수록 월 매출은 낮아지는 방향으로 추정되었다. ㄴ. 주차면수는 월 매출에 유의한 영향을 준다. ㄷ. 다른 변수가 일정할 때 매장면적이 1평 늘면 월 매출은 평균 0.92백만원 증가하는 것으로 해석한다."
  options={['ㄱ, ㄷ', 'ㄱ, ㄴ', 'ㄴ, ㄷ', 'ㄱ, ㄴ, ㄷ']}
  correctAnswer={1}
  explanation="경쟁점포수 계수가 -0.75로 음수이므로 ㄱ은 옳다. 다중회귀 계수의 표준 해석에 따라 ㄷ도 옳다. 주차면수의 유의확률은 0.408로 0.05보다 크므로 ㄴ은 틀렸다. 따라서 옳은 것은 ㄱ, ㄷ이다."
  optionExplanations={['정답이다. 경쟁점포수 계수의 부호(ㄱ)와 매장면적 계수의 표준 해석(ㄷ)이 모두 옳다.', '주차면수의 유의확률이 0.408로 기준을 넘어 유의하지 않으므로 ㄴ을 포함한 이 조합은 틀렸다.', '경쟁점포수의 부호를 다룬 ㄱ이 빠져 있고 유의하지 않은 ㄴ이 포함되어 이 조합은 틀렸다.', '유의하지 않은 ㄴ이 포함되어 있어 이 조합은 틀렸다.']}
/>

<Quiz
  questionNumber={44}
  question="농구 선수의 시즌 득점을 여러 기록 지표로 설명하는 회귀모형의 설명력을 평가하려 한다. 결정계수에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['반응변수의 총변동 가운데 회귀모형이 설명하는 변동이 차지하는 비율이며 0과 1 사이의 값을 갖는다', '설명변수의 개수를 줄일수록 결정계수는 커지므로 변수를 덜어 낼수록 설명력 지표가 좋아진다', '수정된 결정계수는 설명변수 개수에 대한 벌점을 반영하므로 같은 자료에서 결정계수보다 크지 않다', '설명변수가 하나뿐인 회귀모형에서는 결정계수가 두 변수 표본상관계수의 제곱과 같다']}
  correctAnswer={2}
  explanation="설명변수를 추가하면 잔차제곱합은 줄어들거나 최소한 유지되므로 결정계수는 결코 감소하지 않는다. 변수를 줄이면 결정계수는 작아지거나 그대로이므로 증감 방향을 반대로 서술한 2번이 옳지 않다."
  optionExplanations={['결정계수의 정의는 옳게 서술되어 있으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 변수를 추가할수록 결정계수는 커지거나 유지되며, 줄일수록 커진다는 서술은 증감 방향이 반대다.', '수정된 결정계수의 성질은 옳게 서술되어 있으므로 이 문제에서 찾는 답이 아니다.', '단순회귀에서 결정계수와 상관계수 제곱의 관계는 옳게 서술되어 있으므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={45}
  question="회귀모형을 적합한 뒤 설명변수들이 서로 비슷한 정보를 중복해서 담고 있어 계수 추정치가 불안정해지는 문제가 있는지 확인하려 한다. 이 문제를 진단하는 데 직접 사용하는 통계량으로 가장 적절한 것은 무엇인가"
  options={['쿡의 거리', '아카이케 정보량 기준', '분산팽창지수', '더빈-왓슨 통계량']}
  correctAnswer={3}
  explanation="설명변수 간 정보 중복, 즉 다중공선성은 각 설명변수를 나머지 설명변수로 회귀한 결과를 이용해 계수 분산이 몇 배로 부풀었는지를 재는 분산팽창지수로 진단한다."
  optionExplanations={['쿡의 거리는 개별 관측치가 회귀계수 추정에 미치는 영향력을 재는 지표이며 변수 간 중복과는 다른 문제를 다룬다.', '아카이케 정보량 기준은 모형의 상대적 적합도를 비교하는 지표이며 다중공선성 진단과는 다르다.', '정답이다. 분산팽창지수는 설명변수 간 정보 중복 정도를 직접 재는 다중공선성 진단 지표다.', '더빈-왓슨 통계량은 오차항의 자기상관을 진단하는 지표이며 변수 간 중복과는 다른 문제를 다룬다.']}
/>

**문항 46 자료**

| 구분 | 재구매 | 재구매 안 함 | 합계 |
|---|---|---|---|
| 신규 혜택 이용 | 84 | 56 | 140 |
| 신규 혜택 미이용 | 40 | 120 | 160 |

<Quiz
  questionNumber={46}
  question="위 표에서 신규 혜택을 이용한 집단의 재구매 오즈비(오즈의 비)는 얼마인가"
  options={['1.5', '4.5', '6', '2.4']}
  correctAnswer={2}
  explanation="혜택 이용 집단의 오즈는 84/56=1.5이고, 미이용 집단의 오즈는 40/120=1/3이다. 오즈비는 두 오즈의 비이므로 1.5 ÷ (1/3) = 4.5다."
  optionExplanations={['혜택 이용 집단의 오즈 자체를 오즈비로 착각했을 때 나오는 값이다.', '정답이다. 오즈비는 1.5 ÷ (1/3) = 4.5로 계산된다.', '두 오즈를 나누지 않고 곱하거나 분모를 다르게 잡았을 때 나올 수 있는 값이다.', '오즈 대신 비율(84/140과 40/160)의 비를 계산했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={47}
  question="구독 해지 여부를 예측하는 로지스틱 회귀에서 해지 확률 p에 대해 다음 식이 얻어졌다. 로그 오즈는 -0.9에 월 문의 횟수 계수 0.5를 곱한 값과 월 이용 일수 계수 -0.3을 곱한 값을 더한 것으로 표현된다. 이 결과에 대한 해석으로 가장 적절한 것은 무엇인가"
  options={['월 문의 횟수가 1회 늘면 해지 확률이 0.5만큼 커진다', '다른 변수가 일정할 때 월 문의 횟수가 1회 늘면 해지의 오즈는 e의 0.5제곱인 약 1.65배가 된다', '다른 변수가 일정할 때 월 이용 일수가 1일 늘면 해지의 오즈가 0.3만큼 곱해진다', '상수항 -0.9는 월 문의 횟수가 1회일 때의 로그 오즈를 뜻한다']}
  correctAnswer={2}
  explanation="로짓 척도에서 계수는 설명변수 1단위 증가에 따른 로그 오즈의 덧셈 변화다. 원래 척도로 되돌리면 오즈에 e의 계수제곱만큼 곱해지므로 문의 횟수 계수 0.5에 대해 오즈는 e의 0.5제곱, 약 1.65배가 된다."
  optionExplanations={['로짓 계수는 확률 자체의 변화량이 아니라 로그 오즈의 변화량이므로 확률이 0.5만큼 커진다는 해석은 틀렸다.', '정답이다. 로짓 계수 0.5를 지수화하면 오즈는 e의 0.5제곱, 약 1.65배가 된다.', '계수 -0.3을 그대로 곱해지는 배율로 쓰면 안 되며 e의 -0.3제곱으로 지수화해야 하므로 이 해석은 틀렸다.', '상수항은 모든 설명변수가 0일 때의 로그 오즈를 뜻하며 문의 횟수가 1회일 때를 뜻하지 않는다.']}
/>

<Quiz
  questionNumber={48}
  question="대출 심사 자료로 의사결정나무를 만들려 한다. 의사결정나무의 알고리즘과 분리 기준에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['분류 문제에서 CART는 각 마디를 두 갈래로 나누며 지니지수가 가장 많이 줄어드는 분할을 고른다', 'C4.5 계열은 정보이득비를, CHAID는 카이제곱 통계량을 분리 기준으로 사용한다', '마디를 나눌 때는 각 시점에서 다음 상태로 넘어갈 전이 확률을 계산해 확률이 가장 큰 쪽으로 분할한다', '가지치기는 다 자란 나무의 일부 마디를 잘라 내어 과대적합을 줄이고 예측 안정성을 높이는 절차다']}
  correctAnswer={3}
  explanation="의사결정나무의 분리 기준은 자식 마디의 순수도가 얼마나 좋아지는지를 재는 값(지니지수, 정보이득, 카이제곱 등)이다. 상태 전이 확률은 마르코프 모형 계열의 개념으로 나무의 분할 기준이 아니다."
  optionExplanations={['CART의 분리 기준 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', 'C4.5와 CHAID의 분리 기준 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 상태 전이 확률은 마르코프 모형의 개념이며 의사결정나무의 분할 기준이 아니다.', '가지치기의 목적 설명은 옳으므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={49}
  question="택시 승하차 좌표를 군집화하려는데 군집 개수를 미리 알 수 없고 임의 모양의 군집과 잡음 지점이 섞여 있다. 군집 개수를 사전에 입력하지 않아도 되는 알고리즘으로 가장 적절한 것은 무엇인가"
  options={['K-평균 군집', 'DBSCAN', 'K-메도이드 군집', '가우시안 혼합모형']}
  correctAnswer={2}
  explanation="DBSCAN은 반경과 최소 이웃 수라는 밀도 조건만 정하면 군집 개수가 자료의 밀도 구조에서 자동으로 결정되고, 어느 군집에도 속하지 않는 점은 잡음으로 처리된다."
  optionExplanations={['K-평균 군집은 군집 개수 K를 미리 정해 주어야 하는 알고리즘이므로 이 문제에서 찾는 답이 아니다.', '정답이다. DBSCAN은 밀도 조건만으로 군집 개수가 자동으로 정해지고 잡음을 별도로 처리한다.', 'K-메도이드 군집도 K-평균과 마찬가지로 군집 개수를 미리 정해야 하는 알고리즘이다.', '가우시안 혼합모형도 대개 성분(군집) 개수를 미리 정해야 하는 알고리즘이다.']}
/>

<Quiz
  questionNumber={50}
  question="상품평 텍스트에 등장하는 단어를 원-핫 인코딩으로 표현해 분석에 사용하려 한다. 이 표현 방식에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['각 단어를 한 성분만 1이고 나머지는 모두 0인 벡터에 대응시킨다', '서로 다른 단어에 대응하는 두 벡터의 내적이 0이므로 단어 사이의 의미적 유사도를 담지 못한다', '어휘 수가 늘어나도 벡터 차원이 일정하게 유지되므로 저장 공간과 차원 측면에서 효율적이다', '어휘 수가 많을 때 생기는 희소성 문제를 줄이기 위해 저차원 밀집 벡터로 바꾸는 임베딩을 쓰기도 한다']}
  correctAnswer={3}
  explanation="원-핫 인코딩은 어휘 하나마다 차원 하나를 배정하므로 벡터 차원이 어휘 수와 같아진다. 어휘가 늘면 차원도 그만큼 늘어나 대부분의 성분이 0인 희소 벡터가 되므로 공간 효율이 나쁘다."
  optionExplanations={['원-핫 벡터의 정의는 옳으므로 이 문제에서 찾는 답이 아니다.', '원-핫 벡터가 의미 유사도를 담지 못한다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 어휘 수가 늘면 벡터 차원도 함께 늘어나므로 차원이 일정하게 유지된다는 서술은 사실과 반대다.', '임베딩으로 밀집 벡터를 만드는 이유에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={51}
  question="반응변수가 정상 또는 불량 두 범주로 주어진 자료를 분석하려 한다. 이 자료에 바로 적용할 수 있는 분류 모형으로 보기 어려운 것은 무엇인가"
  options={['로지스틱 회귀', '서포트벡터머신', '랜덤 포레스트 분류기', '다중선형회귀']}
  correctAnswer={4}
  explanation="다중선형회귀는 연속형 반응변수의 기댓값을 설명변수의 일차 결합으로 추정하는 모형이다. 반응변수가 두 범주인 자료에는 확률이 0과 1을 벗어나고 오차 가정도 깨지므로 그대로 쓸 수 없다."
  optionExplanations={['로지스틱 회귀는 이진 분류에 바로 쓸 수 있는 대표 모형이므로 이 문제에서 찾는 답이 아니다.', '서포트벡터머신은 이진 분류에 바로 쓸 수 있는 대표 모형이므로 이 문제에서 찾는 답이 아니다.', '랜덤 포레스트 분류기는 이진 분류에 바로 쓸 수 있는 대표 모형이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 다중선형회귀는 연속형 반응변수를 전제로 하므로 두 범주 반응변수에는 그대로 적용할 수 없다.']}
/>

**문항 52 자료**

| 주성분 | PC1 | PC2 | PC3 | PC4 |
|---|---|---|---|---|
| 고유값 | 5.0 | 3.0 | 1.5 | 0.5 |

<Quiz
  questionNumber={52}
  question="설비 센서 자료를 주성분분석으로 요약한 위 표에서 PC2 하나가 설명하는 분산 비율은 얼마인가"
  options={['15%', '30%', '50%', '75%']}
  correctAnswer={2}
  explanation="고유값의 총합은 5.0+3.0+1.5+0.5=10이다. PC2의 분산 설명 비율은 PC2 고유값을 총합으로 나눈 3.0/10=0.3, 즉 30퍼센트다."
  optionExplanations={['고유값 총합을 잘못 계산했거나 PC3 고유값을 총합으로 나누었을 때 나올 수 있는 값이다.', '정답이다. PC2 고유값 3.0을 전체 고유값 합 10으로 나누면 30퍼센트다.', 'PC1과 PC2 고유값의 합을 총합으로 나누었을 때 나올 수 있는 값이다.', 'PC1, PC2, PC3 고유값의 합을 총합으로 나누었을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={53}
  question="주성분분석에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['변수들의 공분산행렬이나 상관행렬의 고유벡터가 주성분의 방향을 나타낸다', '서로 다른 주성분은 직교하며 제1주성분이 가장 큰 분산을 갖는 방향이다', '유지할 주성분 수는 고유값의 누적 기여율이나 스크리 도표의 꺾이는 지점을 보고 정한다', '주성분은 성분 값이 음수가 되지 않도록 제약을 건 행렬분해로 구한다']}
  correctAnswer={4}
  explanation="주성분분석은 공분산행렬 또는 상관행렬의 고유값 분해로 분산이 큰 직교 방향을 찾는다. 성분에 음이 아닌 제약을 두는 것은 비음수 행렬분해의 특징이며 주성분분석의 계산 방식이 아니다."
  optionExplanations={['고유벡터가 주성분 방향을 나타낸다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '직교성과 제1주성분의 분산 최대화 성질은 옳으므로 이 문제에서 찾는 답이 아니다.', '주성분 수를 정하는 방법에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 음이 아닌 제약을 두는 것은 비음수 행렬분해의 특징이며 주성분분석은 그러한 제약을 두지 않는다.']}
/>

<Quiz
  questionNumber={54}
  question="일별 지하철 승객 수 자료에서 과거 시점의 값과 현재 값이 시차별로 얼마나 연관되어 있는지를 살펴보려 한다. 이때 사용하는 도구로 가장 적절한 것은 무엇인가"
  options={['지니지수', '자기상관함수', '실루엣 계수', '혼동행렬']}
  correctAnswer={2}
  explanation="자기상관함수는 같은 시계열을 시차만큼 밀어 놓고 원 계열과의 상관을 시차별로 계산한 함수로, 시점 간 의존 구조와 주기성을 파악하는 기본 진단 도구다."
  optionExplanations={['지니지수는 의사결정나무의 분할 순수도를 재는 지표이며 시계열 시차 연관과는 관련이 없다.', '정답이다. 자기상관함수는 시차별 상관을 계산해 시점 간 의존 구조를 파악하는 도구다.', '실루엣 계수는 군집 품질을 재는 지표이며 시계열 시차 연관과는 관련이 없다.', '혼동행렬은 분류 모형의 성능을 요약하는 표이며 시계열 시차 연관과는 관련이 없다.']}
/>

<Quiz
  questionNumber={55}
  question="월별 전력 판매량 자료를 분해하고 정상성을 검토하면서 다음 진술을 정리했다. ㄱ. 추세 요인은 자료가 장기간에 걸쳐 증가하거나 감소하는 흐름을 뜻한다. ㄴ. 불규칙 요인은 규칙성이 없지만 과거 자료를 충분히 모으면 정확히 예측할 수 있다. ㄷ. 정상 시계열은 평균이 시점에 따라 달라지지 않고 자기공분산이 시차에만 의존한다. ㄹ. 백색잡음 과정은 서로 다른 시점 사이에 일정한 크기의 양의 상관을 가진다. 옳지 않은 것만을 모두 고른 것은 무엇인가"
  options={['ㄱ, ㄴ', 'ㄱ, ㄹ', 'ㄴ, ㄹ', 'ㄷ, ㄹ']}
  correctAnswer={3}
  explanation="ㄴ은 불규칙 요인이 예측 불가능한 우발적 변동이라는 정의와 어긋나고, ㄹ은 백색잡음이 서로 다른 시점에서 상관이 0인 과정이라는 정의와 어긋난다. ㄱ과 ㄷ은 각각 추세 요인과 약정상성의 정의로 옳다."
  optionExplanations={['ㄱ은 추세 요인의 정의로 옳은 진술이므로 옳지 않은 것을 찾는 이 문제의 답에 포함될 수 없다.', 'ㄱ이 옳은 진술이라 이 조합에 포함되면 안 되므로 틀린 선택지다.', '정답이다. ㄴ과 ㄹ은 각각 불규칙 요인과 백색잡음의 정의를 뒤집어 서술했으므로 옳지 않은 진술이다.', 'ㄷ은 정상 시계열의 정의로 옳은 진술이므로 옳지 않은 것을 찾는 이 문제의 답에 포함될 수 없다.']}
/>

<Quiz
  questionNumber={56}
  question="순차 자료를 다루는 신경망 가운데, 장기 의존성 문제를 완화하는 게이트 구조를 유지하면서도 게이트 수를 줄이고 별도 기억 셀을 두지 않아 매개변수를 적게 쓰는 모형은 무엇인가"
  options={['GRU', 'LSTM', '양방향 순환신경망', '기본 순환신경망']}
  correctAnswer={1}
  explanation="GRU는 갱신 게이트와 재설정 게이트 두 개만 사용하고 은닉 상태가 기억 역할을 겸하므로, 세 개의 게이트와 별도 셀 상태를 쓰는 구조보다 매개변수가 적고 계산이 가볍다."
  optionExplanations={['정답이다. GRU는 두 개의 게이트만 쓰고 별도 기억 셀 없이 은닉 상태가 기억을 겸해 매개변수가 적다.', 'LSTM은 세 개의 게이트와 별도 기억 셀을 쓰므로 매개변수가 더 많은 구조다.', '양방향 순환신경망은 게이트 수를 줄이는 구조가 아니라 정보를 양방향으로 처리하는 구조다.', '기본 순환신경망은 게이트 구조 자체가 없어 장기 의존성 문제를 완화하지 못한다.']}
/>

<Quiz
  questionNumber={57}
  question="학습 자료에서는 오차가 매우 작지만 검증 자료에서는 오차가 큰 신경망 모형을 개선하려 한다. 이때의 조치로 적절하지 않은 것은 무엇인가"
  options={['검증 오차가 다시 커지기 시작하는 시점에서 학습 반복을 중단한다', '학습 과정에서 은닉 노드 일부를 무작위로 비활성화해 특정 노드에 대한 의존을 줄인다', '손실함수에 가중치 크기에 비례하는 벌점 항을 더해 모형 복잡도를 억제한다', '학습 자료를 더 정확히 맞추도록 가중치의 절대값을 가능한 한 크게 키운다']}
  correctAnswer={4}
  explanation="학습 오차만 작고 검증 오차가 큰 상태는 과대적합이다. 가중치의 절대값을 키우면 모형이 학습 자료의 잡음까지 더 정교하게 맞추게 되어 과대적합이 심해진다."
  optionExplanations={['조기 종료는 과대적합을 줄이는 표준 조치이므로 이 문제에서 찾는 답이 아니다.', '드롭아웃은 과대적합을 줄이는 표준 조치이므로 이 문제에서 찾는 답이 아니다.', '가중치 규제는 과대적합을 줄이는 표준 조치이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 가중치를 더 크게 키우는 것은 모형을 더 복잡하게 만들어 과대적합을 오히려 심화시킨다.']}
/>

<Quiz
  questionNumber={58}
  question="여러 개의 결정나무를 만들어 결과를 모아 예측하는 랜덤 포레스트에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['각 나무는 복원추출로 만든 부분 표본으로 학습되어 나무마다 학습자료 구성이 달라진다', '각 분할 지점에서 전체 변수 중 일부만 후보로 삼아 나무 사이의 상관을 낮춘다', '분류 문제에서는 개별 나무의 판정을 모아 다수결로 최종 예측을 정한다', '개별 나무는 학습자료 전체와 모든 설명변수를 그대로 사용해 학습된다']}
  correctAnswer={4}
  explanation="랜덤 포레스트의 개별 나무는 복원추출한 부분 표본으로 학습되고, 분할마다 변수도 일부만 후보로 쓴다. 자료와 변수를 모두 그대로 쓴다고 서술한 4번이 옳지 않다."
  optionExplanations={['부트스트랩 표본 사용에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '분할마다 변수 일부만 후보로 삼는다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '다수결 방식의 최종 예측에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 랜덤 포레스트는 자료와 변수를 모두 그대로 쓰지 않고 각각 일부만 무작위로 골라 쓴다.']}
/>

<Quiz
  questionNumber={59}
  question="부스팅 방식의 앙상블 학습 구조에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['앞 단계에서 잘못 분류된 관측치에 더 큰 가중치를 부여해 다음 학습기를 만든다', '성능이 약한 학습기를 여러 단계에 걸쳐 이어 붙여 편향을 줄여 나가는 전략이다', '최종 예측은 각 단계 학습기의 예측에 성능에 따른 가중치를 곱해 합산하는 방식으로 만든다', '구성 학습기들은 서로 참조하지 않고 동시에 독립적으로 학습된 뒤 한꺼번에 결합된다']}
  correctAnswer={4}
  explanation="부스팅은 앞 단계의 오류를 보고 다음 학습기를 만드는 순차적 구조다. 학습기를 서로 독립적으로 동시에 학습시킨다는 설명은 배깅 계열의 특징이며 부스팅에는 맞지 않는다."
  optionExplanations={['오분류 관측치에 가중치를 더 주는 것은 부스팅의 정의와 일치하므로 이 문제에서 찾는 답이 아니다.', '약학습기를 이어 붙여 편향을 줄인다는 것은 부스팅의 정의와 일치하므로 이 문제에서 찾는 답이 아니다.', '가중합으로 최종 예측을 만든다는 것은 부스팅의 정의와 일치하므로 이 문제에서 찾는 답이 아니다.', '정답이다. 학습기를 독립적으로 동시에 학습시키는 것은 배깅의 특징이며 순차적으로 오류를 보완하는 부스팅과는 반대다.']}
/>

<Quiz
  questionNumber={60}
  question="다음 중 비지도학습 과제로 보기 어려운 것은 무엇인가"
  options={['구매 이력만으로 유사한 소비 패턴의 고객군을 나누는 과제', '레이블 없이 문서 집합에서 주제 구조를 찾아내는 과제', '정답이 표시된 신용 등급 자료로 새 고객의 신용 등급을 예측하는 과제', '변수 간 상관을 이용해 차원을 축소하는 과제']}
  correctAnswer={3}
  explanation="정답 표지가 붙은 자료로 새 사례의 값을 예측하는 것은 지도학습(분류)에 해당한다. 나머지 셋은 정답 표지 없이 자료의 구조를 찾는 비지도학습 과제다."
  optionExplanations={['정답 표지 없이 고객군을 나누는 것은 군집화, 즉 비지도학습이므로 이 문제에서 찾는 답이 아니다.', '레이블 없이 주제 구조를 찾는 것은 비지도학습이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 정답 표지가 있는 자료로 새 값을 예측하는 것은 지도학습이며 비지도학습이 아니다.', '차원축소는 정답 표지 없이 구조를 요약하는 비지도학습이므로 이 문제에서 찾는 답이 아니다.']}
/>

## 4과목: 빅데이터 결과 해석 (61~80)

**문항 61 자료**

| 구분 | 예측 양성 | 예측 음성 |
|---|---|---|
| 실제 양성 | 45 | 5 |
| 실제 음성 | 15 | 135 |

<Quiz
  questionNumber={61}
  question="위 혼동행렬에서 정밀도(Precision)는 얼마인가"
  options={['0.75', '0.90', '0.60', '0.25']}
  correctAnswer={1}
  explanation="정밀도는 TP/(TP+FP)로 계산한다. TP는 45, FP는 15이므로 45/(45+15)=45/60=0.75다."
  optionExplanations={['정답이다. 45를 예측 양성 전체 60(45+15)으로 나누면 0.75다.', 'TP를 실제 양성 전체(TP+FN=50)로 나누면 재현율인 0.9가 되며, 이는 정밀도가 아니다.', 'TP를 전체 표본 수(200)와 혼동하거나 다른 값으로 나누었을 때 나올 수 있는 오답이다.', 'FP를 TP로 착각해 15/60을 계산했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={62}
  question="위와 같은 혼동행렬에서 재현율(민감도)은 얼마인가"
  options={['0.75', '0.90', '0.9375', '0.60']}
  correctAnswer={2}
  explanation="재현율은 TP/(TP+FN)으로 계산한다. TP는 45, FN은 5이므로 45/(45+5)=45/50=0.9다."
  optionExplanations={['정밀도 값인 0.75를 재현율로 착각했을 때 나올 수 있는 오답이다.', '정답이다. 45를 실제 양성 전체 50(45+5)으로 나누면 0.9다.', 'TN을 분모에 잘못 포함시켰을 때 나올 수 있는 값이다.', '분모를 실제보다 크게 잡았을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={63}
  question="정밀도가 0.75, 재현율이 0.90일 때 F1 점수는 얼마인가(소수 넷째 자리에서 반올림)"
  options={['0.8182', '0.825', '0.75', '0.90']}
  correctAnswer={1}
  explanation="F1 점수는 정밀도와 재현율의 조화평균이므로 2×0.75×0.90/(0.75+0.90)=1.35/1.65≈0.8182다."
  optionExplanations={['정답이다. 조화평균 공식 2×0.75×0.90/(0.75+0.90)을 계산하면 약 0.8182다.', '정밀도와 재현율의 산술평균(0.75+0.90)/2=0.825를 F1으로 착각했을 때 나올 수 있는 오답이다.', '정밀도 값을 그대로 F1으로 착각했을 때 나올 수 있는 오답이다.', '재현율 값을 그대로 F1으로 착각했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={64}
  question="혼동행렬의 네 칸을 각각 TP, FP, FN, TN이라 할 때 지표 이름과 정의식의 대응이 옳지 않은 것은 무엇인가"
  options={['정밀도 = TP / (TP + FP)', '재현율 = TP / (TP + FN)', '특이도 = TN / (TN + FP)', 'F1 점수 = (정밀도 + 재현율) / 2']}
  correctAnswer={4}
  explanation="F1 점수는 정밀도와 재현율의 조화평균으로 정의되므로 2×정밀도×재현율/(정밀도+재현율)이다. 산술평균으로 서술한 4번이 잘못된 대응이다."
  optionExplanations={['정밀도의 정의식은 옳으므로 이 문제에서 찾는 답이 아니다.', '재현율의 정의식은 옳으므로 이 문제에서 찾는 답이 아니다.', '특이도의 정의식은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. F1 점수는 산술평균이 아니라 조화평균으로 정의되므로 이 대응이 틀렸다.']}
/>

<Quiz
  questionNumber={65}
  question="관측치 350개를 k=7인 k겹 교차검증으로 평가하려 한다. 진행 방식에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['자료를 크기가 비슷한 7개 묶음으로 나누고 각 관측치는 한 묶음에만 속한다', '회차를 바꿔 가며 모든 묶음이 정확히 한 번씩 검증 역할을 맡아 총 7회차가 수행된다', '각 회차에서 검증에 6개 묶음을 쓰고 나머지 1개 묶음으로 학습한다', '7회차에서 얻은 성능값을 평균해 모형의 대표 성능으로 보고한다']}
  correctAnswer={3}
  explanation="k겹 교차검증의 각 회차에서는 검증에 1개 묶음(50개)을 쓰고 나머지 6개 묶음(300개)으로 학습한다. 학습과 검증에 쓰는 묶음 수를 서로 맞바꿔 서술한 3번이 옳지 않다."
  optionExplanations={['묶음 구성 방식에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '모든 묶음이 한 번씩 검증을 맡는다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 검증에는 1개 묶음을 쓰고 나머지 6개로 학습해야 하는데, 이 설명은 검증과 학습에 쓰는 묶음 수를 서로 바꾸어 서술했다.', '결과를 평균해 대표 성능으로 삼는다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={66}
  question="변수 선별과 자료 분할, 모형 학습, 성능 산출을 수행하는 순서로 가장 적절한 것은 무엇인가(자료를 학습용과 평가용으로 나누는 상황을 가정한다)"
  options={['전체 자료로 변수를 먼저 선별한다 → 학습용과 평가용으로 나눈다 → 학습용으로 학습한다 → 평가용에서 성능을 산출한다', '학습용과 평가용으로 먼저 나눈다 → 학습용만으로 변수를 선별한다 → 학습용으로 학습한다 → 그 선별 규칙을 평가용에 그대로 적용해 성능을 산출한다', '학습용과 평가용으로 먼저 나눈다 → 평가용에서 변수를 선별한다 → 학습용으로 학습한다 → 평가용에서 성능을 산출한다', '전체 자료로 변수를 선별한다 → 같은 전체 자료로 학습한다 → 학습에 쓴 자료에서 바로 성능을 산출한다']}
  correctAnswer={2}
  explanation="변수 선별도 자료를 보고 내리는 결정이므로 학습 절차의 일부다. 평가용 자료를 먼저 떼어 낸 뒤 학습용만으로 선별 기준을 정하고, 확정된 규칙을 평가용에 그대로 적용해야 평가용 정보가 선별에 새어 들지 않는다."
  optionExplanations={['전체 자료로 변수를 먼저 선별하면 평가용 정보가 선별 단계에 이미 새어 들어가 성능이 과대평가된다.', '정답이다. 평가용을 먼저 떼어 두고 학습용만으로 변수를 선별해야 평가용 정보가 새어 들지 않는다.', '평가용 자료로 변수를 선별하면 평가의 독립성이 무너져 성능이 과대평가된다.', '평가용 자료를 따로 두지 않고 학습에 쓴 자료로 성능을 매기면 일반화 성능을 알 수 없다.']}
/>

<Quiz
  questionNumber={67}
  question="적합도 검정의 목적과 적용 조건에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['관측된 자료가 가정한 이론적 분포를 따르는지 확인하는 데 쓰인다', '범주형 자료에서는 범주별 관측도수와 기대도수의 차이를 이용해 검정통계량을 구성할 수 있다', '귀무가설은 자료가 가정한 분포를 따른다는 것이며 유의확률이 유의수준보다 작으면 그 가정을 기각한다', '개별 설명변수 계수의 유의성이 모두 확인되면 모형 전체의 적합성 확인은 생략해도 된다']}
  correctAnswer={4}
  explanation="개별 계수의 유의성과 모형 전체의 적합성은 서로 다른 질문이다. 계수가 모두 유의해도 가정한 분포나 함수 형태가 자료와 맞지 않을 수 있으므로 적합도 확인은 생략할 수 없다."
  optionExplanations={['적합도 검정의 목적에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '카이제곱 적합도 검정통계량 구성 방식은 옳으므로 이 문제에서 찾는 답이 아니다.', '귀무가설과 기각 규칙에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 개별 계수의 유의성과 모형 전체의 적합도는 별개의 질문이므로 하나로 다른 하나를 생략할 수 없다.']}
/>

<Quiz
  questionNumber={68}
  question="배송 지연 예측 모형을 만들기 위해 자료를 학습용, 검증용, 평가용 세 갈래로 나누었다. 각 자료의 역할에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['학습용 자료는 모형의 가중치나 분할 규칙 같은 내부 값을 추정하는 데 쓰인다', '검증용 자료는 여러 설정값 조합의 성능을 견주어 하나를 고르는 근거로 쓰인다', '평가용 자료는 설정값 선택이 끝난 뒤 한 번만 사용해 최종 성능을 보고하는 데 쓰인다', '설정값 조합은 학습용 자료에서 가장 높은 성능을 낸 조합을 그대로 고르면 된다']}
  correctAnswer={4}
  explanation="설정값 조합을 고르는 기준 자료는 검증용이다. 학습용 성능은 설정값을 복잡한 쪽으로 밀수록 계속 좋아지므로 선택 기준이 될 수 없다."
  optionExplanations={['학습용 자료의 역할에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '검증용 자료의 역할에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '평가용 자료의 역할에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 설정값은 검증용 자료로 골라야 하며, 학습용 성능만 보고 고르면 복잡한 모형 쪽으로 치우쳐 과대적합된다.']}
/>

<Quiz
  questionNumber={69}
  question="모형을 학습시키기 전에 사람이 정하는 값과 학습 결과로 얻어지는 값을 나누어 보려 한다. 다음 중 타당하지 않은 것은 무엇인가"
  options={['초매개변수는 학습을 시작하기 전에 사람이 값을 지정한다', '신경망의 연결 가중치는 학습 과정에서 자동으로 갱신되는 매개변수다', '의사결정나무의 최대 깊이나 경사하강법의 학습률은 초매개변수에 해당한다', '선형회귀의 회귀계수는 분석가가 학습 전에 직접 정해 두는 초매개변수다']}
  correctAnswer={4}
  explanation="회귀계수는 자료로부터 추정되는 매개변수이지 사람이 미리 정하는 초매개변수가 아니다. 두 개념은 값이 결정되는 시점과 주체로 구분된다."
  optionExplanations={['초매개변수의 정의는 옳으므로 이 문제에서 찾는 답이 아니다.', '신경망 가중치가 매개변수라는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '최대 깊이와 학습률이 초매개변수라는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 회귀계수는 자료로부터 추정되는 매개변수이며 사람이 미리 정하는 초매개변수가 아니다.']}
/>

<Quiz
  questionNumber={70}
  question="초매개변수 탐색 기법과 그 특징을 짝지은 것으로 옳지 않은 것은 무엇인가"
  options={['격자 탐색은 후보값을 축마다 미리 정해 두고 모든 조합을 빠짐없이 시도하므로 축이 늘수록 시도 횟수가 곱으로 증가한다', '무작위 탐색은 정해진 범위에서 조합을 임의로 뽑아 시도하므로 같은 예산에서 중요한 축의 값을 더 다양하게 훑을 수 있다', '베이지안 최적화는 지금까지 시도한 결과로 성능에 대한 확률모형을 세우고 다음 시도 지점을 그 모형으로 정한다', '수동 조정은 분석가의 경험에 의존하는 방식이므로 자동 탐색 기법보다 항상 더 좋은 조합을 찾아낸다']}
  correctAnswer={4}
  explanation="수동 조정은 분석가의 사전 지식을 활용할 수 있다는 장점이 있지만 탐색 범위와 재현성에 한계가 있어 자동 탐색보다 항상 우월하다고 단정할 수 없다."
  optionExplanations={['격자 탐색의 특징 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '무작위 탐색의 특징 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '베이지안 최적화의 특징 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 수동 조정이 자동 탐색보다 항상 더 좋은 결과를 낸다는 단정은 근거가 없다.']}
/>

<Quiz
  questionNumber={71}
  question="다음 중 손실함수의 기울기를 이용해 학습 도중 가중치를 갱신하는 최적화 알고리즘에 해당하지 않는 것은 무엇인가"
  options={['모멘텀', '아담(Adam)', '격자 탐색', '알엠에스프롭(RMSProp)']}
  correctAnswer={3}
  explanation="모멘텀, 아담, 알엠에스프롭은 모두 경사하강법 계열로 기울기를 이용해 학습 중 가중치를 갱신한다. 격자 탐색은 학습 바깥에서 초매개변수 조합을 고르는 절차이므로 가중치 갱신 알고리즘이 아니다."
  optionExplanations={['모멘텀은 기울기를 이용한 최적화 알고리즘이므로 이 문제에서 찾는 답이 아니다.', '아담은 기울기를 이용한 최적화 알고리즘이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 격자 탐색은 초매개변수 조합을 고르는 탐색 절차이며 가중치를 갱신하는 알고리즘이 아니다.', '알엠에스프롭은 기울기를 이용한 최적화 알고리즘이므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={72}
  question="구독 해지 예측 모형을 도입한 뒤 그 분석 결과가 사업에 얼마나 기여했는지 평가하려 한다. 비즈니스 기여도 평가에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['투자 대비 효과는 모형 도입에 들어간 비용과 그로 인해 늘어난 이익을 견주어 산출한다', '모형 도입 전후의 해지율 변화처럼 사업 성과로 환산 가능한 지표를 함께 제시하는 것이 좋다', '기여도는 금액 절감, 매출 증대, 처리 시간 단축처럼 정량화된 항목으로 표현할 수 있다', '투자 대비 효과 지표는 자료의 결측률이나 값의 정확성 같은 자료 품질 속성을 재는 지표다']}
  correctAnswer={4}
  explanation="투자 대비 효과는 사업 성과 축을 재는 지표이고, 결측률이나 값의 정확성은 자료 품질 축을 재는 별개의 항목이다. 두 축을 맞바꿔 서술한 4번이 옳지 않다."
  optionExplanations={['투자 대비 효과의 산출 방식에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '사업 성과 지표를 함께 제시해야 한다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '기여도를 정량화된 항목으로 표현한다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 투자 대비 효과는 사업 성과를 재는 지표이며, 자료 품질 속성을 재는 지표가 아니다.']}
/>

<Quiz
  questionNumber={73}
  question="가로축에 학습 시간, 세로축에 시험 점수를 두고 학생 한 명을 점 하나로 찍어 두 변수의 관계와 밀집 정도를 살피려 한다. 이러한 표현에 해당하는 시각화 기법은 무엇인가"
  options={['점의 크기에 제3의 변수를 대응시킨 버블 차트', '한 변수의 구간별 도수를 막대로 세운 히스토그램', '두 변수의 순서쌍을 좌표 평면에 점으로 찍은 산점도', '항목별 구성비를 부채꼴 면적으로 나눈 원 그래프']}
  correctAnswer={3}
  explanation="두 변수를 각 축에 두고 관측치를 점으로 찍는 기본형 관계 시각화는 산점도다. 버블 차트는 여기에 크기로 제3 변수를 더한 확장형이고, 히스토그램과 원 그래프는 한 변수의 분포나 구성비를 나타낸다."
  optionExplanations={['버블 차트는 산점도에 크기라는 제3 변수를 추가한 확장형이며 이 사례에는 제3 변수가 없다.', '히스토그램은 한 변수의 분포를 나타내는 도구이며 두 변수의 관계를 보는 이 사례와는 다르다.', '정답이다. 두 변수를 좌표에 점으로 찍는 기본형 관계 시각화는 산점도다.', '원 그래프는 구성비를 나타내는 도구이며 두 변수의 관계를 보는 이 사례와는 다르다.']}
/>

<Quiz
  questionNumber={74}
  question="여러 대상에 대해 여러 항목의 값을 한 화면에서 견주는 비교 시각화 기법과 값의 표현 방식을 짝지은 것으로 옳지 않은 것은 무엇인가"
  options={['히트맵은 행과 열이 만나는 칸의 색 농도로 값의 크기를 나타낸다', '평행 좌표 그래프는 항목마다 세로축을 나란히 세우고 한 대상의 값을 선 하나로 이어 대상 간 형태를 비교한다', '체르노프 페이스는 얼굴 각 부위의 모양과 크기에 항목값을 대응시켜 여러 항목을 한 얼굴로 나타낸다', '레이더 차트는 중심에서 뻗은 여러 축 위에 해당 항목의 개수를 세어 표시하고 축 이름만 이어 다각형을 만든다']}
  correctAnswer={4}
  explanation="레이더 차트는 중심에서 뻗은 각 축 위에 항목값의 크기만큼 떨어진 점을 찍고 그 점들을 이어 다각형을 만든다. 값이 아니라 개수를 세어 표시한다고 서술한 4번이 옳지 않다."
  optionExplanations={['히트맵에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '평행 좌표 그래프에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '체르노프 페이스에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 레이더 차트는 항목값의 크기를 축 위의 거리로 표시하는 것이지 개수를 세어 표시하는 것이 아니다.']}
/>

<Quiz
  questionNumber={75}
  question="시각화 산출물을 기획하는 상황에서 인포그래픽에 대한 설명으로 부적절한 것은 무엇인가"
  options={['정보와 그림 요소를 결합해 전달하려는 내용을 한눈에 이해시키는 것을 목적으로 한다', '많은 양의 자료를 압축해 보여 주므로 무엇을 남기고 무엇을 버릴지 정하는 편집 판단이 필요하다', '글로만 적힌 설명보다 기억에 오래 남고 공유되기 쉬운 형태로 만들어질 수 있다', '완성된 산출물을 열람하려면 열람자가 반드시 전용 제작 소프트웨어를 설치해야 한다']}
  correctAnswer={4}
  explanation="인포그래픽 산출물은 대체로 이미지나 문서 형태로 배포되어 일반적인 뷰어나 웹 브라우저로 열람할 수 있다. 열람에 전용 소프트웨어 설치가 반드시 필요하다고 일반화한 4번이 옳지 않다."
  optionExplanations={['인포그래픽의 목적에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '편집 판단이 필요하다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '기억과 공유에 유리하다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 인포그래픽 열람에는 대개 전용 소프트웨어가 필요 없으며, 제작 도구가 필요한 것은 만드는 쪽의 사정일 뿐이다.']}
/>

<Quiz
  questionNumber={76}
  question="한 공공도서관이 개관 이후 15년 동안의 주요 사건을 연도 순서대로 배열해 한 장으로 보여 주려 한다. 각 연도에 무슨 일이 있었는지를 시점 순서에 따라 훑어보게 하는 데 가장 적합한 인포그래픽 유형은 무엇인가"
  options={['숫자 하나를 크게 앞세워 핵심 수치를 강조하는 통계 수치 강조형', '항목 사이의 인과 관계를 화살표로 이어 절차를 안내하는 흐름도형', '구성비를 원과 막대로 나누어 비중을 보여 주는 비중 표현형', '사건을 시점 순서에 따라 축 위에 차례로 배치하는 타임라인형']}
  correctAnswer={4}
  explanation="연도별 사건을 시점 순서로 축 위에 늘어놓아 시간의 경과를 보이는 유형은 타임라인형이다."
  optionExplanations={['통계 수치 강조형은 핵심 수치 하나를 부각하는 유형이며 시간 순서 배열과는 다른 목적이다.', '흐름도형은 절차나 인과 관계를 안내하는 유형이며 시간 순서 배열과는 다른 목적이다.', '비중 표현형은 구성비를 보여 주는 유형이며 시간 순서 배열과는 다른 목적이다.', '정답이다. 시점 순서로 사건을 축 위에 배치하는 것은 타임라인형의 정의와 정확히 맞는다.']}
/>

<Quiz
  questionNumber={77}
  question="분석 활용계획에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['분석 결과를 실제 업무 프로세스에 어떻게 적용할지 정의한다', '모형의 성능 변화를 지속적으로 모니터링하는 방안을 포함한다', '사용자의 피드백을 반영해 시스템을 개선하는 체계를 갖춘다', '분석 모델 개발이 완료되면 모형 재학습이나 운용 계획은 수립할 필요가 없다']}
  correctAnswer={4}
  explanation="모형은 시간이 지나면 성능이 저하될 수 있으므로 개발 완료 후에도 재학습과 운용 계획을 지속적으로 수립해야 한다. 이를 불필요하다고 서술한 4번이 옳지 않다."
  optionExplanations={['업무 프로세스 적용 방안 정의는 활용계획의 필수 요소이므로 이 문제에서 찾는 답이 아니다.', '성능 모니터링 방안 포함은 활용계획의 필수 요소이므로 이 문제에서 찾는 답이 아니다.', '피드백 반영 체계는 활용계획의 필수 요소이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 모형은 시간이 지나면 성능이 떨어질 수 있어 재학습·운용 계획이 계속 필요하며, 필요 없다는 서술은 옳지 않다.']}
/>

<Quiz
  questionNumber={78}
  question="사기경보 모델 감사에서 행은 실제 거래, 열은 경보 결과로 집계했다. 감사팀이 실제 사기 거래 중 모델이 경보를 울린 비율을 확인하려 할 때 봐야 할 지표는 무엇인가"
  options={['재현율(Recall)', '정밀도(Precision)', '특이도(Specificity)', '정확도(Accuracy)']}
  correctAnswer={1}
  explanation="실제 양성(사기) 가운데 올바르게 탐지한 비율을 재는 지표는 재현율이다. 정밀도는 예측 양성 가운데 실제 양성의 비율을, 특이도는 실제 음성 가운데 올바르게 음성으로 판정한 비율을 잰다."
  optionExplanations={['정답이다. 실제 사기 거래 중 경보가 울린 비율은 TP/(TP+FN)이며 이는 재현율의 정의와 정확히 맞는다.', '정밀도는 경보가 울린 거래 중 실제 사기의 비율을 재는 지표로, 이 질문이 묻는 방향과 분모가 다르다.', '특이도는 실제 정상 거래 중 경보가 울리지 않은 비율을 재는 지표로, 이 질문이 묻는 실제 사기 쪽과는 반대다.', '정확도는 전체 거래 중 올바르게 판정한 비율이며 사기 거래만을 따로 보는 이 질문과는 범위가 다르다.']}
/>

<Quiz
  questionNumber={79}
  question="같은 평가 표본에서 모델 A의 AUC는 0.88, 모델 B의 AUC는 0.61일 때 올바른 해석은 무엇인가"
  options={['모델 A가 모델 B보다 무작위로 뽑은 양성-음성 쌍의 순위를 올바르게 구분할 확률이 높다', '모델 B가 모델 A보다 판별 성능이 뛰어남을 뜻한다', '모델 A의 정확도는 무조건 88퍼센트로 확정된다', '두 모델의 분류 임계값이 완전히 동일해야만 AUC를 비교할 수 있다']}
  correctAnswer={1}
  explanation="AUC는 임계값에 관계없이 무작위로 뽑은 양성-음성 쌍에서 양성 표본의 점수가 더 높을 확률을 나타내므로, 값이 큰 모델 A가 순위 판별력에서 더 우수하다."
  optionExplanations={['정답이다. AUC가 클수록 양성-음성 쌍의 순위를 올바르게 구분할 확률이 높다는 해석이 AUC의 정의와 정확히 맞는다.', 'AUC 값의 대소를 서로 반대로 해석한 오답이다.', 'AUC는 정확도와 다른 지표이며 특정 임계값에서의 정확도를 직접 알려 주지 않는다.', 'AUC는 임계값 전체에 걸친 성능을 요약한 지표이므로 두 모델의 임계값이 같아야만 비교할 수 있는 것은 아니다.']}
/>

<Quiz
  questionNumber={80}
  question="다음 중 두 변수 사이의 관계를 표현하는 것이 주된 목적이 아닌 시각화는 무엇인가"
  options={['단일 변수 히스토그램', '산점도', '버블 차트', '상관행렬 히트맵']}
  correctAnswer={1}
  explanation="히스토그램은 한 수치형 변수의 구간별 빈도를 나타내 분포 형태를 보는 도구이며, 두 변수 간 관계 표현이 주목적은 아니다."
  optionExplanations={['정답이다. 히스토그램은 한 변수의 분포를 보는 도구이지 두 변수 간 관계를 보는 도구가 아니다.', '산점도는 두 변수의 관계를 표현하는 대표 도구이므로 이 문제에서 찾는 답이 아니다.', '버블 차트는 두 변수의 관계에 크기까지 더해 표현하는 도구이므로 이 문제에서 찾는 답이 아니다.', '상관행렬 히트맵은 여러 변수 쌍의 관계를 한눈에 보여 주는 도구이므로 이 문제에서 찾는 답이 아니다.']}
/>

## 채점 후 복습 동선표

채점을 마쳤으면 틀린 문항의 과목·번호로 아래 표를 찾아 해당 편을 다시 읽습니다. 같은 개념에서 반복해 틀린다면 그 편을 정독한 뒤 이 모의고사를 다시 풀어 보십시오.

| 틀린 문항 번호 | 다루는 영역 | 복습할 편 |
|---|---|---|
| 1–3 | 빅데이터 특성(3V/5V)과 위기 요인 | 08편 |
| 4–6 | 분석 준비도·성숙도, 조직 구조, 데이터 과학자 역량 | 08편 |
| 7, 9, 20 | 분석 방법론(CRISP-DM), 과제 유형, 마스터플랜·WBS | 07편, 10편 |
| 8 | 하향식·상향식 과제 발굴 | 07편 |
| 10–13 | 개인정보·비식별 기법, 데이터 품질 요소 | 08편, 09편 |
| 14–19 | 정형·비정형 데이터, 분산 저장·NoSQL, ETL·DW | 03편, 09편 |
| 21–25, 39 | 전처리(정제·통합·축소·변환), 결측·이상치, 파생변수 | 11편, 12편 |
| 26–28, 40 | 정규화, 인코딩, 클래스 불균형, 표본추출 | 12편, 02편 |
| 29–33, 38 | 기술통계·확률분포·정규분포·표집분포 | 14편 |
| 34–37 | 표본추출법, 시각적 해석, 탐색적 데이터 분석, 인과관계 | 04편, 13편, 15편 |
| 41 | 지도학습·비지도학습 구분 | 05편 |
| 42–45 | 회귀 가정, 다중회귀 해석, 결정계수, 다중공선성 | 16편 |
| 46–47 | 오즈비, 로지스틱 회귀 해석 | 18편 |
| 48, 58–59 | 의사결정나무, 랜덤포레스트, 부스팅 | 18편 |
| 49, 60 | 군집분석, 비지도학습 과제 | 19편 |
| 50 | 텍스트 인코딩(원-핫) | 12편, 18편 |
| 51 | 이진 분류에 부적절한 모형 | 17편, 18편 |
| 52–53 | 주성분분석(PCA) | 19편 |
| 54–55 | 시계열·자기상관·정상성 | 21편 |
| 56–57 | 신경망 구조, 과대적합 대응 | 21편, 22편 |
| 61–64, 78–80 | 혼동행렬, 정밀도·재현율·F1, AUC | 20편 |
| 65–68 | 교차검증, 데이터 누수, 자료 분할 역할 | 21편 |
| 67 | 적합도 검정 | 15편 |
| 69–71 | 초매개변수·매개변수, 탐색 기법, 최적화 알고리즘 | 21편, 22편 |
| 72 | 비즈니스 기여도 평가 | 24편 |
| 73–77 | 시각화 기법, 인포그래픽, 분석 활용계획 | 23편, 24편 |

## 참고 자료

- [한국데이터산업진흥원 - 빅데이터분석기사 자격시험 안내](https://www.dataq.or.kr/www/sub/a_07.do)
- [코리아IT아카데미 - 빅데이터 분석기사](https://www.koreaisacademy.com/2025/cer/datascience/bigdata.asp)
