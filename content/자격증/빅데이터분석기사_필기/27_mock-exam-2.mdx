---
title: '27. 빅데이터분석기사 필기 예상 모의고사 2회'
description: '평가지표·엔트로피·연관규칙·가설검정 등 계산형 문항 비중을 강화한 두 번째 모의고사, 채점 후 과목별 복습 동선까지 정리'
---

<Callout type="warning">
  **이번 회차는 기출 그대로가 아니라 재구성 출제입니다.** 01~27편에서 다룬 개념과 최근 회차 기출·모의고사 자료의 출제 유형·빈도·오답 구성 방식을 분석해 숫자와 상황을 새로 만든 문제이며, 28편(1회)과 문항이 겹치지 않습니다.
  이번 회차는 **정확도·정밀도·재현율·F1, 엔트로피·지니지수·정보이득, 지지도·신뢰도·향상도, 가설검정 통계량, 표본크기** 같은 계산형 문항 비중을 표준 난이도보다 높였습니다. 권장 풀이 시간은 **120분**이며, 합격 기준은 **과목별 100점 만점에 40점 미만이면 과락**(20문항 중 8문항 미만 정답)이고 **4과목 평균 60점 이상**(80문항 중 48문항 이상 정답)입니다. 실제 응시 회차의 세부 출제기준·배점·합격선은 한국데이터산업진흥원 공고를 반드시 확인하십시오.
</Callout>

## 문항 배분표

| 과목 | 문항 범위 | 문항 수 | 이번 회차의 강조점 |
|---|---|---|---|
| 1과목 빅데이터 분석 기획 | 1–20 | 20 | 품질 지표 계산 1문항 포함, 개념 위주 |
| 2과목 빅데이터 탐색 | 21–40 | 20 | 표준화·표본크기·가설검정 통계량·비율 차이 계산 강화 |
| 3과목 빅데이터 모델링 | 41–60 | 20 | 지니지수·엔트로피·정보이득·연관규칙·거리·PCA 계산 강화 |
| 4과목 빅데이터 결과 해석 | 61–80 | 20 | 혼동행렬 전 지표·F1·제2종오류·표본크기 계산 강화 |
| **합계** | – | **80** | – |

## 1과목: 빅데이터 분석 기획 (1~20)

<Quiz
  questionNumber={1}
  question="한 보험사가 사고 발생 예측 모형을 도입해 손해율을 5퍼센트포인트 낮추고 그만큼 보험료 산정의 정확도를 높였다. 이 성과가 직접 대응하는 빅데이터 특성 확장 요소는 무엇인가"
  options={['가치(Value)', '규모(Volume)', '다양성(Variety)', '정확성(Veracity)']}
  correctAnswer={1}
  explanation="분석 결과가 손해율 개선이라는 실제 효용으로 이어졌으므로 3V 이후 확장된 가치 관점에 해당한다. 규모는 양, 다양성은 형식의 다양함, 정확성은 값의 신뢰 수준을 가리키는 축으로 이 사례의 초점과 다르다."
  optionExplanations={['정답이다. 분석 결과가 손해율 개선이라는 실제 효용으로 이어졌다는 점이 가치가 가리키는 내용과 정확히 일치한다.', '규모는 자료의 양이 많다는 뜻이며 이 사례는 성과를 묻고 있어 양과는 무관하다.', '다양성은 자료 형식이 여러 가지라는 뜻이며 이 사례에는 형식 차이가 등장하지 않는다.', '정확성은 자료 값의 신뢰 수준을 뜻하며 손해율 개선이라는 성과와는 다른 축이다.']}
/>

<Quiz
  questionNumber={2}
  question="다음 중 빅데이터 활용 확대에 따른 위기 요인으로 보기 어려운 것은 무엇인가"
  options={['여러 기관의 자료가 결합되어 개인의 이동 경로가 세밀하게 드러나는 문제', '신용점수 모형이 잘못된 예측을 근거로 대출 거절이라는 불이익을 주는 문제', '편향된 학습 자료로 만든 채용 모형이 특정 성별에 불리한 결과를 반복하는 문제', '공공기관과 민간기업의 자료가 연계되어 새로운 융합 서비스가 가능해지는 변화']}
  correctAnswer={4}
  explanation="통상 거론되는 빅데이터 위기 요인은 사생활 침해, 책임 원칙의 훼손, 데이터 오용 세 갈래다. 자료 연계로 새로운 서비스가 가능해지는 것은 위험이 아니라 빅데이터가 만들어 내는 기대 효과다."
  optionExplanations={['이동 경로가 재조합되어 드러나는 것은 사생활 침해에 해당하므로 이 문제에서 찾는 답이 아니다.', '예측만으로 불이익을 주는 것은 책임 원칙의 훼손에 해당하므로 이 문제에서 찾는 답이 아니다.', '편향된 자료로 인한 차별적 결과는 데이터 오용에 해당하므로 이 문제에서 찾는 답이 아니다.', '정답이다. 자료 연계로 새로운 서비스가 가능해지는 것은 위험이 아니라 빅데이터가 주는 기회이며 위기 요인 세 갈래 어디에도 속하지 않는다.']}
/>

<Quiz
  questionNumber={3}
  question="한 통신사가 분석 준비도와 분석 성숙도 두 축으로 자사 수준을 진단했다. 두 축의 조합과 유형 명칭을 짝지은 설명 중 옳지 않은 것은 무엇인가"
  options={['준비형은 준비도와 성숙도가 모두 낮아 기반 마련과 기법 적용을 함께 시작해야 하는 상태다', '도입형은 준비도는 높지만 성숙도는 낮아 기법 활용을 앞으로 채워야 하는 상태다', '정착형은 준비도는 낮지만 제한된 범위에서 이미 분석을 수행해 성숙도가 높은 상태다', '확산형은 준비도는 높지만 성숙도는 낮아 전사 확산을 앞둔 상태다']}
  correctAnswer={4}
  explanation="확산형은 준비도와 성숙도가 모두 높아 전사 차원으로 분석을 넓혀 갈 수 있는 상태다. 성숙도가 낮다고 서술한 4번은 도입형의 정의를 확산형에 잘못 붙인 것이다."
  optionExplanations={['준비형은 두 축이 모두 낮은 상태를 가리키므로 옳은 설명이다.', '도입형은 준비도가 높고 성숙도가 낮은 조합을 가리키므로 옳은 설명이다.', '정착형은 준비도가 낮고 성숙도만 높은 조합을 가리키므로 옳은 설명이다.', '정답이다. 확산형은 두 축이 모두 높은 상태이며, 성숙도가 낮다는 서술은 도입형과 확산형을 혼동한 것이다.']}
/>

<Quiz
  questionNumber={4}
  question="분석 업무를 담당하는 조직 구조 유형에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['분산구조는 분석 인력을 현업 부서에 배치해 업무 맥락을 아는 사람이 결과를 빠르게 실무에 반영할 수 있다', '기능구조는 전담 조직 없이 각 현업 부서가 자기 범위에서 분석을 처리해 전사 관점의 과제를 다루기 어렵다', '집중구조는 여러 부서에서 올라온 과제의 우선순위를 한곳에서 조정할 수 있다', '분산구조로 인력을 배치하면 부서 간 유사한 분석 작업이 겹치는 문제가 저절로 사라진다']}
  correctAnswer={4}
  explanation="분산구조는 인력을 부서별로 나누어 배치하므로 오히려 부서마다 비슷한 전처리나 분석을 따로 만들어 중복이 발생하기 쉽다. 중복이 저절로 사라진다고 서술한 4번이 옳지 않다."
  optionExplanations={['분산구조의 장점에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '기능구조의 약점에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '집중구조의 장점에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 분산구조는 부서마다 비슷한 분석을 따로 만들기 쉬워 중복 문제가 오히려 생기기 쉬우며 저절로 사라지지 않는다.']}
/>

<Quiz
  questionNumber={5}
  question="데이터 과학자에게 요구되는 역량 중 하드 스킬에 해당하는 것은 무엇인가"
  options={['이해관계자의 서로 다른 요구를 조율해 합의를 이끌어 내는 능력', '분석 결과를 비전문가에게 쉬운 언어로 설명하는 능력', '통계 기법과 알고리즘을 자료에 정확히 적용해 계산하는 능력', '팀원 간 갈등을 조정하고 협업 분위기를 만드는 능력']}
  correctAnswer={3}
  explanation="통계 기법과 알고리즘을 정확히 적용해 계산하는 능력은 도구·기법을 다루는 하드 스킬이다. 나머지 세 능력은 사람과 사람 사이의 소통·조율을 다루는 소프트 스킬에 해당한다."
  optionExplanations={['이해관계자 조율은 대인관계를 다루는 소프트 스킬이다.', '쉬운 설명 능력은 의사소통을 다루는 소프트 스킬이다.', '정답이다. 통계 기법과 알고리즘의 정확한 적용은 분석 도구를 다루는 하드 스킬에 해당한다.', '갈등 조정은 협업을 다루는 소프트 스킬이다.']}
/>

<Quiz
  questionNumber={6}
  question="CRISP-DM 방법론의 단계 순서를 나열한 것 중 옳지 않은 것은 무엇인가"
  options={['업무 이해에서 시작해 데이터 이해로 넘어간다', '데이터 준비를 마친 뒤 모델링을 수행한다', '평가를 마친 뒤 배포로 마무리한다', '모델링을 마친 뒤 업무 이해로 되돌아가는 것으로 전체 절차가 끝난다']}
  correctAnswer={4}
  explanation="CRISP-DM은 업무 이해에서 시작해 데이터 이해, 데이터 준비, 모델링, 평가, 배포 순으로 진행되며 배포로 마무리된다. 모델링 뒤에 업무 이해로 되돌아가는 것으로 전체가 끝난다는 서술은 표준 순서와 다르다."
  optionExplanations={['업무 이해에서 데이터 이해로 이어지는 순서는 옳으므로 이 문제에서 찾는 답이 아니다.', '데이터 준비 뒤 모델링으로 이어지는 순서는 옳으므로 이 문제에서 찾는 답이 아니다.', '평가 뒤 배포로 마무리되는 순서는 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. CRISP-DM은 평가와 배포로 마무리되며, 모델링 뒤 업무 이해로 돌아가는 것을 종료 지점으로 삼지 않는다.']}
/>

<Quiz
  questionNumber={7}
  question="한 보험사가 손해율을 3퍼센트포인트 낮춘다는 사업 목표를 먼저 세운 뒤, 이를 달성하는 데 필요한 계약자 정보와 사고 이력을 역으로 목록화했다. 이 과제 발굴 방식은 무엇인가"
  options={['상향식 접근', '하향식 접근', '무작위 탐색', '기법 우선 접근']}
  correctAnswer={2}
  explanation="사업 목표를 먼저 정의한 뒤 이를 달성하는 데 필요한 데이터를 거꾸로 찾아가는 방식은 하향식 접근이다."
  optionExplanations={['상향식은 목표 없이 데이터를 탐색하다 과제를 발견하는 방식이며 이 사례와는 순서가 반대다.', '정답이다. 사업 목표를 먼저 정한 뒤 필요한 데이터를 역으로 찾는 흐름은 하향식 접근의 정의와 정확히 맞는다.', '무작위 탐색은 이 분류 체계에 속하지 않는 임의의 명칭이다.', '기법 우선 접근은 이 분류 체계의 정식 명칭이 아니다.']}
/>

<Quiz
  questionNumber={8}
  question="한 스타트업이 새로 확보한 위치 자료를 어디에 쓸지, 어떤 분석 기법을 적용할지 모두 정하지 못한 채 우선 자료 구조부터 살펴보고 있다. 이 상태에 해당하는 분석 과제 유형은 무엇인가"
  options={['최적화형', '솔루션형', '통찰형', '발견형']}
  correctAnswer={4}
  explanation="목표와 방법을 모두 아직 정하지 못한 상태는 발견형 과제에 해당한다. 최적화형은 목표와 방법을 모두 알고 있는 경우, 솔루션형은 목표만 아는 경우, 통찰형은 방법만 아는 경우다."
  optionExplanations={['최적화형은 목표와 방법을 모두 아는 상태를 가리키며 이 사례와는 반대다.', '솔루션형은 목표는 알지만 방법을 모르는 상태를 가리키며 이 사례는 목표조차 정해지지 않았다.', '통찰형은 방법은 알지만 목표를 모르는 상태를 가리키며 이 사례는 방법도 정해지지 않았다.', '정답이다. 목표와 방법을 모두 모르는 상태는 발견형 과제의 정의와 정확히 맞는다.']}
/>

<Quiz
  questionNumber={9}
  question="다음 중 개인정보 처리 원칙에 비추어 가장 부적절한 조치는 무엇인가"
  options={['수집 목적에 필요한 최소한의 항목만 수집한다', '보유 기간이 지나고 법적 보존 근거가 없으면 지체 없이 파기한다', '통계 작성이나 과학적 연구 목적이면 가명정보를 정보주체 동의 없이도 처리할 수 있다', '향후 활용할 가능성이 있으므로 목적이 끝난 뒤에도 기간을 정하지 않고 계속 보관한다']}
  correctAnswer={4}
  explanation="막연한 미래 활용 가능성만으로 개인정보를 무기한 보관하는 것은 목적 제한과 최소 보유 원칙에 어긋난다."
  optionExplanations={['최소 수집 원칙에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '목적 달성 후 파기 원칙에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '가명정보의 특례에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 막연한 미래 활용 가능성만으로 기간 없이 무기한 보관하는 것은 목적 제한과 최소 보유 원칙에 어긋난다.']}
/>

<Quiz
  questionNumber={10}
  question="한 의료기관이 진단 코드를 임의 코드로 바꾼 연구용 자료를 만들면서, 원래 코드와의 대응표를 연구 종료와 함께 완전히 폐기했다. 이 자료의 성격으로 옳은 것은 무엇인가"
  options={['가명정보', '익명정보', '공개정보', '원본정보']}
  correctAnswer={2}
  explanation="대응표가 완전히 폐기되어 어떤 추가 정보로도 재식별할 수 없는 상태가 되었으므로 이 자료는 익명정보에 해당한다."
  optionExplanations={['가명정보는 대응표 같은 추가 정보로 재식별이 가능한 상태를 가리키며 이 사례는 대응표가 폐기되어 해당하지 않는다.', '정답이다. 재식별에 쓸 대응표가 완전히 폐기되어 어떤 수단으로도 개인을 알아볼 수 없으므로 익명정보다.', '공개정보는 이미 공개적으로 열람 가능한 정보를 뜻하며 이 사례와는 무관하다.', '원본정보는 가공하지 않은 원래 자료를 가리키는 일반적 표현일 뿐 이 상황을 규정하는 법적 분류가 아니다.']}
/>

<Quiz
  questionNumber={11}
  question="비식별 처리 기법과 적용 사례를 짝지은 것 중 옳은 것만을 모두 고른 것은 다음과 같다. ㄱ. 라운딩 - 소득을 만원 단위로 반올림해 표시한다 ㄴ. 범주화 - 방문 횟수를 0에서 3회, 4에서 7회처럼 구간으로 나눈다 ㄷ. 총계처리 - 개인별 진료비 대신 지역 평균 진료비를 제공한다 ㄹ. 데이터 마스킹 - 주소의 상세 번지를 완전히 삭제해 시군구까지만 남긴다"
  options={['ㄱ, ㄴ, ㄷ', 'ㄱ, ㄴ', 'ㄷ, ㄹ', 'ㄱ, ㄴ, ㄷ, ㄹ']}
  correctAnswer={1}
  explanation="ㄱ, ㄴ, ㄷ은 각각 라운딩, 범주화, 총계처리의 정의와 정확히 일치한다. ㄹ은 값을 가리는 마스킹이 아니라 하위 단위를 잘라 내는 부분삭제(범위 삭제)에 해당하므로 마스킹으로 짝지은 것이 옳지 않다."
  optionExplanations={['정답이다. 라운딩, 범주화, 총계처리 세 사례는 모두 각 기법의 정의와 정확히 일치한다.', 'ㄷ이 빠져 있지만 ㄷ도 총계처리의 정의와 일치하는 옳은 사례이므로 이 조합은 불완전하다.', 'ㄱ과 ㄴ이 빠져 있고 ㄹ이 포함되어 있어 이 조합은 틀렸다.', 'ㄹ은 상세 주소를 잘라 내는 부분삭제이지 값을 다른 문자로 가리는 마스킹이 아니므로 이 조합은 틀렸다.']}
/>

**문항 12 자료**

| 항목 | 값 |
|---|---|
| 전체 레코드 수 | 3,000건 |
| 필수 항목 결측 건수 | 240건 |
| 운영 승인 기준 | 완전성 95퍼센트 이상 |

<Quiz
  questionNumber={12}
  question="위 표의 상품 등록 품질 카드를 검토할 때, 완전성 비율과 운영 승인 여부를 올바르게 짝지은 것은 무엇인가"
  options={['완전성 92퍼센트이며 기준에 못 미쳐 승인을 보류한다', '완전성 92퍼센트이며 기준을 충족해 승인한다', '완전성 8퍼센트이며 기준에 못 미쳐 승인을 보류한다', '완전성 95퍼센트이며 기준을 충족해 승인한다']}
  correctAnswer={1}
  explanation="결측률은 240/3,000=0.08, 즉 8퍼센트다. 완전성은 100퍼센트에서 결측률을 뺀 값이므로 100-8=92퍼센트다. 운영 승인 기준인 95퍼센트에 못 미치므로 승인을 보류하고 결측값을 보완해야 한다."
  optionExplanations={['정답이다. 완전성은 92퍼센트로 계산되며 기준 95퍼센트에 못 미쳐 승인을 보류해야 한다.', '완전성 92퍼센트는 맞게 계산했지만 기준 95퍼센트에 못 미친다는 사실을 놓치고 승인했다고 서술한 오답이다.', '결측률 8퍼센트를 완전성으로 착각한 오답이다.', '결측이 없다고 가정해 완전성을 100퍼센트에 가깝게 잘못 계산한 오답이다.']}
/>

<Quiz
  questionNumber={13}
  question="한 방송사가 실시간 자막 데이터를 다루려 한다. 다음 중 반정형 데이터로 보기 어려운 것은 무엇인가"
  options={['키와 계층 구조를 가진 JSON 형식의 자막 메타데이터', '태그로 구조를 표현한 XML 형식의 프로그램 편성표', '엄격히 고정된 행과 열의 자료형을 미리 정의한 관계형 시청률 테이블', '헤더와 항목이 있지만 항목 구성이 파일마다 조금씩 다른 로그 파일']}
  correctAnswer={3}
  explanation="관계형 테이블은 행과 열, 자료형이 사전에 고정된 정형 데이터의 대표적인 예다. JSON, XML, 항목 구성이 유동적인 로그 파일은 모두 스스로 구조를 나타내는 반정형 데이터에 해당한다."
  optionExplanations={['JSON은 키와 계층으로 구조를 표현하는 반정형 데이터이므로 이 문제에서 찾는 답이 아니다.', 'XML은 태그로 구조를 표현하는 반정형 데이터이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 관계형 테이블은 행과 열, 자료형이 고정된 정형 데이터이며 반정형과는 반대다.', '항목 구성이 유동적인 로그 파일은 반정형 데이터의 예이므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={14}
  question="HDFS를 비롯한 분산 파일 시스템에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['하나의 큰 파일을 고정 크기 블록으로 나누어 여러 노드에 분산 저장한다', '같은 블록의 복제본을 여러 노드에 두어 특정 노드에 장애가 나도 자료를 잃지 않는다', '대용량 파일의 순차적인 일괄 처리에 강점이 있다', '모든 파일에 대해 하나의 노드만 사용하도록 강제해 네트워크 통신을 원천적으로 없앤다']}
  correctAnswer={4}
  explanation="분산 파일 시스템은 파일을 여러 노드에 나누어 저장하고 병렬로 처리하는 것이 핵심이며, 하나의 노드만 쓰도록 강제하는 것은 분산 저장의 취지 자체와 모순된다."
  optionExplanations={['블록 단위 분산 저장에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '복제를 통한 장애 대응에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '대용량 순차 처리에 강점이 있다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 하나의 노드만 쓰도록 강제하는 것은 여러 노드에 분산 저장한다는 분산 파일 시스템의 정의와 정반대다.']}
/>

<Quiz
  questionNumber={15}
  question="조직 간 관계를 노드와 간선으로 표현해 협업 네트워크의 중심성을 분석하려 한다. 이 자료를 저장하고 탐색하기에 가장 적합한 NoSQL 유형은 무엇인가"
  options={['키-값 데이터베이스', '문서 지향 데이터베이스', '그래프 데이터베이스', '컬럼 패밀리 데이터베이스']}
  correctAnswer={3}
  explanation="노드와 그 사이의 관계(간선)를 탐색하는 데는 그래프 데이터베이스가 적합하다. 키-값과 문서 지향은 개별 레코드 조회에, 컬럼 패밀리는 대량 집계에 강점이 있다."
  optionExplanations={['키-값 데이터베이스는 단일 키로 값을 조회하는 데 강점이 있으며 관계 탐색에는 적합하지 않다.', '문서 지향 데이터베이스는 문서 단위 저장에 강점이 있으며 관계 탐색이 주 목적은 아니다.', '정답이다. 그래프 데이터베이스는 노드와 간선으로 표현된 관계를 탐색하는 데 특화되어 있다.', '컬럼 패밀리 데이터베이스는 대량의 열 지향 집계에 강점이 있으며 관계 탐색과는 성격이 다르다.']}
/>

<Quiz
  questionNumber={16}
  question="한 여행 플랫폼이 여러 항공사 웹사이트에서 요금 정보를 자동으로 순회하며 수집하려 한다. 이때 쓰기에 가장 적절한 수집 기술은 무엇인가"
  options={['Sqoop', '크롤링', 'FTP', 'CDC(변경 데이터 캡처)']}
  correctAnswer={2}
  explanation="여러 웹 자원을 링크를 따라 자동으로 순회하며 자료를 수집하는 방식은 크롤링이다. Sqoop은 관계형 데이터베이스와 분산 저장 환경 사이의 정형 자료 이관, FTP는 파일 전송, CDC는 원본 시스템의 변경분만 포착하는 기술이다."
  optionExplanations={['Sqoop은 관계형 데이터베이스와 분산 저장 환경 사이의 이관 도구이며 웹사이트 순회 수집과는 성격이 다르다.', '정답이다. 여러 웹 자원을 자동으로 순회하며 정보를 수집하는 방식은 크롤링의 정의와 정확히 맞는다.', 'FTP는 파일을 서버 간 전송하는 프로토콜이며 웹 페이지 순회 수집과는 다르다.', 'CDC는 원본 시스템에서 변경된 부분만 포착해 옮기는 기술이며 여러 웹사이트를 순회하는 수집과는 다르다.']}
/>

<Quiz
  questionNumber={17}
  question="데이터 웨어하우스의 전통적 특성 중, 특정 시점의 스냅숏을 보존해 시간에 따른 값의 변화 이력을 조회할 수 있게 하는 특성은 무엇인가"
  options={['주제 지향성', '통합성', '시계열성', '비휘발성']}
  correctAnswer={3}
  explanation="시계열성은 데이터 웨어하우스가 시점별 스냅숏을 보존해 과거 시점과 현재 시점의 값을 비교할 수 있게 하는 특성이다. 주제 지향성은 업무 주제 중심 구성, 통합성은 원천 간 기준 통일, 비휘발성은 적재 후 임의 변경이 없다는 특성을 가리킨다."
  optionExplanations={['주제 지향성은 고객, 상품처럼 업무 주제를 중심으로 자료를 구성하는 특성이며 시점별 이력 보존과는 다르다.', '통합성은 여러 원천의 코드와 단위를 통일하는 특성이며 시점별 이력 보존과는 다르다.', '정답이다. 시점별 스냅숏을 보존해 변화 이력을 조회하게 하는 특성은 시계열성의 정의와 정확히 맞는다.', '비휘발성은 적재된 자료가 임의로 갱신되지 않는다는 특성이며 이력 보존 자체를 가리키지는 않는다.']}
/>

<Quiz
  questionNumber={18}
  question="특정 부서의 분석 요구에 맞춰 데이터 웨어하우스의 일부 주제 영역만 추려 별도로 구성한 작은 저장소를 가리키는 명칭은 무엇인가"
  options={['데이터 마트', 'ETL', 'OLTP', '데이터 레이크']}
  correctAnswer={1}
  explanation="특정 부서나 주제에 맞춰 데이터 웨어하우스의 일부를 추려 구성한 저장소는 데이터 마트다. ETL은 처리 절차, OLTP는 실시간 거래 처리 시스템, 데이터 레이크는 원본 형태를 유지한 대규모 저장소를 가리킨다."
  optionExplanations={['정답이다. 특정 부서나 주제에 맞춰 데이터 웨어하우스 일부를 추린 저장소는 데이터 마트의 정의와 정확히 맞는다.', 'ETL은 추출·변환·적재라는 처리 절차를 가리키는 명칭이지 저장소가 아니다.', 'OLTP는 실시간 거래를 처리하는 시스템을 가리키며 부서별 분석용 저장소와는 다르다.', '데이터 레이크는 원본 형태 그대로 대규모로 저장하는 저장소이며 특정 부서를 위해 추린 구조가 아니다.']}
/>

<Quiz
  questionNumber={19}
  question="분석 마스터플랜의 적용 범위와 도입 우선순위를 정할 때 직접 기준으로 가장 부적절한 것은 무엇인가"
  options={['사용 가능한 데이터의 적용 가능성', '분석 기술의 적용 가능성', '업무 프로세스에 내재화할 가능성', '담당자가 당장 실행하기 편한 정도만을 단독 기준으로 삼는 것']}
  correctAnswer={4}
  explanation="개인의 단기 실행 편의만으로 전략적 마스터플랜의 범위를 결정하는 것은 전사 관점의 우선순위 기준으로 부적절하다."
  optionExplanations={['데이터 적용 가능성은 마스터플랜 우선순위의 정당한 기준이므로 이 문제에서 찾는 답이 아니다.', '기술의 적용 가능성은 마스터플랜 우선순위의 정당한 기준이므로 이 문제에서 찾는 답이 아니다.', '업무 내재화 가능성은 마스터플랜 우선순위의 정당한 기준이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 담당자 개인의 단기 실행 편의만을 단독 기준으로 삼는 것은 전사 전략 수립의 정당한 기준이 될 수 없다.']}
/>

<Quiz
  questionNumber={20}
  question="분석 프로젝트의 전체 범위를 계층적으로 쪼개 관리 가능한 작업 패키지와 담당 단위로 만드는 산출물의 명칭은 무엇인가"
  options={['WBS(작업 분해 구조)', 'CRISP-DM', 'ERD(개체관계도)', 'KPI(핵심성과지표)']}
  correctAnswer={1}
  explanation="프로젝트 범위를 계층적으로 분해해 작업 패키지와 담당 단위로 만드는 산출물은 WBS(Work Breakdown Structure, 작업 분해 구조)다."
  optionExplanations={['정답이다. 범위를 계층적으로 쪼개 작업 패키지로 만드는 산출물은 WBS의 정의와 정확히 맞는다.', 'CRISP-DM은 분석 방법론의 단계 체계이며 작업 분해 산출물이 아니다.', 'ERD는 데이터베이스 설계에서 개체 간 관계를 그린 도표이며 작업 분해와는 다르다.', 'KPI는 성과를 측정하는 지표이며 작업을 쪼개는 산출물이 아니다.']}
/>

## 2과목: 빅데이터 탐색 (21~40)

<Quiz
  questionNumber={21}
  question="한 항공사가 예약 자료의 전처리를 정제, 통합, 축소, 변환 네 단계로 나누어 설계했다. 각 단계에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['정제 단계에서는 탑승 시각이 비어 있거나 좌석 번호가 오류로 기록된 값을 찾아 보정하거나 제외한다', '통합 단계에서는 예약 시스템과 탑승 수속 시스템의 기록을 공통 키로 결합해 하나의 분석용 자료로 만든다', '변환 단계에서는 항공권 가격에 로그를 취하거나 지연 시간을 구간으로 나누어 분석에 쓰기 좋은 형태로 바꾼다', '축소 단계에서는 표기 방식이 다른 공항 코드를 하나의 기준으로 통일해 정합성을 맞춘다']}
  correctAnswer={4}
  explanation="서로 다른 표기를 하나의 기준으로 통일하는 작업은 정제 또는 통합 단계의 목적이다. 축소는 관측치 수나 변수 개수 같은 자료의 규모를 줄이는 단계이므로 표기 통일을 축소의 목적으로 서술한 4번이 옳지 않다."
  optionExplanations={['결측 및 오류값 보정은 정제 단계의 정의와 일치하므로 옳은 설명이다.', '서로 다른 시스템의 기록을 공통 키로 합치는 것은 통합 단계의 정의와 일치하므로 옳은 설명이다.', '로그 변환과 구간화는 변환 단계의 정의와 일치하므로 옳은 설명이다.', '정답이다. 표기 통일은 정제·통합의 목적이며, 축소는 관측치나 변수의 규모를 줄이는 단계이므로 이 설명은 단계를 잘못 짝지었다.']}
/>

<Quiz
  questionNumber={22}
  question="설문 응답 자료의 결측값 처리 기법에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['완전 분석법은 결측이 하나라도 있는 관측치를 통째로 제외하는 방식이라 표본이 크게 줄어들 수 있다', '평균 대치법은 결측 자리를 관측된 값들의 평균으로 채우는 방식이라 원래 산포보다 분산을 작게 만드는 경향이 있다', '핫덱 대치법은 특성이 비슷한 다른 관측치의 값을 그대로 가져와 결측을 채우는 방식이다', '다중 대치법은 결측 위치와 무관하게 항상 관측치를 삭제해 완전한 자료만 남기는 방식이다']}
  correctAnswer={4}
  explanation="다중 대치법은 결측 자리를 여러 벌로 서로 다르게 채운 뒤 각각 분석해 결과를 결합하는 기법이다. 관측치를 삭제해 완전한 자료만 남긴다는 서술은 완전 분석법에 대한 설명이며 다중 대치법과는 다르다."
  optionExplanations={['완전 분석법의 특징에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '평균 대치법이 분산을 작게 만든다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '핫덱 대치법에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 관측치를 삭제해 완전한 자료만 남기는 것은 완전 분석법의 특징이며 다중 대치법과는 다르다.']}
/>

<Quiz
  questionNumber={23}
  question="지역 실태조사 자료에서 다른 관측치와 크게 동떨어진 값들이 발견되어 발생 경위를 추적하고 있다. 이상값이 자료에 들어오게 된 원인으로 보기 어려운 것은 무엇인가"
  options={['측정 장비의 영점이 어긋나 특정 시간대에 기록된 값들이 일괄적으로 부풀려진 경우', '설문 응답을 옮겨 입력하면서 자릿수를 잘못 입력해 값이 크게 커진 경우', '모집단에 속하지 않는 이질적인 대상이 표본에 섞여 규모가 전혀 다른 값이 기록된 경우', '분석을 모두 마친 뒤 산출된 요약 수치를 보고서에 옮겨 적으면서 단위를 잘못 표기한 경우']}
  correctAnswer={4}
  explanation="이상값은 자료가 만들어지는 과정, 즉 측정, 입력, 표본추출 단계에서 값이 왜곡될 때 생긴다. 이미 산출된 결과를 보고서에 옮기는 단계의 실수는 자료 자체에 들어 있는 이상값의 발생 원인이 아니다."
  optionExplanations={['측정 오류로 인한 이상값 발생은 대표적인 원인이므로 이 문제에서 찾는 답이 아니다.', '입력 오류로 인한 이상값 발생은 대표적인 원인이므로 이 문제에서 찾는 답이 아니다.', '이질적 표본 혼입으로 인한 이상값 발생은 대표적인 원인이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 이미 산출된 결과를 옮겨 적는 보고 단계의 실수는 자료 생성 과정의 오류가 아니므로 이상값의 발생 원인으로 보기 어렵다.']}
/>

<Quiz
  questionNumber={24}
  question="상자수염그림(박스플롯)의 구성 요소에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['상자의 아래위 경계는 각각 제1사분위수와 제3사분위수다', '상자 안의 굵은 선은 평균값을 나타낸다', '수염은 사분위범위의 1.5배를 넘지 않는 범위에서 가장 먼 관측치까지 뻗는다', '수염 밖에 점으로 찍힌 값은 이상값 후보로 본다']}
  correctAnswer={2}
  explanation="상자 안의 굵은 선은 평균이 아니라 중앙값(제2사분위수)을 나타낸다. 평균은 상자수염그림의 표준 구성 요소가 아니다."
  optionExplanations={['상자의 경계에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 상자 안의 굵은 선은 평균이 아니라 중앙값을 나타내므로 이 설명이 틀렸다.', '수염의 길이 기준에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '수염 밖 점이 이상값 후보라는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={25}
  question="다음 중 이산형 변수로 보기 가장 적절한 것은 무엇인가"
  options={['한 가구가 한 달 동안 주문한 배달 건수', '제품 표면의 두께를 마이크로미터 단위로 측정한 값', '실험 참가자의 체온을 섭씨 0.1도 단위까지 측정한 값', '배송 차량이 하루 동안 주행한 거리를 킬로미터로 기록한 값']}
  correctAnswer={1}
  explanation="배달 건수는 셀 수 있는 정수 값만 가지는 이산형 변수다. 두께, 체온, 주행 거리는 임의의 실수 값을 가질 수 있는 연속형 변수다."
  optionExplanations={['정답이다. 건수는 0, 1, 2처럼 셀 수 있는 값만 가지므로 이산형 변수에 해당한다.', '두께는 임의의 실수 값을 가질 수 있는 연속형 변수이므로 이 문제에서 찾는 답이 아니다.', '체온은 임의의 실수 값을 가질 수 있는 연속형 변수이므로 이 문제에서 찾는 답이 아니다.', '주행 거리는 임의의 실수 값을 가질 수 있는 연속형 변수이므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={26}
  question="다음 중 파생변수 생성의 사례로 보기 어려운 것은 무엇인가"
  options={['가입일과 최근 로그인일의 차이로 휴면 일수를 계산한다', '월별 매출을 전월 매출로 나누어 전월 대비 증감률을 계산한다', '주문 시각을 시, 분, 초 그대로 문자열 열에 다시 옮겨 적는다', '위도와 경도로 배송 권역 범주를 만든다']}
  correctAnswer={3}
  explanation="파생변수 생성은 기존 변수를 조합하거나 계산해 새로운 정보를 담은 변수를 만드는 작업이다. 값을 계산 없이 형식만 바꿔 옮기는 것은 새로운 정보를 만들지 않으므로 파생변수 생성으로 볼 수 없다."
  optionExplanations={['두 시점의 차이를 계산해 새 의미의 변수를 만든 것이므로 파생변수 생성의 예에 해당한다.', '두 값을 나누어 새로운 의미의 변수를 만든 것이므로 파생변수 생성의 예에 해당한다.', '정답이다. 계산 없이 형식만 바꿔 옮기는 것은 새로운 정보를 만들지 않으므로 파생변수 생성이 아니다.', '두 변수를 조합해 새로운 범주를 만든 것이므로 파생변수 생성의 예에 해당한다.']}
/>

**문항 27 자료**

| 항목 | 값 |
|---|---|
| 설비 가동시간 평균 | 60시간 |
| 설비 가동시간 표준편차 | 8시간 |
| 이번 측정값 | 76시간 |
| 경보 기준 | 절댓값 z가 2 이상이면 경보 |

<Quiz
  questionNumber={27}
  question="위 표의 자료로 표준화 점수를 계산할 때, 올바른 z값과 판정은 무엇인가"
  options={['z=2, 상한 경보', 'z=-2, 하한 경보', 'z=8, 경보 없음', 'z=0.5, 경보 없음']}
  correctAnswer={1}
  explanation="z=(76-60)/8=16/8=2다. 평균보다 표준편차 2개만큼 높고 절댓값이 경보 기준인 2 이상이므로 상한 경보에 해당한다."
  optionExplanations={['정답이다. (76-60)을 8로 나누면 z=2이고, 측정값이 평균보다 높은 쪽이므로 상한 경보다.', '분자의 부호를 반대로 계산했을 때 나올 수 있는 오답이다.', '표준편차로 나누지 않고 차이값 16을 절반으로 잘못 줄였을 때 나올 수 있는 오답이다.', '분자와 분모를 뒤바꿔 8/16을 계산했을 때 나올 수 있는 오답이다.']}
/>

**문항 28 자료**

| 항목 | 값 |
|---|---|
| 원하는 오차한계(E) | 0.04 |
| 신뢰수준 95퍼센트의 z값 | 1.96 |
| 사전 추정 비율(p) | 0.5 |

<Quiz
  questionNumber={28}
  question="위 표의 조건으로 모비율 신뢰구간을 추정할 때 필요한 최소 표본크기는 얼마인가(공식은 n=z제곱×p×(1-p)/E제곱이며 올림하여 정수로 답한다)"
  options={['601', '300', '150', '1,200']}
  correctAnswer={1}
  explanation="n=1.96의 제곱×0.5×0.5/0.04의 제곱=3.8416×0.25/0.0016=0.9604/0.0016=600.25이며, 표본크기는 부족하지 않도록 올림해 601이다."
  optionExplanations={['정답이다. 공식에 값을 대입하면 600.25가 나오고, 표본은 부족하면 안 되므로 올림해 601이다.', '분모의 제곱을 하지 않고 계산했을 때 나올 수 있는 값과 비슷한 오답이다.', 'z값을 제곱하지 않고 그대로 곱했을 때 나올 수 있는 값과 비슷한 오답이다.', 'E를 제곱하지 않고 그대로 나누었을 때 나올 수 있는 값과 비슷한 오답이다.']}
/>

**문항 29 자료**

| 항목 | 값 |
|---|---|
| 모평균(귀무가설 값) | 50 |
| 모표준편차 | 6 |
| 표본평균 | 52 |
| 표본크기 | 36 |

<Quiz
  questionNumber={29}
  question="위 표의 자료로 모평균에 대한 z검정을 수행할 때, 검정통계량 z값은 얼마인가(공식은 z=(표본평균-모평균)/(모표준편차/표본크기의 제곱근)이다)"
  options={['2', '12', '0.33', '1']}
  correctAnswer={1}
  explanation="표준오차는 6을 36의 제곱근인 6으로 나눈 1이다. z=(52-50)/1=2다."
  optionExplanations={['정답이다. 표준오차 1로 차이값 2를 나누면 z=2다.', '표준오차로 나누지 않고 분자와 표본크기를 혼동해 곱했을 때 나올 수 있는 값이다.', '분자와 분모를 뒤바꿔 1을 2로 나누었을 때 나올 수 있는 값이다.', '표준오차를 2로 잘못 계산했을 때 나올 수 있는 값이다.']}
/>

**문항 30 자료**

| 구분 | 표본크기 | 성공 건수 |
|---|---|---|
| A집단 | 100 | 60 |
| B집단 | 150 | 75 |

<Quiz
  questionNumber={30}
  question="위 표에서 두 집단의 표본 성공비율 차이(A집단 비율 - B집단 비율)는 얼마인가"
  options={['0.1', '0.6', '0.5', '0.15']}
  correctAnswer={1}
  explanation="A집단 비율은 60/100=0.6, B집단 비율은 75/150=0.5다. 두 비율의 차이는 0.6-0.5=0.1이다."
  optionExplanations={['정답이다. 0.6에서 0.5를 빼면 0.1이다.', 'A집단 비율 자체를 차이값으로 착각했을 때 나올 수 있는 오답이다.', 'B집단 비율 자체를 차이값으로 착각했을 때 나올 수 있는 오답이다.', '두 비율을 반대로 빼거나 계산을 잘못했을 때 나올 수 있는 오답이다.']}
/>

**문항 31 자료**

| 항목 | 값 |
|---|---|
| 부품 개수(n) | 40 |
| 불량률(p) | 0.1 |

<Quiz
  questionNumber={31}
  question="위 표의 조건에서 각 부품이 서로 독립으로 불량 여부가 정해질 때, 불량품 수 X의 분산은 얼마인가(이항분포를 가정한다)"
  options={['3.6', '4', '0.36', '36']}
  correctAnswer={1}
  explanation="X는 이항분포 B(40, 0.1)를 따르므로 분산은 np(1-p)=40×0.1×0.9=3.6이다."
  optionExplanations={['정답이다. 40×0.1×0.9를 계산하면 3.6이다.', 'np만 계산하고 (1-p)를 곱하지 않았을 때 나올 수 있는 오답이다.', '소수점 자리를 잘못 옮겼을 때 나올 수 있는 오답이다.', 'n을 제곱하는 등 공식을 잘못 적용했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={32}
  question="모집단 평균 mu, 분산 시그마 제곱에서 독립표본 n개의 표본평균에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['표본평균의 기대값은 mu와 같다', '표본평균의 분산은 시그마 제곱을 n으로 나눈 값이다', '표본 크기 n이 커질수록 표본평균의 분산은 줄어든다', '표본 크기 n이 커지면 표본평균의 표준편차는 시그마 제곱으로 수렴한다']}
  correctAnswer={4}
  explanation="표본 크기가 커지면 표본평균의 표준편차는 0으로 수렴한다. 시그마 제곱이라는 고정된 값으로 수렴한다고 서술한 4번이 옳지 않다."
  optionExplanations={['표본평균의 기대값에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '표본평균의 분산 공식은 옳으므로 이 문제에서 찾는 답이 아니다.', '표본크기가 커질수록 분산이 줄어든다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 표본크기가 커지면 표본평균의 표준편차는 시그마 제곱이 아니라 0에 가까워진다.']}
/>

<Quiz
  questionNumber={33}
  question="범주형 자료에서 두 변수 사이에 관련성이 있는지 검정하는 카이제곱 독립성 검정과, 한 변수가 특정 분포를 따르는지 검정하는 적합도 검정의 관계에 대한 설명으로 옳은 것은 무엇인가"
  options={['두 검정은 같은 목적을 가지므로 항상 같은 결론을 낸다', '독립성 검정은 두 범주형 변수의 관련성을, 적합도 검정은 한 변수가 가정한 분포를 따르는지를 확인한다는 점에서 목적이 다르다', '적합도 검정은 두 변수 이상에서만 쓸 수 있다', '독립성 검정은 연속형 변수에만 적용할 수 있다']}
  correctAnswer={2}
  explanation="독립성 검정은 두 범주형 변수 사이에 관련성이 있는지를, 적합도 검정은 한 변수의 관측 분포가 가정한 이론적 분포와 맞는지를 확인한다는 점에서 검정 목적 자체가 다르다."
  optionExplanations={['두 검정은 통계량 구성 방식은 비슷하지만 목적이 다르므로 항상 같은 결론을 낸다고 볼 수 없다.', '정답이다. 독립성 검정과 적합도 검정은 각각 관련성 확인과 분포 적합 확인이라는 서로 다른 목적을 갖는다.', '적합도 검정은 한 변수의 분포를 확인하는 검정이며 두 변수 이상을 요구하지 않는다.', '독립성 검정은 범주형 변수의 교차표를 이용하는 검정이며 연속형 변수 전용이 아니다.']}
/>

<Quiz
  questionNumber={34}
  question="분포가 어느 한쪽으로 치우친 정도와 그 방향을 나타내는 통계량은 무엇인가"
  options={['분산', '왜도', '첨도', '변동계수']}
  correctAnswer={2}
  explanation="왜도는 분포의 비대칭성과 꼬리 방향을 측정하는 통계량이다."
  optionExplanations={['분산은 값들이 평균에서 얼마나 퍼졌는지를 나타내는 산포 통계량이며 치우침 방향과는 다르다.', '정답이다. 왜도는 분포의 비대칭성과 꼬리 방향을 측정하는 통계량이다.', '첨도는 분포 봉우리의 뾰족함을 나타내는 통계량이며 치우침 방향과는 다르다.', '변동계수는 표준편차를 평균으로 나눈 상대적 산포 지표이며 치우침 방향과는 다르다.']}
/>

<Quiz
  questionNumber={35}
  question="광고비와 매출의 관계에서 매장 크기의 영향을 통제한 뒤 남는 선형 연관을 나타내는 상관계수는 무엇인가"
  options={['피어슨 상관계수', '스피어만 상관계수', '편상관계수', '켄달의 타우']}
  correctAnswer={3}
  explanation="다른 변수의 영향을 통제한 뒤 남는 두 변수의 선형 연관을 나타내는 상관계수는 편상관계수다."
  optionExplanations={['피어슨 상관계수는 두 변수 사이의 단순 선형 관계를 재는 지표이며 제3의 변수를 통제하지 않는다.', '스피어만 상관계수는 순위를 이용한 상관 지표이며 제3의 변수 통제와는 무관하다.', '정답이다. 제3의 변수를 통제한 뒤 남는 선형 연관을 나타내는 지표는 편상관계수다.', '켄달의 타우는 순위 일치도를 재는 지표이며 제3의 변수 통제와는 무관하다.']}
/>

<Quiz
  questionNumber={36}
  question="고객지원 센터의 상담 대기시간은 대부분 짧지만 일부 장기 대기 건 때문에 오른쪽으로 길게 치우친 분포를 보인다. 이때 성립할 가능성이 가장 큰 대소 관계는 무엇인가"
  options={['최빈값 < 중앙값 < 평균', '평균 < 중앙값 < 최빈값', '평균 = 중앙값 = 최빈값', '중앙값 < 최빈값 < 평균']}
  correctAnswer={1}
  explanation="오른쪽으로 긴 꼬리를 가진 분포에서는 극단적으로 큰 값들이 평균을 오른쪽으로 끌어당기므로 최빈값이 가장 작고 평균이 가장 크며 중앙값은 그 사이에 놓인다."
  optionExplanations={['정답이다. 오른쪽 꼬리 분포에서는 최빈값, 중앙값, 평균 순으로 값이 커지는 배열이 일반적으로 성립한다.', '이 순서는 왼쪽으로 긴 꼬리를 가진 분포에서 나타나는 배열이며 이 사례와는 반대다.', '세 값이 모두 같은 것은 좌우 대칭 분포에서 나타나며 이 사례처럼 치우친 분포와는 맞지 않는다.', '이 순서는 최빈값과 평균의 위치를 뒤바꾼 배열이다.']}
/>

<Quiz
  questionNumber={37}
  question="전국 초등학교를 조사하기 위해 학교를 집단으로 정의하고, 학교 15곳을 무작위로 뽑은 뒤 선택된 학교의 모든 학생을 조사했다. 이 표본설계는 무엇인가"
  options={['단순임의추출', '층화추출', '군집추출', '계통추출']}
  correctAnswer={3}
  explanation="집단(학교)을 먼저 무작위로 선택하고 선택된 집단 내부의 모든 구성원을 조사하는 방법은 군집추출이다."
  optionExplanations={['단순임의추출은 개별 학생을 직접 무작위로 뽑는 방법이며 학교 단위로 뽑는 이 사례와는 다르다.', '층화추출은 모집단을 층으로 나눈 뒤 각 층에서 표본을 뽑는 방법이며 이 사례는 층을 나누지 않았다.', '정답이다. 학교라는 집단을 먼저 선택하고 그 안의 전체 구성원을 조사하는 방식은 군집추출의 정의와 정확히 맞는다.', '계통추출은 일정한 간격으로 표본을 뽑는 방법이며 집단 전체를 뽑는 이 사례와는 다르다.']}
/>

**문항 38 자료**

| 구분 | 표본크기 |
|---|---|
| 1집단 | 15 |
| 2집단 | 20 |

<Quiz
  questionNumber={38}
  question="위 표의 두 독립표본으로 평균 차이를 검정하는 독립표본 t검정을 수행할 때, 등분산을 가정한 자유도는 얼마인가(공식은 n1+n2-2이다)"
  options={['33', '35', '14', '19']}
  correctAnswer={1}
  explanation="등분산을 가정한 독립표본 t검정의 자유도는 두 표본크기의 합에서 2를 뺀 값이므로 15+20-2=33이다."
  optionExplanations={['정답이다. 15와 20을 더하고 2를 빼면 33이다.', '2를 빼지 않고 두 표본크기의 합만 계산했을 때 나올 수 있는 오답이다.', '한 집단의 자유도(15-1=14)만 계산했을 때 나올 수 있는 오답이다.', '다른 집단의 자유도(20-1=19)만 계산했을 때 나올 수 있는 오답이다.']}
/>

**문항 39 자료**

| 구분 | 열 범주 수 |
|---|---|
| 행 범주 수 | 3 |
| 열 범주 수 | 4 |

<Quiz
  questionNumber={39}
  question="위 표와 같이 행이 3개, 열이 4개인 분할표로 카이제곱 독립성 검정을 수행할 때, 검정통계량의 자유도는 얼마인가(공식은 (행 범주 수-1)×(열 범주 수-1)이다)"
  options={['6', '12', '7', '5']}
  correctAnswer={1}
  explanation="자유도는 (3-1)×(4-1)=2×3=6이다."
  optionExplanations={['정답이다. (3-1)과 (4-1)을 곱하면 6이다.', '행 범주 수와 열 범주 수를 그대로 곱했을 때 나올 수 있는 오답이다.', '1을 빼지 않고 계산을 조합했을 때 나올 수 있는 오답이다.', '행과 열 범주 수를 더한 값에서 2를 뺐을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={40}
  question="불량품 15개가 포함된 90개 납품 상자에서 10개를 검사하며 검사한 제품은 상자에 되돌려 놓지 않는다. 불량품 수에 초기하분포를 적용해야 하는 이유로 가장 적절한 것은 무엇인가"
  options={['각 추출이 독립이고 불량 확률이 일정하기 때문이다', '연속형 측정값의 평균을 구하기 때문이다', '유한 모집단에서 비복원추출해 추출 간 의존성과 성공확률 변화가 생기기 때문이다', '불량 여부가 정규분포를 따르기 때문이다']}
  correctAnswer={3}
  explanation="유한한 상자에서 제품을 되돌리지 않으므로 남은 구성에 따라 다음 불량 확률이 달라지고 추출들이 독립이 아니어서 초기하분포를 쓴다."
  optionExplanations={['비복원추출에서는 추출마다 남은 구성이 달라져 확률이 변하므로 독립이고 확률이 일정하다는 서술은 틀렸다.', '초기하분포는 이산적인 불량품 개수를 다루는 것이지 연속형 측정값의 평균을 구하는 상황이 아니다.', '정답이다. 유한 모집단에서 되돌리지 않고 뽑으면 추출 간 의존성과 성공확률 변화가 생기므로 초기하분포를 적용해야 한다.', '불량 여부는 두 값 중 하나를 갖는 범주형 결과이며 정규분포와는 관련이 없다.']}
/>

## 3과목: 빅데이터 모델링 (41~60)

<Quiz
  questionNumber={41}
  question="다음 중 지도학습 과제로 보기 어려운 것은 무엇인가"
  options={['정답 표지가 붙은 신용 자료로 새 고객의 부도 여부를 예측하는 과제', '판매 이력을 이용해 다음 분기 매출액을 예측하는 과제', '레이블 없이 구매 자료의 항목 사이 연관규칙을 찾는 과제', '스팸 여부 표지가 붙은 메일로 새 메일의 스팸 여부를 분류하는 과제']}
  correctAnswer={3}
  explanation="정답 표지 없이 항목 사이의 연관 패턴을 찾는 연관규칙 탐색은 비지도학습 과제에 해당한다."
  optionExplanations={['부도 여부라는 표지가 있는 지도학습(분류) 과제이므로 이 문제에서 찾는 답이 아니다.', '매출액이라는 연속형 정답이 있는 지도학습(회귀) 과제이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 레이블 없이 항목 간 연관 패턴을 찾는 것은 비지도학습인 연관규칙 탐색에 해당한다.', '스팸 여부라는 표지가 있는 지도학습(분류) 과제이므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={42}
  question="아파트 실거래가를 예측하는 선형회귀모형의 가정 충족 여부를 점검하려 한다. 다음 설명 중 옳지 않은 것은 무엇인가"
  options={['오차항의 분산이 설명변수 값과 무관하게 일정해야 한다는 등분산성 가정이 있다', '서로 다른 관측치의 오차항이 독립이어야 한다는 가정이 있다', '오차항이 정규분포를 따라야 한다는 가정이 있다', '설명변수들이 서로 강한 상관을 가져야 회귀계수를 정확히 추정할 수 있다는 가정이 있다']}
  correctAnswer={4}
  explanation="선형회귀의 표준 가정에는 오차항의 등분산성, 독립성, 정규성, 평균 0 등이 포함되지만, 설명변수 간에 강한 상관이 있어야 한다는 가정은 없다. 오히려 설명변수 간 강한 상관은 다중공선성 문제를 일으켜 계수 추정을 불안정하게 만든다."
  optionExplanations={['등분산성 가정은 선형회귀의 표준 가정이므로 이 문제에서 찾는 답이 아니다.', '독립성 가정은 선형회귀의 표준 가정이므로 이 문제에서 찾는 답이 아니다.', '정규성 가정은 선형회귀의 표준 가정이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 설명변수 간 강한 상관은 오히려 다중공선성 문제를 일으키므로 이를 가정으로 서술한 것은 틀렸다.']}
/>

**문항 43 자료**

| 변수 | 계수 | 표준오차 | t | 유의확률 |
|---|---|---|---|---|
| 상수항 | 8.10 | 2.05 | 3.95 | 0.000 |
| 방문자수(백명) | 1.10 | 0.25 | 4.40 | 0.000 |
| 인접경쟁점수(개) | -0.40 | 0.19 | -2.11 | 0.038 |
| 직원수(명) | 0.02 | 0.03 | 0.67 | 0.505 |

<Quiz
  questionNumber={43}
  question="위 표는 편의점 지점 60곳의 월 매출(백만원)을 세 변수로 설명한 다중회귀 결과다. 유의수준 0.05에서 다음 진술 중 옳은 것만을 모두 고른 것은 다음과 같다. ㄱ. 인접경쟁점수가 많을수록 월 매출은 낮아지는 방향으로 추정되었다. ㄴ. 직원수는 월 매출에 유의한 영향을 준다. ㄷ. 다른 변수가 일정할 때 방문자수(백명)가 1단위 늘면 월 매출은 평균 1.10백만원 증가하는 것으로 해석한다."
  options={['ㄱ, ㄷ', 'ㄱ, ㄴ', 'ㄴ, ㄷ', 'ㄱ, ㄴ, ㄷ']}
  correctAnswer={1}
  explanation="인접경쟁점수 계수가 -0.40으로 음수이므로 ㄱ은 옳다. ㄷ도 다중회귀 계수의 표준 해석이므로 옳다. 직원수의 유의확률은 0.505로 0.05보다 크므로 ㄴ은 틀렸다."
  optionExplanations={['정답이다. 인접경쟁점수 계수의 부호(ㄱ)와 방문자수 계수의 표준 해석(ㄷ)이 모두 옳다.', '직원수의 유의확률이 0.505로 기준을 넘어 유의하지 않으므로 ㄴ을 포함한 이 조합은 틀렸다.', 'ㄱ이 빠져 있고 유의하지 않은 ㄴ이 포함되어 이 조합은 틀렸다.', '유의하지 않은 ㄴ이 포함되어 있어 이 조합은 틀렸다.']}
/>

<Quiz
  questionNumber={44}
  question="회귀모형의 결정계수에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['반응변수의 총변동 가운데 회귀모형이 설명하는 변동이 차지하는 비율이다', '설명변수를 추가하면 결정계수는 항상 감소한다', '수정된 결정계수는 설명변수 개수에 대한 벌점을 반영해 결정계수보다 크지 않다', '설명변수가 하나뿐인 회귀모형에서는 결정계수가 상관계수의 제곱과 같다']}
  correctAnswer={2}
  explanation="설명변수를 추가하면 잔차제곱합은 줄어들거나 최소한 유지되므로 결정계수는 감소하지 않고 커지거나 그대로다. 항상 감소한다고 서술한 2번이 증감 방향을 반대로 진술했다."
  optionExplanations={['결정계수의 정의는 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 설명변수를 추가하면 결정계수는 감소하지 않고 커지거나 그대로 유지되므로 항상 감소한다는 서술은 틀렸다.', '수정된 결정계수의 성질은 옳으므로 이 문제에서 찾는 답이 아니다.', '단순회귀에서 결정계수와 상관계수 제곱의 관계는 옳으므로 이 문제에서 찾는 답이 아니다.']}
/>

<Quiz
  questionNumber={45}
  question="다중회귀에서 분산팽창지수(VIF)에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['각 설명변수를 나머지 설명변수로 회귀한 결정계수를 이용해 계산한다', '값이 클수록 그 설명변수의 계수 분산이 많이 부풀려졌다는 뜻이다', '통상 10을 넘으면 다중공선성을 의심한다', 'VIF는 표본 크기와 무관하게 항상 0과 1 사이의 값을 갖는다']}
  correctAnswer={4}
  explanation="VIF는 1/(1-R제곱) 형태로 계산되며 0과 1 사이로 제한되지 않고 1 이상의 값을 가지며 다중공선성이 심할수록 커진다."
  optionExplanations={['VIF의 계산 방식에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', 'VIF가 클수록 계수 분산이 부풀려진다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', 'VIF 10을 기준으로 삼는다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. VIF는 1 이상의 값을 가지며 다중공선성이 심할수록 커지므로 0과 1 사이로 제한된다는 서술은 틀렸다.']}
/>

**문항 46 자료**

| 구분 | 정시 배송 | 지연 배송 | 합계 |
|---|---|---|---|
| 신규 물류 경로 이용 | 108 | 42 | 150 |
| 기존 물류 경로 이용 | 60 | 90 | 150 |

<Quiz
  questionNumber={46}
  question="위 표에서 신규 물류 경로를 이용한 배송의 정시 배송 오즈비(오즈의 비, 신규 대비 기존)는 얼마인가"
  options={['3.86', '1.8', '2.57', '0.65']}
  correctAnswer={1}
  explanation="신규 경로의 오즈는 108/42=2.571이고 기존 경로의 오즈는 60/90=0.667이다. 오즈비는 2.571을 0.667로 나눈 값이므로 약 3.86이다."
  optionExplanations={['정답이다. 신규 경로의 오즈 2.571을 기존 경로의 오즈 0.667로 나누면 약 3.86이다.', '두 비율(108/150과 60/150)의 비를 계산했을 때 나올 수 있는 값이다.', '신규 경로의 오즈 자체를 오즈비로 착각했을 때 나올 수 있는 값이다.', '오즈비의 역수를 계산했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={47}
  question="구독 갱신 여부를 예측하는 로지스틱 회귀에서 갱신 확률 p에 대해 로그 오즈가 0.4에 만족도 점수 계수 0.6을 곱한 값을 더한 식으로 얻어졌다. 다른 변수가 일정할 때 만족도 점수가 1점 늘면 갱신의 오즈는 어떻게 변하는가"
  options={['e의 0.6제곱인 약 1.82배가 된다', '0.6만큼 더해진다', '0.6배로 줄어든다', '갱신 확률이 0.6만큼 커진다']}
  correctAnswer={1}
  explanation="로짓 척도의 계수는 로그 오즈의 덧셈 변화를 나타내므로 원래 오즈 척도로 되돌리면 e의 계수제곱만큼 곱해진다. 계수가 0.6이므로 오즈는 e의 0.6제곱, 약 1.82배가 된다."
  optionExplanations={['정답이다. 로짓 계수 0.6을 지수화하면 오즈는 e의 0.6제곱, 약 1.82배가 된다.', '계수 0.6은 오즈에 곱해지는 배율의 밑이 되는 값이지 오즈에 그대로 더해지는 값이 아니다.', '계수가 양수이므로 오즈는 줄어드는 것이 아니라 늘어나야 하므로 이 방향은 틀렸다.', '로짓 계수는 확률 자체의 변화량이 아니라 로그 오즈의 변화량이므로 확률이 0.6만큼 커진다는 해석은 틀렸다.']}
/>

**문항 48 자료**

| 구분 | 관측치 수 |
|---|---|
| 노드 전체 | 40 |
| 클래스 A | 30 |
| 클래스 B | 10 |

<Quiz
  questionNumber={48}
  question="위 표의 노드에 대한 지니지수는 얼마인가(공식은 1에서 각 클래스 비율 제곱의 합을 뺀 값이다)"
  options={['0.375', '0.5', '0.625', '0.25']}
  correctAnswer={1}
  explanation="클래스 A 비율은 30/40=0.75, 클래스 B 비율은 10/40=0.25다. 지니지수는 1-(0.75의 제곱+0.25의 제곱)=1-(0.5625+0.0625)=1-0.625=0.375다."
  optionExplanations={['정답이다. 1에서 0.5625와 0.0625의 합인 0.625를 빼면 0.375다.', '두 비율 제곱의 합인 0.625를 그대로 지니지수로 착각했을 때 나올 수 있는 값이다.', '비율을 제곱하지 않고 그대로 더한 값(0.75+0.25=1)에서 1을 빼는 등 계산을 잘못했을 때 나올 수 있는 값과 비슷하다.', '클래스 B 비율 0.25를 그대로 지니지수로 착각했을 때 나올 수 있는 값이다.']}
/>

**문항 49 자료**

| 클래스 | 관측치 수 |
|---|---|
| 클래스 A | 4 |
| 클래스 B | 2 |
| 클래스 C | 2 |

<Quiz
  questionNumber={49}
  question="위 표와 같이 8개 관측치가 세 클래스로 나뉜 노드의 엔트로피는 얼마인가(밑이 2인 로그를 사용하며 공식은 각 클래스 비율에 그 비율의 로그를 곱한 값의 합에 음의 부호를 붙인 것이다)"
  options={['1.5', '2.0', '1.0', '0.75']}
  correctAnswer={1}
  explanation="각 클래스 비율은 0.5, 0.25, 0.25다. 엔트로피는 -(0.5×log2 0.5+0.25×log2 0.25+0.25×log2 0.25)=0.5×1+0.25×2+0.25×2=0.5+0.5+0.5=1.5비트다."
  optionExplanations={['정답이다. 세 항을 각각 계산해 더하면 0.5+0.5+0.5=1.5비트다.', '클래스가 4개일 때의 최대 엔트로피(로그2 4=2)와 혼동했을 때 나올 수 있는 값이다.', '두 클래스가 균등할 때의 엔트로피(로그2 2=1)와 혼동했을 때 나올 수 있는 값이다.', '계산 중 한 항을 빠뜨렸을 때 나올 수 있는 값이다.']}
/>

**문항 50 자료**

| 마디 | 클래스 A(양성) | 클래스 B(음성) | 엔트로피 |
|---|---|---|---|
| 부모(8개) | 6 | 2 | 0.811 |
| 왼쪽 자식(4개) | 4 | 0 | 0 |
| 오른쪽 자식(4개) | 2 | 2 | 1.0 |

<Quiz
  questionNumber={50}
  question="위 표의 분할로 얻는 정보이득은 얼마인가(공식은 부모 엔트로피에서 자식 노드 엔트로피의 가중평균을 뺀 값이며 가중치는 각 자식의 관측치 비율이다)"
  options={['0.311', '0.811', '0.5', '1.0']}
  correctAnswer={1}
  explanation="자식 노드의 가중평균 엔트로피는 (4/8)×0+(4/8)×1.0=0.5다. 정보이득은 부모 엔트로피 0.811에서 이 값을 뺀 0.811-0.5=0.311이다."
  optionExplanations={['정답이다. 부모 엔트로피 0.811에서 가중평균 자식 엔트로피 0.5를 빼면 0.311이다.', '부모 엔트로피 값을 그대로 정보이득으로 착각했을 때 나올 수 있는 오답이다.', '가중평균 자식 엔트로피 값을 그대로 정보이득으로 착각했을 때 나올 수 있는 오답이다.', '오른쪽 자식 엔트로피 값을 그대로 정보이득으로 착각했을 때 나올 수 있는 오답이다.']}
/>

**문항 51-53 자료**

| 항목 | 값 |
|---|---|
| 전체 거래 수 | 800건 |
| 품목 A를 포함한 거래 수 | 200건 |
| 품목 B를 포함한 거래 수 | 160건 |
| 품목 A와 B를 함께 포함한 거래 수 | 80건 |

<Quiz
  questionNumber={51}
  question="위 표의 자료에서 연관규칙 A에서 B로 가는 규칙의 지지도는 얼마인가"
  options={['0.10', '0.40', '0.20', '0.50']}
  correctAnswer={1}
  explanation="지지도는 A와 B가 함께 나타난 거래 수를 전체 거래 수로 나눈 값이므로 80/800=0.10이다."
  optionExplanations={['정답이다. 80을 전체 거래 수 800으로 나누면 0.10이다.', 'A와 B가 함께 나타난 거래 수를 A를 포함한 거래 수로 나누어 신뢰도를 계산한 값을 지지도로 착각했을 때 나올 수 있는 오답이다.', 'B를 포함한 거래 수를 전체로 나눈 값(160/800=0.20)을 지지도로 착각했을 때 나올 수 있는 오답이다.', 'A를 포함한 거래 수를 전체로 나눈 값(200/800=0.25에 가까운 근사값)과 혼동했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={52}
  question="위 표의 자료에서 연관규칙 A에서 B로 가는 규칙의 신뢰도는 얼마인가"
  options={['0.40', '0.10', '0.50', '1.6']}
  correctAnswer={1}
  explanation="신뢰도는 A와 B가 함께 나타난 거래 수를 A를 포함한 거래 수로 나눈 값이므로 80/200=0.40이다."
  optionExplanations={['정답이다. 80을 A를 포함한 거래 수 200으로 나누면 0.40이다.', '지지도 값(80/800=0.10)을 신뢰도로 착각했을 때 나올 수 있는 오답이다.', 'B를 포함한 거래 수를 전체로 나눈 값을 신뢰도로 착각했을 때 나올 수 있는 오답이다.', '향상도 값과 혼동했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={53}
  question="위 표의 자료에서 연관규칙 A에서 B로 가는 규칙의 향상도는 얼마인가(공식은 신뢰도를 B의 지지도로 나눈 값이다)"
  options={['2.0', '0.40', '0.10', '0.83']}
  correctAnswer={1}
  explanation="B의 지지도는 160/800=0.20이다. 향상도는 신뢰도 0.40을 B의 지지도 0.20으로 나눈 값이므로 2.0이다."
  optionExplanations={['정답이다. 신뢰도 0.40을 B의 지지도 0.20으로 나누면 2.0이다.', '신뢰도 값을 그대로 향상도로 착각했을 때 나올 수 있는 오답이다.', '지지도 값을 그대로 향상도로 착각했을 때 나올 수 있는 오답이다.', '분자와 분모를 뒤바꿔 계산했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={54}
  question="의사결정나무의 알고리즘과 분리 기준에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['CART는 이진 분할만 허용하며 분류에서는 지니지수, 회귀에서는 분산 감소량을 분리 기준으로 쓴다', 'C4.5는 정보이득비를 분리 기준으로 사용해 범주 수가 많은 변수가 과도하게 선택되는 문제를 완화한다', 'CHAID는 카이제곱 통계량을 이용해 다지 분할을 허용한다', '어떤 분리 기준을 쓰더라도 같은 자료에서는 항상 완전히 동일한 나무 구조가 만들어진다']}
  correctAnswer={4}
  explanation="지니지수, 정보이득비, 카이제곱 통계량은 불순도를 재는 방식이 서로 달라 같은 자료에서도 서로 다른 분할을 고를 수 있다. 항상 동일한 나무가 만들어진다는 서술은 틀렸다."
  optionExplanations={['CART의 특징에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', 'C4.5의 특징에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', 'CHAID의 특징에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 분리 기준이 다르면 불순도를 재는 방식이 달라 서로 다른 나무 구조가 만들어질 수 있으므로 항상 동일하다는 서술은 틀렸다.']}
/>

**문항 55, 56 자료**

| 점 | 좌표 |
|---|---|
| P1 | (2, 3) |
| P2 | (6, 7) |

<Quiz
  questionNumber={55}
  question="위 표의 두 점 사이의 유클리드 거리는 얼마인가(소수 둘째 자리에서 반올림)"
  options={['5.66', '4.00', '8.00', '32.00']}
  correctAnswer={1}
  explanation="두 좌표 차이는 각각 4다. 유클리드 거리는 4의 제곱과 4의 제곱을 더한 값의 제곱근이므로 32의 제곱근인 약 5.66이다."
  optionExplanations={['정답이다. (4의 제곱+4의 제곱)의 제곱근인 32의 제곱근은 약 5.66이다.', '한 축의 차이값만 계산했을 때 나올 수 있는 값이다.', '맨해튼 거리(4+4=8)와 혼동했을 때 나올 수 있는 값이다.', '제곱근을 취하지 않고 32를 그대로 답했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={56}
  question="위 표의 두 점 사이의 맨해튼 거리는 얼마인가"
  options={['8.00', '5.66', '4.00', '16.00']}
  correctAnswer={1}
  explanation="맨해튼 거리는 각 축의 차이의 절댓값을 더한 값이므로 4+4=8이다."
  optionExplanations={['정답이다. 두 축의 차이 절댓값 4와 4를 더하면 8이다.', '유클리드 거리(약 5.66)와 혼동했을 때 나올 수 있는 값이다.', '한 축의 차이값만 계산했을 때 나올 수 있는 값이다.', '차이값을 더하지 않고 제곱해 더했을 때 나올 수 있는 값이다.']}
/>

**문항 57 자료**

| 주성분 | PC1 | PC2 | PC3 | PC4 |
|---|---|---|---|---|
| 고유값 | 8.0 | 4.0 | 2.0 | 2.0 |

<Quiz
  questionNumber={57}
  question="위 표에서 PC1 하나가 설명하는 분산 비율은 얼마인가"
  options={['50%', '25%', '75%', '12.5%']}
  correctAnswer={1}
  explanation="고유값의 총합은 8.0+4.0+2.0+2.0=16이다. PC1의 분산 설명 비율은 8.0/16=0.5, 즉 50퍼센트다."
  optionExplanations={['정답이다. PC1 고유값 8.0을 전체 고유값 합 16으로 나누면 50퍼센트다.', 'PC2 고유값 4.0을 총합으로 나누었을 때 나올 수 있는 값이다.', 'PC1과 PC2 고유값의 합을 총합으로 나누었을 때 나올 수 있는 값이다.', 'PC3 또는 PC4 고유값을 총합으로 나누었을 때 나올 수 있는 값이다.']}
/>

**문항 58 자료**

| 항목 | 값 |
|---|---|
| 관측치의 같은 군집 내 평균 거리(a) | 2 |
| 관측치와 가장 가까운 다른 군집까지의 평균 거리(b) | 5 |

<Quiz
  questionNumber={58}
  question="위 표의 값으로 계산한 이 관측치의 실루엣 계수는 얼마인가(공식은 (b-a)를 a와 b 중 큰 값으로 나눈 것이다)"
  options={['0.6', '0.4', '2.5', '0.28']}
  correctAnswer={1}
  explanation="b-a는 5-2=3이고, a와 b 중 큰 값은 5다. 실루엣 계수는 3/5=0.6이다."
  optionExplanations={['정답이다. (5-2)를 5로 나누면 0.6이다.', 'a를 b로 나눈 값(2/5=0.4)과 혼동했을 때 나올 수 있는 오답이다.', 'b를 a로 나눈 값(5/2=2.5)과 혼동했을 때 나올 수 있는 오답이다.', 'a를 b-a로 나눈 값(2/3에 가까운 근사값)과 혼동했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={59}
  question="시계열 자료의 정상성에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['정상 시계열은 평균이 시점에 따라 달라지지 않는다', '정상 시계열은 자기공분산이 시차에만 의존하고 특정 시점에는 의존하지 않는다', '추세나 계절성이 뚜렷한 원계열은 차분을 통해 정상화할 수 있다', '비정상 시계열은 어떤 방법으로도 정상화할 수 없어 ARIMA 계열 모형을 적용할 수 없다']}
  correctAnswer={4}
  explanation="ARIMA의 통합(I) 부분은 차분을 통해 비정상 계열을 정상화하는 절차이므로, 비정상 시계열이라는 이유만으로 ARIMA 계열을 적용할 수 없다는 서술은 틀렸다."
  optionExplanations={['정상 시계열의 평균에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정상 시계열의 자기공분산에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '차분을 통한 정상화에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. ARIMA는 차분으로 비정상 계열을 정상화한 뒤 적용하는 모형이므로 적용이 불가능하다는 서술은 틀렸다.']}
/>

<Quiz
  questionNumber={60}
  question="배깅과 부스팅의 차이를 가장 올바르게 설명한 것은 무엇인가"
  options={['배깅은 독립 부트스트랩 모형을 병렬로 결합해 주로 분산을 줄이고, 부스팅은 순차적으로 오류를 보완해 주로 편향을 줄인다', '배깅은 이전 모형의 오차를 순서대로 보정하고, 부스팅은 독립 모형을 병렬로 결합한다', '두 방법 모두 언제나 하나의 학습기만 사용한다', '배깅과 부스팅은 모두 학습 데이터의 재사용이 금지된다']}
  correctAnswer={1}
  explanation="배깅은 부트스트랩 표본으로 만든 여러 모형을 병렬로 결합해 주로 분산을 줄이고, 부스팅은 앞선 오류를 순차적으로 보완해 주로 편향을 줄이는 전략이다."
  optionExplanations={['정답이다. 배깅의 병렬·분산 감소 특성과 부스팅의 순차·편향 감소 특성을 올바르게 짝지었다.', '배깅과 부스팅의 특징을 서로 뒤바꿔 서술한 오답이다.', '배깅과 부스팅은 모두 여러 개의 학습기를 사용하는 앙상블 기법이므로 하나만 쓴다는 서술은 틀렸다.', '배깅은 복원추출로 학습 데이터를 재사용하므로 재사용이 금지된다는 서술은 틀렸다.']}
/>

## 4과목: 빅데이터 결과 해석 (61~80)

**문항 61-65 자료**

| 구분 | 예측 양성 | 예측 음성 |
|---|---|---|
| 실제 양성 | 60 | 15 |
| 실제 음성 | 20 | 105 |

<Quiz
  questionNumber={61}
  question="위 혼동행렬에서 정확도(Accuracy)는 얼마인가"
  options={['0.825', '0.75', '0.6', '0.9']}
  correctAnswer={1}
  explanation="정확도는 (TP+TN)을 전체로 나눈 값이다. 전체는 60+15+20+105=200이며 (60+105)/200=165/200=0.825다."
  optionExplanations={['정답이다. 165를 200으로 나누면 0.825다.', 'TP를 (TP+FP)로 나눈 정밀도 값과 혼동했을 때 나올 수 있는 오답이다.', 'TP를 전체 인원 100(가상의 절반)으로 나누는 등 분모를 잘못 잡았을 때 나올 수 있는 오답이다.', '특이도 값과 혼동했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={62}
  question="위와 같은 혼동행렬에서 정밀도(Precision)는 얼마인가"
  options={['0.75', '0.825', '0.8', '0.6']}
  correctAnswer={1}
  explanation="정밀도는 TP/(TP+FP)로 계산한다. 60/(60+20)=60/80=0.75다."
  optionExplanations={['정답이다. 60을 예측 양성 전체 80(60+20)으로 나누면 0.75다.', '정확도 값과 혼동했을 때 나올 수 있는 오답이다.', '재현율 값과 혼동했을 때 나올 수 있는 오답이다.', 'TP를 전체 표본 수와 혼동해 나누었을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={63}
  question="위와 같은 혼동행렬에서 재현율(민감도)은 얼마인가"
  options={['0.8', '0.75', '0.825', '0.6']}
  correctAnswer={1}
  explanation="재현율은 TP/(TP+FN)으로 계산한다. 60/(60+15)=60/75=0.8이다."
  optionExplanations={['정답이다. 60을 실제 양성 전체 75(60+15)로 나누면 0.8이다.', '정밀도 값과 혼동했을 때 나올 수 있는 오답이다.', '정확도 값과 혼동했을 때 나올 수 있는 오답이다.', '특이도 값과 혼동했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={64}
  question="위와 같은 혼동행렬에서 특이도(Specificity)는 얼마인가"
  options={['0.84', '0.8', '0.75', '0.6']}
  correctAnswer={1}
  explanation="특이도는 TN/(TN+FP)으로 계산한다. 105/(105+20)=105/125=0.84다."
  optionExplanations={['정답이다. 105를 실제 음성 전체 125(105+20)로 나누면 0.84다.', '재현율 값과 혼동했을 때 나올 수 있는 오답이다.', '정밀도 값과 혼동했을 때 나올 수 있는 오답이다.', 'TN을 전체 표본 수와 혼동해 나누었을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={65}
  question="위와 같은 혼동행렬에서 계산한 정밀도 0.75와 재현율 0.8을 이용할 때 F1 점수는 얼마인가(소수 넷째 자리에서 반올림)"
  options={['0.7742', '0.825', '0.6', '0.8']}
  correctAnswer={1}
  explanation="F1 점수는 정밀도와 재현율의 조화평균이므로 2×0.75×0.8/(0.75+0.8)=1.2/1.55≈0.7742다."
  optionExplanations={['정답이다. 조화평균 공식 2×0.75×0.8/(0.75+0.8)을 계산하면 약 0.7742다.', '정밀도와 재현율의 산술평균(0.75+0.8)/2=0.775에 가까운 값과 혼동했을 때 나올 수 있는 오답이다.', '정밀도와 재현율을 곱하기만 했을 때(0.75×0.8=0.6) 나올 수 있는 오답이다.', '재현율 값을 그대로 F1으로 착각했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={66}
  question="관측치 420개를 k=7인 k겹 교차검증으로 평가하려 한다. 진행 방식에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['자료를 크기가 같은 7개 묶음으로 나누고 각 관측치는 한 묶음에만 속한다', '각 회차에서 검증에 60개, 학습에 360개의 관측치를 사용한다', '7회차에서 얻은 성능값을 평균해 모형의 대표 성능으로 보고한다', '검증에 사용된 관측치는 다음 회차의 학습에도 항상 포함되어 총 학습 횟수가 실제보다 늘어난다']}
  correctAnswer={4}
  explanation="k겹 교차검증에서는 각 회차마다 검증 묶음이 바뀔 뿐, 어떤 관측치가 검증에 쓰였는지가 다음 회차의 학습 방식을 특별히 바꾸지 않는다. 이 서술은 존재하지 않는 규칙을 지어낸 것이므로 옳지 않다."
  optionExplanations={['묶음 구성 방식에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '420을 7로 나누면 60이므로 검증 60개, 학습 360개라는 계산은 옳다.', '결과를 평균해 대표 성능으로 삼는다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. k겹 교차검증에는 이런 특별한 규칙이 없으며, 각 회차는 정해진 학습·검증 묶음 구성을 따를 뿐이다.']}
/>

**문항 67 자료**

| 항목 | 값 |
|---|---|
| 대립가설 아래 검정통계량 T의 분포 | 평균 3, 표준편차 1인 정규분포 |
| 귀무가설의 채택역 | T가 1.645보다 작은 영역 |

<Quiz
  questionNumber={67}
  question="위 표의 조건에서 제2종 오류의 확률 베타는 어떻게 표현되는가"
  options={['Z가 -1.355보다 작을 확률', 'Z가 1.645보다 클 확률', 'Z가 1.645보다 작을 확률', 'Z가 3보다 클 확률']}
  correctAnswer={1}
  explanation="제2종 오류는 대립가설이 참인데도 귀무가설을 채택하는 오류이므로 베타는 대립가설 분포에서 T가 채택역(1.645보다 작은 영역)에 속할 확률이다. 표준화하면 (1.645-3)/1=-1.355이므로 베타는 Z가 -1.355보다 작을 확률이다."
  optionExplanations={['정답이다. 대립가설 분포를 표준화하면 채택역의 경계가 -1.355가 되므로 베타는 Z가 -1.355보다 작을 확률이다.', '귀무가설 분포에서 채택역 밖의 확률을 구하는 방식이며 대립가설 하의 제2종 오류 계산과는 다르다.', '귀무가설 분포에서 채택역 안의 확률을 구하는 방식이며 대립가설 하의 제2종 오류 계산과는 다르다.', '대립가설 분포의 평균 자체를 표준화 대상으로 착각했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={68}
  question="같은 평가 표본에서 동점이 없는 양성-음성 표본쌍 100개씩을 비교했더니, 모델 A는 58쌍에서, 모델 B는 77쌍에서 양성 점수가 더 높았다. AUC의 경험적 해석으로 옳은 것은 무엇인가"
  options={['모델 B의 약 0.77이 모델 A의 약 0.58보다 순위 판별력이 강하다', '모델 A의 약 0.58이 모델 B의 약 0.77보다 순위 판별력이 강하다', '두 모델의 AUC는 이 비율만으로는 전혀 추정할 수 없다', 'AUC가 0.5보다 크면 반드시 정확도도 0.5보다 크다는 것이 보장된다']}
  correctAnswer={1}
  explanation="양성 점수가 더 높은 표본쌍의 비율은 경험적으로 AUC를 나타낸다. 모델 A는 약 0.58, 모델 B는 약 0.77이므로 값이 더 큰 모델 B의 순위 판별력이 더 우수하다."
  optionExplanations={['정답이다. 0.77이 0.58보다 크므로 모델 B의 순위 판별력이 더 우수하다는 해석이 옳다.', 'AUC 값의 대소를 서로 반대로 해석한 오답이다.', '양성-음성 쌍의 승률은 AUC의 경험적 추정치로 널리 쓰이는 방법이므로 추정할 수 없다는 서술은 틀렸다.', 'AUC는 순위 판별력을 나타내는 지표이며 특정 임계값에서의 정확도를 자동으로 보장하지 않는다.']}
/>

<Quiz
  questionNumber={69}
  question="유전자 발현 자료 1만 개 변수 가운데 질병 여부와 상관이 높은 150개만 남겨 분류모형을 만들고 일반화 성능을 편향 없이 보고하려 한다. 옳은 수행 순서는 무엇인가"
  options={['자료를 학습용과 평가용으로 먼저 나눈다 → 학습용만 사용해 150개 변수를 선별한다 → 학습용으로 모형을 학습한다 → 같은 선별 규칙을 평가용에 적용해 성능을 산출한다', '전체 자료로 150개 변수를 먼저 선별한다 → 자료를 학습용과 평가용으로 나눈다 → 학습용으로 모형을 학습한다 → 평가용에서 성능을 산출한다', '전체 자료로 150개 변수를 선별한다 → 같은 전체 자료로 모형을 학습한다 → 학습에 쓴 자료에서 성능을 산출한다', '자료를 학습용과 평가용으로 먼저 나눈다 → 평가용에서 150개 변수를 선별한다 → 학습용으로 모형을 학습한다 → 평가용에서 성능을 산출한다']}
  correctAnswer={1}
  explanation="변수 선별도 자료를 보고 내리는 결정이므로 학습 절차의 일부다. 평가용 자료를 먼저 떼어 낸 뒤 학습용만으로 선별 기준을 정하고, 확정된 규칙을 평가용에 그대로 적용해야 평가용 정보가 선별 단계에 새어 들지 않는다."
  optionExplanations={['정답이다. 평가용을 먼저 떼어 두고 학습용만으로 변수를 선별해야 평가용 정보가 새어 들지 않는다.', '전체 자료로 변수를 먼저 선별하면 평가용 정보가 선별 단계에 이미 새어 들어가 성능이 과대평가된다.', '평가용 자료를 따로 두지 않고 학습에 쓴 자료로 성능을 매기면 일반화 성능을 알 수 없다.', '평가용 자료로 변수를 선별하면 평가의 독립성이 무너져 성능이 과대평가된다.']}
/>

<Quiz
  questionNumber={70}
  question="초매개변수와 매개변수를 구별하는 기준에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['매개변수는 학습 과정에서 자료로부터 추정되는 값이다', '초매개변수는 학습을 시작하기 전에 사람이 값을 지정한다', '의사결정나무의 최대 깊이는 초매개변수에 해당한다', '신경망의 학습률은 손실 최적화 과정에서 자료로부터 자동으로 추정되는 매개변수다']}
  correctAnswer={4}
  explanation="학습률은 학습을 시작하기 전에 사람이 값을 지정하는 초매개변수다. 손실 최적화 과정에서 자료로부터 자동으로 추정되는 것은 가중치 같은 매개변수다."
  optionExplanations={['매개변수의 정의는 옳으므로 이 문제에서 찾는 답이 아니다.', '초매개변수의 정의는 옳으므로 이 문제에서 찾는 답이 아니다.', '최대 깊이가 초매개변수라는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 학습률은 사람이 미리 지정하는 초매개변수이며 자료로부터 자동으로 추정되는 매개변수가 아니다.']}
/>

<Quiz
  questionNumber={71}
  question="경사하강법 계열의 최적화 알고리즘에 대한 설명으로 옳지 않은 것은 무엇인가"
  options={['모멘텀은 이전 기울기의 방향을 누적해 진동을 줄이고 수렴을 돕는다', '아담(Adam)은 기울기의 1차, 2차 모멘트를 함께 활용해 학습률을 자료에 맞게 조정한다', '알엠에스프롭(RMSProp)은 최근 기울기의 제곱 평균으로 학습률을 조정한다', '배치 정규화는 경사하강법 계열의 최적화 알고리즘으로 가중치를 직접 갱신한다']}
  correctAnswer={4}
  explanation="배치 정규화는 각 층의 입력 분포를 정규화해 학습을 안정시키는 기법이며, 기울기를 이용해 가중치를 직접 갱신하는 최적화 알고리즘이 아니다."
  optionExplanations={['모멘텀에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '아담에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '알엠에스프롭에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 배치 정규화는 입력 분포를 정규화하는 기법이며 가중치를 직접 갱신하는 최적화 알고리즘이 아니다.']}
/>

<Quiz
  questionNumber={72}
  question="이탈 예측 모형을 도입한 뒤 비즈니스 기여도를 평가하려 한다. 다음 설명 중 옳지 않은 것은 무엇인가"
  options={['모형 도입 전후의 이탈률 변화를 함께 제시하면 사업 성과를 가늠하는 데 도움이 된다', '기여도는 비용 절감, 매출 증대처럼 정량화된 항목으로 표현할 수 있다', '투자 대비 효과는 도입 비용과 그로 인한 이익 증가분을 견주어 산출한다', '모형의 예측 정확도가 목표치를 넘기만 하면 비즈니스 기여도는 자동으로 검증된다']}
  correctAnswer={4}
  explanation="예측 정확도는 모형 성능 지표일 뿐이며, 그 성능이 실제 매출이나 비용 절감 같은 사업 성과로 이어졌는지는 별도로 확인해야 한다. 정확도만으로 비즈니스 기여도가 자동으로 검증된다는 서술은 옳지 않다."
  optionExplanations={['이탈률 변화 제시에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '기여도를 정량화된 항목으로 표현한다는 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '투자 대비 효과의 산출 방식에 대한 설명은 옳으므로 이 문제에서 찾는 답이 아니다.', '정답이다. 모형 성능과 비즈니스 성과는 별개의 축이므로 정확도만으로 기여도가 자동 검증된다고 볼 수 없다.']}
/>

<Quiz
  questionNumber={73}
  question="선거구별 의석수 비율에 맞춰 지도 영역의 크기와 모양을 실제 면적과 다르게 변형해 표현하는 지도 시각화 기법은 무엇인가"
  options={['카토그램', '단계구분도', '도트 맵', '버블 맵']}
  correctAnswer={1}
  explanation="실제 면적 대신 특정 수치(의석수, 인구수 등)의 비율에 맞춰 지도 영역의 크기와 모양을 변형하는 기법은 카토그램이다."
  optionExplanations={['정답이다. 실제 면적을 특정 수치 비율에 맞춰 변형하는 기법은 카토그램의 정의와 정확히 맞는다.', '단계구분도는 실제 지도 경계를 유지한 채 색상 농도로 값을 표현하는 기법이며 면적을 변형하지 않는다.', '도트 맵은 실제 지도 위에 점을 찍어 밀도를 표현하는 기법이며 면적을 변형하지 않는다.', '버블 맵은 실제 지도 위에 원의 크기로 값을 표현하는 기법이며 지역 면적 자체를 변형하지 않는다.']}
/>

<Quiz
  questionNumber={74}
  question="한 기업이 창립 이후의 성장 과정을 도입, 전개, 결론이 자연스럽게 이어지는 하나의 이야기로 구성해 보여 주려 한다. 이에 가장 적합한 인포그래픽 유형은 무엇인가"
  options={['스토리텔링형', '도표형', '비교형', '지도형']}
  correctAnswer={1}
  explanation="하나의 주제를 도입, 전개, 결론으로 이어지는 이야기 흐름으로 구성해 완결성을 강조하는 유형은 스토리텔링형이다."
  optionExplanations={['정답이다. 이야기 흐름으로 완결성을 강조하는 것은 스토리텔링형의 정의와 정확히 맞는다.', '도표형은 여러 항목의 수치를 표 형태로 정리하는 유형이며 이야기 흐름과는 다르다.', '비교형은 두 대상의 특징을 좌우로 대비하는 유형이며 이 사례와는 목적이 다르다.', '지도형은 지역별 분포를 지도 위에 표현하는 유형이며 이 사례와는 목적이 다르다.']}
/>

<Quiz
  questionNumber={75}
  question="분석 활용계획에 포함되어야 할 요소로 보기 어려운 것은 무엇인가"
  options={['분석 결과를 업무 프로세스에 적용하는 방안', '모형 성능을 지속적으로 모니터링하는 방안', '사용자 피드백을 반영해 개선하는 체계', '분석에 사용한 원본 자료를 즉시 영구 삭제하는 절차']}
  correctAnswer={4}
  explanation="원본 자료의 즉시 영구 삭제는 재학습이나 사후 검증에 필요한 근거 자료를 없애 버리는 절차이며, 활용계획이 갖추어야 할 요소로 보기 어렵다."
  optionExplanations={['업무 프로세스 적용 방안은 활용계획의 필수 요소이므로 이 문제에서 찾는 답이 아니다.', '성능 모니터링 방안은 활용계획의 필수 요소이므로 이 문제에서 찾는 답이 아니다.', '피드백 반영 체계는 활용계획의 필수 요소이므로 이 문제에서 찾는 답이 아니다.', '정답이다. 원본 자료를 즉시 영구 삭제하면 재학습과 사후 검증의 근거가 사라지므로 활용계획의 요소로 보기 어렵다.']}
/>

<Quiz
  questionNumber={76}
  question="다음 중 신경망 학습에서 훈련 손실은 계속 감소하지만 검증 손실은 어느 시점부터 다시 증가하는 패턴에 대한 해석으로 가장 적절한 것은 무엇인가"
  options={['과대적합이 진행되고 있다', '과소적합이 점점 심해지고 있다', '모형이 전역 최솟값에 완전히 도달했다', '학습률이 지나치게 작아서 생기는 현상이다']}
  correctAnswer={1}
  explanation="훈련 자료에 대한 적합은 계속 좋아지는데 보지 않은 검증 자료 성능이 나빠지는 패턴은 모형이 훈련 자료의 잡음까지 암기하는 과대적합의 전형적인 신호다."
  optionExplanations={['정답이다. 훈련 손실과 검증 손실이 벌어지는 패턴은 과대적합의 대표적인 신호다.', '과소적합은 훈련 손실조차 충분히 낮아지지 않는 상태이며 이 사례와는 반대다.', '검증 손실이 다시 증가하는 것은 전역 최솟값에 안정적으로 도달했다는 신호가 아니다.', '학습률이 지나치게 작으면 수렴이 느려질 뿐이며 검증 손실이 다시 증가하는 이 패턴을 직접 설명하지 못한다.']}
/>

<Quiz
  questionNumber={77}
  question="여러 대상과 항목의 값을 한 화면에서 비교하는 시각화 기법 중, 항목마다 세로축을 나란히 두고 한 대상의 값을 선으로 이어 형태를 비교하는 기법은 무엇인가"
  options={['평행 좌표 그래프', '히트맵', '모자이크 플롯', '누적 영역 그래프']}
  correctAnswer={1}
  explanation="항목마다 세로축을 나란히 세우고 한 대상의 값을 선 하나로 이어 형태를 비교하는 기법은 평행 좌표 그래프다."
  optionExplanations={['정답이다. 항목별 세로축을 나란히 두고 선으로 잇는 방식은 평행 좌표 그래프의 정의와 정확히 맞는다.', '히트맵은 행과 열이 만나는 칸의 색 농도로 값을 나타내는 기법이며 선으로 형태를 잇지 않는다.', '모자이크 플롯은 범주형 변수 간 관계를 사각형 면적으로 나타내는 기법이며 이 사례와는 다르다.', '누적 영역 그래프는 시간에 따른 구성 항목의 변화를 쌓아 보여 주는 기법이며 대상 간 형태 비교가 목적은 아니다.']}
/>

**문항 78 자료**

| 항목 | 값 |
|---|---|
| 원하는 오차한계(E) | 0.03 |
| 신뢰수준 95퍼센트의 z값 | 1.96 |
| 사전 추정 비율(p) | 0.3 |

<Quiz
  questionNumber={78}
  question="위 표의 조건으로 모비율 신뢰구간을 추정할 때 필요한 최소 표본크기는 얼마인가(공식은 n=z제곱×p×(1-p)/E제곱이며 올림하여 정수로 답한다)"
  options={['897', '600', '450', '1,200']}
  correctAnswer={1}
  explanation="n=1.96의 제곱×0.3×0.7/0.03의 제곱=3.8416×0.21/0.0009=0.806736/0.0009≈896.4이며, 표본은 부족하지 않도록 올림해 897이다."
  optionExplanations={['정답이다. 공식에 값을 대입하면 약 896.4가 나오고 올림해 897이다.', 'p를 0.5로 잘못 대입해 계산했을 때 나올 수 있는 값과 비슷한 오답이다.', 'p(1-p) 항을 절반으로 줄여 계산했을 때 나올 수 있는 값과 비슷한 오답이다.', 'z값을 제곱하지 않고 계산했을 때 나올 수 있는 값과는 다르지만 큰 자릿수 오류로 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={79}
  question="표본이 150개뿐인데 자유 파라미터가 매우 많은 모델을 규제 없이 학습했다. 예상되는 과대적합 패턴으로 가장 적절한 것은 무엇인가"
  options={['훈련 오차는 매우 낮지만 테스트 오차는 상대적으로 높다', '훈련 오차와 테스트 오차가 모두 높고 거의 같다', '모델을 단순하게 할수록 과대적합이 반드시 증가한다', '표본을 더 모을수록 과대적합이 항상 심해진다']}
  correctAnswer={1}
  explanation="표본 수에 비해 모델 용량이 지나치게 크면 훈련 자료를 암기해 훈련 오차는 매우 낮아지지만 보지 않은 자료에서는 오차가 상대적으로 높아지는 경향이 있다."
  optionExplanations={['정답이다. 훈련 오차는 매우 낮고 테스트 오차는 상대적으로 높은 격차가 과대적합의 전형적인 패턴이다.', '훈련 오차와 테스트 오차가 모두 높고 비슷한 패턴은 과소적합의 특징이며 과대적합과는 반대다.', '모델을 단순하게 할수록 과대적합은 대체로 줄어드는 경향이 있으므로 반드시 증가한다는 서술은 틀렸다.', '표본을 더 모으면 대체로 과대적합이 완화되는 경향이 있으므로 항상 심해진다는 서술은 틀렸다.']}
/>

<Quiz
  questionNumber={80}
  question="다음 중 두 변수 사이의 관계를 표현하는 것이 주된 목적이 아닌 시각화는 무엇인가"
  options={['원 그래프(파이 차트)', '산점도', '버블 차트', '상관행렬 히트맵']}
  correctAnswer={1}
  explanation="원 그래프는 전체에 대한 각 범주의 구성비를 나타내는 도구이며, 두 변수 간 관계를 표현하는 것이 주목적은 아니다."
  optionExplanations={['정답이다. 원 그래프는 구성비를 보는 도구이지 두 변수 간 관계를 보는 도구가 아니다.', '산점도는 두 변수의 관계를 표현하는 대표 도구이므로 이 문제에서 찾는 답이 아니다.', '버블 차트는 두 변수의 관계에 크기까지 더해 표현하는 도구이므로 이 문제에서 찾는 답이 아니다.', '상관행렬 히트맵은 여러 변수 쌍의 관계를 한눈에 보여 주는 도구이므로 이 문제에서 찾는 답이 아니다.']}
/>

## 채점 후 복습 동선표

채점을 마쳤으면 틀린 문항의 과목·번호로 아래 표를 찾아 해당 편을 다시 읽습니다. 28편(1회)과 함께 틀린 영역이 겹친다면 그 개념이 실제로 취약하다는 뜻이므로 우선순위를 두고 복습하십시오.

| 틀린 문항 번호 | 다루는 영역 | 복습할 편 |
|---|---|---|
| 1–2 | 빅데이터 특성(가치)과 위기 요인 | 08편 |
| 3–5 | 분석 준비도·성숙도, 조직 구조, 데이터 과학자 역량 | 08편 |
| 6, 19–20 | 분석 방법론(CRISP-DM), 마스터플랜, WBS | 07편, 10편 |
| 7–8 | 하향식·상향식 과제 발굴, 과제 유형 | 07편 |
| 9–12 | 개인정보·비식별 기법, 데이터 품질 계산 | 08편, 09편 |
| 13–18 | 정형·반정형 데이터, 분산 저장·NoSQL, ETL·DW·마트 | 03편, 09편 |
| 21–26 | 전처리(정제·통합·축소·변환), 결측·이상치, 파생변수 | 11편, 12편 |
| 27 | 표준화(z-점수) 계산 | 12편, 14편 |
| 28, 78 | 표본크기 계산 | 14편, 15편 |
| 29–30 | 가설검정 통계량, 비율 차이 계산 | 15편 |
| 31–33 | 이항분포, 표본평균의 성질, 검정 목적 구분 | 14편, 15편 |
| 34–37, 40 | 왜도, 편상관, 분포 치우침 해석, 표본추출법, 초기하분포 | 04편, 13편, 14편 |
| 38–39 | t검정·카이제곱검정의 자유도 계산 | 15편 |
| 41 | 지도학습·비지도학습 구분 | 05편 |
| 42–45 | 회귀 가정, 다중회귀 해석, 결정계수, VIF | 16편 |
| 46–47 | 오즈비, 로지스틱 회귀 해석 계산 | 18편 |
| 48–50 | 지니지수·엔트로피·정보이득 계산 | 18편 |
| 51–53 | 지지도·신뢰도·향상도 계산 | 19편 |
| 54, 60 | 의사결정나무 분리 기준, 배깅·부스팅 | 18편 |
| 55–56 | 유클리드·맨해튼 거리 계산 | 19편 |
| 57 | 주성분분석(PCA) 분산 비율 계산 | 19편 |
| 58 | 실루엣 계수 계산 | 19편 |
| 59 | 시계열 정상성·ARIMA | 21편 |
| 61–65 | 정확도·정밀도·재현율·특이도·F1 계산 | 20편 |
| 66 | 교차검증 진행 방식 | 21편 |
| 67 | 제2종 오류(베타) 계산 | 15편, 21편 |
| 68 | AUC의 경험적 해석 | 20편 |
| 69 | 변수 선별과 데이터 누수 방지 | 21편 |
| 70–72 | 초매개변수·매개변수, 최적화 알고리즘, 비즈니스 기여도 | 21편, 22편, 24편 |
| 73–74, 77, 80 | 지도 시각화, 인포그래픽, 비교 시각화 기법 | 23편 |
| 75 | 분석 활용계획 | 24편 |
| 76, 79 | 과대적합·과소적합 패턴 진단 | 21편 |

## 참고 자료

- [한국데이터산업진흥원 - 빅데이터분석기사 자격시험 안내](https://www.dataq.or.kr/www/sub/a_07.do)
- [이패스코리아 - 빅데이터분석기사 시험정보](https://www.epasskorea.com/public_html/Examinfo/info_bd.asp?cate_idx=13280110&type=C&link_idx=11112)
