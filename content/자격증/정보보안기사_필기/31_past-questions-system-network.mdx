---
title: '31. 기출·예상문제: 시스템보안·네트워크보안'
description: '1·2과목(시스템보안·네트워크보안) 핵심 개념과 계산(서브넷팅·증폭계수) 문제를 출제기준 기반으로 재구성한 22문항으로 점검하고, 보기별 함정과 오답 이유를 하나씩 해부한다'
---

<Callout type="warning">
  **이 편의 문제는 실제 기출을 그대로 복제한 것이 아닙니다.** 06편부터 15편까지 다룬 1과목(시스템보안)·2과목(네트워크보안) 세부항목을 기준으로, 같은 개념·같은 난이도로 **재구성**한 문항입니다. 숫자·상황·보기 표현을 바꾸었으므로 문장을 통째로 외우는 방식으로는 이득이 없고, 개념과 계산 절차를 이해해야만 풀립니다.

  또한 정답과 오답의 경계가 흐린 문항은 만들지 않고, 각 문항이 무엇을 판별하려는지 조건을 명확히 다듬어 출제했습니다.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="PAM(Pluggable Authentication Modules)에 대한 설명으로 옳지 않은 것은?"
  options={['① 인증 로직을 애플리케이션 코드에서 분리해 모듈 단위로 교체·조합할 수 있게 한다', '② 계정 잠금 정책이나 비밀번호 복잡도 규칙을 애플리케이션을 재컴파일하지 않고 설정 파일 수준에서 바꿀 수 있다', '③ 다중요소 인증(MFA) 모듈을 추가해 지식 기반 인증에 소유·생체 기반 인증을 결합할 수 있다', '④ PAM은 리눅스 커널 내부에서만 동작하며 사용자 공간의 응용 프로그램과는 독립적으로 작동한다']}
  correctAnswer={4}
  explanation="PAM은 사용자 공간에서 동작하는 인증 프레임워크로, login·sshd 같은 응용 프로그램이 PAM 라이브러리를 호출해 인증을 위임하는 구조다. 커널 내부에서만 동작한다는 4번이 옳지 않다."
  optionExplanations={['인증 로직을 모듈화해 분리하는 것이 PAM의 핵심 설계 목적이므로 옳은 설명이다.', '설정 파일(예: /etc/pam.d) 수준에서 정책을 바꿀 수 있다는 것은 PAM의 실제 장점이므로 옳다.', 'PAM 모듈 체계에 MFA용 모듈을 추가해 인증 요소를 조합할 수 있으므로 옳은 설명이다.', 'PAM은 사용자 공간의 응용 프로그램이 호출하는 라이브러리이지 커널 내부 기능이 아니므로 이 설명이 옳지 않다.']}
/>

<Quiz
  questionNumber={2}
  question="관리자가 sudo -l 명령으로 확인한 결과, 사용자 계정이 비밀번호 없이 find 명령을 root 권한으로 실행할 수 있도록 허용되어 있었다. 이 설정이 위험한 이유로 가장 적절한 것은?"
  options={['① find 명령은 파일 검색만 수행하므로 근본적으로 위험할 수 없다', '② find 명령의 실행 옵션을 이용해 임의의 다른 명령을 root 권한으로 실행시키는 우회로가 될 수 있다', '③ find 명령은 네트워크 트래픽을 도청하는 기능을 내장하고 있다', '④ find 명령은 항상 비밀번호를 두 번 요구하므로 오히려 더 안전하다']}
  correctAnswer={2}
  explanation="find는 검색 대상 파일에 대해 임의 명령을 실행시키는 옵션을 갖고 있어, 이 명령을 root 권한으로 제한 없이 허용하면 그 옵션을 통해 root 셸을 얻는 권한 상승 통로가 될 수 있다."
  optionExplanations={['검색 기능 외에 실행 옵션이 있다는 점을 간과한 서술이므로 옳지 않다.', 'find의 실행 옵션을 이용한 권한 상승이 실제 위험이므로 옳은 설명이다.', 'find 명령 자체에는 도청 기능이 없으므로 옳지 않다.', 'NOPASSWD로 설정되면 오히려 비밀번호 확인 절차 자체가 생략되므로 옳지 않다.']}
/>

<Quiz
  questionNumber={3}
  question="리눅스 파일의 권한을 chmod 4755로 설정했다. 이 설정이 부여하는 특수 권한과 그 의미로 옳은 것은?"
  options={['① SetGID가 설정되어, 실행 시 파일 소유 그룹의 권한으로 동작한다', '② Sticky bit가 설정되어, 디렉터리 안에서 소유자만 자신의 파일을 삭제할 수 있다', '③ SetUID가 설정되어, 다른 사용자가 이 파일을 실행할 때도 파일 소유자의 권한으로 동작한다', '④ 특수 권한이 전혀 설정되지 않은 일반 실행 파일 권한이다']}
  correctAnswer={3}
  explanation="권한 앞자리 4는 SetUID를 의미한다. SetUID가 설정된 실행 파일은 그 파일을 누가 실행하든 파일 소유자의 권한으로 프로세스가 동작하며, 나머지 755는 소유자 읽기·쓰기·실행, 그룹과 기타 사용자는 읽기·실행 권한을 뜻한다."
  optionExplanations={['SetGID는 앞자리 2에 해당하므로 이 설명은 옳지 않다.', 'Sticky bit는 앞자리 1에 해당하며 디렉터리에 주로 적용되므로 이 설명은 옳지 않다.', '앞자리 4는 SetUID를 의미하며 실행 시 소유자 권한으로 동작하게 하므로 옳은 설명이다.', '앞자리 4가 특수 권한 SetUID를 나타내므로 특수 권한이 없다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={4}
  question="파일 무결성 점검 도구(Tripwire·AIDE)의 동작 원리로 가장 적절한 것은?"
  options={['① 네트워크 트래픽을 실시간으로 캡처해 침입 시그니처와 대조한다', '② 기준 시점에 파일들의 해시값을 데이터베이스로 저장해 두고, 이후 재계산한 해시값과 비교해 변조 여부를 판별한다', '③ 파일의 생성 날짜만 비교해 최신 파일인지 확인한다', '④ 파일의 확장자를 검사해 실행 파일 여부만 판별한다']}
  correctAnswer={2}
  explanation="Tripwire·AIDE 같은 무결성 점검 도구는 기준 시점의 파일 해시값을 데이터베이스로 저장해 두고, 주기적으로 현재 파일의 해시값을 다시 계산해 저장된 값과 비교함으로써 파일이 변조되었는지 탐지한다."
  optionExplanations={['실시간 트래픽 캡처와 시그니처 대조는 IDS의 동작 방식이며 파일 무결성 점검과는 다르다.', '기준 해시값과 현재 해시값의 비교가 무결성 점검 도구의 핵심 원리이므로 옳은 설명이다.', '생성 날짜만으로는 내용 변조 여부를 확인할 수 없으므로 옳지 않다.', '확장자 검사만으로는 내용이 변조되었는지 알 수 없으므로 옳지 않다.']}
/>

<Quiz
  questionNumber={5}
  question="버퍼 오버플로우 공격에서 스택에 저장된 리턴 주소를 조작했을 때 발생할 수 있는 가장 심각한 결과는?"
  options={['① 프로그램의 실행 속도가 느려진다', '② 함수 종료 시 공격자가 지정한 임의의 코드로 실행 흐름이 넘어갈 수 있다', '③ 디스크의 여유 공간이 줄어든다', '④ 네트워크 대역폭이 소모된다']}
  correctAnswer={2}
  explanation="버퍼 오버플로우로 스택의 리턴 주소까지 덮어쓰면, 함수가 끝나는 시점에 프로그램이 원래 돌아가야 할 곳이 아니라 공격자가 지정한 주소로 실행 흐름을 옮겨 임의 코드를 실행시킬 수 있다."
  optionExplanations={['실행 속도 저하는 이 취약점의 핵심 위험이 아니다.', '리턴 주소 조작을 통한 임의 코드 실행이 버퍼 오버플로우의 가장 심각한 결과이므로 옳은 설명이다.', '디스크 공간과는 직접적인 관련이 없다.', '네트워크 대역폭 소모는 DoS 공격의 특징이며 버퍼 오버플로우의 핵심 위험이 아니다.']}
/>

<Quiz
  questionNumber={6}
  question="일반 사용자 A 계정의 권한으로 로그인한 공격자가 시스템 취약점을 이용해 관리자(root) 권한을 획득했다. 이 공격의 분류로 옳은 것은?"
  options={['① 수평적 권한 상승', '② 수직적 권한 상승', '③ 세션 하이재킹', '④ 스니핑']}
  correctAnswer={2}
  explanation="일반 사용자 등급에서 관리자 등급으로 올라가는 것은 등급 자체가 높아지는 방향이므로 수직적 권한 상승에 해당한다."
  optionExplanations={['수평적 권한 상승은 같은 등급의 다른 계정 권한을 탈취하는 것이므로 이 상황과 다르다.', '더 높은 등급으로 올라간 상황이므로 수직적 권한 상승이 맞다.', '세션 하이재킹은 이미 성립된 세션을 가로채는 별개의 공격 유형이다.', '스니핑은 트래픽을 엿듣는 행위이며 권한 상승과는 다른 범주다.']}
/>

<Quiz
  questionNumber={7}
  question="루트킷(Rootkit)과 백도어(Backdoor)에 대한 설명으로 옳은 것은?"
  options={['① 백도어는 자신과 관련 프로세스의 존재를 숨기는 기술 모음이고, 루트킷은 정상 인증을 우회하는 접근 통로 자체다', '② 루트킷은 자신과 백도어, 관련 프로세스·파일·연결의 존재를 시스템에서 숨기는 기술 모음이다', '③ 루트킷과 백도어는 항상 커널 영역에서만 동작한다', '④ 백도어가 설치되면 반드시 루트킷도 함께 설치된다']}
  correctAnswer={2}
  explanation="백도어는 정상 인증 절차를 우회하는 접근 통로 자체를 가리키고, 루트킷은 그 백도어와 관련 프로세스·파일·네트워크 연결의 존재를 시스템에서 숨기는 기술 모음을 가리킨다. 2번이 루트킷의 정의를 정확히 설명한다."
  optionExplanations={['백도어와 루트킷의 역할 설명이 서로 뒤바뀌었으므로 옳지 않다.', '루트킷의 정의를 정확히 설명한 문장이므로 옳다.', '루트킷은 사용자 레벨과 커널 레벨 모두에서 존재할 수 있으므로 옳지 않다.', '백도어가 있다고 항상 루트킷이 함께 설치되는 것은 아니므로 옳지 않다.']}
/>

<Quiz
  questionNumber={8}
  question="어떤 서버가 매주 일요일 전체 백업을 수행하고, 나머지 요일에는 증분 백업(전날 백업 이후 변경분만 저장)을 수행한다. 수요일에 장애가 발생해 화요일 자정 시점으로 복구하려 할 때 필요한 백업본은?"
  options={['① 화요일 증분 백업본 하나만 있으면 된다', '② 일요일 전체 백업본과 월·화요일 증분 백업본을 순서대로 모두 적용해야 한다', '③ 월요일 증분 백업본 하나만 있으면 된다', '④ 백업본과 무관하게 복구할 수 없다']}
  correctAnswer={2}
  explanation="증분 백업은 매번 직전 백업 이후의 변경분만 저장하므로, 특정 시점으로 완전히 복원하려면 가장 최근의 전체 백업본에 그 이후의 모든 증분 백업본을 시간 순서대로 차례로 적용해야 한다."
  optionExplanations={['증분 백업본은 변경분만 담고 있어 그 자체만으로는 전체 상태를 복원할 수 없다.', '전체 백업 뒤에 이어지는 모든 증분 백업을 순서대로 적용해야 하므로 옳은 설명이다.', '월요일 증분만으로는 화요일의 변경분이 반영되지 않으므로 부족하다.', '전체 백업과 증분 백업을 조합하면 복구가 가능하므로 옳지 않다.']}
/>

<Quiz
  questionNumber={9}
  question="SIEM(Security Information and Event Management)의 상관분석(correlation analysis) 기능이 하는 역할로 가장 적절한 것은?"
  options={['① 여러 장비에서 수집한 로그를 개별적으로만 저장하고 서로 연결하지 않는다', '② 서로 다른 장비·시스템의 로그를 시간·패턴 기준으로 연결해, 개별 로그만으로는 안 보이던 공격 흐름을 찾아낸다', '③ 로그를 압축해 저장 공간만 절약한다', '④ 방화벽 규칙을 자동으로 삭제한다']}
  correctAnswer={2}
  explanation="SIEM의 상관분석은 방화벽·서버·IDS 등 여러 출처의 로그를 시간과 패턴 기준으로 연결해, 하나의 로그만 봐서는 드러나지 않는 침투 흐름 전체를 재구성해 보여주는 기능이다."
  optionExplanations={['개별 저장만으로는 상관분석이라 부를 수 없으며 SIEM의 핵심 가치를 놓친 설명이다.', '여러 출처의 로그를 연결해 공격 흐름을 재구성하는 것이 상관분석의 정확한 역할이다.', '저장 공간 절약은 상관분석의 목적이 아니다.', '방화벽 규칙 자동 삭제는 상관분석의 기능이 아니다.']}
/>

<Quiz
  questionNumber={10}
  question="서버 취약점 점검도구(OpenVAS·Nessus류)의 활용 절차에 관한 설명으로 옳지 않은 것은?"
  options={['① 점검 대상 시스템의 알려진 취약점 데이터베이스와 대조해 패치 누락·설정 오류를 찾아낸다', '② 점검 결과에서 발견된 취약점은 심각도에 따라 우선순위를 정해 조치한다', '③ 한 번 점검을 완료하면 이후에는 재점검 없이 영구적으로 안전이 보장된다', '④ 정기적인 재점검이 필요한 이유는 새로운 취약점이 계속 공개되기 때문이다']}
  correctAnswer={3}
  explanation="취약점은 시간이 지나면서 새로 발견·공개되므로 한 번의 점검으로 영구히 안전하다고 볼 수 없다. 정기적인 재점검이 필요하다는 4번과 반대되는 3번이 옳지 않다."
  optionExplanations={['알려진 취약점 데이터베이스와의 대조는 이런 점검도구의 표준 동작 방식이므로 옳다.', '심각도 기반 우선순위 조치는 실무의 표준 절차이므로 옳다.', '새로운 취약점이 계속 공개되므로 한 번의 점검으로 영구히 안전하다는 서술은 옳지 않다.', '정기적 재점검의 필요성을 정확히 설명하므로 옳다.']}
/>

<Quiz
  questionNumber={11}
  question="10.0.20.0/24 네트워크를 크기가 같은 8개의 서브넷으로 나누려 한다. 새로 사용해야 할 서브넷 마스크는?"
  options={['① /26', '② /27', '③ /28', '④ /25']}
  correctAnswer={2}
  explanation="서브넷 8개를 만들려면 2의 3제곱이 8이 되도록 3비트를 빌려야 한다. 기존 24비트에 3을 더하면 27비트, 즉 슬래시 27이 된다."
  optionExplanations={['2비트만 빌린 것으로 4개 서브넷만 만들어지므로 부족하다.', '3비트를 빌려 24에서 27이 되어야 8개 서브넷이 만들어지므로 정답이다.', '4비트를 빌린 것으로 16개 서브넷이 만들어져 필요한 수보다 많다.', '1비트만 빌린 것으로 2개 서브넷만 만들어지므로 크게 부족하다.']}
/>

<Quiz
  questionNumber={12}
  question="TCP 3-way handshake의 첫 번째 단계에서 클라이언트가 서버로 보내는 세그먼트에 설정되는 플래그와 그 의미로 옳은 것은?"
  options={['① ACK 플래그, 이전 데이터 수신 확인', '② SYN 플래그, 초기 순서 번호 동기화 요청', '③ FIN 플래그, 연결 종료 요청', '④ RST 플래그, 연결 강제 초기화']}
  correctAnswer={2}
  explanation="3-way handshake의 첫 단계는 클라이언트가 자신의 초기 순서 번호를 담아 SYN 플래그를 보내 연결 시작과 순서 번호 동기화를 요청하는 단계다."
  optionExplanations={['ACK만 보내는 것은 연결이 이미 성립된 이후의 확인 절차이며 첫 단계가 아니다.', 'SYN 플래그로 초기 순서 번호 동기화를 요청하는 것이 첫 단계의 정확한 설명이다.', 'FIN은 연결 종료 단계에서 쓰이는 플래그로 연결 시작과는 무관하다.', 'RST는 비정상 종료를 위한 플래그로 정상적인 연결 시작 절차와는 다르다.']}
/>

<Quiz
  questionNumber={13}
  question="사무실 네트워크에서 NAT(Network Address Translation)를 사용하는 주된 목적으로 가장 적절한 것은?"
  options={['① 내부의 사설 IP 주소를 하나 또는 소수의 공인 IP 주소로 변환해 인터넷과 통신하게 한다', '② 각 단말에 고정된 MAC 주소를 자동으로 할당한다', '③ 도메인 이름을 IP 주소로 변환한다', '④ 암호화된 터널을 만들어 원격지 네트워크와 연결한다']}
  correctAnswer={1}
  explanation="NAT는 조직 내부에서 쓰는 사설 IP 주소를 소수의 공인 IP 주소로 변환해, 부족한 공인 IP 자원을 절약하면서도 내부 단말이 인터넷과 통신할 수 있게 하는 기술이다."
  optionExplanations={['사설-공인 IP 주소 변환이 NAT의 핵심 목적이므로 옳은 설명이다.', 'MAC 주소 자동 할당은 DHCP나 별도 설정의 영역이며 NAT의 목적이 아니다.', '도메인 이름을 IP로 변환하는 것은 DNS의 역할이다.', '암호화된 터널 구성은 VPN의 역할이며 NAT 자체의 목적이 아니다.']}
/>

<Quiz
  questionNumber={14}
  question="nmap으로 SYN 스캔(-sS)을 수행했을 때, 대상 포트가 열려 있는 경우 공격자 쪽에서 최종적으로 보내는 패킷으로 가장 적절한 것은?"
  options={['① ACK를 보내 완전한 연결을 성립시킨다', '② RST를 보내 연결을 완성하지 않고 끊는다', '③ FIN을 보내 정상적으로 연결을 종료한다', '④ 아무 패킷도 보내지 않고 응답을 무한정 기다린다']}
  correctAnswer={2}
  explanation="SYN 스캔은 SYN을 보내 SYN/ACK 응답으로 포트가 열려 있음을 확인한 뒤, ACK 대신 RST를 보내 연결을 완성하지 않는다. 이 때문에 완전한 연결이 성립되지 않아 애플리케이션 로그에 흔적이 덜 남는다."
  optionExplanations={['ACK를 보내 연결을 완성하는 것은 커넥트 스캔의 동작 방식이다.', 'RST로 연결을 완성하지 않는 것이 SYN 스캔의 정확한 동작이므로 옳다.', 'FIN은 정상 종료 단계에서 쓰이며 SYN 스캔의 동작과는 다르다.', 'SYN 스캔은 포트 상태를 즉시 판정하고 넘어가므로 무한정 기다리지 않는다.']}
/>

<Quiz
  questionNumber={15}
  question="ARP 스푸핑 공격이 성립할 수 있는 근본적인 이유로 가장 적절한 것은?"
  options={['① ARP 프로토콜에 요청자를 검증하는 인증 절차가 없어 거짓 응답도 그대로 신뢰되기 때문이다', '② ARP는 암호화되지 않은 상태로 전송되는 파일 데이터이기 때문이다', '③ ARP는 라우터에서만 동작하고 스위치에서는 동작하지 않기 때문이다', '④ ARP 캐시는 절대 갱신되지 않고 최초 설정값을 영구히 유지하기 때문이다']}
  correctAnswer={1}
  explanation="ARP는 요청 없이 응답만 보내도 수신 측이 그대로 자신의 ARP 캐시를 갱신하는 구조로, 응답의 진위를 검증하는 인증 절차가 없다. 이 허점을 이용해 공격자가 거짓 IP-MAC 매핑을 뿌리는 것이 ARP 스푸핑의 핵심 원리다."
  optionExplanations={['인증 절차 부재가 ARP 스푸핑이 가능한 근본 이유이므로 옳다.', 'ARP는 파일 데이터가 아니라 주소 매핑을 위한 프로토콜 메시지이므로 이 설명은 옳지 않다.', 'ARP는 같은 브로드캐스트 도메인 안의 스위치 환경에서도 동일하게 동작하므로 옳지 않다.', 'ARP 캐시는 응답을 받을 때마다 갱신될 수 있어 영구히 고정된다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={16}
  question="DNS 캐시 포이즈닝(cache poisoning)이 단순 hosts 파일 변조보다 더 위험하다고 평가되는 이유로 가장 적절한 것은?"
  options={['① hosts 파일 변조는 암호화되어 있어 탐지가 더 어렵기 때문이다', '② DNS 서버 자체의 캐시를 조작하면 그 서버를 이용하는 다수 사용자 전체가 동시에 잘못된 주소로 유도되기 때문이다', '③ DNS 캐시 포이즈닝은 물리적으로 케이블을 절단해야만 가능하기 때문이다', '④ hosts 파일 변조는 재부팅해도 절대 복구되지 않기 때문이다']}
  correctAnswer={2}
  explanation="개별 hosts 파일 변조는 그 한 대의 단말에만 영향을 주지만, DNS 서버의 캐시 자체를 조작하면 그 DNS 서버를 이용하는 모든 사용자가 동시에 같은 잘못된 IP로 연결되어 피해 범위가 훨씬 크다."
  optionExplanations={['hosts 파일은 일반 텍스트 파일로 암호화되어 있지 않으며, 이 설명은 위험성의 근거가 아니다.', 'DNS 서버 캐시 조작이 미치는 광범위한 영향이 위험성의 정확한 근거이므로 옳다.', 'DNS 캐시 포이즈닝은 네트워크를 통한 원격 공격이며 물리적 케이블 절단이 필요하지 않다.', 'hosts 파일은 관리자가 원래대로 되돌리면 복구되므로 이 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={17}
  question="fail2ban이 SSH 무차별 대입 공격에 대응하는 방식으로 가장 적절한 것은?"
  options={['① SSH 프로토콜 자체의 암호화 알고리즘을 강제로 상위 버전으로 교체한다', '② 인증 로그를 실시간으로 감시하다가 짧은 시간 안에 로그인 실패가 임계치를 넘은 출발지 IP를 방화벽 규칙으로 자동 차단한다', '③ 모든 SSH 접속 시도를 예외 없이 영구적으로 차단한다', '④ 서버의 SSH 포트 번호를 무작위로 계속 변경한다']}
  correctAnswer={2}
  explanation="fail2ban은 인증 로그를 실시간으로 감시하다가 짧은 시간 안에 로그인 실패 횟수가 임계치를 넘은 출발지 IP를 방화벽 규칙에 자동으로 추가해 일정 시간 차단하는 방식으로 동작한다."
  optionExplanations={['암호화 알고리즘 교체는 fail2ban의 기능이 아니다.', '로그 기반 임계치 초과 IP의 자동 차단이 fail2ban의 정확한 동작 방식이므로 옳다.', '정상 사용자의 접속까지 영구 차단하면 서비스가 마비되므로 이런 식으로 동작하지 않는다.', '포트 번호 자동 변경은 fail2ban의 기능이 아니며 근본 대책도 아니다.']}
/>

<Quiz
  questionNumber={18}
  question="SYN Flood 공격이 서버에 미치는 직접적인 영향으로 가장 적절한 것은?"
  options={['① 서버에 저장된 파일이 암호화되어 열람할 수 없게 된다', '② half-open 연결이 백로그 큐를 가득 채워 정상 사용자의 새 연결 요청을 처리하지 못하게 된다', '③ 서버의 관리자 비밀번호가 평문으로 노출된다', '④ 서버가 발송하는 이메일에 악성코드가 자동으로 첨부된다']}
  correctAnswer={2}
  explanation="SYN Flood는 위조된 발신지로 SYN을 대량 전송해 ACK이 돌아오지 않는 half-open 연결을 백로그 큐에 계속 쌓아, 큐가 가득 차면 정상적인 새 연결 요청까지 처리하지 못하게 만든다."
  optionExplanations={['파일 암호화는 랜섬웨어의 특징이며 SYN Flood와는 다른 공격 유형이다.', '백로그 큐 고갈을 통한 정상 연결 거부가 SYN Flood의 정확한 직접적 영향이므로 옳다.', '비밀번호 노출은 SYN Flood와 무관한 별개의 공격 결과다.', '이메일 첨부 악성코드는 피싱·멀웨어 유포와 관련된 것으로 SYN Flood와는 무관하다.']}
/>

<Quiz
  questionNumber={19}
  question="공격자가 오픈 NTP 서버에 80바이트짜리 요청을 보내 4,480바이트짜리 응답을 유도했다. 이때 증폭 계수는 얼마인가?"
  options={['① 0.018배', '② 56배', '③ 4480배', '④ 80배']}
  correctAnswer={2}
  explanation="증폭 계수는 응답 패킷 크기를 요청 패킷 크기로 나눈 값이다. 4480을 80으로 나누면 56이 되므로 증폭 계수는 56배다."
  optionExplanations={['요청을 응답으로 나눈 역수 값으로, 정의를 반대로 계산한 결과다.', '4480을 80으로 나누면 56이 되므로 정답이다.', '응답 패킷 크기 자체를 그대로 답으로 고른 오류다.', '요청 패킷 크기 자체를 그대로 답으로 고른 오류다.']}
/>

<Quiz
  questionNumber={20}
  question="2016년 미국의 DNS 서비스 업체 Dyn을 대상으로 대규모 접속 장애를 일으킨 DDoS 공격에 동원된 봇넷으로 옳은 것은?"
  options={['① 제우스(Zeus)', '② 스턱스넷(Stuxnet)', '③ 미라이(Mirai)', '④ 워너크라이(WannaCry)']}
  correctAnswer={3}
  explanation="2016년 Dyn 공격은 기본 비밀번호를 사용하는 IoT 기기 수십만 대를 감염시킨 미라이 봇넷에 의해 수행되었으며, 이 공격으로 트위터·넷플릭스 등 다수 대형 서비스가 동시에 접속 장애를 겪었다."
  optionExplanations={['제우스는 금융 정보 탈취를 목적으로 한 뱅킹 악성코드로 이 사고와는 다른 사례다.', '스턱스넷은 산업제어시스템을 겨냥한 표적형 악성코드로 DDoS 봇넷과는 성격이 다르다.', 'IoT 기기를 감염시킨 미라이 봇넷이 이 대규모 DDoS 공격의 주체이므로 정답이다.', '워너크라이는 SMB 취약점을 이용해 전파된 랜섬웨어로 이 DDoS 사고와는 무관하다.']}
/>

<Quiz
  questionNumber={21}
  question="IPSec의 AH(Authentication Header)와 ESP(Encapsulating Security Payload)의 차이로 가장 적절한 것은?"
  options={['① AH는 기밀성(암호화)과 무결성을 모두 제공하고, ESP는 무결성만 제공한다', '② AH는 무결성과 인증만 제공하고, ESP는 암호화를 통한 기밀성과 무결성을 함께 제공할 수 있다', '③ AH와 ESP는 완전히 동일한 기능을 제공하는 중복 프로토콜이다', '④ AH와 ESP는 모두 전송 계층에서만 동작하며 IP 계층과는 무관하다']}
  correctAnswer={2}
  explanation="AH는 데이터의 무결성과 발신지 인증만 제공하며 암호화 기능이 없다. 반면 ESP는 암호화를 통한 기밀성을 제공하면서 무결성·인증 기능도 함께 수행할 수 있어, 실무에서는 ESP가 더 널리 쓰인다."
  optionExplanations={['AH에는 암호화 기능이 없으므로 기밀성을 제공한다는 설명은 옳지 않다.', 'AH는 무결성·인증만, ESP는 기밀성까지 함께 제공한다는 차이를 정확히 설명하므로 옳다.', '두 프로토콜은 제공하는 보안 서비스 범위가 다르므로 동일하다는 설명은 옳지 않다.', 'IPSec은 네트워크(IP) 계층에서 동작하는 프로토콜이므로 이 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={22}
  question="IDS(침입 탐지 시스템)와 IPS(침입 방지 시스템)의 배치와 대응 방식에 관한 설명으로 옳지 않은 것은?"
  options={['① IDS는 스위치의 미러링(SPAN/TAP) 포트를 통해 트래픽을 아웃오브밴드로 분석한다', '② IPS는 트래픽 경로에 인라인으로 위치해 공격으로 판단된 패킷을 즉시 차단할 수 있다', '③ IDS와 IPS는 서로 완전히 다른 탐지 알고리즘만 사용할 수 있으며 시그니처 기반과 이상행위 기반을 공유할 수 없다', '④ IPS 장비 자체에 장애가 발생하면 전체 통신이 끊기거나 검사 없이 통과되는 정책을 미리 정해 두어야 한다']}
  correctAnswer={3}
  explanation="IDS와 IPS는 같은 탐지 엔진(시그니처 기반·이상행위 기반)을 공유해 쓸 수 있으며, 근본적인 차이는 탐지 알고리즘이 아니라 아웃오브밴드 탐지(IDS)와 인라인 즉시 차단(IPS)이라는 배치·대응 방식에 있다. 따라서 3번이 옳지 않다."
  optionExplanations={['IDS가 미러링 포트를 통해 아웃오브밴드로 동작한다는 설명은 옳다.', 'IPS가 인라인에 위치해 즉시 차단한다는 설명은 옳다.', 'IDS와 IPS는 같은 탐지 엔진을 공유할 수 있으므로 완전히 다른 알고리즘만 쓴다는 서술이 옳지 않다.', 'IPS 장애 시 fail-close·fail-open 정책을 미리 정해야 한다는 설명은 옳다.']}
/>

## 참고 자료

- [itwiki - 정보보안기사 필기 출제기준](https://itwiki.kr/w/정보보안기사_필기_출제기준)
- [한국방송통신전파진흥원(cq.or.kr) - 정보보안기사 자격정보](https://www.cq.or.kr/qh_quagm01_020.do)
- [공공데이터포털 - 한국방송통신전파진흥원_국가기술자격검정 정보보안 종목 출제기준](https://www.data.go.kr/data/15140079/fileData.do)
- [KISA 보호나라&KrCERT/CC - 사이버 위협 동향 보고서](https://www.krcert.or.kr/)
