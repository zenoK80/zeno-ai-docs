---
title: '33. 기출·예상문제: 정보보안일반(암호·인증·접근통제)'
description: '4과목 정보보안일반의 인증·접근통제 모델·대칭키공개키 암호 알고리즘·해시전자서명PKI·키분배 문제를 출제기준 기반으로 재구성한 24문항, RSA와 모듈러 연산 계산 문제 집중 훈련 포함'
---

<Callout type="warning">
  **이 편의 문제는 실제 기출을 그대로 복제한 것이 아닙니다.** 한국방송통신전파진흥원 출제기준의 4과목(정보보안일반) 세부항목인 보안요소기술·암호학을 기준으로 숫자·상황·보기 표현을 새로 구성한 **재구성 문항**입니다. 특히 RSA·모듈러 연산 계산 문항은 실제 시험에서 자주 다루는 계산 패턴을 그대로 익히도록 소수를 새로 바꾸어 재구성했습니다.
</Callout>

## 문항 구성표

| 영역 | 관련 편 | 문항 번호 | 문항 수 |
|---|---|---|---|
| 인증 기술 심화 | 21 | 1–5 | 5 |
| 접근통제 정책과 모델 | 22 | 6–10 | 5 |
| 대칭키·공개키 암호 알고리즘 심화(계산 포함) | 23 | 11–15 | 5 |
| 해시함수·전자서명·PKI 실무 | 24 | 16–20 | 5 |
| 키 분배 프로토콜과 최신 암호기술 동향 | 25 | 21–24 | 4 |
| **합계** | – | – | **24** |

계산 문항은 반드시 손으로 직접 나눗셈과 나머지를 계산해 보고 답을 확정한 뒤 해설과 맞춰 보십시오. 암산이나 눈대중으로 짚으면 나머지 계산 실수를 스스로 발견하지 못합니다.

## 인증 기술 심화 (1–5)

> **출제 포인트:** 인증의 세 요소인 지식 기반·소유 기반·존재(생체) 기반을 사례에서 정확히 골라내는 문제입니다. 두 요소를 동시에 요구하면 다중요소인증(MFA)이 됩니다.

<Quiz
  questionNumber={1}
  question="한 온라인 뱅킹 서비스가 로그인 시 비밀번호 입력과 함께, 사용자의 휴대전화에 미리 설치된 앱이 매 30초마다 새로 생성하는 6자리 숫자를 함께 입력하도록 요구한다. 이 인증 방식의 조합으로 가장 적절한 것은?"
  options={['지식 기반 요소와 소유 기반 요소의 조합', '지식 기반 요소와 존재 기반 요소의 조합', '소유 기반 요소와 존재 기반 요소의 조합', '지식 기반 요소만을 이중으로 사용한 조합']}
  correctAnswer={1}
  explanation="비밀번호는 사용자가 기억하고 있는 정보이므로 지식 기반(what you know) 요소이고, 휴대전화 앱이 생성하는 일회용 숫자는 그 기기를 실제로 소유하고 있어야만 얻을 수 있는 값이므로 소유 기반(what you have) 요소다. 서로 다른 범주의 두 요소를 함께 요구하므로 이 방식은 다중요소인증(MFA, Multi-Factor Authentication)에 해당하며, 비밀번호 하나만 유출되어도 공격자가 휴대전화까지 가지고 있지 않으면 로그인할 수 없다는 점에서 단일 요소 인증보다 안전하다."
  optionExplanations={['정답이다. 비밀번호는 지식 기반, 휴대전화 앱의 일회용 숫자는 소유 기반 요소이므로 이 조합이 맞다.', '존재 기반(what you are) 요소는 지문·홍채 같은 생체 정보를 가리키며, 이 사례에는 생체 정보가 등장하지 않는다.', '이 사례에는 생체 정보가 없고 비밀번호라는 지식 기반 요소가 포함되어 있으므로 이 조합은 맞지 않는다.', '휴대전화 앱의 숫자는 기억하는 정보가 아니라 소유한 기기에서 생성되는 값이므로 지식 기반 요소를 이중으로 쓴 것이 아니다.']}
/>

> **출제 포인트:** 메시지 인증코드(MAC)를 재전송 공격(Replay Attack)으로부터 지키는 방법을 확인합니다. MAC 값 자체는 메시지 위조를 막지만, 정당한 메시지를 그대로 다시 보내는 재전송까지는 막지 못한다는 점이 핵심입니다.

<Quiz
  questionNumber={2}
  question="계좌 이체 요청 메시지에 메시지 인증코드(MAC, Message Authentication Code)를 첨부해 무결성과 발신자 인증을 확인하는 시스템이 있다. 공격자가 이 메시지를 그대로 가로챈 뒤 시간이 지난 후 똑같은 메시지를 다시 서버로 전송해 이체를 중복 발생시켰다. 이 문제를 막기 위한 대응으로 가장 적절한 것은?"
  options={['MAC 계산에 사용하는 비밀키의 길이를 두 배로 늘린다', '메시지에 매번 달라지는 순번(nonce)이나 타임스탬프를 포함해 MAC을 계산하고, 서버가 이미 처리한 값인지 확인한다', 'MAC 알고리즘을 해시함수에서 공개키 암호 알고리즘으로 교체한다', '메시지를 압축한 뒤 전송해 가로채기 자체를 물리적으로 어렵게 만든다']}
  correctAnswer={2}
  explanation="MAC은 메시지 내용이 위조되었는지를 검증할 뿐, 정당하게 생성된 메시지를 공격자가 그대로 복사해 나중에 다시 보내는 재전송 공격까지는 막지 못한다. 이를 막으려면 메시지마다 값이 달라지는 순번(nonce)이나 시간 정보(timestamp)를 포함시켜 MAC을 계산하고, 서버가 이미 처리한 순번이거나 허용된 시간 범위를 벗어난 요청은 거부하도록 해야 한다. 이렇게 하면 공격자가 가로챈 메시지를 그대로 재전송해도 순번이나 시간 조건이 이미 소진되었거나 만료되어 거부된다."
  optionExplanations={['비밀키 길이를 늘리는 것은 무차별 대입으로 키를 알아내기 어렵게 할 뿐, 정당한 메시지를 그대로 복사해 재전송하는 문제와는 무관하다.', '정답이다. 매번 달라지는 순번이나 시간 정보를 포함해 MAC을 계산하고 중복 여부를 확인하면 동일한 메시지의 재전송을 탐지해 거부할 수 있다.', '알고리즘 종류를 바꾸는 것은 위변조 탐지 능력과 관련될 뿐, 정당한 메시지의 반복 재전송 여부를 판별하는 것과는 무관하다.', '메시지 압축은 전송 효율과 관련된 조치이며 가로채기 자체를 막거나 재전송 여부를 판별하는 보안 조치가 아니다.']}
/>

> **출제 포인트:** 디바이스 인증서 기반 인증이 아이디·비밀번호 인증과 다른 점을 확인합니다. 인증서 기반 인증의 핵심은 개인키를 소유한 기기만이 서명을 생성할 수 있다는 것입니다.

<Quiz
  questionNumber={3}
  question="한 기업이 사내망 접속 단말을 통제하기 위해, 각 단말에 개별 인증서와 그에 대응하는 개인키를 설치하고 접속 시 그 인증서로 서명한 값을 검증하는 방식을 도입했다. 이 방식에 대한 설명으로 옳지 않은 것은?"
  options={['정당한 인증서와 개인키가 설치된 단말만 서명을 생성할 수 있어, 아이디와 비밀번호만 아는 것으로는 접속할 수 없다', '개인키가 단말 밖으로 복제되어 다른 기기에 설치되면 그 기기도 정당한 단말처럼 인식될 위험이 있다', '이 방식은 단말이라는 사물을 인증 대상으로 삼는 디바이스 인증에 해당한다', '인증서 기반 인증은 비밀번호 기반 인증보다 관리가 필요 없어 인증서 발급과 폐기 절차를 별도로 두지 않아도 된다']}
  correctAnswer={4}
  explanation="인증서 기반 인증은 비밀번호를 외우지 않아도 된다는 장점이 있지만, 그 대신 인증서 발급·갱신·폐기라는 별도의 생명주기 관리가 반드시 필요하다. 단말을 폐기하거나 분실했을 때 해당 인증서를 즉시 폐지 목록에 올리지 않으면, 개인키가 유출된 단말이 여전히 정당한 단말처럼 인증을 통과할 수 있다. 따라서 인증서 기반 인증이 관리를 아예 필요로 하지 않는다는 서술은 옳지 않다."
  optionExplanations={['개인키가 설치된 단말만 유효한 서명을 만들 수 있으므로 아이디와 비밀번호만으로는 접속할 수 없다는 설명은 옳다.', '개인키가 복제되어 유출되면 그 키를 가진 다른 기기도 정당한 단말처럼 인식될 수 있다는 것은 실제 위험이므로 옳다.', '사람이 아니라 단말이라는 사물 자체를 인증 대상으로 삼는다는 점에서 디바이스 인증에 해당한다는 설명은 옳다.', '정답이다. 인증서 기반 인증도 발급·갱신·폐기 절차라는 별도의 관리 체계가 반드시 필요하며, 관리가 필요 없다는 서술은 옳지 않다.']}
/>

> **출제 포인트:** 생체인증 성능 지표인 FAR(오인식률)과 FRR(오거부율)을 구분하는 문제입니다. 두 지표는 서로 상충 관계에 있어 하나를 낮추면 다른 하나가 올라가는 경향이 있습니다.

<Quiz
  questionNumber={4}
  question="생체인증 시스템의 오류율 지표에 대한 설명으로 옳지 않은 것은?"
  options={['FAR(False Acceptance Rate, 오인식률)는 등록되지 않은 사람을 정당한 사람으로 잘못 인식해 통과시키는 비율이다', 'FRR(False Rejection Rate, 오거부율)는 정당하게 등록된 사람을 인식하지 못해 거부하는 비율이다', '판정 기준(임계값)을 엄격하게 조정해 FAR을 낮추면 일반적으로 FRR도 함께 낮아진다', 'FAR과 FRR이 같아지는 지점의 오류율을 등오류율(EER, Equal Error Rate)이라 하며 시스템 성능 비교에 활용된다']}
  correctAnswer={3}
  explanation="FAR과 FRR은 판정 기준(임계값)을 어느 쪽으로 조정하느냐에 따라 서로 반대 방향으로 움직이는 상충(trade-off) 관계에 있다. 판정 기준을 엄격하게 조정해 타인을 잘못 통과시키는 비율(FAR)을 낮추면, 그만큼 정당한 사용자도 걸러질 가능성이 커져 오거부율(FRR)은 오히려 높아진다. 그래서 실무에서는 두 오류율이 같아지는 지점인 등오류율을 기준으로 삼아 서로 다른 생체인증 시스템의 전반적인 성능을 비교한다."
  optionExplanations={['등록되지 않은 사람을 정당한 사람으로 잘못 인식하는 비율이 FAR이라는 정의는 옳다.', '정당한 사람을 인식하지 못해 거부하는 비율이 FRR이라는 정의는 옳다.', '정답이다. FAR을 낮추도록 기준을 엄격하게 조정하면 정당한 사용자도 함께 걸러질 가능성이 커져 FRR은 오히려 올라가는 상충 관계이므로, 함께 낮아진다는 서술은 옳지 않다.', 'FAR과 FRR이 같아지는 지점의 오류율을 등오류율이라 부르고 성능 비교 기준으로 쓴다는 설명은 옳다.']}
/>

> **출제 포인트:** 인증 요소 결합 시 보안 강도가 어떻게 달라지는지 확인합니다. 서로 다른 범주의 요소를 결합해야 진정한 다중요소인증이 되며, 같은 범주를 여러 개 요구하는 것과는 다릅니다.

<Quiz
  questionNumber={5}
  question="다중요소인증(MFA)에 대한 설명으로 옳지 않은 것은?"
  options={['비밀번호와 함께 보안 질문(어머니의 성함 등)을 추가로 묻는 방식은 두 요소 모두 지식 기반이므로 엄밀한 의미의 다중요소인증으로 보기 어렵다', '지식 기반 요소 하나와 소유 기반 요소 하나를 결합하면 서로 다른 범주가 결합되어 다중요소인증의 요건을 충족한다', '다중요소인증을 적용하면 한 요소가 유출되어도 공격자가 나머지 요소까지 확보해야 인증을 통과할 수 있어 보안성이 높아진다', '다중요소인증은 요소의 개수만 중요하므로 같은 범주의 인증 수단을 세 개 요구하면 서로 다른 범주 두 개를 요구하는 것보다 항상 더 안전하다']}
  correctAnswer={4}
  explanation="다중요소인증의 핵심은 단순히 인증 수단의 개수가 아니라 서로 다른 범주(지식·소유·존재)를 결합하는 데 있다. 같은 범주 안에서 여러 수단을 요구하면 그 범주 전체가 뚫리는 공격 방법 하나에 모든 수단이 함께 무력화될 수 있지만, 범주가 다르면 공격자가 전혀 다른 방식의 공격을 각각 성공시켜야 하므로 실질적인 방어 효과가 더 크다. 그래서 같은 범주의 수단을 많이 쌓는 것이 항상 더 안전하다고 단정할 수 없다."
  optionExplanations={['비밀번호와 보안 질문은 둘 다 기억에 의존하는 지식 기반 요소이므로, 엄밀한 다중요소인증으로 보기 어렵다는 설명은 옳다.', '서로 다른 범주인 지식 기반과 소유 기반을 결합하면 다중요소인증의 요건을 충족한다는 설명은 옳다.', '한 요소가 유출되어도 다른 범주의 요소까지 확보해야 하므로 보안성이 높아진다는 설명은 다중요소인증의 실제 효과이므로 옳다.', '정답이다. 다중요소인증의 핵심은 요소 범주의 다양성이며, 같은 범주 여러 개가 서로 다른 범주 결합보다 항상 더 안전하다고 볼 수는 없다.']}
/>

## 접근통제 정책과 모델 (6–10)

> **출제 포인트:** 접근통제 3요소인 식별-인증-인가의 순서와 각 단계가 답하는 질문을 구분합니다.

<Quiz
  questionNumber={6}
  question="접근통제의 3요소인 식별(Identification), 인증(Authentication), 인가(Authorization)에 대한 설명으로 옳지 않은 것은?"
  options={['식별은 사용자가 자신이 누구라고 주장하는 단계로, 아이디를 입력하는 행위가 이에 해당한다', '인증은 그 주장이 사실인지 비밀번호나 생체정보 등으로 검증하는 단계다', '인가는 인증에 성공한 사용자에게 어떤 자원에 어떤 작업을 허용할지 결정하는 단계다', '인증에 성공하면 그 사실만으로 시스템의 모든 자원에 대한 접근 권한이 자동으로 부여된다']}
  correctAnswer={4}
  explanation="인증은 그 사용자가 주장한 신원이 맞는지를 확인하는 단계일 뿐, 그 사용자가 무엇을 할 수 있는지는 별개의 인가 단계에서 결정된다. 인증에 성공했다고 해서 모든 자원에 대한 권한이 자동으로 주어진다면 최소 권한 원칙이 무너지고, 낮은 권한이어야 할 일반 사용자가 관리자 전용 기능까지 접근할 수 있게 되는 심각한 취약점이 된다."
  optionExplanations={['아이디 입력처럼 자신이 누구라고 주장하는 단계가 식별이라는 정의는 옳다.', '그 주장을 비밀번호나 생체정보로 검증하는 단계가 인증이라는 정의는 옳다.', '인증된 사용자에게 구체적으로 어떤 권한을 허용할지 정하는 단계가 인가라는 정의는 옳다.', '정답이다. 인증은 신원 확인일 뿐이며, 자원별 권한 부여는 별도의 인가 절차를 거쳐야 하므로 자동으로 모든 권한이 주어진다는 서술은 옳지 않다.']}
/>

> **출제 포인트:** DAC(임의적 접근통제)·MAC(강제적 접근통제)·RBAC(역할기반 접근통제) 세 모델을 "누가 권한을 정하는가"라는 기준으로 구분하는 문제입니다.

<Quiz
  questionNumber={7}
  question="접근통제 모델인 DAC, MAC, RBAC에 대한 설명으로 옳지 않은 것은?"
  options={['DAC(Discretionary Access Control, 임의적 접근통제)는 자원의 소유자가 다른 사용자에게 줄 권한을 스스로 결정할 수 있는 모델이다', 'MAC(Mandatory Access Control, 강제적 접근통제)는 자원 소유자가 아니라 시스템 관리자가 정한 보안 등급(레이블)에 따라 접근 여부가 강제로 결정되는 모델이다', 'RBAC(Role-Based Access Control, 역할기반 접근통제)는 사용자 개인이 아니라 역할(직무)에 권한을 부여하고, 사용자에게는 역할을 할당하는 방식으로 관리 부담을 줄인다', 'MAC은 사용자의 자율적 판단을 최대한 존중하는 모델이므로 군사·정부 기관보다 자유로운 정보 공유가 중요한 일반 기업 환경에 더 적합하다']}
  correctAnswer={4}
  explanation="MAC은 오히려 사용자 개인의 자율적 판단을 배제하고, 중앙에서 정한 보안 등급 규칙에 따라 접근 여부가 강제로 결정되는 모델이다. 소유자나 사용자가 마음대로 권한을 바꿀 수 없어 통제는 엄격하지만 유연성이 떨어지므로, 정보의 자유로운 공유보다 엄격한 기밀 등급 통제가 중요한 군사·정부 기관에서 주로 쓰인다. 자유로운 정보 공유가 중요한 일반 기업 환경에는 오히려 소유자가 권한을 유연하게 조정할 수 있는 DAC이나 역할 단위로 관리하는 RBAC이 더 널리 쓰인다."
  optionExplanations={['자원의 소유자가 권한을 스스로 정할 수 있다는 것은 DAC의 정의와 정확히 일치하므로 옳다.', '소유자가 아니라 중앙에서 정한 보안 등급에 따라 강제로 접근이 결정된다는 것은 MAC의 정의와 정확히 일치하므로 옳다.', '개인이 아니라 역할에 권한을 부여하고 사용자에게 역할을 할당해 관리 부담을 줄인다는 것은 RBAC의 정의와 정확히 일치하므로 옳다.', '정답이다. MAC은 사용자의 자율적 판단을 배제하고 강제로 통제하는 모델이며, 오히려 자유로운 공유보다 엄격한 등급 통제가 필요한 군사·정부 기관에 적합하므로 이 서술은 옳지 않다.']}
/>

> **출제 포인트:** 접근통제 목록(ACL)과 권한 목록(Capability)의 관점 차이를 구분합니다. ACL은 자원을 기준으로, Capability는 사용자(주체)를 기준으로 권한을 기록합니다.

<Quiz
  questionNumber={8}
  question="접근통제 기법인 ACL(Access Control List)과 Capability(권한 목록)에 대한 설명으로 옳지 않은 것은?"
  options={['ACL은 자원(객체)마다 그 자원에 접근할 수 있는 사용자와 허용된 작업 목록을 관리하는 방식이다', 'Capability는 사용자(주체)마다 그 사용자가 접근할 수 있는 자원과 권한 목록을 관리하는 방식이다', '특정 사용자가 접근 가능한 전체 자원 목록을 한눈에 파악하기에는 자원 중심으로 흩어져 기록되는 ACL 방식보다 Capability 방식이 유리하다', 'ACL 방식에서는 특정 자원에 접근 가능한 전체 사용자 목록을 확인하기가 Capability 방식보다 오히려 더 어렵다']}
  correctAnswer={4}
  explanation="ACL은 애초에 자원(객체)을 기준으로 권한 목록을 관리하는 방식이므로, 특정 자원에 접근 가능한 사용자 전체를 확인하는 작업은 그 자원의 ACL 항목만 보면 되어 오히려 쉽다. 반대로 Capability는 사용자를 기준으로 권한을 관리하므로 특정 자원에 대한 접근자 전체를 파악하려면 모든 사용자의 권한 목록을 뒤져야 해 더 번거롭다. 두 방식은 서로 정반대의 강점과 약점을 가진다."
  optionExplanations={['자원마다 접근 가능한 사용자와 허용 작업을 관리한다는 것은 ACL의 정의와 정확히 일치하므로 옳다.', '사용자마다 접근 가능한 자원과 권한을 관리한다는 것은 Capability의 정의와 정확히 일치하므로 옳다.', '사용자 중심으로 권한이 묶여 있으므로 그 사용자의 목록만 보면 전체 접근 가능 자원을 파악할 수 있어 Capability가 유리하다는 설명은 옳다.', '정답이다. ACL은 자원 중심으로 관리되므로 특정 자원의 접근자 전체를 확인하는 것은 오히려 Capability보다 쉬우며, 이 서술은 방향이 반대로 되어 있다.']}
/>

> **출제 포인트:** 최소권한 원칙과 직무분리 원칙의 목적 차이를 구분합니다. 최소권한은 개인이 가진 권한의 범위를 줄이는 것이고, 직무분리는 하나의 중요한 업무를 여러 사람에게 나누는 것입니다.

<Quiz
  questionNumber={9}
  question="한 기업의 재무 시스템에서 결제 등록과 결제 승인을 서로 다른 담당자가 각각 수행하도록 시스템 권한을 나누어, 한 사람이 결제를 등록하고 스스로 승인까지 끝낼 수 없게 만들었다. 이 조치가 구현하고 있는 원칙으로 가장 적절한 것은?"
  options={['최소 권한 원칙(Least Privilege)', '직무 분리 원칙(Separation of Duties)', '보안 등급 격리 원칙', '단일 장애점 제거 원칙']}
  correctAnswer={2}
  explanation="직무 분리 원칙은 하나의 중요한 업무를 한 사람이 처음부터 끝까지 혼자 처리하지 못하도록 여러 사람에게 단계를 나누어 배정하는 통제다. 결제 등록자와 승인자를 분리하면, 한 사람이 부정한 결제를 등록하더라도 다른 사람의 승인을 거치지 않고는 완결시킬 수 없어 내부자에 의한 부정 행위 위험이 줄어든다. 이와 달리 최소 권한 원칙은 한 사람이 가진 권한의 범위 자체를 업무에 필요한 최소한으로 좁히는 것으로, 이 사례처럼 업무를 여러 사람에게 나누는 것과는 초점이 다르다."
  optionExplanations={['최소 권한 원칙은 한 사람이 가진 권한의 범위를 줄이는 것이며, 이 사례처럼 업무 단계를 여러 사람에게 나누는 것과는 초점이 다르다.', '정답이다. 결제 등록과 승인을 서로 다른 담당자에게 나누어 한 사람이 업무 전체를 완결하지 못하게 한 것은 직무 분리 원칙의 정의와 정확히 일치한다.', '보안 등급 격리 원칙이라는 명칭은 이 분류 체계에서 통용되는 표준 용어가 아니다.', '단일 장애점 제거는 시스템 구성 요소가 하나만 있어서 그것이 멈추면 전체가 멈추는 상황을 없애는 가용성 관점의 원칙으로, 업무 분담과는 성격이 다르다.']}
/>

> **출제 포인트:** RBAC(역할기반 접근통제)에서 역할 계층(Role Hierarchy)과 직무 분리 제약을 함께 적용하는 사례를 확인합니다.

<Quiz
  questionNumber={10}
  question="RBAC(역할기반 접근통제)를 도입한 조직에서, 신입 직원에게 개별 권한을 하나씩 부여하지 않고 그 직원이 맡은 팀원 역할에 이미 정의된 권한 묶음을 그대로 할당했다. RBAC의 이러한 특징이 주는 이점으로 가장 적절한 것은?"
  options={['개인별로 서로 다른 조합의 권한을 자유롭게 설정할 수 있어 가장 세밀한 통제가 가능하다', '역할에 권한을 미리 정의해 두면 인사이동이 잦아도 역할 할당만 바꾸면 되어 권한 관리 부담이 줄어든다', '역할이라는 개념을 사용하면 시스템 관리자를 별도로 두지 않아도 권한이 자동으로 결정된다', '역할기반 접근통제를 적용하면 직무 분리 제약을 시스템적으로 표현할 필요가 없어진다']}
  correctAnswer={2}
  explanation="RBAC의 핵심 이점은 개인마다 권한을 일일이 다시 설정하지 않고, 역할에 미리 정의해 둔 권한 묶음을 사용자에게 할당하거나 해제하는 것만으로 권한 관리를 끝낼 수 있다는 점이다. 신입 직원이 들어오거나 부서를 이동해도 그 사람에게 맞는 역할만 바꿔 주면 되므로, 개인별로 권한을 하나하나 재설정해야 하는 방식보다 관리 부담이 크게 줄어든다. 오히려 세밀한 개인별 커스터마이징이 필요한 경우에는 RBAC보다 유연성이 떨어질 수 있다."
  optionExplanations={['개인별로 자유롭게 권한을 설정하는 것은 RBAC이 아니라 DAC에 가까운 특징이며, RBAC은 오히려 역할 단위로 표준화해 세밀한 개인별 조정을 줄이는 방식이다.', '정답이다. 역할에 권한을 미리 정의해 두고 역할 할당만 바꾸는 방식이 인사이동이 잦은 조직에서 관리 부담을 줄이는 RBAC의 실제 이점이다.', 'RBAC을 도입해도 역할과 권한을 정의하고 유지 관리하는 시스템 관리자의 역할은 여전히 필요하다.', 'RBAC에서는 오히려 상호 배타적인 역할을 동시에 할당하지 못하게 하는 방식으로 직무 분리 제약을 시스템적으로 표현할 수 있으며, 이 서술은 반대다.']}
/>

## 대칭키·공개키 암호 알고리즘 심화(계산 포함) (11–15)

> **출제 포인트:** 대칭키와 공개키 암호의 근본적인 차이를 키 개수와 속도 측면에서 구분하는 문제입니다.

<Quiz
  questionNumber={11}
  question="대칭키 암호와 공개키 암호에 대한 설명으로 옳지 않은 것은?"
  options={['대칭키 암호는 암호화와 복호화에 같은 키를 사용하므로 통신 상대와 사전에 키를 안전하게 공유해야 한다', '공개키 암호는 공개키로 암호화한 내용을 그 쌍이 되는 개인키로만 복호화할 수 있는 구조를 이용한다', '동일한 안전성 수준을 내려면 공개키 암호가 대칭키 암호보다 훨씬 짧은 키 길이로도 충분하다', '일반적으로 같은 양의 데이터를 처리할 때 공개키 암호 연산이 대칭키 암호 연산보다 느리다']}
  correctAnswer={3}
  explanation="같은 안전성 수준을 얻으려면 공개키 암호가 대칭키 암호보다 오히려 훨씬 더 긴 키 길이를 필요로 한다. 예를 들어 128비트 대칭키 암호와 비슷한 안전성을 얻으려면 RSA는 3072비트 안팎의 키가 필요하다. 이는 공개키 암호가 소인수분해나 이산로그처럼 수학적으로 풀기 어려운 문제에 기반하는데, 이 문제들은 키 길이가 늘어나는 만큼 안전성이 비례해서 커지지 않아 더 긴 키가 필요하기 때문이다."
  optionExplanations={['대칭키 암호는 같은 키로 암호화와 복호화를 모두 수행하므로 사전에 안전한 키 공유가 필요하다는 설명은 옳다.', '공개키로 암호화한 내용은 그 쌍인 개인키로만 풀 수 있다는 것은 공개키 암호의 정의와 정확히 일치하므로 옳다.', '정답이다. 동일한 안전성을 위해서는 공개키 암호가 대칭키 암호보다 훨씬 더 긴 키 길이를 필요로 하므로, 더 짧아도 된다는 서술은 반대로 되어 있다.', '수학적 구조가 복잡한 공개키 연산이 대칭키 연산보다 일반적으로 느리다는 것은 실제 성능 특성이므로 옳다.']}
/>

> **출제 포인트:** 블록 암호 운용 모드인 ECB와 CBC의 차이를 확인합니다. 핵심은 이전 블록과의 연쇄 여부이며, 이것이 패턴 노출 여부를 결정합니다.

<Quiz
  questionNumber={12}
  question="블록 암호 운용 모드인 ECB(Electronic Codebook)와 CBC(Cipher Block Chaining)에 대한 설명으로 옳지 않은 것은?"
  options={['ECB는 각 평문 블록을 이전 블록과 무관하게 독립적으로 암호화하므로, 같은 평문 블록은 항상 같은 암호문 블록으로 나타난다', 'CBC는 각 평문 블록을 암호화하기 전에 직전 암호문 블록과 XOR 연산을 수행해 블록 간 연쇄를 만든다', 'CBC는 최초 블록을 암호화할 때 사용할 직전 블록이 없으므로 초기화 벡터(IV, Initialization Vector)를 대신 사용한다', 'ECB는 평문의 반복 패턴이 암호문에도 그대로 드러날 수 있다는 약점이 있지만, 이 약점은 이미지처럼 반복 패턴이 있는 데이터에서는 문제되지 않는다']}
  correctAnswer={4}
  explanation="ECB의 대표적인 약점은 같은 평문 블록이 항상 같은 암호문 블록으로 매핑된다는 점이며, 이 성질 때문에 오히려 이미지처럼 넓은 영역에 반복되는 패턴(같은 색상 배경 등)이 있는 데이터일수록 그 반복 패턴이 암호문에서도 고스란히 드러난다. 실제로 ECB로 암호화한 이미지에서 원본의 윤곽이 그대로 비쳐 보이는 사례가 이 약점을 설명할 때 자주 인용된다. 즉 반복 패턴이 있는 데이터일수록 오히려 ECB의 약점이 더 크게 드러나며, 문제되지 않는다는 서술은 옳지 않다."
  optionExplanations={['각 블록을 독립적으로 암호화해 같은 평문 블록이 같은 암호문 블록이 된다는 것은 ECB의 정의와 정확히 일치하므로 옳다.', '직전 암호문 블록과 XOR한 뒤 암호화해 블록 간 연쇄를 만든다는 것은 CBC의 정의와 정확히 일치하므로 옳다.', '첫 블록에는 직전 블록이 없으므로 초기화 벡터를 대신 사용한다는 것은 CBC의 실제 동작 방식이므로 옳다.', '정답이다. 반복 패턴이 있는 데이터일수록 ECB의 패턴 노출 약점이 오히려 더 뚜렷하게 드러나므로, 문제되지 않는다는 서술은 옳지 않다.']}
/>

> **출제 포인트:** RSA 키 생성과 암복호화의 실제 계산 절차를 소수 예시로 직접 밟아 보는 문제입니다. 소인수 선택 → 모듈러스 계산 → 오일러 파이 함수 계산 → 공개키·개인키 결정의 순서를 정확히 따라가야 합니다.

<Quiz
  questionNumber={13}
  question={"두 소수 p=3, q=11로 RSA 키를 생성하려 한다. 모듈러스(modulus) n과 오일러 파이 함수 값 phi(n)을 바르게 구한 것은?\n\n계산 순서: n = p x q, phi(n) = (p-1) x (q-1)"}
  options={['n=33, phi(n)=20', 'n=33, phi(n)=22', 'n=30, phi(n)=20', 'n=33, phi(n)=8']}
  correctAnswer={1}
  explanation="모듈러스는 두 소수를 곱한 값이므로 n = 3 곱하기 11 = 33이다. 오일러 파이 함수 phi(n)은 두 소수에서 각각 1을 뺀 값을 곱해 구하므로 phi(n) = (3-1) 곱하기 (11-1) = 2 곱하기 10 = 20이다. 이후 공개키 지수 e는 1보다 크고 phi(n)인 20보다 작으면서 20과 서로소인 값 중에서 고르는데, 여기서는 e=3을 흔히 사용한다. 이 n=33, phi(n)=20이라는 두 값이 이어지는 e, d 계산의 기초가 된다."
  optionExplanations={['정답이다. n=3×11=33이고 phi(n)=(3-1)×(11-1)=2×10=20이므로 두 값 모두 맞다.', 'phi(n)을 (p-1)×(q-1)이 아니라 (p+1)×(q+1)이나 다른 방식으로 잘못 계산했을 때 나올 수 있는 값이다.', 'n을 p×q가 아니라 p+q×(q-1) 같은 다른 계산으로 잘못 구했을 때 나올 수 있는 값이다.', 'phi(n)을 p-1과 q-1의 곱이 아니라 q-1 하나만으로 잘못 계산했을 때 나올 수 있는 값이다.']}
/>

> **출제 포인트:** 13번 문제에서 구한 n=33, phi(n)=20을 이어받아, 공개키 지수 e가 주어졌을 때 개인키 지수 d를 구하는 문제입니다. d는 e와 곱했을 때 phi(n)으로 나눈 나머지가 1이 되는 값입니다.

<Quiz
  questionNumber={14}
  question={"13번 문제와 같은 RSA 키(n=33, phi(n)=20)에서 공개키 지수를 e=3으로 정했다. e와 d를 곱한 값을 phi(n)인 20으로 나눈 나머지가 1이 되도록 하는 개인키 지수 d의 값은? (d는 1보다 크고 phi(n)보다 작은 정수)\n\n조건: (e x d) mod phi(n) = 1, 즉 (3 x d) mod 20 = 1"}
  options={['d=5', 'd=7', 'd=9', 'd=11']}
  correctAnswer={2}
  explanation="조건은 3과 d를 곱한 값을 20으로 나눈 나머지가 1이 되는 d를 찾는 것이다. d=7을 대입하면 3 곱하기 7은 21이고, 21을 20으로 나눈 나머지는 1이므로 조건을 만족한다. 이렇게 구한 d=7이 이 키 쌍의 개인키 지수가 되며, 공개키는 (e=3, n=33), 개인키는 (d=7, n=33)이 되어 이후 평문을 m 제곱 e 승 mod n으로 암호화하고, 암호문을 c 제곱 d 승 mod n으로 복호화하는 데 각각 사용된다."
  optionExplanations={['3×5=15이고 15를 20으로 나눈 나머지는 15이므로 조건을 만족하지 않는다.', '정답이다. 3×7=21이고 21을 20으로 나눈 나머지는 1이므로 조건을 만족하는 d는 7이다.', '3×9=27이고 27을 20으로 나눈 나머지는 7이므로 조건을 만족하지 않는다.', '3×11=33이고 33을 20으로 나눈 나머지는 13이므로 조건을 만족하지 않는다.']}
/>

> **출제 포인트:** 대칭키·공개키 알고리즘의 수학적 기반이 되는 어려운 문제(소인수분해, 이산로그)와 타원곡선암호(ECC)의 실무적 장점을 연결하는 문제입니다.

<Quiz
  questionNumber={15}
  question="공개키 암호 알고리즘의 수학적 기반에 대한 설명으로 옳지 않은 것은?"
  options={['RSA의 안전성은 큰 합성수를 소인수분해하기 어렵다는 문제에 기반한다', '디피-헬만(Diffie-Hellman) 키 교환은 이산로그 문제를 풀기 어렵다는 점에 기반한다', 'ECC(타원곡선암호, Elliptic Curve Cryptography)는 타원곡선 위에서 정의된 이산로그 문제의 어려움에 기반하며, RSA와 비슷한 안전성을 훨씬 짧은 키 길이로 얻을 수 있다', 'ECC는 수학적 기반이 RSA와 동일한 소인수분해 문제이므로 RSA를 대체할 실질적 이유가 없다']}
  correctAnswer={4}
  explanation="ECC는 RSA와 전혀 다른 수학적 기반인 타원곡선 이산로그 문제(ECDLP)에 기반한다. 이 문제는 같은 키 길이에서 소인수분해 문제보다 훨씬 풀기 어려운 것으로 알려져 있어, ECC는 RSA와 비슷한 안전성을 훨씬 짧은 키 길이로 달성할 수 있다는 실질적인 이점이 있다. 이 때문에 연산 자원이 제한적인 모바일 기기나 IoT(사물인터넷) 기기에서는 ECC가 RSA를 대체하는 경우가 많다."
  optionExplanations={['RSA의 안전성이 소인수분해의 어려움에 기반한다는 설명은 옳다.', '디피-헬만 키 교환이 이산로그 문제의 어려움에 기반한다는 설명은 옳다.', '타원곡선 이산로그 문제에 기반해 짧은 키로도 강한 안전성을 얻는다는 설명은 ECC의 실제 특징이므로 옳다.', '정답이다. ECC는 RSA와 다른 수학적 기반인 타원곡선 이산로그 문제에 기반하며, 짧은 키로 비슷한 안전성을 얻을 수 있다는 실질적 대체 이유가 있으므로 이 서술은 옳지 않다.']}
/>

## 해시함수·전자서명·PKI 실무 (16–20)

> **출제 포인트:** 해시함수가 갖추어야 할 핵심 성질인 충돌 저항성·역상 저항성을 구분하는 문제입니다.

<Quiz
  questionNumber={16}
  question="암호학적 해시함수가 갖추어야 할 성질에 대한 설명으로 옳지 않은 것은?"
  options={['역상 저항성(Preimage Resistance)은 주어진 해시값으로부터 그 값을 만들어 낸 원래 입력을 찾아내기 어려워야 한다는 성질이다', '충돌 저항성(Collision Resistance)은 서로 다른 두 입력이 같은 해시값을 만들어 내는 경우를 찾기 어려워야 한다는 성질이다', '해시함수는 입력의 길이와 관계없이 항상 고정된 길이의 출력값을 낸다', '해시함수는 원래 입력을 복원할 수 있도록 설계되어 있어 암호화와 마찬가지로 복호화 키가 존재한다']}
  correctAnswer={4}
  explanation="해시함수는 한쪽 방향으로만 계산이 가능한 일방향 함수(one-way function)로 설계된다. 즉 입력에서 해시값을 계산하기는 쉽지만, 해시값만 가지고 원래 입력을 복원하는 것은 계산적으로 매우 어렵도록 만들어져 있다. 암호화·복호화처럼 키를 이용해 원래 값을 되돌리는 개념 자체가 해시함수에는 존재하지 않으며, 이 점이 해시함수와 암호 알고리즘을 구분하는 근본적인 차이다."
  optionExplanations={['해시값으로부터 원래 입력을 찾기 어려워야 한다는 것은 역상 저항성의 정의와 정확히 일치하므로 옳다.', '서로 다른 입력이 같은 해시값을 내는 경우를 찾기 어려워야 한다는 것은 충돌 저항성의 정의와 정확히 일치하므로 옳다.', '입력 길이와 무관하게 고정 길이 출력을 낸다는 것은 해시함수의 기본 특성이므로 옳다.', '정답이다. 해시함수는 복호화 키가 존재하지 않는 일방향 함수로 설계되며, 원래 입력을 복원하도록 설계되었다는 서술은 해시함수의 정의와 정반대다.']}
/>

> **출제 포인트:** 전자서명 생성·검증 절차에서 개인키와 공개키 중 어느 쪽이 어느 단계에 쓰이는지 확인합니다. 암호화와 반대 방향으로 키를 사용한다는 점이 핵심입니다.

<Quiz
  questionNumber={17}
  question="전자서명의 생성과 검증 절차에 대한 설명으로 옳지 않은 것은?"
  options={['서명자는 원문의 해시값을 자신의 개인키로 암호화해 서명값을 만든다', '검증자는 수신한 서명값을 서명자의 공개키로 복호화해 얻은 해시값과, 자신이 원문으로 직접 계산한 해시값을 비교한다', '두 해시값이 일치하면 원문이 위변조되지 않았고 그 서명이 해당 개인키 소유자에 의해 생성되었음을 확인할 수 있다', '전자서명은 기밀성을 확보하기 위한 기술이므로, 서명이 붙은 문서는 제3자가 내용을 열람할 수 없다']}
  correctAnswer={4}
  explanation="전자서명이 보장하는 것은 서명자의 신원 확인(인증)과 문서의 위변조 여부 확인(무결성)이지, 문서 내용을 제3자로부터 숨기는 기밀성이 아니다. 서명이 붙은 문서라도 암호화되지 않았다면 문서 자체는 누구나 읽을 수 있으며, 내용을 숨기려면 별도로 암호화를 적용해야 한다. 전자서명과 암호화는 목적이 다른 별개의 기술이라는 점을 혼동해서는 안 된다."
  optionExplanations={['원문의 해시값을 개인키로 암호화해 서명을 만든다는 것은 전자서명 생성 절차의 정의와 정확히 일치하므로 옳다.', '서명값을 공개키로 복호화한 값과 직접 계산한 해시값을 비교한다는 것은 전자서명 검증 절차의 정의와 정확히 일치하므로 옳다.', '두 해시값이 일치하면 무결성과 서명자 인증을 함께 확인할 수 있다는 설명은 옳다.', '정답이다. 전자서명은 인증과 무결성을 보장하는 기술이며 기밀성과는 무관하므로, 제3자가 열람할 수 없다는 서술은 옳지 않다.']}
/>

> **출제 포인트:** PKI(공개키 기반구조)에서 CA(인증기관)와 RA(등록기관)의 역할 분담을 확인합니다.

<Quiz
  questionNumber={18}
  question="PKI(Public Key Infrastructure, 공개키 기반구조)의 구성요소인 CA(Certificate Authority, 인증기관)와 RA(Registration Authority, 등록기관)에 대한 설명으로 옳지 않은 것은?"
  options={['CA는 신청자의 신원과 공개키를 묶어 인증서를 발급하고 그 인증서에 자신의 서명을 남긴다', 'RA는 인증서 발급 신청자의 신원을 확인하는 등록 업무를 대행해 CA의 부담을 줄이는 역할을 한다', 'CA는 자신이 발급한 인증서에 대해서만 서명하며, 폐지된 인증서 목록을 관리하는 책임은 갖지 않는다', '여러 지역에 흩어진 신청자를 상대할 때, CA 하나가 모든 신원 확인을 직접 수행하는 대신 지역별 RA에 신원 확인을 위임할 수 있다']}
  correctAnswer={3}
  explanation="CA는 인증서를 발급하는 역할과 함께, 유효기간이 남았지만 개인키 유출이나 소속 변경 등의 이유로 더 이상 신뢰할 수 없게 된 인증서를 폐지 목록(CRL, Certificate Revocation List)으로 관리하고 공개할 책임도 함께 가진다. 발급만 하고 폐지 관리는 책임지지 않는다면, 유출된 개인키로 만든 서명을 검증자가 걸러낼 방법이 없어 PKI 체계 전체의 신뢰가 무너진다."
  optionExplanations={['신원과 공개키를 묶어 인증서를 발급하고 서명을 남긴다는 것은 CA의 핵심 역할이므로 옳다.', '신원 확인 업무를 대행해 CA의 부담을 줄인다는 것은 RA의 정의와 일치하므로 옳다.', '정답이다. CA는 인증서 발급뿐 아니라 폐지 목록 관리 책임도 함께 가지므로, 그 책임이 없다는 서술은 옳지 않다.', '지역별 RA에 신원 확인을 위임해 CA의 업무를 분산할 수 있다는 것은 PKI의 실제 운영 방식이므로 옳다.']}
/>

> **출제 포인트:** 인증서 폐지 확인 방법인 CRL(인증서 폐지 목록)과 OCSP(온라인 인증서 상태 프로토콜)의 차이를 확인합니다. 핵심은 확인 시점과 실시간성의 차이입니다.

<Quiz
  questionNumber={19}
  question="인증서 폐지 여부를 확인하는 CRL과 OCSP에 대한 설명으로 옳지 않은 것은?"
  options={['CRL은 CA가 정기적으로 발행하는 폐지된 인증서 일련번호 목록으로, 검증자는 이 목록을 내려받아 대조한다', 'OCSP(Online Certificate Status Protocol)는 검증자가 특정 인증서 하나의 상태를 실시간으로 CA(또는 그 대리 서버)에 질의해 즉시 응답받는 방식이다', 'CRL 방식은 목록이 갱신되기 전 짧은 기간 동안 이미 폐지된 인증서가 여전히 유효한 것으로 오판될 여지가 있다', 'OCSP는 매번 실시간으로 조회해야 하므로 CRL보다 항상 느리고 CA 서버 부하도 항상 더 크다']}
  correctAnswer={4}
  explanation="OCSP는 인증서 하나에 대한 상태만 질의하므로 응답이 매우 가볍고, 실무에서는 오히려 대용량 CRL 파일 전체를 내려받는 것보다 빠르게 동작하는 경우가 많다. CA 서버 부하 측면에서도 OCSP 스테이플링(OCSP Stapling)처럼 서버가 미리 받아 둔 응답을 함께 제공하는 방식으로 부하를 줄이는 기법이 널리 쓰인다. 따라서 OCSP가 CRL보다 항상 느리고 부하가 크다고 단정하는 것은 옳지 않다."
  optionExplanations={['CA가 정기적으로 발행하는 폐지 목록을 검증자가 내려받아 대조한다는 것은 CRL의 정의와 정확히 일치하므로 옳다.', '인증서 하나의 상태를 실시간으로 질의해 즉시 응답받는다는 것은 OCSP의 정의와 정확히 일치하므로 옳다.', '목록이 갱신되기 전에는 이미 폐지된 인증서가 유효한 것으로 오판될 수 있다는 것은 CRL의 실제 한계이므로 옳다.', '정답이다. OCSP는 인증서 하나만 질의하므로 오히려 대용량 CRL보다 빠를 수 있고, 스테이플링 등으로 부하를 줄일 수 있어 항상 느리고 부하가 크다는 단정은 옳지 않다.']}
/>

> **출제 포인트:** SHA 계열 해시함수와 MD5의 안전성 차이를 확인합니다. 출제기준에서는 이미 충돌이 발견되어 폐기된 알고리즘과 현재 권고되는 알고리즘을 구분하는 문제가 자주 나옵니다.

<Quiz
  questionNumber={20}
  question="해시 알고리즘의 안전성에 대한 설명으로 옳지 않은 것은?"
  options={['MD5는 이미 충돌 사례가 실제로 발견되어 무결성 검증 등 보안 목적으로는 더 이상 권고되지 않는다', 'SHA-1도 실제 충돌 공격이 성공적으로 시연되어 현재는 보안 목적의 신규 사용이 권고되지 않는다', 'SHA-256, SHA-3 계열은 현재까지 실용적인 충돌 공격이 알려지지 않아 보안 목적으로 널리 권고된다', 'MD5는 출력 해시값의 길이가 SHA-256보다 길기 때문에 충돌 저항성이 더 뛰어나다']}
  correctAnswer={4}
  explanation="MD5의 출력 길이는 128비트로 SHA-256의 256비트보다 오히려 짧으며, 안전성이 취약해진 이유도 출력 길이 자체보다는 내부 압축 함수의 설계에서 실제 충돌을 만들어 내는 방법이 발견되었기 때문이다. 출력 길이가 길다고 항상 충돌 저항성이 좋은 것은 아니지만, 이 문제에서는 애초에 MD5의 출력 길이가 더 짧다는 사실관계부터 틀렸으므로 옳지 않은 설명이다."
  optionExplanations={['MD5의 충돌 사례가 실제로 발견되어 권고되지 않는다는 것은 사실이므로 옳다.', 'SHA-1도 실제 충돌 공격이 시연되어 신규 사용이 권고되지 않는다는 것은 사실이므로 옳다.', 'SHA-256과 SHA-3이 현재까지 실용적 충돌 공격 없이 널리 권고된다는 것은 사실이므로 옳다.', '정답이다. MD5의 출력 길이는 128비트로 SHA-256의 256비트보다 짧으며, 안전성 약화의 원인도 길이가 아니라 내부 설계의 결함이므로 이 서술은 옳지 않다.']}
/>

## 키 분배 프로토콜과 최신 암호기술 동향 (21–24)

> **출제 포인트:** 케르베로스(Kerberos)의 핵심 아이디어인 티켓 기반 인증을 확인합니다. 비밀번호를 반복해서 각 서버에 보내지 않고, 신뢰할 수 있는 제3자가 발급한 티켓으로 인증을 대신한다는 점이 핵심입니다.

<Quiz
  questionNumber={21}
  question="케르베로스(Kerberos) 인증 프로토콜에 대한 설명으로 옳지 않은 것은?"
  options={['사용자는 최초 한 번 키 분배 센터(KDC, Key Distribution Center)에 인증받아 티켓 발급용 티켓(TGT)을 받는다', '사용자는 이후 각 서비스에 접속할 때마다 자신의 비밀번호를 그 서비스 서버에 직접 전송해 재인증받는다', '서비스 티켓에는 유효 기간이 설정되어 있어, 기간이 지나면 다시 인증 절차를 거쳐야 한다', '케르베로스는 신뢰할 수 있는 제3자인 키 분배 센터를 통해 사용자와 서비스 간의 상호 인증을 지원한다']}
  correctAnswer={2}
  explanation="케르베로스의 핵심 이점은 사용자가 최초에 키 분배 센터에만 인증하면, 이후 각 서비스에 접속할 때는 비밀번호를 다시 서버에 전송하지 않고 키 분배 센터가 발급한 티켓을 제시하는 것만으로 인증이 이루어진다는 점이다. 비밀번호를 매번 여러 서버에 반복 전송하지 않아도 되므로, 비밀번호가 네트워크 여러 지점에 노출될 위험이 줄어든다. 이 사례처럼 각 서비스마다 비밀번호를 직접 보낸다면 케르베로스를 도입하는 의미가 사라진다."
  optionExplanations={['최초 한 번 KDC에 인증받아 TGT(Ticket Granting Ticket)를 받는다는 것은 케르베로스의 정의와 정확히 일치하므로 옳다.', '정답이다. 케르베로스의 핵심은 비밀번호를 각 서비스에 반복 전송하지 않고 티켓으로 인증을 대신하는 것이므로, 매번 비밀번호를 직접 전송한다는 서술은 옳지 않다.', '서비스 티켓에 유효 기간이 있어 기간 만료 후 재인증이 필요하다는 것은 케르베로스의 실제 동작이므로 옳다.', '신뢰할 수 있는 제3자를 통해 상호 인증을 지원한다는 것은 케르베로스의 설계 목적과 일치하므로 옳다.']}
/>

> **출제 포인트:** 키 관리 생명주기(생성-배포-저장-교체-폐기)의 단계 순서를 확인합니다. 키는 한 번 만들면 영구히 쓰는 것이 아니라 정기적으로 교체해야 한다는 점이 핵심입니다.

<Quiz
  questionNumber={22}
  question="암호키 관리 생명주기에 대한 설명으로 옳지 않은 것은?"
  options={['키 생성 단계에서는 충분한 무작위성을 갖춘 난수 생성기를 사용해 예측 불가능한 키를 만들어야 한다', '키 배포 단계에서는 키 자체가 노출되지 않도록 안전한 채널이나 별도의 키 암호화 키(KEK)를 사용해 전달한다', '키는 한 번 안전하게 생성해 배포하면 그 이후로는 영구히 동일한 값을 그대로 사용하는 것이 권장된다', '키 폐기 단계에서는 더 이상 사용하지 않는 키가 복구되어 악용되지 않도록 완전히 삭제해야 한다']}
  correctAnswer={3}
  explanation="키를 영구히 같은 값으로 계속 사용하면, 그 키로 암호화된 데이터가 누적될수록 분석에 활용할 수 있는 자료가 늘어나 공격자가 키를 추정하거나 취약점을 찾아낼 기회도 함께 늘어난다. 그래서 키 관리 생명주기에는 정기적으로 키를 교체하는 키 순환(Key Rotation) 단계가 포함되며, 특정 키가 유출된 것으로 의심되면 즉시 교체해야 한다. 영구히 동일한 키를 쓰는 것이 권장된다는 서술은 키 관리의 기본 원칙과 반대다."
  optionExplanations={['무작위성을 갖춘 난수 생성기로 예측 불가능한 키를 만들어야 한다는 것은 키 생성 단계의 기본 요건이므로 옳다.', '안전한 채널이나 키 암호화 키를 통해 배포한다는 것은 키 배포 단계의 표준 절차이므로 옳다.', '정답이다. 키는 정기적으로 교체(순환)해야 하며, 영구히 동일한 값을 쓰는 것이 권장된다는 서술은 키 관리 생명주기의 기본 원칙과 반대다.', '더 이상 쓰지 않는 키를 완전히 삭제해 복구되지 않게 해야 한다는 것은 키 폐기 단계의 기본 요건이므로 옳다.']}
/>

> **출제 포인트:** 최신 암호기술 동향인 양자내성암호(PQC)가 왜 필요한지 확인합니다. 핵심은 양자컴퓨터가 기존 공개키 암호의 수학적 기반을 무력화할 수 있다는 우려입니다.

<Quiz
  questionNumber={23}
  question="양자내성암호(PQC, Post-Quantum Cryptography)에 대한 설명으로 옳지 않은 것은?"
  options={['PQC는 충분한 성능의 양자컴퓨터가 등장하더라도 안전성이 유지되도록 설계된 암호 알고리즘을 가리킨다', 'PQC가 주목받는 이유는 쇼어 알고리즘(Shor algorithm) 같은 양자 알고리즘이 소인수분해·이산로그 문제를 기존 컴퓨터보다 훨씬 빠르게 풀 수 있다는 이론적 결과 때문이다', 'PQC는 대칭키 암호에는 적용할 필요가 없으며 오직 대칭키 알고리즘의 안전성 강화만을 목표로 한다', 'PQC 연구는 격자 기반, 코드 기반 등 양자 알고리즘으로도 풀기 어려운 것으로 알려진 다른 수학적 문제에 기반한 알고리즘을 개발하는 방향으로 진행된다']}
  correctAnswer={3}
  explanation="양자컴퓨터의 위협은 주로 소인수분해·이산로그 문제에 기반한 RSA·디피-헬만·ECC 같은 공개키 암호에 집중되며, 이런 이유로 PQC 연구도 주로 공개키 암호를 대체할 새로운 알고리즘을 찾는 데 초점을 맞춘다. 대칭키 암호도 양자 알고리즘(그로버 알고리즘)의 영향을 아예 받지 않는 것은 아니지만, 그 영향은 키 길이를 늘리는 것만으로 상당 부분 완화할 수 있어 공개키 암호만큼 근본적인 재설계가 필요하지는 않다고 여겨진다. PQC를 대칭키 암호만의 강화로 한정하는 서술은 옳지 않다."
  optionExplanations={['양자컴퓨터 등장 이후에도 안전성이 유지되도록 설계된 암호를 가리킨다는 정의는 옳다.', '쇼어 알고리즘이 소인수분해·이산로그를 빠르게 풀 수 있다는 이론적 위협이 PQC 연구의 배경이라는 설명은 옳다.', '정답이다. PQC 연구의 핵심 대상은 양자 알고리즘에 특히 취약한 공개키 암호이며, 대칭키 암호만을 목표로 한다는 서술은 옳지 않다.', '격자 기반, 코드 기반 등 다른 수학적 난제에 기반한 알고리즘을 개발하는 방향이라는 설명은 실제 PQC 연구 동향과 일치하므로 옳다.']}
/>

> **출제 포인트:** 동형암호(Homomorphic Encryption)가 해결하는 문제를 확인합니다. 핵심은 데이터를 복호화하지 않은 암호화된 상태 그대로 연산할 수 있다는 점입니다.

<Quiz
  questionNumber={24}
  question="동형암호(Homomorphic Encryption)에 대한 설명으로 가장 적절한 것은?"
  options={['동형암호는 암호화된 데이터를 복호화하지 않은 상태에서 연산을 수행하고, 그 결과를 복호화하면 평문에 같은 연산을 한 것과 동일한 값을 얻을 수 있게 하는 암호 기술이다', '동형암호는 여러 서버에 흩어진 암호키를 하나로 통합해 관리하는 키 관리 기술이다', '동형암호는 인증서의 폐지 여부를 실시간으로 조회하는 프로토콜의 한 종류다', '동형암호는 대칭키 암호의 운용 모드 중 하나로, 블록 간 연쇄 없이 병렬로 처리할 수 있게 하는 방식이다']}
  correctAnswer={1}
  explanation="동형암호는 암호화된 상태의 데이터에 대해 덧셈이나 곱셈 같은 연산을 직접 수행할 수 있고, 그 결과를 복호화하면 평문 상태에서 같은 연산을 수행한 결과와 동일한 값이 나오도록 설계된 암호 기술이다. 이 성질 덕분에 민감한 데이터를 외부 클라우드 서버에 암호화된 채로 맡기고, 그 서버가 원본 내용을 전혀 알지 못한 상태에서도 필요한 연산(통계 집계, 검색 등)을 대신 수행하게 할 수 있다. 이는 클라우드 환경에서 데이터 기밀성을 유지하면서도 위탁 연산을 가능하게 하는 최신 암호기술 동향으로 자주 언급된다."
  optionExplanations={['정답이다. 복호화 없이 암호화된 상태로 연산하고 그 결과를 복호화하면 평문 연산 결과와 같아지는 성질이 동형암호의 정의와 정확히 일치한다.', '여러 키를 통합 관리하는 것은 키 관리 시스템의 역할이며 동형암호의 정의와 무관하다.', '인증서 폐지 여부를 실시간으로 조회하는 것은 OCSP의 정의이며 동형암호와는 전혀 다른 기술이다.', '동형암호는 대칭키 운용 모드가 아니라 암호화된 데이터에 대한 연산을 가능하게 하는 별개의 암호 방식이다.']}
/>

## 참고 자료

- [itwiki - 정보보안기사 필기 출제기준](https://itwiki.kr/w/정보보안기사_필기_출제기준)
- [공공데이터포털 - 한국방송통신전파진흥원_국가기술자격검정 정보보안 종목 출제기준](https://www.data.go.kr/data/15140079/fileData.do)
- [한국방송통신전파진흥원(cq.or.kr) - 정보보안기사 자격정보](https://www.cq.or.kr/qh_quagm01_020.do)
