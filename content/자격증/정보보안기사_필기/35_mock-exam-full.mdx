---
title: '35. 정보보안기사 필기 통합 모의고사'
description: '1과목 시스템보안부터 5과목 정보보안관리 및 법규까지 전 범위를 고르게 다룬 25문항으로 구성한 통합 모의고사, 문항마다 해설과 보기별 오답 이유를 정리'
---

<Callout type="warning">
  **이 편의 문제는 실제 기출을 그대로 복제한 것이 아닙니다.** 06편부터 30편까지 다룬 5과목(시스템보안·네트워크보안·애플리케이션보안·정보보안일반·정보보안관리 및 법규) 전체 범위에서, 31편부터 34편까지의 과목별 기출·예상문제와는 다른 새로운 상황·숫자·보기로 재구성한 문항입니다. 5과목을 한 회차 시험처럼 고르게 섞어 전체 범위를 균형 있게 점검하도록 구성했습니다.
</Callout>

## 문항 구성표

| 과목 | 관련 편 | 문항 번호 | 문항 수 |
|---|---|---|---|
| 1과목 시스템보안 | 06–10 | 1–5 | 5 |
| 2과목 네트워크보안 | 11–15 | 6–10 | 5 |
| 3과목 애플리케이션보안 | 16–20 | 11–15 | 5 |
| 4과목 정보보안일반 | 21–25 | 16–20 | 5 |
| 5과목 정보보안관리 및 법규 | 26–30 | 21–25 | 5 |
| **합계** | – | – | **25** |

계산 문항(서브넷팅·RSA)은 반드시 손으로 직접 계산해 보고 답을 확정한 뒤 해설과 맞춰 보십시오. 과목별로 5문항씩 고르게 배치했으므로, 특정 과목에서 유독 오답이 몰린다면 그 과목의 본론 편(표에 적힌 관련 편)을 먼저 복습하는 것이 효율적입니다.

## 1과목 시스템보안 (1–5)

> **출제 포인트:** 백업 전략인 전체·증분·차등 백업의 백업 용량 증가 패턴과 복구 절차 차이를 구분하는 문제입니다.

<Quiz
  questionNumber={1}
  question="백업 전략인 전체 백업, 증분 백업, 차등 백업에 대한 설명으로 옳지 않은 것은?"
  options={['전체 백업은 매번 모든 데이터를 백업하므로 복구 시 가장 단순하지만 백업에 걸리는 시간과 저장 공간이 가장 크다', '증분 백업은 직전 백업(전체 또는 증분) 이후 변경된 데이터만 백업하므로 백업 용량은 작지만 복구 시 여러 백업본을 순서대로 적용해야 한다', '차등 백업은 마지막 전체 백업 이후 변경된 모든 데이터를 매번 백업하므로 복구 시 전체 백업본과 가장 최근 차등 백업본 하나만 있으면 된다', '증분 백업과 차등 백업은 항상 같은 크기의 백업 파일을 생성하므로 두 방식을 구분할 실익이 없다']}
  correctAnswer={4}
  explanation="증분 백업은 직전 백업 이후 변경분만 담아 시간이 지날수록 각 백업본의 크기가 상대적으로 작게 유지되는 반면, 차등 백업은 마지막 전체 백업 이후 누적된 변경분을 매번 백업하므로 시간이 지날수록 파일 크기가 점점 커진다. 두 방식의 백업 크기 증가 패턴과 복구 절차가 서로 다르므로 구분할 실익이 분명히 있다."
  optionExplanations={['전체 백업의 정의와 특징에 대한 정확한 설명이므로 옳다.', '증분 백업의 정의와 복구 절차에 대한 정확한 설명이므로 옳다.', '차등 백업의 정의와 복구 절차에 대한 정확한 설명이므로 옳다.', '정답이다. 두 방식은 백업 크기 증가 패턴과 복구 절차가 서로 달라 구분할 실익이 분명히 있다.']}
/>

> **출제 포인트:** 파일 무결성 점검 도구(Tripwire·AIDE)가 해시값 비교 방식으로 변조를 탐지한다는 원리를 확인하는 문제입니다.

<Quiz
  questionNumber={2}
  question="파일 무결성 점검 도구인 Tripwire와 AIDE의 동작 원리로 가장 적절한 것은?"
  options={['네트워크 트래픽을 실시간으로 감시해 침입 패턴을 탐지한다', '주요 파일의 해시값을 기준 시점에 저장해 두고, 이후 주기적으로 다시 계산한 해시값과 비교해 변경 여부를 탐지한다', '방화벽 규칙을 자동으로 최적화해 트래픽 처리 속도를 높인다', '사용자의 로그인 시도 횟수를 제한해 무차별 대입 공격을 차단한다']}
  correctAnswer={2}
  explanation="Tripwire와 AIDE 같은 파일 무결성 점검 도구는 보호 대상 파일의 해시값을 기준선(baseline)으로 저장해 두고, 이후 주기적으로 파일의 해시값을 다시 계산해 기준값과 비교함으로써 파일이 변조되었는지를 탐지하는 방식으로 동작한다."
  optionExplanations={['네트워크 트래픽 실시간 감시는 IDS(침입탐지시스템)의 역할이며 파일 무결성 점검 도구의 동작 원리가 아니다.', '정답이다. 해시값을 기준선으로 저장해 두고 이후 값과 비교하는 방식이 이 도구들의 핵심 원리다.', '방화벽 규칙 최적화는 이 도구들의 기능과 무관하다.', '로그인 시도 횟수 제한은 계정 잠금 정책의 영역이며 파일 무결성 점검과 무관하다.']}
/>

> **출제 포인트:** 루트킷이 자신의 존재를 은폐하도록 설계된 악성코드라는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={3}
  question="루트킷(rootkit)에 대한 설명으로 옳지 않은 것은?"
  options={['루트킷은 시스템에 침투한 뒤 자신의 존재와 활동을 숨기기 위해 커널이나 시스템 도구의 동작을 조작하는 악성코드다', '루트킷이 설치되면 일반적인 프로세스 목록 조회 명령으로도 루트킷 관련 프로세스가 숨김없이 정상적으로 나타난다', '루트킷 탐지에는 신뢰할 수 있는 별도의 매체로 부팅해 파일 해시를 오프라인으로 비교하는 방법이 활용되기도 한다', '루트킷은 관리자 권한을 획득한 뒤 설치되는 경우가 많아, 권한 상승 공격과 함께 다뤄지는 경우가 흔하다']}
  correctAnswer={2}
  explanation="루트킷의 핵심 목적이 바로 자신의 존재를 숨기는 것이므로, 감염된 시스템 위에서 실행하는 일반적인 프로세스 목록 조회 명령 자체가 조작되어 루트킷 관련 프로세스가 나타나지 않도록 은폐되는 경우가 많다. 정상적으로 숨김없이 나타난다는 서술은 루트킷의 정의와 반대다."
  optionExplanations={['루트킷의 정의에 대한 정확한 설명이므로 옳다.', '정답이다. 루트킷은 자신의 존재를 숨기도록 설계되므로, 일반 조회 명령으로 숨김없이 드러난다는 서술은 정의와 반대다.', '오프라인 비교를 통한 루트킷 탐지 방법에 대한 정확한 설명이므로 옳다.', '루트킷이 권한 상승 공격과 함께 다뤄지는 경우가 흔하다는 설명은 실제 공격 패턴과 일치하므로 옳다.']}
/>

> **출제 포인트:** 윈도우 NTFS 권한 상속이 상위 폴더의 권한 변경에 따라 하위 항목에도 영향을 미친다는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={4}
  question="윈도우 NTFS 파일시스템의 권한 상속에 대한 설명으로 옳지 않은 것은?"
  options={['하위 폴더와 파일은 기본적으로 상위 폴더의 권한 설정을 상속받는다', '특정 파일에 대해 상속을 명시적으로 해제하고 개별 권한을 다시 설정할 수 있다', '상위 폴더의 권한을 변경하면 상속을 해제하지 않은 모든 하위 항목의 권한도 함께 바뀐다', 'NTFS 권한은 한 번 설정되면 상속 여부와 무관하게 이후 상위 폴더의 권한 변경에 전혀 영향을 받지 않는다']}
  correctAnswer={4}
  explanation="NTFS 권한 상속을 해제하지 않은 하위 항목은 상위 폴더의 권한 변경에 따라 함께 바뀌는 것이 상속의 기본 동작이다. 상속 여부와 무관하게 전혀 영향을 받지 않는다는 서술은 상속의 정의와 반대다."
  optionExplanations={['NTFS 권한 상속의 기본 동작에 대한 정확한 설명이므로 옳다.', '상속 해제와 개별 권한 재설정이 가능하다는 설명은 실제 NTFS 기능과 일치하므로 옳다.', '상위 폴더 권한 변경이 상속을 해제하지 않은 하위 항목에 반영된다는 설명은 옳다.', '정답이다. 상속을 해제하지 않았다면 상위 폴더의 권한 변경에 영향을 받는 것이 기본 동작이므로, 전혀 영향을 받지 않는다는 서술은 옳지 않다.']}
/>

> **출제 포인트:** 취약점 점검도구(OpenVAS·Nessus류)의 일반적인 활용 절차를 확인하는 문제입니다.

<Quiz
  questionNumber={5}
  question="서버 취약점 점검 도구인 OpenVAS·Nessus류 도구의 일반적인 활용 절차로 가장 적절한 것은?"
  options={['대상 시스템의 알려진 취약점 데이터베이스와 대조해 점검한 뒤, 발견된 취약점의 위험도에 따라 우선순위를 매겨 보고서를 생성한다', '대상 시스템에 백도어를 설치해 원격 접속 경로를 확보한다', '네트워크 트래픽을 암호화해 도청을 원천적으로 차단한다', '사용자 비밀번호를 무작위로 변경해 계정 탈취를 예방한다']}
  correctAnswer={1}
  explanation="취약점 점검도구는 대상 시스템을 스캔해 알려진 취약점 데이터베이스와 대조하고, 발견된 취약점을 심각도별로 분류해 우선순위가 매겨진 점검 보고서를 만들어 주는 것이 일반적인 활용 절차다."
  optionExplanations={['정답이다. 알려진 취약점 데이터베이스와 대조해 위험도별로 우선순위를 매긴 보고서를 만드는 것이 이 도구들의 핵심 기능이다.', '백도어 설치는 오히려 공격 기법에 해당하며 점검 도구의 정상적인 기능이 아니다.', '트래픽 암호화는 별도의 보안 프로토콜(TLS 등)의 역할이며 취약점 점검도구의 기능이 아니다.', '비밀번호 무작위 변경은 계정 관리 정책의 영역이며 취약점 점검도구의 활용 절차가 아니다.']}
/>

## 2과목 네트워크보안 (6–10)

> **출제 포인트:** /24 네트워크를 /26으로 서브넷팅했을 때 서브넷 개수와 서브넷당 사용 가능한 호스트 수를 손으로 직접 계산하는 문제입니다.

<Quiz
  questionNumber={6}
  question="192.168.10.0/24 네트워크를 /26으로 서브넷팅했을 때 생성되는 서브넷의 개수와 서브넷당 사용 가능한 호스트 수를 옳게 짝지은 것은? (네트워크 주소와 브로드캐스트 주소는 호스트 수에서 제외)"
  options={['4개 서브넷, 서브넷당 62개 호스트', '2개 서브넷, 서브넷당 128개 호스트', '8개 서브넷, 서브넷당 30개 호스트', '4개 서브넷, 서브넷당 64개 호스트']}
  correctAnswer={1}
  explanation="/24에서 /26으로 서브넷팅하면 호스트 비트 8개 중 2개를 서브넷 비트로 빌려오므로 2의 2제곱인 4개의 서브넷이 생성된다. 각 서브넷은 호스트 비트가 6개 남으므로 2의 6제곱인 64개의 주소를 가지며, 여기서 네트워크 주소와 브로드캐스트 주소 2개를 제외하면 실제 사용 가능한 호스트는 62개다."
  optionExplanations={['정답이다. 2비트를 빌리면 4개 서브넷이 생기고, 각 서브넷의 64개 주소에서 2개를 제외하면 62개의 호스트를 쓸 수 있다.', '/25로 서브넷팅했을 때에 가까운 값과 혼동한 경우로, /26 서브넷팅 결과와 맞지 않는다.', '/27로 서브넷팅했을 때 나오는 서브넷 개수(8개)와 다른 계산이 섞인 값이다.', '네트워크 주소와 브로드캐스트 주소를 제외하지 않고 전체 주소 개수(64개)를 그대로 쓴 값이다.']}
/>

> **출제 포인트:** ARP 스푸핑이 위조된 ARP 응답으로 피해자의 트래픽을 공격자에게 경유시키는 원리를 확인하는 문제입니다.

<Quiz
  questionNumber={7}
  question="스위칭 환경에서 공격자가 ARP 스푸핑(ARP Spoofing)을 이용해 스니핑을 시도하는 원리로 가장 적절한 것은?"
  options={['공격자가 위조된 ARP 응답을 보내 피해자의 ARP 테이블에 공격자의 MAC 주소를 게이트웨이 주소로 등록시켜, 피해자의 트래픽이 공격자를 거쳐 가도록 만든다', '공격자가 DNS 서버의 캐시에 허위 레코드를 주입해 도메인 이름을 다른 IP로 연결시킨다', '공격자가 TCP 3-way handshake의 초기 순서번호를 예측해 세션을 가로챈다', '공격자가 방화벽 규칙을 직접 수정해 특정 포트를 개방한다']}
  correctAnswer={1}
  explanation="ARP 스푸핑은 위조된 ARP 응답으로 피해자의 ARP 테이블을 조작해, 원래 게이트웨이의 MAC 주소 자리에 공격자의 MAC 주소를 등록시키는 공격이다. 이렇게 되면 피해자가 외부로 보내는 트래픽이 실제 게이트웨이가 아니라 공격자를 거쳐 가게 되어, 스위칭 환경에서도 트래픽을 가로채 스니핑할 수 있게 된다."
  optionExplanations={['정답이다. 위조된 ARP 응답으로 피해자의 ARP 테이블을 조작해 트래픽을 가로채는 것이 ARP 스푸핑의 핵심 원리다.', 'DNS 캐시에 허위 레코드를 주입하는 것은 DNS 스푸핑(캐시 포이즈닝)의 원리이며 ARP 스푸핑과는 다른 공격이다.', 'TCP 순서번호 예측을 통한 세션 가로채기는 TCP 세션 하이재킹의 원리이며 ARP 스푸핑과는 다른 공격이다.', '방화벽 규칙을 직접 수정하는 것은 관리자 권한이 필요한 별개의 행위이며 ARP 스푸핑의 원리와 무관하다.']}
/>

> **출제 포인트:** DNS 증폭 공격이 출발지 IP 위조와 오픈 리졸버를 이용해 작은 질의로 큰 트래픽을 유발하는 원리를 확인하는 문제입니다.

<Quiz
  questionNumber={8}
  question="DNS 증폭(Amplification) 공격의 원리로 가장 적절한 것은?"
  options={['공격자가 출발지 IP를 피해자의 IP로 위조한 작은 질의를 다수의 오픈 DNS 리졸버에 보내, 훨씬 큰 응답이 피해자에게 집중되도록 만든다', '공격자가 피해자의 DNS 서버 설정 파일을 직접 변조해 서비스를 중단시킨다', '공격자가 피해자의 웹 서버에 정상적인 로그인 요청을 반복해서 보내 계정을 잠근다', '공격자가 피해자의 라우팅 테이블에 잘못된 경로 정보를 주입해 트래픽을 우회시킨다']}
  correctAnswer={1}
  explanation="DNS 증폭 공격은 공격자가 출발지 IP 주소를 피해자의 주소로 위조한 뒤, 이 위조된 주소로 크기가 작은 DNS 질의를 다수의 오픈 리졸버에 보낸다. DNS 응답은 질의보다 훨씬 크기가 커서, 여러 리졸버가 동시에 큰 응답을 피해자에게 보내면 소량의 공격 트래픽으로 훨씬 큰 규모의 트래픽을 피해자에게 집중시킬 수 있다."
  optionExplanations={['정답이다. 출발지 IP 위조와 오픈 리졸버의 큰 응답을 이용해 트래픽을 증폭시키는 것이 이 공격의 핵심 원리다.', '설정 파일을 직접 변조하려면 이미 관리자 권한을 획득해야 하며, 이는 증폭 공격의 원리와는 다른 접근이다.', '반복적인 로그인 요청으로 계정을 잠그는 것은 계정 잠금을 노린 별개의 공격이며 트래픽 증폭과는 무관하다.', '라우팅 테이블 조작은 경로 하이재킹에 가까운 별개의 공격이며 DNS 증폭의 원리와는 다르다.']}
/>

> **출제 포인트:** IPSec의 AH와 ESP를 함께 사용할 수 있다는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={9}
  question="IPSec의 AH(Authentication Header)와 ESP(Encapsulating Security Payload)에 대한 설명으로 옳지 않은 것은?"
  options={['AH는 데이터의 무결성과 발신지 인증을 제공하지만 기밀성(암호화)은 제공하지 않는다', 'ESP는 데이터의 암호화를 통한 기밀성과 함께 무결성 인증 기능도 함께 제공할 수 있다', '터널 모드는 원본 IP 헤더까지 포함해 전체 패킷을 캡슐화하므로 네트워크 간 게이트웨이 구간 보호에 흔히 쓰인다', 'AH와 ESP는 각각 사용하는 목적이 완전히 달라 하나의 IPSec 연결에서 함께 사용할 수 없다']}
  correctAnswer={4}
  explanation="IPSec은 필요에 따라 AH와 ESP를 함께 적용할 수 있도록 설계되어 있다. 예를 들어 ESP로 기밀성과 무결성을 확보하면서 추가로 AH를 적용해 IP 헤더 수준의 인증까지 강화하는 구성도 가능하다. 완전히 함께 사용할 수 없다는 서술은 옳지 않다."
  optionExplanations={['AH의 기능과 한계에 대한 정확한 설명이므로 옳다.', 'ESP가 기밀성과 무결성을 함께 제공할 수 있다는 설명은 옳다.', '터널 모드의 동작과 활용처에 대한 정확한 설명이므로 옳다.', '정답이다. AH와 ESP는 하나의 IPSec 연결에서 함께 적용할 수 있으므로, 함께 사용할 수 없다는 서술은 옳지 않다.']}
/>

> **출제 포인트:** 제로 트러스트가 네트워크 위치와 무관하게 모든 접근을 매번 검증한다는 핵심 원칙을 확인하는 문제입니다.

<Quiz
  questionNumber={10}
  question="제로 트러스트(Zero Trust) 네트워크 아키텍처의 핵심 원칙으로 가장 적절한 것은?"
  options={['한 번 사내망에 접속해 인증에 성공하면 이후 모든 내부 자원에 대해 별도 검증 없이 신뢰한다', '네트워크의 위치(사내망인지 외부망인지)와 무관하게 모든 접근 요청을 매번 검증하고 최소 권한만 부여한다', '내부 네트워크 구간은 이미 안전하다고 가정하고 외부 경계에서만 방화벽으로 통제한다', '한 번 발급된 인증서는 유효기간과 무관하게 영구적으로 신뢰한다']}
  correctAnswer={2}
  explanation="제로 트러스트의 핵심은 아무것도 기본적으로 신뢰하지 않는다는 원칙으로, 사내망이든 외부망이든 네트워크 위치와 무관하게 모든 접근 요청을 매번 검증하고 필요한 최소한의 권한만 부여한다. 전통적인 경계 방어처럼 내부망을 안전하다고 가정하지 않는다는 점이 기존 모델과의 핵심 차이다."
  optionExplanations={['한 번 인증되면 이후 검증 없이 신뢰하는 것은 전통적인 경계 방어 모델의 특징이며 제로 트러스트의 원칙과 반대다.', '정답이다. 위치와 무관하게 매번 검증하고 최소 권한만 부여하는 것이 제로 트러스트의 핵심 원칙이다.', '내부망을 안전하다고 가정하는 것은 제로 트러스트가 극복하려는 전통적인 경계 방어 모델의 전제다.', '인증서도 유효기간과 신뢰 조건에 따라 재검증이 필요하며, 영구적으로 신뢰한다는 서술은 제로 트러스트 원칙과 반대다.']}
/>

## 3과목 애플리케이션보안 (11–15)

> **출제 포인트:** SQL 인젝션·XSS·CSRF가 서로 다른 계층과 메커니즘을 이용하는 별개의 공격이라는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={11}
  question="웹 공격 기법인 SQL 인젝션, XSS(Cross-Site Scripting), CSRF(Cross-Site Request Forgery)에 대한 설명으로 옳지 않은 것은?"
  options={['SQL 인젝션은 입력값 검증이 미흡한 자리에 조작된 SQL 구문을 삽입해 데이터베이스 질의 결과를 공격자가 원하는 대로 바꾸는 공격이다', 'XSS는 공격자가 삽입한 스크립트가 다른 사용자의 브라우저에서 실행되도록 만들어 세션 정보를 탈취하거나 화면을 조작하는 공격이다', 'CSRF는 로그인된 사용자의 브라우저가 사용자 의도와 무관하게 위조된 요청을 정상 서버로 보내도록 유도하는 공격이다', '세 공격은 모두 서버 측 데이터베이스에 직접 조작된 쿼리를 실행시켜야만 성립하는 동일한 메커니즘의 공격이다']}
  correctAnswer={4}
  explanation="세 공격은 서로 다른 계층과 메커니즘을 이용한다. SQL 인젝션은 데이터베이스 질의 조작, XSS는 브라우저에서 실행되는 스크립트 삽입, CSRF는 인증된 세션을 이용한 위조 요청 전송으로 공격 방식이 전혀 다르며, 데이터베이스 쿼리 조작이 공통 요건이 아니다."
  optionExplanations={['SQL 인젝션의 정의에 대한 정확한 설명이므로 옳다.', 'XSS의 정의에 대한 정확한 설명이므로 옳다.', 'CSRF의 정의에 대한 정확한 설명이므로 옳다.', '정답이다. 세 공격은 서로 다른 메커니즘을 이용하며, 데이터베이스 쿼리 조작이 공통 요건이라는 서술은 옳지 않다.']}
/>

> **출제 포인트:** 클라이언트 측 입력값 검증만으로는 충분하지 않고 서버 측 검증이 반드시 필요하다는 시큐어코딩 원칙을 확인하는 문제입니다.

<Quiz
  questionNumber={12}
  question="시큐어코딩 가이드에서 입력값 검증에 대한 설명으로 옳지 않은 것은?"
  options={['클라이언트 측(브라우저)에서의 입력값 검증뿐 아니라 서버 측에서도 동일한 검증을 반드시 다시 수행해야 한다', '화이트리스트(허용 목록) 방식으로 허용된 문자·형식만 통과시키는 것이 블랙리스트 방식보다 우회에 더 강하다', '클라이언트 측 자바스크립트로 입력값을 검증했다면 서버 측 검증은 성능을 위해 생략해도 안전하다', '파일 업로드 기능에서는 확장자뿐 아니라 실제 파일 내용(매직 바이트 등)까지 확인해야 위장된 악성 파일을 걸러낼 수 있다']}
  correctAnswer={3}
  explanation="클라이언트 측 검증은 개발자 도구나 프록시 도구로 얼마든지 우회할 수 있으므로, 서버 측에서 동일한 검증을 반드시 다시 수행해야 한다. 클라이언트 검증만으로 충분하다는 서술은 시큐어코딩의 기본 원칙과 반대다."
  optionExplanations={['클라이언트·서버 이중 검증 원칙에 대한 정확한 설명이므로 옳다.', '화이트리스트 방식이 블랙리스트보다 우회에 강하다는 설명은 시큐어코딩의 일반 원칙이므로 옳다.', '정답이다. 클라이언트 측 검증은 우회가 가능하므로 서버 측 검증을 생략해도 안전하다는 서술은 옳지 않다.', '파일 내용 확인이 필요하다는 설명은 파일 업로드 취약점 대응의 기본 원칙이므로 옳다.']}
/>

> **출제 포인트:** SPF·DKIM·DMARC가 발신자 인증 기술이지 메일 본문의 기밀성을 보장하는 암호화 기술이 아니라는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={13}
  question="이메일 발신자 인증 기술인 SPF, DKIM, DMARC에 대한 설명으로 옳지 않은 것은?"
  options={['SPF(Sender Policy Framework)는 특정 도메인을 대신해 메일을 보낼 수 있는 서버의 IP 주소 목록을 DNS에 공개해 위조 여부를 판단하게 한다', 'DKIM(DomainKeys Identified Mail)은 발신 메일에 전자서명을 추가해 메일이 전송 중 변조되지 않았는지 검증할 수 있게 한다', 'DMARC(Domain-based Message Authentication, Reporting and Conformance)는 SPF·DKIM 검증에 실패한 메일을 어떻게 처리할지에 대한 정책을 도메인 소유자가 지정할 수 있게 한다', 'SPF·DKIM·DMARC는 메일 본문의 내용을 암호화해 제3자가 내용을 열람하지 못하게 하는 기밀성 기술이다']}
  correctAnswer={4}
  explanation="SPF·DKIM·DMARC는 발신자가 실제로 그 도메인의 정당한 발신자인지를 검증하는 발신자 인증 기술이며, 메일 본문의 기밀성을 보장하는 암호화 기술이 아니다. 메일 내용을 암호화하려면 S/MIME이나 PGP 같은 별도의 기술이 필요하다."
  optionExplanations={['SPF의 정의에 대한 정확한 설명이므로 옳다.', 'DKIM의 정의에 대한 정확한 설명이므로 옳다.', 'DMARC의 정의에 대한 정확한 설명이므로 옳다.', '정답이다. 세 기술은 발신자 인증 기술이며 메일 본문을 암호화하는 기밀성 기술이 아니다.']}
/>

> **출제 포인트:** TDE가 정상 인증된 애플리케이션의 질의에는 투명하게 복호화된 결과를 반환한다는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={14}
  question="데이터베이스 보안 기술인 TDE(Transparent Data Encryption)와 전자상거래 인증기술인 SSO(Single Sign-On)에 대한 설명으로 옳지 않은 것은?"
  options={['TDE는 데이터베이스 파일이 저장되는 디스크 수준에서 데이터를 암호화해, 디스크 자체를 탈취당해도 평문 데이터를 바로 열람할 수 없게 한다', 'SSO는 사용자가 한 번의 인증으로 여러 연계된 서비스에 재로그인 없이 접근할 수 있게 해 주는 인증 통합 기술이다', 'TDE가 적용된 데이터베이스는 정상적으로 인증된 애플리케이션의 질의에 대해서도 항상 암호화된 값만 반환해 조회 자체가 불가능하다', 'SSO 환경에서는 하나의 계정 정보가 유출되면 연계된 여러 서비스에 동시에 영향을 줄 수 있어 다중요소인증을 함께 적용하는 것이 권장된다']}
  correctAnswer={3}
  explanation="TDE는 디스크에 저장된 데이터 파일 수준에서 암호화를 적용하는 것이지, 정상적으로 인증된 애플리케이션이 데이터베이스 엔진을 통해 질의할 때는 엔진이 자동으로 복호화해 평문 결과를 반환한다. 정상 질의에도 항상 암호화된 값만 반환해 조회가 불가능하다는 서술은 TDE의 투명한 동작 방식과 반대다."
  optionExplanations={['TDE의 정의와 목적에 대한 정확한 설명이므로 옳다.', 'SSO의 정의에 대한 정확한 설명이므로 옳다.', '정답이다. TDE는 정상 질의에는 투명하게 복호화된 결과를 반환하므로, 조회 자체가 불가능하다는 서술은 옳지 않다.', 'SSO 환경의 위험과 다중요소인증 권장에 대한 설명은 실제 보안 실무이므로 옳다.']}
/>

> **출제 포인트:** 디지털 포렌식에서 원본 확보 직후 해시값을 기록하고 사본에서 분석해야 무결성을 증명할 수 있다는 절차를 확인하는 문제입니다.

<Quiz
  questionNumber={15}
  question="디지털 포렌식 절차에서 증거의 무결성을 보장하기 위해 수행하는 조치로 가장 적절한 것은?"
  options={['증거 원본을 직접 열어 필요한 파일만 골라 복사한다', '증거 원본을 확보한 직후 해시값을 계산해 기록하고, 이후 분석은 원본이 아닌 사본에서 수행해 원본과 사본의 해시값이 처음과 동일하게 유지되도록 관리한다', '분석 편의를 위해 원본 파일의 생성 시각과 수정 시각을 최신 시각으로 갱신한다', '여러 조사관이 동시에 원본에 접근해 각자 필요한 부분을 편집한다']}
  correctAnswer={2}
  explanation="디지털 포렌식은 증거의 무결성이 핵심이므로, 원본을 확보한 즉시 해시값을 계산해 기록해 두고, 실제 분석은 이 원본을 그대로 복제한 사본에서 수행한다. 분석이 끝난 뒤에도 원본의 해시값이 최초 확보 시점과 동일하다는 것을 증명해야 법정에서 증거로서의 신뢰성을 인정받을 수 있다."
  optionExplanations={['원본을 직접 열어 편집하면 시각 정보 등 메타데이터가 변경될 수 있어 무결성이 훼손된다.', '정답이다. 확보 직후 해시값을 기록하고 사본에서 분석하는 것이 무결성을 보장하는 표준 절차다.', '원본의 시각 정보를 임의로 갱신하는 것은 증거를 훼손하는 행위이므로 옳지 않다.', '여러 조사관이 원본을 동시에 편집하면 원본이 훼손되어 증거로서의 신뢰성을 잃는다.']}
/>

## 4과목 정보보안일반 (16–20)

> **출제 포인트:** 비밀번호(지식 기반)와 지문(존재 기반)을 함께 요구하는 경우 서로 다른 범주의 결합인 다중요소인증이 된다는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={16}
  question="한 회사가 사내 시스템 로그인 시 비밀번호 입력 후, 등록된 지문 센서로 지문을 인식해야 접속을 허용한다. 이 인증 방식의 조합으로 가장 적절한 것은?"
  options={['지식 기반 요소와 소유 기반 요소의 조합', '지식 기반 요소와 존재 기반 요소의 조합', '소유 기반 요소와 존재 기반 요소의 조합', '존재 기반 요소만을 이중으로 사용한 조합']}
  correctAnswer={2}
  explanation="비밀번호는 기억하는 정보인 지식 기반 요소이고, 지문은 신체적 특징인 존재(생체) 기반 요소다. 서로 다른 범주의 두 요소를 결합했으므로 다중요소인증에 해당한다."
  optionExplanations={['이 사례에는 소유 기반 요소(기기 등)가 등장하지 않으므로 옳지 않다.', '정답이다. 비밀번호는 지식 기반, 지문은 존재 기반 요소이므로 이 조합이 맞다.', '비밀번호는 소유 기반이 아니라 지식 기반 요소이므로 이 조합은 맞지 않는다.', '비밀번호는 존재 기반이 아니라 지식 기반 요소이므로 존재 기반만 이중으로 썼다는 서술은 옳지 않다.']}
/>

> **출제 포인트:** 등급(레이블) 비교로 접근 여부를 강제로 결정하는 방식이 MAC(강제적 접근통제)의 전형적인 사례라는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={17}
  question="국방 부문의 문서관리 시스템이 각 문서에 기밀, 대외비, 일반 등급을 매기고, 사용자에게도 열람 가능한 최고 등급을 부여해 등급이 낮은 사용자는 등급이 높은 문서를 열람할 수 없도록 강제한다. 이 방식에 가장 가까운 접근통제 모델은?"
  options={['DAC(임의적 접근통제)', 'MAC(강제적 접근통제)', 'RBAC(역할기반 접근통제)', 'Capability(권한 목록) 방식']}
  correctAnswer={2}
  explanation="문서와 사용자에게 각각 보안 등급(레이블)을 부여하고, 소유자나 사용자의 임의적 판단이 아니라 시스템이 등급 비교로 접근 여부를 강제로 결정하는 방식은 MAC(강제적 접근통제)의 전형적인 사례다."
  optionExplanations={['DAC은 자원 소유자가 권한을 임의로 정하는 방식으로, 등급 비교로 강제 결정되는 이 사례와는 다르다.', '정답이다. 보안 등급을 매기고 시스템이 등급 비교로 접근을 강제하는 방식은 MAC의 정의와 정확히 일치한다.', 'RBAC은 개인이 아니라 역할에 권한을 부여하는 방식으로, 등급 비교가 핵심인 이 사례와는 초점이 다르다.', 'Capability는 사용자를 기준으로 권한 목록을 관리하는 기법으로, 등급 비교에 의한 강제 통제와는 다른 개념이다.']}
/>

> **출제 포인트:** RSA 키 생성의 첫 단계인 모듈러스와 오일러 파이 함수 계산을 새로운 소수 쌍으로 직접 밟아 보는 문제입니다.

<Quiz
  questionNumber={18}
  question={"두 소수 p=5, q=11로 RSA 키를 생성하려 한다. 모듈러스(modulus) n과 오일러 파이 함수 값 phi(n)을 바르게 구한 것은?\n\n계산 순서: n = p x q, phi(n) = (p-1) x (q-1)"}
  options={['n=55, phi(n)=40', 'n=55, phi(n)=44', 'n=50, phi(n)=40', 'n=55, phi(n)=16']}
  correctAnswer={1}
  explanation="모듈러스는 두 소수를 곱한 값이므로 n = 5 곱하기 11 = 55이다. 오일러 파이 함수 phi(n)은 두 소수에서 각각 1을 뺀 값을 곱해 구하므로 phi(n) = (5-1) 곱하기 (11-1) = 4 곱하기 10 = 40이다."
  optionExplanations={['정답이다. n=5×11=55이고 phi(n)=(5-1)×(11-1)=4×10=40이므로 두 값 모두 맞다.', 'phi(n)을 (p-1)×(q-1)이 아니라 다른 방식으로 잘못 계산했을 때 나올 수 있는 값이다.', 'n을 p×q가 아니라 다른 계산으로 잘못 구했을 때 나올 수 있는 값이다.', 'phi(n)을 p-1과 q-1의 곱이 아니라 두 값 중 하나만으로 잘못 계산했을 때 나올 수 있는 값이다.']}
/>

> **출제 포인트:** 18번 문제에서 구한 n=55, phi(n)=40을 이어받아, 공개키 지수 e가 주어졌을 때 개인키 지수 d를 구하는 문제입니다.

<Quiz
  questionNumber={19}
  question={"18번 문제와 같은 RSA 키(n=55, phi(n)=40)에서 공개키 지수를 e=7로 정했다. e와 d를 곱한 값을 phi(n)인 40으로 나눈 나머지가 1이 되도록 하는 개인키 지수 d의 값은? (d는 1보다 크고 phi(n)보다 작은 정수)\n\n조건: (e x d) mod phi(n) = 1, 즉 (7 x d) mod 40 = 1"}
  options={['d=13', 'd=17', 'd=23', 'd=33']}
  correctAnswer={3}
  explanation="조건은 7과 d를 곱한 값을 40으로 나눈 나머지가 1이 되는 d를 찾는 것이다. d=23을 대입하면 7 곱하기 23은 161이고, 161을 40으로 나눈 나머지는 1이므로 조건을 만족한다. 이렇게 구한 d=23이 이 키 쌍의 개인키 지수가 된다."
  optionExplanations={['7×13=91이고 91을 40으로 나눈 나머지는 11이므로 조건을 만족하지 않는다.', '7×17=119이고 119를 40으로 나눈 나머지는 39이므로 조건을 만족하지 않는다.', '정답이다. 7×23=161이고 161을 40으로 나눈 나머지는 1이므로 조건을 만족하는 d는 23이다.', '7×33=231이고 231을 40으로 나눈 나머지는 31이므로 조건을 만족하지 않는다.']}
/>

> **출제 포인트:** 전자서명 검증에서 두 해시값이 일치하지 않으면 위변조나 서명자 불일치를 의심해야 한다는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={20}
  question="전자서명 검증 과정에서 검증자가 계산한 원문 해시값과, 서명값을 서명자의 공개키로 복호화해 얻은 해시값이 서로 다르게 나왔다. 이 상황에 대한 해석으로 가장 적절한 것은?"
  options={['원문이 서명 이후 변조되었거나 서명자의 개인키가 아닌 다른 키로 서명되었을 가능성이 크다', '단순히 네트워크 전송 속도가 느려서 발생하는 정상적인 현상이다', '해시함수의 출력 길이가 서로 다른 알고리즘을 썼기 때문에 항상 발생하는 현상이므로 무시해도 된다', '공개키 암호 대신 대칭키 암호를 썼기 때문에 발생하는 문제이므로 알고리즘만 바꾸면 해결된다']}
  correctAnswer={1}
  explanation="두 해시값이 다르다는 것은 원문이 서명 이후 변조되었거나, 애초에 검증에 사용한 공개키에 대응하는 개인키로 서명되지 않았음을 의미한다. 전자서명 검증에서 이 불일치는 무결성 또는 서명자 인증에 실패했다는 신호로 해석해야 하며, 결코 무시해도 되는 정상 현상이 아니다."
  optionExplanations={['정답이다. 해시값 불일치는 위변조 또는 서명자 불일치를 의미하는 무결성·인증 실패 신호다.', '전송 속도와 해시값 비교 결과는 무관하므로 정상 현상이라는 서술은 옳지 않다.', '검증 절차는 동일한 해시 알고리즘으로 두 값을 계산해 비교하도록 설계되므로, 알고리즘 차이 때문에 항상 발생하는 현상이 아니다.', '전자서명은 공개키 암호 구조를 전제로 하며, 대칭키 암호로 교체하는 것은 이 문제의 해결책이 아니다.']}
/>

## 5과목 정보보안관리 및 법규 (21–25)

> **출제 포인트:** 정성적 위험도 계산에서 자산가치가 큰 자산이라도 위협·취약점이 작으면 위험도가 더 낮게 나올 수 있다는 점을 다시 확인하는 문제입니다.

<Quiz
  questionNumber={21}
  question="자산가치 6점, 위협 발생 가능성 3점, 취약점 정도 4점인 인사정보시스템과, 자산가치 9점, 위협 발생 가능성 2점, 취약점 정도 2점인 재무정보시스템이 있다. 정성적 위험도(자산가치×위협 발생 가능성×취약점 정도)를 비교한 결과로 옳은 것은?"
  options={['인사정보시스템 72점, 재무정보시스템 36점으로 인사정보시스템의 위험도가 더 높다', '인사정보시스템 36점, 재무정보시스템 72점으로 재무정보시스템의 위험도가 더 높다', '두 시스템 모두 위험도가 54점으로 동일하다', '자산가치가 더 큰 재무정보시스템의 위험도가 항상 더 높으므로 별도 계산이 필요 없다']}
  correctAnswer={1}
  explanation="위험도는 세 값의 곱이므로 인사정보시스템은 6×3×4=72, 재무정보시스템은 9×2×2=36이다. 자산가치만 보면 재무정보시스템이 더 커 보이지만, 위협과 취약점이 큰 인사정보시스템의 위험도가 오히려 더 높게 나온다."
  optionExplanations={['정답이다. 6×3×4=72, 9×2×2=36으로 계산되며 인사정보시스템의 위험도가 더 높다.', '두 계산 결과를 서로 뒤바꾼 값이다.', '곱셈이 아닌 다른 방식으로 계산했을 때 나올 수 있는 값이다.', '자산가치만으로 위험도를 단정할 수 없으며, 위협·취약점을 함께 곱해 계산해야 하므로 이 서술은 옳지 않다.']}
/>

> **출제 포인트:** 관리적·기술적·물리적 통제를 실제 조치 사례에서 정확히 분류하는 문제입니다.

<Quiz
  questionNumber={22}
  question="회사 출입문에 지문 인식 장치를 설치하고, 서버 접근 시 방화벽 룰로 특정 IP만 허용하며, 매년 전 직원에게 정보보호 서약서에 서명받는다. 이 세 조치를 관리적·기술적·물리적 통제로 옳게 분류한 것은?"
  options={['지문 인식 장치: 물리적, 방화벽 룰: 기술적, 서약서 서명: 관리적', '지문 인식 장치: 기술적, 방화벽 룰: 물리적, 서약서 서명: 관리적', '지문 인식 장치: 관리적, 방화벽 룰: 기술적, 서약서 서명: 물리적', '세 조치 모두 기술적 통제에 해당한다']}
  correctAnswer={1}
  explanation="물리적 공간에 대한 출입을 통제하는 지문 인식 장치는 물리적 통제, 시스템 설정으로 구현하는 방화벽 룰은 기술적 통제, 규정 준수를 서약받는 서약서 서명은 관리적 통제에 해당한다."
  optionExplanations={['정답이다. 각 조치를 물리적·기술적·관리적 통제로 정확히 분류했다.', '지문 인식 장치는 물리적 통제이고 방화벽 룰은 기술적 통제이므로 이 분류는 서로 뒤바뀌었다.', '지문 인식 장치는 관리적 통제가 아니라 물리적 통제이므로 이 분류는 옳지 않다.', '세 조치는 각각 물리적·기술적·관리적 통제로 서로 다르게 분류되므로 모두 기술적 통제라는 서술은 옳지 않다.']}
/>

> **출제 포인트:** 개인정보 유출 사고에서 사전 관리 소홀(안전조치의무)과 사후 대응 의무(통지·신고)가 각각 다른 조문으로 규정된다는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={23}
  question="한 온라인 쇼핑몰이 회원 개인정보를 암호화하지 않고 평문으로 저장해 두었다가 외부 공격으로 유출되었다. 이 상황에서 쇼핑몰의 사전 관리 소홀과 사후 대응 의무를 각각 규정하는 개인정보보호법 조문을 옳게 짝지은 것은?"
  options={['사전 관리 소홀: 제29조(안전조치의무), 사후 대응 의무: 제34조(유출 등의 통지·신고)', '사전 관리 소홀: 제17조(제공), 사후 대응 의무: 제18조(목적 외 이용 제한)', '사전 관리 소홀: 제23조(민감정보 처리 제한), 사후 대응 의무: 제24조(고유식별정보 처리 제한)', '사전 관리 소홀: 제39조(손해배상책임), 사후 대응 의무: 제15조(수집·이용)']}
  correctAnswer={1}
  explanation="암호화 등 안전성 확보조치를 소홀히 한 것은 제29조(안전조치의무) 위반의 문제이고, 유출 사실을 인지한 이후 정보주체와 규제기관에 알려야 하는 절차는 제34조(개인정보 유출 등의 통지·신고)가 규율한다."
  optionExplanations={['정답이다. 사전 관리는 제29조, 사후 대응은 제34조가 각각 규율한다.', '제17조와 제18조는 제공과 목적 외 이용에 관한 조문이며 이 사례의 안전조치·유출 대응과는 직접 관련이 없다.', '이 사례에는 민감정보나 고유식별정보가 특정되어 있지 않으므로 제23조·제24조가 직접 적용되는 사례가 아니다.', '제39조는 손해배상책임을, 제15조는 수집·이용 근거를 규정할 뿐 사전 관리·사후 대응 의무를 직접 규정하는 조문이 아니다.']}
/>

> **출제 포인트:** 2026년 정보통신망법 개정 논의의 실제 방향(CISO 격상·매출액 기준 과징금 도입)을 확인하는 문제입니다.

<Quiz
  questionNumber={24}
  question="2026년 정보통신망법 개정 논의의 방향으로 가장 적절한 것은?"
  options={['정보보호최고책임자(CISO)의 지위를 임원급으로 격상하고 매출액 기준 과징금을 도입하는 방향', '전자서명의 공인·사설 구분을 다시 부활시키는 방향', 'ISMS-P 인증제도를 전면 폐지하는 방향', '주요정보통신기반시설 지정 권한을 민간 기업 자율에 맡기는 방향']}
  correctAnswer={1}
  explanation="2026년 정보통신망법 개정 논의는 정보보호최고책임자(CISO)의 지위를 임원급으로 격상하고, 매출액 기준 과징금을 도입해 침해사고 대응과 예방 책임을 강화하는 방향으로 진행되었다. 공인·사설 인증서 구분은 오히려 2020년에 이미 폐지된 제도로 부활 논의와 무관하다."
  optionExplanations={['정답이다. CISO 지위 격상과 매출액 기준 과징금 도입이 실제 개정 논의의 방향이다.', '공인·사설 구분은 2020년 개정으로 이미 폐지되었으며 부활 논의와는 무관하다.', 'ISMS-P는 계속 운영 중인 제도이며 폐지 논의의 대상이 아니다.', '주요정보통신기반시설 지정 권한은 정보통신기반보호법에 따라 중앙행정기관의 장에게 있으며, 민간 자율로 이전하는 논의가 아니다.']}
/>

> **출제 포인트:** 클라우드보안인증제(CSAP) 개편이 국가정보원 중심의 단일 검증체계로 이관되는 방향이라는 점을 확인하는 문제입니다.

<Quiz
  questionNumber={25}
  question="클라우드보안인증제(CSAP) 개편 논의의 방향으로 가장 적절한 것은?"
  options={['공공 부문 클라우드 보안 검증을 국가정보원 중심의 단일 검증 체계로 이관하고, 기존 CSAP는 민간 영역의 자율 보안 인증으로 전환하는 방향', '공공 부문 클라우드 서비스 이용을 전면 금지하는 방향', '해외 클라우드 사업자에게만 적용하고 국내 사업자는 인증 대상에서 제외하는 방향', 'ISMS-P 인증을 받은 사업자는 CSAP 심사를 아예 받을 필요가 없게 통합하는 방향']}
  correctAnswer={1}
  explanation="클라우드보안인증제 개편 논의는 공공 부문의 클라우드 보안 검증을 국가정보원 중심의 단일 검증 체계로 이관하고, 기존의 상·중·하 등급 CSAP는 민간 영역을 대상으로 한 자율 보안 인증으로 전환하는 방향으로 진행되고 있다."
  optionExplanations={['정답이다. 국가정보원 중심 단일 검증체계로의 이관과 CSAP의 민간 자율 인증 전환이 실제 개편 방향이다.', '공공 부문 클라우드 서비스 이용 자체를 금지하는 논의는 아니며, 검증 체계를 바꾸는 것이 핵심이다.', 'CSAP는 국내외 사업자를 구분하지 않고 공공 부문에 클라우드를 공급하려는 사업자 전반에 적용되는 인증이다.', 'ISMS-P와 CSAP는 서로 다른 법적 근거와 목적을 가진 별개의 인증제도이며, 완전히 통합해 심사를 면제하는 논의는 아니다.']}
/>

## 참고 자료

- [itwiki - 정보보안기사 필기 출제기준](https://itwiki.kr/w/정보보안기사_필기_출제기준)
- [공공데이터포털 - 한국방송통신전파진흥원_국가기술자격검정 정보보안 종목 출제기준](https://www.data.go.kr/data/15140079/fileData.do)
- [한국방송통신전파진흥원(cq.or.kr) - 정보보안기사 자격정보](https://www.cq.or.kr/qh_quagm01_020.do)
- [OWASP - OWASP Top 10:2021 (공식)](https://owasp.org/Top10/en/)
