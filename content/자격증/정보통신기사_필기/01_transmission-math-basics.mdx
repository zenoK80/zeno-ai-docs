---
title: '01. 통신 계산을 위한 수학 기초'
description: '로그와 데시벨(dB) 계산, 삼각함수와 위상·주기·주파수·파장의 관계, 통신에서 쓰는 벡터·복소수의 초급 감각을 손 계산으로 익힌다'
---

<Callout type="info">
  **이번 문서의 목표:** 이 문서를 다 읽으면 신호의 증폭·손실을 데시벨(dB)로 손 계산하고, 주파수·주기·파장·위상의 관계식을 서로 변환하며, 교류 신호를 다룰 때 왜 복소수가 필요한지 설명할 수 있다.
</Callout>

## 왜 통신 계산에는 이 수학이 먼저 필요한가

정보통신기사 필기의 "정보전송일반" 과목은 변조·다중화·전송선로처럼 거의 전부 **계산 문제**로 이루어져 있습니다. 그런데 이 계산 문제들은 전부 세 가지 수학 도구 위에 서 있습니다. 신호의 세기 변화를 다루는 **로그와 데시벨**, 신호의 파형 자체를 다루는 **삼각함수**, 그리고 교류 신호의 크기와 위상을 한 번에 다루는 **벡터·복소수**입니다. 이 세 가지를 여기서 확실히 잡아 두지 않으면, 06편부터 나오는 변조·다중화·전송선로 계산에서 공식은 외웠는데 손이 멈추는 상황이 반복됩니다.

> **쉽게 말하면:** 이 편은 통신 계산 전용 계산기를 손에 익히는 시간입니다. 뒤에 나오는 모든 편이 이 계산기를 빌려 씁니다.

## 1. 로그(log) 복습 — 데시벨 계산의 재료

**로그**(log, logarithm)는 지수의 반대 연산입니다. "밑을 몇 번 곱해야 이 수가 되는가"를 답합니다.

$$
\log_{10} 1000 = 3
$$

- 밑 $10$: 곱할 수
- 진수 $1000$: 만들고 싶은 결과
- 값 $3$: 10을 세 번 곱하면 1000이 되므로 3

통신 계산에서는 밑이 10인 **상용로그**(common logarithm)만 쓰므로, 이 문서 전체에서 $\log$는 $\log_{10}$을 뜻합니다. 데시벨 계산에 실제로 쓰이는 로그 법칙은 세 가지뿐입니다.

| 법칙 | 식 | 뜻 |
|------|-----|-----|
| 곱셈 → 덧셈 | $\log(A \times B) = \log A + \log B$ | 곱셈이 로그를 통과하면 덧셈이 된다 |
| 나눗셈 → 뺄셈 | $\log(A / B) = \log A - \log B$ | 나눗셈이 로그를 통과하면 뺄셈이 된다 |
| 거듭제곱 → 곱셈 | $\log(A^{n}) = n \times \log A$ | 지수가 로그 앞으로 내려와 곱셈이 된다 |

**왜 이 법칙이 통신에서 중요한가:** 신호가 여러 구간(케이블·증폭기·커넥터)을 지나면 세기는 원래 **곱셈**으로 누적됩니다(구간마다 몇 배씩 줄거나 늘어나므로). 그런데 로그를 씌우면 이 곱셈이 **덧셈**으로 바뀝니다. 데시벨이 통신에서 표준 단위로 쓰이는 이유가 바로 이것입니다 — 여러 구간을 지난 신호의 총 변화량을 곱셈이 아니라 덧셈·뺄셈만으로 구할 수 있기 때문입니다.

## 2. 데시벨(dB) — 통신에서 가장 많이 쓰는 단위

> **쉽게 말하면:** 데시벨은 신호가 몇 배로 커지거나 작아졌는지를, 곱셈 대신 덧셈으로 계산할 수 있게 바꿔주는 압축 단위입니다.

### 정의

**데시벨**(dB, decibel)은 두 전력(power, 파워)의 비율을 로그로 표현한 단위입니다.

$$
\text{dB} = 10 \log_{10}\left(\frac{P_2}{P_1}\right)
$$

- $P_2$: 비교하려는 전력(예: 출력 전력)
- $P_1$: 기준 전력(예: 입력 전력)
- 앞의 $10$: "데시(deci)"가 10분의 1을 뜻하는 접두어이기 때문에 벨(bel, 벨 단위) 값에 10을 곱해 데시벨을 만든다

전압이나 전류처럼 전력이 아닌 값을 비교할 때는 계수가 20으로 바뀝니다. 전력은 전압의 제곱에 비례하기 때문입니다($P = V^2/R$).

$$
\text{dB} = 20 \log_{10}\left(\frac{V_2}{V_1}\right)
$$

**자주 틀리는 점:** 전력 비율은 `10 log`, 전압·전류 비율은 `20 log`를 씁니다. 문제에 "전력이 100배"라고 나오면 10을 곱하고, "전압이 100배"라고 나오면 20을 곱해야 합니다. 이 둘을 바꿔 쓰는 실수가 기출에서 가장 흔한 함정입니다.

### 증폭(이득) 계산 — 손으로 끝까지

신호가 증폭기를 지나며 전력이 100배로 커졌다면 dB로 몇 인가.

**1단계 — 비율 정리**

$$
\frac{P_2}{P_1} = 100
$$

**2단계 — 로그 적용**

$$
\log_{10} 100 = 2
$$

**3단계 — 10 곱하기**

$$
\text{dB} = 10 \times 2 = 20\ \text{dB}
$$

**해석:** 전력이 100배가 되는 것은 **+20dB**로 표현됩니다. 이렇게 곱셈으로 세면 자릿수가 계속 늘어나는 값(10배, 100배, 1000배…)이 데시벨에서는 10, 20, 30처럼 **10씩 증가하는 값**으로 바뀝니다. 이것이 데시벨이 "큰 범위를 다루는 압축 단위"라고 불리는 이유입니다.

### 손실(감쇠) 계산 — 손으로 끝까지

신호가 케이블을 지나며 전력이 원래의 1000분의 1로 줄었다면 dB로 몇 인가.

**1단계 — 비율 정리**

$$
\frac{P_2}{P_1} = \frac{1}{1000} = 0.001
$$

**2단계 — 로그 적용**

$$
\log_{10} 0.001 = -3
$$

**3단계 — 10 곱하기**

$$
\text{dB} = 10 \times (-3) = -30\ \text{dB}
$$

**해석:** 전력이 줄어드는 손실(loss, 감쇠)은 **음수 dB**로 나타납니다. 통신 회선에서 "감쇠 30dB"라고 말하면, 실제로는 이 계산의 부호를 뒤집어 손실을 표현한 것뿐입니다.

### 여러 구간을 지날 때 — 덧셈으로 합산

신호가 다음 순서로 지나갑니다. 케이블 손실 -3dB, 증폭기 이득 +17dB, 커넥터 손실 -1dB.

```mermaid
flowchart LR
  A["송신 신호<br/>0 dB 기준"] -->|"케이블<br/>-3 dB"| B["구간 1"]
  B -->|"증폭기<br/>+17 dB"| C["구간 2"]
  C -->|"커넥터<br/>-1 dB"| D["수신 신호"]
```

**1단계 — 구간별 dB를 그대로 더한다**

$$
\text{총 이득} = (-3) + 17 + (-1) = 13\ \text{dB}
$$

**2단계 — dB를 다시 배율(비율)로 되돌린다**

역으로 배율을 구하려면 위 정의식을 반대로 풀어야 합니다.

$$
\frac{P_2}{P_1} = 10^{\left(\frac{\text{dB}}{10}\right)}
$$

$$
\frac{P_2}{P_1} = 10^{\left(\frac{13}{10}\right)} = 10^{1.3}
$$

$$
10^{1.3} \approx 19.95
$$

**해석:** 세 구간을 곱셈으로 계산하면 $10^{-0.3} \times 10^{1.7} \times 10^{-0.1}$ 처럼 지수 계산이 번거롭지만, dB로는 **-3+17-1=13을 더하기만 하면** 끝납니다. 그리고 최종 배율이 필요하면 마지막에 한 번만 $10^{(13/10)}$ 로 되돌리면 됩니다. 신호는 최종적으로 약 19.95배 커진 상태로 수신됩니다.

### 절대 단위 dBm — 기준이 있는 데시벨

dB 자체는 **비율**이라 "몇 배 커졌는가"만 말할 뿐, 실제 전력이 얼마인지는 알려주지 않습니다. 실제 전력값을 나타내려면 기준을 고정한 **절대 단위**가 필요합니다. 통신에서 가장 흔한 것이 **dBm**으로, 1밀리와트(mW)를 기준(0dBm)으로 삼습니다.

$$
\text{dBm} = 10 \log_{10}\left(\frac{P[\text{mW}]}{1\ \text{mW}}\right)
$$

**계산 예시:** 송신 전력이 1와트(W)일 때 dBm은?

**1단계 — 단위를 mW로 통일**

$$
1\ \text{W} = 1000\ \text{mW}
$$

**2단계 — 정의식 대입**

$$
\text{dBm} = 10 \log_{10}(1000) = 10 \times 3 = 30\ \text{dBm}
$$

**해석:** 1와트는 30dBm입니다. 이렇게 절대 전력에 기준을 붙여 두면, 앞서 배운 "구간별 dB를 더하는" 계산을 그대로 이어서 쓸 수 있습니다. 예를 들어 30dBm으로 송신한 신호가 13dB만큼 커졌다면, 수신 전력은 $30 + 13 = 43\ \text{dBm}$ 이 됩니다. **자주 틀리는 점:** dB는 순수 비율(단위 없음)이고, dBm·dBW는 절대 전력값(기준이 있음)입니다. "dB"와 "dBm"을 같은 것으로 혼동해 그냥 더하거나 빼면 안 됩니다 — dBm끼리는 뺄셈으로 dB(비율)를 얻을 수 있지만, dB에 dB를 더해 dBm을 만들 수는 없습니다(기준이 없기 때문입니다).

## 3. 삼각함수와 위상·주기·주파수·파장의 관계

> **쉽게 말하면:** 통신 신호는 결국 시간에 따라 오르내리는 물결이고, 그 물결의 생김새를 숫자로 표현하는 언어가 삼각함수입니다.

### 사인파와 각도·라디안

통신에서 다루는 기본 신호는 **사인파**(sine wave)입니다. 시계 방향으로 회전하는 점의 세로 좌표가 시간에 따라 그리는 궤적이 사인 함수 $\sin\theta$ 입니다. 각도는 도(degree)로도, **라디안**(radian, 호도법)으로도 잴 수 있습니다. 라디안은 반지름과 같은 길이의 호(arc, 원둘레의 일부)가 만드는 각도를 1라디안으로 정의합니다. 원 한 바퀴(360도)는 $2\pi$ 라디안입니다.

$$
360^{\circ} = 2\pi\ \text{rad}
$$

통신 공식은 대부분 라디안 단위를 기본으로 씁니다. 예를 들어 90도는 $\pi/2$ 라디안, 180도는 $\pi$ 라디안입니다.

### 주기·주파수·각주파수

- **주기**(period, $T$): 파형이 한 번 반복하는 데 걸리는 시간(초 단위)
- **주파수**(frequency, $f$): 1초 동안 반복되는 횟수(헤르츠, Hz 단위)
- **각주파수**(angular frequency, $\omega$, 오메가): 1초 동안 회전하는 각도(라디안 단위)

이 셋은 다음 관계로 묶여 있습니다.

$$
f = \frac{1}{T}
$$

$$
\omega = 2\pi f
$$

**계산 예시:** 주기가 0.01초인 신호의 주파수와 각주파수는?

**1단계 — 주파수**

$$
f = \frac{1}{0.01} = 100\ \text{Hz}
$$

**2단계 — 각주파수**

$$
\omega = 2\pi \times 100 = 200\pi\ \text{rad/s} \approx 628.3\ \text{rad/s}
$$

**해석:** 이 신호는 1초에 100번 반복되고(100Hz), 그동안 총 628.3라디안(약 100바퀴)을 회전한 것과 같습니다. **자주 틀리는 점:** $f$(주파수, 단위 Hz)와 $\omega$(각주파수, 단위 rad/s)는 값이 다릅니다. $\omega$에는 항상 $2\pi$가 곱해져 있다는 것을 잊고 둘을 같은 값으로 대입하는 실수가 흔합니다.

### 위상

**위상**(phase, $\phi$, 파이)은 사인파가 기준 시점(t=0)에서 얼마나 "밀리거나 당겨져" 있는지를 나타내는 각도입니다. 일반적인 사인파 신호는 다음과 같이 씁니다.

$$
v(t) = A \sin(\omega t + \phi)
$$

- $A$: 진폭(amplitude, 파형의 최대 높이)
- $\omega t$: 시간 $t$가 지나면서 누적된 각도
- $\phi$: 위상(초기 시점의 밀림 정도)

생활 비유로는 같은 노래를 부르는 두 사람이 있는데, 한 사람이 다른 사람보다 반 박자 늦게 시작하는 상황이 위상차입니다. 두 신호의 진폭과 주파수가 완전히 같아도 위상이 다르면 서로 다른 시점에 봉우리(peak, 최고점)가 나타납니다. 이 위상차 개념은 07편의 위상변조(PM)와 PSK를 이해하는 데 그대로 쓰이므로 여기서 확실히 잡아 둡니다.

### 파장과 전파속도

**파장**(wavelength, $\lambda$, 람다)은 파동이 한 주기 동안 실제로 나아가는 거리입니다. 전자기파가 진공(또는 공기)에서 나아가는 속도인 **빛의 속도**(전파속도, $c$)는 초당 약 $3 \times 10^{8}$ 미터로 일정합니다.

$$
\lambda = \frac{c}{f}
$$

- $\lambda$: 파장(미터, m)
- $c$: 전파속도, 약 $3 \times 10^{8}\ \text{m/s}$
- $f$: 주파수(Hz)

**계산 예시:** 주파수 100MHz(FM 라디오 대역)의 파장은?

**1단계 — 단위 정리**

$$
f = 100\ \text{MHz} = 100 \times 10^{6}\ \text{Hz}
$$

**2단계 — 공식 대입**

$$
\lambda = \frac{3 \times 10^{8}}{100 \times 10^{6}} = \frac{3 \times 10^{8}}{1 \times 10^{8}} = 3\ \text{m}
$$

**해석:** 100MHz 신호의 파장은 3미터입니다. **주파수가 높을수록 파장은 짧아진다**는 반비례 관계를 여기서 확인할 수 있습니다. 이 관계는 09편에서 안테나 길이와 전파 특성을 계산할 때 그대로 다시 쓰입니다.

## 4. 벡터와 복소수의 초급 감각

> **쉽게 말하면:** 교류 신호는 크기 하나만으로는 설명이 안 되고, 크기와 방향(위상)을 함께 표현해야 합니다. 그 표현 도구가 벡터와 복소수입니다.

### 왜 필요한가

직류(DC) 신호는 숫자 하나(전압값)로 충분히 설명됩니다. 그런데 교류(AC)나 통신 신호는 시간에 따라 크기가 오르내리고, 여러 신호를 더할 때는 위상차까지 고려해야 합니다. 두 신호를 그냥 숫자로 더하면 위상 정보가 사라지므로, **크기(magnitude)와 방향(각도)을 한 쌍으로 갖는 값**이 필요합니다. 이것이 벡터이고, 통신·회로 계산에서는 이를 복소수로 표현한 **페이저**(phasor)로 다룹니다.

### 복소수의 두 가지 표현

복소수는 실수부와 허수부로 이루어진 수입니다.

$$
z = a + jb
$$

- $a$: 실수부(real part)
- $b$: 허수부(imaginary part)
- $j$: 허수 단위($j^2 = -1$). 전기·통신 분야에서는 $i$가 전류 기호와 겹치므로 관례적으로 $j$를 씁니다

이 복소수는 좌표평면 위의 한 점으로 그릴 수 있고, 같은 점을 "크기와 각도"로도 표현할 수 있습니다. 이를 **극형식**(polar form)이라 합니다.

$$
z = A \angle \theta
$$

- $A$: 크기(magnitude, 원점에서 그 점까지의 거리)
- $\theta$: 각도(그 점이 실수축에서 얼마나 회전했는지)

### 직교좌표 → 극형식 변환 — 손으로 끝까지

복소수 $z = 3 + j4$ 를 극형식으로 바꿔 보겠습니다.

**1단계 — 크기 구하기(피타고라스 정리)**

$$
A = \sqrt{a^{2} + b^{2}} = \sqrt{3^{2} + 4^{2}} = \sqrt{9+16} = \sqrt{25} = 5
$$

**2단계 — 각도 구하기(역탄젠트)**

$$
\theta = \tan^{-1}\left(\frac{b}{a}\right) = \tan^{-1}\left(\frac{4}{3}\right) \approx 53.1^{\circ}
$$

**해석:** $3+j4$ 는 크기 5, 각도 약 53.1도인 벡터와 같습니다. 실수부 3은 "동쪽으로 3만큼", 허수부 4는 "북쪽으로 4만큼" 이동한 것에 비유할 수 있고, 이 둘을 합친 실제 이동 거리(대각선)가 5, 방향이 53.1도인 것입니다. 07편의 QAM(직교진폭변조)에서 신호를 좌표평면 위의 점으로 나타낼 때, 그리고 09편의 임피던스 계산에서 이 변환을 그대로 다시 사용합니다.

## 핵심 정리

- 로그의 곱셈→덧셈, 나눗셈→뺄셈 법칙 덕분에, 통신에서는 신호 세기의 곱셈적 변화를 데시벨(dB)이라는 덧셈 단위로 바꿔 계산한다.
- 전력비는 $10\log_{10}$, 전압·전류비는 $20\log_{10}$ 을 쓴다. dB는 상대 비율, dBm·dBW는 절대 전력값이다.
- 여러 구간을 지난 신호의 총 dB는 각 구간의 dB를 그대로 더하면 되고, 실제 배율이 필요하면 $10^{(\text{dB}/10)}$ 으로 마지막에 한 번만 되돌린다.
- $f = 1/T$, $\omega = 2\pi f$, $\lambda = c/f$ 세 관계식으로 주기·주파수·각주파수·파장을 서로 변환할 수 있다. 주파수가 높을수록 파장은 짧아진다.
- 위상(phase)은 사인파가 기준 시점에서 얼마나 밀려 있는지를 나타내며, 위상변조·PSK·QAM을 이해하는 기초가 된다.
- 복소수는 직교좌표($a+jb$)와 극형식($A\angle\theta$)으로 서로 변환할 수 있으며, 이는 교류 신호와 통신 임피던스 계산에서 크기와 위상을 함께 표현하는 도구다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="어떤 증폭기를 통과한 신호의 전력이 원래보다 1000배 커졌다. 이 이득을 데시벨로 나타내면?"
  options={['10 dB', '20 dB', '30 dB', '1000 dB']}
  correctAnswer={3}
  explanation="전력비가 1000배이므로 log10(1000)=3이고, 전력 데시벨 공식은 10을 곱하므로 10x3=30dB이다. 전압비가 아니라 전력비이므로 20을 곱하면 안 된다."
  optionExplanations={['log10(1000)=3에 10을 곱한 값이 아니라 그 절반 수준으로 잘못 계산한 값이다.', '전압비 공식(20log)을 잘못 적용했을 때 나오는 값이다.', '10x log10(1000)=10x3=30dB로 옳다.', '단위 계수 10을 곱하지 않고 그대로 곱한 값을 쓴 오류다.']}
/>

<Quiz
  questionNumber={2}
  question="신호가 케이블에서 -6dB, 증폭기에서 +14dB, 스플리터에서 -2dB를 거쳤다. 최종 총 이득(dB)은?"
  options={['22 dB', '6 dB', '10 dB', '18 dB']}
  correctAnswer={2}
  explanation="데시벨은 구간마다 곱셈이 아니라 덧셈으로 누적되므로 -6+14-2=6dB가 정답이다. 각 값을 그대로 더하기만 하면 되고 별도의 로그 재계산이 필요 없다."
  optionExplanations={['부호를 무시하고 절댓값만 더한 값이다.', '-6+14-2=6으로 정확히 계산된 값이다.', '어느 한 구간의 부호를 반대로 계산했을 때 나오는 값이다.', '증폭 이득만 반영하고 손실 두 구간을 빼지 않은 값이다.']}
/>

<Quiz
  questionNumber={3}
  question="송신 전력이 1와트(W)일 때 이를 dBm으로 옳게 나타낸 것은?"
  options={['0 dBm', '10 dBm', '30 dBm', '100 dBm']}
  correctAnswer={3}
  explanation="1W는 1000mW이고 dBm=10log10(P[mW]/1mW)이므로 10log10(1000)=10x3=30dBm이다. 0dBm은 1mW를 기준으로 한 값이지 1W가 아니다."
  optionExplanations={['0dBm은 1mW(0.001W)를 나타내는 값으로 1W와는 다르다.', '10dBm은 10mW에 해당하는 값이다.', '10log10(1000)=30으로 정확히 계산된 값이다.', '단위 환산을 잘못해 자릿수를 하나 더 올린 값이다.']}
/>

<Quiz
  questionNumber={4}
  question="주기가 0.005초인 신호의 주파수는?"
  options={['5 Hz', '50 Hz', '200 Hz', '500 Hz']}
  correctAnswer={3}
  explanation="주파수는 주기의 역수이므로 f=1/T=1/0.005=200Hz이다. 소수점 계산을 정리하면 1을 0.005로 나눈 값이 200이 된다."
  optionExplanations={['소수점 자리를 잘못 옮겨 계산한 값이다.', '자릿수 계산을 한 자리 축소한 값이다.', '1/0.005=200으로 정확히 계산된 값이다.', '역수 대신 곱셈을 적용했을 때 나오는 값이다.']}
/>

<Quiz
  questionNumber={5}
  question="주파수 150MHz 신호의 파장(전파속도 3x10^8 m/s 기준)에 가장 가까운 값은?"
  options={['0.5 m', '2 m', '15 m', '150 m']}
  correctAnswer={2}
  explanation="파장은 lambda=c/f=(3x10^8)/(150x10^6)=2m이다. 분자와 분모의 지수를 정리하면 3/1.5에 10의 지수 차이를 곱해 2가 된다."
  optionExplanations={['공식을 뒤집어 f/c 형태로 계산했을 때 근접하게 나오는 값이다.', '(3x10^8)/(150x10^6)=2로 정확히 계산된 값이다.', '단위 지수(10의 거듭제곱)를 하나 잘못 옮긴 값이다.', '메가(10^6) 단위를 반영하지 않고 그대로 나눈 값이다.']}
/>

<Quiz
  questionNumber={6}
  question="복소수 3+j4를 극형식으로 바꿀 때 크기(A)는?"
  options={['3', '4', '5', '7']}
  correctAnswer={3}
  explanation="크기는 피타고라스 정리로 구하므로 A=제곱근(3제곱+4제곱)=제곱근(9+16)=제곱근(25)=5이다. 3과 4는 실수부·허수부일 뿐 크기가 아니다."
  optionExplanations={['3은 실수부일 뿐 벡터 전체의 크기가 아니다.', '4는 허수부일 뿐 벡터 전체의 크기가 아니다.', '제곱근(9+16)=5로 정확히 계산된 값이다.', '실수부와 허수부를 단순히 더한 값으로 크기 공식과 다르다.']}
/>

<Quiz
  questionNumber={7}
  question="데시벨(dB)과 dBm의 관계에 대한 설명으로 옳은 것은?"
  options={['dB는 1mW를 기준으로 한 절대 전력값이다', 'dBm은 기준이 없는 순수 비율이다', 'dB는 상대적인 비율이고 dBm은 1mW를 기준으로 한 절대 전력값이다', 'dB와 dBm은 항상 같은 값을 가리킨다']}
  correctAnswer={3}
  explanation="dB는 두 전력의 비율(상대값)을 로그로 나타낸 단위이고, dBm은 1mW라는 고정 기준을 두어 실제 전력의 크기(절대값)까지 나타낸다. 두 단위는 성격이 달라 서로 그대로 바꿔 쓸 수 없다."
  optionExplanations={['dB는 절대값이 아니라 두 값의 비율을 나타내는 단위다.', 'dBm은 1mW라는 명확한 기준을 갖는 절대 단위다.', 'dB는 상대 비율, dBm은 1mW 기준의 절대값이라는 설명이 정확하다.', '기준이 있는 dBm과 기준이 없는 dB는 같은 종류의 값이 아니다.']}
/>

<Quiz
  questionNumber={8}
  question="각주파수(omega)와 주파수(f)의 관계식으로 옳은 것은?"
  options={['omega = f', 'omega = 2 x pi x f', 'omega = f / 2', 'omega = pi x f x f']}
  correctAnswer={2}
  explanation="각주파수는 1초 동안 회전한 각도(라디안)로, 한 바퀴가 2pi 라디안이므로 초당 반복 횟수인 f에 2pi를 곱해 omega=2 x pi x f가 된다."
  optionExplanations={['단위가 다른 두 값(Hz와 rad/s)을 같다고 놓은 오류다.', '한 바퀴가 2pi 라디안이라는 사실을 반영한 정확한 관계식이다.', '2pi를 곱하는 대신 나눈 값으로 방향이 반대다.', 'f를 두 번 곱하는 형태로 물리적 의미가 맞지 않는다.']}
/>

## 참고 자료

- [한국방송통신전파진흥원(KCA) - 출제기준 자료실](https://www.cq.or.kr/qh_cusgm10_001.do)
- [Q-Net(한국산업인력공단) - 국가자격 종목별 상세정보(정보통신기사)](https://www.q-net.or.kr/crf005.do?id=crf00503&jmCd=1192)
