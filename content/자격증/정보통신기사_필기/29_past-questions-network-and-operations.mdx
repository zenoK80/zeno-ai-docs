---
title: '29. 기출·복원 유형으로 보는 정보통신네트워크·정보시스템운용 핵심 함정'
description: '서브네팅·VLSM·수퍼네팅 계산, HSRP·VRRP·GLBP 같은 이중화 프로토콜과 SDH·MPLS·WDM 전송망 기술 용어 구분, DAS·NAS·SAN과 가상화·보안 장비 기능 구분 함정을 재구성 문항 25개로 점검한다'
---

<Callout type="warning">
  **이 편의 문제는 실제 기출을 그대로 복제한 것이 아닙니다.** 15~23편에서 다룬 개념을 바탕으로 숫자·상황·보기 표현을 새로 구성한 문항입니다. 문장을 통째로 외우지 말고, 왜 그 보기가 정답이거나 오답인지 근거를 이해하는 방식으로 풀어야 실전에서도 흔들리지 않습니다.
</Callout>

## 출제 포인트

- **서브네팅·VLSM·수퍼네팅 계산**은 매 회차 빠지지 않는 영역입니다. 주어진 요구 호스트 수에서 필요한 호스트 비트 수를 거꾸로 구하는 연습과, 네트워크 주소·브로드캐스트 주소를 손으로 찾는 연습을 함께 해 둡니다.
- **이중화 프로토콜**(HSRP·VRRP·GLBP)은 "누가 표준이고 누가 시스코 독자 규격인가", "동시에 몇 대가 트래픽을 처리하는가"라는 두 축으로 구분하는 문제가 반복됩니다.
- **전송망·광가입자망 기술**(SDH·MPLS·CWDM·DWDM·PON·xDSL)은 이름이 비슷한 두 기술의 설명을 서로 바꿔치기하는 함정이 잦습니다.
- **스토리지·가상화·보안 장비**는 이름이 가리키는 기능의 층위(파일 단위인가 블록 단위인가, 탐지만 하는가 차단까지 하는가)를 정확히 구분해야 합니다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="OSI 7계층 참조모델에서 MAC 주소를 이용해 프레임을 목적지로 전달하는 계층은 무엇인가?"
  options={['물리 계층', '데이터링크 계층', '네트워크 계층', '전송 계층']}
  correctAnswer={2}
  explanation="MAC 주소는 데이터링크 계층에서 프레임을 같은 네트워크 안의 목적지로 전달할 때 사용하는 물리적 주소다. 물리 계층은 비트를 전기·광 신호로 바꾸는 역할만 하고, IP 주소를 이용한 목적지 결정은 네트워크 계층, 종단 간 신뢰성 있는 전달은 전송 계층의 역할이다."
  optionExplanations={['물리 계층은 비트 신호의 전기적·광학적 전송만 담당하며 주소 개념 자체가 없다.', 'MAC 주소를 읽어 프레임을 전달하는 것은 데이터링크 계층의 정의와 정확히 일치한다.', '네트워크 계층은 IP 주소를 이용해 경로를 결정하는 계층이며 MAC 주소를 직접 다루지 않는다.', '전송 계층은 포트 번호를 이용한 종단 간 통신을 담당하며 MAC 주소와는 관련이 없다.']}
/>

<Quiz
  questionNumber={2}
  question="200.100.100.0/24 네트워크를 호스트 수가 같은 4개의 서브넷으로 나누려 한다. 이때 사용해야 할 서브넷 마스크와 서브넷당 사용 가능한 호스트 수로 옳은 것은?"
  options={['255.255.255.192, 62개', '255.255.255.224, 30개', '255.255.255.240, 14개', '255.255.255.128, 126개']}
  correctAnswer={1}
  explanation="4개의 서브넷이 필요하므로 2를 몇 번 곱해야 4가 되는지 따지면 2제곱, 즉 2비트를 호스트 비트에서 빌려 와야 한다. 24비트였던 프리픽스에 2비트를 더하면 26비트가 되어 서브넷 마스크는 255.255.255.192다. 남은 호스트 비트는 32에서 26을 뺀 6비트이므로 표현 가능한 조합은 2를 6번 곱한 64가지이고, 여기서 네트워크 주소와 브로드캐스트 주소 2개를 제외하면 서브넷당 사용 가능한 호스트는 62개다."
  optionExplanations={['2비트를 빌려 26비트 프리픽스로 나누면 마스크 255.255.255.192, 호스트 62개가 되어 4개의 동일 크기 서브넷 요구에 정확히 맞는다.', '이 마스크는 27비트 프리픽스로 8개의 서브넷을 만드는 경우이며 4개 분할 요구보다 더 잘게 나눈 결과다.', '이 마스크는 28비트 프리픽스로 16개의 서브넷을 만드는 경우이며 4개 분할 요구와 맞지 않는다.', '이 마스크는 25비트 프리픽스로 2개의 서브넷만 만들어져 4개 분할 요구에 미달한다.']}
/>

<Quiz
  questionNumber={3}
  question="192.168.1.0/24 대역을 VLSM(가변길이 서브넷 마스킹)으로 나누어 호스트 100대가 필요한 부서에 배정하려 한다. 이 부서에 배정할 수 있는 최소 프리픽스 길이는?"
  options={['/25', '/26', '/27', '/28']}
  correctAnswer={1}
  explanation="100대를 수용하려면 사용 가능한 호스트 수가 100 이상이어야 한다. 25비트 프리픽스는 호스트 비트가 7개 남아 2를 7번 곱한 128에서 2를 뺀 126개를 수용해 요구를 만족하는 가장 작은 블록이다. 26비트 프리픽스는 호스트 비트가 6개뿐이라 2를 6번 곱한 64에서 2를 뺀 62개만 수용해 100대에 미달한다."
  optionExplanations={['호스트 비트 7개로 126개를 수용해 100대 요구를 만족하면서도 낭비가 가장 적은 최소 블록이다.', '호스트 비트 6개로는 62개만 수용되어 100대 요구를 만족하지 못한다.', '호스트 비트 5개로는 30개만 수용되어 100대 요구에 크게 미달한다.', '호스트 비트 4개로는 14개만 수용되어 100대 요구와는 거리가 멀다.']}
/>

<Quiz
  questionNumber={4}
  question="10.20.30.45/28 주소가 속한 서브넷의 브로드캐스트 주소는?"
  options={['10.20.30.31', '10.20.30.47', '10.20.30.63', '10.20.30.255']}
  correctAnswer={2}
  explanation="프리픽스가 28이면 호스트 비트가 4개 남아 서브넷 하나의 크기는 2를 4번 곱한 16이다. 네 번째 옥텟은 0, 16, 32, 48 단위로 서브넷 경계가 나뉘므로 45는 32에서 47 사이의 서브넷에 속하고, 그 서브넷의 마지막 주소인 47이 브로드캐스트 주소가 된다."
  optionExplanations={['31은 16부터 31까지 서브넷의 브로드캐스트 주소이며 45가 속한 서브넷과는 다른 구간이다.', '45가 속한 32부터 47까지 서브넷의 마지막 주소인 47이 정확한 브로드캐스트 주소다.', '63은 48부터 63까지 서브넷의 브로드캐스트 주소이며 45가 속한 구간보다 한 칸 뒤다.', '255는 서브넷을 나누지 않은 클래스 C 전체 대역의 브로드캐스트 주소이며 이 문제의 조건과 맞지 않는다.']}
/>

<Quiz
  questionNumber={5}
  question="ARP(주소 결정 프로토콜, Address Resolution Protocol)의 기능으로 옳은 것은?"
  options={['IP 주소를 이용해 같은 네트워크 안 장치의 MAC 주소를 알아낸다', '도메인 이름을 IP 주소로 변환한다', '사설 IP 주소를 공인 IP 주소로 변환한다', '전송 중 발생한 오류를 상대에게 알린다']}
  correctAnswer={1}
  explanation="ARP는 같은 네트워크 안에서 상대의 IP 주소는 알지만 MAC 주소를 모를 때, 네트워크 전체에 브로드캐스트로 질의를 보내 해당 IP 주소를 가진 장치의 MAC 주소를 응답받는 프로토콜이다."
  optionExplanations={['IP 주소로 MAC 주소를 알아내는 것이 ARP의 정의와 정확히 일치한다.', '도메인 이름을 IP 주소로 바꾸는 것은 DNS의 역할이다.', '사설 IP를 공인 IP로 바꾸는 것은 NAT(네트워크 주소 변환)의 역할이다.', '오류를 알리는 것은 ICMP의 역할이며 주소 변환과는 다른 기능이다.']}
/>

<Quiz
  questionNumber={6}
  question="DHCP를 통해 단말이 IP 주소를 자동으로 할당받는 과정을 순서대로 나열한 것은?"
  options={['Discover, Offer, Request, Acknowledge', 'Request, Discover, Offer, Acknowledge', 'Offer, Discover, Acknowledge, Request', 'Acknowledge, Offer, Discover, Request']}
  correctAnswer={1}
  explanation="단말이 먼저 IP 주소가 필요하다고 네트워크 전체에 알리는 Discover 메시지를 브로드캐스트하고, DHCP 서버가 사용 가능한 IP 주소를 제안하는 Offer로 응답하며, 단말이 그 제안을 받아들이겠다고 Request를 보내면, 서버가 최종 확인 메시지인 Acknowledge를 보내 할당이 완료된다."
  optionExplanations={['탐색으로 시작해 제안, 요청, 확인으로 이어지는 이 순서가 DHCP의 표준 절차와 정확히 일치한다.', '단말이 아직 서버를 발견하지도 않은 상태에서 Request를 먼저 보내는 것은 순서가 뒤바뀐 것이다.', '서버가 존재를 알기도 전에 Offer를 먼저 보내는 것은 순서상 불가능하다.', '할당 확인인 Acknowledge가 가장 먼저 오는 것은 절차의 흐름과 정반대다.']}
/>

<Quiz
  questionNumber={7}
  question="TCP와 UDP를 비교한 설명으로 옳지 않은 것은?"
  options={['TCP는 연결을 맺은 뒤 데이터를 주고받는 연결지향형 프로토콜이다', 'UDP는 연결 설정 없이 데이터를 보내는 비연결형 프로토콜이다', 'UDP는 순서 제어와 재전송을 통해 TCP보다 더 높은 신뢰성을 보장한다', 'TCP는 흐름 제어와 오류 제어 기능을 갖추고 있어 신뢰성이 필요한 전송에 적합하다']}
  correctAnswer={3}
  explanation="순서 제어와 재전송으로 신뢰성을 보장하는 쪽은 TCP다. UDP는 이런 절차를 생략해 오버헤드를 줄이는 대신 신뢰성을 포기한 프로토콜이므로, UDP가 TCP보다 신뢰성이 높다는 설명은 정의를 반대로 뒤집은 것이다."
  optionExplanations={['3방향 핸드셰이크로 연결을 맺고 통신하는 TCP의 정의와 일치하는 옳은 설명이다.', '연결 설정 절차 없이 바로 데이터를 보내는 UDP의 정의와 일치하는 옳은 설명이다.', '순서 제어와 재전송은 TCP의 기능이며 UDP는 이를 생략하므로 이 설명이 옳지 않다.', 'TCP가 흐름 제어와 오류 제어로 신뢰성을 확보한다는 설명은 TCP의 특징과 정확히 일치한다.']}
/>

<Quiz
  questionNumber={8}
  question="유선 이더넷의 CSMA/CD와 무선랜의 CSMA/CA를 비교한 설명으로 옳지 않은 것은?"
  options={['CSMA/CD는 충돌이 발생한 것을 감지한 뒤 대응하는 방식이다', 'CSMA/CA는 충돌 발생 자체를 미리 피하려는 방식이다', '무선매체는 유선매체와 달리 송신 중 자신의 신호와 충돌을 동시에 감지하기 어려워 CD 대신 CA 방식을 쓴다', 'CSMA/CD는 무선랜에서, CSMA/CA는 유선 이더넷에서 주로 사용된다']}
  correctAnswer={4}
  explanation="CSMA/CD(충돌 감지)는 유선 이더넷에서, CSMA/CA(충돌 회피)는 무선랜에서 주로 사용된다. 이 설명은 두 방식이 쓰이는 매체를 서로 뒤바꿔 놓았으므로 옳지 않다."
  optionExplanations={['충돌을 감지한 뒤 대응한다는 CSMA/CD의 정의와 일치하는 옳은 설명이다.', '충돌을 사전에 회피한다는 CSMA/CA의 정의와 일치하는 옳은 설명이다.', '무선매체가 자신의 송신 신호에 묻혀 충돌을 감지하기 어렵다는 것은 CA 방식이 채택된 실제 이유이므로 옳은 설명이다.', '실제로는 CSMA/CD가 유선 이더넷, CSMA/CA가 무선랜에서 쓰이므로 이 설명은 두 매체를 뒤바꾼 것이다.']}
/>

<Quiz
  questionNumber={9}
  question="어떤 통신장비의 MTBF(평균고장간격)가 970시간, MTTR(평균수리시간)이 30시간이다. 이 장비의 가용도(availability)는 몇 퍼센트인가?"
  options={['97퍼센트', '90퍼센트', '99퍼센트', '70퍼센트']}
  correctAnswer={1}
  explanation="가용도는 전체 운용 시간 중 실제로 정상 작동한 시간의 비율로, MTBF를 MTBF와 MTTR의 합으로 나누어 구한다. 970을 970과 30을 더한 1000으로 나누면 0.97, 즉 97퍼센트가 된다."
  optionExplanations={['970을 1000으로 나눈 값을 백분율로 바꾸면 97퍼센트가 되어 정확한 계산 결과다.', '분모나 분자를 잘못 어림했을 때 나올 수 있는 값이며 실제 계산 결과와 다르다.', 'MTTR을 무시하고 MTBF만으로 판단했을 때 나올 수 있는 값이며 실제 계산과 다르다.', 'MTBF와 MTTR의 비율을 반대로 계산했을 때 나올 수 있는 값이며 실제 계산과 다르다.']}
/>

<Quiz
  questionNumber={10}
  question="정보통신기자재 관련 인증제도에 대한 설명으로 옳지 않은 것은?"
  options={['KC(국가통합인증)마크는 전기용품의 안전성과 전자파적합성 등을 확인하는 인증이다', 'TTA인증은 한국정보통신기술협회가 통신기기의 상호운용성과 성능을 시험해 부여하는 인증이다', 'GS인증은 소프트웨어 제품의 품질을 시험해 부여하는 인증이다', 'KC·TTA·GS 세 인증은 발급 목적과 시험 항목이 모두 동일한 사실상 하나의 제도다']}
  correctAnswer={4}
  explanation="KC는 전기·전자 제품의 안전성을 확인하는 강제 인증, TTA인증은 통신기기의 상호운용성·성능 시험 인증, GS인증은 소프트웨어 품질 인증으로 대상과 목적이 서로 다르다. 세 제도를 동일한 제도로 서술한 것이 옳지 않다."
  optionExplanations={['전기용품 안전성과 전자파적합성을 확인한다는 KC마크에 대한 설명은 옳다.', '통신기기의 상호운용성과 성능을 시험한다는 TTA인증에 대한 설명은 옳다.', '소프트웨어 품질을 시험한다는 GS인증에 대한 설명은 옳다.', '세 인증은 대상 제품과 시험 항목이 서로 다른 별개의 제도이므로 동일한 제도라는 이 설명이 옳지 않다.']}
/>

<Quiz
  questionNumber={11}
  question="스위치 네트워크에서 표준 STP(Spanning Tree Protocol)보다 빠른 컨버전스(수렴) 속도로 루프를 방지하도록 개선된 프로토콜은?"
  options={['VTP', 'RSTP', 'HSRP', 'GLBP']}
  correctAnswer={2}
  explanation="RSTP(Rapid Spanning Tree Protocol)는 표준 STP의 느린 포트 상태 전이 절차를 개선해 훨씬 빠른 시간 안에 루프 없는 토폴로지로 수렴하도록 만든 프로토콜이다."
  optionExplanations={['VTP는 VLAN 구성 정보를 여러 스위치에 배포하는 프로토콜로 루프 방지와는 목적이 다르다.', '표준 STP보다 빠르게 수렴하도록 개선된 프로토콜이 RSTP라는 설명이 정확하다.', 'HSRP는 게이트웨이 이중화 프로토콜로 스위치 루프 방지와는 관련이 없다.', 'GLBP도 게이트웨이 이중화 프로토콜로 스위치 루프 방지와는 관련이 없다.']}
/>

<Quiz
  questionNumber={12}
  question="여러 스위치에 걸쳐 VLAN 구성 정보를 자동으로 배포하고 동기화하는 시스코 독자 프로토콜은?"
  options={['STP', 'VTP', 'RSTP', 'HSRP']}
  correctAnswer={2}
  explanation="VTP(VLAN Trunking Protocol)는 하나의 관리 도메인 안에서 VLAN을 생성·삭제·수정하면 그 정보를 트렁크로 연결된 다른 스위치들에 자동으로 전파해 VLAN 구성을 일치시키는 시스코 독자 프로토콜이다."
  optionExplanations={['STP는 루프 방지를 위한 프로토콜로 VLAN 정보 배포와는 목적이 다르다.', 'VLAN 구성 정보를 여러 스위치에 자동 배포하는 것이 VTP의 정의와 정확히 일치한다.', 'RSTP는 STP를 개선해 수렴 속도를 높인 프로토콜로 VLAN 정보 배포와는 무관하다.', 'HSRP는 게이트웨이 이중화 프로토콜로 VLAN 정보 배포와는 관련이 없다.']}
/>

<Quiz
  questionNumber={13}
  question="라우팅 프로토콜 분류에 대한 설명으로 옳지 않은 것은?"
  options={['Static 라우팅은 관리자가 경로를 직접 입력하는 방식이다', 'Dynamic 라우팅은 라우터끼리 정보를 교환해 경로를 자동으로 계산한다', 'OSPF는 자율시스템(AS) 내부에서 사용하는 IGP(내부 게이트웨이 프로토콜)에 속한다', 'BGP는 자율시스템 내부에서만 사용되도록 설계된 IGP에 속한다']}
  correctAnswer={4}
  explanation="BGP(Border Gateway Protocol)는 서로 다른 자율시스템 사이에서 경로 정보를 교환하는 EGP(외부 게이트웨이 프로토콜)다. 자율시스템 내부에서만 쓰인다고 서술한 것은 IGP와 EGP의 구분을 뒤바꾼 것이다."
  optionExplanations={['관리자가 직접 입력한다는 Static 라우팅의 정의는 옳다.', '라우터끼리 정보를 교환해 자동 계산한다는 Dynamic 라우팅의 정의는 옳다.', 'OSPF가 자율시스템 내부에서 쓰이는 IGP라는 설명은 옳다.', 'BGP는 자율시스템 사이를 잇는 EGP이므로 내부 전용 IGP라는 이 설명이 옳지 않다.']}
/>

<Quiz
  questionNumber={14}
  question="게이트웨이 이중화 프로토콜인 HSRP·VRRP·GLBP에 대한 설명으로 옳지 않은 것은?"
  options={['HSRP는 시스코 독자 프로토콜이고 VRRP는 개방형 표준 프로토콜이다', 'HSRP와 VRRP는 평상시 하나의 라우터만 트래픽을 실제로 전달한다', 'GLBP는 HSRP·VRRP와 마찬가지로 항상 하나의 라우터만 트래픽을 전달하도록 설계되었다', 'GLBP는 여러 대의 라우터가 동시에 트래픽을 처리하도록 부하를 분산할 수 있다']}
  correctAnswer={3}
  explanation="GLBP(Gateway Load Balancing Protocol)는 HSRP·VRRP와 달리 여러 대의 라우터가 동시에 활성 상태로 트래픽을 나누어 처리하도록 설계된 프로토콜이다. 항상 하나의 라우터만 전달한다고 서술한 것은 GLBP의 핵심 특징과 반대다."
  optionExplanations={['HSRP는 시스코 독자, VRRP는 개방형 표준이라는 설명은 옳다.', 'HSRP와 VRRP는 액티브-스탠바이 구조로 평소 한 대만 전달한다는 설명은 옳다.', 'GLBP는 여러 라우터가 동시에 트래픽을 처리할 수 있으므로 이 설명이 옳지 않다.', 'GLBP가 부하를 분산해 여러 라우터를 동시에 활용한다는 설명은 옳다.']}
/>

<Quiz
  questionNumber={15}
  question="HSRP와 VRRP의 공통점으로 가장 적절한 것은?"
  options={['가상의 게이트웨이 IP 주소를 두고 평상시 한 대의 라우터만 트래픽을 전달한다', '두 프로토콜 모두 여러 라우터가 항상 동시에 트래픽을 전달한다', '두 프로토콜 모두 스위치 사이의 VLAN 정보를 동기화하는 데 쓰인다', '두 프로토콜 모두 광케이블 구간의 신호 손실을 측정하는 데 쓰인다']}
  correctAnswer={1}
  explanation="HSRP와 VRRP는 모두 가상의 게이트웨이 IP 주소를 여러 라우터가 공유하고, 평상시에는 그중 하나(액티브 또는 마스터)만 실제 트래픽을 전달하다가 장애가 나면 다른 라우터가 그 역할을 넘겨받는 구조라는 공통점이 있다."
  optionExplanations={['가상 게이트웨이를 공유하고 평상시 한 대만 전달한다는 공통점은 두 프로토콜의 정의와 정확히 일치한다.', '동시에 여러 라우터가 전달하는 것은 GLBP의 특징이며 HSRP·VRRP의 공통점이 아니다.', 'VLAN 정보 동기화는 VTP의 역할이며 게이트웨이 이중화 프로토콜과는 다른 기능이다.', '광케이블 손실 측정은 09편에서 다룬 전송매체 측정 영역이며 게이트웨이 이중화와는 무관하다.']}
/>

<Quiz
  questionNumber={16}
  question="PON(수동광가입자망, Passive Optical Network)에 대한 설명으로 가장 적절한 것은?"
  options={['국사에 설치된 OLT 하나가 전력 공급 없는 광분배기(스플리터)를 거쳐 다수의 ONU·ONT와 신호를 주고받는 점대다중점 구조다', '가입자 자택마다 별도의 능동 전원 증폭 장비를 두어 신호를 재생하는 구조다', '기존 전화선의 구리 회선을 그대로 사용하는 비대칭 디지털 가입자 회선 기술이다', '동축케이블과 광케이블을 혼합해 방송 신호를 분배하는 케이블TV 전용 기술이다']}
  correctAnswer={1}
  explanation="PON은 국사(중앙국)의 OLT(광선로종단장치) 하나가 전력이 필요 없는 수동형 광분배기를 거쳐 여러 가입자의 ONU·ONT(광네트워크종단장치)와 신호를 주고받는 점대다중점 구조로, 분배기에 전원이 필요 없다는 점이 핵심이다."
  optionExplanations={['수동 분배기를 거친 점대다중점 구조라는 설명이 PON의 정의와 정확히 일치한다.', 'PON의 분배기는 전원이 필요 없는 수동 소자이므로 능동 전원 장비를 둔다는 설명은 옳지 않다.', '구리 회선을 그대로 쓰는 것은 xDSL 계열 기술에 대한 설명이며 광가입자망인 PON과는 다르다.', '동축과 광을 혼합하는 것은 HFC(하이브리드 광동축)에 대한 설명이며 PON과는 다른 기술이다.']}
/>

<Quiz
  questionNumber={17}
  question="다음 설명에 해당하는 인터넷 접속 기술은? 기존 전화선의 구리 회선을 그대로 사용하며, 다운로드 속도가 업로드 속도보다 빠른 비대칭 구조를 갖는다"
  options={['ADSL', 'FTTH', 'HFC', 'PON']}
  correctAnswer={1}
  explanation="ADSL(Asymmetric Digital Subscriber Line, 비대칭 디지털 가입자 회선)은 기존 전화선의 구리 회선을 그대로 이용하면서 다운로드와 업로드 속도를 다르게(비대칭) 구성해 일반 가입자의 인터넷 사용 패턴에 맞춘 xDSL 계열 기술이다."
  optionExplanations={['구리 회선을 그대로 쓰며 비대칭 속도 구조를 갖는다는 설명이 ADSL의 정의와 정확히 일치한다.', 'FTTH(광케이블을 가입자 댁내까지 직접 연결)는 구리 회선이 아니라 광케이블을 사용하는 기술이다.', 'HFC는 광케이블과 동축케이블을 혼합한 구조로 전화선 구리 회선과는 다른 매체를 쓴다.', 'PON은 광가입자망 기술로 구리 회선이 아니라 광케이블 기반 점대다중점 구조를 갖는다.']}
/>

<Quiz
  questionNumber={18}
  question="CWDM과 DWDM을 비교한 설명으로 옳지 않은 것은?"
  options={['DWDM은 채널 간격이 매우 좁아 CWDM보다 훨씬 많은 파장 채널을 하나의 광섬유에 실을 수 있다', 'CWDM은 DWDM보다 채널 간격이 넓어 상대적으로 적은 수의 채널만 수용한다', 'CWDM은 채널 간격이 좁아 더 많은 채널을 수용하므로 장거리 백본망에 주로 쓰인다', 'DWDM은 광증폭기를 활용한 장거리 전송에 적합해 통신사의 장거리 백본망에 주로 쓰인다']}
  correctAnswer={3}
  explanation="채널 간격이 좁아 많은 채널을 수용하고 장거리 백본망에 주로 쓰이는 쪽은 DWDM이다. CWDM은 채널 간격이 넓어 상대적으로 적은 채널만 수용하며 주로 근거리·중거리 구간에 저비용으로 쓰인다. 이 설명은 두 기술의 특징을 서로 바꿔 놓았으므로 옳지 않다."
  optionExplanations={['DWDM이 좁은 채널 간격으로 더 많은 채널을 수용한다는 설명은 옳다.', 'CWDM이 넓은 채널 간격으로 적은 채널만 수용한다는 설명은 옳다.', '채널 간격이 좁아 많은 채널을 수용하고 장거리 백본망에 쓰이는 것은 DWDM의 특징인데 이를 CWDM으로 서술했으므로 이 설명이 옳지 않다.', 'DWDM이 광증폭기를 활용해 장거리 백본망에 쓰인다는 설명은 옳다.']}
/>

<Quiz
  questionNumber={19}
  question="IPTV와 OTT(Over The Top) 서비스를 비교한 설명으로 가장 적절한 것은?"
  options={['IPTV는 통신사업자가 관리하는 폐쇄형 네트워크를 통해 제공되고, OTT는 공중 인터넷망을 통해 제공된다', 'IPTV와 OTT는 모두 동축케이블 기반 방송망을 통해서만 서비스된다', 'OTT는 반드시 통신사업자의 셋톱박스를 거쳐야만 콘텐츠를 재생할 수 있다', 'IPTV는 사업자 구분 없이 어떤 인터넷 회선에서도 동일한 화질이 보장된다']}
  correctAnswer={1}
  explanation="IPTV는 통신사업자가 품질을 관리하는 폐쇄형(관리형) IP망을 통해 제공되어 안정적인 화질을 보장하는 반면, OTT는 특정 사업자의 망에 묶이지 않고 공중 인터넷망을 통해 다양한 기기에서 콘텐츠를 제공한다는 점이 핵심 차이다."
  optionExplanations={['통신사업자의 관리형 망을 쓰는 IPTV와 공중 인터넷망을 쓰는 OTT의 구분이 정확하다.', '동축케이블 기반 방송망은 CATV에 대한 설명이며 IPTV·OTT 모두와 정확히 일치하지 않는다.', 'OTT는 스마트TV, 스마트폰, PC 등 다양한 기기에서 앱만으로 재생 가능해 특정 셋톱박스에 묶이지 않는다.', 'IPTV의 화질은 가입한 통신사의 망 품질 관리 수준에 따라 달라질 수 있어 항상 동일하다고 볼 수 없다.']}
/>

<Quiz
  questionNumber={20}
  question="네트워크에 연결되어 파일 단위로 데이터를 여러 사용자와 공유하는 저장장치 방식은?"
  options={['DAS', 'NAS', 'SAN', 'RAID']}
  correctAnswer={2}
  explanation="NAS(Network Attached Storage, 네트워크 결합 스토리지)는 이더넷 등 일반 네트워크에 직접 연결되어 여러 사용자가 파일 단위로 자료를 공유할 수 있게 해 주는 저장장치다."
  optionExplanations={['DAS는 서버 한 대에 직접 연결되는 저장장치로 네트워크를 통한 파일 공유가 기본 목적이 아니다.', '네트워크에 연결되어 파일 단위로 공유되는 저장장치라는 설명이 NAS의 정의와 정확히 일치한다.', 'SAN은 서버에서 디스크처럼 인식되는 블록 단위 전용 네트워크로 파일 단위 공유와는 접근 방식이 다르다.', 'RAID는 여러 디스크를 묶어 성능이나 안정성을 높이는 구성 기술이지 그 자체가 네트워크 저장장치 방식은 아니다.']}
/>

<Quiz
  questionNumber={21}
  question="2테라바이트 하드디스크 5개로 RAID5를 구성했을 때 실제로 사용 가능한 총 저장 용량은?"
  options={['10테라바이트', '8테라바이트', '6테라바이트', '4테라바이트']}
  correctAnswer={2}
  explanation="RAID5는 디스크 하나 분량의 용량을 패리티(오류 복구 정보) 저장에 분산해서 사용하므로, 사용 가능한 총 용량은 디스크 개수에서 1을 뺀 값에 디스크 한 개의 용량을 곱해서 구한다. 디스크 5개에서 1을 뺀 4에 2테라바이트를 곱하면 8테라바이트가 된다."
  optionExplanations={['디스크 5개 용량을 그대로 더한 값으로, 패리티에 쓰이는 용량을 반영하지 않은 계산이다.', '디스크 개수에서 1을 뺀 4개 분량인 8테라바이트가 RAID5의 정확한 사용 가능 용량이다.', '디스크 2개 분량을 패리티로 뺀 경우에 나올 수 있는 값으로 RAID5가 아니라 RAID6에 해당하는 계산이다.', '디스크 3개 분량을 뺀 경우에 나올 수 있는 값으로 RAID5의 패리티 구조와 맞지 않는다.']}
/>

<Quiz
  questionNumber={22}
  question="하이퍼바이저 방식 가상화와 컨테이너 방식 가상화를 비교한 설명으로 옳지 않은 것은?"
  options={['하이퍼바이저 방식은 가상머신마다 별도의 완전한 게스트 운영체제를 포함한다', '컨테이너는 호스트 운영체제의 커널을 여러 컨테이너가 함께 공유한다', '컨테이너는 각각 별도의 게스트 운영체제를 완전히 포함하므로 하이퍼바이저 방식보다 자원 소모가 크다', '컨테이너는 하이퍼바이저 방식보다 시작 속도가 빠르고 자원 오버헤드가 작은 편이다']}
  correctAnswer={3}
  explanation="컨테이너는 호스트 운영체제의 커널을 공유하고 별도의 게스트 운영체제를 포함하지 않기 때문에 하이퍼바이저 방식보다 자원 소모가 오히려 작다. 게스트 운영체제를 완전히 포함해 자원 소모가 크다고 서술한 것은 하이퍼바이저 방식에 대한 설명을 컨테이너에 잘못 붙인 것이다."
  optionExplanations={['가상머신마다 완전한 게스트 운영체제를 둔다는 것은 하이퍼바이저 방식의 정확한 특징이다.', '호스트 커널을 공유한다는 것은 컨테이너 방식의 정확한 특징이다.', '컨테이너는 게스트 운영체제를 포함하지 않으므로 이 설명이 옳지 않다.', '커널 공유 구조 덕분에 컨테이너가 더 가볍고 빠르다는 설명은 옳다.']}
/>

<Quiz
  questionNumber={23}
  question="네트워크 장비 간 시각을 서로 동기화하기 위해 사용하는 프로토콜은?"
  options={['NTP', 'SNMP', 'ICMP', 'ARP']}
  correctAnswer={1}
  explanation="NTP(Network Time Protocol, 네트워크 시각 동기화 프로토콜)는 계층화된 시각 기준(스트라텀)을 이용해 네트워크에 연결된 장비들의 시계를 서로 맞추는 데 쓰인다. 로그 분석이나 인증서 검증처럼 여러 장비의 시각이 일치해야 하는 작업에 필수적이다."
  optionExplanations={['장비 간 시각을 동기화한다는 설명이 NTP의 정의와 정확히 일치한다.', 'SNMP는 네트워크 장비의 상태를 조회·관리하는 프로토콜로 시각 동기화가 목적이 아니다.', 'ICMP는 오류 보고와 상태 확인(핑 등)에 쓰이는 프로토콜로 시각 동기화와는 다른 기능이다.', 'ARP는 IP 주소로 MAC 주소를 알아내는 프로토콜로 시각 동기화와는 무관하다.']}
/>

<Quiz
  questionNumber={24}
  question="무정전전원공급장치(UPS)를 구내통신 설비에 두는 주된 목적은?"
  options={['정전이 발생해도 일정 시간 동안 안정적으로 전원을 공급해 장비의 갑작스러운 중단을 막는다', '실내 온도와 습도를 일정하게 유지해 장비 과열을 막는다', '외부에서 들어오는 통신 회선을 이중으로 구성해 장애에 대비한다', '데이터를 암호화해 저장 매체의 도난에 대비한다']}
  correctAnswer={1}
  explanation="UPS(Uninterruptible Power Supply, 무정전전원공급장치)는 정전이 발생한 순간부터 예비 전원(배터리 등)으로 전환해 서버·통신장비가 갑자기 꺼지지 않도록 일정 시간 전원을 공급하는 설비다."
  optionExplanations={['정전 시에도 전원을 안정적으로 공급해 장비 중단을 막는다는 설명이 UPS의 정의와 정확히 일치한다.', '온습도 유지는 항온항습기의 역할이며 UPS의 주된 목적이 아니다.', '회선 이중화는 통신 회선 자체의 이중화 설계에 해당하며 전원 공급과는 다른 영역이다.', '데이터 암호화는 보안 설비의 역할이며 전원 공급 장치인 UPS의 목적과는 무관하다.']}
/>

<Quiz
  questionNumber={25}
  question="다음 설명에 해당하는 공격 기법은? 공격자가 자신의 출발지 주소를 실제와 다른 값으로 위조해 다른 시스템인 것처럼 속인다"
  options={['스니핑', '스푸핑', 'APT', 'DDoS']}
  correctAnswer={2}
  explanation="스푸핑(spoofing)은 IP 주소, MAC 주소, 이메일 발신자 등 출발지 정보를 위조해 자신을 다른 신뢰할 수 있는 대상으로 속이는 공격 기법이다."
  optionExplanations={['스니핑은 네트워크를 지나가는 패킷을 몰래 엿듣는 공격으로 주소 위조와는 다른 기법이다.', '출발지 주소를 위조해 신분을 속인다는 설명이 스푸핑의 정의와 정확히 일치한다.', 'APT(지능형 지속 위협)는 장기간에 걸쳐 은밀하게 침투하는 표적 공격을 가리키며 주소 위조 자체를 뜻하지 않는다.', 'DDoS는 다수의 시스템을 동원해 대상 서비스를 마비시키는 공격으로 주소 위조와는 목적이 다르다.']}
/>

## 참고 자료

- [계명대학교 게시판 - 정보통신기사 출제기준(필기·실기) PDF (2026.1.1.~2028.12.31. 적용)](https://cms.kmu.ac.kr/bbs/computer/763/172569/download.do)
- [킨즈(kinz.kr) - 정보통신기사 객관식 필기 기출문제 CBT 목록](https://www.kinz.kr/subject/7996)
