---
title: '32. 정보통신기사 필기 예상 모의고사 2회'
description: '변복조·전송성능(dB·SNR·채널용량·BER), 서브네팅·수퍼네팅, 법규 조항 적용까지 계산형 문제 비중을 강화해 5과목을 고르게 재구성한 문항 25개로 점검한다'
---

<Callout type="warning">
  **이 회차는 기출 그대로가 아니라 재구성 출제입니다.** 01~27편에서 다룬 개념을 바탕으로 숫자와 상황을 새로 만들었습니다. 1회차보다 계산 과정이 더 많이 필요하도록 구성했으니, 답을 맞혔더라도 해설의 중간 계산 과정을 반드시 손으로 따라가 봅니다.
</Callout>

## 출제 포인트

- 이 회차는 5과목에서 고르게 5문항씩 뽑되, **계산형 문제의 비중을 1회차보다 강화**했습니다. 특히 변복조·전송성능 계산(데시벨·신호대잡음비·채널용량·비트에러율), 서브네팅·수퍼네팅 계산, 가용도·저장용량 계산, 법규 조항을 실제 상황에 적용하는 문제를 늘렸습니다.
- 계산 문제는 답만 외우지 말고, 해설에 적힌 **중간 계산 단계**를 그대로 따라가며 왜 그 공식을 쓰는지 이해해야 합니다.
- 법규 조항 적용 문제는 조문을 그대로 묻지 않고 구체적인 상황을 제시하므로, "이 상황에 어느 조항의 취지가 적용되는가"를 먼저 판단하는 연습이 필요합니다.

## 마무리 복습

### 1과목: 정보전송일반 (1~5번)

<Quiz
  questionNumber={1}
  question="대역폭이 3킬로헤르츠이고 신호대잡음비(선형값)가 1000인 아날로그 전화 채널의 섀넌 채널용량은 약 얼마인가?"
  options={['약 30킬로비트매초', '약 3킬로비트매초', '약 300킬로비트매초', '약 9킬로비트매초']}
  correctAnswer={1}
  explanation="섀넌의 채널용량 공식은 대역폭에 밑이 2인 로그(1 더하기 신호대잡음비)를 곱한 값이다. 1000에 1을 더한 1001에 밑이 2인 로그를 취하면 약 9.97이 나오고, 여기에 대역폭 3000헤르츠를 곱하면 약 29910비트매초, 즉 약 30킬로비트매초가 된다."
  optionExplanations={['3000헤르츠에 약 9.97을 곱하면 약 30000비트매초, 즉 약 30킬로비트매초가 되어 정확한 계산 결과다.', '로그값을 곱하지 않고 대역폭만 그대로 답으로 낸 값으로 계산 과정이 빠진 오답이다.', '로그값을 100배로 잘못 곱했을 때 나올 수 있는 값이다.', '로그값을 3으로 잘못 어림했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={2}
  question="어떤 통신 회선의 신호대잡음비가 30데시벨일 때, 이를 선형값(비율)으로 바꾸면 신호전력은 잡음전력의 몇 배인가?"
  options={['1000배', '300배', '100배', '3000배']}
  correctAnswer={1}
  explanation="데시벨 값을 선형 비율로 바꾸려면 10을 밑으로 하는 지수로 되돌려야 한다. 30을 10으로 나누면 3이 되고, 10의 3제곱은 1000이므로 신호전력은 잡음전력의 1000배다."
  optionExplanations={['10의 3제곱인 1000배가 30데시벨에 대응하는 정확한 선형 비율이다.', '이 값은 지수 계산을 잘못 어림했을 때 나올 수 있는 오답이다.', '20데시벨(10의 2제곱)에 해당하는 값으로 이 문제의 30데시벨과는 다르다.', '이 값도 지수 계산을 잘못했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={3}
  question="잡음이 없는 채널에서 대역폭이 4킬로헤르츠이고 신호 준위(레벨)가 4단계인 디지털 신호를 보낼 때, 나이퀴스트 공식으로 구한 최대 데이터 전송률은?"
  options={['16킬로비트매초', '8킬로비트매초', '32킬로비트매초', '4킬로비트매초']}
  correctAnswer={1}
  explanation="나이퀴스트 최대 전송률 공식은 대역폭에 2를 곱하고 다시 밑이 2인 로그(신호 준위 수)를 곱한 값이다. 준위가 4단계이므로 밑이 2인 로그 4는 2이고, 4000헤르츠에 2를 곱한 8000에 다시 2를 곱하면 16000비트매초, 즉 16킬로비트매초가 된다."
  optionExplanations={['4000헤르츠에 2를 곱하고 다시 로그값 2를 곱하면 16000비트매초가 되어 정확한 계산 결과다.', '로그값을 곱하지 않고 2배만 계산했을 때 나올 수 있는 값이다.', '로그값을 4로 잘못 대입했을 때 나올 수 있는 값이다.', '2를 곱하는 단계를 빠뜨렸을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={4}
  question="동축케이블의 손실이 1킬로미터당 2데시벨이고 송신전력이 1와트일 때, 10킬로미터를 전송한 뒤 수신전력은 몇 밀리와트인가?"
  options={['10밀리와트', '100밀리와트', '1밀리와트', '500밀리와트']}
  correctAnswer={1}
  explanation="10킬로미터를 전송하면 총 손실은 1킬로미터당 2데시벨에 10을 곱한 20데시벨이다. 20데시벨을 선형 비율로 바꾸면 10의 2제곱인 100배이므로, 송신전력 1000밀리와트를 100으로 나누면 수신전력은 10밀리와트가 된다."
  optionExplanations={['1000밀리와트를 100으로 나누면 10밀리와트가 되어 정확한 계산 결과다.', '손실 배율을 10배로 잘못 계산했을 때 나올 수 있는 값이다.', '손실 배율을 1000배로 잘못 계산했을 때 나올 수 있는 값이다.', '손실을 절반으로만 계산했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={5}
  question="100만 비트를 전송했는데 그중 100비트에서 오류가 발생했다. 이 구간의 비트에러율(BER)은 얼마인가?"
  options={['0.0001', '0.01', '0.000001', '0.001']}
  correctAnswer={1}
  explanation="비트에러율은 오류가 발생한 비트 수를 전체 전송한 비트 수로 나눈 값이다. 100을 100만으로 나누면 0.0001이 된다."
  optionExplanations={['100을 100만으로 나눈 값이 정확히 0.0001이다.', '분모를 만(10000)으로 잘못 잡았을 때 나올 수 있는 값이다.', '분자와 분모의 자릿수를 잘못 맞췄을 때 나올 수 있는 값이다.', '분모를 10만으로 잘못 잡았을 때 나올 수 있는 값이다.']}
/>

### 2과목: 정보통신기기 (6~10번)

<Quiz
  questionNumber={6}
  question="한 신호요소(심볼)가 4비트를 표현하는 변조 방식에서 보오율이 2400보오일 때, 실제 비트 전송률은 얼마인가?"
  options={['9600비피에스', '2400비피에스', '4800비피에스', '19200비피에스']}
  correctAnswer={1}
  explanation="비트 전송률은 보오율에 한 신호요소가 표현하는 비트 수를 곱해서 구한다. 2400보오에 4비트를 곱하면 9600비피에스가 된다."
  optionExplanations={['2400에 4를 곱한 9600비피에스가 정확한 계산 결과다.', '심볼당 비트 수를 곱하지 않고 보오율을 그대로 답으로 낸 값이다.', '심볼당 비트 수를 2로 잘못 대입했을 때 나올 수 있는 값이다.', '심볼당 비트 수를 8로 잘못 대입했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={7}
  question="광가입자망에서 가입자 댁내에 설치되어 광신호를 최종적으로 전기신호로 변환하는 장치로 가장 적절한 것은?"
  options={['ONT', 'OLT', 'DSU', 'CSU']}
  correctAnswer={1}
  explanation="ONT(Optical Network Terminal, 광가입자망종단장치)는 가입자 댁내에 설치되어 국사의 OLT로부터 받은 광신호를 최종적으로 전기신호로 변환해 단말기기에 전달하는 장치다."
  optionExplanations={['가입자 댁내에서 광신호를 전기신호로 변환한다는 설명이 ONT의 정의와 정확히 일치한다.', 'OLT는 국사에 설치되는 장치이며 가입자 댁내 장치가 아니다.', 'DSU는 디지털 회선의 신호 형식을 변환하는 장치로 광신호 변환과는 다른 기능이다.', 'CSU는 디지털 회선을 종단하고 신호 품질을 관리하는 장치로 광신호 변환이 주된 기능이 아니다.']}
/>

<Quiz
  questionNumber={8}
  question="PoE(Power over Ethernet) 표준이 세대를 거듭하며 발전해 온 일반적인 경향으로 가장 적절한 것은?"
  options={['표준이 발전할수록 이더넷 케이블 한 가닥으로 공급할 수 있는 전력량이 늘어났다', '표준이 발전할수록 케이블을 통한 데이터 전송 기능이 사라졌다', '표준이 발전할수록 아날로그 신호만 전송할 수 있게 되었다', '표준이 발전할수록 전력 공급 기능이 완전히 폐지되었다']}
  correctAnswer={1}
  explanation="PoE 표준은 세대를 거듭하며 이더넷 케이블 한 가닥으로 공급할 수 있는 최대 전력량을 늘려 와, IP카메라 같은 저전력 장치뿐 아니라 더 많은 전력이 필요한 장치까지 지원할 수 있게 발전했다."
  optionExplanations={['공급 가능한 전력량이 늘어났다는 설명이 PoE 표준 발전의 실제 경향과 일치한다.', 'PoE는 여전히 데이터와 전력을 함께 전송하는 기술이므로 데이터 전송 기능이 사라졌다는 설명은 옳지 않다.', 'PoE는 디지털 이더넷 신호를 다루는 기술이며 아날로그 전용으로 바뀌지 않았다.', 'PoE의 핵심 기능이 전력 공급이므로 이 기능이 폐지되었다는 설명은 정의 자체와 모순된다.']}
/>

<Quiz
  questionNumber={9}
  question="VoLTE(Voice over LTE)에 대한 설명으로 가장 적절한 것은?"
  options={['음성 통화까지 회선교환을 거치지 않고 LTE의 IP 패킷망으로 전달하는 방식이다', '음성 통화만은 여전히 회선교환망을 거치도록 남겨 둔 방식이다', '데이터 통신에는 쓸 수 없고 음성 통화 전용으로만 설계된 회선교환 기술이다', '2G 이동통신에서 처음 도입된 음성 전달 방식이다']}
  correctAnswer={1}
  explanation="VoLTE는 4세대 이동통신인 LTE 환경에서 음성 통화까지 별도의 회선교환망을 거치지 않고 데이터와 마찬가지로 IP 패킷으로 전달하는 방식으로, 음성까지 전 구간을 IP화한 것이 핵심이다."
  optionExplanations={['음성까지 IP 패킷망으로 전달한다는 설명이 VoLTE의 정의와 정확히 일치한다.', 'VoLTE는 오히려 회선교환을 거치지 않는 것이 핵심이므로 이 설명은 반대다.', 'VoLTE는 음성 통화를 위한 방식이지만 IP 패킷망 자체는 음성과 데이터를 함께 다루는 망이다.', 'VoLTE는 2G가 아니라 4G LTE에서 도입된 방식이다.']}
/>

<Quiz
  questionNumber={10}
  question="IP 카메라 10대를 하나의 PoE 스위치에 연결하려 한다. 카메라 한 대가 최대 7와트를 소비할 때, 스위치가 감당해야 하는 최소 전력 용량은?"
  options={['70와트', '7와트', '700와트', '100와트']}
  correctAnswer={1}
  explanation="카메라 한 대의 최대 소비전력 7와트에 카메라 대수 10을 곱하면 70와트가 되며, 이는 스위치가 동시에 감당해야 할 최소 전력 용량이다."
  optionExplanations={['7와트에 10대를 곱한 70와트가 정확한 계산 결과다.', '카메라 한 대분의 전력만 계산해 대수를 곱하지 않은 값이다.', '자릿수를 잘못 곱했을 때 나올 수 있는 값이다.', '카메라 한 대당 소비전력을 10와트로 잘못 가정했을 때 나올 수 있는 값이다.']}
/>

### 3과목: 정보통신네트워크 (11~15번)

<Quiz
  questionNumber={11}
  question="10.10.0.0/23 대역을 VLSM으로 나누어 호스트 200대가 필요한 부서에 배정하려 한다. 이 부서에 배정할 수 있는 최소 프리픽스 길이는?"
  options={['/24', '/25', '/26', '/23']}
  correctAnswer={1}
  explanation="200대를 수용하려면 사용 가능한 호스트 수가 200 이상이어야 한다. 24비트 프리픽스는 호스트 비트가 8개 남아 2를 8번 곱한 256에서 2를 뺀 254개를 수용해 요구를 만족하는 가장 작은 블록이다. 25비트 프리픽스는 호스트 비트가 7개뿐이라 126개만 수용해 200대에 미달한다."
  optionExplanations={['호스트 비트 8개로 254개를 수용해 200대 요구를 만족하면서도 낭비가 가장 적은 최소 블록이다.', '호스트 비트 7개로는 126개만 수용되어 200대 요구를 만족하지 못한다.', '호스트 비트 6개로는 62개만 수용되어 200대 요구에 크게 미달한다.', '이 프리픽스는 원래 대역 전체를 그대로 두는 것으로 200대만을 위해 512개 주소 전체를 낭비하게 된다.']}
/>

<Quiz
  questionNumber={12}
  question="192.168.0.0/24, 192.168.1.0/24, 192.168.2.0/24, 192.168.3.0/24 네 개의 연속된 네트워크를 하나의 경로로 요약(수퍼네팅)할 때 가장 적절한 것은?"
  options={['192.168.0.0/22', '192.168.0.0/23', '192.168.0.0/21', '192.168.0.0/24']}
  correctAnswer={1}
  explanation="네 개의 네트워크를 하나로 묶으려면 2를 2번 곱해야 4가 되므로 프리픽스에서 2비트를 줄여야 한다. 24비트에서 2비트를 줄이면 22비트가 되어 192.168.0.0/22 하나로 네 대역을 모두 포함하는 경로 요약이 완성된다."
  optionExplanations={['24비트에서 2비트를 줄인 22비트 프리픽스가 네 개의 /24 대역을 정확히 하나로 묶는 결과다.', '이 프리픽스는 두 개의 /24 대역만 묶으므로 네 개를 모두 포함하지 못한다.', '이 프리픽스는 여덟 개의 /24 대역을 포함할 수 있어 필요한 것보다 더 넓은 범위까지 포함한다.', '이 프리픽스는 원래의 개별 대역 하나만을 가리키므로 요약이 전혀 이루어지지 않은 상태다.']}
/>

<Quiz
  questionNumber={13}
  question="서브넷 마스크 255.255.255.240에 대응하는 와일드카드 마스크는?"
  options={['0.0.0.15', '0.0.0.31', '0.0.0.7', '0.0.0.240']}
  correctAnswer={1}
  explanation="와일드카드 마스크는 서브넷 마스크의 각 자리를 255에서 뺀 값이다. 255에서 255를 세 번 빼면 0이 되고, 255에서 240을 빼면 15가 되므로 와일드카드 마스크는 0.0.0.15가 된다."
  optionExplanations={['255에서 240을 뺀 15가 마지막 옥텟의 정확한 와일드카드 값이다.', '이 값은 서브넷 마스크 255.255.255.224(프리픽스 27)에 대응하는 와일드카드다.', '이 값은 서브넷 마스크 255.255.255.248(프리픽스 29)에 대응하는 와일드카드다.', '서브넷 마스크 값을 그대로 옮겨 적은 것으로, 반전 계산을 하지 않은 오답이다.']}
/>

<Quiz
  questionNumber={14}
  question="트렁크(trunk) 포트에 대한 설명으로 가장 적절한 것은?"
  options={['여러 VLAN의 트래픽을 하나의 물리적 링크로 동시에 전달하며 프레임의 VLAN 태그로 소속을 구분한다', '하나의 VLAN에 속한 일반 사용자 단말만 연결하는 포트다', '스위치와 라우터 사이에서만 존재할 수 있는 특수한 케이블 종류를 가리킨다', '전원 공급 전용으로 사용되는 포트를 가리킨다']}
  correctAnswer={1}
  explanation="트렁크 포트는 여러 VLAN에 속한 트래픽을 하나의 물리적 링크로 함께 실어 나르며, 각 프레임에 삽입된 VLAN 태그로 어느 VLAN에 속하는지 구분해 처리하는 스위치 포트다."
  optionExplanations={['여러 VLAN 트래픽을 함께 전달하고 태그로 구분한다는 설명이 트렁크 포트의 정의와 정확히 일치한다.', '하나의 VLAN 사용자 단말만 연결하는 포트는 액세스(access) 포트에 대한 설명이다.', '트렁크는 케이블 종류가 아니라 포트의 동작 모드를 가리키는 개념이다.', '전원 공급 전용 포트는 PoE와 관련된 개념이며 트렁크 포트의 정의와는 무관하다.']}
/>

<Quiz
  questionNumber={15}
  question="관리거리(administrative distance)가 낮을수록 우선적으로 채택된다고 할 때, 정적경로(Static, 관리거리 1)·OSPF(관리거리 110)·RIP(관리거리 120) 세 가지 경로 정보가 같은 목적지에 대해 동시에 존재한다면 라우터가 우선 채택하는 것은?"
  options={['정적경로', 'OSPF', 'RIP', '셋 다 동일한 우선순위로 처리된다']}
  correctAnswer={1}
  explanation="관리거리는 값이 낮을수록 신뢰도가 높다고 보아 우선 채택된다. 정적경로의 관리거리 1은 OSPF의 110, RIP의 120보다 훨씬 낮으므로 세 경로가 동시에 존재하면 정적경로가 우선 채택된다."
  optionExplanations={['관리거리 1인 정적경로가 세 값 중 가장 낮아 우선 채택된다.', 'OSPF의 관리거리 110은 정적경로의 1보다 높아 우선순위에서 밀린다.', 'RIP의 관리거리 120은 셋 중 가장 높아 우선순위에서 가장 밀린다.', '관리거리 값이 서로 다르므로 동일한 우선순위로 처리된다는 설명은 옳지 않다.']}
/>

### 4과목: 정보시스템운용 (16~20번)

<Quiz
  questionNumber={16}
  question="1테라바이트 하드디스크 4개로 RAID5를 구성했을 때 사용 가능한 총 저장 용량은?"
  options={['3테라바이트', '4테라바이트', '2테라바이트', '1테라바이트']}
  correctAnswer={1}
  explanation="RAID5는 디스크 하나 분량의 용량을 패리티 저장에 사용하므로, 디스크 개수 4에서 1을 뺀 3에 디스크 한 개의 용량인 1테라바이트를 곱하면 사용 가능한 총 용량은 3테라바이트가 된다."
  optionExplanations={['디스크 개수에서 1을 뺀 3개 분량인 3테라바이트가 RAID5의 정확한 사용 가능 용량이다.', '디스크 4개 용량을 그대로 더한 값으로 패리티에 쓰이는 용량을 반영하지 않은 계산이다.', '이 값은 RAID10 등 절반만 사용하는 구성에서 나올 수 있는 값으로 RAID5의 계산 방식과 다르다.', '디스크 3개 분량을 패리티로 뺀 경우에 나올 수 있는 값으로 RAID5의 실제 계산과 다르다.']}
/>

<Quiz
  questionNumber={17}
  question="1테라바이트 하드디스크 4개로 RAID10을 구성했을 때 사용 가능한 총 저장 용량은?"
  options={['2테라바이트', '4테라바이트', '3테라바이트', '1테라바이트']}
  correctAnswer={1}
  explanation="RAID10은 디스크를 두 대씩 미러링(복사)한 뒤 그 쌍들을 다시 스트라이핑(분산)하는 구조이므로, 전체 디스크 개수의 절반만큼만 실제로 사용할 수 있다. 4개의 절반인 2에 디스크 한 개의 용량 1테라바이트를 곱하면 2테라바이트가 된다."
  optionExplanations={['디스크 개수의 절반인 2개 분량, 즉 2테라바이트가 RAID10의 정확한 사용 가능 용량이다.', '디스크 4개 용량을 그대로 더한 값으로 미러링으로 절반이 복사본에 쓰인다는 점을 반영하지 않았다.', '이 값은 RAID5 구성(디스크 4개 중 1개를 패리티로 사용)에서 나오는 용량이며 RAID10과는 계산 방식이 다르다.', '디스크 한 개 용량만 답으로 낸 값으로 전체 디스크 개수를 반영하지 않았다.']}
/>

<Quiz
  questionNumber={18}
  question="어떤 서버의 MTBF(평균고장간격)가 190시간, MTTR(평균수리시간)이 10시간이다. 이 서버의 가용도는 몇 퍼센트인가?"
  options={['95퍼센트', '90퍼센트', '99퍼센트', '80퍼센트']}
  correctAnswer={1}
  explanation="가용도는 MTBF를 MTBF와 MTTR의 합으로 나누어 구한다. 190을 190과 10을 더한 200으로 나누면 0.95, 즉 95퍼센트가 된다."
  optionExplanations={['190을 200으로 나눈 값을 백분율로 바꾸면 95퍼센트가 되어 정확한 계산 결과다.', 'MTTR을 실제보다 크게 어림했을 때 나올 수 있는 값이다.', 'MTTR을 무시하고 어림했을 때 나올 수 있는 값으로 실제 계산과 다르다.', '분모를 실제보다 크게 어림했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={19}
  question="어떤 서비스의 연간 가용성 목표가 99.9퍼센트(흔히 쓰리 나인이라 부르는 수준)일 때, 1년(8760시간) 동안 허용되는 최대 다운타임은 약 얼마인가?"
  options={['약 8.76시간', '약 87.6시간', '약 0.876시간', '약 17.52시간']}
  correctAnswer={1}
  explanation="가용성이 99.9퍼센트라는 것은 가동되지 못하는 비율이 0.1퍼센트, 즉 0.001이라는 뜻이다. 1년 전체 시간인 8760시간에 0.001을 곱하면 약 8.76시간이 허용 다운타임이 된다."
  optionExplanations={['8760시간에 0.001을 곱한 8.76시간이 정확한 계산 결과다.', '0.001이 아니라 0.01을 곱했을 때 나올 수 있는 값으로 자릿수를 잘못 잡은 오답이다.', '0.001이 아니라 0.0001을 곱했을 때 나올 수 있는 값이다.', '0.001의 두 배를 곱했을 때 나올 수 있는 값이다.']}
/>

<Quiz
  questionNumber={20}
  question="웹서버 2대가 각각 초당 500건을 처리할 수 있을 때, 로드밸런서로 두 서버에 트래픽을 균등하게 분산하면 전체 처리량은 초당 몇 건인가?"
  options={['1000건', '500건', '750건', '250건']}
  correctAnswer={1}
  explanation="서버 2대가 각각 초당 500건을 처리할 수 있고 로드밸런서가 트래픽을 두 서버에 나누어 동시에 처리하게 하므로, 전체 처리량은 두 서버의 처리량을 합한 초당 1000건이 된다."
  optionExplanations={['500건에 서버 2대를 곱한 1000건이 균등 분산 시의 전체 처리량이다.', '서버 한 대의 처리량만 답으로 낸 값으로 나머지 한 대의 처리량을 반영하지 않았다.', '두 서버의 처리량을 더하지 않고 평균과 다른 값을 임의로 어림한 오답이다.', '전체 처리량을 서버 대수로 다시 나눈 값으로 질문이 요구하는 전체 처리량이 아니다.']}
/>

### 5과목: 컴퓨터일반 및 정보설비기준 (21~25번)

<Quiz
  questionNumber={21}
  question="클래스 C 네트워크 하나(프리픽스 24)에서 사용 가능한 호스트 주소의 최대 개수는?"
  options={['254개', '256개', '126개', '510개']}
  correctAnswer={1}
  explanation="클래스 C는 프리픽스가 24이므로 호스트 비트는 8개다. 2를 8번 곱한 256가지 조합에서 네트워크 주소와 브로드캐스트 주소 2개를 빼면 사용 가능한 호스트는 254개가 된다."
  optionExplanations={['256에서 2를 뺀 254개가 정확한 사용 가능 호스트 수다.', '256은 네트워크·브로드캐스트 주소를 빼기 전의 전체 조합 수이며 실제 사용 가능한 호스트 수가 아니다.', '이 값은 프리픽스 25(호스트 비트 7개)일 때 나오는 호스트 수로 클래스 C 전체 대역과 맞지 않는다.', '이 값은 프리픽스 23(호스트 비트 9개)일 때 나오는 호스트 수로 클래스 C 전체 대역과 맞지 않는다.']}
/>

<Quiz
  questionNumber={22}
  question="이동통신사가 한 가입자에게 프리픽스 길이 48인 IPv6 대역을 할당했다. 이 대역 안에서 프리픽스 길이 64인 서브넷을 몇 개까지 만들 수 있는가?"
  options={['65536개', '256개', '4096개', '16개']}
  correctAnswer={1}
  explanation="48비트 대역을 64비트 서브넷으로 나누려면 64에서 48을 뺀 16비트만큼 서브넷을 구분하는 데 쓸 수 있다. 2를 16번 곱하면 65536이므로 최대 65536개의 서브넷을 만들 수 있다."
  optionExplanations={['64에서 48을 뺀 16비트로 2를 16번 곱한 65536개가 정확한 계산 결과다.', '2를 8번 곱한 값으로 비트 수를 잘못 계산했을 때 나올 수 있는 오답이다.', '2를 12번 곱한 값으로 비트 수를 잘못 계산했을 때 나올 수 있는 오답이다.', '2를 4번 곱한 값으로 비트 수를 크게 과소평가했을 때 나올 수 있는 오답이다.']}
/>

<Quiz
  questionNumber={23}
  question="정보통신공사업 등록을 하지 않은 개인 사업자 B가, 등록된 공사업자 A로부터 통신설비 공사의 일부를 하도급받아 시공했다. 정보통신공사업법에 따를 때 이 상황에 대한 설명으로 가장 적절한 것은?"
  options={['하도급을 받는 자도 등록된 공사업자여야 하므로 이 하도급은 위법하다', 'B가 등록되지 않았더라도 A가 등록되어 있으므로 전체 공사는 적법하다', '하도급 금액이 소액이면 등록 여부와 관계없이 항상 적법하다', '정보통신공사업법은 하도급받는 자의 등록 여부를 규율하지 않는다']}
  correctAnswer={1}
  explanation="정보통신공사업법은 부실 시공을 막기 위해 도급뿐 아니라 하도급을 받는 자도 등록된 정보통신공사업자일 것을 요구한다. 등록되지 않은 개인이 공사의 일부라도 하도급받아 시공하는 것은 이 요건을 어긴 위법한 상황이다."
  optionExplanations={['하도급받는 자도 등록된 공사업자여야 한다는 요건에 따라 이 하도급이 위법하다는 설명이 정확하다.', '원도급자가 등록되어 있다고 해서 등록되지 않은 하도급자의 시공까지 자동으로 적법해지지는 않는다.', '금액의 많고 적음이 등록 요건 자체를 면제해 주지는 않는다.', '정보통신공사업법은 하도급을 받는 자의 등록 여부도 규율 대상으로 삼고 있다.']}
/>

<Quiz
  questionNumber={24}
  question="한 공공기관이 청사 출입구에 방범과 시설 안전을 목적으로 고정형 영상정보처리기기를 설치하고, 설치 사실을 알리는 안내판을 함께 부착했다. 개인정보 보호법에 따를 때 이 조치에 대한 설명으로 가장 적절한 것은?"
  options={['허용된 목적 범위 안에서 설치하고 안내판 등 필요한 조치를 했으므로 적법하다', '공공기관은 어떤 목적이든 안내판 없이도 설치할 수 있다', '출입구는 사생활 침해 우려가 뚜렷한 장소이므로 원칙적으로 설치가 금지된다', '방범 목적의 설치는 금지되어 있어 이 조치는 위법하다']}
  correctAnswer={1}
  explanation="시설 안전과 방범은 개인정보 보호법이 고정형 영상정보처리기기 설치를 허용하는 목적에 해당하며, 안내판 부착 등 필요한 조치까지 함께 이행했으므로 이 설치는 적법하다."
  optionExplanations={['허용된 목적 범위와 안내판 등 필요한 조치 이행이 함께 충족되어 적법하다는 설명이 정확하다.', '공공기관이라도 설치 사실을 알리는 안내판 등 필요한 조치는 생략할 수 없다.', '출입구는 화장실·탈의실 같은 사생활 침해 우려가 뚜렷한 장소에 해당하지 않는다.', '방범(범죄 예방)은 법령이 허용하는 대표적인 설치 목적 중 하나이므로 금지된다는 설명은 옳지 않다.']}
/>

<Quiz
  questionNumber={25}
  question="정보통신공사의 실시설계가 완료된 직후, 시공에 들어가기 전에 발주자와 시공자가 우선적으로 처리해야 하는 절차로 가장 적절한 것은?"
  options={['착공신고', '준공검사', '설계도서 폐기', '감리원 배치 완료 보고만으로 절차를 종료']}
  correctAnswer={1}
  explanation="실시설계가 끝나면 그 설계도서를 바탕으로 공사를 시작한다는 사실을 관계 기관에 알리는 착공신고 절차를 거친 뒤에 실제 시공에 들어간다. 준공검사는 시공과 감리가 모두 끝난 다음 단계이므로 이 시점에 할 수 없다."
  optionExplanations={['설계 완료 직후 시공 전에 거쳐야 하는 절차는 착공신고이므로 이 설명이 정확하다.', '준공검사는 공사가 끝난 뒤에 이루어지는 절차이므로 시공을 시작하기도 전인 이 시점과는 맞지 않는다.', '설계도서는 시공 중과 감리, 준공검사 단계에서도 계속 참조해야 하므로 이 시점에 폐기할 수 없다.', '감리원 배치는 시공 과정을 관리·감독하기 위한 절차의 일부일 뿐 착공 전 신고 절차를 대신하지 않는다.']}
/>

## 참고 자료

- [계명대학교 게시판 - 정보통신기사 출제기준(필기·실기) PDF (2026.1.1.~2028.12.31. 적용)](https://cms.kmu.ac.kr/bbs/computer/763/172569/download.do)
- [한국방송통신전파진흥원(KCA) - 출제기준 자료실](https://www.cq.or.kr/qh_cusgm10_001.do)
- [킨즈(kinz.kr) - 정보통신기사 객관식 필기 기출문제 CBT 목록](https://www.kinz.kr/subject/7996)
